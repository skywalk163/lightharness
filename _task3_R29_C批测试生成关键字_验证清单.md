# 第29轮 任务3：C批（测试/生成+关键字 10条）CCW 逐条验证清单

日期：2026-09-15 ｜ 结论：**删除 7 条（CCW 37→30），保留 3 条真护栏**
验证脚本：`lightharness/_antirun_r29_t3_C批验证.py`（逐条 monkeypatch 隔离）
证据文件：`lightharness/_task3_R29_C批验证_证据.json`
删除后确认：全语料 839 文件对照 R26 基线 **token 零变化**（`_r29_c_verify_zero.py`）

---

## 一、逐条验证结果（三重判据：G1 全语料 850 文件 ∧ G2 编译门 ∧ G3 边界门）

### ✅ 可删 7 条（G1∧G2∧G3 全通过）

| 条目 | G1（撤后全语料） | G2（编译门） | G3（边界门） | 删除理由 |
|---|---|---|---|---|
| 生成分析 | PASS | PASS | PASS | 词法通用机制已覆盖：名字内无运算符/语句关键字成分 |
| 生成问候语 | PASS | PASS | PASS | 同上；`测试_生成问候语` 的整串保护独立于本条（见保留清单说明） |
| 最终答案 | PASS | PASS | PASS | 同上 |
| 当然 | PASS | PASS | PASS | 「当」字词首场景由既有 `当` 关键字上下文规则覆盖（非冒号/非空格后随→并入） |
| 当前 | PASS | PASS | PASS | 同上 |
| 终点 | PASS | PASS | PASS | 「终」非独立语句语义场景，通用机制覆盖 |
| 开启调试 | PASS | PASS | PASS | 「开启」无关键字成分，「调试」亦然 |

### ⛔ 保留 3 条（真护栏，G3 边界门失败）

| 条目 | G1 | G2 | G3 失败形态（撤后） | 保留理由 |
|---|---|---|---|---|
| 测试_生成问候语 | PASS | PASS | `返回 测试_生成问候语(1)` → 切成 `测试_` + `生成问候语` 两 token | ASCII 下划线+Han 粘连整词依赖 CCW 整串登记；语料 0 命中属防御性护栏（测试钉名） |
| 应当 | PASS | PASS | `如果 应当 那么…` → 切成 `应` + `当`（"当"是循环关键字） | "当"关键字在词首劈开复合名，无通用规则可替代（R29 时点） |
| 除非 | PASS | PASS | `返回 除非(1)` → 切成 `除` + `非`（"除"∈DUAL 运算符、"非"是逻辑关键字） | 双侧关键字夹击，撤掉即切分，无通用规则可替代 |

## 二、执行的 lexer.py 修改（仅 CCW 表定义 + 注释）

- 保留 A 区删除 4 条：`最终答案`、`开启调试`、`生成分析`、`生成问候语`；
- 保留 B 区删除 3 条：`当前`、`当然`、`终点`；
- 两区注释追加 R29 任务3 精简记录（判据+证据指针）；
- 保留条目 `应当`/`除非` 注释补 G3 失败形态说明。

**未触碰**：A批（位运算符+异步 9 条）、B批（错误类型+设置 10 条）、D批（其他 8 条）
均保持原样（归任务1/2/4 各路）。

## 三、删除后全量反跑（零回归确认）

```
语料 = 850 个 .light 文件（R28 新增测试后）
可比 839 文件（既有失败 2 排除：集合.light / _test_nested_closure.light；
              R29 本路未新增语料文件）
对照 R26 基线快照：逐文件 token 序列零变化 ✅
```

## 四、给任务5（合并验证）的输入

- C批删除并集 = {生成分析, 生成问候语, 最终答案, 当然, 当前, 终点, 开启调试}（7 条）；
- 保留并集 = {测试_生成问候语, 应当, 除非}；
- ⚠️ 合并注意：若 A/B/D 批删除了 `生成问候语` 以外的条目无交互；但
  `测试_生成问候语` 的 G3 依赖「CCW 中其余条目的存在形态不变」，合并后须重跑
  本脚本确认 G3 仍为基线口径（本批删除不影响它，因其依赖的是自身条目）。

## 五、铁律自检

| 铁律 | 自检 |
|---|---|
| 只改 C 批 10 条，不碰 A/B/D 批 | ✅ 仅删 7 条 C 批条目，其余 30 条原样 |
| 逐条隔离验证，不批量删除 | ✅ 10 条逐一 monkeypatch 撤除验证 |
| 可删条件 G1∧G2∧G3 全通过 | ✅ 删除 7 条三判据全 PASS；3 条 G3 失败即保留 |
| 删除后全量反跑零回归 | ✅ 839 文件对照 R26 基线 token 零变化 |
| 新增一律 .light，禁 Python 绕语言 | ✅ G2 探针均为临时 .light（验证后清理） |
