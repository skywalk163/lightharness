# 第36轮 任务2：light-merge test_lexer 3 条基线红修正

> 交付日期：2026-09-16 ｜ 修改范围：`light-merge/tests/test_lexer.py`（仅此一个文件）
> 结果：**10/10 通过**（修正前 7/10）｜ 未改 `lexer.py`｜ 未引入新测试缺口

---

## 一、结论一览

| # | 测试 | 方案 | 原输入 | 实测输出（修正前） | 重写后输入 |
|---|---|---|---|---|---|
| 1 | `test_basic_keywords` | **B 重写** | `设甲为三。` | `[IDENTIFIER 设甲为三, PERIOD]` | `设 甲 为 三。` |
| 2 | `test_multiple_keywords` | **B 重写** | `遍历列表映射筛选` | `[KEYWORD 遍历, IDENTIFIER 列表映射筛选]` | `遍历映射。` / `映射 筛选。` / `遍历 列表。` |
| 3 | `test_complex_expression` | **B 重写** | `设甲为三加五。` | `[IDENTIFIER 设甲为三加五, PERIOD]` | `设 甲 为 三 加 五。` |

三条全部选 **方案B（重写）**：原始意图仍然有价值且在规范写法下完全成立，
但原输入基于「旧版确定性切词」假设，已过时。

---

## 二、逐条修正

### 2.1 `test_basic_keywords` —— 方案 B

**原断言**：`tokens[0] == KEYWORD('设')`、`tokens[1] == IDENTIFIER('甲')`。
**原意图**：验证赋值关键字 `设`、`为` 被识别为关键字，操作数 `甲` 是标识符。

**根因（实测取证）**：`为` 属于 `_EMBED_MAX_MATCH_KEYWORDS`（`{'为','尝试','返回'}`）。
`lexer.py` L2292 的「任务1（L-084/L-092/L-137）：嵌入关键字最大匹配」分支规定——
`为`/`返回`/`尝试` 恒带空格使用，**黏在更长汉字游程里必为标识符的一部分**。
因此 `设甲为三` 这个无空格游程被整体并成一个 IDENTIFIER：

```
设甲为三。  ->  IDENTIFIER('设甲为三'), PERIOD('。')
设 甲 为 三。->  KEYWORD('设'), IDENTIFIER('甲'), KEYWORD('为'), CHINESE_NUM(3), PERIOD('。')
```

**为什么是测试过时而非实现回归**：`设甲为三` 不是光明赋值语句的规范写法
（规范写法在关键字与操作数之间带空格，与同文件 `test_no_space_separation`
对 `甲加1。` 的断言口径一致——`加` 这类中缀运算符在游程内仍被切出，
但 `为`/`返回`/`尝试` 这类「恒带空格」关键字不切）。

**重写内容**：
1. 主用例改为 `设 甲 为 三。`，保留原两条断言（`设`→KEYWORD、`甲`→IDENTIFIER）；
2. 补齐原用例漏掉的 `为`→KEYWORD 与 `三`→CHINESE_NUM(3) 两条断言；
3. **新增钉现状断言**：`设甲为三。` 必须整体成词 `["设甲为三", "。"]`，
   把当前行为固化，防止未来静默漂移。

### 2.2 `test_multiple_keywords` —— 方案 B

**原断言**：`keywords` 含 `遍历`、`映射`、`筛选`（同一用例里还注释了「`列表` 不是关键字」）。
**原意图**：连续关键字都能被切出；非关键字保持标识符。

**根因（实测取证）**：现词法只在**独立汉字游程的词首**切关键字。
`列表` 不在 `_ALL_KEYWORDS_WITH_VERBS`（实测 `列表 in ALL_KEYWORDS = False`），
于是从 `列表` 起的那整段游程整体成一个 IDENTIFIER，把 `映射`/`筛选` 吞在里面：

```
遍历列表映射筛选  ->  KEYWORD('遍历'), IDENTIFIER('列表映射筛选')
遍历映射。        ->  KEYWORD('遍历'), KEYWORD('映射'), PERIOD
映射 筛选。      ->  KEYWORD('映射'), KEYWORD('筛选'), PERIOD
遍历 列表。      ->  KEYWORD('遍历'), IDENTIFIER('列表'), PERIOD
```

**为什么是测试过时**：原用例把非关键字 `列表` 混进了「连续关键字」序列，
与本用例自己的注释自相矛盾；`映射`/`筛选` 是双字动词关键字，
只有在各自独立游程（词首）才会被切出，这是既定契约（同文件
`test_if_statement` 的 `如果 甲 大于 乙 那么 打印 甲。` 在规范写法下全绿）。

**重写内容**：拆成三段，三个原意图各钉一处——
1. `遍历映射。`：连续关键字对（原用例的核心意图）；
2. `映射 筛选。`：另一组连续关键字，覆盖原断言的 `筛选`；
3. `遍历 列表。`：钉住「`列表` 是标识符、不是关键字」这一原注释意图
   （同时断言 `列表 not in keywords`，双向钉死）。

### 2.3 `test_complex_expression` —— 方案 B

**原断言**：`keywords` 含 `设`、`为`、`加`。
**原意图**：一条赋值语句里同时出现赋值关键字与中缀运算符关键字，都能识别。

**根因**：与 2.1 同源——`设甲为三加五` 因 `为` 属嵌入关键字被整体并成一个 IDENTIFIER。
实测：

```
设甲为三加五。    ->  IDENTIFIER('设甲为三加五'), PERIOD
设 甲 为 三加五。 ->  设, 甲, 为, IDENTIFIER('三加五'), PERIOD      ← 注意：三加五 仍整体
设 甲 为 三 加 五。->  设, 甲, 为, CHINESE_NUM(3), 加, CHINESE_NUM(5), PERIOD
```

**重写内容**：
1. 主用例改为 `设 甲 为 三 加 五。`，三条原断言（`设`/`为`/`加`）全部保留；
2. 新增断言：中文数字值部分 `== [3, 5]`（原用例未验证值，补上）；
3. **新增钉现状断言**：`三加五` 粘连时整体成词、`加` 不被切出为关键字
   ——这条很重要：它说明「中缀运算符切开中文数字游程」只在带空格时成立，
   避免后来者误以为 `三加五` 应该切成 `三+加+五`。

---

## 三、为什么三条都不选 A 或 C

**为什么不选 A（把断言改成 IDENTIFIER）**：
A 会把「关键字识别」用例反向改成「关键字**不**识别」用例，测试意图被彻底反转，
读代码的人会以为项目主张 `设甲为三` 应该整体成词是设计目标。而事实是
`设甲为三。` 在解析层根本走不通（实测报「期望'函数'…但得到 标识符」），
不是可接受的合法输入。三条的原意图（关键字识别 / 连续关键字 / 复杂表达式）
在规范写法下都能 100% 验证，没有必要降级测试价值。

**为什么不选 C（移除）**：
任务书要求「移除必须确认意图已被其他测试覆盖」。逐条核对 `tests/test_lexer.py`
现有 10 条的覆盖面：

| 原意图 | 现有覆盖 |
|---|---|
| `设` 是赋值关键字、`甲` 是标识符 | 无（`test_keyword_priority` 测的是 `定义甲。`，`定义` 是双字关键字，口径不同） |
| 赋值关键字 `为` 被识别 | **完全无覆盖**（全部 10 条没有任何一条断言 `为`） |
| 连续关键字都能切出 | 无（`test_if_statement` 是单个游程内的多关键字） |
| 非关键字保持标识符 | 无 |
| 复杂表达式里 `设/为/加` 同时识别 | 无 |

三条都留了真实缺口，移除等于净减覆盖。故选 B 重写（保意图 + 修输入 + 额外钉现状）。

---

## 四、验证

1. **pytest**：`python -m pytest tests/test_lexer.py -v` → **10 passed**（0.71s），
   含被改的 3 条 + 原 7 条（含 L-010 回归 `test_l010_keyword_split_regression`）。
2. **文件自带入口**：`python tests/test_lexer.py` → `[OK] 所有测试通过！`
   （该入口用 `run_all_tests()` 顺序调用 10 条，与 pytest 结果一致。）
3. **改动范围**：`git diff tests/test_lexer.py` 只有 3 个 hunk（@18 / @115 / @186），
   一一对应 3 个被测函数；其余 7 条测试、模块 docstring、`run_all_tests()` 原样未动。
4. **铁律核对**：`lexer.py` 未被本任务修改。
   ⚠️ 注意：工作区 `src/lexer.py` 存在**他人并行改动**（R36 任务3 的
   `_OPERATOR_KEYWORDS_NO_UNARY_PREFIX` 预计算常量 + 自校验断言④），
   非本任务产物，本任务未触碰也未依赖。
5. **无新增缺口**：三条的 10 条原断言全部保留（`设`/`甲` 的 type+value、`遍历`/`映射`/`筛选`、
   `设`/`为`/`加`），另新增 12 条断言（`为`、`三` 的 type+value、粘连钉现状、
   `列表` 双向、值列表 `[3,5]`、`映射 筛选` 组合、`三加五` 粘连钉现状），净增覆盖。

---

## 五、顺手发现（仅记录，不在本任务范围）

1. `tests/test_lexer.py` 这 3 条红属同一类过时假设：**无空格粘连写法 + 旧版
   确定性切词期望**。该文件其余 7 条多用带空格或单游程写法，说明这 3 条是
   早期版本遗留、后续词法重构（嵌入关键字最大匹配 R20 修复）未同步更新。
   建议后续单独审计该文件其余用例的输入形态（本任务未扩展范围）。
2. `为`/`返回`/`尝试` 这个「恒带空格」三元组是 L-154 的同类现象（见
   `_task4_R36_L-154深度评估.md`）：这类关键字在游程内被并入标识符，
   既是 L-084/L-092/L-137 的修复成果，也是本次 3 条红的共同根因。
