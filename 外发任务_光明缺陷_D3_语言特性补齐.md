# 外发任务 D3：语言特性补齐（L-070 + L-071 + L-074）

## 任务目标

补齐光明语言三个特性：默认参数支持字典/列表字面量（L-070）、字典安全访问原语（L-071）、类型判断函数内置化（L-074）。

---

## 缺陷 L-070：默认参数不支持 `{}`/`[]` 字典列表字面量

### 现象

方法/段落定义中默认参数写 `配置元数据 = {}` 或 `选项 = []` 时报编译错误，只能用 `空` 代替，方法体内再判空（`如果 X == 空: 设 X 为 {}`）。这增加了样板代码，且与 Python 默认参数语法（`def f(x={})`）不一致。

### 最小复现

```光明
段落 测试 接收 选项 = {}:
  如果 选项 == 空:
    打印 "空"
  否则:
    打印 序列化JSON(选项)

测试({"a": 1})   # ← 当前：编译报错（{} 作为默认参数不支持）
测试()            # ← 期望：选项 = {}，输出 {}
```

### 期望能力

默认参数支持 `{}`/`[]` 等字面量（与 Python 一致）。注意 Python 可变默认参数的共享语义（`def f(x={})` 中同一个 dict 对象在多次调用间共享）——光明可选择：
- **方案A**：与 Python 一致（共享语义），文档化警告。
- **方案B**：每次调用时重新求值（`{}`/`[]` 每次新建），避免共享陷阱（更安全，但与 Python 不一致）。

建议方案B（每次新建），因为光明用户可能不熟悉 Python 的可变默认参数陷阱，且 lightharness 代码中默认参数多用于「可选配置字典」，每次新建更安全。

### 验收标准

- [ ] 最小复现用例 `examples/test_L070.light`：`{}`/`[]` 作为默认参数编译通过，调用时正确初始化。
- [ ] 多次调用无默认参数时，`{}`/`[]` 是独立对象（若选方案B）或共享对象（若选方案A），行为与决策一致。
- [ ] 现有用 `空` + 方法体内判空的绕法继续可用（向后兼容）。
- [ ] 全量回归不破坏。

---

## 缺陷 L-071：普通字典键访问不安全，无通用 `.get()` 安全访问原语

### 现象

`字典["键"]` 当键不存在时直接抛 `'键'`（KeyError），无返回 `空` 的安全访问方式。虽然 HTTP 请求体有 `载荷.获取("key", 默认值)`，但这是请求体专用方法，普通字典没有统一的安全访问原语。L-036 新增了 `取可选(字典, 键[, 缺省])` 一等助手，但 `取可选` 命名不直观（新手不知其存在），且与 `载荷.获取` 命名不一致。

### 最小复现

```光明
设 字典 为 {"a": 1}
打印 字典["b"]              # ← 当前：抛 'b'（KeyError）
打印 字典.获取("b", 空)     # ← 期望：返回 空（不抛异常）
打印 字典.获取("a", 0)      # ← 期望：返回 1（键存在时返回值）
```

### 期望能力

所有字典支持 `字典.获取("键", 默认值)` 安全访问（缺失键返回默认值而非抛异常），与 `载荷.获取` 同口径。默认值参数可选（缺省为 `空`）。

同时考虑：
- `列表.获取(索引, 默认值)` 是否一并支持（列表越界返回默认值）。
- 可选链语法 `字典?["键"]`（若实现成本低可一并考虑，否则仅 `.获取()` 即可）。

### 验收标准

- [ ] 最小复现用例 `examples/test_L071.light`：`字典.获取("不存在的键", 空)` 返回 `空`，`字典.获取("存在的键", 默认)` 返回实际值。
- [ ] `载荷.获取`（HTTP 请求体专用）行为不变，与 `字典.获取` 统一。
- [ ] L-036 新增的 `取可选` 继续可用（向后兼容，可文档化为 `字典.获取` 的别名）。
- [ ] 全量回归不破坏。

---

## 缺陷 L-074：类型判断函数 `是字典`/`是字符串`/`是列表` 非内置原语

### 现象

`是字典`/`是字符串`/`是列表` 不在光明内置原语中，需 `从 内置核心判型 导入 是字典, 是字符串, 是列表`。新手不知 `内置核心判型` 模块的存在，写 `类型(x) == "字典"` 不可靠（`类型` 返回 Python 类对象而非字符串），导致类型判断代码写错或绕路。

对比：`是整数` 已是自动内置（L-028 确认），但 `是字典`/`是列表`/`是字符串` 仍需导入，不一致。

### 最小复现

```光明
# 模块头部无任何导入
设 值 为 {"a": 1}
如果 是字典(值):        # ← 当前：name '是字典' is not defined（需导入）
  打印 "是字典"

如果 是整数(3):         # ← 是整数已内置，正常
  打印 "是整数"
```

### 期望能力

`是字典`/`是列表`/`是字符串` 作为内置原语直接可用（与 `是整数` 同口径），无需导入。同时考虑补齐其他常用类型判断：`是函数`/`是布尔`/`是数字`（若实现成本低可一并补齐，否则仅补齐上述三个最常用的）。

### 验收标准

- [ ] 最小复现用例 `examples/test_L074.light`：无导入时 `是字典`/`是列表`/`是字符串` 直接可用，返回正确判断。
- [ ] `从 内置核心判型 导入 ...` 的旧写法继续可用（向后兼容，导入同名函数不冲突）。
- [ ] `是整数` 行为不变。
- [ ] 全量回归不破坏。

---

## 交付物

1. 修复代码（编译器默认参数解析 / 字典内置方法 / code_generator 内置映射）
2. `examples/test_L070.light` + `examples/test_L071.light` + `examples/test_L074.light` 最小复现用例
3. 修复说明：根因 + 改动点 + 决策理由（尤其 L-070 可变默认参数语义、L-071 是否支持列表.获取）+ 验证结果（含全量回归结果）
4. 更新 `docs/功能对标/语言缺陷账.md` 中 L-070/L-071/L-074 的状态为「已修复」

## 注意事项

- L-070 默认参数字面量的解析在 `parser_stmt.py`（参数解析），需确认当前为何拒绝 `{}`/`[]`（可能是词法器在参数上下文中不识别字面量起始）。
- L-071 `字典.获取` 需在 code_generator 中为字典类型添加成员方法映射（类似 `列表获取` 的实现方式，参考 `stdlib/内置核心列表.light` 的 `列表获取`）。
- L-074 内置化在 `code_generator.py` / `code_generator_unified.py` 的 `builtin_map` 中登记（参考 `是整数` 的登记方式），同时 `stdlib/builtins.py` 加宿主转发。
- 三个缺陷的修复都涉及 code_generator 或 stdlib 内置，注意互不冲突（尤其 builtin_map 登记）。
