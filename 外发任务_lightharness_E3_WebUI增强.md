# 外发任务 E3：Web UI 增强

## 任务目标

增强 lightharness 的 Web UI，从"基础聊天界面"升级为"完整 agent 工作台"。实现会话侧边栏、工具调用卡片、消息历史渲染优化、模型状态展示。

参考 deepseek-harness `packages/client/ui-*`（ui-session/ui-chat/ui-tool/ui-sidebar/ui-trajectory）的设计，以及 `apps/web` 的整体布局。

## 核心设计

### 1. 整体布局

三栏布局（参考 deepseek-harness web app）：

```
┌──────────┬──────────────────────────────┬──────────┐
│ 侧边栏    │ 主聊天区                      │ 右侧面板  │
│ (240px)  │                              │ (可折叠)  │
│          │ ┌──────────────────────────┐ │          │
│ 会话列表  │ │ 顶栏：标题 + 模型 + 设置  │ │ 工具调用  │
│ + 新建    │ ├──────────────────────────┤ │ 历史/轨迹 │
│          │ │                          │ │          │
│ 会话项    │ │ 消息流                    │ │ (E4 审批  │
│ (标题/时间)│ │ (用户/助手/工具/系统)    │ │  队列也在 │
│          │ │                          │ │  这里)    │
│          │ │                          │ │          │
│          │ ├──────────────────────────┤ │          │
│          │ │ 输入区 + 发送/停止        │ │          │
│          │ └──────────────────────────┘ │          │
└──────────┴──────────────────────────────┴──────────┘
```

右侧面板默认折叠（窄屏），有工具调用时自动展开。

### 2. 会话侧边栏（`webui/sidebar.js` + CSS）

- **新建会话按钮**：顶部"+ 新建会话"，点击调用 POST /api/sessions，切换到新会话
- **会话列表**：从 GET /api/sessions 加载，按最后更新倒序
- **会话项**：显示标题（首行）+ 最后更新时间（次行，相对时间如"3分钟前"）+ 消息数（角标）
- **当前会话高亮**：当前会话项有左侧蓝色边框 + 深色背景
- **会话操作**：悬停显示删除按钮（×），点击删除（确认弹窗）
- **重命名**：双击会话标题可编辑（调用 PUT /api/sessions/:id/标题）
- **空状态**：无会话时显示"还没有会话，点击新建开始"

### 3. 消息渲染（`webui/messages.js` + CSS）

支持 5 种消息类型，每种有独立的渲染组件：

#### 用户消息
- 右侧对齐，浅色气泡，头像（用户图标）
- 内容：Markdown 渲染（支持代码块、列表、链接）

#### 助手消息
- 左侧对齐，深色气泡，头像（AI 图标）
- 内容：Markdown 渲染，代码块带复制按钮
- 流式渲染：SSE 逐字追加，光标闪烁

#### 工具调用消息（契约 1）
- 助手消息下方嵌入工具调用卡片
- 卡片头部：工具名（彩色标签）+ 参数摘要 + 耗时
- 卡片主体：可展开/折叠，展开显示完整参数（JSON 格式化）和结果
- 状态指示：执行中（旋转加载图标）→ 成功（绿色✓）→ 错误（红色✗）
- bash 工具特殊渲染：结果用终端样式（黑底绿字），支持 ANSI 颜色码

#### 工具结果消息
- 紧跟工具调用卡片，不单独显示（合并到工具调用卡片的结果区）

#### 系统消息
- 居中，灰色小字，如"会话已创建"、"配置已更新"、"工具调用轮次超限"

### 4. 顶栏增强

- **会话标题**：显示当前会话标题，可点击编辑
- **模型显示**：显示当前配置的模型名（从 GET /api/config 获取），未配置时显示"未配置模型"
- **连接状态**：绿色圆点（已连接）/ 红色（断开）/ 黄色（连接中）
- **设置按钮**：齿轮图标，打开配置面板（已有功能）
- **右侧面板切换**：面板图标，展开/折叠右侧工具调用历史

### 5. 输入区增强

- **多行输入**：Shift+Enter 换行，Enter 发送
- **停止按钮**：生成中显示红色停止按钮，点击中止（已有功能，优化样式）
- **附件按钮**：预留（本轮不实现文件上传，只放占位图标）
- **字数统计**：输入框右下角显示当前字数
- **快捷键提示**：输入框下方灰色小字"Enter 发送，Shift+Enter 换行"

### 6. 右侧面板：工具调用历史

- 时间线展示本轮所有工具调用
- 每项：工具名 + 状态图标 + 耗时
- 点击跳转到消息流中对应的工具调用卡片
- 统计：本轮工具调用次数、总耗时

## 交付物

1. `webui/index.html`（修改）— 三栏布局 + 右侧面板
2. `webui/style.css`（修改）— 侧边栏/工具卡片/消息类型样式
3. `webui/app.js`（修改）— 会话切换/消息渲染/工具事件处理
4. `webui/sidebar.js`（新建）— 会话侧边栏逻辑
5. `webui/messages.js`（新建）— 消息渲染组件（5种类型）
6. `webui/tool-card.js`（新建）— 工具调用卡片组件
7. 修复说明：改动点 + 验证结果（浏览器实测截图）

## 验收标准

- [ ] 三栏布局正常渲染，侧边栏 240px，主区自适应，右侧面板可折叠
- [ ] 新建会话：点击按钮创建新会话，切换到新会话，输入区清空
- [ ] 会话列表：显示所有会话，按最后更新倒序，当前会话高亮
- [ ] 删除会话：悬停显示删除按钮，确认后删除，列表更新
- [ ] 重命名会话：双击标题编辑，回车保存，调用 API 更新
- [ ] 会话切换：点击会话项加载历史消息，消息流完整渲染
- [ ] 用户消息：右侧对齐，Markdown 渲染正确
- [ ] 助手消息：左侧对齐，流式逐字渲染，代码块带复制按钮
- [ ] 工具调用卡片：显示工具名/参数/结果/耗时，可展开折叠，状态指示正确
- [ ] bash 工具结果：终端样式渲染，ANSI 颜色码正确显示
- [ ] 系统消息：居中灰色小字，不混入对话流
- [ ] 顶栏：显示会话标题/模型名/连接状态/设置按钮
- [ ] 输入区：多行输入（Shift+Enter），Enter 发送，字数统计
- [ ] 右侧面板：工具调用历史时间线，点击跳转
- [ ] 响应式：窄屏（<768px）侧边栏可收起，右侧面板默认折叠
- [ ] 全量回归不破坏（188 passed）

## 注意事项

- 与 E1 的工具事件格式（契约 1）严格对齐：SSE 流中 `种类:"工具调用"` 和 `种类:"工具结果"` 事件驱动工具卡片渲染
- 与 E2 的会话 API（契约 2）严格对齐：所有会话操作通过 REST API
- 现有配置面板/设置按钮/登录逻辑保持不变，只增强 UI
- Markdown 渲染：用轻量库（如 marked.js CDN）或自实现基础渲染（加粗/斜体/代码块/列表/链接），不引入重型依赖
- 代码块复制：用 navigator.clipboard.writeText，复制成功显示"已复制"提示
- 相对时间：实现 `相对时间(时间戳)` 函数，返回"刚刚/X分钟前/X小时前/昨天/日期"
- 工具卡片的参数格式化：JSON.stringify(params, null, 2)，长参数截断（>500 字符显示"..."+展开按钮）
- 流式渲染与工具调用的协调：助手消息流式输出中，如果遇到 tool_calls，先完成文本渲染，再显示工具调用卡片
- 深色主题：沿用现有深色主题，工具卡片用稍浅的背景色区分，工具名用彩色（bash=橙色、读文件=蓝色、写文件=绿色、搜索=紫色）
- 不引入构建工具：纯 HTML/CSS/JS，直接在浏览器运行，与现有 webui/ 目录结构一致
