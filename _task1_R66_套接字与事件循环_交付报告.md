# _task1_R66_套接字与事件循环_交付报告

> R66 硬骨头攻坚 · 原生 socket API + IO 事件循环基础（基础设施层）
> 规划文档：`R66硬骨头攻坚规划_socket事件循环沙箱FreeBSD-jail.md`
> 日期：2026-09-19 ｜ 本轮不动 light-merge 一行（遵守总纲）

## 交付物

| 文件 | 说明 |
|---|---|
| `stdlib/套接字.light` | 纯光明实现（首行魔数）：原生 socket 封装，~300 行 |
| `stdlib/事件循环.light` | 纯光明实现：selectors IO 事件循环，~310 行 |
| `examples/test_套接字.light` | 9 组回归，全绿 |
| `examples/test_事件循环.light` | 9 组回归，全绿 |
| `examples/test_套接字事件循环.light` | 4 组集成回归，全绿 |
| `docs/功能对标/对标清单.json` | 追加 #205~#207（编号顺延说明见下） |
| `docs/功能对标/行为差异清单.md` | 追加 R66-D 段（Windows errno 方言 + libuv 语义映射） |

## 66.1 套接字.light（对标 #205）

- 套接字 类：TCP/UDP 创建/连接/绑定/监听/接受/收发/关闭；`连接限时` 做临时超时且失败恢复原超时
- 地址族：AF_INET / AF_INET6 恒有；AF_UNIX 仅 POSIX（Windows 上为 空，常量层不炸）
- 选项：SO_REUSEADDR / TCP_NODELAY / SO_RCVBUF / SO_SNDBUF 设置+读回
- 三模式：阻塞 / 超时 / 非阻塞；字节层收发（与 进程树.light 采集器同口径，不做字符层）
- `发送全部` 循环发送 + 0 字节停滞检测；200KB 大载荷实测走多轮 send
- `新建套接字对(AF_INET, 非)` 为事件循环唤醒通道专用（Windows select 只认 socket）
- **不做**（明确边界）：HTTP（网络请求.py 已有）、TLS（移交后续）、字符解码
- 不吞异常：愿等（EAGAIN/EWOULDBLOCK）分类交给 选择器.light 的 是否愿等读/写

## 66.2 事件循环.light（对标 #206）

- 同步回调式（光明无协程语法 = L-归档-02，设计决策非绕法）；复用 选择器.light 不重复封装
- 注册读/写（回调签名 `回调(fd)`）+ 独立注销；`更新注册` 维护 fd→掩码表，**首次注册绝不误 unregister**（开发中实测踩过：无状态表时首注册必炸 KeyError，已修）
- 运行一次(超时毫秒) → 真/假；运行持续 / 停止（50ms 心跳封顶，停止后最多半个心跳退出）
- 延时回调：heapq 最小堆与 select 超时联动；取消定时器幂等（已取消句柄返回 假——开发中实测由反跑揪出，已修）
- 回调异常隔离：错误表 + stderr 双通道，隔离不沉默
- 立即调度：queue.Queue + socketpair 非阻塞唤醒，任意线程可投递
- 掩码等值三分支派发（READ=1/WRITE=2/和=3），规避方言位运算限制（与 选择器.light 同款约束）
- 派发顺序冻结：待执行队列 → IO 事件 → 到期定时器

## 66.3 集成测试（对标 #207）

非阻塞 TCP echo 服务器完整链路：accept → 注册读 → 待发队列 → 尝试直发 → 发不完挂写事件 → 写腿排空 → 对端 EOF 拆链。3 客户端线程并发，全部回显一致；保险定时器触发前被取消（定时器清理）；立即调度收尾 + 停止 + 关闭幂等。

## 验证

```
cd lightharness
python 运行.py examples/test_套接字.light          # exit=0
python 运行.py examples/test_事件循环.light        # exit=0
python 运行.py examples/test_套接字事件循环.light   # exit=0
```

- 反跑判据三条全部验证立红（对标清单 反跑判据 字段有逐条说明）：
  - #205：回环断言 ping-001 → ping-002 必红
  - #206：定时器期望序列反转必红
  - #207：回显完成数 3 → 4 必红
- 新文件零旧式「接收」语法（R65 括号式方向），测试链路上剩余 DeprecationWarning 全部为既有 stdlib 存量（选择器.light 等），本轮零新增。
- 临时探针/反跑文件已全部清理。

## 编号说明

规划文档写「对标清单追加 #203-#205」，但清单现状 #203（R62）、#204（R65）已被占用，按顺延原则取 **#205~#207**，已在清单 说明 段登记此差异。

## 缺陷与遗留

- 开发过程中发现并修复（均为新模块自身，不留档）：更新注册 首注册误 unregister；取消定时器 幂等语义。
- 无新增语言缺陷：全部语法特性（括号式段落、类/新建、kwargs 调用、heapq/queue/threading 直调）在现有光明上可表达。
- L-归档-03（stdlib 无原生 socket）随本轮落地，可考虑在缺陷账标注修复状态。
- **遗留到 R68**：套接字/事件循环 的 FreeBSD 交叉验证（规划 68.3）；TLS 层移交后续。
- **遗留到 R69**：异步子进程 / 异步PTY 在事件循环原语上升级。
