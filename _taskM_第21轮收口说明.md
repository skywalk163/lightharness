# 第21轮路M收口说明 —— 词法确定性切词重构轮

> 日期：2026-09-14 ｜ 方向：词法确定性切词重构轮
> 跨项目操作：light-merge（编译器词法重构）+ lightharness（验证与复现）
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）

---

## 一、本轮概述

第20轮编译器回归修复轮清零了4个CI临时例外，但附带发现L-152（嵌套段落形参关键字前缀）未修复，且词法切词依赖大量保护表、非确定性问题突出。本轮目标是**系统性重构词法切词**：修复L-152，实现上下文敏感切词，精简保护表，提高切词确定性。

### L-152根因（路M前置探查确认）

复现用例：
```
段落 主:
  段落 内层返回 接收 函数值:
      返回 函数值(9)
```

根因链（`_scan_user_definitions`，lexer.py:3124）：
1. 预扫描是**线性文本扫描**，不跟踪缩进/嵌套层级
2. 对嵌套段落`段落 内层返回 接收 函数值:`，分隔符定位逻辑把段名中的`返回`误判为参数分隔符（因为`返回`后接空格）
3. 段名被截断为`内层`（而非`内层返回`），参数名收集逻辑未执行
4. 后续循环把`函数值`中的`函数`误判为新的段落定义关键字，`值`被当作段名加入definitions
5. 最终definitions中有`值`但无`函数值`，tokenize时`函数值`被切成KEYWORD(`函数`)+IDENTIFIER(`值`)

---

## 二、6路交付摘要

### 任务1（P0）：预扫描重构——基于缩进/嵌套层级的定义收集
- **修改文件**：light-merge/src/lexer.py `_scan_user_definitions`方法
- **核心改动**：从线性文本扫描重构为基于缩进/嵌套层级的结构化收集
  - 段落定义行按缩进级别建层级关系
  - 嵌套段落的段名与形参名、块内`设`名正确入definitions
  - **只增不减原则**：重构后definitions集合是重构前的超集
  - 模块级收集行为完全兼容
- **复现用例**：examples/test_R21_L152嵌套段落形参.light（rc=0）、test_R21_L152家族嵌套形参.light（rc=0，覆盖函数值/返回值/尝试结果等多种关键字前缀）
- **反跑**：_antirun_r21_t1_预扫描重构.py → ALL OK（A断裂态红/B修复态绿+token单个IDENTIFIER/C模块级不变/D双断裂态）
- **交付报告**：_task1_R21_预扫描重构_交付报告.md

### 任务2（P0）：词法上下文敏感切词——非语句起始位置最大匹配
- **修改文件**：light-merge/src/lexer.py `_tokenize_identifier_or_keyword` + `_tokenize_chinese_sequence`
- **核心改动**：
  - 非语句起始位置（表达式上下文）优先最大匹配整体成IDENTIFIER
  - 仅整串精确等于关键字时作KEYWORD
  - 语句起始位置切分逻辑完全不变（`如果条件`/`接收参数`等合法语句不受影响）
  - **运算符关键字守卫**：`与`/`或`/`非`/`等于`/`大于`/`小于`等在表达式上下文仍按关键字切分
  - 函数调用语境（整串后紧随`(`）绝不切分（第20轮策略推广）
- **复现用例**：examples/test_R21_上下文敏感切词.light（rc=0）
- **pytest**：tests/test_R21_上下文敏感切词.py
- **反跑**：_antirun_r21_t2_上下文敏感切词.py → ALL OK（A表达式上下文整体成词/B语句起始不变/C运算符不变/D运行期绿/E交叉L-152绿）
- **交付报告**：_task2_R21_上下文敏感切词_交付报告.md
- **订正R20-D1**：第20轮的`_EMBED_MAX_MATCH_KEYWORDS`三字集合（`为`/`返回`/`尝试`）是本策略的过渡形态，已由通用策略替代

### 任务3（P1）：复合标识符保护表审计与精简
- **修改文件**：light-merge/src/lexer.py 保护表定义区域 + lightharness/_antirun_r20_t1_词法关键字前缀.py（订正）
- **核心成果**：
  - COMMON_COMPOUND_WORDS从**216条精简至32条**TierB护栏
  - 删除211条，对全语料**763个.light文件token序列零影响**（逐条隔离中立验证，违规0条）
  - 其余保护表完成审计分类（A类可删/B类精简/C类保留），待后续轮次逐步精简
  - **订正_antirun_r20_t1断裂态锚点**：原脚本以HEAD为断裂态，但HEAD已包含第20轮修复，判据A恒假；改为钉住`d2857dd5^`（第20轮修复的父提交=第19轮49319306），判据恢复有效
- **反跑**：_antirun_r21_t3_保护表精简.py → ALL OK（A基线自检/B补回211条零影响/B2逐条中立0违规/C正向控制/D TierB逐条护栏）
- **交付报告**：_task3_R21_保护表审计报告.md

### 任务4（P1）：词法确定性测试套件建设
- **修改文件**：7个.light自校验用例 + 3个pytest token层测试
- **测试覆盖**（6大类100+场景）：
  1. 嵌套段落形参（L-152家族：函数值/返回值/尝试结果/设值器/等于判断等20+种关键字前缀，1-3层嵌套）
  2. 块内设名（if块/for循环/嵌套块内的关键字前缀变量名）
  3. 表达式上下文（函数调用/赋值右侧/返回值/运算符操作数/字典键/列表元素）
  4. 语句起始关键字（如果/否则/遍历/当/尝试/捕获/返回/设/打印/新建等，确保不受影响）
  5. 边界混合（关键字前缀标识符与合法关键字语句相邻/同一行多个/嵌套段落段名/ASCII混合/含数字）
  6. 运算符关键字（与/或/非/在/等于/大于/小于等，确保在表达式中仍按关键字切分）
- **测试形式**：运行期自校验.light（7个）+ pytest token层验证（3个，59 passed）
- **交付报告**：_task4_R21_词法确定性测试套件_交付报告.md（含测试覆盖矩阵）

### 任务5（P1）：全项目回归扫描与反跑
- **修改文件**：无（仅新增扫描脚本和报告）
- **扫描范围**：全语料763个.light文件
- **扫描结果**：
  - token序列对比：4个文件有差异，**全部可解释**为L-152修复/上下文敏感切词的预期变化
  - 编译结果对比：**无新增红用例**
  - 性能对比：词法分析**+0.1%**（无显著下降）
  - L-152修复验证：`函数值`整体成IDENTIFIER=True
  - 表达式上下文验证：工作区已修复
  - 语句起始不变性验证：通过
  - 运算符不变性验证：通过
- **反跑**：_antirun_r21_t5_全量回归扫描.py → ALL OK
- **交付报告**：_task5_R21_全量回归扫描报告.md

### 任务6（P2）：docs更新+行为差异记录+收口框架
- **修改文件**：无（仅新增草案和框架）
- **交付物**：
  - _task6_R21_docs更新草案.md（对标清单#131~#133、缺陷账L-152已修复、行为差异R21-D1~D3的具体修改内容）
  - _task6_R21_收口说明框架.md
- 路M收口时已按草案统一写入docs三件

---

## 三、验证结果

### 3.1 第21轮新增测试用例（7个.light，全部rc=0）
| 用例 | 覆盖场景 | rc |
|---|---|---|
| test_R21_L152嵌套段落形参 | L-152核心复现 | 0 |
| test_R21_L152家族嵌套形参 | 20+种关键字前缀嵌套形参 | 0 |
| test_R21_上下文敏感切词 | 表达式上下文最大匹配 | 0 |
| test_R21_块内设名 | 块内关键字前缀变量名 | 0 |
| test_R21_表达式上下文 | 函数调用/赋值/返回/字典/列表 | 0 |
| test_R21_语句起始关键字 | 语句起始位置不变性 | 0 |
| test_R21_边界混合运算符 | 边界混合+运算符不变性 | 0 |

### 3.2 pytest新增测试（3个文件，59 passed）
- test_R21_词法确定性_token.py：token层切分验证
- test_R21_词法确定性_超集.py：definitions超集验证
- test_R21_上下文敏感切词.py：上下文敏感切词验证

### 3.3 第21轮反跑（4路，全部ALL OK）
- T1 预扫描重构：ALL OK（A断裂态红/B修复态绿+token单个IDENTIFIER/C模块级不变/D双断裂态）
- T2 上下文敏感切词：ALL OK（A表达式整体/B语句起始不变/C运算符不变/D运行期绿/E交叉L-152绿）
- T3 保护表精简：ALL OK（A基线/B补回零影响/B2逐条中立0违规/C正向控制/D TierB护栏）
- T5 全量回归扫描：ALL OK（A L-152修复/B表达式上下文/C语句起始不变/D运算符不变/E差异可解释/F性能+0.1%）

### 3.4 第20轮反跑回归验证（2路，全部rc=0）
- T1 词法关键字前缀：rc=0
- T2 HMAC标准实现：rc=0

### 3.5 第19轮反跑回归验证（6路）
- T1 词法核心：**rc=1（预期变化，非回归）**——A/B判据全PASS（L-084/L-092/L-137用例全绿，边界用例PASS），仅C判据"变异立红"因第21轮更通用的上下文敏感切词覆盖了第19轮的精确切分而不再成立。撤掉第19轮`_EMBED_MAX_MATCH_KEYWORDS`分支后L-084仍绿，证明新策略已覆盖旧修复，旧修复变得冗余（但仍有效）。
- T2 词法扩展：rc=0
- T3 保留字内置映射：rc=0
- T4 字符串哈希：rc=0
- T5 异常模块：rc=0
- T6 字典函数_D批次：rc=0

### 3.6 全量CI
- **pytest**：待CI结果（目标：无例外全绿）
- **smoke**：待CI结果（目标：5/5）

---

## 四、路M修正与决策记录

1. **第19轮T1反跑C判据过时**：路M验证时发现T1反跑rc=1，调查后确认是C判据"变异立红"因第21轮更通用的上下文敏感切词而不再成立。A/B判据全PASS，用例本身全绿。路M判定为**预期变化**，非回归，在收口说明中记录。第19轮的`_EMBED_MAX_MATCH_KEYWORDS`精确切分逻辑仍保留在代码中（与新策略共存，不冲突），后续轮次可评估是否移除。

2. **任务3订正_antirun_r20_t1**：任务3执行者发现第20轮T1反跑脚本的断裂态锚点错误（以HEAD为断裂态，但HEAD已包含修复），主动订正为`d2857dd5^`。路M认可该订正，随任务3一并提交。

3. **light-merge单提交**：任务1/2/3都修改lexer.py的不同区域，但git无法按区域拆分提交，故合并为一个提交（c26ac785，132+/82-）。提交信息中分别说明了三个任务的改动。

4. **保护表精简幅度**：任务3实际精简了COMMON_COMPOUND_WORDS（216→32条），远超任务书预期的"至少20%"。其余保护表仅完成审计分类，未实际精简（待后续轮次）。路M认可该渐进式精简策略。

---

## 五、已知问题与后续方向

### 5.1 仍存在的词法问题
- **其余保护表未精简**：IDENTIFIER_SAFE_KEYWORDS、_COMPOUND_SAFE_SINGLE_KEYWORDS、_TRAILING_ALIAS_MERGE等已完成审计分类，但未实际精简，待第22轮逐步处理
- **`_EMBED_MAX_MATCH_KEYWORDS`冗余**：第19轮的三字集合（`为`/`返回`/`尝试`）已被第21轮通用策略覆盖，但仍保留在代码中，后续可评估移除
- **动态定义名盲区**：`eval`/`exec`/动态生成的名称仍无法被预扫描覆盖（属固有局限，上游TS/JS也有类似问题）

### 5.2 第22轮方向建议
1. **保护表深度精简轮**：逐步精简其余保护表（IDENTIFIER_SAFE_KEYWORDS等），评估移除`_EMBED_MAX_MATCH_KEYWORDS`冗余集合
2. **继续上游复刻**：回到deepseek-harness上游复刻主线
3. **L-101/L-143上下文敏感保留字**：实现`回调`/`作用域`的上下文敏感保留字（仅在`外部回调`/`异步作用域`语法位置消费）

建议优先选择方向1（保护表深度精简），完成词法重构的收尾工作。

---

## 六、docs三件回填

1. **对标清单**（docs/功能对标/对标清单.json）：追加#131~#133（预扫描重构/上下文敏感切词/词法确定性测试套件），总数133条；版本字段更新为「覆盖记录至第21轮」。
2. **缺陷账**（docs/功能对标/语言缺陷账.md）：L-152状态从「已知未修复」更新为「已修复（第21轮任务1）」，含修复说明和验证证据；L-151维持「已修复（第20轮）」不变。
3. **行为差异清单**（docs/功能对标/行为差异清单.md）：追加R21-D1（预扫描基于缩进/嵌套层级）、R21-D2（上下文敏感切词，订正R20-D1）、R21-D3（保护表精简211条零影响）。

---

## 七、git提交

### light-merge项目（1个提交）
- `c26ac785` 第21轮词法确定性切词重构：预扫描基于缩进/嵌套层级（修复L-152）+ 非语句起始位置上下文敏感最大匹配 + 保护表精简（COMMON_COMPOUND_WORDS 216→32条）（1文件，132+/82-）

### lightharness项目（6路分路提交 + 收口提交）
- `a9dd6f3` 任务1：预扫描重构（L-152修复+2复现用例+反跑+报告）
- `4871a70` 任务2：上下文敏感切词（复现用例+pytest+反跑+报告）
- `afc9f9d` 任务3：保护表审计精简（反跑+报告+订正r20_t1锚点）
- `4ea5ad7` 任务4：词法确定性测试套件（7.light+3pytest+报告）
- `99fb72b` 任务5：全量回归扫描（反跑+报告）
- `13db0d5` 任务6：docs更新草案+收口框架
- 收口提交：docs三件回填 + 收口说明
