# 任务6 交付报告：标识符与词法安全工具库（src/词法安全.light）

> 任务来源：`G:\dswork\duan-light-merge\复刻_第18轮_任务prompt分发_缺陷修复轮 (1).md` 任务6
> 日期：2026-09-13 ｜ 对标 #121（光明语言工具库·缺陷修复轮）
> 命名预检查：src/ 下无 词法安全.light 同名模块 ✓

---

## 一、函数清单与修复层级（含设计决策）

| 函数 | 修复对象 | 层级 | 说明 |
|---|---|---|---|
| 保留字表() | L-119 | **A级** | 数据源：L-119 单字别名 49 字实证表逐字展开 + 历轮多字关键字合并去重 |
| 是保留字(标识符) | L-084/L-092/L-101/L-119/L-137/L-143 | **A级** | 子串匹配；返回 {是保留字, 匹配的保留字, 位置} |
| 标识符可切分检查(标识符) | L-120 | B级 | 贪婪按长度降序剥离保留字前缀；完全覆盖且步数≥2 才判可切分（单保留字整体匹配归 是保留字 范畴） |
| 安全标识符(候选名, 备选名) | A级配套 | B级 | 候选安全则原样；不安全返回备选；备选空（是空() 或 ==""）返回 候选_安全 |
| 标识符规范化(名称) | B级 | B级 | 按长度降序移除保留字子串；结果空则原名+_安全（如 "返回"→"返回_安全"） |
| 是内置映射名(标识符) | L-087 | B级 | builtin_map 实证子集 45 名 |
| 变量名安全检查(名称) | 综合 | B级 | 保留字子串+可切分+内置映射+空名+数字开头；返回 {安全, 问题列表, 建议} |
| 生成安全变量名(前缀, 上下文) | B级 | B级 | 模块级 生成序号表 计数器（按上下文键持久递增），保证两次调用结果不同且非保留字形态 |
| 模块名安全检查(模块名) | L-094 配套 | B级 | 既有模块/测试名静态已知清单冲突检查 |
| 绕法指南(缺陷编号) | L-084/086/087/089/090/091/093/094/095/097/098 | **C级** | 10 个编译器层面缺陷的标准绕法查询；未知编号返回 空 |

设计决策：
- （R18-D6）单字别名 49 字串是「数据实证」而非行为复刻：保留字表以 L-119 第17轮任务书全表为唯一单字来源，多字关键字以历轮编译报错实证为准；本模块自身标识符已按本模块规则自检（避开保留字字样与单字别名切分）。
- 可切分检查为静态保守近似（L-120 实际词法行为以编译器为准），对「可完全切分」宁可误报不漏报。

## 二、测试验证

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"; python 运行.py examples/test_词法安全.light
test_词法安全 PASS   （rc=0）
```

10 组 ≥40 断言：保留字表（规模/含 为 返回 回调 作用域 尝试/元素全字符串）、是保留字（行为→为/配置→配/纯保留字/子串/安全名判假/空串判假）、可切分检查（设为/返回为 可切分，存储核心/单保留字 不可切分）、安全标识符（原样/备选/自动后缀）、规范化（移除 为/安全名不变/全保留字退化后缀）、内置映射名、变量名安全检查（通过/保留字/数字开头）、生成安全变量名（含前缀/两次不同/非保留字）、模块名冲突、绕法指南（L-084/L-095/L-098/未知编号）。

## 三、反跑结果（3/3 ALL OK）

`lightharness/_antirun_t6_词法安全.py`（BASE 自定位；try/finally 恢复）：

```
PASS C=变异 单字别名串移除 配 字 -> 是保留字(配置) 断言立红 -> 红 (变异后运行 rc=1（期望非 0，真红）)
PASS 字节级恢复校验 (sha256 bdebb875f440)
PASS A/B 判据（保留字检查 + 可切分/生成名持久）回归绿 (rc=0)
ALL OK
```

C 变异：单字别名串移除 `配` 字 → 保留字表缺 `配` → `是保留字("配置")` 判假断言立红，恢复后绿。
（C 锚点选型说明：原定锚点移除 `为` 字无效——`为` 同时在多字关键字表中兜底，测试仍绿；故改选仅存在于 L-119 单字表的 `配` 字，并在测试中补 `是保留字("配置")` 专条断言。）

## 四、编译器层面缺陷绕法指南（绕法指南() 内置，10 条）

| 缺陷 | 绕法 |
|---|---|
| L-084 | 为 字切碎：标识符避开 为 字；用 等于/赋值 语义词替代 |
| L-086 | 正则花括号量词被破坏：模式经运行时字符串构造（字符串增强.拼接量词），或字符类展开替代量词 |
| L-087 | 当前目录 内置映射遮蔽：不用 当前目录 作变量名，改用 工作目录/当前路径 |
| L-089 | 字符串花括号插值：字符串增强.构造JSON字符串 构造 JSON 文本，或字符串拼接替代字面量 |
| L-090 | 字符串原语导入失败：不导入字符串模块，直接用编译器内置裸名 |
| L-091 | 匿名段落跨行内联限制：先定义命名段落再作实参传递 |
| L-093 | 跨模块异常位置锚定错误：依赖错误 message 而非位置信息 |
| L-094 | 入口目录遮蔽：测试文件名不与 src 模块同名；必要时显式路径导入 |
| L-095 | HMAC_SHA256 非标准：自洽链路内使用；对外对拍必须宿主面标准 hmac 校验 |
| L-097 | if 块内嵌套段落提升：段落定义放模块级或函数体顶层 |
| L-098 | 闭包修改外层变量不生效：用字典属性修改（状态[键]）或 函数增强.闭包包装 |

## 五、语言差异（本轮新缺陷登记）

- 无新缺陷（L-160 编号空缺，后续顺延）。本轮发现的过程性问题：反跑 C 锚点 `为` 字双源兜底（单字串+多字表各含一份），属数据冗余而非语言缺陷，已通过锚点改选解决并记录于第三节。
- 测试自身踩 L-119 同族（`参数为取` 含 为 字）按既有绕法保留作正例断言。

## 六、移交清单

- `lightharness/src/词法安全.light`、`examples/test_词法安全.light`（PASS rc=0）、`lightharness/_antirun_t6_词法安全.py`（3/3 ALL OK）、本报告。
- 移交路M：对标清单新增 **#121**；行为差异 **R18-D6**；CI 增 1 test + 1 反跑。
