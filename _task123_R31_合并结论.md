# 第31轮 任务1-3 合并结论：_EMBED 表三条（为/返回/尝试）逐字验证

日期：2026-09-15 ｜ 验证方法：进程内 monkeypatch `lexer._EMBED_MAX_MATCH_KEYWORDS`，全语料 856 文件三重判据隔离验证

## 一、结论速览

| 字 |  verdict | G1 语料变化 | G2 编译门 | G3 边界门 | 含该字语料 |
|---|---|---|---|---|---|
| 为 | **保留** | FAIL（23 文件） | PASS | FAIL（3 形态） | 763 / 856 |
| 返回 | **保留** | FAIL（20 文件） | PASS | FAIL（2 形态） | 647 / 856 |
| 尝试 | **保留** | PASS（0） | PASS | FAIL（3 形态） | 239 / 856 |

**`_EMBED_MAX_MATCH_KEYWORDS` 维持 3 条 `{为, 返回, 尝试}`，lexer.py 零改动。**

> 与第22轮结论一致：这 3 条是**真护栏**，非冗余。本轮_EMBED方向无删除成果，但已逐条隔离验证并记录保留理由（符合任务书风险预判#1）。

## 二、机制说明（为何不可删）

_EMBED 块（lexer.py L2292-2371）是 `为/返回/尝试` 触发的嵌入式扫描：含该字的复合名（如 `行为`/`返回表`/`尝试记录`）被整体成 IDENTIFIER；`设 X为 <值>` 形态精确切分（前缀+为+值）。

撤除某字后，含该字的复合名落回 R21 上下文敏感块（L2394）——因 `行/返/尝` 不是关键字前缀，无法整体成词，被关键字劈开：

- `行为` → `行` + `为`（ID 变两 token）
- `返回表` → `返回`(KEY) + `表`(ID)
- `尝试记录` → `尝试`(KEY) + `记录`(ID)
- `设甲为五` → `设` + `甲` + `为` + `五`

这些劈开会破坏函数定义名 / 变量名，属硬回归 → 必须保留。

## 三、逐条要点

### 为（G1 变化 23 文件，G3 失败 3 形态）
- 边界形态：`断言为真(1)`（函数调用名）、`行为`、`末位行为` 撤除后均被劈开。
- 语料实证：`作为f`/`导入字符串为str_utils`/`替换为`/`检查是否为字母`/`导入数学为math`/`设甲为五` 等 23 文件 token 流变化。
- 保留理由：无通用规则可替代（R21 块靠关键字前缀，行非关键字）。

### 返回（G1 变化 20 文件，G3 失败 2 形态）
- 边界形态：`返回表`/`返回值` 撤除后被劈成 `返回`+`表`/`值`。
- 语料实证：`返回结果`/`返回模板`/`返回文本`/`返回底数` 等 20 文件变化（均为「返回X」复合名依赖 EMBED 整体成词）。
- 保留理由：同上，`返` 非关键字前缀。

### 尝试（G1 零变化，但 G3 失败 3 形态 → 仍保留）
- 边界形态：`尝试记录`/`尝试次数`/`尝试错误` 撤除后被劈开。
- 特殊性：当前语料不含上述复合名（G1 零变化），但 G3 证明一旦语料引入即崩 → **保守保留**为真护栏。
- 保留理由：词法机制上无替代，`尝` 非关键字前缀。

## 四、铁律自检

| 铁律 | 自检 |
|---|---|
| 只改本字，不碰其他两条 | ✅ 每字独立 monkeypatch，互不干扰 |
| 逐条隔离验证，不批量删除 | ✅ 三路各自 G1∧G2∧G3 |
| G1∧G2∧G3 全通过才可删 | ⛔ 三条均未全通过，全部保留 |
| 删除后全量反跑零回归 | N/A（未删，主树零改动） |
| 新增一律 .light，禁 Python 绕语言 | ✅ G2 探针为临时 .light（验证后清理） |
| 跨项目：改编译器验证在 lightharness | ✅ monkeypatch 进程内隔离，未触主树 |

## 五、交付物

- 验证文档：`_task1_R31_EMBED为字验证.md` / `_task2_R31_EMBED返回字验证.md` / `_task3_R31_EMBED尝试字验证.md`
- 反跑脚本：`_antirun_r31_t1_为字验证.py` / `_antirun_r31_t2_返回字验证.py` / `_antirun_r31_t3_尝试字验证.py`
- 引擎与合并：`_r31_embed_engine.py` / `_r31_merge_evidence.py` / `_task123_R31_证据.json`
- lexer.py：未修改（sha `a56c41413190`）

## 六、待办提示（归路M/其他 agent）

- 任务4（test_审批.light flaky 修复）未在本轮范围，由对应 agent 处理。
- 任务5（合并验证+全量反跑+性能对比）、任务6（docs 回填 #149 / R31-D1 / R31-D2）、路M 收口归主 Agent。
- 联动保护表评估：_EMBED 维持 3 条，其他保护表（CCW=1、HM/DUAL/SPLIT 等）不受影响，无需联动删除。
- git 手工推（按红线，不自动 push）；`_task*_`/`_r31*`/`_antirun*`/`_verify*` 按惯例保留不提交。
