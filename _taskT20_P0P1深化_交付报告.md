# 第20轮 lightharness · P0/P1 深化（#3 #4 #11 v2）—— 交付报告

> 仓库：`g:\dswork\duan-light-merge\lightharness`
> 承接：第 19 轮 P2 四模块（遥测/code-runtime/e2b/#9溢出恢复）完成。本轮按用户指示回到 P0/P1 深化其它条目。
> 日期：2026-08-29

---

## 1. 本轮成果总览

深化 3 个 P0/P1 条目从 v1 到 v2，pytest 门禁 **78 passed 全绿**（75→78，新增 3 个测试）。

| # | 模块 | v1 → v2 | 测试 |
|---|---|---|---|
| 4 | 消息词汇表 | v1 4种块 → v2 7种块（+image/audio/resource） | test_消息深化.light |
| 3 | JSONL 持久化 | v1 整盘快照 → v2 增量追加 | test_持久化增量.light |
| 11 | CLI 入口 | v1 整盘保存 → v2 增量持久化 + 会话列表 | test_cli深化.light |

---

## 2. #4 消息词汇表 v2（多媒体块）

### 实现（`src/消息.light`）
- **新增 `造图片块(数据, 类型)`**：`{type: "image", data: base64, mimeType}`，默认 `image/png`
- **新增 `造音频块(数据, 类型)`**：`{type: "audio", data: base64, mimeType}`，默认 `audio/wav`
- **新增 `造资源块(资源ID, 文本, 来源)`**：`{type: "resource", resourceId, text, source?}`，对齐原版 ResourceContentBlock
- **更新 `取消息文本`**：支持新块类型投影
  - image → `[图片 image/png: base64预览...]`（超长截断到 40 字符）
  - audio → `[音频 audio/wav]`
  - resource → `[资源 res_001: 资源文本]`
  - reasoning → 直接拼接文本（v1 只处理 text）

### 验证（`test_消息深化.light`，PASS）
- 图片块：构造 + 默认 mimeType + data 验证
- 音频块：构造 + mimeType + data 验证
- 资源块：有来源/无来源两种构造
- 取消息文本：多块消息（text+image+audio+resource）投影验证
- 反跑判据：块类型或文本投影断言改反即红 ✓

---

## 3. #3 JSONL 持久化 v2（增量追加）

### 实现（`src/持久化.light`）
- **新增 `初始化日志(根, 会话对象)`**：只写首行 header（不写事件），用于增量模式初始化
- **新增 `追加事件行(根, 工作目录, 标识, 事件记录)`**：在文件末尾追加单个事件行
  - 文件不存在时自动初始化（写最小 header）
  - 事件记录格式与整盘快照一致：`{type, seq, time, data, 遮蔽?, 来源事件序号?}`
- **保留 `写出会话`**：整盘快照模式不变（向后兼容）

### 两种持久化模式
- **整盘快照**（v1）：`写出会话` —— 每次覆盖写全部事件，适合小会话
- **增量追加**（v2）：`初始化日志` + 每次事件后 `追加事件行` —— 只写新增事件，适合大会话/REPL

### 验证（`test_持久化增量.light`，PASS）
- 初始化 + 逐个追加 4 个事件 → 读入验证完整（类型/数据/序号连续递增）
- 与整盘快照模式对比：事件数一致
- 反跑判据：事件数或事件类型断言改反即红 ✓

---

## 4. #11 CLI 入口 v2（增量持久化 + 会话列表）

### 实现（`src/总入口.light`）
- **新增 `增量保存(根, 会话对象, 起始序号)`**：可测试导出函数
  - 文件不存在 → `初始化日志` + 追加全部事件
  - 文件已存在 → 只追加 `序号 >= 起始序号` 的事件
  - 返回日志路径
- **REPL 模式改用增量保存**：每轮前记录事件数，每轮后只追加新增事件（性能提升）
- **单轮模式改用增量保存**：同样从记录前事件数开始追加
- **新增 `HARNESS_LIST=1` 会话列表**：
  - 不需要 API key
  - 遍历 `HARNESS_ROOT` 目录下所有项目/会话，列出 `session.jsonl` 存在的会话
  - 输出格式：`[序号] 项目名 / 会话名` + 总数
- **新增 `列出会话(根)`**：可测试导出函数

### 验证（`test_cli深化.light`，PASS）
- 组装注册表：返回兼容注册表，含 1 个 bash 工具
- 增量保存：创建会话 → 记录事件 → 增量保存 → 读入验证（用"包含"检查而非绝对数，兼容自动记录事件）
- 增量追加：第二次保存只追加新增事件，读入验证事件数增加
- 列出会话：验证不报错且能遍历
- 反跑判据：事件包含或工具名断言改反即红 ✓

---

## 5. 开发中遇到的坑

1. **反斜杠转义**：Python 脚本写入光明文件时，`'"\\"'` 实际写入 1 个反斜杠（`"\"`），导致光明字符串未闭合。需要 `'"\\\\"'` 才能写入 2 个反斜杠（`"\\"`）。与持久化.light 对比发现此问题。
2. **新建会话自动记录事件**：`新建会话` 会自动记录 `session/start` 等事件，测试不能假设绝对事件数，改用增量/包含检查。
3. **增量保存起始序号**：必须用会话对象的事件数（而非读回的事件数）作为起始序号，因为读入时序号会重新分配。

---

## 6. 文档与回归

- `对标清单.json`：#3 → done(v2 增量追加)、#4 → done(v2 多媒体块)、#11 → done(v2 增量持久化+会话列表)
- pytest 全量门禁 → **78 passed 全绿**

---

## 7. P0/P1 当前状态总览

21 个条目中：
- **v3**：#9（压缩自动触发+溢出恢复）
- **v2**：#3（增量持久化）、#4（多媒体块）、#11（CLI增量+列表）、#15（PTY终端）
- **v1**：#1 #2 #5 #6 #7 #8 #10 #12 #13 #14（10 个，可继续深化）
- **P2**：#16 #17 #18 #19 #20 #21（6 个，done）

---

## 8. 复跑方式

```powershell
Set-Location G:\dswork\duan-light-merge\lightharness
python 运行.py examples/test_消息深化.light      # #4 多媒体块
python 运行.py examples/test_持久化增量.light     # #3 增量追加
python 运行.py examples/test_cli深化.light        # #11 CLI增量+列表
python -m pytest tests/ -q                        # 回归门禁（78 passed）
```
