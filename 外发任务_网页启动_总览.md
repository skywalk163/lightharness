# 外发任务总览：lightharness 网页启动（像 `dsh web` 那样"一条命令 → 浏览器 → 登录 → 用 agent"）

> 定位：lightharness（`G:\dswork\duan-light-merge\lightharness`）核心已就绪——70 张功能对标卡
> 全部 done、回归门禁 186 passed 全绿、OpenAI 兼容 Web API 壳（`src/web服务器.light` + SSE）已实测
> 可用（`python 运行.py examples/运行Web服务器.light` → `POST /v1/chat/completions` 正常返回）。
> 本批把"API 壳"升级成"可用的网页应用"：前端页面 + 登录认证 + 一键启动，对齐
> deepseek-harness 的 `dsh web` 体验（参考原型 `G:\github\deepseek-harness`，URL 带 token 登录）。

## 一句判定

核心引擎与 Web API 层已实测通过，缺的是"最后一公里"——**前端 UI、登录认证、静态页面路由、
一键启动开浏览器**。四块互不重叠，可完全并行。

## 工作量与并行方案

| 任务 | 内容 | 白名单文件（互不重叠） | 预估 |
|------|------|----------------------|------|
| **W1** | 前端聊天 UI（纯静态） | 新建 `webui/index.html`、`webui/app.js`、`webui/style.css` | 0.5~1 天 |
| **W2** | 登录认证模块（纯新增） | 新建 `src/认证.light`、`examples/test_认证.light` | 0.25~0.5 天 |
| **W3** | 服务端路由扩展 | 独占改 `src/web服务器.light`（可新建 `examples/test_web路由.light`） | 0.5~1 天 |
| **W4** | 启动器升级 | 独占改 `examples/运行Web服务器.light` | 0.25~0.5 天 |

- 四路并行关键路径约 **1~1.5 天**；主线集成 + 全量回归约 **0.5 天**；总投入 2~3.5 人天。
- 并行方式与历批一致：每人独立 git worktree + 独立分支（`wt-W1~W4`，分支 `task-W1~W4`），
  只跑自己的定向测试，全量回归由主线独占机器统一跑。

## 共享接口契约（先定死，四路按此开发，主线最后集成）

以下契约是四路并行的"焊接点"，任何一路都不得私自改动签名；确需变更必须回写本文件并同步其余各路。

### 契约 1：静态资源目录与路由（W1 产出文件，W3 服务，W4 传路径）

- 静态目录：`lightharness/webui/`（仓库根下，与 `运行.py` 同级）。
- 路由（W3 实现）：
  - `GET /` → `webui/index.html`（`Content-Type: text/html; charset=utf-8`）
  - `GET /app.js` → `webui/app.js`（`application/javascript; charset=utf-8`）
  - `GET /style.css` → `webui/style.css`（`text/css; charset=utf-8`）
  - 其余静态路径 → 404 JSON
- **只支持文本类资源**（html/js/css/json/svg/txt）；二进制（png/jpg/ico 等）返回 404/415，前端不得依赖。
- 静态目录绝对路径由调用方注入（见契约 4），web服务器.light 内部不做路径自推导。

### 契约 2：认证接口（W2 实现，W3/W4 调用）

`src/认证.light` 导出（签名必须一字不差）：

```
从 认证 导入 初始化认证, 取令牌, 校验令牌

初始化认证()          # 幂等：首次生成并保存 32 位十六进制令牌；再次调用返回同一令牌，不覆盖
取令牌()              # 返回当前令牌字符串；未初始化 → ""
校验令牌(令牌)        # 空 → 假；等于当前令牌 → 真；否则假
```

- 令牌生成用 `从 随机 导入 安全随机令牌`（已有，`stdlib/随机.py`），不要自造随机。
- 支持环境变量 `HARNESS_WEB_TOKEN` 固定令牌（非空时 初始化认证 直接用该值，联调/门禁用）。
- 令牌只存内存（模块级单例），不落盘；进程重启即换新（除非固定令牌）。

### 契约 3：认证行为（W3 在路由层实现，S2 语义）

- 判定"认证是否启用"：`取令牌() == ""` → 未启用，维持 S1 行为（全放行，向后兼容）；
  `取令牌() != ""` → 启用，除白名单外一律校验。
- 校验方式（二选一，都认）：请求头 `X-Auth-Token: <令牌>`，或 URL query `?token=<令牌>`。
- 白名单（放行，不校验）：`GET /health`；静态资源 `GET /`、`/app.js`、`/style.css` 是否放行
  **由 W3 决定并在交付报告写明**（建议放行，避免"先看到页面再被 401"的别扭体验；
  前端 JS 从 URL query 读 token 后，后续 API 请求自动带头）。
- 校验失败 → `401` + JSON `{"error":{"message":"未认证","type":"unauthorized"}}`。
- **不做独立 /login 端点**：登录 = URL 直接带 `?token=` 访问（对齐 deepseek-harness），
  前端 JS 读取 query 中的 token 存内存，之后所有请求带 `X-Auth-Token` 头。

### 契约 4：装配签名变更（W3 改 `配置`，W4 按新签名传参）

- `Web服务端.配置(客户端对象, 注册表, 根, 超时值, 界面目录)`——第 5 参 `界面目录`：
  - 类型：字符串，webui 绝对路径；`""` 或 `空` 表示不启用静态路由（**向后兼容**现有调用）。
  - 启用后 `GET /` 等静态路由生效。
- 调用方（W4 / W3 自己的测试文件）负责解析绝对路径：
  优先环境变量 `HARNESS_WEB_UI_DIR`；否则用 `获取脚本路径()` 推导（脚本在 `examples/`，
  其上级目录即仓库根，再拼 `webui`）；推导失败 → 打印警告并传 `""`（不启用静态路由，不崩）。

### 契约 5：CORS（W3 实现）

- 所有响应统一带 `Access-Control-Allow-Origin: *`（前端本地开发跨域联调用；本机回环 + token 认证，
  风险可控）。环境变量 `HARNESS_WEB_CORS=0` 可关闭。

### 契约 6：启动器输出（W4 实现）

- 启动成功打印（替换现有单行打印）：`web 服务已启动: http://127.0.0.1:<实际端口>/?token=<令牌>`
  （未启用认证时打印原格式 `http://127.0.0.1:<实际端口>`）。
- 自动打开浏览器：默认开（Windows 用 `start`）；`HARNESS_WEB_NO_OPEN=1` 关闭（**门禁必须设**，
  否则 CI 会弹浏览器）。打开失败不崩，仅打印提示。
- 保留既有行为不变：mock/真实双模式、`HARNESS_PORT_FILE`、`HARNESS_WEB_LIFETIME`
  （回归门禁 `RESIDENT_CASES` 已注入 `HARNESS_WEB_LIFETIME=3`，依赖它正常停机 rc=0）。

## 铁律（复用历批协作规程）

1. **不许碰别人的文件。** 只许新建/修改本任务书白名单列出的文件。
2. **新增 stdlib/语言能力一律 `.light`，一律不许建同名 `.py`**（`_light_import_hook.py` 会让 `.py` 遮蔽 `.light`）。
3. **不许用 `引 Python` 绕过语言缺陷。** 绕不过就登记 `docs/功能对标/语言缺陷账.md`，别顺手改编译器。
4. 只跑自己的定向测试，**不跑全量**。全量回归由主线独占机器统一跑。
5. 临时文件 `_taskWX_` 前缀；PowerShell 中文输出落盘再读；不用 `&&` 与 heredoc。
6. 固定测试端口互不重叠：W3 用 `19100-19199`、W4 用 `19400-19499`（web 服务本身内核分配端口，不冲突）。
7. 新增 `.light` 测试用例退出码断言必须立得住反跑（判据改反必红）。

## 主线集成清单（本批完成后由主线做）

1. 合并 W1~W4 四个分支（`--no-ff`，按 W2 → W3 → W4 → W1 顺序解冲突，理论上零冲突）。
2. 全量回归：`python -m pytest tests/` 应保持 186 全绿 + 新增用例全绿。
3. 手工联调：`python 运行.py examples/运行Web服务器.light`（mock）→ 浏览器打开带 token URL →
   发"你好"→ 流式回复逐字渲染；再 `HARNESS_WEB_MOCK=0` 接真实 DeepSeek 密钥端到端验证。
4. 交付说明：登录 URL 格式、环境变量清单（HARNESS_WEB_*）、安全边界（仅 127.0.0.1 + token）。

## 运行通道

```powershell
cd G:\dswork\duan-light-merge\lightharness
python 运行.py examples/test_XXX.light       # 各任务自己的测试，逐个绿
python -m pytest tests/                      # 全量门禁（仅主线跑）
```
