# E5 交付报告：上下文压缩 + 令牌计量 + SSE 统计推送

**任务**：E5（对齐原版 compaction + token-meter）
**日期**：2026-09-09
**门禁**：`py -3.10 -m pytest tests/ -q` → **195 passed**（基线 188，新增 7 个全部通过）

---

## 一、交付物清单

| 类型 | 路径 | 说明 |
| --- | --- | --- |
| 新建 | `src/令牌计量.light` | 令牌计量器 + 单价表 + 消息启发式估算 |
| 新建 | `src/压缩E5.light` | 滑动窗口压缩 + 工具结果修剪 + 环境变量配置 |
| 修改 | `src/agent.light` | 新增 `默认修剪钩` / `默认压缩钩` / `注册默认钩子`（不改主循环） |
| 修改 | `src/web服务器.light` | SSE 追加 `统计` 事件；`构造()` 初始化计量器与压缩配置 |
| 修改 | `webui/app.js` | 顶栏统计渲染 + 使用率变色 + 压缩通知 |
| 修改 | `webui/index.html` | 顶栏新增 `<span id="stats">` |
| 修改 | `webui/style.css` | `.stats` 样式 + `.warn` / `.danger` 变色 |
| 新建 | `examples/test_令牌计量.light` | 8 用例全绿 |
| 新建 | `examples/test_压缩E5.light` | 10 用例全绿 |
| 新建 | `examples/test_agentE5钩子.light` | 3 用例全绿（集成） |
| 新建 | `examples/test_web服务器E5.light` | 3 用例全绿 |

> 备注：任务文档要求「新建 `src/令牌计量.light`」和「新建 `src/压缩.light`」。
> 仓库已存在 `src/令牌估算.light`（v1 递归块定价）和 `src/压缩.light`（v1/v2 会话压缩）。
> 为避免破坏既有导出（`压缩`、`压缩管理器`、`估算内容`、`估算消息` 等被
> `web服务器.light` / `test_压缩.light` / `test_压缩自动.light` 等引用），
> 新建文件命名为 `压缩E5.light` 与 `令牌计量.light`，各自独立、不覆盖。
> 若后续需要合并，只需将 E5 新函数复制进旧文件。

---

## 二、令牌计量（`src/令牌计量.light`）

### 2.1 单价表（$ / 1M tokens）

| 模型 | 输入 | 输出 |
| --- | --- | --- |
| deepseek-chat | 0.27 | 1.10 |
| deepseek-reasoner | 0.27 | 1.10 |
| gpt-4o | 2.50 | 10.00 |
| gpt-4o-mini | 0.15 | 0.60 |
| 其它（未知） | 1.00 | 2.00 |

### 2.2 估算启发式

- 中文（CJK 统一表意 + 扩展 A + 兼容表意 + 假名/韩文 + 中日韩标点）：**1.5 字/token**
- 英文（A–Z / a–z / 0–9）：**4 字符/token**
- 其余（符号/空白/其他 Unicode）：**3 字符/token**
- 按字符码点区间分类，非 hex 字面量（光明语不支持 `0x4E00`，已改用十进制 19968/40959）。

### 2.3 类 `令牌计量器`

属性：`模型 / 单价 / 本轮输入 / 本轮输出 / 累计输入 / 累计输出 / 累计花费`

方法：
- `构造(模型="")` — 默认 deepseek-chat
- `设置模型(模型)` — 热切换，累计花费保留
- `记录响应(响应)` — 优先 `usage.prompt_tokens / completion_tokens`，回落 `输入token / 输出token`；追加本轮与累计
- `本轮统计()` / `累计统计()` — 快照
- `重置本轮()` — 清零本轮，保留累计
- `取单价表()` — 诊断

导出：`令牌计量器 / 估算消息 / 估算消息表 / 估算文本 / 取单价`

---

## 三、压缩（`src/压缩E5.light`）

### 3.1 策略 A：滑动窗口压缩 `滑动窗口压缩(消息表, 阈值token, 保留轮数)`

- 触发：`估算消息表(消息表) > 阈值token`
- 保留：全部 `system` 消息 + 最近 `保留轮数 * 2` 条非系统消息
- 折叠：旧消息替换为一条 `{"角色":"system","内容":"[已省略 N 条历史消息，保留最近 K 轮]"}`
- 返回：`{"消息": 新表, "压缩": 真/假, "省略数": N, "估算": 新估算}`

### 3.2 策略 B：`生成摘要(消息列表)`

- 当前实现：返回滑动窗口提示（不真正调用 LLM，保持轻量）
- 签名保留，可替换为 LLM 摘要函数

### 3.3 工具结果修剪 `修剪工具结果(工具名, 结果)`

| 工具名 | 上限（字符） |
| --- | --- |
| bash | 8000 |
| 读文件 / 读取文件 / 读文本 | 16000 |
| 搜索 / search / grep | 4000 |
| ls / list | 4000 |
| 其它 | 8000 |

超长时：`前 70% + "\n... [结果已截断，原长 N 字符，保留首尾] ...\n" + 后 30%`。

### 3.4 `压缩配置`（环境变量）

| 变量 | 默认 |
| --- | --- |
| `HARNESS_COMPACTION_THRESHOLD` | 8000 |
| `HARNESS_COMPACTION_KEEP_ROUNDS` | 6 |
| `HARNESS_MODEL_CONTEXT_WINDOW` | 32768 |

---

## 四、agent 钩子注册（`src/agent.light`）

新增（模块级，不改 `运行智能体` 主循环）：

```
段落 默认修剪钩 接收 工具名, 结果:
  返回 修剪工具结果(工具名, 结果)

段落 默认压缩钩 接收 消息:
  设 配 为 新建 压缩配置()
  设 结果 为 滑动窗口压缩(消息, 配.阈值token, 配.保留轮数)
  如果 结果["压缩"] == 假: 返回 消息
  # 原地清空消息并插入压缩后的表
  ...

段落 注册默认钩子 接收 钩子:
  钩子.注册后工具(默认修剪钩)
  钩子.注册后轮次(默认压缩钩)
  返回 真
```

**关键设计**：光明语钩子返回值被忽略（`agent.light` 主循环 `钩(消息)` 不接返回值），
因此压缩钩子通过**原地修改消息列表**生效——先 `.弹出(i)` 清空，再 `.追加()` 插入新表。
已实测光明语 `列表.弹出(索引)` 可原地清空。

---

## 五、SSE 统计事件（`src/web服务器.light`）

- `Web服务端` 构造时初始化 `计量器`（`令牌计量器("deepseek-chat")`）与 `压缩管理器`（`压缩配置`）
- `处理对话`：跑一轮前 `设置模型 + 重置本轮`，跑完后 `记录响应(结果)`
- `生成SSE`：在结束块之后、`[DONE]` 之前追加一条：

```
data: {"种类":"统计","本轮输入":N,"本轮输出":N,"累计总":N,"花费":X,"上下文使用率":Y%}

data: [DONE]
```

- `生成统计事件()` 方法可独立调用（测试用）

---

## 六、Web UI（`webui/app.js` + `index.html` + `style.css`）

### 6.1 顶栏展示

`<header id="topbar">` 新增 `<span class="stats" id="stats">`，格式：

```
in {本轮输入} out {本轮输出} | 累计 {累计总} | 花费 ${花费:.5f} | 使用率 {使用率}%
```

### 6.2 使用率变色

- 使用率 > 80% → `.stats.warn`（黄底）
- 使用率 > 95% → `.stats.danger`（红底 + 呼吸动画）

### 6.3 压缩提示

- 服务端推送 `{"种类":"压缩"}` → `addSystem("已触发上下文压缩，旧消息可能被省略。")`
- 前端预留该分支；当前服务端未主动推送压缩事件（SSE 仅含统计），
  可在后续扩展中于 `默认压缩钩` 触发时追加事件推送。

---

## 七、验证结果

### 7.1 单元/集成测试（光明运行器）

| 测试 | 结果 |
| --- | --- |
| `examples/test_令牌计量.light` | ✅ 8 用例全绿 |
| `examples/test_压缩E5.light` | ✅ 10 用例全绿 |
| `examples/test_agentE5钩子.light` | ✅ 3 用例全绿（钩子注册 + 修剪端到端 + 压缩端到端） |
| `examples/test_web服务器E5.light` | ✅ 3 用例全绿 |
| `examples/test_令牌估算.light`（既有） | ✅ 通过（未破坏） |
| `examples/test_agent工具调用.light`（既有） | ✅ 通过（未破坏） |
| `examples/test_web路由.light`（既有） | ✅ 通过（未破坏） |
| `examples/test_会话存储.light`（E2 交付） | ✅ 通过（未破坏） |

### 7.2 全量回归门禁

```
$ py -3.10 -m pytest tests/ -q
195 passed in 235.97s (0:03:55)
```

- 基线 188 passed → 现 195 passed（新增 7 个测试全部通过）
- 无失败、无跳过、无 xfail 破坏

### 7.3 语言坑规避记录

- 光明语**不支持 hex 字面量**（`0x4E00` 报语法错误），已改用十进制 `19968`
- 光明语**`空` 是保留字**，不可作变量名（测试里 `设 空 为 ...` 报错），已改用 `空内容`
- 光明语列表无 `.移除()` / `.弹栈()` 方法，用 `.弹出(i)` 原地删除
- 光明语钩子返回值被忽略（`钩(消息)` 不接返回值），压缩钩子必须**原地修改**消息列表
- agent 主循环用 `content`（英文键），mock 工具返回也须用 `content` 键
- 光明语 `包含()` 对不存在键返回 `假`，可安全用于分支判断

---

## 八、遗留 / 未做

1. **策略 B 未接真实 LLM 摘要**：`生成摘要()` 当前返回滑动窗口提示；若需 LLM 摘要，
   应替换为异步函数并在 post_turn 钩子里 await。当前实现保持轻量、无外部依赖。
2. **SSE 未主动推送「压缩」事件**：Web UI 已预留 `种类=压缩` 分支，服务端尚未在
   `默认压缩钩` 触发时追加事件（因为钩子无事件推送通道）。若需要，需在
   `agent.light` 扩展钩子签名为 `钩(消息, 事件推送)`。
3. **上下文窗口未与真实模型能力表联动**：目前仅从 `HARNESS_MODEL_CONTEXT_WINDOW`
   读环境变量（默认 32768）；若按模型名区分（deepseek-chat 64K、gpt-4o 128K 等）
   需扩展 `压缩配置` 类。
4. **非流式响应未含统计**：`生成对话JSON` 已包含 `usage` 字段（原有实现），
   但 E5 的「累计总 / 花费 / 使用率」未加入非流式 JSON（非流式客户端不消费顶栏统计）。

---

## 九、环境变量

| 变量 | 默认 | 作用 |
| --- | --- | --- |
| `HARNESS_COMPACTION_THRESHOLD` | 8000 | 触发滑动窗口压缩的 token 阈值 |
| `HARNESS_COMPACTION_KEEP_ROUNDS` | 6 | 保留最近 N 轮（user/assistant 对） |
| `HARNESS_MODEL_CONTEXT_WINDOW` | 32768 | 上下文窗口（计算使用率的分母） |

示例：
```bash
HARNESS_COMPACTION_THRESHOLD=16000 \
HARNESS_COMPACTION_KEEP_ROUNDS=10 \
HARNESS_MODEL_CONTEXT_WINDOW=65536 \
python 运行.py examples/运行Web服务器.light
```
