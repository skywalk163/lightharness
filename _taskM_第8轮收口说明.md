# 第8轮收口说明 —— 行为级对照验证

- 轮次：第 8 轮（行为级对照验证）｜日期：2026-09-12
- 任务书：`行为对照_第8轮_任务prompt分发.md`（4 并行 + 路M）
- 合入后 HEAD：`ae834a7` ｜ 全量 CI：**222 passed / 0 failed（保持全绿）**

---

## 1. 本轮目标与结果

把上游关键包的测试场景**翻译成光明用例**逐条对照，检验"纯逻辑覆盖 ≠ 行为一致"的差距。这是首次用上游断言验证行为，而非只对照源码同构。

| 项 | 结果 |
|---|---|
| 合入 | 任务1/4 分支 `--no-ff` 合并（`1df5f3b`/`f5aee6c`）+ 任务2/3 直提（`8b53b37`/`ae834a7`），零冲突 |
| 新增测试 | 4 个 examples/test_行为对照_*.light（69 节场景断言） |
| 反跑判据 | 4 脚本共 **13 项变异**（ALL RED→恢复 GREEN） |
| src 改动 | **零内容改动**（对照验证轮；任务3 反跑内存 patch/restore 后仅 stat 缓存过期，hash 与 HEAD 一致已复核） |
| 回归门禁 | **222 passed / 0 failed（保持全绿）**，smoke 5/5 |
| 行为差异 | **14 项登记**（`docs/功能对标/行为差异清单.md`）：🔴 1 真实 bug / 🟡 10 复刻缺口与语义差异 / ⚪ 3 语言层与宿主面 |

## 2. 四路对照摘要

| 路 | 上游依据 | 翻译场景 | 反跑 | 发现 |
|---|---|---|---|---|
| 任务1 代理循环 | agent-loop 13 spec（cancel/loop/tool-calls/request-error） | 20 节 S1-S20（Cancel 原语/中断结算/max-tokens 终止与工具丢弃/客户端异常/工具屏障/并发上限/事件序列） | 4/4 | concludesTurn 不传播（🟡）；5 项宿主面无法对照（中流取消/撕裂恢复/scope 并发/inject 异步） |
| 任务2 会话/持久化 | session 15 + persistence-jsonl 16 项 | 31 断言 8 节（surface 投影/replace/规范信封/序号准入/jsonl 严格宽容/迁移链） | 3/3 | **D5 模块级投影节点0 键名笔误（🔴 真实 bug）**；D1-D4 语义差异；G1 repair 缺口 |
| 任务3 LLM | llm 13 + deepseek 12 spec | 六节 A-F（消息构造/流式翻译/请求体映射/模型目录/单价/token 分桶） | 5/5 | usage 无 totalTokens 桶（🟡）；消息按引用组装非深拷贝（🟡）；无图片 token（⚪） |
| 任务4 工具/子智能体/预设 | tools 12 + subagent 12 + presets 12 spec | 12 节（JSON Schema 校验/描述表净化/错误收敛/最后回答/目录保序/元数据/排序） | 2/2 | 取最后回答 形态差异（🟡）；执行策略默认 parallel vs exclusive（🟡）；中英文案（⚪）；shipped 字典缺口（🟡） |

## 3. 🔴 真实 bug（行为对照轮核心产出）

**T2-D5：`src/会话格式.light:537` 模块级 `投影节点0` append 连接分支读英文键 `["system"]`，而词汇定义（:111）与 `记录系统提示`（:520）均用中文键 `["系统"]`** ——第二条 append 的 system/message 内容必丢（KeyError/空值）。类方法 `会话.投影节点0` 用 `["系统"]` 正确，故既有测试未触发。**一行级修复**（:537 `["system"]` → `["系统"]`），建议单独开修复轮并补一条 append 连接分支断言。

## 4. 回归与反跑汇总

| 路 | 反跑判据 | 回归 |
|---|---|---|
| 任务1 | 4/4（max-tokens 种类/错误种类/并行数/事件数） | test_代理系统提示/代理深化/agent工具调用 绿 |
| 任务2 | 3/3（编解码值/迁移版本/删节） | test_会话格式/会话V3迁移/持久化 绿 |
| 任务3 | 5/5（请求体 model 键/token 分桶） | test_消息/客户端/令牌计量 绿 |
| 任务4 | 2/2（Schema 校验值/删节11） | test_工具/子智能体/预设 绿 |
| 路M | 全量 CI **222 passed / 0 failed**（= 218 基线 + 4 新测试），smoke 5/5 | — |

## 5. 遗留事项

1. **🔴 T2-D5 修复轮**（建议下轮首选，一行 + 一条断言）。
2. **🟡 语义缺口排期**：T3-D1 totalTokens 桶、T1-S18 concludesTurn 传播、T4-D2 执行策略默认值（上游 exclusive）为行为缺口，建议优先；T2-D1 replace 区间校验、T2-D2 sourceEventSeqs 快照、T2-D3 V0 头英文键、T4-D1 最后回答块形态、T4-D4 shipped 字典、T2-G1 repair 域为增强项，可后置。
3. **⚪ 维持登记**：T2-D4 负零（语言层）、T3-D3 图片 token（无字节级模型）、T4-D3 文案语言（语义等价）。
4. **上游窗口**：仍无 0.1.5-rc.2 之后官方版本。
5. 工作区仅剩 `_taskM_*` 路M 工具（保留备查）；`task-8-agentloop`/`task-8-toolsub` 分支与 `wt-R8T1`/`wt-R8T4` worktree 已清理（收口动作）。
