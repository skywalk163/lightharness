# 第19轮 任务6 交付报告（补全版）：字典与函数 L-096 / L-097 + D批次验收（L-068~L-074）

> 补记说明：本报告为**第20轮任务5 补齐**——第19轮任务6 的交付物（修复代码+复现用例+反跑脚本）齐备，但交付报告缺失。
> 依据：① `light-merge` 提交 `49319306` 的真实 diff、② `lightharness` 提交 `ffa987e`、③ 反跑脚本 `_antirun_t6_字典函数_D批次.py`、
> ④ 复现用例 `examples/test_L096.light` / `test_L097.light` 与 D批次 `test_L068~L074.light`（含各自头部「验收结果」标注）。
> 补记日期：2026-09-14 ｜ 补记路：第20轮任务5

---

## 1. 任务定位

- 轮次：第19轮（编译器缺陷修复轮）
- 任务：任务6 —— 字典与函数：修 L-096（字典属性访问）、验 L-097（if 块内嵌套段落），并做 D批次（L-068~L-074）验收
- 修复文件（文件互斥表内）：`light-merge/src/code_generator.py`

## 2. L-096：字典属性访问 `对象.键` 不可用

### 2.1 现象与根因

Python `dict` 无属性访问，`.键` 直通生成 `对象.键` → 运行期 `AttributeError`。
这也使 `对象.键 为 值`（属性赋值）无法表达「键不存在则新增」的语义。

### 2.2 修复（`code_generator.py` 三处）

1. **新增两个发射期辅助函数**（随生成代码头部注入）：

```python
def _light_attr_get(_o, _k):
    if isinstance(_o, dict):
        return _o.get(_k)          # dict → 键访问（缺键返回 空，与 字典获取 缺省一致）
    return getattr(_o, _k)         # 类实例/模块 → 原生属性，行为不变

def _light_attr_set(_o, _k, _v):
    if isinstance(_o, dict):
        _o[_k] = _v
    else:
        setattr(_o, _k, _v)
    return _v
```

2. **赋值目标为 `MemberAccess` 且非方法调用** → 发射 `_light_attr_set(对象, '键', 值)`。
   关键点：**绝不能把 `_light_attr_get(...)` 放在赋值左侧**（Python `cannot assign to function call`）。
3. **表达式位置的 `MemberAccess`** → 由旧的 `f"{obj}.{member}"` 直通改为 `f"_light_attr_get({obj}, {member!r})"`。

### 2.3 反跑判据

| 判据 | 内容 | 结果 |
|---|---|---|
| A | `test_L096` rc=0 | PASS |
| B | 直接调 codegen 验证发射：`对象.键` → `_light_attr_get(对象, '键')`；`己.标识 为 1` → `_light_attr_set(self, '标识', 1)` 且左值**不**走 get；`无财.叫()` 保持原生方法调用发射 | PASS |
| C1 | 变异：属性访问发射改回 `obj.member` 直通 → `test_L096` 立红（恢复后绿） | PASS |
| C2 | 变异：属性赋值拦截删除 → `test_会话` 立红（`cannot assign to function call`，恢复后绿） | PASS |

变异锚点（C1/C2）：
```python
C1_OLD = '                return f"_light_attr_get({obj}, {expr.member!r})"'
C2_OLD = "            if isinstance(stmt.target, MemberAccess) and not stmt.target.is_method_call:"
```

## 3. L-097：if 块内嵌套段落提升错误

- 复现：`段落 测试:` 内 `如果 真:` 内定义 `段落 内部函数 接收:` 并引用外层局部变量 —— 期望保持闭包语义。
- **第19轮实测结论：rc=0（绿）**，但**本批次无对应代码 diff**（`49319306` 未触及 `parser_stmt.py` 等嵌套定义路径）
  → 说明该行为在本批次之前**已由既有实现覆盖**，本任务只是**验收确认**，非新修。
  ⚠️ 缺陷账状态行「第19轮任务6 … if块嵌套段落解析修复」措辞偏强，建议按本条订正为「验收确认（既有已绿）」。
- 第20轮补记一例**边界限制**（本轮发现，非 L-097 回归）：块内（嵌套）段落定义的**形参名**若以关键字开头
  （如 `段落 内层返回 接收 函数值:`），因 `_scan_user_definitions` 预扫描不覆盖嵌套段落的形参，
  `函数值` 会被切成 `函数` + `值` → `name '值' is not defined`。模块级段落无此问题。
  处置：`examples/test_R20_词法边界综合.light` 的该处形参改用 `算子` 并就地加注；作为**新登记项**移交（见第20轮任务3/6 报告）。

## 4. D批次验收（L-068 ~ L-074，7 项）

| 编号 | 主题 | 验收结果 | 依据 |
|---|---|---|---|
| L-068 | 类方法定义边界（多类连续定义方法归属） | ✅ 已修复/正常 | 已实现类结束标记 + 编译器警告透出（`cli/light.py`）；`test_L068` rc=0 |
| L-069 | `序列化JSON` 布尔值 | ✅ 已修复 | `序列化JSON(真)="true"` / `(假)="false"`；`test_L069` rc=0 |
| L-070 | 默认参数 `{}` / `[]` 字面量 | ✅ 已修复 | `段落 甲 接收 选项 = {}:` 可用；`test_L070` rc=0 |
| L-071 | 字典键安全原语 | ✅ 已有内置 | `字典获取(字典, 键, 缺省)` 缺键返缺省、不抛；`test_L071` rc=0 |
| L-072 | `转字符串` 字典输出格式 | ❌ **未修复（记录当前行为）** | 仍输出 Python `repr`（`{'键': 1}`）；真身 `str(值)`；格式变更涉及全仓断言兼容面 → 纳入后续批次。`test_L072` 记录 repr 形态并 rc=0 |
| L-073 | 运行时错误翻译（AttributeError/KeyError） | ✅ 已修复 | `键错误` 顶层翻译为「键错误: 字典缺少键 '乙'（文件:行）」+ 光明建议；可被 `捕获` 捕获不崩溃；`test_L073` rc=0 |
| L-074 | 类型判断内置（是字典/是字符串/是列表/是整数） | ✅ 已有内置 | 三者均为内置原语；`test_L074` rc=0 |

- 反跑 A 判据：`test_L096` + `test_L068/L069/L070/L071/L072/L074` + `test_L073` 全部 rc=0 → PASS。
- **诚实标注**：7 项中仅 L-072 为「未修复」，且是**有意识记录当前行为**（非漏修）；其余 6 项为「已修复」或「原有内置支持」，
  本批次对 D批次 的贡献是**验收与固化用例**，不是逐项改代码。

## 5. 已知局限与移交

1. L-096 的 `_light_attr_get` 对 dict 缺键返回 `空`（`None`），与 `字典获取` 缺省口径一致；
   而 `对象["键"]` 对缺键**仍抛 键错误**（L-036 已裁定语言层不变）——两种取值语义并存，需在文档中并列表述。
2. L-096 未处理「动态计算成员名」（`对象.变量` 形态），只处理字面成员名。
3. L-097 的嵌套段落形参名限制（见 §3）→ 移交第20轮任务1/3 与后续词法重构（P0-A）。
4. L-072 待后续批次统一「友好格式」并同步更新全仓断言。

## 6. 与第20轮的关系

- 第20轮任务1 的 `lexer.py` 改动**未触及** `code_generator.py`，L-096 发射逻辑不受影响；
  第20轮全量扫描中 `test_L096`/`test_L097` 均 rc=0（无回归）。
- 第20轮任务2（HMAC）与任务6 无关，但同属「stdlib .py 侧下沉」的同一类处置范式。
