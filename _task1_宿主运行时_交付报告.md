# 任务1 交付报告：宿主运行时与插件生命周期原型（src/宿主运行时.light）

> 任务来源：`G:\dswork\duan-light-merge\复刻_第17轮_任务prompt分发_宿主接线原型 (1).md` 任务1
> 日期：2026-09-13 ｜ 对标 #110（**原型设计**，上游参考 Cordis Context/Service/插件生命周期、packages/boot 启动流程、core/session 插件结构——非逐行复刻）
> 命名预检查：src/ 下无 宿主运行时.light 同名模块 ✓

---

## 一、接口设计（设计决策登记，R17-D1）

| 接口 | 形状 | 设计决策 |
|---|---|---|
| 运行时状态机 | 未初始化→初始化中→运行中→停止中→已停止；初始化中/停止中→错误 | 合法转换表 是否可转换 + 转换状态（非法抛 `非法运行时状态转换: A -> B`）；拓扑排序失败/装载致命错误置「错误」态（直接赋值，绕过转换表——错误路径设计决策） |
| 插件形状 | {名称, 版本, 依赖列表(插件名), 提供服务列表, 监听事件列表, 安装/启动/停止/卸载(函数)} | 钩子缺省可为空操作段落；钩子第一参=插件上下文；依赖按名称（版本范围原型不开放，登记） |
| 插件上下文 | {运行时, 插件名, 时间, 注册服务(服务名, 实例), 依赖服务(服务名)} | 注册服务重复抛 `服务重复注册: X`；依赖服务缺失抛 `依赖服务未就绪: X` |
| 注册插件 | 仅「未初始化」态可注册 | 运行中注册属动态加载，原型不开放（抛 `运行时已启动，不能再注册插件`，登记 R17-D1） |
| 拓扑排序 | Kahn（入度+邻接），稳定序=名称升序 | 依赖缺失 `插件依赖缺失: X 依赖 Y`；循环 `插件依赖循环: 涉及 N 个插件` |
| 生命周期 | 启动：拓扑序逐插件 安装→启动（错误隔离，记录 {插件, 阶段, 消息} 继续后续）；停止：加载序**逆序** 停止→卸载 | 启动前派发 运行时/启动前、拓扑失败抛并置错误态 |
| 服务注册表 | 注册服务/获取服务/尝试获取服务/列举服务（含提供者） | 独立操作面 + 上下文内注册服务 双入口，同一张服务表 |
| 运行时事件 | 监听运行时事件 / 派发运行时事件（轻量监听器表，发布模式，不做 waterfall） | 事件名：运行时/启动前、运行时/启动后、运行时/停止前、运行时/停止后、运行时/插件安装前、运行时/插件安装后、运行时/插件启动失败、运行时/插件停止 |
| 元数据 | 获取元数据(运行时, 当前时间) | 版本/状态/启动时间/插件计数（加载序）/服务计数/错误计数/运行时长（当前−启动，负值归 0） |
| 重启 | 停止→清加载序/服务表→状态回未初始化→启动 | 插件表保留（同一批插件重新装载）；已停止→未初始化为内部路径不经转换表（设计决策） |

## 二、测试验证

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"; python 运行.py examples/test_宿主运行时.light
test_宿主运行时 PASS   （rc=0）
```

`examples/test_宿主运行时.light` 覆盖 7 组 ≥50 断言：状态机（5 合法转换/2 非法转换抛错/启动停止往返）、插件注册（重复/运行中拒）、拓扑排序（三插件依赖序/依赖缺失/依赖循环）、生命周期钩子顺序（安装→启动拓扑序、停止→卸载逆序，8 位置逐位断言）+ 服务注册表（获取/尝试获取命中与未命中/缺失抛/重复抛/列举含提供者）+ 依赖注入（应用插件启动钩子经 依赖服务 拿到 kv 实例）、错误隔离（坏插件失败记录、好插件仍装载、运行时仍运行、计数核对）、元数据（版本/启动时间/运行时长/插件计数/错误计数）、运行时事件（启动后事件携带插件数）、重启（状态/新启动时间）。

## 三、反跑结果（3/3 ALL OK）

`lightharness/_antirun_t1_宿主运行时.py`（BASE 自定位；字节级备份→变异→跑本路测试→断红→恢复→断绿；恢复置于 try/finally）：

```
PASS C=变异 拓扑排序退化为注册顺序 -> 红 (变异后运行 rc=1（期望非 0，真红）)
PASS 字节级恢复校验 (sha256 52cf98593276)
PASS A/B 判据（依赖插件装载 + 依赖循环报错）回归绿 (rc=0)
ALL OK
```

A=正常（2 个依赖插件装载→服务可获取→停止）与 B=边界（依赖循环报错不崩溃、错误被记录）为测试内场景，随回归绿验证；C=变异（拓扑排序插件 函数体首行插入 `返回 字典键列表(插件表)` 退化为注册顺序）——拓扑序断言与钩子顺序断言双红，恢复后绿。

## 四、未移植项 / 与上游 Cordis 的架构差异（R17-D1）

- Cordis 的 Context 作用域事件派发/服务父子层级/热插拔（运行中注册）——原型仅启动前注册（运行中注册抛错）。
- 插件依赖的版本范围（semver）——原型按插件名依赖。
- 服务懒加载/循环服务依赖（服务级）——原型只做插件级依赖。
- 真实异步（Promise 排干/fiber）——光明顺序执行，错误隔离用 尝试/捕获。
- packages/boot 的真实启动流程（profile/manifest/launcher flags）——由宿主配置原型（任务6，另路）承担。

## 五、语言差异（本轮新缺陷登记）

- 无新缺陷（L-141~L-142 编号空缺，路M 顺延）。开发中踩到已知缺陷 L-091（匿名段落内联，钩子改顶层命名段落）与 L-101（`回调` 保留字，形参改 处理器），均按既有绕法处理。

## 六、移交清单

- `lightharness/src/宿主运行时.light`（新增，≈340 行）
- `lightharness/examples/test_宿主运行时.light`（新增，PASS rc=0）
- `lightharness/_antirun_t1_宿主运行时.py`（反跑 3/3 ALL OK，BASE 自定位）
- 本报告。
- 移交路M：对标清单新增 **#110**（宿主运行时与插件生命周期原型，标注「原型设计」）；行为差异清单 **R17-D1**；CI 增 1 个 test_ 文件（302+ 占 1）。
