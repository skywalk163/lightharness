# 第28轮 任务4 交付报告：CS表2字清零——边界测试套件 + 全量反跑

日期：2026-09-15 ｜ 状态：✅ 全部完成
前提：任务1（`_P0A_TAIL_CUT_SINGLE={'列'}` 词尾切出反向规则）与任务2
（user_definitions 前缀余部单字并入扩展至 CS∪HM∪DUAL）已落地，CS 表已清零。

---

## 一、交付物清单

| 交付物 | 路径 | 状态 |
|---|---|---|
| 列词尾切出测试（.light） | `examples/test_R28_列词尾切出.light` | ✅ rc=0 |
| 类声明词尾并入测试（.light） | `examples/test_R28_类声明词尾并入.light` | ✅ rc=0 |
| 混合场景测试（.light） | `examples/test_R28_混合场景.light` | ✅ rc=0 |
| pytest token 层测试 | `tests/test_R28_CS表2字词尾上下文_token.py` | ✅ **38 passed** |
| 全量反跑脚本 | `_antirun_r28_t4_全量反跑.py` | ✅ |
| 反跑结果 JSON | `_task4_R28_反跑结果.json` | ✅ |

## 二、测试覆盖说明

### 2.1 列三形态（任务书 4.1/4.2）
- **词首并入**：列数/列表/列名 → 整词 IDENTIFIER；
- **词尾切出**：`对于元素在序列:`（无空格 bootstrap 快照形态）→ `序|列:`；
  嵌入关键字分段变体 `甲在序列:`/`甲之序列:`/`循环 对于项目在序列:`；
  ASCII 前缀整词对照 `解析ESC序列`；
- **词尾并入（非冒号）**：`序列 为 [1,2,3]`/`序列.长度()` → 整词；
- 独立关键字：`设 列表项 为 列` → 列 为 KEYWORD。

### 2.2 类五形态（任务书 4.2）
- **类声明词尾并入**：`类 独立类:`/`类 测试类:`/`类 有界队列类:` → KEYWORD(类)+整词；
- **独立关键字**：`类 名称:` → 类 KEYWORD 不受影响；
- **词首并入**：类别/类似 → 整词；
- **继承**：`类 子类 继承 基类:` → 子类/基类 整词；
- **使用处**（R27 打红形态）：`新建 独立类()` → 整词。

### 2.3 混合场景（.light 运行期 4 组 + pytest）
- 列数 与 for-in 序列共存；类体内 类别/列数 词首并入名；
- 无空格遍历、R27 已删 14 字回归抽查（乘法/减法/真空/到位/段落名/配置/是否）；
- R26/R27 回归钉：段[0]/配[0] 下标、己之姓名、真空/真 双形态。

### 2.4 保护表形态断言（铁律）
- `CS == ∅`（清零）；`_P0A_TAIL_CUT_SINGLE == {'列'}`；
- `_R28_CS_REMOVED == {列,类}`；净增量 `HM − CS == R26∪R27∪R28 移除集`；
- DUAL=8/HM=22 保持；列类 ∈ F；`_R27_BCLASS` 8 字；import 自校验重载幂等。

## 三、全量反跑结果

### 3.1 [A] 边界测试用例
```
test_R28_列词尾切出.light       rc=0
test_R28_类声明词尾并入.light   rc=0
test_R28_混合场景.light         rc=0
```

### 3.2 [B] pytest
```
test_R28_CS表2字词尾上下文_token.py   38 passed, 0 failed
R26+R27+R28 合跑                      319 passed, 1 skipped, 0 failed
```

### 3.3 [C] 全语料 token 零变化（硬门槛）
```
语料 = 845 个 .light 文件
可比 839 文件（既有失败 2 排除；R28 新增测试文件不在 R26 基线，跳过）
对照 R26 基线快照：逐文件 token 序列零变化 ✅
```
即：**列 的词尾切出反向规则 + 类 的前缀余部并入扩展 + CS 2→0 清零，
在全部语料上 token 行为与 R26 基线完全一致。**

### 3.4 [D] 性能（纯 tokenize × 10 次取平均）
```
R28 实测：平均 9.94s | 中位 9.80s
R25 基线：9.63s
```
处于 R25/R26/R27 同一波动带（负载波动 8.6~12s，静置极值 6.0~6.2s）。
结论：CS 清零 + 反向规则**未引入性能退化**。

## 四、运行期语义说明（本轮新发现，供使用方注意）

无空格 `循环 对于项目在序列:` 形态下，`序列` 按语料基线被切为 `序|列`
两个 token——**该形态下以"序列"命名的变量在运行期不可直接引用**（转译产物
bootstrap 文件仅为 token 快照快照比对，不参与运行）。运行期光明代码应使用
`遍历 X 之 表：`/`循环 对于 X 在 表：`（带空格整词形态）。已在
`test_R28_列词尾切出.light` 中以两种写法分别钉住。

## 五、铁律自检

| 铁律 | 自检 |
|---|---|
| 正向覆盖列/类词首并入 | ✅ 列数/列表/列名 + 类别/类似 |
| 反向覆盖列词尾切出 + 类声明关键字 | ✅ 无空格/嵌入分段/循环对于 三变体 + 类 名称: |
| 类声明词尾并入必须测试 | ✅ 独立类/测试类/有界队列类 + 使用处 |
| pytest 含保护表形态断言（CS 最终条数） | ✅ CS=∅ / TAIL_CUT={列} / 三轮净增量 |
| 全量反跑零变化硬门槛 | ✅ 839 文件可比零变化 |
| 新增一律 .light，禁 Python 绕语言 | ✅ 3 个新测试均 .light；pytest 仅 token 层断言 |

## 六、结论

任务4 全部交付物落地且通过：边界测试 rc 全 0、pytest 38 passed
（R26+R27+R28 合跑 319 passed）、全语料 839 文件对照 R26 基线 token 零变化、
性能无退化。**CS 表至此清零（R21 前 52 字 → R28 为 0）**，八轮通用化全部
以正面类别/形态规则替代逐词登记，`_P0A_TAIL_CUT_SINGLE`（1字）与
`_R27_BCLASS`（8字）为仅存的显式小集合，均有防漂移断言钉住。
