# 第28轮路M收口说明 —— CS表剩余2字（列/类）逐字通用化

> 日期：2026-09-15 ｜ 方向：阶段G——CS表剩余2字逐字通用化（CS表完整清零，累计52→0）
> 跨项目操作：light-merge（编译器规则扩展+死代码清理）+ lightharness（验证+测试+反跑）
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）

---

## 一、本轮概述

第27轮将CS表从16条缩减为2条（列/类保留为真护栏，两字行为互为镜像）。本轮逐字通用化这2字，最终CS表清零。

### 核心成果

| 指标 | 结果 |
|---|---|
| 列的通用化 | `_P0A_TAIL_CUT_SINGLE = {'列'}`词尾切出反向规则 |
| 类的通用化 | user_definitions前缀余部并入扩展（CS∪HM∪DUAL） |
| CS表变化 | **2→0（清零）** |
| 累计精简 | **52→0，精简100%** |
| 死代码清理 | 12项全部执行 |
| 全语料token | **0/839文件变化** |
| pytest | 38 passed（R26+R27+R28合跑319 passed） |
| .light边界测试 | 3个 rc=0 |
| 性能提升 | **~32%**（16.5s→11.2s） |

---

## 二、列的词尾切出反向规则（任务1）

### 规则定义
- **`_P0A_TAIL_CUT_SINGLE = frozenset({'列'})`**
- **规则**：列在词尾位置（scan_pos+sub_len==len(full_identifier)）+ 前导汉字（`_is_han(fi[scan_pos-1])`）+ 嵌入输出循环（段内命中真关键字分段后）→ 不跳过、落出KEYWORD

### 关键机制
- 切出只发生在嵌入输出循环（`甲在序列`/`甲之序列`分段形态）
- 裸`序列:`/`甲序列:`/`ESC序列:`（段内无真关键字，`embedded_found=False`）走整词出口不受影响
- 列的词首并入（`列数/列表/列名`）由HM正面类别覆盖，不受影响

### 与R25词尾并入的关系
- R25词尾规则默认词尾+前导汉字→并入标识符
- 列的反向规则是例外，优先级高于R25词尾并入
- 列∈F（正面类别）不可加入词尾排除集（否则HM词首`列数`打红），故需独立的词尾切出反向规则

---

## 三、类的类声明上下文词尾并入（任务2）

### 规则实现
- 扩展user_definitions前缀匹配"余部单字并入"判据从"仅CS锚"扩展为"CS∪HM∪DUAL"
- **规则**：`独立`∈user_definitions（段落名/设名预扫描）+ 余部`类`∈可构词单字 → 不拆分整词

### 打红场景修正
- R27删除清单定位类的打红场景为`类 独立类:`声明处
- R28地面真相探针（`_dbg_r28_ground.py`）逐token二分定位修正为`新建 独立类()`使用处
- 声明处`类 独立类:`/`类 名称:`/`类 子类 继承 基类:`无需额外规则（KEYWORD(类)+IDENTIFIER整词）

### 词首并入
- `类别/类似`由HM覆盖（G2通过）

---

## 四、CS表逐条验证删除（任务3）

### 删除2字（G1∧G2∧G3全通过）
- **列**：由`_P0A_TAIL_CUT_SINGLE`词尾切出反向规则覆盖
- **类**：由user_definitions前缀余部并入扩展覆盖

### CS表清零
- `_COMPOUND_SAFE_SINGLE_KEYWORDS = frozenset()`

### 自校验断言（5条全部通过）
| 断言 | R28口径 |
|---|---|
| ① | CS == ∅（空frozenset） |
| ② | _TRAILING_ALIAS_CLASS == F（43字，不变） |
| ③ | HM − CS == _R26_CS_REMOVED ∪ _R27_CS_REMOVED ∪ _R28_CS_REMOVED（= HM 22字） |
| ④ | _P0A_TAIL_CUT_SINGLE == {'列'}（恒1字防漂移） |
| ⑤ | _P0A_HEAD_MERGE_DUAL == 8字（恒8字防漂移） |

### 场景矩阵
- 10/10全过（列三形态+类声明/关键字/继承/使用处，含两个打红形态钉）

---

## 五、边界测试与反跑（任务4）

- **列词尾切出.light**：`test_R28_列词尾切出.light` rc=0
- **类声明词尾并入.light**：`test_R28_类声明词尾并入.light` rc=0
- **混合场景.light**：`test_R28_混合场景.light` rc=0
- **pytest**：`test_R28_CS表2字词尾上下文_token.py` 38 passed
- **全量反跑**：ALL OK，839文件token零变化

---

## 六、CS清零后联动清理与性能（任务5）

### 12项死代码清理（全部执行）
| # | 清理点 | 落地动作 |
|---|---|---|
| 1 | 模块常量_COMPOUND_SAFE_SINGLE_KEYWORDS | 任务3已清零frozenset() |
| 2 | 类属性compound_safe_single_keywords | × 删除（恒空），grep零残留 |
| 3 | 局部缓存_compound_safe（_skip内） | × 删除缓存+CS项 |
| 4 | 局部缓存_compound_safe（tokenize内） | × 删除缓存 |
| 5 | _match_keyword递归入口 | ≈ 删CS项→DUAL∪B类 |
| 6 | _skip_compound_safe_and_match递归入口 | ≈ 删CS项→DUAL∪B类（函数保留） |
| 7-9 | 嵌入扫描三处CS项 | ≈ 重写为平衡结构（HM∪DUAL+tail） |
| 10 | 前缀拆分检查 | ≈ 改写为HM∪DUAL（G1零变化实证） |
| 11 | L-119下标判据 | ≈ 删CS项→HM∪DUAL |
| 12 | R26词首并入判据 | ≈ 删CS项→(HM and is_han) or (DUAL and is_han) |

### _skip_compound_safe_and_match非死代码
- 任务书5.1预设CS清零后该函数成为死代码可删除
- 实测DUAL(8字)∪B类(8字)非空，递归入口仍命中，函数非死代码
- 与R27报告5.3结论一致，按任务书明示保留

### 联动保护表（七张全部保留）
CCW 37条、_EMBED 3条、OPERATOR_VERBS 19条、_P0A_MERGE_WHOLE 9条、_P0A_NEVER_SPLIT 4条、HM 22字、DUAL 8字。CS清零不构成语义冲击。

### 性能提升~32%
- 修改前（CS=2）：165.36s/×10（×1均16.536s）
- 修改后（CS=∅+清理）：112.236s/×10（×1均11.224s）
- 提升~32%，多轮保护表精简以来最大单次性能提升

---

## 七、验证结果

### 7.1 编译器方向反跑
- T3 CS表删除：ALL OK（839文件token零变化）
- T4 全量反跑：ALL OK（839文件token零变化）

### 7.2 全量CI
- **pytest**：待确认（CI运行中）
- **smoke**：待确认

### 7.3 数据集方向
- 第25轮数据集（2200条）不受本轮影响（编译器修改为词法层规则扩展+死代码清理，全语料token零变化）

---

## 八、路M修正与决策记录

1. **CS表清零达成**：任务书目标"CS表清零"，实际2→0达成。列由`_P0A_TAIL_CUT_SINGLE`词尾切出反向规则覆盖，类由user_definitions前缀余部并入扩展（CS∪HM∪DUAL）覆盖。

2. **类的打红场景修正**：R27删除清单定位为声明处，R28地面真相修正为使用处（`新建 独立类()`）。机制结论以本轮为准。

3. **_skip_compound_safe_and_match非死代码**：任务书5.1预设CS清零后该函数成为死代码可删除。实测DUAL∪B类非空，递归仍命中。路M认可保留。

4. **性能提升~32%**：CS清零+12项死代码清理后，全语料tokenize耗时从16.5s降至11.2s，提升~32%。多轮以来最大单次性能提升。

---

## 九、CS表八轮演化总览

```
R21前：52字 → R22：30 → R23：30（三表白名单清零）
→ R24：30（真护栏确认）→ R25：30（词尾正面类别F=43，CS退化为词首锚）
→ R26：16（词首并入正面类别HM=22）→ R27：2（DUAL=8 + L-119等价）
→ R28：0（列 词尾切出反向规则 + 类 前缀余部并入扩展）✅ 清零
```

累计精简：52→0，精简100%。

---

## 十、已知问题与后续方向

### 10.1 持续存在的问题
- test_审批.light flaky（R20-C）：从第20轮持续存在，`等待结果`用`time.time()`而非`time.monotonic()`，非编译器缺陷。

### 10.2 第29轮方向建议
1. **CCW 37条分批精简**（推荐）：CS表已清零，下一个保护表精简目标是CCW（COMMON_COMPOUND_WORDS，37条）
2. **test_审批.light flaky修复**（`time.time()`→`time.monotonic()`）
3. **继续上游复刻**
4. **性能进一步优化**（CS清零后词法热路径的进一步优化空间）

---

## 十一、docs三件回填

1. **对标清单**（docs/功能对标/对标清单.json）：追加#146（CS表剩余2字逐字通用化，CS表清零，累计52→0），总数146条。
2. **缺陷账**（docs/功能对标/语言缺陷账.md）：本轮无新发现缺陷，未更新。
3. **行为差异清单**（docs/功能对标/行为差异清单.md）：追加R28-D1（CS表剩余2字逐字通用化，CS表清零，订正R27-D1）、R28-D2（列的词尾切出反向规则）、R28-D3（CS表清零后死代码清理与性能提升~32%）。

---

## 十二、git提交

### light-merge项目（1个提交）
- 待提交：src/lexer.py（101+/72-，列的反向规则+类的前缀余部并入+CS表清零+12项死代码清理+自校验断言更新）

### lightharness项目（6路分路提交 + 收口提交）
- 任务1：列的词尾切出反向规则设计+实现
- 任务2：类的类声明上下文词尾并入设计+实现
- 任务3：CS表2字逐条验证删除（清零）
- 任务4：边界测试套件+全量反跑
- 任务5：CS清零后联动清理+自校验断言更新+性能
- 任务6：质量审查+docs回填
- 收口提交：docs三件 + 质量审查报告 + 收口说明
