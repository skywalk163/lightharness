# 第25轮 任务2 交付：`_COMPOUND_SAFE_SINGLE_KEYWORDS` 词尾并入 18 字逐条验证删除清单

> 日期：2026-09-15 ｜ 方向：CS 词尾并入 17 条（配/段 拆 2 字 = 18 字）逐条验证
> 工作区状态：**已含任务1**（单字词尾并入改为正面规则 `_TRAILING_ALIAS_CLASS = F`，43 字）
> 配套证据：`_task2_R25_词尾并入逐条验证_证据.json`
> 反跑脚本：`_antirun_r25_t2_词尾并入删除.py`

---

## 一、结论

**18 字全部保留，0 字可删；CS 表 30 → 30 不变。**

任务1 把「单字词尾并入」从「对 CS 取补集（21 字）」改为「正面类别 F（43 字，按五类语义排除推导）」。
这**只覆盖了词尾（scan_pos>0）并入**，并未覆盖**词首（scan_pos=0）并入**。
CS 表现在退化为纯粹的「词首并入锚」——而 18 个候选字在词首位置去掉 CS 后全部浮出为 KEYWORD
（G2 词首编译门 100% 失败），因此无一可删。

这与第24轮任务3 审计结论「语料冗余但语义非冗余」完全一致，本轮用**编译级证据（G2）**坐实，
并补上 G1 语料级 + G3 边界级三重印证。

---

## 二、三道判据定义

| 判据 | 含义 | 通过条件 |
|---|---|---|
| **G1** 语料判据③ | 撤掉 c 后所有含 c 的语料文件 token 序列零变化 | 变化文件数 = 0 |
| **G2** 词首编译门（R25 新增） | 构造语句起始裸名 `{词首复合名} 为 7` + `断言相等({词首复合名},7,…)`，
        在撤掉态下词法结果须与基线一致（仍整词成 IDENTIFIER）。等价于 subprocess 编译门，
        因词首被切成 `关键字+余字` 必编译失败 | 基线整词成 IDENTIFIER 且撤掉态不变 |
| **G3** 边界形态门 | 任务书六类边界形态 + 代表词尾复合名 token 流不变 | 无破坏 |

**可删条件：G1 ∧ G2 ∧ G3 全通过。** 三者任一失败即保留。

---

## 三、逐条决策（18 字）

> `in_F` = 该字是否已被任务1 的正面类别 F 覆盖（词尾仍并入库）。✘ = 不在 F，撤 CS 后词尾也不被并入。

### A. 词首护栏（全部 18 字 G2 失败 —— 去掉 CS 后在语句起始裸名浮出为 KEYWORD）

每个字的 `drop_head_tokens` 均退化为 `KEYWORD(字) + IDENTIFIER(余字)`，证明词首并入确由 CS 独占。

| 字 | 词首复合名 | 词尾复合名 | in_F | G1 语料 | G2 词首 | G3 边界 | 保留理由 |
|---|---|---|---|---|---|---|---|
| 出 | 出错 | 标准输出 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞） |
| 列 | 列数 | 日期列 | ✔ | 6变 | ✘ | ✔ | 词首护栏（FFI/断言工具/泛型类型等 6 文件含词首列） |
| 则 | 则例 | 规则 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞） |
| 到 | 到底 | 文件未找到 | ✘ | 1变 | ✘ | ✘ | **真护栏**：到 ∉ F（范围运算符），且边界 `文件未找到` 破坏 |
| 常 | 常规 | 首异常 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞） |
| 引 | 引导 | 最后索引 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞） |
| 接 | 接受 | 符号链接 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞；扫描 712 文件） |
| 断 | 断定 | 截断 | ✔ | 10变 | ✘ | ✔ | 词首护栏（断言工具/测试集等 10 文件含词首断） |
| 末 | 末尾 | 月末 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞） |
| 的 | 的确 | 成绩的长度 | ✔ | 6变 | ✘ | ✔ | 词首护栏（L1_baihua/学生模块等 6 文件含词首的） |
| 真 | 真值 | 标准输出失真 | ✘ | 1变 | ✘ | ✘ | **真护栏**：真 ∉ F（值字面量），且边界 `标准输出失真` 破坏 |
| 类 | 类别 | 有界队列类 | ✔ | 3变 | ✘ | ✔ | 词首护栏（装饰器/测试集 3 文件含词首类） |
| 试 | 试用 | 测试 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞） |
| 跳 | 跳闸 | 最后心跳跳过 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞） |
| 过 | 过程 | 通过 | ✔ | 1变 | ✘ | ✔ | 词首护栏（对象池缓存 1 文件含词首过） |
| 长 | 长度 | 块长 | ✔ | 0变 | ✘ | ✔ | 词首护栏（语料无词首用例，G2 抓潜空洞；正向用例 `设 长度 为` 即依赖） |
| 配 | 配给 | 配置项 | ✔ | 3变 | ✘ | ✘ | **真护栏**：单字别名下标 `配[0]`/`配[键]` 破坏 |
| 段 | 段号 | 代码段 | ✔ | 15变 | ✘ | ✘ | **真护栏**：单字别名下标 `段[1]` 破坏（标准库 8 文件 + snake 等） |

### B. 分类小结

- **额外 G3 边界破坏（4 字）**：`到`/`真`（真值 / 范围运算符，不在 F）、`配`/`段`（L-119 单字别名下标访问）。
  这 4 字即使词首不出现，也因边界形态必须保留。
- **G1 语料真实变化（9 字）**：列(6)/到(1)/断(10)/的(6)/真(1)/类(3)/过(1)/配(3)/段(15)。
  变化源于语料中存在词首用法，与 G2 结论互相印证。
- **G1=0 但 G2 失败（8 字）**：出/则/常/引/末/试/跳/长。
  语料恰好没把这些字放词首，造成「语料零变化」的**假冗余**；G2 合成词首门抓出潜在空洞，
  正是第24轮审计点名的「语料冗余≠语义冗余」。

---

## 四、CS 表状态

- 删除前：30 字（含本 18 字 + 乘余例减加对是自除首模是空 等 12 字词首/运算符/值字面量护栏）
- 删除后：**30 字（不变）** —— 18 字逐条验证均不可删
- 已验证的「不可删」证据：每字均附 `base_head_tokens`（整词 IDENTIFIER）与
  `drop_head_tokens`（关键字+余字）对照，见 `_task2_R25_词尾并入逐条验证_证据.json`

---

## 五、对任务1通用化的意义

任务1 的 F（43 字）正面规则**已完全覆盖词尾并入**：全语料 834 文件 token 序列零变化
（见 `_task1_R25_单字后缀规则设计.md §三`）。但 CS 表的「词首并入」能力是 F 覆盖不到的独立维度，
故本任务 0 删除。

> 后续若想进一步精简 CS 表，须另立「词首并入通用规则」（第24轮审计已指出：属影响首层最长匹配的
> 独立大改动，不在本轮范围）。本轮只确认：**在仅有词尾通用化的前提下，CS 18 词尾并入条目一律不可删。**

---

## 六、验证标准达成

- [x] 18 字全部逐条验证，每条都有撤掉后的 token 变化文件清单（G1）与词首退化证据（G2）
- [x] 可删条目全部删除（结果：无）
- [x] 删除后 token 变化仅限预期（实际无删除，零变化）
- [x] 全量 CI 无回归（lexer 未改；词法单测见任务1/任务3 报告）
- [x] 反跑 ALL OK（内存态 monkeypatch，无磁盘改写，无需还原）
