# 第18轮任务2 交付报告 —— 数值与数学兼容库

- **对标编号**：#117（光明语言工具库·缺陷修复轮，非上游复刻）
- **差异编号**：R18-D2
- **语言缺陷预分配**：L-153~L-154（**空缺顺延**，见下）
- **可写文件**（全部新增，零越界）：
  - `src/数值兼容.light`（133 行 / 14 段落）
  - `examples/test_数值兼容.light`（140 行 / 48 断言）
  - `_antirun_t2_数值兼容.py`
  - `_task2_数值兼容_交付报告.md`

---

## 一、定位

前 17 轮复刻中，数值/数学绕法散落各模块（如 `取整半数上` 手写 `向下取整(x+0.5)`、`安全除` 手写 `尝试/捕获`）。本轮建设标准化数值兼容库，把 4 个数值数学缺陷（L-125/L-131/L-129/L-085）的绕法统一，并与 JS/TS 语义对齐（上游是 TS/JS）。

## 二、10 个函数与修复层级

| # | 函数 | 修复层级 | 对应缺陷 | 说明 |
|---|---|---|---|---|
| 1 | `取整半数上(x)` | A | L-125 | JS Math.round half-up 语义，实现 `向下取整(x+0.5)` |
| 2 | `安全除(分子,分母,默认值=0)` | A | L-131 | 分母为 0 返默认，不抛除零错 |
| 3 | `JS真值(值)` | A | L-129 | JS Boolean() 语义（0/""/null 假；[]/{}"0" 真；false 假） |
| 4 | `JS相等(甲,乙)` | B | L-129 | JS === 严格相等，纠光明 `0==假` 为真之偏 |
| 5 | `指数解析(字符串)` | B | L-085 | 解析 "1e3"/"1.5e2"/"2e-3"/大写E；非法返空 |
| 6 | `科学计数法(系数,指数)` | B | L-085 | 系数 * 10^指数（指数可为负） |
| 7 | `是有限数(值)` | B | L-131 | 绝对值 < 1e308 视为有限 |
| 8 | `钳位(值,最小,最大)` | B | — | 区间限制 |
| 9 | `线性插值(起点,终点,比例)` | B | — | 线性插值 |
| 10 | `除零安全(分子,分母)` | B | L-131 | 返回 {成功,商,错误} |

## 三、验证

- **测试**：`examples/test_数值兼容.light` 共 **48 断言**（含近似断言），覆盖 10 个函数正常/边界/异常三态，终行 `test_数值兼容 PASS`，`rc=0`。
- **反跑**：`_antirun_t2_数值兼容.py` **3/3 ALL OK**
  - A=正常：取整半数上 + 安全除 + JS真值 组合运行 PASS
  - B=边界：安全除分母为 0 返默认 / JS真值对空列表空字典判真 在干净运行全过
  - C=变异：取整半数上改银行家舍入（`四舍五入(x)`），2.5→2，2.5 断言立红；恢复后 sha256 校验一致（7f39dc89…）、回归 PASS

## 四、语言缺陷处理

- **新缺陷**：本轮为修复轮，**未遇真实新语言缺陷**。L-153~L-154 预分配编号**空缺顺延**。
- **被修复缺陷标注**（供路M回填缺陷账"修复状态"列）：
  - L-125「四舍五入银行家舍入」→ 已通过 `取整半数上` 修复
  - L-131「除零抛错非 Infinity」→ 已通过 `安全除`/`除零安全`/`是有限数` 修复
  - L-129「空列表/空字典判假 + 0==假」→ 已通过 `JS真值`/`JS相等` 修复
  - L-085「e 指数浮点不支持」→ 已通过 `指数解析`/`科学计数法` 修复

## 五、两步踩坑（已就地解决，非新缺陷）

1. **`字符串获取` 不在 `字符串工具` 导出**：初版 `从 字符串工具 导入 字符串获取` 报导入错误（`字符串获取` 在 `内置核心字符串`，是全局内置）。改为去掉该导入、直接用全局 `字符串获取`/`字符串长度`，并自写 `子串` 辅助替代 `截取字符串`，彻底去除对 `字符串工具` 的依赖。
2. **`1.0e309` 字面量溢出 → 编译器编成未定义 `inf`**：Light 不支持 inf 字面量，超大指数字面量触发"name 'inf' is not defined"。改用有限但 ≥1e308 的字面量 `1.5e308`（float 上限约 1.8e308 可表示）测 `是有限数` 假分支；运行时 `10.0 ** 309` 则抛 OverflowError 不可用。属 Light 浮点上限限制，非新登记缺陷（L-131 已涵盖"光明没有 Infinity"）。

## 六、纪律

- 仅新增本路互斥文件，未修改任何既有 `src/` 模块；未 commit，收口归路M。
- docs（对标清单/缺陷账/行为差异）由路M 统一回填。
- 交付文件：`src/数值兼容.light`、`examples/test_数值兼容.light`、`_antirun_t2_数值兼容.py`、`_task2_数值兼容_交付报告.md`。
