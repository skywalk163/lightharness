# _taskM_第13轮收口说明.md

> 轮次：第 13 轮（2026-09-13）｜上游基线：`G:\github\deepseek-harness` @ `a305303422`（0.1.5-rc.2）
> 任务书：`G:\dswork\duan-light-merge\复刻_第13轮_任务prompt分发.md`（6 并行 + 路M）
> 提交链：任务1~6 分路提交 + 本轮收口提交（见 §三）

---

## 一、本轮概要

第 13 轮完成 **6 个上游纯逻辑包**复刻（均为对标清单外/点名未实现域），6 路交付物齐全、反跑 3/3 ALL OK、6 单测全 PASS：

| 任务 | 域 | 上游包 | 光明模块 | 提交 |
|---|---|---|---|---|
| 1 | fs 编辑器 | tool-str-replace-editor（#60 点名未实现收口） | `src/字符串替换编辑器.light` | 任务1 提交 |
| 2 | shell 环境 | shell-env（DSH_* 注册表） | `src/外壳环境.light` | 任务2 提交 |
| 3 | goal 轮驱动 | goal-round-driver（纯逻辑面） | `src/目标轮驱动.light` | 任务3 提交 |
| 4 | session 日志 | session-log-deepseek（线格式+水印折叠） | `src/会话日志增量.light` | 任务4 提交 |
| 5 | 授权链 | credentials/authorization（注册表+校验链） | `src/授权链.light` | 任务5 提交 |
| 6 | webhook | webhook-github（验签+载荷归一） | `src/网络钩子GitHub.light` | 任务6 提交 |

## 二、文件互斥核验

- 6 路仅写各自互斥表内文件（新增 src 模块 6 + 用例 6 + 反跑 6 + 报告 6 + repro 4）。
- `src/` 既有模块、`stdlib/`、`scripts/`、`运行.py`、`tests/`、README/运行Web服务器 零改动。
- 反跑脚本位置修正：任务1/2 原交付在 `G:\dswork\duan-light-merge\` 根目录（不在仓库内），收口移入 `lightharness\` 并修正内部 BASE 路径后重跑 ALL OK（与第12轮惯例一致）。

## 三、提交链（main）

```
381a6cf  web: HARNESS_WEB_MOCK 缺省自动检测（第13轮前整理，未推送，用户手工推）
（第13轮分路提交 6 个）
任务1(fs编辑器域): tool-str-replace-editor纯逻辑复刻…
任务2(shell环境域): shell-env受信环境注册表复刻…
任务3(goal轮驱动域): goal-round-driver纯逻辑面复刻…
任务4(session日志域): session-log-deepseek纯逻辑面复刻…
任务5(授权链域): authorization注册表+校验链+单键单尝试状态机复刻…
任务6(webhook域): webhook-github验签+载荷归一+配置校验纯逻辑复刻…
（本轮收口提交 1 个）
收口(第13轮): 对标回填 #86~#91 / L-091~L-103 / R13-D1~D6 + _taskM_第13轮收口说明.md
```

## 四、对标回填

### 对标清单（85 → 91 条）

| 编号 | 功能 | 光明模块 | 状态 |
|---|---|---|---|
| #86 | fs/tool-str-replace-editor 编辑器工具 | `src/字符串替换编辑器.light` | done（0.1.5第13轮） |
| #87 | shell/shell-env 受信环境注册表 | `src/外壳环境.light` | done |
| #88 | goal/goal-round-driver 目标轮驱动纯逻辑面 | `src/目标轮驱动.light` | done（与 #35 驱动区块并存待归并） |
| #89 | session/session-log-deepseek 增量日志线格式 | `src/会话日志增量.light` | done |
| #90 | credentials/authorization 授权链 | `src/授权链.light` | done |
| #91 | webhook/webhook-github GitHub 签名 webhook | `src/网络钩子GitHub.light` | done |

### 行为差异清单（R12 汇 19✅/0🟡/10⚪ → 追加 R13-D1~D6 全 ⚪）

R13-D1 localeCompare 码点序近似（任务2，语言层限制）｜R13-D2 HMAC pbkdf2 单轮近似（任务6，→L-095）｜R13-D3 #88/#35 驱动区块并存待归并（任务3）｜R13-D4 invariant 继承分叉豁免未移植（任务4）｜R13-D5 秘密不可用 401 vs 上游 503（任务6，宿主接线时收敛）｜R13-D6 异步中止同步模拟（任务5，语言层限制）

## 五、问题与缺陷留档（本轮重点，供后续解决）

> 用户要求：各任务发现的问题、缺陷注意留档。全部已登记入 `docs/功能对标/语言缺陷账.md`（L-091~L-103）与各交付报告；repro 文件 4 个已落地 rc=0。

### 语言缺陷账新增 13 项（L-091~L-103）

| 编号 | 来源 | 严重度 | 现象 | 复现文件 |
|---|---|---|---|---|
| L-091 | 任务1 | 中 | 匿名段落不能跨行内联在调用实参位置 | `_repro_L091.light` |
| L-092 | 任务2+任务5(D7) | 中 | 标识符含关键字字样（返回/为…）被词法切碎 | `_repro_L092.light` |
| L-093 | 任务3/4 共用 | 中（调试期误导） | 跨模块未捕获异常「位置块」恒锚定函数末尾，行号错配（L-061 残留场景） | `_repro_L093.light` |
| L-094 | 任务4 | 低（工具层） | 入口目录遮蔽 src 同名模块且静默失败（sys.path[0]） | 无 repro（登记原因见账本） |
| L-095 | 任务6（撞号重排） | 高（宿主面差异） | 哈希.HMAC_SHA256 实为 pbkdf2 单轮近似，非标准 HMAC | `_repro_L095.light`（收口补写） |
| L-096 | 任务5 D1 | 高 | 字典属性访问不可用（必须下标） | 无（绕法已固化） |
| L-097 | 任务5 D2 | 高 | if 块内段落定义导致缩进跟踪错误（段落被提升到模块级） | 无 |
| L-098 | 任务5 D3 | 高 | 段落从列表取出调用时闭包 nonlocal 绑定丢失（字典修改生效） | 无 |
| L-099 | 任务5 D4 | 中 | 抛出包装字典/类丢失自定义属性（.码 访问失败） | 无（已形成 [码]消息 约定） |
| L-100 | 任务5 D5 | 低 | 空字典 {} / [:] 不支持，须用 字典() | 无 |
| L-101 | 任务5 D6 | 低 | 回调 是保留关键字不能作变量名 | 无 |
| L-102 | 任务5 D8 | 低 | 不支持默认参数值（须显式传 空 + 判空） | 无 |
| L-103 | 任务5 D9 | 低 | 可选字典键访问抛 KeyError（须 取可选/字典包含键） | 无 |

**建议后续解决优先级**：
1. **L-095（高）**：修 stdlib/哈希.light 的 HMAC_SHA256 为标准 HMAC（ipad/opad），或改名/标注近似；webhook 宿主接线必须标准 hmac 对拍。
2. **L-093（中）**：异常位置块在「跨模块抛出+未捕获」时错配——编译器异常格式化器补跨模块调用帧归属；修后可大幅减少调试误判。
3. **L-096/L-097/L-098（高，编译器/运行时）**：字典属性访问、块内段落提升、闭包 nonlocal——均为光明语言核心缺陷，建议语言侧优先评估。
4. **L-092（中）**：关键字词法约束扩展——标识符含关键字子串应合法（仅整词为关键字时生效）。
5. **L-094（低，工具层）**：导入钩子对「入口目录遮蔽」给显式错误。
6. **L-091/L-099~L-103（低~中）**：语法糖/约定类，绕法已固化，按需。

### 编号冲突处理（收口裁定）

- 并行路期间 L-091 被任务1（匿名段落）与任务6（HMAC）**同时占用**；任务4 报告知晓冲突并按「避开已占用」自取 L-093/L-094。
- 收口以**先到先得 + 文件落地为准**：L-091=匿名段落内联（`_repro_L091.light` 落地）；任务6 的 HMAC 项重排为 **L-095**，收口补写 `_repro_L095.light`（自洽链路断言 rc=0）。
- 后续并行轮任务书应预置「缺陷编号由路M 统一预分配」防再撞号。

## 六、测试与验证

- 6 单测全 PASS（rc=0）：test_字符串替换编辑器 / test_外壳环境 / test_目标轮驱动 / test_会话日志增量 / test_授权链 / test_网络钩子GitHub。
- 6 反跑全 ALL OK（3/3）：判据覆盖 行号宽度/歧义编辑/insert 上界｜保留键/键排序/注销｜轮号/上限/提示收尾｜水印最大/seedLength 条件/畸形抛错｜ALREADY_IN_FLIGHT/NOT_COMMITTED/INVARIANT｜恒定时间/必需头/内容类型。
- 4 个 repro（L-091/092/093/095）rc=0。
- 全量 CI：见 §七（CI 完成后回填）。

## 七、全量 CI 结果

- `python scripts/ci_test.py` → **pytest: 通过（267 passed, 0 failed）＋ smoke: 通过（5/5）**，退出码 0，耗时约 6 分钟。
- 首轮 CI 曾失败 1 项：`test_授权链.light` 组 10c「INVARIANT 重抛」断言红——根因：`src/授权链.light` 结算广播处于**反跑点 C 变异残留**（`设 占位 为 0` 未恢复为 `抛出 不变失败`，任务5 反跑脚本执行 C 判据时异常退出未走恢复分支）。
- 处置：恢复正确形态 → 单测绿 → 6 反跑全 ALL OK（无残留变异）→ pytest 267 passed → 完整 CI 全过；修复单独提交 `e981125`。
- 教训：**反跑脚本的恢复必须放在 finally/异常安全路径**，且收口核验反跑输出不能截断（首轮收口 `Select-Object -First 2` 截断导致 C 判据未核验）。

## 八、遗留与待决策（延续）

1. **工具注册表接入点**（R12-D9/D12，延续）：ask_user_question 与 查询工具 未注册进 工具.light 注册表——维持 ⚪。
2. **approval/* 事件词汇注册**（R12-D7，延续）：审计事件词汇未注册——维持 ⚪。
3. **#88/#35 驱动区块归并**（R13-D3，新增）：`目标折叠.light` 内置驱动区块与 `目标轮驱动.light` 并存——待决策收敛方向。
4. **L-095 HMAC 宿主面对拍**（R13-D2，新增）：webhook 宿主接线须标准 hmac。
5. **本轮缺陷编号撞号**：后续任务书预分配编号防冲突。
6. **反跑脚本异常安全**（本轮新教训）：变异后若断言/运行异常，恢复须在 finally 保证执行；路M 核验反跑须看完整输出。

## 九、交付物

- 6 路交付报告（_task1~_task6，已随分路提交）
- `_taskM_第13轮收口说明.md`（本文件）
- 收口提交含：对标清单.json（91 条）、语言缺陷账.md（L-001~L-103）、行为差异清单.md（R11+R12+R13）
