# 外发任务 D2：序列化与字符串化（L-069 + L-072）

## 任务目标

修复光明语言序列化/字符串化的两个缺陷：`序列化JSON` 不支持布尔值（L-069）、`转字符串` 对字典输出 Python repr（L-072）。

---

## 缺陷 L-069：`序列化JSON` 不支持光明布尔值 `真`/`假`

### 现象

需要序列化为 JSON 的字典中若含光明布尔值 `真`/`假`，`序列化JSON` 输出异常（布尔值未被正确映射为 JSON 的 `true`/`false`）。lightharness Web 配置 API 的配置元数据因此改用数字 `1`/`0` 代替布尔，前端靠 `0` 是 falsy 的特性判断，语义不直观。

### 最小复现

```光明
从 JSON 导入 序列化JSON

设 配置 为 {"enabled": 真, "mock": 假, "count": 3}
设 文本 为 序列化JSON(配置)
打印 文本
# 当前：布尔值位置异常（可能输出空、报错、或 Python True/False）
# 期望：{"enabled": true, "mock": false, "count": 3}
```

### 期望能力

`序列化JSON` 自动将光明 `真`/`假` 映射为 JSON 的 `true`/`false`（与 Python `json.dumps(True)` 一致）。同时确认嵌套结构（字典内列表、列表内字典）中的布尔值也正确映射。

### 验收标准

- [ ] 最小复现用例 `examples/test_L069.light`：输出 `{"enabled": true, "mock": false, "count": 3}`。
- [ ] 嵌套结构（字典→列表→字典）中的布尔值正确映射。
- [ ] `解析JSON` 反向解析 `true`/`false` 为光明 `真`/`假`（若当前不是，一并修复或文档化）。
- [ ] 现有用数字 1/0 代替布尔的绕法继续可用（向后兼容）。
- [ ] 全量回归不破坏。

---

## 缺陷 L-072：`转字符串` 对字典输出 Python repr，非 JSON/友好格式

### 现象

`转字符串(字典)` 输出 Python 字典的 `repr()`：`{'种类': 'error', '失败': 'HTTP 请求失败 [401]: Unauthorized'}`——单引号、无缩进、Python 风格。对中文用户不够直观，且无法直接被 JSON 解析器消费。当错误消息需要展示给终端用户时，Python repr 格式增加了理解成本。

### 最小复现

```光明
设 字典 为 {"名字": "张三", "年龄": 25, "标签": ["开发者", "测试"]}
设 文本 为 转字符串(字典)
打印 文本
# 当前：{'名字': '张三', '年龄': 25, '标签': ['开发者', '测试']}
# 期望（二选一）：
#   方案A（JSON 格式）：{"名字": "张三", "年龄": 25, "标签": ["开发者", "测试"]}
#   方案B（友好格式）：{名字: 张三, 年龄: 25, 标签: [开发者, 测试]}
```

### 期望能力（任选其一，需与 L-069 协调）

- **方案A（推荐）**：`转字符串` 对字典/列表输出 JSON 格式（双引号、中文不转义），与 `序列化JSON` 统一。注意：`转字符串` 对字符串应仍输出字符串本身（不加额外引号），对数字/布尔输出其字面形式。
- **方案B**：保持 `转字符串` 为 Python repr，但提供 `友好格式(对象)` 或确保 `序列化JSON` 可作为通用替代（全局可用、支持布尔——依赖 L-069 修复）。

**关键决策点**：`转字符串` 的语义是「调试用字符串表示」还是「用户可见字符串表示」？如果是前者，Python repr 可接受但应文档化；如果是后者，应输出友好格式。建议与光明语言规范团队确认后决策。

### 验收标准

- [ ] 最小复现用例 `examples/test_L072.light`：字典/列表的 `转字符串` 输出符合决策后的格式。
- [ ] 字符串的 `转字符串` 仍输出字符串本身（不加额外引号）。
- [ ] 嵌套结构正确处理。
- [ ] 现有依赖 `转字符串` 输出格式的测试/代码不破坏（若格式变化，需排查全仓 `转字符串` 调用点）。
- [ ] 全量回归不破坏。

---

## 交付物

1. 修复代码（JSON 序列化模块 / `转字符串` 内置）
2. `examples/test_L069.light` + `examples/test_L072.light` 最小复现用例
3. 修复说明：根因 + 改动点 + 格式决策理由 + 验证结果（含全量回归结果）
4. 更新 `docs/功能对标/语言缺陷账.md` 中 L-069/L-072 的状态为「已修复」

## 注意事项

- L-069 和 L-072 都涉及序列化/字符串化，修复时需协调两者的格式一致性。
- `序列化JSON` 当前在 `stdlib/JSON.py`（或 `.light`），`转字符串` 是 code_generator 内置映射（`str()`），两者实现路径不同，需分别定位。
- L-072 若改变 `转字符串` 输出格式，需全仓排查 `转字符串` 调用点（尤其错误消息拼接、日志输出），确认不破坏依赖。
- 中文不转义：JSON 输出中中文字符应直接输出（`ensure_ascii=False`），不应变成 `\uXXXX`。
