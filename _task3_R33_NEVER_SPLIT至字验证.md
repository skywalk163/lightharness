# 第33轮 任务3 交付报告 —— _P0A_NEVER_SPLIT「至」字逐条验证

> 日期：2026-09-15 ｜ 优先级 P0 ｜ 修改区域：`light-merge/src/lexer.py` 的 `_P0A_NEVER_SPLIT`
> 结论：**可删**。撤除后全语料 token 零变化；「至」移除后进入 F/HM，但语料形态为数字范围独立汉字段，词首/词尾并入均不触发。

---

## 1. 目标
对 `_P0A_NEVER_SPLIT` 中的「至」字逐条验证，判定是否可移除（三重判据 G1∧G2∧G3）。

## 2. 场景定位（语料分类）
- **数字范围（独立汉字段）**：`1至10` / `甲至10` —— 「至」前后为数字/变量，属「整串即关键字」兜底，不依赖 NEVER_SPLIT。
- **复合名成分（词尾）**：至于 / 至少 / 甚至 —— 「至」在词尾，由 F（词尾并入类别）兜底整词；语料中「至X」词首起始形态极少。
- **函数调用 / 变量名**：无高频「至」收尾标识符。

## 3. 三重判据
| 判据 | 口径 | 结果 |
|---|---|---|
| **G1 语料门** | 撤「至」后全语料 tokenize 逐文件 sha 比对 | **通过**：变化文件 = 0 / 857，新增错误 = 0 |
| **G2 编译门** | 含「至」语句可编译 | **通过**：合成探针 `从1至10` tokenize 无异常；全语料 token 流一致 ⇒ 解析一致 |
| **G3 边界门** | 六类边界形态 token 流不变 | **通过**：`乃至`（至 词尾）基线/变体均为单 IDENTIFIER；数字范围 `1至10` 不受影响 |

### 关键发现：至 进入 F/HM，但语料不受影响
活性探针确认：撤「至」后 `NEVER_SPLIT True→False`、`F False→True`、`HM False→True`、`_P0A_SUFFIX_SPLIT_SINGLE True→False`。
移除使「至」由「后缀排除集」移入 F 与 HM。但「至」语料唯一形态为**数字后独立汉字段**（后随数字非汉字），词首并入（需后随汉字）与词尾并入（需前导汉字整词）均不触发，故全语料 token 流零变化。

## 4. 验证方法
- 引擎 `_antirun_r33_t1234_NEVER_SPLIT逐字验证.py`：快照基线 + 单字变体，全语料比对。
- 单字脚本 `_antirun_r33_t3_至字验证.py` → `_antirun_r33_t3_至字验证.json`（变化=0）。
- 活性探针 `_r33_probe_active.py`：确认 F/HM/SUFFIX 派生集差异与边界形态。

## 5. 执行删除
- 已应用：`_P0A_NEVER_SPLIT` 清零（含「至」）。
- **HM 自校验更新**：「至」合法进入 HM，文末断言已扩充为 `(R26∪R27∪R28 移除 ∪ {'步','至','到'})`，import 自检通过。

## 6. 铁律核对
- ✅ 只改「至」所在集合（与模/步/到并集清零）。
- ✅ 逐条隔离验证。
- ✅ G1∧G2∧G3 全通过。
- ✅ 删除后全量反跑零回归（`_antirun_r33_final_zeroregress.py`：0 变化 / 0 错误）。

## 7. 交付物
- `light-merge/src/lexer.py`
- `lightharness/_task3_R33_NEVER_SPLIT至字验证.md`
- `lightharness/_antirun_r33_t3_至字验证.py` + `_antirun_r33_t3_至字验证.json`
- 全量反跑证据：`_antirun_r33_final_zeroregress.json`
