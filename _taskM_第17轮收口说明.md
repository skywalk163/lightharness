# 第17轮路M收口说明（宿主接线原型方向）

> 日期：2026-09-13 ｜ 上游：`G:\github\deepseek-harness`（本地 HEAD 9d9035b7c1，基线 0.1.5-rc.2）
> 编译器：`G:\dswork\duan-light-merge\light-merge` ｜ 工作区：`G:\dswork\duan-light-merge\lightharness`（branch main）
> 前置：第16轮收口 `a409345`（对标109条 / 缺陷L-001~L-137 / pytest 296 passed）
> 本轮定位：**宿主接线原型**——前16轮已复刻上游 packages/ 下全部可复刻的纯逻辑面（109个对标功能），剩余均为 Cordis 宿主装配面/Node IO 面/进程外执行面/工具定义面，无法直接逐行复刻。本轮在光明语言中设计一套"虚拟宿主运行时"接线框架，定义 Context/Service/Events/插件生命周期/IO抽象/配置接线等接口层，使得已复刻的纯逻辑模块可以通过这层接口被组装和调用。**本轮是探索性/设计性任务，产出是可运行的原型模块+接口协议+测试验证，不是上游文件的逐行复刻。**

---

## 一、6路交付摘要

| 任务 | 域 | 光明模块 | 核心子系统数 | 新增行数 | 测试断言 | 反跑 | 新缺陷 |
|---|---|---|---|---|---|---|---|
| 1 | 宿主运行时与插件生命周期 | `src/宿主运行时.light` | 5 | 709 | ≥12 | 3/3 | 无(L-141/142空缺) |
| 2 | 宿主上下文与作用域 | `src/宿主上下文.light` | 5 | 533 | ≥12 | 3/3 | L-143 |
| 3 | 宿主事件接线 | `src/宿主事件.light` | 5 | 601 | ≥12 | 3/3 | 无(L-145/146空缺) |
| 4 | 宿主工具接线 | `src/宿主工具.light` | 5 | 654 | ≥12 | 3/3 | 无(L-147/148空缺) |
| 5 | 宿主IO抽象层 | `src/宿主IO.light` | 7 | 732 | ≥12 | 3/3 | 无(L-149空缺) |
| 6 | 宿主配置接线 | `src/宿主配置.light` | 6 | 634 | ≥12 | 3/3 | 无(L-150空缺) |

**合计**：6个新src模块 + 6个新测试 + 1个repro + 6个反跑脚本 + 6份交付报告 = 3863行新增代码。

### 各模块与已有模块的关系（互补，不重叠）

| 新模块（接线层/框架层） | 已有模块（具体功能实现） | 关系 |
|---|---|---|
| `宿主事件.light` | `事件.light`（99行，事件总线实现：发布/瀑布/监听器） | 互补：事件.light是总线实现，宿主事件是接线协议（事件类型注册表/绑定器/作用域过滤/错误隔离），只读导入事件.light |
| `宿主工具.light` | `工具.light`（463行，工具注册表+执行流水线） | 互补：工具.light是注册表+执行，宿主工具是接线协议（工具发现器/执行上下文注入/权限控制/分组命名空间），只读导入工具.light的校验参数 |
| `宿主配置.light` | `设置.light`（106行）/`凭据.light`（78行）/`启动配置.light`（341行） | 互补：设置/凭据/启动配置是具体功能，宿主配置是接线框架（配置注册表/多层加载器/变更事件/凭证接线），只读导入分层合并/脱敏值/凭证引用 |
| `宿主IO.light` | `子进程.light`（62行）/`文件系统工具.light`（535行）/`流.light`（157行） | 互补：子进程/文件工具是具体实现，宿主IO是抽象层（IO提供者接口/统一结果形状/取消令牌/超时重试/内存模拟） |
| `宿主运行时.light` | （无） | 全新 |
| `宿主上下文.light` | （无） | 全新 |

---

## 二、路M核验结果

### 2.1 6测试单独跑全 PASS rc=0

| 测试 | 结果 | rc |
|---|---|---|
| test_宿主运行时 | PASS | 0 |
| test_宿主上下文 | PASS | 0 |
| test_宿主事件 | PASS | 0 |
| test_宿主工具 | PASS | 0 |
| test_宿主IO | PASS | 0 |
| test_宿主配置 | PASS | 0 |

### 2.2 6反跑全 ALL OK（A/B/C三项判据全过）

| 反跑脚本 | A正常 | B边界 | C变异立红 | 字节级恢复 | 恢复后绿 |
|---|---|---|---|---|---|
| _antirun_t1_宿主运行时.py | ✓ | ✓ | ✓(拓扑排序退化为注册顺序 rc=1) | ✓(sha256 52cf9859) | ✓ |
| _antirun_t2_宿主上下文.py | ✓ | ✓ | ✓(查找链只查当前不查父 rc=1) | ✓(sha256 07b8c081) | ✓ |
| _antirun_t3_宿主事件.py | ✓ | ✓ | ✓(插件停止时不解绑 rc=1) | ✓(sha256 aa68351c) | ✓ |
| _antirun_t4_宿主工具.py | ✓ | ✓ | ✓(停止不自动注销 rc=1) | ✓(sha256 a6df4cb2) | ✓ |
| _antirun_t5_宿主IO.py | ✓ | ✓ | ✓(重试判定翻转 rc=1) | ✓(sha256 87815a32) | ✓ |
| _antirun_t6_宿主配置.py | ✓ | ✓ | ✓(加载优先级排序翻转 rc=1) | ✓(sha256 b8da246b) | ✓ |

### 2.3 文件互斥零越界

`git diff --name-only -- src/` 确认：6路均只新增文件，**未修改任何既有 src/ 模块**（特别是事件.light/工具.light/设置.light/凭据.light等已有模块——本轮是接线层，通过只读导入复用，不修改）。零越界。

### 2.4 1个repro全 rc=0

| repro | 缺陷 | 任务 | rc |
|---|---|---|---|
| examples/_repro_L143.light | L-143 作用域保留关键字 | 任务2 | 0 |

**注**：任务5初判 L-149（模块级函数无闭包捕获）经探针隔离确认为标识符笔误（参数中文`数据`，函数体误写英文`data`），修正后 append 正常生效，非语言缺陷不登记，L-149 空缺。原 _repro_L149.light 因断言期望与实际行为不符（期望长度0，实际长度1）导致 CI 红，已删除。

### 2.5 整合验证（本轮特有）

路M编写整合验证脚本 `examples/_taskM_integration_r17.light`，验证6个宿主接线模块可以共存、接口兼容：
- 同时导入6个模块，无命名冲突
- 运行时创建→启动→停止（状态机转换正确）
- 上下文创建→属性继承链（子继承父/子覆盖父/父看不到子）
- 事件接线器创建→事件类型注册→列举
- 工具接线器创建→工具注册→列举
- IO内存文件系统→写文件→读文件→文件存在→读不存在文件返回NOT_FOUND
- 配置注册表→注册配置项→设置配置→获取配置
- 所有创建函数返回非空对象

**结果**：22断言全PASS，rc=0。整合验证 ALL PASS。

**整合验证过程中发现的问题**：
- 整合验证脚本最初使用模块级变量+嵌套段落函数做断言计数，触发**嵌套段落函数变量捕获限制**（嵌套在`主`中的`断言`函数无法修改`主`的局部变量`断言计数`，计数始终为0）。注意：这与任务5初判的L-149（模块级函数无闭包）不同——L-149经探针隔离确认为标识符笔误，模块级段落函数可以正常修改模块级变量；但嵌套段落函数无法捕获外层段落函数的局部变量是真实存在的工程限制。
- 改用字典承载状态+作为参数传递，仍触发 L-123（字典下标赋值在函数中静默失败）。
- 最终改用极简版（失败即抛错，全部通过打印ALL PASS），验证通过。
- 这确认了 L-123（字典下标赋值）和嵌套段落函数变量捕获限制对宿主接线原型的影响——事件监听器/IO回调/配置变更回调中需要累积状态时，必须用传入字典承载且用 `字典设置` 函数修改键值，不能用模块级变量或直接下标赋值。

---

## 三、新缺陷留档（L-143）

### L-143（任务2 宿主上下文域）：作用域保留关键字

- **现象**：`作用域` 是光明保留关键字——不能作为变量名。`设 作用域 为 {}` 报「作用域是保留关键字，不能直接作为语句开头」；`作用域[键] 为 值` 同样报错。
- **同族缺陷**：与 L-119（L0单字别名静默保留字）/ L-120（标识符可被关键字序列完全切分）/ L-101（「回调」保留字）/ L-137（变量名以关键字「尝试」开头被断开）同族。
- **影响面**：任何需要用「作用域」命名变量/属性的场景——宿主上下文域的 scope 机制设计。
- **绕法**：变量名改 `域标记`（避开「作用域」关键字字样），属性键仍可用 `作用域` 字符串。
- **repro**：`examples/_repro_L143.light`（rc=0）

### 空缺编号

L-141/L-142（任务1）、L-144（任务2仅L-143）、L-145/L-146（任务3）、L-147/L-148（任务4）、L-149（任务5初判模块级函数无闭包，经探针隔离实为标识符笔误——参数中文`数据`函数体误写英文`data`，修正后append正常生效，非语言缺陷不登记）、L-150（任务6）空缺顺延。

---

## 四、行为差异 R17-D1~D6

| 编号 | 任务 | 核心差异 | 状态 |
|---|---|---|---|
| R17-D1 | 任务1 | Cordis插件基于TS类继承+装饰器+DI容器，光明用字典形状+段落函数表达，内存模拟 | ✅ 原型可用 |
| R17-D2 | 任务2 | Cordis Context用TS声明合并，光明用属性字典+父上下文引用表达继承链；作用域用运行时匹配函数；L-143作用域保留关键字 | ✅ 原型可用 |
| R17-D3 | 任务3 | Cordis Events用TS声明合并，光明用事件类型注册表字典；事件绑定显式函数调用；基于已有事件.light | ✅ 原型可用 |
| R17-D4 | 任务4 | defineTool是Cordis装饰器+Context注入，光明用工具声明字典+注册函数；执行上下文显式创建；权限检查显式函数；基于已有工具.light | ✅ 原型可用 |
| R17-D5 | 任务5 | Node.js fs/child_process/net/stream，光明用段落函数接口+内存模拟；初判L-149经探针隔离实为标识符笔误非缺陷 | ✅ 原型可用 |
| R17-D6 | 任务6 | settings基于Cordis服务+schemastery，光明用配置项字典+注册表；多层加载器显式函数；基于已有设置/凭据/启动配置 | ✅ 原型可用 |

---

## 五、docs三件回填

| 文档 | 追加内容 | 当前状态 |
|---|---|---|
| `docs/功能对标/对标清单.json` | #110~#115（6条，标注为"原型设计非上游逐行复刻"） | 115条 |
| `docs/功能对标/语言缺陷账.md` | L-143（1条+空缺说明，L-149确认为非缺陷空缺） | L-001~L-143（L-141/142/144/145~L-150空缺） |
| `docs/功能对标/行为差异清单.md` | R17-D1~D6（6条，标注为"原型设计非逐行复刻"） | R11~R17 |

---

## 六、git 提交链

```
a409345  收口(第16轮): 对标回填 #104~#109 / L-131/L-133/L-135/L-137 / R16-D1~D6 (pytest 296全绿)
0e16e4c  任务1(宿主运行时域): 虚拟宿主运行时原型-运行时状态机/插件接口/插件加载器/服务注册表
e0bfeba  任务2(宿主上下文域): 虚拟宿主上下文原型-Context数据结构/属性继承链/作用域机制/上下文传递/快照恢复(L-143)
d863d6a  任务3(宿主事件域): 虚拟宿主事件接线原型-事件类型注册表/事件绑定器/作用域过滤扇出/错误隔离/事件统计
12a322c  任务4(宿主工具域): 虚拟宿主工具接线原型-工具发现器/执行上下文注入/权限控制/分组命名空间
f08fccf  任务5(宿主IO域): 虚拟宿主IO抽象层原型-IO结果统一形状/取消令牌/超时重试/文件系统/进程提供者接口内存模拟(L-149初判经探针确认为标识符笔误非缺陷空缺)
9793680  任务6(宿主配置域): 虚拟宿主配置接线原型-配置注册表/多层加载器/配置变更事件/凭证接线/配置验证器
[待提交]  收口(第17轮): docs三件回填 + 收口说明(CI全绿)
```

---

## 七、全量CI结果

**CI全绿**（2026-09-13，耗时 6分50秒）：

| 项目 | 结果 | 详情 |
|---|---|---|
| pytest | **304 passed** | 296基线 + 6新测试 + 1repro(L-143) + 1整合验证(_taskM_integration_r17) = 304；rc=0 |
| smoke | **5/5 通过** | test_会话/test_代理/test_工具/test_消息/test_流 全通过 |
| 总耗时 | 410.03s (0:06:50) | pytest 411.3s + smoke ~8s |

**CI首轮红1项已修复**：首轮 `test_example_exit_code[_repro_L149.light]` 失败（断言期望长度0，实际长度1）。根因：任务5初判L-149（模块级函数无闭包）经探针隔离确认为标识符笔误（参数中文`数据`，函数体误写英文`data`），修正后append正常生效(length=1)，非语言缺陷不登记。但执行者仍创建了断言期望为0的repro文件，导致CI红。修复：删除_repro_L149.light，缺陷账L-149空缺，docs三件修正。复跑全绿。

**已知 flaky 未复现**：test_审批.light（第14轮发现偶发 rc=1）本轮未复现，全量CI一次通过。

---

## 八、宿主接线原型的架构总结

### 8.1 6模块协作关系

```
宿主运行时（骨架）
  ├── 服务注册表 → 注册/获取所有服务
  ├── 插件加载器 → 按依赖拓扑排序加载插件
  └── 运行时事件 → 启动/停止/插件生命周期事件
       │
       ├── 宿主上下文（数据载体）
       │    ├── 属性字典 + 父上下文引用（继承链）
       │    ├── 作用域标记（全局/agent/session）
       │    └── 快照/恢复（测试/回滚）
       │
       ├── 宿主事件（通信机制）
       │    ├── 事件类型注册表（emit/waterfall双模式）
       │    ├── 事件绑定器（插件自动绑定/解绑）
       │    ├── 作用域过滤（复用宿主上下文作用域匹配）
       │    └── 错误隔离（单监听器失败不影响其他）
       │
       ├── 宿主工具（能力注册）
       │    ├── 工具发现器（插件自动注册/注销）
       │    ├── 执行上下文注入（取消令牌/请求ID/作用域）
       │    ├── 权限控制（可见性/审计/速率限制）
       │    └── 分组命名空间（工具名冲突处理）
       │
       ├── 宿主IO（外部交互）
       │    ├── IO结果统一形状（成功/失败/错误码表）
       │    ├── 取消令牌（IO操作中途取消）
       │    ├── 超时与重试（重试策略/退避延迟）
       │    ├── 文件系统提供者接口（内存模拟）
       │    └── 进程提供者接口（内存模拟）
       │
       └── 宿主配置（参数管理）
            ├── 配置注册表（键/类型/默认值/校验/可变性）
            ├── 多层加载器（命令行>环境变量>配置文件>默认值）
            ├── 配置变更事件（变更前可否决/变更后）
            ├── 凭证接线（提供者注册/解析/脱敏）
            └── 配置验证器（启动时验证）
```

### 8.2 原型的局限性（待后续轮次解决）

1. **内存模拟**：IO文件系统/进程提供者都是内存模拟，实际IO（Node fs/child_process/net）需要宿主注入。
2. **无实际插件加载**：插件加载器是内存中的插件定义字典，没有从文件系统动态加载插件的能力。
3. **无异步**：所有操作都是同步的，没有异步/并发/事件循环。上游 Cordis 大量使用 Promise/async-await，光明原型需要异步原语才能完整模拟。
4. **嵌套段落函数变量捕获限制**：嵌套在段落函数内的段落函数无法捕获外层段落函数的局部变量（注意：这与L-149不同，L-149经确认为非缺陷；模块级段落函数可以正常修改模块级变量）。事件监听器/IO回调/配置变更回调中需要累积状态时，必须用传入字典承载且用`字典设置`函数修改键值。
5. **无网络/流实际实现**：IO抽象层定义了网络/流接口，但没有实际实现（内存模拟只覆盖了文件系统和进程）。

---

## 九、后续方向建议

本轮宿主接线原型验证了光明语言中设计虚拟宿主框架的可行性。后续轮次可选方向：

1. **缺陷修复轮（用户已询问）**：集中修复语言缺陷账中的高频缺陷，特别是 L-123（双层下标赋值静默失败）、L-135（字典缺失键抛KeyError）、L-137/L-143（关键字/保留字）、嵌套段落函数变量捕获限制等对宿主接线原型影响最大的缺陷。
2. **宿主接线深化**：将已有纯逻辑模块（如会话.light/工具.light/设置.light）适配到宿主接线框架上，作为插件注册到宿主运行时，验证端到端组装。
3. **IO抽象层深化**：完善网络/流的实际实现（或更完整的内存模拟），补充HTTP客户端/WebSocket/流管道等。
4. **异步原语探索**：探索光明语言中的异步/并发原语设计（如 Promise/async-await/事件循环的光明等价物）。
5. **上游增量差异分析**：上游本地 HEAD 已从 a305303422 更新至 9d9035b7c1，需做增量差异分析。

---

## 十、教训与注意事项

1. **原型任务的API不稳定性**：本轮是探索性原型，各模块的API（函数名/参数/返回值形状）由执行者设计，路M整合验证时需要实际查看各模块的导出函数才能正确调用。后续轮次如果做宿主接线深化，需要先统一API规范。
2. **嵌套段落函数变量捕获限制对回调设计的影响**：整合验证过程中遇到嵌套段落函数无法捕获外层段落函数局部变量的问题（注意：L-149经确认为非缺陷，模块级段落函数可以正常修改模块级变量）。这是宿主接线原型中事件/IO回调设计的工程限制——事件监听器/IO回调/配置变更回调中需要累积状态时，必须用传入字典承载且用 `字典设置` 函数修改键值。
3. **内联函数在字典字面量内的语法限制**：整合验证脚本最初尝试在字典字面量内直接定义内联函数作为监听器/执行函数，触发语法错误。光明语言的内联函数可能不支持在字典字面量内直接定义，需要用模块级段落函数。
4. **列举函数返回值形状不一致**：`列举事件类型` 返回字符串列表（事件名），`列举工具` 返回字典列表（工具定义），各模块的列举函数返回值形状不统一。后续深化时需要统一。
5. **反跑脚本恢复必须放 finally/异常安全路径**：第13轮教训，本轮6个反跑脚本均遵守。
6. **文件互斥铁律**：本轮特别强调不修改已有模块（事件.light/工具.light/设置.light/凭据.light），6路均通过只读导入复用，零越界验证通过。
