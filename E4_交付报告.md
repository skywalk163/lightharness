# E4 交付报告：权限与审批框架

## 任务目标

实现工具调用的权限控制与用户审批机制：agent 执行危险操作前需要用户确认。包含权限预设 + 审批队列 + 审批 API + Web 审批 UI。

## 交付物

| 文件 | 类型 | 说明 |
|---|---|---|
| `src/权限.light` | 新建 | 4 种权限预设 + 工具分类 + bash 命令分类 + 危险命令检测 |
| `src/审批.light` | 新建 | 审批管理器（请求/处理/等待/列表/清理）+ 统计与查询辅助 |
| `src/agent.light` | 修改 | 新增 `审批上下文`、`审批钩` 类；`注册默认钩子` 扩展 3 个可选参数（向后兼容） |
| `src/web服务器.light` | 修改 | 新增审批/权限 6 个处理方法 + 5 条路由 + SSE 审批请求事件 |
| `webui/index.html` | 修改 | 审批按钮（徽章计数）+ 权限预设下拉 + 右侧可折叠审批面板 |
| `webui/app.js` | 修改 | 审批渲染/批准/拒绝/3s 轮询/SSE 事件识别/权限预设读写 |
| `webui/style.css` | 修改 | 追加 E4 审批面板样式（约 160 行） |
| `examples/test_权限.light` | 新建 | 13 用例 |
| `examples/test_审批.light` | 新建 | 13 用例 |
| `examples/test_审批钩.light` | 新建 | 5 用例（agent 集成） |
| `examples/test_审批API.light` | 新建 | 7 用例（Web API） |
| `examples/test_审批深化.light` | 迁移 | 旧策略式 API 迁移到 E4 契约（统计+查询） |

## 核心设计

### 权限预设（4 种）

| 预设 | 行为 |
|---|---|
| 全放行 | 所有工具直接执行 |
| 按类别（默认） | 安全类放行 / 修改类审批 / 危险类拒绝 |
| 全拒绝 | 所有工具直接拒绝 |
| 自定义 | 每工具配置（放行/审批/拒绝），未配置走按类别逻辑 |

- 工具分类：读文件/搜索/ls 放行；写文件/删除审批；bash 按命令分类
- bash 分类：只读命令表（ls/cat/grep/...）放行；修改命令表（rm/mv/...）+ 网络命令表（curl/wget/...）审批；git/npm/pip/docker/kubectl 子命令前缀匹配；未知命令保守审批
- 危险命令：子串匹配（rm -rf /、mkfs、dd、fork bomb、chmod 777 /、> /dev/sda、shutdown 等），**优先于一切分类直接拒绝**
- 环境变量：`HARNESS_PERMISSION_MODE`（默认 按类别）、`HARNESS_APPROVAL_TIMEOUT`（默认 300）

### 审批队列

- 审批 id：`approval_微秒时间戳_6位hex`，用全局递增计数器 + 微秒保证进程内唯一（修复了 `time.time()` 精度不足导致的同毫秒碰撞）
- `请求审批 → 处理审批（幂等）→ 等待结果（0.1s 轮询，超时默认拒绝并落已处理状态"超时"）`
- 会话隔离：待审批/已处理列表支持按会话 id 过滤
- 并发模型：HTTP 服务是异步的，`处理审批` 来自另一连接；等待者轮询内存字典（CPython GIL 保证原子性）

### agent 集成（不改主循环）

- 光明语无闭包/嵌套函数 → 用**类 + 方法引用**：`审批钩.裁决` 是 bound method，签名 `裁决(工具名, 参数json) → {"批准":..,"需审批":..,"理由":..}` 完全对齐 E1 契约 4
- 光明语无嵌套属性赋值语法 → `设 己.X.Y 为 值` 需写成 `己.X.Y 为 值`
- `注册默认钩子(钩子)` 单参数调用仍工作（E5 兼容）；`(钩子, 审批管理器, 权限管理器, 上下文)` 四参数时自动注册 pre_tool 审批钩

### Web API（契约 3）

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/approvals?session=xxx` | 待审批列表 |
| GET | `/api/approvals/history?session=xxx` | 已处理历史 |
| POST | `/api/approvals/:id` | 处理审批（body: `{"批准":true/false,"理由":""}`） |
| GET | `/api/permissions` | 当前预设 + 超时 |
| POST | `/api/permissions` | 切换预设/更新超时/自定义表/追加危险模式 |

SSE 新增事件：`{"种类":"审批请求","审批id":"...","工具名":"bash","参数":{...},"会话id":"...","轮次":3}`

### WebUI

- 顶栏：权限预设下拉（读取/保存 `/api/permissions`）+ 审批按钮（红色徽章显示待审批数）
- 右侧可折叠审批面板：卡片含工具名红色标签 + 参数 JSON 摘要 + 批准（绿）/拒绝（红，弹窗输入理由）
- 双通道更新：SSE 事件驱动 + 3 秒轮询兜底；新审批自动展开面板
- 拒绝理由作为工具结果回传 LLM（`用户拒绝: ...`）

## 验证结果

| 测试 | 用例数 | 结果 |
|---|---|---|
| `examples/test_权限.light` | 13 | ✅ 全绿 |
| `examples/test_审批.light` | 13 | ✅ 全绿 |
| `examples/test_审批钩.light` | 5 | ✅ 全绿 |
| `examples/test_审批API.light` | 7 | ✅ 全绿 |
| `examples/test_审批深化.light`（迁移） | 全部断言 | ✅ 通过 |
| `examples/test_agentE5钩子.light`（E5 兼容） | 3 | ✅ 通过 |
| **全量回归 `py -3.10 -m pytest tests/ -q`** | **198** | ✅ **198 passed**（基线 195 不破坏） |

## 验收标准对照

- [x] 权限预设：4 种可切换，"按类别"默认
- [x] 工具分类：读/搜索放行，写/bash 修改审批，危险拒绝
- [x] 危险命令检测：`rm -rf /`、`mkfs` 等直接拒绝，不进入审批
- [x] 审批请求：SSE 推送审批请求事件
- [x] 审批等待：agent 循环暂停，超时（可配，默认 300s）默认拒绝
- [x] 审批处理：POST 处理后 agent 恢复
- [x] 批准后执行 / 拒绝后跳过（理由回传 LLM）
- [x] 审批 UI：右侧面板 + 批准/拒绝按钮
- [x] 审批历史：已处理结果与理由可查
- [x] 全放行/全拒绝模式：不生成审批项
- [x] 权限配置：预设切换 + 自定义模式
- [x] 并发安全：多会话审批项按会话 id 隔离
- [x] 全量回归不破坏：198 passed

## 已知限制与说明

1. 审批队列仅存内存（文档明确：重启丢失，不需持久化）
2. 等待审批期间当前 HTTP 连接被同步阻塞（光明语运行智能体是同步调用）；其他连接可正常处理审批
3. 自定义权限表当前在内存中，`data/permissions.json` 持久化未实现（POST /api/permissions 可写但重启丢失）
4. 旧样例 `test_审批深化.light` 已迁移到 E4 新契约（旧策略式 API 由 `权限.light` 取代），迁移逻辑保留统计/查询语义
