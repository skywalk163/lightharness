# 任务2 交付报告：宿主上下文与作用域原型（src/宿主上下文.light）

> 任务来源：`G:\dswork\duan-light-merge\复刻_第17轮_任务prompt分发_宿主接线原型 (1).md` 任务2
> 日期：2026-09-13 ｜ 对标 #111（**原型设计**，上游参考 Cordis Context 类型增强、packages/scope 作用域过滤、core/session scope-filtered dispatch——非逐行复刻）
> 命名预检查：src/ 下无 宿主上下文.light 同名模块 ✓

---

## 一、接口设计（设计决策登记，R17-D2）

| 接口 | 形状 | 设计决策 |
|---|---|---|
| 上下文形状 | {标识, 父(上下文\|空), 作用域, 属性: {}, 创建时间, 销毁时间: 空, 已销毁: 假, 根, 下一序号(仅根)} | 根持有计数器；子标识=`根标识-序号`（链上溯根分配，唯一性测试专条）；上下文可变性=字典内容原地改（第15轮 L-082/L-123 后原语可用） |
| 属性查找链 | 获取属性：当前→父→…→根；未命中返回 空（不抛） | 递归委托；**反跑 C 锚点**（递归委托行改返回空即断链） |
| 自有属性 | 自有属性(键)：仅当前层 | 与 获取属性 的链式语义区分 |
| 删除属性 | 仅当前层；不存在不崩溃 | 父同名属性不受影响 |
| 列举属性 | 当前+父链全部属性名，去重、子层键在前 | 子覆盖父的名字视图 |
| 快照/恢复 | 快照=**当前层自有属性**深拷贝；恢复=清当前层+写回快照 | 设计决策：恢复只作用于当前层（父链不受影响），配合「测试前快照→测试后恢复」防污染语义；父链变更不受子层恢复影响（测试专条） |
| 销毁 | 销毁(时间)→已销毁标记+销毁时间；销毁后 设置/造子 抛 `上下文已销毁`；**读取仍可用** | 读后不失效为设计决策（销毁=写保护，登记 R17-D2） |
| 作用域形状 | {类型, 目标ID, 父作用域} | 类型如 global/agent/session/tool |
| 作用域匹配 | 监听器 global→匹配所有；同类型同目标→匹配；监听器是事件作用域祖先（沿事件父链上溯）→匹配；空事件作用域仅被 global 匹配 | 供宿主事件（任务3）的作用域过滤复用 |
| 作用域链 | 自身→…→global 的 `类型:目标ID` 列表 | 诊断/遍历用 |
| 传递 | 传递上下文(钩子, 上下文)：钩子接收上下文参数 | 插件钩子/服务方法的统一传递形态（与任务1 插件上下文一致） |

## 二、测试验证

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"; python 运行.py examples/test_宿主上下文.light
test_宿主上下文 PASS   （rc=0）
```

`examples/test_宿主上下文.light` 覆盖 6 组 ≥45 断言：基本操作与继承（A 判据链：子覆盖→删子回落父、自有属性区分、删除/获取不存在的边界、三级查找链、列举去重）、标识唯一性（根计数器：三子 ctx-b-1/2/3）、快照/恢复（快照含自有、变更生效、恢复清临时/回归自有/父链不受影响、恢复后设置正常）、销毁语义（标记/时间/销毁后设置与造子抛/读后不失效）、作用域匹配（B 判据：全局/同域/祖先/跨代理不匹配/空事件域/作用域链三层）、上下文传递（钩子经传递取链上属性）。

## 三、反跑结果（3/3 ALL OK）

`lightharness/_antirun_t2_宿主上下文.py`（BASE 自定位；字节级备份→变异→跑本路测试→断红→恢复→断绿；恢复置于 try/finally）：

```
PASS C=变异 查找链只查当前不查父 -> 红 (变异后运行 rc=1（期望非 0，真红）)
PASS 字节级恢复校验 (sha256 07b8c0817ffb)
PASS A/B 判据（父子查找链 + 作用域匹配组合）回归绿 (rc=0)
ALL OK
```

A=正常（父设置→子覆盖→删除子→回落父）与 B=边界（global→agent→session 匹配组合）为测试内场景，随回归绿验证；C=变异（获取属性 的递归委托行 `返回 获取属性(父, 键)` 改 `返回 空`——只查当前不查父）立红、恢复后绿。

## 四、未移植项 / 与上游 Cordis 的架构差异（R17-D2）

- Cordis Context 的 服务解析（ctx[service] 属性访问）/事件派发集成（ctx.emit 作用域过滤派发）——事件派发由宿主事件原型（任务3）承担，本模块提供 作用域匹配 原语。
- Cordis 的 运行时回收/waterfall 收集器——原型无。
- packages/scope 的 `@dshScopeScan` 装饰器/Scoped<T> 类型层——以形状字典 + 匹配函数承载。
- 上下文继承的写穿透/写隔离策略——原型固定「写当前层、读沿链向上」。
- 弱引用/GC 语义——销毁为标记+写保护（读后不失效）。

## 五、语言差异（本轮新缺陷登记）

- **L-143（按任务2预分配区间，本轮唯一新缺陷）**：`作用域` 是**保留关键字**——不能作为变量名（`设 作用域 为 …`/`作用域[键] 为 值` 报「作用域是保留关键字，不能直接作为语句开头」）。绕法：变量名改 域标记。最小复现 `examples/_repro_L143.light`（绕法形态 rc=0）。L-144 空缺顺延。
- 开发中另确认 import 语句 `从` 后必须与模块名空格分隔（`从小域类型 导入` 触发 L-120 同族词法合并）——标准空格形态正常，不重复登记。

## 六、移交清单

- `lightharness/src/宿主上下文.light`（新增，≈200 行）
- `lightharness/examples/test_宿主上下文.light`（新增，PASS rc=0）
- `lightharness/_antirun_t2_宿主上下文.py`（反跑 3/3 ALL OK，BASE 自定位）
- `lightharness/examples/_repro_L143.light`（L-143 绕法形态复现，rc=0）
- 本报告。
- 移交路M：对标清单新增 **#111**（宿主上下文与作用域原型，标注「原型设计」）；缺陷账 **L-143** 登记（L-144 空缺顺延）；行为差异清单 **R17-D2**；CI 增 1 个 test_ 文件 + 1 个 repro（302+ 占 2）。
