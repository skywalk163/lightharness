# 任务3（R42·P0）交付报告 —— bash工具跨平台

> 日期：2026-09-16 ｜ 负责人：主Agent查收重修 ｜ 状态：**完成（Windows cmd + 0.82 /bin/sh 双平台 rc=0）**

---

## 一、查收时发现的问题（已修）
初版 `test_R42_bash跨平台.light` 无法运行（rc=1）：
1. **导入方式错**：写成 `从 G:/.../工具_bash.light 导入 执行bash`（文件系统路径导入），光明要求模块名导入 `从 工具_bash 导入 ...`。
2. **符号名错**：`执行bash` 不存在——工具_bash.light 实际导出 `bash处理`，参数键是中文 `"命令"`（非 `command`）。
3. **平台特有命令**：用了 `pwd`/`ls`/`dir`，不跨平台。
已重写：模块名导入 `bash处理`，跨平台只用 `echo`/`cd`（两平台都认），平台用 `COMSPEC`/`SHELL` 环境变量判定。

## 二、验证结果
| 平台 | shell | echo真实子进程 | cd逃逸护栏 | 结果 |
|---|---|---|---|---|
| Windows 10 本机 | C:\Windows\system32\cmd.exe | rc=0，输出含marker | `cd ..`/`cd /tmp` 均退出码-1拒 | ✅ rc=0 |
| 0.82 FreeBSD 15.1 | /bin/sh | rc=0 | cd逃逸护栏生效 | ✅ rc=0 |

## 三、平台差异记录
- 工作目录：Windows cmd vs POSIX sh，工具_bash 内部统一 `取根目录()` 固定工作目录，两平台一致。
- 跨平台命令：只选 `echo`/`cd`（两平台都认）；`ls`/`dir`/`pwd` 不通用，不在断言里混用。
- cd逃逸正则两平台同一条（拒绝 `cd ..`/绝对路径/`~`/盘符），两平台都正确拒。
- 退出码语义一致（成功0，护栏拒-1）。

## 四、铁律遵守
- 未改 `工具_bash.light`（已真实）；只验证。
- cd逃逸护栏两平台都验证生效。
- 平台差异如实记录。
