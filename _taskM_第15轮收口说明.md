# 第15轮路M收口说明

> 日期：2026-09-13 ｜ 上游：`G:\github\deepseek-harness`（本地 HEAD 9d9035b7c1，任务书标注 a305303422=0.1.5-rc.2，上游有更新但复刻文件无变化）
> 编译器：`G:\dswork\duan-light-merge\light-merge` ｜ 工作区：`G:\dswork\duan-light-merge\lightharness`（branch main）
> 前置：第14轮收口 `31d6a31`（对标97条 / 缺陷L-001~L-120 / pytest 276 passed）

---

## 一、6 路交付总览

| 任务 | 域 | 上游源（文件数） | 光明模块 | 测试断言 | 反跑 | 新缺陷 |
|---|---|---|---|---|---|---|
| 1 | 会话引用深化 | context/session-reference（5文件，剔除index装配；uri.ts复用#33） | `src/会话引用深化.light` | — | 3/3 | 无（L-121/L-122空缺） |
| 2 | 存储核心与JSON | storage(3) + storage-json(3)，剔除atomic fs依赖 | `src/存储核心.light` | — | 3/3 | **L-123** |
| 3 | 附件与反馈 | attachment(5) + command-feedback(2) + message-feedback(2) | `src/附件反馈.light` | 148 | 3/3 | **L-125** |
| 4 | 调度深化与任务守卫 | schedule深化(3) + jobs核心(2) + guard(2) | `src/调度深化.light` | 128 | 3/3 | **L-127** |
| 5 | 预设深化与设置技能角色 | preset深化(3) + persona(1) + settings(2) + skill(2) | `src/预设深化.light` | 150 | 3/3 | **L-129** |
| 6 | 协议深化——ACP+钩子 | acp深化(3) + hook-protocol深化(4) | `src/协议深化.light` | 159 | 3/3 | 无（L-130空缺） |

**文件互斥零越界**：6 路均只新增文件，未修改任何既有 `src/` 模块（git diff --name-only -- src/ 无既有文件）。

---

## 二、分路提交链

```
9e86707  任务6(协议深化域): acp mcp/模型控制/更新 + hooks分离/事件/运行器/类型（用户先行提交）
66ca499  任务1(会话引用深化域): session-reference config/序列化/投影/溢出/类型（命名冲突改深化名复用#33 URI编解码；R15-D1；反跑3/3）
083d06b  任务2(存储核心域): storage后端/错误/注册表 + storage-json格式/单记录/单文件单元（介质字典模拟；L-123；R15-D2；反跑3/3）
695d24c  任务3(附件反馈域): attachment准入/品牌/错误/请求投影/类型 + command/message-feedback（L-125；R15-D3；反跑3/3）
3c28635  任务4(调度深化域): schedule投影/事务/类型 + jobs品牌/类型 + guard重复工具提醒/超时策略（L-127；R15-D4；反跑3/3）
8ef5843  任务5(预设深化域): preset组合清单/显示/类型 + persona + settings脱敏/类型 + skill注册/徽章（L-129；R15-D5；反跑3/3）
```

注：任务6由执行者先行提交（9e86707），路M验证完整性后确认；任务1-5由路M分路提交。

---

## 三、路M 过程中发现并裁定的问题

### 3.1 任务1 命名冲突（执行者已正确处理，路M裁定确认）

- **现象**：任务书写作 `src/会话引用.light`，但该文件已存在（提交 91658b3，早期轮次 #33 覆盖 uri.ts 全部四个函数 + 转义标签/去转义标签，120 行）。
- **处置**：任务1执行者正确地按铁律6命名预检查，改用 `src/会话引用深化.light`，并**只读导入**既有 `会话引用.light` 的 URI 编解码能力复用（不重复实现），只复刻未覆盖的 config/serialization/projection/spill/types 纯逻辑面。路M 裁定确认此处理正确（与第14轮任务6命名冲突改族名先例一致）。

### 3.2 任务6 先行提交（路M验证确认）

- **现象**：任务6（协议深化）由执行者先行提交为 9e86707，包含 src/测试/反跑/报告 4 文件，完整无缺。
- **处置**：路M验证提交完整性（4文件、1264行）、测试单独跑 PASS rc=0、反跑 ALL OK，确认无需补提交。

### 3.3 上游仓库版本差异（记录，不影响复刻）

- **现象**：任务3/4/5/6 交付报告标注上游本地 HEAD 为 `9d9035b7c1`，而任务书标注 `a305303422`（0.1.5-rc.2）。
- **处置**：上游仓库有更新，但本轮复刻的 6 个域的源文件无变化（执行者已逐文件核对），测试全绿，不影响复刻结果。记录为已知差异，后续轮次任务书应更新上游基线版本号。

---

## 四、新缺陷登记（L-123 / L-125 / L-127 / L-129）

### L-123（任务2 存储核心域）：双层下标赋值静默失败

- **现象**：`设 字典[键甲][键乙] 为 值` 双层下标赋值**静默失败**——编译产物不含内层键赋值，无任何报错，子字典保持原样。单层下标赋值正常。
- **绕法**：用 `字典设置(子字典, 键, 值)`（先单层取出子字典引用再设置），或先取子字典到局部变量再单层赋值。
- **repro**：`examples/_repro_L123.light`（rc=0）。

### L-125（任务3 附件反馈域）：四舍五入银行家舍入与 JS Math.round 不一致

- **现象**：内建 `四舍五入` 采用**银行家舍入（round-half-even，.5 时取最近偶数）**，与 JS `Math.round`（half-up，.5 一律进位）不一致。实测：`四舍五入(2.5)==2`、`四舍五入(0.5)==0`；JS `Math.round(2.5)===3`。
- **影响面**：任何由 JS `Math.round` 移植而来的取整——attachment/request-projection.ts 的 requestImageDimensions 几何收缩尺寸差 1 像素。
- **绕法**：`取整半数上(x) = 向下取整(x + 0.5)`，对非负数与 `Math.round` 逐值等价。
- **repro**：`examples/_repro_L125.light`（rc=0）。

### L-127（任务4 调度深化域）：字典对象身份键不可表达 + Python 原生 TypeError 未包装

- **现象**：光明字典键必须是可哈希标量；**对象/列表身份键不可表达**，且失败时抛出 **Python 运行时原生 TypeError**（`unhashable type: 'list'`），未经光明错误体系包装——既无中文诊断也不可被错误判据识别。
- **上游触发面**：schedule/transaction.ts 用 `WeakMap<Agent, Promise>` 以 Agent 对象身份为键做按归属串行化。
- **绕法**：以「归属令牌（字符串）」作键，把「尾是否仍属本事务」记录为标记值而非对象身份。
- **repro**：`examples/_repro_L127.light`（rc=0）。

### L-129（任务5 预设深化域）：空列表/空字典判假 + 0==假 真值语义与 JS 不一致

- **现象（两面同根：光明沿用 Python 语义）**：① 空列表/空字典**判假**（JS `Boolean([])`/`Boolean({})` 均为真）；② `0 == 假` 为真、`1 == 真` 为真（JS `0 === false` 为假），故不能用 `== 真/假` 判布尔。
- **影响面**：preset/composition-inventory.ts 的 `Boolean(value)` 直译会静默翻转 `[]`/`{}` 取值。
- **绕法**：显式实现 JS 真值表函数（`JS布尔(值)`），布尔判别用 `type(值) == bool`。
- **repro**：`examples/_repro_L129.light`（rc=0）。

**编号空缺说明**：L-121/L-122（任务1无缺陷）、L-124（任务2仅L-123）、L-126（任务3仅L-125）、L-128（任务4仅L-127）、L-130（任务6无缺陷）均空缺顺延。本轮共新增 4 项缺陷，缺陷账累计 L-001~L-130（含空缺）。

---

## 五、对标回填

对标清单 `docs/功能对标/对标清单.json` 新增 #98~#103：

| 编号 | 功能 | 光明模块 |
|---|---|---|
| #98 | context/session-reference 会话引用纯逻辑面 | `src/会话引用深化.light` |
| #99 | storage 存储核心与JSON纯逻辑面 | `src/存储核心.light` |
| #100 | attachment 附件 + feedback 反馈纯逻辑面 | `src/附件反馈.light` |
| #101 | schedule 调度深化 + jobs 任务核心 + guard 守卫 | `src/调度深化.light` |
| #102 | preset 预设深化 + persona + settings + skill | `src/预设深化.light` |
| #103 | acp ACP深化 + hooks 钩子协议深化 | `src/协议深化.light` |

对标清单累计 103 条。

---

## 六、行为差异回填（R15-D1~D6）

行为差异清单 `docs/功能对标/行为差异清单.md` 新增 R15 段：

| 编号 | 任务 | 差异要点 | 状态 |
|---|---|---|---|
| R15-D1 | 任务1 | Config 校验补充实现+错误码文案自拟；不可序列化分支不可达；命名冲突改深化名复用#33 | ✅ 已裁定 |
| R15-D2 | 任务2 | StorageError 结构化code→文本承载；KvUnit list/exists→loadAll快照推导；atomic fs依赖剔除用字典介质模拟 | ⚪ |
| R15-D3 | 任务3 | 四舍五入银行家舍入(L-125)→取整半数上绕法；attachment-local宿主面剔除 | ⚪ |
| R15-D4 | 任务4 | WeakMap对象身份键不可表达(L-127)→归属令牌字符串绕法；已覆盖域不重复 | ⚪ |
| R15-D5 | 任务5 | 空列表空字典判假+0==假(L-129)→显式JS真值表绕法；已覆盖域不重复；宿主面剔除 | ⚪ |
| R15-D6 | 任务6 | ACP JSON-RPC宿主面剔除只做消息形状；钩子运行器结果纯字典承载；已覆盖域不重复 | ⚪ |

---

## 七、全量 CI 结果

### 7.1 pytest 全量回归

| 运行方式 | 结果 | 耗时 |
|---|---|---|
| `python scripts/ci_test.py`（全量CI） | **pytest 286 passed + smoke 5/5，全部通过** | pytest 405.5s + smoke ~6s |

### 7.2 测试数变化

- 第14轮基线：276 passed（pytest）+ smoke 5/5
- 第15轮：**286 passed**（+10：6 新测试 + 4 repro：L-123/L-125/L-127/L-129）+ smoke 5/5
- 任务书期望 282+，实际 286（含 4 个 repro 文件也被 pytest 遍历）

### 7.3 已知 flaky 观察

- `test_审批.light`（第14轮发现的偶发 flaky）本轮全量 CI **未复现**，286 全绿。
- 持续观察中，若后续轮次频繁出现再考虑给 `test_回归.py` 的 `_run` 加重试机制。

---

## 八、遗留与待决策（延续）

1. **工具注册表接入点**（R12-D9/D12，第13轮决策1已落地）：维持已决策状态。
2. **approval/* 事件词汇**（第13轮决策2已落地）：已完成。
3. **#88/#35 驱动区块归并**（R13-D3，第13轮决策3已落地）：维持已决策状态。
4. **L-095 HMAC 标准实现**（第13轮决策4已落地）：编译器侧缺陷，维持自洽链路。
5. **L-116 序列化JSON 键序敏感**（第14轮）：待编译器侧修复。
6. **L-119 L0 单字别名静默保留字**（第14轮）：待编译器侧修复。
7. **L-120 标识符关键字全切分**（第14轮）：待编译器侧修复。
8. **L-123 双层下标赋值静默失败**（本轮新增）：建议编译器支持多层下标赋值或显式报错——待编译器侧修复。
9. **L-125 四舍五入银行家舍入**（本轮新增）：建议 `四舍五入` 对齐 JS half-up 或新增 `四舍五入半数上`——待编译器侧修复。
10. **L-127 字典对象身份键不可表达+Python原生错误未包装**（本轮新增）：建议支持对象身份键/弱映射原语，且不可哈希键错误经光明错误体系包装——待编译器侧修复。
11. **L-129 空列表空字典判假+0==假真值语义**（本轮新增）：建议条件真值对齐 JS 或新增 `JS真值` 原语，`==` 不做隐式类型转换——待编译器侧修复。
12. **上游基线版本更新**（本轮发现）：上游本地 HEAD 已从 a305303422(0.1.5-rc.2) 更新到 9d9035b7c1，后续轮次任务书应更新基线版本号并核对复刻文件是否有变化。
13. **test_审批.light 偶发 flaky**（第14轮发现）：全量运行时偶发 rc=1，单独跑全绿，疑似系统资源竞争——持续观察，若频繁出现再深入诊断（可考虑给 test_回归.py 的 _run 加重试机制）。
