# 第22轮 保护表通用化替代策略建议

> 日期：2026-09-14 ｜ 作者：第22轮任务6 ｜ 供后续轮次参考
> 背景：第22轮对保护表完成逐条隔离中立验证精简，删除53条冗余（IDENTIFIER_SAFE 17→5、_TRAILING_ALIAS_MERGE 10→1、CCW TierB 32条全部冗余 216→184）。**冗余的直接原因**是第21轮「非语句起始位置上下文敏感最大匹配」切词策略落地——逐词白名单是上下文规则缺位时代的补丁，规则一旦补齐，补丁即批量失效。

---

## 一、剩余保护表盘点与共性分析

| 表 | 现有条数 | 剩余条目共性 | 可通用化程度 |
|---|---|---|---|
| COMMON_COMPOUND_WORDS | 184 | ① 有 codegen 映射的内建复合名（异步读取文件/并发等待/低级关闭/常量时间比较 等）；② 剩余语料命中护栏（真护栏需逐条重估） | 高（①应迁出词法表） |
| IDENTIFIER_SAFE_KEYWORDS | 5 | 函数/输出/模块/打印/标准库——高频定义/I-O关键字作复合标识符后缀 | 中 |
| IDENTIFIER_SAFE_SUFFIX_ONLY_KEYWORDS | 3 | 同上子集，词首必须作关键字 | 低（语义上就是"词首关键字"规则，已是最小形态） |
| _TRAILING_ALIAS_MERGE | 1 | 己（L-120 词尾并入） | 高（上下文规则可覆盖） |
| _COMPOUND_SAFE_SINGLE_KEYWORDS | 52 | 单字关键字构词保护（数/列/串/典/加/减…），引用点多（_match_keyword/_skip_compound_safe/_tokenize_chinese_sequence/_P0A_COMPOUND_SAFE） | 中低（风险大，最后动） |

## 二、通用替代方案：上下文规则化

**核心规则（R22 建议，供第23轮"保护表通用化替代轮"验证）**：

> 非语句起始位置 + 前缀非运算符关键字 + 后随汉字/字母/数字 → 汉字串整体成 IDENTIFIER；
> 仅当整串精确等于关键字（或为登记的语句头）时才作 KEYWORD。

第21轮已落地该规则的「非语句起始位置」半边；剩余保护表保护的场景大多是它的自然推论：
- IDENTIFIER_SAFE（函数/输出等作后缀）：后缀位置天然满足"非语句起始"，规则可直接覆盖——第22轮删除的12条正是被它覆盖的实证；
- _TRAILING_ALIAS_MERGE：词尾并入是同一规则在单字别名上的特例（`己` 保留仅因 L-120 用例尚依赖旧行为，规则覆盖后可删）；
- CCW 剩余护栏：前缀不是关键字首字的复合词在 P0-A 确定性切词下已不拆分，真正需要白名单的只剩"前缀恰为关键字首字"的场景（如 `低级关闭` 的 `低`、`异步读取文件` 的 `异`）。

**分阶段路径（建议每步独立验证、独立反跑）**：
1. 阶段A：`己` 并入通用规则，删除 _TRAILING_ALIAS_MERGE 整表（10→0）；
2. 阶段B：IDENTIFIER_SAFE_KEYWORDS 5条用「后缀位置上下文规则」替代，逐条验证后清表；
3. 阶段C：CCW 中有 codegen 映射的复合名迁移到内建登记（builtin_map/保留名表），词法层不再白名单；
4. 阶段D（最远）：_COMPOUND_SAFE_SINGLE_KEYWORDS 52条单字——须先解决 `的` 递归吞分隔符等已知独立缺陷（见 lexer.py `幂` 注释），再做全仓 token A/B。

## 三、风险评估

| 风险 | 说明 | 缓解 |
|---|---|---|
| 合法「关键字+名」结构被误并 | R20 曾实测把 _EMBED 集合泛化到全部关键字引发 104 个解析回归（`接收参数`/`如果条件`） | 规则必须限定"非语句起始位置"，且排除运算符关键字（第21轮已验证此边界） |
| 单测契约破坏 | `标准库` 教训：语料中立≠无契约，合成串断言仍在 | 每删一条先跑 tests/unit/test_lexer*.py 全集 |
| 预扫描盲区 | 嵌套段落形参（L-152）无预扫描覆盖，规则依赖"非语句起始"判定须与预扫描口径一致 | 阶段B先行小范围验证 |
| 性能回退 | 上下文判定比集合 membership 慢 | P0-A 已有字符分类查表热路径；每步跑 test_lexer_perf |

## 四、收益与目标

- 最终目标：**消除所有逐词白名单**，词法层只保留"上下文规则 + 内建名登记"两类结构，与上游 deepseek-harness「无保护表」形态对齐；
- 每删一批即减少一次"新复合词→加白名单→旧词失效"的打地鼠循环（lexer.py 注释中 L-004 家族的原始痛点）；
- 建议第23轮优先做阶段A+B（风险低、证据充分），阶段C随内建治理推进，阶段D单独立轮。

## 五、证据链

- 第21轮：_task3_R21_保护表审计报告.md、_antirun_r21_t3_保护表精简.py（216条表审计+211条删除）
- 第22轮：_sweep_r22_t2_identifier_safe.py、_sweep_r22_t4_trailing_alias.py、_sweep_r22_t4_ccw_guard.py（53条逐条验证）、_antirun_r22_t2/_t4 反跑 ALL OK
- 本轮关键教训样本：R21 正向控制 `弹性模量` 在 R22 重估中转为冗余（上下文切词覆盖），是"规则替代白名单"路线的最直接实证。
