# 任务4 交付报告：R29 D批（其他类型 8条）CCW 逐条验证清单

> 任务来源：`G:\dswork\duan-light-merge\复刻_第29轮_任务prompt分发_CCW37条分批精简.md` 任务4
> 日期：2026-09-15
> 铁律：只改 D 批 8 条，逐条三重判据验证；全量反跑零回归；新增一律 .light。

---

## 一、场景定位清单（4.1）

D批8条在 lightharness/light-merge 全部 .light 文件中的出现场景分类：

| 条目 | 出现位置（真实命中） | 分类 | 说明 |
|---|---|---|---|
| 函数对象 | `src/团队折叠.light:33`、`src/团队花名册.light:47`、`examples/test_L055.light:1`、`examples/test_团队折叠.light:14` | 注释 | 全部为注释文本，无代码级依赖 |
| 常量时间比较 | `examples/test_R23_CCW内建名.light:19/37/38` | 用户定义函数名 | 测试文件内自定义段落名 + 调用，受预扫描覆盖 |
| 幂次 | — | 语料 0 命中 | 库内无整词出现；边界形态 G3 失败 |
| 枚举值 | `examples/test_R20_词法边界综合.light:73/164` | 用户定义函数名 | 测试文件内自定义段落名 + 调用，受预扫描覆盖 |
| 正则匹配 | `stdlib/度量.light:37/53`（段落+调用）、`:3/36`（注释） | 用户定义函数名 | stdlib 内自定义段落，受预扫描覆盖 |
| 环境枚举 | `stdlib/路径护栏.light:478`、`stdlib/进程树.light:478`（调用）；`代理工具集.light:588/699`、`路径护栏.light:40`（注释） | 内置函数调用 | 调用点在语句中非词首/预扫描覆盖，撤掉零变化 |
| 结构体值 | `stdlib/FFI.light:42` 导出「创建结构体值」 | 标识符组成部分 | 仅作「创建结构体值」一词的子串，整词零独立出现 |
| 记录类型 | — | 语料 0 命中 | 库内无整词出现；边界形态 G3 失败 |

## 二、逐条验证结果（4.2 三重判据）

判据：**G1** 语料（撤掉后全语料 850 文件 token 零变化）∧ **G2** 编译门（含该条目语句编译 rc 前后一致且 =0）∧ **G3** 边界门（六类边界形态 token 流不变）。**可删条件：G1∧G2∧G3 全通过。**

| 条目 | G1 | G2 | G3 | 结论 |
|---|---|---|---|---|
| 函数对象 | PASS | PASS | PASS | **可删** |
| 常量时间比较 | PASS | PASS | PASS | **可删** |
| 幂次 | PASS | PASS | FAIL（`幂次` → `幂`+`次`，被关键字"幂"劈开） | **保留** |
| 枚举值 | PASS | PASS | PASS | **可删** |
| 正则匹配 | PASS | PASS | PASS | **可删** |
| 环境枚举 | PASS | PASS | PASS | **可删** |
| 结构体值 | PASS | PASS | PASS | **可删** |
| 记录类型 | PASS | PASS | FAIL（`记录类型` → `记录`+`类型`，被关键字"类型"劈开） | **保留** |

逐条证据：`_task4_R29_D批验证_证据.json`（含 G1 变化文件明细、G2 rc、G3 失败 diff）。

## 三、删除清单（4.3 执行删除）

从 `light-merge/src/lexer.py` COMMON_COMPOUND_WORDS 删除 6 条（CCW 30 → 24）：

- 保留 A 段（3 条）：`常量时间比较`、`正则匹配`、`环境枚举`
- 保留 B 段（3 条）：`函数对象`、`枚举值`、`结构体值`

保留 2 条（G3 边界门失败，撤后词首被关键字劈开，属真实语义护栏）：
- `幂次`（幂+次）
- `记录类型`（记录+类型）

删除后 CCW 表 24 条：位与 位异或 位或 位非 低级关闭 导入错误 幂次 并发等待 应当 异步写入文件 异步睡眠 异步读取文件 异步追加文件 测试_生成问候语 类型错误 记录类型 设指针 设指针值 设系统 设系统错误码 设置 设置数组 除非 零除错误

## 四、全量反跑（零回归验证）

### 4.1 全语料 token 零变化
`_antirun_r29_t4_D批全量反跑.py`：删除后（CCW 24）vs 加回6条（CCW 30）→ **850 文件 token 序列零变化，ALL OK**。

### 4.2 受影响测试（rc=0）
| 测试 | 结果 |
|---|---|
| `examples/test_R23_CCW内建名.light`（含 常量时间比较 段落/调用） | ✅ 全部通过 |
| `examples/test_R20_词法边界综合.light`（含 枚举值 段落/调用） | ✅ PASS |
| `examples/test_会话控制类型.light` | ✅ PASS |
| `examples/test_字符串增强.light`（含 安全正则匹配） | ✅ PASS |
| `examples/test_团队折叠.light`（含 函数对象 注释） | ✅ 全部用例通过 |
| `examples/test_L055.light`（含 函数对象 注释） | ✅ PASS |

## 五、交付物清单

- `light-merge/src/lexer.py`：D 批删除 6 条 + 注释回填（CCW 37→30→24，本轮 D 批删 6 条）。
- `lightharness/_antirun_r29_t4_D批验证.py`：逐条三重判据验证脚本。
- `lightharness/_task4_R29_D批验证_证据.json`：逐条证据（G1/G2/G3）。
- `lightharness/_antirun_r29_t4_D批全量反跑.py`：删除后全量反跑脚本。
- 本验证清单。

## 六、铁律符合性

- ✅ 只改 D 批 8 条，不碰 A/B/C 批。
- ✅ 逐条隔离验证（非批量），三重判据 G1∧G2∧G3 全通过才删。
- ✅ 删除后全量反跑（token 零变化）+ 受影响测试 rc=0。
- ✅ 全部 .light 验证，无 Python 绕语言缺陷。
