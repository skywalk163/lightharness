# 第6轮收口说明 —— E5 长期挂账失败专项清零

- 轮次：第 6 轮（E5 长期挂账专项）｜日期：2026-09-11
- 任务书：`E5修复_第6轮_任务prompt分发.md`（2 并行 + 路M）
- 合入后 HEAD：`0a09340` ｜ 全量 CI：**213 passed / 0 failed（首次全绿）**

---

## 1. 本轮目标与结果

自第 4 轮起恒定的 2 个全量 CI 失败（`test_agentE5钩子.light` / `test_压缩E5.light`）全部清零，CI 首次全绿。

| 项 | 结果 |
|---|---|
| 合入 | 任务1 工作区直提（`db7208e`）+ 任务2 分支 `--no-ff` 合并（`0a09340`），零冲突 |
| 改动 src | 1 个（`src/压缩E5.light`，1 行修复 + 1 行注释） |
| 改动测试 | 1 个（`examples/test_agentE5钩子.light`，1 行修复 + 注释） |
| 新增 | `_antirun_compresse5.py`（反跑 2/2）、`_antirun_agentE5hooks.py`（反跑 1/1）、2 份交付报告、`examples/_repro_L080.light`（最小复现，修复形态可运行） |
| 回归门禁 | **213 passed / 0 failed（首次全绿）**，smoke 5/5 |
| 缺陷账 | **L-080 新登记**（光明 / 浮点除法 + 切片下标需 int） |

## 2. 两处根因与修复

| 失败用例 | 根因 | 修复 |
|---|---|---|
| `test_压缩E5.light` | `src/压缩E5.light` `修剪工具结果`：`设 头部长 为 (长 * 70) / 100` 是浮点除法（实测 20000→14000.0 float），`结果[0:头部长]` 用 float 切片报 `slice indices must be integers`（超长工具结果 >8000 字符路径触发） | `设 头部长 为 整数((长 * 70) / 100)` → 14000 int，切片正常；`尾部长 = 长 - 头部长` 自动 int 无需改；修复只改下标类型不改切分位置，既有断言全部成立 |
| `test_agentE5钩子.light` | 行 33 `设 空 为 事件`——保留字 `空`（None）作变量名（L-059/L-076 同源）→ 整文件解析失败 | `设 空事件 为 事件` + 注释；段落 `收集事件` 未被调用，仅消除不合法定义 |

任务2 修复后第二用例曾暴露依赖前置：`运行智能体 → 默认修剪钩 → 修剪工具结果` 依赖任务1 的 `压缩E5` 修复（基版浮点除法被钩子捕获后结果原样返回）。任务2 agent 本地同步任务1 修复版验证后 3 用例全绿，但**提交不含 `src/压缩E5.light`**（归属任务1），文件互斥保持干净。

## 3. 回归与反跑

- 任务1：`test_压缩E5` 10 用例全绿；回归 `test_压缩配套`/`test_压缩`/`test_令牌计量`/`test_令牌估算`/`test_令牌估算1.5` 全绿；反跑 2/2（A：改回浮点 → 红复现；B：`//` 等价可用，实测光明已支持整数除法）
- 任务2：`test_agentE5钩子` 3 用例全绿；回归 `test_代理`/`test_代理深化`/`test_钩子协议` 全绿；反跑 1/1（`空事件` 改回 `空` → 解析红）
- 全量 CI（路M）：**213 passed / 0 failed**（= 第5轮 211 + 2 清零用例），smoke 5/5，耗时 284s

## 4. 缺陷账收口

登记 **L-080**：光明 `/` 为浮点除法（Python `/` 语义），字符串/列表切片下标要求 int；float 作下标报 `slice indices must be integers...`，且光明包装提示「文本和数字不能直接进行运算」误导。
- 绕法：`整数(表达式)` 显式转换（落地）；等价备选 `//`（光明已支持，截断语义）
- 建议光明侧：切片下标非 int 时给出明确中文报错
- 最小复现：`examples/_repro_L080.light`（修复形态 rc=0，保证进 CI 门禁；失败形态以注释展示）

## 5. 遗留事项

1. **建议光明侧改进**（L-080 建议项）：切片下标类型错误的中文报错提示。
2. **上游窗口**：无 0.1.5-rc.2 之后的官方版本，仍无新对标窗口。
3. 工作区仅剩 `_taskM_*` 路M 工具文件（保留备查）；`task-6-e5hooks` 分支与 `wt-R6T2` worktree 已清理（本轮收口动作）。
