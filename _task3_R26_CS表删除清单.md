# R26 任务3 交付：CS 表逐条验证删除清单

> 前置：任务1+2 的「词首并入正面规则」（`_P0A_HEAD_MERGE_SINGLE`）已落地。
> 判据（与第25轮同口径，三重判据全通过才可删）：
> - **G1 语料判据**：撤掉 c 后含 c 的语料文件 token 序列零变化（变化文件数 = 0）
> - **G2 词首编译门**：`{词首复合名} 为 7` 语句起始裸名，撤掉态与基线一致（整词成 IDENTIFIER）
> - **G3 边界形态门**：六类边界形态 token 流不变
>
> 证据：`_task3_R26_CS表逐条验证_证据.json`（30 字逐条）｜反跑：`_antirun_r26_t3_CS表删除.py`（843 文件）

---

## 一、结论：CS **30 → 16**（净删 14 字）

### 可删 14 字（G1 ∧ G2 ∧ G3 全通过 → 已删除）

| 字 | G1 | G2 | G3 | 词首复合名（G2 用例） | 词首并入改由谁覆盖 |
|---|---|---|---|---|---|
| 余 | 0 | ✅ | ✅ | 余数 | 正面类别 |
| 例 | 0 | ✅ | ✅ | 例表 | 正面类别 |
| 出 | 0 | ✅ | ✅ | 出错 | 正面类别 |
| 则 | 0 | ✅ | ✅ | 则例 | 正面类别 |
| 常 | 0 | ✅ | ✅ | 常规 | 正面类别 |
| 引 | 0 | ✅ | ✅ | 引导 | 正面类别 |
| 接 | 0 | ✅ | ✅ | 接续 | 正面类别 |
| 断 | 0 | ✅ | ✅ | 断裂 | 正面类别（**R26 新增可删**：R25 基线撤断 G1=10 打红） |
| 末 | 0 | ✅ | ✅ | 末项 | 正面类别 |
| 试 | 0 | ✅ | ✅ | 试探 | 正面类别 |
| 跳 | 0 | ✅ | ✅ | 跳表 | 正面类别 |
| 过 | 0 | ✅ | ✅ | 过载 | 正面类别（**R26 新增可删**：R25 基线撤过 G1=1 打红） |
| 长 | 0 | ✅ | ✅ | 长度 | 正面类别 |
| 首 | 0 | ✅ | ✅ | 首项 | 正面类别 |

> **R26 相对基线的增量**：`断`/`过` 在 R25 态撤出会使 G1 打红（10 / 1 文件），
> 加入词首并入正面规则后其**词首**并入由 `_P0A_HEAD_MERGE_SINGLE` 覆盖 → G1 归零、G2/G3 通过 → 可删。
> 其余 12 字（余例出则常引接末试跳长首）在 R25 态 G1 已为 0，但 G2 会失败（`长度 为 7` 被切碎），
> 正面规则落地后 G2 通过 → 可删。

### 保留 16 字（某道判据失败）

| 字 | G1 | G2 | G3 | 失败判据 | 保留理由 |
|---|---|---|---|---|---|
| 乘 | 1 | ❌ | — | G2 | 运算符，∉正面类别；词首后随汉字须并入（乘法） |
| 减 | 1 | ❌ | — | G2 | 运算符，∉正面类别（减/除 单测钉住） |
| 加 | 8 | ❌ | — | G1+G2 | 运算符，∉正面类别；词首锚（加法） |
| 除 | 2 | ❌ | — | G1+G2 | 运算符，∉正面类别（减/除 单测钉住） |
| 模 | 29 | ❌ | — | G1+G2 | `_P0A_NEVER_SPLIT`（模组/模块，2900+ 高频成分） |
| 真 | 1 | ❌ | — | G1+G2 | 值字面量，∉正面类别（真空） |
| 空 | 1 | ❌ | — | G1+G2 | 值字面量，∉正面类别（空格） |
| 到 | 1 | ❌ | — | G1+G2 | 范围结束符 `_P0A_NEVER_SPLIT`（到位） |
| 列 | 1 | ✅ | ✅ | G1 | 语料载重构词字（列数） |
| 对 | 4 | ✅ | ✅ | G1 | 语料载重构词字（对立） |
| 是 | 2 | ✅ | ✅ | G1 | 语料载重构词字（是否；单测钉住） |
| 段 | 9 | ✅ | ❌ | G1+G3 | 语料载重构词字 + `段[1]` 下标访问边界（L-119） |
| 的 | 5 | ✅ | ✅ | G1 | 语料载重构词字（的确/结构助词） |
| 类 | 2 | ✅ | ✅ | G1 | 语料载重构词字（类别） |
| 自 | 1 | ✅ | ✅ | G1 | 语料载重构词字（自然/自己） |
| 配 | 4 | ✅ | ❌ | G1+G3 | 语料载重构词字 + `配[0]` 下标访问边界（L-119） |

---

## 二、CS 表结构（16 字，两类）

```
A. DUAL（运算符/值字面量/范围，8 字）：乘 减 加 除 模 真 空 到
   —— 词首后随汉字须并入，但 ∉ 正面类别 F（F 显式排除运算符/值字面量/范围），
      故必须保留作词首锚。
B. 语料载重构词字（8 字）：列 对 是 段 的 类 自 配
   —— 撤销即 G1 打红（段 9 / 的 5 / 对 4 / 配 4 / 是 2 / 类 2 / 列 1 / 自 1 文件）；
      段/配 另有 G3 下标边界（L-119）。
```

断言 `CS == (F∩CS) ∪ DUAL`（文末 import 自校验）在 CS=16 下仍成立：**8 ∪ 8 = 16**。
新增断言 `_P0A_HEAD_MERGE_SINGLE − CS == _R26_CS_REMOVED`（14 字）硬钉两者关系。

---

## 三、全量反跑结果

| 项目 | 结果 |
|---|---|
| 全语料 token 零变化（基线 R25 态 vs R26 新态） | ✅ **0 / 843 文件**（`_r26_ab_final.json`） |
| 快速验证 `.light` | ✅ rc=0（`examples/test_R26_词首并入_快速.light`） |
| G2 词首编译门（30 字逐条） | ✅ 可删 14 字全通过；保留 16 字中 8 字 G2 失败（运算符/值字面量/范围，符合预期） |
| G3 边界形态（17 条形态） | ✅ 仅 段/配 的 `段[1]`/`配[0]` 破坏（二者本因 G1 保留） |
| import 期自校验 | ✅ 断言①/②/③ 全过 |
| 既有单测零新增打红 | ✅ 与 R25 基线逐条相同（6 条基线红） |

---

## 四、删除动作（已执行）

`light-merge/src/lexer.py` CS 表：30 → 16，移除 `余 例 出 则 常 引 接 断 末 试 跳 过 长 首`；
新增 `_P0A_HEAD_MERGE_SINGLE` / `_P0A_HEAD_SPLIT_SINGLE` / `_R26_STMT_HEAD_SINGLE` / `_R26_CS_REMOVED` 及 3 条自校验断言；
第一层 skip_verb + 嵌入扫描三处接入词首正面规则（详见任务1/任务2 交付）。
