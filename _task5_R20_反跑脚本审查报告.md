# 第20轮 任务5 · 反跑脚本体系审查报告

> 审查对象：第19轮 6 路反跑脚本 + 第20轮 2 路新增反跑脚本（共 **8 个**）
> 实测方式：2026-09-14 在 `lightharness` 根目录**串行**复跑全部 8 个脚本（反跑会原地改写 `light-merge` 源码，与任何测试**不可并发**），
> 原始输出留存于 `_r20_antirun_all.txt`。
> 审查人：第20轮任务5 ｜ 日期：2026-09-14

---

## 一、复跑结果总览

| # | 脚本 | 轮次/任务 | EXIT | 判据覆盖 | 结论 |
|---|---|---|---|---|---|
| 1 | `_antirun_t1_词法核心.py` | R19-T1（L-084/L-092/L-137） | 0 | A×4 / B×1 / C×3 / 恢复校验 / 恢复后绿 | ALL OK |
| 2 | `_antirun_t2_词法扩展.py` | R19-T2（L-119/L-120） | 0 | A / B×4 / C1 / C2 / 终态校验 | ALL OK |
| 3 | `_antirun_t3_保留字内置映射.py` | R19-T3（L-087/L-101/L-143） | 0 | A×3 / B×2 / C / 恢复校验 / 恢复后绿 | ALL OK |
| 4 | `_antirun_t4_字符串哈希.py` | R19-T4（L-086/L-095） | 0 | A / B×3 / C1 / C2 / 双份一致 | ALL OK |
| 5 | `_antirun_t5_异常模块.py` | R19-T5（L-093/L-094） | **1** | A / B / C / 恢复校验 全 PASS | ⚠️ **A/B/C 全过但退出码 1 —— 失败在「清理步骤」**（见 §二.1） |
| 6 | `_antirun_t6_字典函数_D批次.py` | R19-T6（L-096/D批次） | 0 | A×2 / B×4 / C1 / C2 | ALL OK |
| 7 | `_antirun_r20_t1_词法关键字前缀.py` | R20-T1 | 0 | A×2 / B×2 / C×2 | ALL OK |
| 8 | `_antirun_r20_t2_HMAC标准实现.py` | R20-T2 | 0 | A1/A2 / B / C | ALL OK |

**7/8 ALL OK；1 项（R19-T5）判据全过但收尾失败。**

---

## 二、发现的问题（按严重度）

### 2.1 【高】R19-T5 的临时用例删除会被工作区 safe-delete **批量阈值**拦下 → 残留文件成为 CI 假红地雷

**现象（实测两态对比）**

| 执行方式 | 输出 | EXIT | `examples/_tmp_*.light` 残留 |
|---|---|---|---|
| 8 个反跑**批量串行**执行（本报告首次复跑） | `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":50,"threshold":50,"scope":"turn","targets":["...\\examples\\_tmp_t5_uncaught.light"],"targetCount":1}`，随后 A/B/C/恢复校验全 PASS 但**未打印 `【恢复后】A 判据 PASS` / `ANTIRUN T5 ALL OK`** | **1** | **存在**（71 字节） |
| 单独执行 `python _antirun_t5_异常模块.py` | A/B/C/恢复校验 + `【恢复后】A 判据 PASS` + `ANTIRUN T5 ALL OK` | 0 | 无 |

**根因**：脚本为让 `从 载体模块 导入 触发` 正常解析（`_resolve_local_imports` 只认同目录模块），
必须把临时未捕获用例写到 `examples/` 目录；收尾删除在**单次执行**时可正常完成，
但在同一 turn 内累计触发 safe-delete 的**批量阈值**（`count=50 ≥ threshold=50`）后，
该删除被拦下 → 文件留在盘上 → 后续「恢复后 A 判据」复跑受污染 → 脚本非零退出。

**危害（真实且已发生）**：`tests/test_回归.py::_collect()` 用的是 `glob(examples/*.light)`（**不是** `test_*.light`），
残留的 `examples/_tmp_t5_uncaught.light` 会被收进门禁，而它**故意是未捕获异常**（rc≠0）→ 全量 CI **假红**。
本次审查已实测发现该文件并手工删除（另：`_sweep_*` / 本报告用的 `glob('examples/test_*.light')` 扫描**不会**发现它，
这是个隐蔽的扫描盲区）。

**改进建议（任选，建议 3+4 叠加）**
1. 临时用例写进 `examples/` 下**子目录**——`glob('examples/*.light')` 不递归，自然不进 CI；但 `_resolve_local_imports` 只认同目录，此法需先验证导入仍可用（风险较高）。
2. 收尾改为「先 `shutil.move` 到同盘临时目录再删」，绕开工作区 safe-delete 策略。
3. **兜底登记**：把 `_tmp_*.light` 加入 `tests/test_回归.py` 的 `SKIP`（文件不存在时无害，属自清理式登记）。
4. **反跑脚本自检**：`finally` 后检查 `examples/_tmp_*.light` 是否存在，存在则打印醒目告警并置退出码 2 ——
   避免「判据全过却留下地雷」被误读为成功。
5. 全量 CI 的用例集合从 `glob('*.light')` 收紧为 `glob('test_*.light')` + 显式 `_repro_*` 白名单，从根本上消除「临时文件误入 CI」。

### 2.2 【中】A 判据「只看 rc=0」的证明力依赖用例内部断言实现

- R19-T1/T2/T3/T6 的 A 判据均为「rc=0」聚合；这在**用例内部断言有效**时证明力充分，
  但本仓历史教训 **L-037**（`断言(条件,标签)` 曾被编译成 `assert (条件, 标签)` 元组 → 恒真 no-op）说明：
  「rc=0」可能只证明「没崩」，不证明「算对」。
- **R19-T4 的 A 判据正是「rc=0」**，而第19轮收口恰恰出现了「test_L095 被登记为部分修复」的状态不一致（见
  `_task4_R20补全_第19轮字符串哈希_交付报告.md` §3.5）。
- **R20-T2 做了正确升级**：新增 **A2 = 直接 `importlib` 加载两份 `哈希.py`，7 组对拍与预计算常量逐一致**，
  把「算对」写进脚本本身，不再只依赖 .light 用例。**建议推广**：凡语义关键（哈希/编码/解析/协议）的反跑，
  都应有一项「宿主 API 直接对拍」判据。
- 自动化检查范式（四类，缺一不可）：
  1. A：修复后绿（rc=0）+ **关键输出的宿主侧对拍**（非仅 rc）；
  2. B：边界与反向用例（既覆盖「正例不误切」也覆盖「反例仍切」）；
  3. C：真变异立红 + **finally 恢复 + 字节级 sha256 校验**（8/8 脚本已具备）；
  4. 终态自检：工作区无残留临时文件、双份同步文件 sha256 一致。

### 2.3 【中】变异锚点是「源码字符串字面量」，脆弱（但**均已 fail-fast**，不存在静默假过）

**先纠正一个易误判的点**：8 个脚本**都已显式处理「锚点未命中」**，且都是**硬失败**而非静默跳过：

| 脚本 | 锚点未命中的行为 |
|---|---|
| R19-T1 / T3 / T5 | `if ANCHOR not in original: print("【前置检查】未找到变异锚点…"); raise SystemExit(1)` |
| R19-T2 / T4 / T6 | `mutate_case` 内 `print("FAIL (skip: 变异锚点未命中)"); return False` → 计入总判据失败 |
| R20-T1 / T2 | 同类硬失败 / `all_ok = False` |

→ 结论：**不存在「锚点失配 = C 判据静默假过」的风险**，这一点是达标的（审查中一度误判，此处订正）。

**仍然存在的脆弱点**

1. **CRLF/LF 硬编码**：`_antirun_t4_字符串哈希.py` 的 `C2_OLD` 与 `_antirun_r20_t2_HMAC标准实现.py` 的 `C_OLD`
   都把换行写成 `\r\n`（跨行锚点）。当前 `stdlib/哈希.py` 恰为 CRLF（实测 151 CRLF / 151 LF）故命中；
   一旦某轮把该文件规范化为 LF，锚点即失配——表现为**硬失败**（不会假过），但会被误读为「反跑挂了」而非「需更新锚点」。
2. **锚点跨越版本改动区**：`_antirun_t1_词法核心.py` 的锚点
   `_EMBED_MAX_MATCH_KEYWORDS = frozenset({'为', '返回', '尝试'})` 与第20轮任务1 的改动处于同一行域。
   本轮 R20 的修改（新增 `full_identifier not in user_definitions` 守卫、补注释、删重复定义）**恰好未破坏该锚点**，
   复跑命中；但已属「擦肩而过」，下一轮再动这一带就会失配。
3. **无「变异确实生效」的二次校验**：脚本校验了「恢复后 sha256 一致」，但未校验「变异后文件确实不同于原文件」
   （当前所有脚本在锚点命中时必已生效，故属加固建议而非缺陷）。

**改进建议**
1. 锚点尽量取**单行、不含换行符**的稳定片段（如 `"return hmac.new("`），消除 CRLF/LF 依赖；
2. 变异后加一行断言（或让「锚点失配」用独立退出码，与「判据失败」区分），便于收口时快速辨认原因；

### 2.4 【低】「反跑与测试不可并发」靠纪律而非机制

- 8/8 脚本都会**原地改写** `light-merge/src/*.py` 或 `stdlib/*.py`；若与 `pytest` / 全量扫描并行，
  被测进程会读到变异态 → 假红/假绿（本仓 2026-09-13 已踩过该坑）。
- 现状：仅靠「串行执行」的纪律（本报告即按串行复跑）。
- **改进建议**：在反跑脚本入口写一个锁文件（如 `.antirun.lock`），存在即拒绝启动；
  或在 `scripts/ci_test.py` 里显式声明「反跑为独立阶段、不进 pytest」。

### 2.5 【低】R19-T5 的 A 判据依赖「未捕获异常的顶层输出文本」

- L-093 的验收对象是**错误位置块文本**（`载体模块.light:6`），断言基于 stdout 子串匹配 —— 这是合理且必要的
  （位置块就是可观测行为），但**不防御「输出格式调整」**：一旦 `error_formatter` 改变渲染（如去掉文件名冒号），
  判据会假红而非暴露真缺陷。建议在断言里同时匹配「文件名 + 行号」两个独立子串，降低格式耦合。

---

## 三、交付报告规范总结（基于第19轮 3 份报告缺失的教训）

**每路交付报告的必要要素（七项，缺一不可）**

| # | 要素 | 内容要求 | 反例（第19轮缺失报告导致的问题） |
|---|---|---|---|
| 1 | **修复文件清单** | 绝对/仓库相对路径 + 改动行数 + 是否跨仓（light-merge / lightharness） | R19-T2/T4/T6 报告缺失 → 只能靠 `git show` 反推 |
| 2 | **修复策略与判据** | 根因 → 方案 → 逐条判据（含关键代码/锚点片段） | 同上；且 L-097「修复 vs 验收确认」被误写成「修复」 |
| 3 | **复现用例** | 文件路径 + rc + 断言数 + 期望值来源（须离线预计算可复算） | R19-T4 的 test_L095 期望值来源未记录，导致「部分修复」误判无法快速澄清 |
| 4 | **反跑判据与实测结果** | A/B/C 三类 + 变异锚点 + 恢复校验方式 + **实测输出摘要** | R19-T6 无报告 → C2 的 `test_会话` 立红证据无据可查 |
| 5 | **不破坏回归验证** | 全量/切片回归数字（含日期）+ 与基线对比 | R19-T4 未记录「全量 CI 中 test_L095 的实际 rc」，是「状态滞后」得以发生的直接土壤 |
| 6 | **已知局限** | 未覆盖形态、白名单维护成本、跨任务边界 | 缺失会使后续轮次重复踩坑（如本次发现 L-152 嵌套段落实参名） |
| 7 | **移交与依赖** | 移交对象（任务/M）、外部依赖（编译器版本、stdlib 双份同步） | R19 收口只得自行推断，产生登记矛盾 |

**报告必须与「可机检事实」对齐**：报告里的每个数字（rc、断言数、sha256、CI 通过数）
都应能从反跑脚本输出 / 全量 CI 日志直接复核。**报告写「已修复」时，必须同时给出「反跑 A 判据实测输出」与「用例 rc 的实测值」**。

**交付报告自检清单（可直接贴到任务书）**
- [ ] 修复文件清单（路径 + 行数）
- [ ] 根因 → 方案 → 判据锚点（含代码片段）
- [ ] 复现用例路径 + rc + 断言数 + 期望值来源
- [ ] 反跑 A/B/C 判据**实测输出**（原文粘贴）
- [ ] 恢复校验方式（sha256 值）
- [ ] 回归验证数字（日期 + 命令）
- [ ] 已知局限（≥1 条，无则写「无，理由…」）
- [ ] 移交对象与外部依赖
- [ ] 报告内所有数字均可机检复核

---

## 四、本次审查直接产出的处置动作

1. **删除** `lightharness/examples/_tmp_t5_uncaught.light`（R19-T5 反跑残留，会成为 CI 假红）。
2. **修订** `lightharness/tests/test_回归.py`：`SKIP` 去掉 `_taskM_integration_r17.light`、
   `EXPECT_RED` 去掉 `test_L095.light`（两者第20轮实测均 rc=0）。
3. **改写** `_verify_r20_exception_cleanup.py`：从「验证 4 例外清理状态」升级为「断言清理**目标态**」
   （`SKIP`/`EXPECT_RED` 收敛断言 + 保留字预期红不被误清 + 本轮正向控制 + 可选 `--sweep` 全量回落）。
4. **补全** 第19轮 3 份缺失交付报告（依 §三 七要素）：
   `_task2_R20补全_第19轮词法扩展_交付报告.md`、`_task4_R20补全_第19轮字符串哈希_交付报告.md`、
   `_task6_R20补全_第19轮字典函数_D批次_交付报告.md`。
5. **移交路M**：R19-T5 收尾删除方案（§2.1 建议 3/4）；R19-T1/T3/T5 补「锚点未命中即失败」校验（§2.3）。
