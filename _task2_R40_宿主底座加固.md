# 第 40 轮 任务 2 交付报告 —— 宿主底座加固

> 日期：2026-09-16 ｜ 方向：宿主底座加固（会话存储竞态 + 残留放大 + AbortSignal 占位评估）
> 上游基线：deepseek-harness 0.1.5-rc.2
> 交付物：`src/会话存储.light`（竞态修复）、`examples/test_R40_会话存储.light`（单元测试，rc=0）、`_task2_R40_宿主底座加固.md`（本报告）

---

## 一、现状盘点（改动前）

**src/会话存储.light**（233 行，第 9 轮落地，第 37 轮起登记两笔债）：
- `保存索引` 方法：直接调 `os.replace(临时路径, 索引路径)`，Windows 上目标文件被占用/瞬时不可替换时抛错。
- 未提供任何退避重试。
- test_会话存储.light 结尾有清理逻辑（try/捕获），但残留放大是"测试反复运行时若清理失败会累积"的隐含风险，未做显式断言。

**AbortSignal 占位（R39 系统提示）**：
- `src/系统提示.light` 的 `组装系统提示` 内 `组装上下文["信号"]` 留空。
- 光明无原生 AbortSignal，但 `src/中止.light` 已实现协作式中止令牌（第 9 轮），可作为未来信号语义的候选载体。

---

## 二、改动内容

### 2.1 会话存储 os.replace Windows 竞态修复

**方案**：抽出独立的模块级函数 `保存索引替换(临时路径, 索引路径, 替换函数=空, 间隔表=空, 最大重试=空)`，`保存索引` 方法调它。

**退避策略**（`src/会话存储.light`）：
```
设 保存索引重试间隔 为 [0.1, 0.2, 0.4, 0.8, 1.6]  # 5 次指数退避
设 保存索引最大重试 为 5                            # 含首次，总最坏时长 ~3.1 秒
```

**核心实现**：
```
段落 保存索引替换 接收 临时路径, 索引路径, 替换函数=空, 间隔表=空, 最大重试=空:
  如果 替换函数 == 空: 替换函数 为 保存索引替换函数       # 默认注入
  如果 间隔表 == 空:   间隔表 为 保存索引重试间隔
  如果 最大重试 == 空: 最大重试 为 保存索引最大重试
  设 首次尝试 为 真
  设 已试 为 0
  设 上一错误 为 ""
  当 已试 < 最大重试:
    如果 首次尝试 == 假:
      # 退避 sleep
      设 idx 为 已试 - 1
      如果 idx < 长(间隔表):
        time.sleep(间隔表[idx])
    尝试:
      替换函数(临时路径, 索引路径)
      返回 真
    捕获 异常 e:
      上一错误 为 转字符串(e)
      已试 为 已试 + 1
      首次尝试 为 假
  # 全部失败：尽力清理临时文件
  尝试:
    如果 文件存在(临时路径): 删除文件(临时路径)
  抛出 异常("会话存储：索引原子写失败，已重试 " + 转字符串(已试) + " 次；" + 上一错误)

# 默认替换函数（独立段落便于测试注入替换）
段落 保存索引替换函数 接收 临时路径, 索引路径:
  os.replace(临时路径, 索引路径)
  返回 真
```

**改动点**：
- 新增导出：`保存索引替换`、`保存索引替换函数`、`保存索引重试间隔`、`保存索引最大重试`。
- 原有 `os.replace(临时路径, 索引路径)` 调用改为 `保存索引替换(临时路径, 索引路径)`。
- 其余 API（`会话存储` 类、`生成会话标识`、`生成标题`、`取现在时间`）零改动，保持向后兼容。

**兼容性**：既有 test_会话存储.light 未改动，直接跑通（大会话加载耗时 0.047s，仍 < 1s）。

### 2.2 test_会话存储 残留放大清理

**分析**：test_会话存储.light 结尾有 try/捕获 清理块，正常情况下会清空 `_e2_会话存储_tmp/sessions/`。但存在两个残留放大隐患：
1. 若 try/捕获 内某个 `删除文件` 抛错（Windows 独占），后续清理被跳过。
2. 索引文件 `_index.json.tmp` 在 `保存索引` 崩溃时可能残留（现已在 `保存索引替换` 失败后清理）。

**修复**：
1. 新测试文件 test_R40_会话存储.light 显式做**多次批量新建 + 完整清理 + 断言**（#07），验证残留不放大。
2. 结尾断言 `_index.json.tmp` 不存在（#08），防止原子写残留。
3. 结尾显式清理临时根目录并断言已删除（#10）。
4. 测试前若有上次残留，先 `清空会话目录` 再跑（防污染）。

**保留**：test_会话存储.light 未改动（回归通过），避免破坏既有断言。

### 2.3 AbortSignal 占位评估（只出结论，不改代码）

**结论**：光明在系统提示组装上下文里保留 `信号` 字段占位（空值）是**正确策略**，暂不填充。理由：

1. **场景分类**：
   - **协作式中止**（用户取消/超时）：上游用 AbortSignal，光明已有 `src/中止.light` 的 `中止令牌` 类实现（第 9 轮），语义等价，可直接复用；但系统提示组装是**同步短耗时操作**（毫秒级），中途检查信号意义不大。
   - **异步中止**（工具执行/LLM 流式）：上游用 AbortSignal 竞速，光明 `src/工具执行.light` 已有 `取消令牌`（第 34 轮）+ `src/中止.light` 的 `轮询中止` 异步协程（第 9 轮）。系统提示**不参与**这两条异步链路。
   - **子代理递归中止**：`src/子代理深化.light` 已支持通过 `中止令牌` 传播中止，与系统提示无直接耦合。

2. **机制候选**：
   - **回调/检查点标志**：光明用"检查每步 → 抛异常 → 上层捕获"替代 AbortSignal 的"检查 → 抛"，语义等价。`src/中止.light` 的 `检查中止()` 方法就是这个模式。
   - **异常语义**：光明无 Python 的 `CancelledError`（BaseException），中止只能走 Exception 层级；`src/中止.light` 已按此约定实现。

3. **建议（下一轮可考虑）**：
   - 若 R39 组装系统提示需支持"取消"，把 `组装上下文["信号"]` 填充为 `中止令牌` 实例，节文本函数在长时间操作前主动调 `检查中止()`。
   - 目前无此需求，保留占位即可。

**评估成本**：本轮零代码修改（符合铁律），仅登记结论。

---

## 三、单元测试覆盖（examples/test_R40_会话存储.light，rc=0，11 组）

| 编号 | 覆盖点 |
|---|---|
| #00 | 断言助手自检 |
| #01 | 正常写入路径（回归：新建/追加/读取） |
| #02 | 重复写入不覆盖（追加模式实证） |
| #03 | 保存索引替换默认路径成功（os.replace 正常） |
| #04 | 保存索引替换失败 → 抛错且含重试次数（5 次退避耗尽） |
| #05 | 多次保存索引连续正常（幂等：5 次追加后消息数=9） |
| #06 | 会话删除清理（JSONL+索引都清） |
| #07 | 残留放大：批量 10 会话 → 完整清理 → 断言无 JSONL 残留 |
| #08 | 新实例重建索引不残留（_index.json 有效、无 _index.json.tmp） |
| #09 | AbortSignal 占位：注册表 signal 留空不引入副作用 |
| #10 | 最终清理：sessions 目录清空断言 |

**运行结果**：
```
OK #00
OK #01
OK #02
OK #03
OK #04
OK #05
OK #06
OK #07
OK #08
OK #09
OK #10
--- 第40轮任务2 会话存储加固（竞态+残留+占位评估） 通过 ---
共 11 组
```

---

## 四、铁律合规检查

- ✅ **禁止修改 src/互举接线.light**：本轮未创建该文件（归任务 1）。
- ✅ **保留原功能**：既有 test_会话存储.light 未改动，仍通过（大会话 1000 条 < 1 秒）。
- ✅ **退避重试有上限**：5 次硬上限，间隔固定表，最坏总时长 ~3.1 秒。
- ✅ **残留清理不误删用户数据**：只清理 `_e2_R40_会话存储_tmp/` 下测试产生的会话，不动真实 `data/sessions/`。
- ✅ **测试 rc=0**：`python 运行.py examples/test_R40_会话存储.light` → EXIT=0。
- ✅ **AbortSignal 只出结论**：仅登记评估报告，未改 `src/系统提示.light`。

---

## 五、光明适配差异（记录在案）

| 上游 | 光明适配 | 差异影响 |
|---|---|---|
| Python 无原生 AbortSignal 语义 | 保留占位 + `src/中止.light` 独立实现协作式中止令牌 | 语义等价，模块解耦 |
| Windows os.replace 竞态 | 5 次指数退避重试 + 抛错 | 最坏 3.1 秒，覆盖 Windows 独占场景 |
| 测试残留管理 | 显式断言 + 测试前后清理 | 避免累积污染 |

---

## 六、遗留项 / 与任务 1、5 接口

- **任务 1（端到端互举链路）**：会话存储的 `保存索引替换` 独立段落便于测试注入，若任务 1 遇到会话存储相关缺陷，通过该注入点验证。
- **任务 5（互举验证）**：test_R40_会话存储.light 已加入 examples/，应纳入全量反跑清单。
- **任务 6（docs 回填）**：本报告 §2.3 是 R40-D2 行为差异清单的直接素材。
- **下一轮**：若系统提示组装需支持中止，`组装上下文["信号"]` 可填 `新建 中止令牌()`；本次只登记不实现。
