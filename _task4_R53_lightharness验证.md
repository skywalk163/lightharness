# 任务4（R53·P1）交付报告 —— lightharness 侧验证（L-172 修复不破既有）

> 日期：2026-09-17 ｜ 状态：**完成**
> 交付物：本报告 ｜ 铁律遵守：只验证不改源。
> 验证对象：任务2 编译器修复（`light-merge/src/code_generator.py` + `cli/light.py`）
> 对 lightharness 全仓（412+ examples / 684 .light）的影响。

---

## 一、lightharness 全量 pytest（修后编译器）

```
python -m pytest tests -q -o addopts=''
→ 10 failed, 1266 passed, 3 skipped, 2 warnings in 1778.65s
```

failed 名单逐项归因：

| 类别 | 数量 | 明细 | 判定 |
|---|---|---|---|
| 存量词法红 | 7 | test_R31 EMBED×2、test_R32×1、test_R22×1、test_R26×2、test_R27×1 | 与 R47→R52 历轮基线**完全同名**，非新增 |
| `_l172*` 探针 | 3 | `_l172mut_m1/m2.light`、`_l172w.light` | 任务1/2 诊断期临时文件，被 examples 回归门收集；**已全部移除**（探针期收集时点恰逢其存在，属瞬时红） |
| L-172 相关 | 0 | — | **零新增红** |

修复前基线（同一轮先跑，未被修复污染）：7 failed / 1254 passed（R48 口径）——修复后
passed 增量来自 R50-R52 各轮新增判据，failed 端除瞬时探针外零变化。

## 二、L-172 规避写法排查（重点项）

任务1诊断已澄清：**阻断真根因 = 模块级顶层对入口名的裸引用**（`设 X 为 主`），
而非"辅助段落体内按名调用导入函数"（该形态实测不阻断）。两个触发面分别全树扫描：

### 1. 真根因面：模块级入口裸引用

```
grep -E "^\s*设\s+\S+\s+为\s+主\s*$"（examples/src/stdlib 全部 .light）
→ 0 处命中
```
lightharness 全树**无任何**踩 L-172 真根因的写法——修复对该仓是纯无感（只会更好，
不可能改变既有行为）。

### 2. 任务书原描述面：非主段落体内按名调用被导入函数

自定义静态扫描（导入名集合 × 段落体语句行首调用），全树命中 **2 文件 14 处**：

| 文件 | 命中 | 实测 |
|---|---|---|
| `examples/test_授权链.light` | 13 处（`注册流程`/`添加结算监听器` 段内按名调用） | **实跑 rc=0，`主()` 八段 `=== N ===` 全部打印，断言全执行** —— 未触发阻断，非假绿 |
| `examples/_taskM_integration_r18.light` | 1 处（`检查` 段内调用 `深度设置`） | 同上未阻断（该文件为历史集成样例，未入回归门） |

结论：与任务1诊断互证——"段落体内按名调用导入函数"**不触发** L-172；这 14 处无需
改造，也佐证修复未改变该形态的行为。

## 三、互举反跑（修后编译器 × lightharness 全树）

```
python scripts/互举反跑.py
→ 扫描 677 个 .light；可解析 673；失败 4（词法 0/语法 4）；新增失败 0 —— 绿 rc=0
```

（探针期首跑为 680 文件，差 3 即已移除的 `_l172*` 临时文件；4 条基线内语法失败为
2026-09-16 基线标定的存量欠账。）

## 四、结论

1. lightharness 全量 pytest **零新增红**（10 failed = 7 存量词法 + 3 已清场的瞬时探针）；
2. 全树 **0 处**踩 L-172 真根因（模块级入口裸引用）——修复对 lightharness 纯无感；
3. 14 处"段落体内按名调用导入函数"形态实测均不阻断（与任务1诊断一致），无需回改；
4. 互举反跑 0 新增解析失败。
