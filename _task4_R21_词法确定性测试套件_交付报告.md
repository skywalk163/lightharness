# 任务4 交付报告：词法确定性测试套件（第21轮）

> 任务来源：`复刻_第21轮_任务prompt分发_词法确定性切词重构轮.md` 任务4
> 日期：2026-09-14 ｜ 本路仅新增文件，未修改既有文件与编译器源码 ✓
> 依赖说明：场景①③的验证依赖任务1（已落盘，L-152 已修复）与任务2（未落盘，报告标注）

---

## 一、交付物与验证结果

| 文件 | 类型 | 覆盖大类 | 结果 |
|---|---|---|---|
| `examples/test_R21_L152家族嵌套形参.light` | 运行期自校验 | ①嵌套段落形参（L-152 家族） | **rc=0 PASS** |
| `examples/test_R21_块内设名.light` | 运行期自校验 | ②块内设名 | **rc=0 PASS** |
| `examples/test_R21_表达式上下文.light` | 运行期自校验 | ③表达式上下文（定义登记路径） | **rc=0 PASS** |
| `examples/test_R21_语句起始关键字.light` | 运行期自校验 | ④语句起始位置关键字 | **rc=0 PASS** |
| `examples/test_R21_边界混合运算符.light` | 运行期自校验 | ⑤边界混合 + ⑥运算符关键字 | **rc=0 PASS** |
| `tests/test_R21_词法确定性_token.py` | pytest token 层 | ①~⑥ 全类 | **33 passed** |
| `tests/test_R21_词法确定性_超集.py` | pytest definitions | 任务1 兼容性（只增不减） | **3 passed** |

pytest 合计 **36 passed**；.light 合计 5 文件全 rc=0。命名均符合 `test_R21_*` 规范。

## 二、测试覆盖矩阵

| 大类 | 场景 | .light | pytest |
|---|---|---|---|
| ①嵌套段落形参（L-152 家族） | 19 个关键字前缀形参 ×（形参作函数/作变量）+ 2 层/3 层嵌套 | L152家族嵌套形参 | token.py：19 参数化整体成词 + 三层嵌套 + L-152 复现源码形态 |
| ②块内设名 | if/遍历/当/尝试 块内 `设 关键字前缀名 为 值` + 块外使用 | 块内设名 | token.py：3 名参数化（if/遍历块） |
| ③表达式上下文 | 赋值右侧/返回值/嵌套表达式/字典键/列表元素/运算符操作数（定义登记路径） | 表达式上下文 | token.py：定义登记后调用名整体成词 |
| ④语句起始位置 | 如果/否则/否则若/遍历/当/尝试/捕获/返回/设/打印 | 语句起始关键字 | token.py：8 语句头参数化 KEYWORD |
| ⑤边界混合 | 与合法语句相邻/同行嵌套调用/嵌套段名/ASCII 中文混合/含数字标识符 | 边界混合运算符 | — |
| ⑥运算符关键字 | 与/或/非（带空格运算形态）/等于/大于/成员 | 边界混合运算符 | token.py：4 形态 KEYWORD 不变性 |
| 任务1 兼容性 | definitions 超集（全量 examples 340 文件逐文件） | — | 超集.py：superset 断言 + L-152 文件增长断言 |

## 三、关键设计口径

1. **定义登记路径优先**：任务3（第20轮）扫描已证明真实代码的关键字前缀标识符由 `_scan_user_definitions` 保护（1708/1708）。本套件③按该真实口径构建；任务2 落地后孤立形态亦应整体成词（届时可扩展断言）。
2. **definitions 超集判据含"预期残片收敛"豁免**：旧版预扫描对嵌套段落的截断残片名（`内层`/`主内层`/`计算`，L-152 根因链第3步错误产物）在新版中由完整名（`内层返回` 等）替代。判据规则：丢失名若为新版任一名的真前缀则归类残片收敛（预期），否则才计违规。当前实测违规 0。
3. **运算符 `非` 的形态**：无空格 `非甲` 当前整体成 IDENTIFIER（任务1 通用最大匹配现状）；逻辑非运算形态为带空格 `非 真`。套件按带空格形态断言（无空格形态的归属属任务2 上下文敏感策略决策，已在报告中标注）。

## 四、编写过程修正记录（真实行为校准）

- `形参作函数` 场景首版直接 `函数值(1)`（形参=int）→ 传函数 `加一` 修正。
- `如果→否则:` 后再 `否则 如果:` 为非法（否则链互斥），改为完整独立如果链。
- `设 等于判断 为 5` 撞 `等于` 赋值字切分（`等于` 属运算符集合）——该形态列入任务2 待验证，套件换用 `定义变量`。
- 列表输入 + 整数拼接类型错——输入改整数。

## 五、验证命令

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"
for t in L152家族嵌套形参 块内设名 表达式上下文 语句起始关键字 边界混合运算符: python 运行.py examples/test_R21_$t.light   # 全部 rc=0
python -m pytest tests/test_R21_词法确定性_token.py tests/test_R21_词法确定性_超集.py -q   # 36 passed
```
