# 第19轮 任务2 交付报告（补全版）：词法扩展 L-119 / L-120

> 补记说明：本报告为**第20轮任务5 补齐**——第19轮任务2 的交付物（修复代码+复现用例+反跑脚本）齐备，但交付报告缺失。
> 本报告内容依据① `light-merge` 提交 `49319306` 的真实 diff、② 反跑脚本 `_antirun_t2_词法扩展.py` 的判据、
> ③ 复现用例 `examples/test_L119.light` / `examples/test_L120.light` 反推而成，**不臆造未发生的事实**。
> 补记日期：2026-09-14 ｜ 补记路：第20轮任务5

---

## 1. 任务定位

- 轮次：第19轮（编译器缺陷修复轮）
- 任务：任务2 —— 词法扩展，修复 L-119（L0 单字别名静默保留字误报错）与 L-120（标识符可被关键字序列完全切分）
- 修复文件（文件互斥表内）：`light-merge/src/lexer.py`
- 未改文件：parser / codegen / stdlib（**纯词法层修复**）

## 2. 修复内容（据 `49319306` diff）

### 2.1 L-120：新增 `_TRAILING_ALIAS_MERGE`（词尾单字别名单）

```python
_TRAILING_ALIAS_MERGE = frozenset({
    '遍', '捕', '抛', '终', '返', '己', '设', '承', '宏', '掷',
})
```

- **判定口径**：49 个 L0 单字别名中，既不在 `_COMPOUND_SAFE_SINGLE_KEYWORDS`、也不是 `_P0A_OP` 运算符/分隔符、
  更不是值字面量（真/假/空）的「语句别名单字」。这类字只在**词首**具备语句语义（`设 X 为…` / `捕 X:` / `抛 X` / `返 X`），
  出现在复合标识符**词尾**时（`错误己` / `预设` / `投掷` / `承…`）绝不可能是合法语句 →
  并入标识符，符合「最大匹配优先于关键字序列切分」。
- 刻意排除 `真/假/空`：词尾（`返回真`）是合法且常见的无空格写法；也排除既有 compound-safe 成员（已有专门分支管辖）。
- 生效位置两处（与输出循环判据**严格一致**，避免「扫描判词中/输出判词尾」不一致的老坑）：
  - 内嵌关键字扫描分支（`scan_pos > 0 and scan_pos + sub_len == len(full_identifier) and sub_kw in _TRAILING_ALIAS_MERGE`）；
  - 输出分支同上（`scan_pos += sub_len; continue`）。

### 2.2 L-119：新增「单字别名 + 下标」收窄（四条同时成立）

在 `skip_verb` 判定前新增：

```python
if (length == 1 and len(full_identifier) == 1
        and keyword in _compound_safe
        and pos + 1 < n and source[pos + 1] == '['):
    skip_verb = True
```

- **四条判据**：① 关键字长度为 1；② 整个汉字段就是该单字（`len(full_identifier) == 1`）；
  ③ 该字在 `_COMPOUND_SAFE_SINGLE_KEYWORDS`（具备构词能力的别名字）；④ 紧随 `[`（下标访问）。
- 为什么必要：`设 配 为 [1,2]` 合法，但 `打印 配[0]` 旧行为把 `配` 发成 KEYWORD → 整行被解析成「匹配 [0]:」→
  codegen 产出 `match [0]:` 空体 → 运行期报 **误导性缩进错误**（病根是保留字冲突，报错却指向缩进，排查代价极高）。
- **不破坏面**（收窄而非放宽）：`设 配 为 [...]`（后随空格）与 `配 模式:`（匹配语句）均不受影响。

## 3. 反跑判据（`_antirun_t2_词法扩展.py`，5 项）

| 判据 | 内容 | 结果 |
|---|---|---|
| A | 修复后绿：`test_L119` rc=0、`test_L120` rc=0 | PASS |
| B | 边界 token 流：`配 情况:` 的 `配` 仍 KEYWORD；`打印 真` 的 `真` 仍 KEYWORD；`设 配 为 [1,2]` 的 `配` 仍 KEYWORD；`错误甲` 整体 IDENTIFIER | PASS |
| C1 | 变异：删除 `'配'` 的 compound-safe 登记 → `test_L119` 立红（恢复后绿） | PASS |
| C2 | 变异：`_TRAILING_ALIAS_MERGE` 移除 `'己'` → `test_L120` 立红（恢复后绿） | PASS |
| 终态 | `lexer.py` 字节级 sha256 恢复校验一致 | PASS |

- 变异/恢复走 `try/finally` 异常安全路径，恢复后校验 sha256 逐字节一致（杜绝半恢复污染工作区）。
- 结论：**ALL OK（第20轮任务1 修复后复跑仍 ALL OK）**。

## 4. 复现用例

- `examples/test_L119.light`（rc=0）：`设 配 为 [1, 2]` + `打印 配[0]`，断言输出 `1`。
- `examples/test_L120.light`（rc=0）：`捕获 异常 错误己:` 绑定变量并打印，断言不报错。
- 两者均在 `_scan_user_definitions` 预扫描口径下真实生效。

## 5. 已知局限

1. 本修复只覆盖**词尾**单字别名（`_TRAILING_ALIAS_MERGE`）。词中出现的单字别名仍按既有规则处理
   （如 `三段` = 中文数词 3 + `段`，属合法切分、非 bug）。
2. `配` 的下标豁免仅覆盖「整段即单字 + 后随 `[`」。`配.属性` / `配(` 等其它形态未纳入（未发现真实用例）。
3. `_TRAILING_ALIAS_MERGE` 为**白名单**，49 字中仅收 10 字（其余已被 compound-safe 或值字面量分支覆盖）。
   若未来新增 L0 别名，需同步核对本表——属**打地鼠式维护**，根治方向见 `复刻harness驱动_light升级计划.md` P0-A
   「词法确定性切词重构」。

## 6. 与第20轮的关系

- 第20轮任务1（`_EMBED_MAX_MATCH_KEYWORDS` 的 `为/返回/尝试` 赋值尾巴切分）与本任务同处 `lexer.py` 汉字序列路径，
  但**互不覆盖**：本任务管「词尾单字别名并入」，任务1 管「`X为` 赋值尾巴切回关键字」。
- R20 任务1 修复后复跑本反跑：A/B/C1/C2 全 PASS（无相互回归）。
