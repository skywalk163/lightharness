# 第20轮路M收口说明框架 —— 编译器回归修复轮

> 日期：2026-09-14 ｜ 方向：编译器回归修复轮（清零第19轮引入的 4 个 CI 临时例外）
> 跨项目：light-merge（编译器修复：lexer.py）+ lightharness（验证/复现/反跑/报告）
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）
> **本文件为框架**：路M在 6 路交付后填入实际数据、删除占位「⟨待填⟩」、按需增删。

---

## 一、本轮概述

第19轮首次修改 light-merge 编译器，6 路修复 15 个 C 级编译器缺陷，全量 CI 329 passed + smoke 5/5，但**引入 4 个 CI 临时例外**（SKIP/EXPECT_RED）：

1. `test_宿主上下文.light` → SKIP（解析回归）
2. `test_宿主事件.light` → SKIP（解析回归）
3. `test_L095.light` → EXPECT_RED（HMAC 对拍不一致）
4. `_taskM_integration_r17.light` → SKIP（第17轮临时集成文件）

本轮（第20轮）以「**清零所有临时例外、全量 CI 无例外全绿**」为目标。核心是任务1（词法关键字前缀/赋值尾回归修复）与任务2（HMAC 标准实现复核）。

---

## 二、6 路交付摘要

> 格式：修复文件 / 修复策略 / 复现用例 / 反跑 / 交付报告。路M按实填入。

### 任务1：词法分析器关键字前缀标识符回归修复（P0，本轮核心）
- **修复文件**：`light-merge/src/lexer.py`（唯一改动源码）
- **真实根因（更正第19轮误判）**：例外①②的病根**不是**「关键字前缀标识符被切碎」，而是 `设 合并为 <值>` 中 `合并`+`为` 黏连 → `为` 被吞进标识符，**赋值语句丢失赋值关键字**（`去重占位` 等"语句开头段落名"由 `_scan_user_definitions` 预扫描整串保留，本不切碎）。
- **修复要点**：
  1. 嵌入关键字最大匹配收敛为 `{'为','返回','尝试'}`（**拒绝**泛化合并全部非运算符关键字——实测引发 104 个解析回归）。
  2. 新增「函数调用语境」守卫：整串后**紧随** `(`（中间无空格）→ 整串即函数名，绝不切分（修复 6 个 `断言为真(...)` 类回归）。
  3. 新增用户定义预守卫：`full_identifier in user_definitions` 时不做嵌入切分（修复 `段落 断言为真 接收:` 被切）。
- **复现用例**：`examples/test_R20_关键字前缀标识符.light`（≥10 场景）、`examples/test_R20_词法边界综合.light`（100+ 场景）
- **反跑**：`_antirun_r20_t1_词法关键字前缀.py` → **ALL OK**（A：断裂态红；B：修复态绿；C：正向控制绿 + 词法探针过）
- **交付报告**：`_task1_R20_词法关键字前缀_交付报告.md`

### 任务2：HMAC RFC 2104 标准实现复核（P0）
- **修复文件**：`light-merge/stdlib/哈希.py` + `lightharness/stdlib/哈希.py`（复核为**已标准实现**，两库一致）
- **关键结论**：第19轮登记的「部分修复」实为**状态滞后**——代码早已用 `hmac.new(key, msg, hashlib.sha256)`（RFC 2104），第20轮以 7 组对拍逐字节复核一致。
- **复现用例**：`examples/test_L095.light`（7 组：2 标准 + 5 边界：空 key/空 msg/长 key>64B/中文）→ rc=0
- **反跑**：`_antirun_r20_t2_HMAC标准实现.py` → **ALL OK**
- **交付报告**：`_task2_R20_HMAC标准实现_交付报告.md`

### 任务3：_taskM_integration_r17 评估 + 全项目关键字前缀标识符扫描（P1）
- **结论**：`_taskM_integration_r17.light` 评估为**有价值的跨模块集成测试**（六模块共存/22 断言/模拟多轮交互）→ **修复保留**（rc=0），非 SKIP。
- **扫描**：全项目 .light 扫描关键字前缀标识符，输出完整清单（文件/行号/上下文/是否切碎）。
- **交付物**：`_task3_R20_关键字前缀标识符扫描报告.md`、`_scan_r20_keyword_prefix.py`
- **关键发现**：真实文件经预扫描后 1708 个关键字前缀标识符**全部整串保留（0 被切碎）**；孤立 `Lexer().tokenize()` 的"切碎"为假阳性。

### 任务4：词法分析器边界测试套件建设（P1）
- **新增用例**：`examples/test_R20_词法边界综合.light`（100+ 场景）、`tests/test_R20_词法边界.py`
- **验证**：词法边界 pytest **76 passed**（含 4 个由 skip 翻为实断言的回归守卫）
- **交付报告**：`_task4_R20_词法边界测试套件_交付报告.md`

### 任务5：第19轮缺失交付报告补全 + 反跑脚本审查（P2）
- **补全 3 份 R19 缺失报告**：任务2（词法扩展 L-119/L-120）、任务4（字符串哈希 L-086/L-095）、任务6（字典函数_D批次 L-096/L-097/L-068~L-074）
- **反跑审查**：8 个反跑脚本逐一审查，发现 R19-T5 安全删除批量阈值遗留文件问题（已清理）
- **交付物**：`_task2_R20补全_第19轮词法扩展_交付报告.md`、`_task4_R20补全_第19轮字符串哈希_交付报告.md`、`_task6_R20补全_第19轮字典函数_D批次_交付报告.md`、`_task5_R20_反跑脚本审查报告.md`

### 任务6：docs 更新 + 临时例外清理验证 + 收口准备（P2，路M前置）
- **交付物**：`_task6_R20_docs更新草案.md`、`_verify_r20_exception_cleanup.py`、`_task6_R20_临时例外清理验证报告.md`、本框架文件
- **`test_回归.py` 清理**：`SKIP` 移除 `_taskM_integration_r17.light`；`EXPECT_RED` 移除 `test_L095.light`

---

## 三、4 个临时例外修复结果（本轮的"成绩单"）

| # | 例外 | 第19轮状态 | 第20轮结果 | 依据 |
|---|---|---|---|---|
| ① | test_宿主上下文.light | SKIP | ✅ **rc=0**，移出 SKIP | 任务1 修复 `设 X为<值>` 赋值尾 |
| ② | test_宿主事件.light | SKIP | ✅ **rc=0**，移出 SKIP | 同① |
| ③ | test_L095.light | EXPECT_RED | ✅ **rc=0**，移出 EXPECT_RED | 任务2 复核 RFC 2104 逐字节一致 |
| ④ | _taskM_integration_r17.light | SKIP | ✅ **rc=0**，移出 SKIP | 任务3 评估为必要集成测试并修复 |

**清理后 `tests/test_回归.py` 目标态**：
```python
SKIP       = {'_helper_L004.light', '_helper_L007b.light'}      # 仅 2 个载体模块（必要项）
EXPECT_RED = {'test_L101.light', 'test_L143.light'}             # 仅 2 个合规保留字预期红（必要项）
```
> 验证脚本：`_verify_r20_exception_cleanup.py` → **EXIT=0**（4 例外全清零 + 目标态断言过）

---

## 四、反跑与验证结果

### 4.1 复现用例（本轮新增）
- `test_R20_关键字前缀标识符.light` → rc=0
- `test_R20_词法边界综合.light` → rc=0
- `test_R20_词法边界.py`（pytest）→ 76 passed

### 4.2 8 个反跑脚本（串行）
- R20-T1 词法关键字前缀：**ALL OK**
- R20-T2 HMAC 标准实现：**ALL OK**
- R19-T1 词法核心 / T2 词法扩展 / T3 保留字内置映射 / T4 字符串哈希 / T6 字典函数_D批次：**ALL OK**
- R19-T5 异常模块：单独运行 **EXIT=0**；批量串跑时曾因安全删除阈值遗留 `examples/_tmp_t5_uncaught.light` 误红 → **已清理该遗留文件**（详见 `_task5_R20_反跑脚本审查报告.md` §2.1）

### 4.3 全量 examples sweep（任务1 归因）
- 修复后：**296 个 `examples/test_*.light` → 293 绿 + 2 合规 EXPECT_RED（test_L101/test_L143）+ 1 既有红（`test_会话存储.light`，HEAD 基线同样红，非本轮引入）**
- 注：`_sweep_r20.py` 原始输出为「292 passed / 3 failed」——该脚本 `EXPECT_RED` 仍含陈旧的 `test_L095.light`（被排除计数），而 test_L095 本轮已 rc=0，故真实绿数 = 292 + 1（L095）= **293**。

### 4.4 全量 CI
- **第1次运行**：pytest **409 passed + 1 failed**（`test_审批.light` 计时抖动，R20-C，非本轮） + smoke **5/5 通过**
- **第2次运行（确认）**：pytest **410 passed（EXIT=0，957.94s）** + smoke **5/5 通过** → **全部通过**
- **验收判定**：**✅ 达成「全量CI无例外全绿」目标**（410 passed ≥ 330；smoke 5/5）；第1次的唯一红为环境侧 wall-clock 抖动（R20-C），第2次未复现。

---

## 四·附、本轮成功标准对照（任务书 §本轮成功标准）

| 成功标准 | 结果 | 依据 |
|---|---|---|
| 4 个 CI 临时例外全部清零 | ✅ | ①②③④ 全部 rc=0；`_verify_r20_exception_cleanup.py` EXIT=0 |
| 全量 CI 无例外全绿（pytest 330+ / smoke 5/5） | ✅ | 第2次 **410 passed** + smoke **5/5** |
| 关键字前缀标识符系统性修复 | ✅ | 真实文件口径 1708 个全整体成词；收敛式最大匹配 + 调用语境守卫 |
| HMAC 与 RFC 2104 完全一致 | ✅ | test_L095 7 组对拍逐字节一致 |
| 词法边界测试套件覆盖 100+ 场景 | ✅ | `test_R20_词法边界综合.light` + `tests/test_R20_词法边界.py`（76 passed） |
| 6 路交付物齐全（每路：复现+反跑+报告） | ⟨待路M核对⟩ | 见 §二 各路索引 |
| docs 三件回填 | ⟨待路M执行⟩ | 草案见 `_task6_R20_docs更新草案.md` |
| 两项目 git 提交完成（用户手工 push） | ⟨待路M执行⟩ | 见 §六 |

---

## 五、docs 三件回填

> 依据 `_task6_R20_docs更新草案.md` 执行。

1. **对标清单**（`docs/功能对标/对标清单.json`）：追加 **#128 ~ #130**，总数 127 → **130**。
2. **缺陷账**（`docs/功能对标/语言缺陷账.md`）：
   - 订正 `L-095` 行 → 「已修复（2026-09-14；第20轮复核 RFC 2104 一致）」+ 详述段补充；
   - 订正 `L-097` 行 → 「已修复（既有实现覆盖，第19轮为验收确认非新修）」；
   - 新增 **L-151**（赋值尾 `X为<值>` 并坏）、**L-152**（嵌套段落形参名不被预扫描覆盖）。
3. **行为差异清单**（`docs/功能对标/行为差异清单.md`）：追加 **R20-D1 ~ R20-D3**（词法最大匹配收敛化 / HMAC 标准实现订正 / 关键字前缀标识符行为）。
4. **非缺陷登记（不入账）**：R20-C（test_审批.light wall-clock 计时抖动，测试健壮性）。

---

## 六、git 提交

> 用户手工 push。分路提交（light-merge + lightharness 独立）。

### light-merge 项目
- ⟨待填：`⟨commit⟩` 第20轮：src/lexer.py 回归修复（`设 X为<值>` 赋值尾 + 函数调用语境守卫 + 用户定义预守卫）⟩

### lightharness 项目（分路 + 收口）
- ⟨待填：任务1 提交⟩
- ⟨待填：任务2 提交⟩
- ⟨待填：任务3 提交⟩
- ⟨待填：任务4 提交⟩
- ⟨待填：任务5 提交⟩
- ⟨待填：任务6 提交⟩
- ⟨待填：收口提交（docs 三件 + 收口说明 + test_回归.py 清理）⟩

---

## 七、遗留问题与后续方向

### 7.1 本轮遗留
- **L-152**（嵌套段落形参名被切碎）：未修，属 P0-A「词法确定性切词重构」范畴。
- **R20-C**（test_审批.light 计时抖动）：未改测试；建议 `等待结果` 改用 `time.monotonic()` 或放宽下界。
- **`test_会话存储.light`** 既有红（非本轮引入），未登记为本轮例外。

### 7.2 后续方向建议
1. **P0-A 词法确定性切词重构**：从启发式打地鼠改为「最长匹配优先 + 标识符内部不拆分」的确定性切词，根治 L-151/L-152 类问题（本轮已确认泛化合并会引发 104 回归，须以重构而非扩表解决）。
2. **回归主线**：回到 deepseek-harness 上游复刻，继续对标未完成模块。
3. **上下文敏感保留字**（L-101/L-143）：如需放开 `回调`/`作用域` 为变量名，需词法/parser 协同重构。

---

## 八、路M修正记录

> 路M填入收口中对交付物的修正（用例改判、反跑预期修正、清单收敛等）。
- ⟨待填⟩
