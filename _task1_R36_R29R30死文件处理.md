# 任务1（R36）交付报告：R29/R30 死文件终态处理

> 目标：将 `examples/test_R29_CCW精简边界.light` 与 `examples/test_R30_CCW通用化边界.light`
> 从「EXPECT_RED 止血清」转为**终态**。
> 采用方案：**方案B（移入 docs/历史存档/）**——两文件统一处理，不删文件、不一个重写一个存档。

---

## 一、方案选择理由

| 维度 | 方案A（重写为合法 .light） | 方案B（移入 docs/历史存档/）✅ |
|---|---|---|
| 前提是否成立 | 原文件的「CCW 清零后位与/幂次/除非仍整词成 IDENTIFIER 可作函数调用」已被 R30 自推翻 | 同左，前提已死 |
| 能否写出有意义断言 | 不能：这些名字现被切分（`位与`→`位`+`与`），`.light` 运行期无法钉桩 token 切分；且 `位与(3,1)` 等是未定义函数，必 rc!=0 | 无需：token 层钉桩已由 pytest 接管 |
| 历史意图可追溯 | 是（改写后） | 是（`git mv` 保留完整历史） |
| 门禁噪音 | 需持续维护新文件 | 彻底移出收集范围，门禁零噪音 |

**决定性事实**（第35轮路M 收口已用 lexer 实证）：
- `位与`→`位`+`与`、`幂次`→`幂`+`次`、`除非`→`除`+`非`、`应当`→`应`+`当`。
- 两文件还各含**孤立 `捕获` 块缺 `尝试:`** + 调用**未定义函数名**（位与/位非/幂次/记录类型…）。
- 自入库（R29/R30）起**从未 rc=0**，整份用例从未真正执行。

→ 运行期 `.light` 边界套件已**不可恢复**；其唯一有价值的信息（token 切分形态）已由
`tests/test_R29_CCW表分批精简_token.py` / `tests/test_R30_CCW保留10条通用化_token.py`
的 token 流用例钉住。故**方案B 是最诚实、零噪音的终态**。

---

## 二、原始意图分析（两文件想测什么）

### test_R29_CCW精简边界.light（CCW 37→10 精简边界）
- 原意图：钉住保留的 10 条 CCW 真护栏（位与/位异或/位或/位非/零除错误/应当/除非/测试_生成问候语/幂次/记录类型）整词成 IDENTIFIER，并验证删除的 27 条 token 序列不变。
- 有效部分：被删条目的「token 序列不变」已由 G1 全量反跑覆盖；保留条目的「整词」前提已因 R30 CCW 清零（184→0）自推翻。

### test_R30_CCW通用化边界.light（CCW 10→0 清零边界）
- 原意图：钉住原 10 条 CCW 条目通用化后的「整词成 IDENTIFIER」现状 + CCW 清零验证。
- 有效部分：同上，前提已死；且文件中 `非 真`/`位 为 1`/`零 个` 等独立关键字用法与运行期无关。

**结论**：两文件的设计前提均被 R30 自身推翻，运行期内容（孤立捕获块 + 未定义函数）从未可执行。无「仍有效断言」可保留，无「需重写验证」的合理意图。

---

## 三、执行（已落地）

1. 建存档目录：`docs/历史存档/`
2. `git mv`（保留历史）：
   - `examples/test_R29_CCW精简边界.light` → `docs/历史存档/test_R29_CCW精简边界.light`
   - `examples/test_R30_CCW通用化边界.light` → `docs/历史存档/test_R30_CCW通用化边界.light`
3. `tests/test_回归.py`：从 `EXPECT_RED` 移除这 2 条登记（原止血清注释改为说明性注释，
   解释移档原因），并保留「不删文件」说明。

> 不删除文件、两文件统一处理（均移档）均满足铁律。

---

## 四、验证

- `tests/test_回归.py` 全量运行：**381 passed / 0 failed**（原 383 含 2 条 EXPECT_RED 用例，
  现随文件移出 `examples/` 收集范围，门禁不再感知，噪音消除）。
- `test_expected_red_registered` / `test_green_exception_consistent` 自洽通过。
- `git status` 确认两文件为 rename（R）状态，examples/ 已无残留，docs/历史存档/ 含两文件。

---

## 五、收口影响

- 门禁从「2 条 EXPECT_RED 止血清」转为「零死文件噪音」，符合「EXPECT_RED 应保持为空」验收口径。
- 后续若有人想恢复运行期 CCW 边界测试，应先在 token 层（pytest）补齐，而非 resurrect 这两个死文件。
