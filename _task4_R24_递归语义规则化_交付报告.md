# 第24轮 任务4 交付报告：`_match_keyword` 递归语义规则化

> 日期：2026-09-14 ｜ 任务书：复刻_第24轮_任务prompt分发_阶段D单字通用化.md ｜ 状态：已完成
> 修改文件：light-merge/src/lexer.py（CSS 定义区：类别代数文档块 + 文件末尾 import 时自校验断言，共 +67 行，运行时代码零改动）

---

## 一、递归语义分析（任务书工作项1）

`_match_keyword`/`_skip_compound_safe_and_match` 的递归返回路径穷举（行1290 附近）：

| 递归结果 | 返回 | 下游消费 | 语义 |
|---|---|---|---|
| 从 pos+1 起【找到`之`】 | (当前单字, 1) | 输出 KEYWORD(当前单字) | `之` 是成员访问符始终切分 → 当前单字成词（自之X 的 自） |
| 找到【其他关键字】 | (None, 0) | 不匹配 → 调用方整体并入标识符 | `去除空格`：除→空，`空` 是真关键字命中 → 除 被并入 |
| 【没找到】关键字 | (内层单字, 1) | 内层是 compound-safe → 被 skip | `乘阶乘`：乘→阶(非关键字)→回报 阶？——实际 阶 非关键字即"没找到"分支，内层回报使 调用方把 乘 视为复合词前缀跳过 |
| pos+1 已到词尾 / 非关键字起始 | (当前单字, 1) | 输出 KEYWORD | 词尾单字照旧（返回 真 的 真——但 真 在表中由第一层输出 KEYWORD） |

**副作用语义的本质**：compound-safe 集合的成员资格决定了「单字+汉字串」是浮出为 KEYWORD 还是作为构词成分被并入。代码注释警告：改成纯最长匹配会引发 100+ 回归（37255 文件 A/B 实测：10的幂/索引/种类/阶乘 等）。

## 二、规则化方案：类别代数（任务书工作项2/3/4）

探索路径（全部有全语料 A/B 数据支撑，逐步收敛）：
1. 纯类别推导 `全集 − _P0A_OP − _OPERATOR_KEYWORDS − NS − AW − VAL`（43字）直接替换 → **26 文件漂移**（丢失乘/减/加/除/模/真/空/除 8 个"语义双栖"字的递归资格）；
2. 加入 `- _P0A_SEP` → 40 漂移（之/为 被吞，破坏 数列之长度/成员访问）；
3. 加入 `- _TRAILING_ALIAS_CLASS` → 26 漂移（同1，TAM 推导本就依赖 CSS，需先锚定）；
4. **不可约性证明**：F(43字) 需二分为「构词胶水」(22) 与「语句别名」(21)，尝试用 KEYWORDS_* 类别表区分——实测各类别两族混杂（KEYWORDS_CLASS G:3/T:2、KEYWORDS_LOOP G:4/T:3 等），**无法从现有类别表推导**。该二分是句法行为差异（词中浮出 vs 词尾并入），R23 任务1 已用字面量 CSS 作为二分锚。

**最终落地方案（与 R23 同构的"类别代数 + 字面量锚 + import 自校验"）**：
- CSS 定义上方：完整推导代数文档（F/DUAL/TAM 三段定义 + 不可约性说明）；
- CSS 保持 30 字字面量（后被任务3 并发精简为 17 字，见 §五）；
- 文件末尾新增 **import 时自校验断言**：`字面量 == (F − TAM) ∪ DUAL` 且 `_TRAILING_ALIAS_CLASS == F − 字面量`——两处任何漂移都会让编译器 import 直接失败；
- **运行时代码零改动**：递归语义行为与 HEAD 逐字节一致。

DUAL（8字语义双栖集）的类别表达：`(OPERATOR_VERBS ∩ 单字 − {步,至,幂}) ∪ {真,空} ∪ {到}`——乘/减/加/除/模 是算术运算符兼构词胶水（甲加乙 切分 / 加法 并入），真/空 是值字面量兼构词胶水（返回 真 / 标准输出失真），到 是范围符兼构词（文件未找到）。步/至/幂 无构词需求（甲至10/甲步2/10的幂 实证）。

## 三、验证结果（任务书验证标准逐项）

1. **递归语义规则化**：✅ 类别代数 + 等价断言落地（防漂移机制）；递归运行路径零改动，行为与 R23 基线逐字节一致。
2. **100+自由名保护**：✅ 探针 16 形态全 PASS（_r24_t4_probe.json）：去除空格/10的幂/种类/索引/阶乘/乘阶乘/加斐波那契/标准输出失真 均整体成标识符。
3. **`之` 成员访问符**：✅ 自之姓名 → 自+之+姓名；数列之长度 → 数列+之+长度；遍历 块长 之 块长表 → 块长(词尾长并入)+之+块长表。
4. **全语料 token 零变化**：✅ 断裂态(HEAD R23) vs 修复态 **772 文件零差异**（后续合并态含任务1 的 `的` 修复预期变化 2 文件，见反跑 B 判据）。
5. **全量CI无回归**：✅ 词法5测试文件 11 failed 中归属本任务的为 **0**（内存回滚实验 + HEAD/NEW token 逐形态对比实证：如果数…/如果为真 HEAD 与 NEW 完全一致，属 HEAD 既有噪音；新增 2 个 subtest 失败归任务3 删除 是/首 的单测契约——正是任务2 报告 §四预警的"单测契约需同步修订"场景）。
6. **反跑 ALL OK**：_antirun_r24_t4_递归语义规则化.py（A 基线自检 / B 合并态差异⊆任务1预期 / C 16形态正向控制 / D 断言有效性——篡改字面量后 import 必 AssertionError）。
7. **性能不回退**：✅ 运行时代码零改动，性能与基线持平（任务5 全量扫描的独立计时佐证）。

## 四、与任务1的区域协调（任务书互斥注记）

任务1 修复 `的` 递归吞分隔符（_skip_compound_safe_and_match 下游 4 道闸门），本任务**未触碰**该函数与 `_match_keyword` 运行时代码——本任务的落点在【数据源治理】（成员资格的类别代数 + 自校验），与任务1 的【分支行为修复】在代码上正交，实际合并无冲突。任务1 已并发落地且其预期 token 变化（学生模块.light）已在反跑 B 判据中显式登记。

## 五、并发合并说明

- 任务3 并发将 CSS 30→17（删除13条，其隔离验证引用了本任务 `_task2_R24_单字逐条验证_证据.json`）；本任务的等价断言对任意子集成立（(F−TAM)∪DUAL 恒等式随字面量同步收敛），合并后 import 断言通过。
- 任务1 并发新增 test_R24_的递归修复/运算符单字守卫/值字面量守卫.light 及 的专项修复；反跑 B 判据的预期变化集合已据实登记。

## 六、遗留与建议（供路M/后续轮次）

1. **首/是 的 2 个单测契约失败**（任务3区域）：tests/unit/test_lexer.py 的 test_compound_safe_supplement_merges_words（首字母大写/首项）与 test_identifier_safe_module_print_merged_inside_word（是可打印）断言这两字词中合并——任务3 删除后立红。请任务3 路按 R22 确立的"语料中立+单测契约双验证"补课（恢复条目或修订契约并明示）。
2. 后续若要彻底消除 CSS 字面量，需要语言层裁决"构词胶水 vs 语句别名"的可判定判据（当前是句法行为二分），建议在阶段D 第二轮以语料统计+人工裁决固化。
