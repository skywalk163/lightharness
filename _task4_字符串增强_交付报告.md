# 任务4 交付报告：字符串与文本增强库（src/字符串增强.light）

> 任务来源：`G:\dswork\duan-light-merge\复刻_第18轮_任务prompt分发_缺陷修复轮 (1).md` 任务4
> 日期：2026-09-13 ｜ 对标 #119（光明语言工具库·缺陷修复轮）
> 命名预检查：src/ 下无 字符串增强.light 同名模块 ✓

---

## 一、函数清单与修复层级

| 函数 | 修复对象 | 层级 | 说明 |
|---|---|---|---|
| 码转字符(码) | L-133 | **A级** | 标准命名包装编译器内置 字符自码位（chr）——第16轮已确认编译器 builtin_map 含 字符自码位/码位（"字符 <-> 码位"），L-133「缺 chr 内置」旧账以此收口；测试专条验证 码转字符(0)=NUL、(123)/(125)=花括号 |
| 字符转码(字符) | L-133 配套 | B级 | 包装内置 码位（ord），统一命名；round-trip 断言（含码点 200） |
| 构造JSON字符串(字典) | L-089 | B级 | 用 序列化JSON 从字典构造 JSON 文本，消除手写 `{"…}"` 源码字面量的插值破坏风险；round-trip 断言 |
| 转义花括号(文本) | L-089 | B级 | 单花括号→双花括号（源码安全形态）；测试输入/期望全部经 码转字符(123)/(125) 构造（本测试自身就是 L-086/L-089 绕法的演示） |
| 安全正则匹配(模式, 文本) | L-086 | B级 | 模式以**运行时字符串参数**传入（源码字面量里的 `{n,m}` 会被编译破坏，运行时拼接不受影响）；底层 stdlib 正则表达式.查找（search 语义） |
| 拼接量词(前缀, 最小, 最大, 后缀) | L-086 | B级 | 用 码转字符(123)/(125) 构造 `{n,m}` 量词，覆盖上游原始场景（1~4 位十六进制段） |
| 字符串重复文本 | 通用 | B级 | 乘法实现（避免与内置 字符串重复 同名互调） |
| 填充左/填充右 | 通用 | B级 | 包装内置 字符串对齐右/左（rjust/ljust），填充符缺省空格 |
| 截断文本 | 通用 | B级 | 超长截断+省略号（缺省 "..."）；最大长度 ≤ 省略号长时纯截断 |
| 分割保持分隔符 | 通用 | B级 | 经典 split+重组：首段原样、后续每段带分隔符前缀（空段保留、连续分隔符产生独立段） |

## 二、测试验证

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"; python 运行.py examples/test_字符串增强.light
test_字符串增强 PASS   （rc=0）
```

9 组 ≥40 断言：码转字符/字符转码（6 码点+round-trip+NUL）、构造JSON（简单/嵌套/数组/空/round-trip）、转义花括号（四形态，全部经 码转字符 构造）、安全正则（量词拼接/上下限/普通正则/十六进制段场景）、重复（4 形态）、填充左右（5 形态）、截断（5 形态含纯截断）、分割保持分隔符（5 形态）、组合使用（A 判据：量词入 JSON）。

## 三、反跑结果（3/3 ALL OK）

`lightharness/_antirun_t4_字符串增强.py`（BASE 自定位；try/finally 恢复）：

```
PASS C=变异 码转字符 码偏移改错 -> 红 (变异后运行 rc=1（期望非 0，真红）)
PASS 字节级恢复校验 (sha256 a090e8f7da79)
PASS A/B 判据（组合使用 + NUL/量词边界）回归绿 (rc=0)
ALL OK
```

## 四、开发中发现并登记的新缺陷（L-157）

- **L-157（按任务4预分配区间）**：调用名为 `匹配` 的函数（stdlib 正则表达式.匹配 = re.search 包装）时，编译产物生成 Python `match(模式, 文本)` **语句**（Python 3.10 match-case 语法），报 `expected an indented block after 'match' statement`。绕法：改用同模块的 `查找(模式, 文本)`（re.search 返回匹配串或 空）。L-158 空缺顺延。
- 附带确认：L-086/L-089 现行犯在测试断言字面量中复现（`{1,4}`、`{甲}` 被破坏），全部以 码转字符 构造绕法——这正是本库的用途演示。

## 五、移交清单

- `lightharness/src/字符串增强.light`、`examples/test_字符串增强.light`（PASS rc=0）、`lightharness/_antirun_t4_字符串增强.py`（3/3 ALL OK）、本报告。
- 移交路M：对标清单新增 **#119**；缺陷账 **L-157** 登记（L-158 空缺）；行为差异 **R18-D4**（安全正则匹配为 search 语义、转义花括号为源码安全形态约定）；CI 增 1 test + 1 反跑。
