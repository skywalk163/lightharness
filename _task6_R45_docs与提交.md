# 第45轮 任务6：docs 回填 + 探针移档 + 双仓提交

## 1. 缺陷账（`docs/功能对标/语言缺陷账.md`）

| 编号 | 标题 | 状态 |
|---|---|---|
| **L-166** | 语言层对「函数内写模块级同名变量但未声明 `全局`」无任何告警 | ✅ **已修**（本轮任务2） |
| **L-167** | `外部命令.等待进程` 超时 kill 后未收尸 → 僵尸/管道泄漏 | ✅ **已修**（本轮任务4） |
| **L-168** | `test_启动配置` 的「模块级夹具跨段读写」不成立（注释声称 L-006 自动 nonlocal，实测无效）+ 两个包装段落是死代码 | 登记立项，**未修** |
| **L-169** | light-merge 自带示例存在成片 L-165 类影子变量缺陷（5 文件 19 条） | 登记立项，**未修**（跨仓 + 行为变更） |

L-165 条目下补了指向 L-166 的链接（其「建议给编译告警」已被落地）。

## 2. 其余 docs

- **对标清单**：追加 **#174**（L-166 影子变量编译告警）、**#175**（0.82 git 就位 + 双平台对齐 + flaky 加固 L-167）。
  ⟶ 用**外科式文本插入**（文件是 CRLF + 非标准缩进，整体 `json.dump` 会产生 5000+ 行 diff）：
  实际 diff **3 增 1 改**，条目数 173 → 175，JSON 合法性已校验。
- **行为差异清单**：新增 **R45-D1**（L-166 告警）、**R45-D2**（L-167 收尸 + 解释器不写死 `python`）。
- **多平台差异清单**：新增「R45 双平台回归（git 就位后）」对照表，并把 R44 那节标记为历史。

## 3. 探针移档

`docs/历史存档/R45探针/`（工作区已无 `_r45*` 残留）：

| 文件 | 用途 |
|---|---|
| `_r45_flaky_probe.py` | 复刻 `_run()` 条件复现 flaky（15/15 绿 → 证明与隔离 cwd 无关） |
| `_r45_影子告警测量.py` | 全语料影子变量扫描（702 文件 / 40 条） |
| `_r45_回填对标清单.py` | 对标清单外科式插入（一次性） |
| `_r45_影子探针.light` | 告警浮出验证 |
| `_r45_L006探针.light` | **决定性证据**：证明「L-006 自动 nonlocal 跨段读写」不成立（跨段读 = `[]`） |

临时大包 `_r44_sync.tar.gz` 已由脚本自动清理。

## 4. 双仓提交（**均不 push**）

### light-merge（语言本体）
- `src/scope_shadow_check.py`（新增）
- `src/compiler.py`（补 `warnings` 键 + 按次重置 + 可选接入检查）
- `cli/light.py`（`_warn_shadow_scope` 接入主文件与依赖模块）

**light-merge 侧自证**（跨仓改动必须自证）：
- `examples/advanced.light` rc=0 ✅
- `compile()` 返回 `warnings` 键可用、`errors=[]` ✅
- 两处红（`_test_nested_closure.light` BOM 词法错、`basic.light`）经验证为**既有**问题：
  关掉告警（`LIGHT_WARN_GLOBAL_SHADOW=0`）后**同样失败**，与本轮改动无关。
- 另有 1248 条 lightharness 全量 pytest（含 403 个 .light 用例的真实编译执行）
  在基线 7 红不变 ⇒ 编译器改动未引入回归。

### lightharness
`stdlib/外部命令.py`、`运行.py`、`examples/test_子进程后台.light`、`examples/test_子进程码.light`、
`examples/test_R45_影子变量告警.light`、`tests/test_R45_L166影子变量告警.py`、
`scripts/同步0.82.py`、`scripts/多平台矩阵.py`、`reports/R45_跨平台门_core.json`、
docs 三件套 + 缺陷账、`docs/历史存档/R45探针/`、6 份任务报告。

> 未纳入提交：`reports/同步0.82_远程目录.txt`（机器本地的远程副本指针，`sync` 会重建）。

## 5. 遗留与建议

1. **L-168**：建议下轮立项——`主()` 补三条 `全局` 声明，并启用或删除 `抛数组manifest`/`抛缺失manifest` 两个死段落。
2. **L-169**：light-merge 自带示例的成片影子变量缺陷（5 文件 19 条），建议由 light-merge 侧决定是否修（补 `全局` 或改显式传参）。
3. **light-merge 自身测试套件**本轮未跑（时间与范围所限）；当前证据来自 lightharness 1248 条全量 + 冒烟。建议下轮补跑。
