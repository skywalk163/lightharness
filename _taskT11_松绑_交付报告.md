# 第11轮 lightharness · 松绑绕法 + 缺陷账回填 —— 交付报告

> 仓库：`g:\dswork\duan-light-merge\lightharness`（独立 git 仓库）
> 背景：编译器 L1/L2/L3 缺陷修复（2026-08-27 合入 light-merge main）后，第十轮总纲增补 A
> 要求的「修复后应逐步松绑绕法（见『处置完回填』要求）」落地。
> 日期：2026-08-28

---

## 1. 本轮目标与结论速览

用修复后的光明编译器重跑缺陷复现套件（18 用例），确认 L1/L2/L3 修复全部生效后，
**移除 lightharness 源码中的全部安全名/工厂/改名绕法、恢复正式写法**，并回填缺陷账/验收判据/对标清单状态。

| 项 | 结果 |
|---|---|
| 缺陷复现套件（L-001~L-016，18 用例） | **全部转绿**（含修复 test_L011 用例自身入参缺陷） |
| 全量 examples 回归（62 个用例） | **0 红**（含修复 test_二分2 遗留键错误） |
| 绕法移除 | 退出码值 / 收尾进程 / 会话工厂 / 补录 / 协程代理模块名 全部恢复正式写法 |
| 文档回填 | 语言缺陷账 / 编译器缺陷验收判据 / 反跑判据 / 对标清单 / 命名规范 状态同步 |

---

## 2. 改了哪些文件（逐个 + 一句意图）

### 2.1 源码松绑（src/）

| 文件 | 动作 | 意图 |
|---|---|---|
| `src/子进程.light` | 修改 | `结果.退出码值`→`结果.返回码`（L-010 已修复，整段成词）；`外部命令.收尾进程`→`外部命令.等待进程`（L-011 已修复）；清理绕法注释。 |
| `src/沙箱.light` | 修改 | `结果.退出码值`→`结果.返回码`；清理绕法注释。 |
| `src/终端.light` | 修改 | 删除安全名工厂 `会话工厂`；调用方改直接 `新建 终端.终端会话(...)`（L-010/L-012 已修复）。 |
| `src/事件.light` | 修改 | 移除 L-005 绕法：`造取消处理器` 内直接 `总线.监听器表[事件名]` 下标访问（不再先取局部变量）。 |
| `src/钩子.light` | 修改 | 移除 L-005 绕法：`分发` 内直接 `总线.监听器表[类型]` 下标访问。 |
| `src/会话.light` | 修改 | 删除 L-007b 安全名方法 `补录`（调用方现可直接 `记录事件`）；清理 L-003/L-004 过时注释。 |
| `src/持久化.light` | 修改 | `会话对象.补录(...)`→`会话对象.记录事件(...)`（L-007b 已修复）。 |
| `src/协程代理.light` → `src/异步代理.light` | 重命名 | 恢复任务书正式模块名（L-015 已修复，`从 异步代理 导入` 可用）；删除原占位 stub；更新头部注释。 |

### 2.2 stdlib 清理

| 文件 | 动作 | 意图 |
|---|---|---|
| `stdlib/外部命令.py` | 修改 | 删除死代码绕法别名：`退出码值` property（L-010 安全字段）、`收尾进程` alias（L-011 安全名）；证明正式写法 `返回码`/`等待进程` 可直接使用。 |

### 2.3 用例修复（examples/）

| 文件 | 动作 | 意图 |
|---|---|---|
| `examples/test_终端.light` | 修改 | 5 处 `终端.会话工厂(...)`→`新建 终端.终端会话(...)`。 |
| `examples/test_异步代理.light` | 修改 | `从 协程代理 导入 异步代理循环`→`从 异步代理 导入 异步代理循环`。 |
| `examples/test_L011.light` | 修改 | 修复用例自身入参缺陷：主段落 `等待进程(空, 5)`→真实进程 `后台执行("echo hi")`；启用 `断言修复后()`；用例转绿（编译器侧 L-011 已修复，原红仅因 `空`(None) 非合法进程）。 |
| `examples/test_二分2.light` | 修改 | 补 `记录事件` 缺 `序号` 键（早期遗留半成品测试，键错误基线红），全量回归清零。 |

### 2.4 文档回填（docs/功能对标/）

| 文件 | 动作 | 意图 |
|---|---|---|
| `语言缺陷账.md` | 修改 | L-001~L-017 状态列更新为「已修复（L1/L2/L3，2026-08-27；第11轮验证绿）」，复现套件列红→绿；L-009 保留工程层未修。 |
| `编译器缺陷验收判据.md` | 修改 | 缺陷矩阵「当前」列全部更新为绿；头部加第 11 轮结论。 |
| `反跑判据.md` | 修改 | 状态一览表更新为绿，补 L-012~L-016 行；头部加第 11 轮结论。 |
| `对标清单.json` | 修改 | 「说明」数组加第 11 轮松绑条目（JSON 校验通过）。 |
| `光明命名规范v1.md` | 修改 | 头部加松绑说明：L-004/L-007/L-010/L-011/L-012/L-015 相关约束已随缺陷关闭松绑。 |

---

## 3. 验证方式与结果

1. **编译器修复前置验证**：重跑 18 个缺陷用例，L-001~L-016 全部 rc=0（L1/L2/L3 修复生效）。
2. **探针验证 L-015**：`从 异步代理 导入 异步代理循环` 解析通过（此前 stub 不可导入），随后完成模块名回归。
3. **定向回归**：子进程/子进程码/子进程后台/沙箱/终端/事件/局部事件/局部事件2/钩子/会话/持久化/异步代理/联调CLI/总入口/客户端/代理/并行/压缩/文件/文件深/工具/工具深/流/消息 全部 rc=0。
4. **全量回归**：`examples/*.light` 62 个用例（排除 2 个 helper 载体）全部 rc=0，**0 红**。
5. **std 库清理验证**：删除 `退出码值`/`收尾进程` 后，子进程/沙箱/终端模块仍全部通过（证明正式写法成立，无残留依赖）。

---

## 4. 已知遗留 / 边界

1. **L-009（工程层）未修**：`-replace` 类文本批量替换命中注释内 `导入 X` 字样的问题属工程层陷阱，非语言运行时缺陷；保持「工具」状态，需脚本演示红（见 `反跑判据.md` §4）。
2. **stdlib 顶层模块重名冲突未修**：`src/重试.light` 与 `stdlib/重试.light` 顶层重名时 `from 重试 import` 恒命中 stdlib，`src/重试策略.light` 命名保留（L-001 家族遗留，非本轮修复范围）。
3. **成员级改名未做**：如 `头信息`（原 `头部`）、`轮号/步号`（原 `轮次计数/步骤计数`）等历史安全改名仍保留——属 L-004 家族命名约束的历史产物，功能等价，未纳入本轮（避免大范围改动引入回归风险）。
4. **平台**：仅 Windows 本机实测（与项目既有惯例一致），POSIX 分支未实测。
5. 全部修改保留了原 CRLF/LF 行尾格式，未引入行尾变更。

---

## 5. 复跑方式

```powershell
Set-Location G:\dswork\duan-light-merge\lightharness
python 运行.py examples/test_L缺陷清单.light   # 逐条打印 L-* 复现状态
python 运行.py examples/test_L011.light        # L-011（已启用断言，绿）
python 运行.py examples/test_终端.light        # 终端（会话工厂 已移除，直接 终端.终端会话）
python 运行.py examples/test_异步代理.light     # 异步代理（模块名已回归正式名）
```
