# 第26轮路M收口说明 —— CS词首并入通用化

> 日期：2026-09-15 ｜ 方向：阶段E——_COMPOUND_SAFE_SINGLE词首并入通用化（CS表完整通用化关键一步）
> 跨项目操作：light-merge（编译器词首并入规则）+ lightharness（验证+测试+反跑）
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）

---

## 一、本轮概述

第25轮将CS词尾并入从"CS补集"改为"正面类别F"，CS表退化为"词首并入锚"（30条）。本轮实现词首并入的正面通用规则，使CS表不再需要逐词登记，最终CS表从30条缩减为16条。

### 核心成果

| 指标 | 结果 |
|---|---|
| 词首并入正面规则 | `_P0A_HEAD_MERGE_SINGLE`（22字，F−排除集） |
| 决定性站点 | 第一层skip_verb（lexer.py:2553） |
| CS表变化 | **30→16**（净删14字） |
| 删除14字 | 余/例/出/则/常/引/接/断/末/试/跳/过/长/首 |
| 保留16字 | 乘/减/列/到/加/对/是/模/段/的/真/空/类/自/配/除 |
| 全语料token变化 | **0/843文件** |
| pytest | 167 passed, 1 skipped |
| .light边界测试 | 3个 rc=0（正向/反向/混合） |

---

## 二、词首并入正面规则（任务1-2）

### 规则定义
- **正面类别**：`_P0A_HEAD_MERGE_SINGLE = F − _P0A_HEAD_SPLIT_SINGLE`（22字）
- **排除集**：`_P0A_HEAD_SPLIT_SINGLE = _P0A_SUFFIX_SPLIT_SINGLE`（复用R25词尾排除集，五类语义排除：运算符/分隔符/值字面量/范围步长/await）
- **规则**：单字关键字在词首位置（scan_pos=0）+ 后随汉字（`_is_han`）+ ∈正面类别 → 并入标识符

### 实现站点（勘察偏差）
任务书预期改`_match_keyword`（:1227），实测决定性站点在**第一层skip_verb（:2553）**：
- `_match_keyword`（:1227）不套用正面规则——会打掉R24 L-153的`自之姓名`修复（`之`∈_P0A_OP，但判据看的是`自`的后随字）
- `_match_keyword`的CS递归保留为配套保护——"最内层CS单字回报"语义被`去除空格`等stdlib函数名依赖
- 嵌入式扫描三处（B/C/D，:2745/2829/2982）逐字同判据同步，保证探测循环与输出循环一致性

### 与词尾规则的对称结构
| 维度 | 词尾并入（R23/R25） | 词首并入（R26） |
|---|---|---|
| 正面类别 | `_TRAILING_ALIAS_CLASS = F`（43字） | `_P0A_HEAD_MERGE_SINGLE = F−排除集`（22字） |
| 位置条件 | scan_pos>0 | scan_pos==0 |
| 后随条件 | 无（已在标识符内部） | 后随汉字（`_is_han`） |
| 排除集 | `_P0A_SUFFIX_SPLIT_SINGLE`（五类21字） | `_P0A_HEAD_SPLIT_SINGLE`（复用同上） |

**不对称性**：词首并入多一个"后随汉字"条件，因为词首位置的关键字可能是真正的语句关键字（`类 名称`→关键字，`类型`→并入标识符）。

---

## 三、CS表逐条验证删除（任务3）

### 删除14字（G1∧G2∧G3全通过）

| 字 | G1 | G2 | G3 | 词首复合名 | 说明 |
|---|---|---|---|---|---|
| 余 | 0 | ✅ | ✅ | 余数 | R25态G1=0但G2失败，正面规则后G2通过 |
| 例 | 0 | ✅ | ✅ | 例表 | 同上 |
| 出 | 0 | ✅ | ✅ | 出错 | 同上 |
| 则 | 0 | ✅ | ✅ | 则例 | 同上 |
| 常 | 0 | ✅ | ✅ | 常规 | 同上 |
| 引 | 0 | ✅ | ✅ | 引导 | 同上 |
| 接 | 0 | ✅ | ✅ | 接续 | 同上 |
| 断 | 0 | ✅ | ✅ | 断裂 | **R26增量可删**：R25基线G1=10打红，正面规则后G1归零 |
| 末 | 0 | ✅ | ✅ | 末项 | R25态G1=0但G2失败 |
| 试 | 0 | ✅ | ✅ | 试探 | 同上 |
| 跳 | 0 | ✅ | ✅ | 跳表 | 同上 |
| 过 | 0 | ✅ | ✅ | 过载 | **R26增量可删**：R25基线G1=1打红，正面规则后G1归零 |
| 长 | 0 | ✅ | ✅ | 长度 | R25态G1=0但G2失败 |
| 首 | 0 | ✅ | ✅ | 首项 | 同上 |

### 保留16字（某道判据失败）

| 分类 | 字 | 保留理由 |
|---|---|---|
| 运算符（5字） | 乘/减/加/除/模 | ∉正面类别（排除集），词首后随汉字须并入（乘法/模组），G2失败 |
| 值字面量（2字） | 真/空 | ∉正面类别，词首后随汉字须并入（真空/空格），G1+G2失败 |
| 特殊语义（9字） | 列/到/对/是/段/的/类/自/配 | 各有独立保留理由（详见任务3交付报告） |

---

## 四、边界测试与反跑（任务4）

- **正向.light**：`test_R26_词首并入正向.light` rc=0（14字已删+16字保留的词首并入场景）
- **反向.light**：`test_R26_词首并入反向.light` rc=0（硬语句关键字必须切分：如果/那么/段落/设/己/返回/尝试/为/打印等）
- **混合.light**：`test_R26_词首并入混合.light` rc=0
- **pytest**：`test_R26_词首并入_token.py` 167 passed, 1 skipped（含保护表形态断言CS=16、_P0A_HEAD_MERGE_SINGLE定义断言）
- **全量反跑**：ALL OK，843文件token零变化

---

## 五、递归简化与联动评估（任务5）

### 递归与类属性
- **`_skip_compound_safe_and_match`**：保留现状不简化。递归深度仅1-3层（CS 16字），性能收益~0；"最内层CS单字回报"语义被`去除空格`等stdlib函数名依赖。
- **`compound_safe_single_keywords`类属性**：不可删除。嵌入式扫描三处仍使用，局部缓存是64K+次热路径的性能优化。

### 联动保护表（全部保留）
| 保护表 | 条数 | 结论 | 理由 |
|---|---|---|---|
| CCW | 37 | 保留 | 与词首并入规则互补无重叠，撤条会打红 |
| _EMBED_MAX_MATCH_KEYWORDS | 3 | 保留 | 为/尝试/返回的嵌入最大匹配仍需 |
| OPERATOR_VERBS | 19 | 保留 | 运算符语义核心，DUAL 8字以CS保留作词首锚 |
| _P0A_MERGE_WHOLE | 9 | 保留 | 有界精确整串合并，与词首并入规则无重叠 |

---

## 六、验证结果

### 6.1 编译器方向反跑
- T3 CS表删除：ALL OK（843文件token零变化）
- T4 全量反跑：ALL OK（843文件token零变化）

### 6.2 全量CI
- **pytest**：待确认（CI运行中）
- **smoke**：待确认

### 6.3 数据集方向
- 第25轮数据集（2200条）不受本轮影响（编译器修改为词法层正面规则，全语料token零变化）

---

## 七、路M修正与决策记录

1. **任务1实现站点偏差**：任务书预期改`_match_keyword`（:1227），实测决定性站点在第一层skip_verb（:2553）。路M认可——正面规则落地在决定性站点，`_match_keyword`的CS递归保留为配套保护。

2. **CS表未清零**：任务书预期"大幅缩减或清零"，实际30→16。路M认可——保留的16字中5个是运算符、2个是值字面量，这些字∉正面类别（排除集），其词首并入仍需CS表作为锚。CS表完整清零需将运算符/值字面量的词首并入也通用化（更难，需后续轮次）。

3. **递归未简化**：任务5评估后决定保留`_skip_compound_safe_and_match`现状。路M认可——递归深度仅1-3层，性能收益~0，且"最内层CS单字回报"语义被stdlib函数名依赖，简化风险大于收益。

4. **联动保护表全部保留**：CCW/_EMBED/OPERATOR_VERBS/_P0A_MERGE_WHOLE评估后全部保留。路M认可——这些表与词首并入规则互补无重叠，撤条会打红。

---

## 八、已知问题与后续方向

### 8.1 CS表剩余16字的进一步通用化
- 运算符5字（乘/减/加/除/模）：词首后随汉字须并入（乘法/模组），但作为独立运算符时必须切分。通用化需区分"运算符后随汉字"vs"运算符后随空白/操作数"。
- 值字面量2字（真/空）：词首后随汉字须并入（真空/空格），但作为独立值字面量时必须切分。
- 其他9字：各有独立特殊语义，需逐字分析。

### 8.2 持续存在的问题
- test_审批.light flaky（R20-C）：从第20轮持续存在，`等待结果`用`time.time()`而非`time.monotonic()`，非编译器缺陷。

### 8.3 第27轮方向建议
1. **CS表剩余16字逐字通用化**（推荐）：运算符/值字面量的词首并入通用化，CS表目标清零
2. **CCW 37条分批精简**
3. **test_审批.light flaky修复**
4. **继续上游复刻**

---

## 九、docs三件回填

1. **对标清单**（docs/功能对标/对标清单.json）：追加#144（CS词首并入通用化，CS表30→16），总数144条。
2. **缺陷账**（docs/功能对标/语言缺陷账.md）：本轮无新发现缺陷，未更新。
3. **行为差异清单**（docs/功能对标/行为差异清单.md）：追加R26-D1（CS词首并入正面规则，CS表30→16，订正R25-D1）、R26-D2（词首/词尾并入规则对称结构与不对称性）、R26-D3（递归与联动保护表评估结论）。

---

## 十、git提交

### light-merge项目（1个提交）
- 待提交：src/lexer.py（96+/20-，词首并入正面规则+CS表30→16）

### lightharness项目（6路分路提交 + 收口提交）
- 任务1：词首并入规则设计+_match_keyword评估+第一层skip_verb实现
- 任务2：嵌入式扫描三处词首并入通用化
- 任务3：CS表30→16逐条验证删除（三重判据）
- 任务4：边界测试套件+全量反跑
- 任务5：递归简化+联动保护表评估+性能
- 任务6：质量审查+docs回填
- 收口提交：docs三件 + 质量审查报告 + 收口说明

---

## 十一、路M 合流补正（2026-09-15 11:12–11:41）

### 11.1 收口已完成的提交（本会话核实）
| 仓 | 提交 | 内容 |
|---|---|---|
| light-merge | `80f4125c` | 第26轮 CS 词首并入通用化（`src/lexer.py`，CS 30→16 + 词首正面规则 + 3 条自校验断言）；工作树 ≡ HEAD |
| lightharness | `d8b937b` | 任务3：CS 表 30→16 逐条验证删除 |
| lightharness | `9d10f33` | 任务4：边界测试套件 + 全量反跑 |
| lightharness | `ace5cb9` | 任务5：递归简化评估 + 联动保护表评估 |
| lightharness | `3397974` | 路M收口：docs 两件回填 + 质量审查报告 + 收口说明 |

13 个 R26 交付物 + `_antirun_r26_t3/t4` + `_antirun_r26_基线快照.json` + `_taskM_第26轮收口说明.md` 均已入库。

### 11.2 本次补正（`fadd436`）：修复「已提交态不一致」
**问题**：HEAD 的 `tests/test_R24_的递归_token.py`（`test_compound_safe_table_is_30`）与
`tests/test_R25_单字后缀_token.py::test_protection_table_shape`（`CS == 30`）仍断言旧值，
而同一 HEAD 的编译器已 CS=16 ⇒ **已提交态测试跑不过**。（成因：收口在**工作树**跑绿后，
`git add` 只带 R26 名额文件，漏带这两个 R24/R25 测试。）

**修复**：
1. `tests/test_R24_的递归_token.py`：`test_compound_safe_table_is_30` → `_is_16`；
   `test_retained_single_chars_present` 改判「在 CS **或** 在 `_P0A_HEAD_MERGE_SINGLE`」。
2. `tests/test_R25_单字后缀_token.py`：`test_protection_table_shape` CS=30 → 16；
   18 候选字循环改判「在 CS **或** 在正面类别」；docstring 同步。
3. `tests/test_R21_词法确定性_超集.py`：**tar 可移植性缺陷修复** —— 原
   `subprocess.run(["tar","-xf",<绝对路径>,"-C",tmp])` 在 **Git-Bash 的 GNU tar 1.35** 下把
   `C:/...` 当作远程主机规格（`tar: Cannot connect to C: resolve failed`）→ 2 ERROR；
   改「相对名 `src.tar` + `cwd=tmp`」后 **GNU tar 与 Windows 自带 bsdtar 双通**（各 2 passed）。
   不可用 `--force-local`：FreeBSD 门禁机的 bsdtar 不支持该选项。
4. 清理并行 agent 遗留 scratch 探针 `examples/_probe_r26_kw.light` / `_probe_r26_op.light`（含非法 `设 己 为 5`，污染 `test_回归.py`）。

### 11.3 终态全量 CI（`scripts/ci_test.py`）
```
pytest : 858 passed, 1 skipped          (rc=0)
smoke  : 5/5 通过（会话/代理/工具/消息/流）
汇总   : 全部通过
```
> 附：CI 前一轮的 2 个 ERROR 经定位是 **shell 环境差异**而非缺陷 —— `tar --version`
> 在 Git-Bash 下为 GNU tar 1.35、在 Windows 控制台(System32)下为 bsdtar 3.5.2；
> 任务书给的 CI 命令是 Windows 控制台态，修 11.2-3 后两种 shell 均全绿。

### 11.4 遗留（非 R26，未动）
`lightharness` 仍有 3 个 **R23 报告**为已跟踪-未提交状态
（`_task1_R23_TRAILING_ALIAS清表_交付报告.md`、`_task3_R23_CCW内建名迁移_交付报告.md`、
`_task5_R23_全量回归扫描报告.md`），配套证据 `_r23_subset_ab.py` + `_task5_R23_失败子集AB_证据.json`
（2026-09-14）亦未跟踪 ⇒ 属 **09-14 的「R23 全量失败子集 A/B 证据补录」工作流**，与 R26 无关，
本会话不擅自提交，交用户裁决。
