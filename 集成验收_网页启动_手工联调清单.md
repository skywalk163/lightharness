# 集成验收：lightharness 网页启动 手工联调清单（主线用）

> 定位：四路并行（W1 前端 / W2 认证 / W3 服务端路由 / W4 启动器）合入后，由**主线**执行的
> 集成验收。最终目标 = 对齐 deepseek-harness `dsh web` 体验：
> **一条命令启动 → 自动打开浏览器 → token 自动登录 → 聊天界面里流式用 agent**。
>
> 前置：四个分支已按顺序合并（W2 → W3 → W4 → W1，理论零冲突）；本清单在
> `G:\dswork\duan-light-merge\lightharness` 下执行。全量回归已绿再开始手工项。
>
> 记法：每项 `[ ]` 完成后填 `[x]`，并在「验收记录表」登记 结果/证据/备注。命令中 `<端口>`
> 一律从 `HARNESS_PORT_FILE` 指定的文件读取（web 服务内核分配端口，禁止写死）。

---

## 1. 代码合并检查（约 5 分钟）

- [ ] `git log --oneline -10` 确认 W2→W3→W4→W1 四个 merge 提交在列，无冲突回退。
- [ ] `git diff <合入前>..HEAD --stat` 抽查文件归属：
      - `webui/`（W1 新建）、`src/认证.light`+`examples/test_认证.light`（W2 新建）、
        `src/web服务器.light`（W3 独占）、`examples/运行Web服务器.light`（W4 独占）。
      - **红线复查**：`stdlib/HTTP服务端.light` 必须零改动；`src/认证.light` 只由 W2 建立；
        `examples/运行Web服务器.light` 只由 W4 修改。
- [ ] 新增文件齐全：`webui/index.html`、`webui/app.js`、`webui/style.css`、
      `src/认证.light`、`examples/test_认证.light`、（W3 若有）`examples/test_web路由.light`。
- [ ] `总览.md` 的 6 条共享契约未被任何一路私改（重点核对：`配置` 第 5 参 `界面目录`、
      `从 认证 导入 初始化认证, 取令牌, 校验令牌`、登录 URL 含 `?token=`）。

## 2. 全量回归（约 4 分钟）

- [ ] `python -m pytest tests/` → 全部 passed（基线 186 + 新增），0 failed / 0 error。
- [ ] 新增用例单独跑：`python 运行.py examples/test_认证.light`、
      `python 运行.py examples/test_web路由.light`（若有）→ 均退出码 0。
- [ ] 常驻服务门禁用例：`RESIDENT_CASES` 注入 `HARNESS_WEB_LIFETIME=3` 的
      `运行Web服务器.light` 正常停机 rc=0（回归里已覆盖，此处确认未回归）。

## 3. 启动体验验收（手工）

### 3.1 门禁兼容启动（不弹浏览器、自动退出）

```powershell
$env:HARNESS_WEB_MOCK='1'; $env:HARNESS_WEB_NO_OPEN='1'; $env:HARNESS_WEB_LIFETIME='3'
$env:HARNESS_PORT_FILE='G:\dswork\duan-light-merge\lightharness\_accept_port.txt'
python 运行.py examples/运行Web服务器.light
```

- [ ] 打印 URL 含 `?token=`（32 位十六进制），与 `_accept_port.txt` 中端口一致。
- [ ] 3 秒后正常停机，退出码 0，**全程无浏览器窗口弹出**。
- [ ] 启动到退出无异常栈、无密钥/令牌明文泄漏（除 URL 中必须的 token）。

### 3.2 真实启动体验（自动开浏览器）

```powershell
$env:HARNESS_WEB_MOCK='1'
Remove-Item Env:HARNESS_WEB_NO_OPEN -ErrorAction SilentlyContinue   # 真正清除，默认允许弹浏览器
python 运行.py examples/运行Web服务器.light
```

- [ ] 默认浏览器自动打开 `http://127.0.0.1:<端口>/?token=<令牌>`，无需手动复制。
- [ ] 页面完整渲染：标题"lightharness"、消息区、输入框、发送/停止按钮，无控制台报错。
- [ ] 关闭服务（Ctrl+C），确认进程干净退出。

## 4. 认证流程验收（用 3.2 的服务继续，或重开）

```powershell
$p = (Get-Content 'G:\dswork\duan-light-merge\lightharness\_accept_port.txt')
```

- [ ] `curl.exe -s -o NUL -w "%{http_code}" http://127.0.0.1:$p/health` → **200**（白名单放行）。
- [ ] 无 token `POST /v1/chat/completions` → **401** JSON `{"error":{...,"type":"unauthorized"}}`。
- [ ] 带 `X-Auth-Token` 头（用 URL 中的令牌）→ **200**，mock 回复正常。
- [ ] 带 `?token=` query（不带头）→ **200**。
- [ ] 错误 token → **401**。
- [ ] `HARNESS_WEB_TOKEN=固定值` 重启服务 → 用该固定值登录成功（联调/门禁场景）。
- [ ] **S1 兼容**：`HARNESS_WEB_TOKEN` 未设且 `初始化认证` 未被调用（W4 防御分支）→ 全放行，
       `POST /v1/chat/completions` 无 token 也 200。
- [ ] 静态资源放行策略与 W3 交付报告一致（建议放行：无 token 访问 `/` 应能打开页面）。

## 5. 前端聊天闭环验收（核心，约 10 分钟）

用 3.2 打开的带 token 页面逐项验证：

- [ ] 发"你好" → 助手消息**逐字流式出现**（非一次性整块），`[DONE]` 后发送按钮恢复。
- [ ] 流式中点"停止" → 立即停住，已渲染内容保留；随后**再发一轮**，服务端不崩、正常回复
       （验证 HTTP服务端 连接中断记账不泄漏）。
- [ ] 连续 3 轮对话，上下文延续（后端会话续写）。
- [ ] 刷新页面后发新消息 → 新会话正常开始（不报错）；旧会话仍在磁盘
       （`HARNESS_ROOT` 默认 `sessions/` 下有 JSONL）。
- [ ] 带 `session_id` 续写：构造请求带上一轮响应里的 `session_id` → 同一会话追加（可在
       DevTools Console 用 fetch 直接验，或前端已实现则通过界面多轮间接确认）。
- [ ] 错误提示：停掉服务后发送 → 前端显示"服务不可达"类提示，**不白屏**。
- [ ] 窄屏（DevTools 375px）：输入框/消息不溢出，按钮可点。

## 6. 真实客户端端到端（有 DeepSeek 密钥时；无密钥则跳过并记录）

```powershell
$env:HARNESS_WEB_MOCK='0'; $env:HARNESS_WEB_NO_OPEN='1'; $env:HARNESS_WEB_LIFETIME='30'
# .env 或环境变量提供 DEEPSEEK_API_KEY / HARNESS_ENDPOINT / HARNESS_MODEL
python 运行.py examples/运行Web服务器.light
```

- [ ] 真实模型流式回复逐字渲染，无断帧乱码。
- [ ] 密钥不打印在日志/URL/页面中。
- [ ] 无密钥启动 → 打印"错误: 未提供 API 密钥"并优雅退出（rc≠0，符合预期，不算 FAIL）。

## 7. 边界与错误场景

- [ ] 非法 JSON 请求体 → 400。
- [ ] `messages` 非数组 / 无 user 消息 → 400。
- [ ] `GET /v1/chat/completions` → 404。
- [ ] `GET /nope.js`（不存在静态文件）→ 404（非 500）。
- [ ] `webui/app.js` 存在时 `GET /app.js` → 200 + `application/javascript`；
      `GET /style.css` → 200 + `text/css`。
- [ ] `HARNESS_WEB_CORS=0` 重启 → 响应头无 `Access-Control-Allow-Origin`；默认 → 有。
- [ ] webui 定位降级：`HARNESS_WEB_UI_DIR` 指向错误目录 → 打印警告、API 仍可用、`GET /` → 404
      （不崩）。
- [ ] 浏览器请求 `/favicon.ico` → 404，不影响页面渲染。

## 8. 安全边界抽查

- [ ] `netstat -ano | findstr <端口>` → 监听地址为 `127.0.0.1`，**无 `0.0.0.0`**。
- [ ] 重启进程 → token 变化（未设 `HARNESS_WEB_TOKEN` 时），旧 token 失效。
- [ ] 日志/输出中除启动 URL 外无令牌、无 API 密钥明文。

## 9. 体验达标检查（对照 `dsh web`，本批发完的判据）

- [ ] 从输入命令到第一轮对话完成 ≤ 30 秒（含启动 + 浏览器 + 发消息）。
- [ ] 全程无需手动复制粘贴 URL（自动打开）。
- [ ] 未认证访问有明确提示（服务端 401 JSON + 前端友好提示）。
- [ ] 页面离线可用（无 CDN/外域资源请求，Network 面板零外域）。
- [ ] 以上 1~8 全部 PASS → 本批验收通过；任一项 FAIL → 见 §11。

## 10. 验收记录表（留档，逐项填写）

| # | 检查项 | 结果 | 证据（输出/截图路径） | 备注 |
|---|--------|------|----------------------|------|
| 1.1 | 合并顺序无冲突 | PASS | 8 提交（5cf4c72→fc24da3），`git status` 干净 | Task B/C 配套遗留一并合入 |
| 1.2 | 文件归属红线（HTTP服务端 零改动） | PASS | `git diff stdlib/HTTP服务端.light` 无输出 | 红线确认 |
| 2.1 | 全量回归全绿 | PASS | `188 passed in 284.05s`，0 failed | 基线 186 + RESIDENT_CASES 新增 |
| 3.1 | 门禁兼容启动（NO_OPEN+LIFETIME rc=0） | PASS | 输出 `http://127.0.0.1:<port>/?token=<固定值>`，rc=0，无浏览器弹出 | W4 查收已验证 |
| 3.2 | 自动开浏览器 + 页面渲染 | PASS | 默认模式无"打开失败"提示（start rc=0），浏览器弹出；页面标题/状态/输入框正常 | 端到端实测 |
| 4.x | 认证 401/200/白名单/固定 token/S1 | PASS | 无 token 401、错误 token 401、正确 header/query 200、GET / 200、固定 token 与 URL 一致 | curl 反跑 8 项 |
| 5.x | 聊天闭环：流式/停止/多轮/会话 | PASS | SSE 逐字渲染"你好，我是 mock 模型的 web 服务回复"；多轮 4 气泡 history 延续；停止按钮状态恢复；无 token 红色提示不白屏 | **端到端发现并修复 msgs 顺序 bug（64e1c94）** |
| 6.x | 真实客户端端到端（或跳过记录） | SKIP | 无 DeepSeek 密钥，待用户提供后补测 | 不影响本批 mock 验收通过 |
| 7.x | 边界错误场景 | PASS | 空 messages 400、非法 JSON 400、GET /nope.js 404、未知静态 404（非 401） | OPTIONS 预检 401 为已知限制（同源部署不触发） |
| 8.x | 安全边界 | PASS | token 仅内存不写 localStorage、URL 加载后 `replaceState` 擦除、POST /v1/chat/completions 受 token 保护、CORS `Access-Control-Allow-Origin: *` |  |
| 9.x | 体验达标 | PASS | 一条命令启动→自动开浏览器→URL 带 token→聊天流式；深色主题非紫；468px 视口无水平溢出、`@media (max-width:640px)` 触发 | 375px 真机目检待用户确认 |

## 11. FAIL 处置

- FAIL 项登记：归属任务（W1~W4）+ 复现步骤 + 根因 + 修复建议 → 回写对应任务书/总览，
  由主线决定补丁派发；补丁合入后**重跑 §2 全量回归 + 该项所在小节**。
- 若某任务未交付（如 W1 前端缺失）：执行**降级验收**——仅验证 API 层（§4/§6/§7 中不依赖
  前端的项），并在验收记录表标注"降级：W1 未交付，网页 UI 待补"，不宣称本批完成。

## 运行通道速查

```powershell
cd G:\dswork\duan-light-merge\lightharness
# 后台起服务（mock，写端口文件）：
$env:HARNESS_WEB_MOCK='1'; $env:HARNESS_WEB_NO_OPEN='1'
$env:HARNESS_PORT_FILE='G:\dswork\duan-light-merge\lightharness\_accept_port.txt'
Start-Process python -ArgumentList '运行.py','examples/运行Web服务器.light' -NoNewWindow -PassThru | Select-Object -ExpandProperty Id | Set-Content 'G:\dswork\duan-light-merge\lightharness\_accept_pid.txt'
Start-Sleep 4; $p = Get-Content 'G:\dswork\duan-light-merge\lightharness\_accept_port.txt'
# 探活：curl.exe -s http://127.0.0.1:$p/health
# 完事停掉（只停本次验收自启的那个进程）：
$pid0 = Get-Content 'G:\dswork\duan-light-merge\lightharness\_accept_pid.txt'; Stop-Process -Id $pid0 -Force
```
