# 第26轮 任务6 质量审查报告

> 日期：2026-09-15 ｜ 审查对象：CS词首并入通用化（任务1-5交付）

---

## 一、编译器方向质量审查

### 1.1 任务1：词首并入正面规则设计+实现

| 检查项 | 结果 | 说明 |
|---|---|---|
| 规则定义 | ✅ | `_P0A_HEAD_MERGE_SINGLE = F - _P0A_HEAD_SPLIT_SINGLE`（22字） |
| 排除集 | ✅ | `_P0A_HEAD_SPLIT_SINGLE = _P0A_SUFFIX_SPLIT_SINGLE`（R25词尾排除集，五类语义排除） |
| 实现站点 | ⚠️ 偏差 | 任务书预期改`_match_keyword`（:1227），实测决定性站点在第一层skip_verb（:2553）；`_match_keyword`不套用正面规则（会打掉R24 L-153自之姓名修复），只随CS精简收缩 |
| 快速验证 | ✅ | `test_R26_词首并入_快速.light` rc=0，覆盖DUAL 8词首并入 |
| 全语料token零变化 | ✅ | 任务1+2合并后0/843文件 |

**偏差说明**：任务1勘察发现真实站点与任务书假设有三点偏差，已记录在交付报告§一。`_match_keyword`（:1227）的CS递归路径保留，因为其"最内层CS单字回报"语义被`去除空格`等stdlib函数名依赖。路M认可这一偏差——正面规则落地在决定性站点（第一层skip_verb），`_match_keyword`的递归是配套保护。

### 1.2 任务2：嵌入式扫描词首并入通用化

| 检查项 | 结果 | 说明 |
|---|---|---|
| 三处修改 | ✅ | B/C/D三处逐字同判据（scan_pos==0 + 后随汉字 + _P0A_HEAD_MERGE_SINGLE） |
| 第一层联动 | ✅ | 决定性站点在第一层（:2553），任务书2.2的审计区实际是核心修改区 |
| 全语料token零变化 | ✅ | 0/843文件 |
| 历史坑规避 | ✅ | 探测循环与输出循环一致性（v7单02历史坑），B/C/D三处同判据 |

### 1.3 任务3：CS表逐条验证删除

| 检查项 | 结果 | 说明 |
|---|---|---|
| CS表变化 | ✅ | 30→16（净删14字） |
| 可删14字 | ✅ | 余/例/出/则/常/引/接/断/末/试/跳/过/长/首 |
| 保留16字 | ✅ | 乘/减/列/到/加/对/是/模/段/的/真/空/类/自/配/除 |
| 三重判据 | ✅ | G1语料+G2词首编译门+G3边界门，逐条验证 |
| R26增量可删 | ✅ | 断/过在R25基线G1打红（10/1文件），正面规则落地后G1归零→可删 |
| 反跑ALL OK | ✅ | 843文件token零变化 |

**保留16字分类**：
- 运算符（5字）：乘/减/加/除/模——∉正面类别，词首后随汉字须并入（乘法/模组）
- 值字面量（2字）：真/空——∉正面类别（真空/空格）
- 其他特殊语义（9字）：列/到/对/是/段/的/类/自/配——各有独立保留理由（详见任务3交付报告）

### 1.4 任务4：边界测试+全量反跑

| 检查项 | 结果 | 说明 |
|---|---|---|
| 正向.light | ✅ | `test_R26_词首并入正向.light` rc=0 |
| 反向.light | ✅ | `test_R26_词首并入反向.light` rc=0 |
| 混合.light | ✅ | `test_R26_词首并入混合.light` rc=0 |
| pytest | ✅ | 167 passed, 1 skipped（语料缺失时skip零变化断言） |
| 全量反跑 | ✅ | ALL OK，843文件token零变化 |
| 保护表形态断言 | ✅ | CS表=16、_P0A_HEAD_MERGE_SINGLE定义断言 |

### 1.5 任务5：递归简化+联动评估+性能

| 检查项 | 结果 | 说明 |
|---|---|---|
| _skip_compound_safe_and_match | ✅ 保留 | 递归深度1-3层，性能收益~0；"最内层CS单字回报"语义被去除空格等依赖 |
| compound_safe_single_keywords类属性 | ✅ 保留 | 嵌入式扫描三处仍使用，局部缓存是热路径性能优化 |
| CCW 37条 | ✅ 保留 | 与词首并入规则互补无重叠，撤条会打红 |
| _EMBED 3条 | ✅ 保留 | 为/尝试/返回的嵌入最大匹配仍需 |
| OPERATOR_VERBS 19条 | ✅ 保留 | 运算符语义核心，DUAL 8字以CS保留作词首锚 |
| _P0A_MERGE_WHOLE 9条 | ✅ 保留 | 有界精确整串合并，与词首并入规则无重叠 |
| 死代码清理 | ✅ | 无新增死代码，文末自校验断言通过 |

---

## 二、数据集方向确认

- 第25轮数据集（2200条）不受本轮编译器修改影响
- 抽样验证数据集中的代码段仍可编译（编译器修改为词法层正面规则，全语料token零变化）

---

## 三、路M修正与决策记录

1. **任务1实现站点偏差**：任务书预期改`_match_keyword`（:1227），实测决定性站点在第一层skip_verb（:2553）。路M认可——正面规则落地在决定性站点，`_match_keyword`的CS递归保留为配套保护（"最内层CS单字回报"语义被依赖）。

2. **CS表未清零**：任务书预期"大幅缩减或清零"，实际30→16。路M认可——保留的16字中5个是运算符、2个是值字面量，这些字∉正面类别（排除集），其词首并入仍需CS表作为锚。剩余9个有独立特殊语义。CS表的完整清零需将运算符/值字面量的词首并入也通用化（更难，需后续轮次）。

3. **递归未简化**：任务5评估后决定保留`_skip_compound_safe_and_match`现状。路M认可——递归深度仅1-3层（CS 16字），性能收益~0，且"最内层CS单字回报"语义被stdlib函数名依赖，简化风险大于收益。

4. **联动保护表全部保留**：CCW/_EMBED/OPERATOR_VERBS/_P0A_MERGE_WHOLE评估后全部保留。路M认可——这些表与词首并入规则互补无重叠，撤条会打红。

---

## 四、已知问题与后续方向

### 4.1 CS表剩余16字的进一步通用化
- 运算符5字（乘/减/加/除/模）：词首后随汉字须并入（乘法/模组），但作为独立运算符时必须切分。通用化需区分"运算符后随汉字"vs"运算符后随空白/操作数"。
- 值字面量2字（真/空）：词首后随汉字须并入（真空/空格），但作为独立值字面量时必须切分。
- 其他9字：各有独立特殊语义，需逐字分析。

### 4.2 test_审批.light flaky持续存在
- 从第20轮到第26轮持续存在（`等待结果`用`time.time()`而非`time.monotonic()`）
- 非编译器缺陷，建议后续轮次专门修复

### 4.3 第27轮方向建议
1. **CS表剩余16字逐字通用化**（推荐）：运算符/值字面量的词首并入通用化，CS表目标清零
2. **CCW 37条分批精简**
3. **test_审批.light flaky修复**
4. **继续上游复刻**

---

## 五、结论

第26轮CS词首并入通用化质量良好：
- 正面规则`_P0A_HEAD_MERGE_SINGLE`（22字）落地，决定性站点在第一层skip_verb
- CS表30→16（删14字），保留16字有充分理由（运算符5+值字面量2+特殊语义9）
- 全语料843文件token零变化，无回归
- 边界测试覆盖全面（3个.light + pytest 167 passed）
- 递归/联动保护表评估严谨，全部保留有依据
- 任务1实现站点偏差已记录并认可

可进入路M收口。
