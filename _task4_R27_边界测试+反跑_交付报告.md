# 第27轮 任务4 交付报告：CS表16字逐字通用化——边界测试套件 + 全量反跑

日期：2026-09-15 ｜ 状态：✅ 全部完成
前提：任务1（`_P0A_HEAD_MERGE_DUAL` A类词首/词尾正面规则）与任务2（L-119 嵌入
扫描等价修复）已落地（注：lexer.py 由多路并行修改，本次合并验证以磁盘当前态为准）；
任务3 已执行 CS 表 16→2（移除 14 字，残部 列/类）。

---

## 一、交付物清单

| 交付物 | 路径 | 状态 |
|---|---|---|
| 正向边界测试（.light） | `examples/test_R27_词首并入正向.light` | ✅ rc=0（16字×2名=32标识符） |
| 反向边界测试（.light） | `examples/test_R27_词首并入反向.light` | ✅ rc=0（运算符5/值字面量3/范围/硬语句/下标） |
| 混合场景测试（.light） | `examples/test_R27_词首并入混合.light` | ✅ rc=0（9 组） |
| pytest token 层测试 | `tests/test_R27_CS表16字词首并入_token.py` | ✅ **113 passed** |
| 全量反跑脚本 | `_antirun_r27_t4_全量反跑.py` | ✅ |
| 反跑结果 JSON | `_task4_R27_反跑结果.json` | ✅ |

## 二、测试覆盖说明

### 2.1 正向（16字全量，名字级参数化 32 用例）
- A类 8 字 × 2 名：乘法/乘积、减法/减速、到位/到达、加法/加数、模组/模型名、
  真空/真实值、空格/空白页、除法/除非名 → 整词成 IDENTIFIER；
- B类 6 字 × 2 名 + CS 残部 2 字 × 2 名（列数/列表项、类别名/分类号）；
- A类词尾并入钉：`文件未找到`、`标准输出失真` 整词。

### 2.2 反向（切分语义，双形态对照）
- 运算符 5 字（加减乘除模）后随空白切分 + 「并入 vs 切分」双形态对照（`乘法法`型）；
- 值字面量 真/假/空 独立切分；范围遍历 `遍历 i 于 1 至 4`（hello.light 规范写法）；
- 「到」词中形态 `捕获到异常`（词中整词不切出）；
- 硬语句 10 字（如果/那么/设/为/返回/段落/尝试/捕获/类/打印）全部切分；
- 下标访问 L-119：段[0]/配[0]/对[0] 单字名不切关键字。

### 2.3 混合（.light 运行期 + pytest 双层）
- `乘法 为 7; 如果 乘法 大于 5`、`真空/真`、`配置[0]+段落名[1]`、
  `加法 为 甲 加 乙`、`模组 模 3`、`到位系列+范围遍历`、`己/之 语义不受并入影响` 等；
- R26 回归钉 5 组：`己之姓名`、`对于` 词首整词、`真的` 词尾整词、`10的幂`、
  `配[0]` 原钉、`是非` 列表元素切开（既有行为显式钉住防漂移）。

### 2.4 保护表形态断言（铁律）
- `CS == ['列','类']`（R27 残部 2 字）；
- `_P0A_HEAD_MERGE_DUAL == 8 字`、与 CS 不相交、⊆ 运算符∪值字面量∪范围来源；
- `HM − CS == _R26_CS_REMOVED ∪ _R27_CS_REMOVED`（净增量恒等）；
- `HM ⊆ F`、HM 含 B类 6 字不含 A类 8 字、CS ⊆ F；
- 文末 import 自校验重载幂等。

## 三、全量反跑结果

### 3.1 [A] 边界测试用例
```
test_R27_词首并入正向.light   rc=0
test_R27_词首并入反向.light   rc=0
test_R27_词首并入混合.light   rc=0
```

### 3.2 [B] pytest
```
test_R27_CS表16字词首并入_token.py   113 passed, 0 failed
R26+R27 合跑                         281 passed, 1 skipped, 0 failed
```
（1 skipped 为 R26 语料指纹用例在 pytest 独立跑时的守卫降级；
全量反跑脚本内同口径比对以 839 文件全量执行并通过。）

### 3.3 [C] 全语料 token 零变化（硬门槛）
```
语料 = 844 个 .light 文件（新增 R27 三个测试文件后）
成功 tokenize = 842 | 既有失败 = 2（集合.light / _test_nested_closure.light，
                  R26 前即失败，排除比对）
对照 R26 基线快照：839 文件可比，逐文件 token 序列零变化 ✅
```
即：**DUAL 正面规则 + L-119 等价 + CS 16→2 的全部 R27 修改，在 843 文件语料上
token 行为与 R26 基线完全一致**（842 成功 + 2 既有失败均不因 R27 改变）。

### 3.4 [D] 性能（口径：纯 tokenize × 10 次取平均）
```
R27 实测：平均 10.51s ~ 11.78s，中位 9.77s ~ 12.59s，静置极值 6.19s
R25 基线：9.63s
```
与第26轮同期观测一致（负载波动带 8.6~12.0s），静置极值 6.19s 与 R26 静置
极值 6.0s 同档。结论：CS 16→2 + DUAL 正面规则**未引入性能退化**；
精确静置复测建议归任务6。

## 四、铁律自检

| 铁律 | 自检 |
|---|---|
| 正向覆盖 CS 表全部 16 字 | ✅ 14 已删字 + 残部 列/类 共 16 字 × 2 名 |
| 反向覆盖运算符/值字面量/范围/下标访问 | ✅ 见 2.2 |
| 「后随汉字并入」vs「后随空白切分」都测 | ✅ 双形态对照参数化（5 运算符 × 2） |
| pytest 含保护表形态断言（CS 最终条数） | ✅ CS=2 / DUAL=8 / HM=22 / 净增量恒等 |
| 全量反跑零变化硬门槛 | ✅ 839 文件可比零变化 |
| 新增一律 .light，禁 Python 绕语言 | ✅ 3 个新测试均 .light；pytest 仅 token 层断言 |

## 五、结论

任务4 全部交付物落地且通过：边界测试（正向16字/反向切分/混合9组）rc 全 0、
pytest 113 passed（R26+R27 合跑 281 passed）、全语料 839 文件对照 R26 基线
token 零变化、性能无退化。CS 表经 R27 任务3 精简至残部 2 字（列/类，真护栏，
删除清单见 `_task3_R27_CS表删除清单.md`），词首/词尾并入已由
`_P0A_HEAD_MERGE_DUAL` + `_P0A_HEAD_MERGE_SINGLE` + L-119 等价规则全面正面化。
