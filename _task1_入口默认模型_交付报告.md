# 任务1 交付报告 —— 入口默认模型生效 + 计量价格

> 轮次：0.1.5 遗留 · 第 5 轮 ｜ 任务：1（入口三文件 + 计量价格）｜ 日期：2026-09-11
> 对应遗留：第 4 轮路 5 移交项（默认模型目录已建未接线）
> 上游依据：0.1.5 提交 `bc5fd3b8dc`（default Chat Completions to DeepSeek V41 Flash）

## 1. 改动清单（只动任务1 文件）

| 文件 | 性质 | 内容 |
|----|----|----|
| `src/总入口.light` | 改动 | ① 新增注入式 `段落 解析模型(环境取值, 文件甲取值, 文件乙取值)`（解析链抽段，供测试注入、避免真实读环境）；② `主()` 模型末级 fallback 由硬编码 `"deepseek-chat"` 改为 `取默认模型()`；③ 导入补 `取默认模型`；④ 头注释同步 |
| `src/web服务器.light` | 改动 | ① 构造处计量器默认模型 → `取默认模型()`；② `/v1/config` 载荷 `model` 缺省 → `取默认模型()`；③ 导入补 `取默认模型` |
| `src/令牌计量.light` | 改动 | 单价表增加 `"deepseek-flash": [0.27, 1.10]`（与 deepseek-chat 同代同价，注释注明「如官方定价有更新可调整」）；未知模型回落缺省价逻辑不变；计量器构造注释同步 |
| `examples/test_入口默认模型.light` | 新增 | 3 节用例（默认模型与目录 / 注入式解析链 / 计量命中新单价） |
| `_antirun_default_model.py` | 新增 | 反跑判据脚本（2 项，字节级备份/恢复源码，不改 git 状态） |

未触碰其它任务的文件（工作树中 `代理.light`/`消息.light`/`附件准入.light` 的改动属任务 2，与本任务无关）。

## 2. 上游对应表

| 上游（0.1.5） | 提交 | lightharness 落点 | 说明 |
|----|----|----|----|
| `llm-deepseek` DEFAULT_MODELS Chat Completions 默认 → V41 Flash | `bc5fd3b8dc` | `总入口.解析模型` 末级 fallback / `web服务器` 计量器与配置缺省 | 全部经 `客户端.取默认模型()`（第 4 轮路 5 已交付，返回 `"deepseek-flash"`），三处接线，单点取值 |
| 环境变量/.env 配置链 | 既有语义 | `解析模型` 优先级链 | 环境变量 `HARNESS_MODEL` → `.env HARNESS_MODEL` → `.env OPENAI_MODEL` → 内置默认；**用户显式配置永远优先于内置默认**，与上游行为一致 |
| 模型定价（V41 Flash 与 chat 同代同价） | 上游目录定价口径 | `令牌计量.单价表["deepseek-flash"] = [0.27, 1.10]` | 未知模型回落 `[1.00, 2.00]` 的既有契约不变 |

**模型路由核对（任务要点 4）**：`客户端.light` 对模型 id 无分支路由——模型名原样进请求体 `["model": 己.模型名]`（客户端.light:242），端点统一走 chat/completions；`deepseek-flash` 为默认目录第一项（`按ID取模型` 可命中），无需额外映射。V41 Flash 的 `systemPromptUpdate: "in-history"` 语义（surface 节点 0）已在第 4 轮路 1/3/5 交付，本任务不涉及。

## 3. 实现要点

1. **解析链抽段**：原 `主()` 内联的 4 级 fallback 抽为模块级 `解析模型`（纯函数，三个取值入参注入），`主()` 用真实 `环境变量()`/`读取环境文件值()` 调用之——行为逐分支等价，测试可注入不读环境。
2. **单点默认**：三处硬编码 `"deepseek-chat"`（总入口:225、web服务器:65、web服务器:377）全部收敛到 `取默认模型()`，后续模型换代只改 `客户端.light` 目录。
3. **计量价格**：`deepseek-flash` 与 `deepseek-chat` 同价 0.27/1.10；测试用「缺省价对照」证明命中新价而非回落（新价花费 < 缺省价花费）。

## 4. 测试与 CI

| 用例/检查 | 结果 |
|----|----|
| `examples/test_入口默认模型.light`（新增，`python 运行.py`） | rc=0，3 节全过 |
| `pytest tests/ -k "5165"`（门禁定向，含新用例） | 4 passed, 207 deselected |
| 既有回归：`test_令牌计量` / `test_令牌估算` / `test_令牌估算1.5` / `test_模型目录` / `test_web路由` / `test_web服务器E5` / `test_总入口` | 全部 rc=0 |

**反跑判据（`_antirun_default_model.py`，实测 2/2 成立）**：
- A：把 `总入口.light` 解析链兜底 `返回 取默认模型()` 改回 `返回 "deepseek-chat"` → 用例 rc=1（红）；恢复后 rc=0。
- B：把 `令牌计量.light` 单价表去掉 `"deepseek-flash": [0.27, 1.10], ` → 用例 rc=1（红）；恢复后 rc=0。

全量 CI（207+新增 passed / 2 failed 既有）由路 M 统一跑。

## 5. 未移植项与移交清单

- **未移植**：无。本任务全部为纯接线与纯数据（单价表），不涉宿主层。
- **移交 → 路M**：`对标清单.json` 中入口/计量相关卡片（含上轮路 5 移交项）可回填「任务1 已接线」；`web服务器.light` 与 `总入口.light` 的默认模型语义现已单点化，后续模型目录变更只需改 `客户端.light`。
- **移交 → 运维备注**：若用户 `.env`/环境变量里存有旧显式 `OPENAI_MODEL=deepseek-chat`，显式配置仍优先生效（符合「显式优先」契约）；如需切到 V41 Flash 需删除该显式项。
- **语言缺陷**：本轮未发现新缺陷，无 L-0xx 登记。
