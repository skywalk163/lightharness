# 第45轮 任务3：全语料影子变量清单审定

> 用 L-166 新告警回扫全语料，逐条判定「刻意局部遮蔽（合法）」还是
> 「潜在 L-165 类缺陷」。**如实记录，不预设结论。**

## 1. 扫描口径

- 范围：`lightharness/examples/*.light` + `lightharness/src/*.light`
        ∪ `light-merge/examples/**/*.light`（含一层子目录）
- 文件数：**702**（解析失败 13，为既有无法解析文件，与本检查无关）
- 命中：**12 个文件 / 40 条告警**（1.7% 的文件）

## 2. 逐条清单

### A. 判定为「刻意局部遮蔽」——合法，无需处理（22 条）

| 文件:行 | 段落 | 变量 | 判定依据 |
|---|---|---|---|
| `test_R28_列词尾切出.light:29` | 测试_遍历序列 | 序列 | 段落自带测试数据 `[1,2,3]`，与模块级同值纯属巧合 |
| `test_文件系统工具.light:42/44/57` | 造宿主 | 文件表 / 已观察快照 / 头字节表 | 每个宿主一份**独立**字典；模块级 L62-64 只是从宿主取出的引用，作为夹具初值 |
| `test_字符串替换编辑器.light:48/66` | 造宿主 / 造限宿主 | 文件表 / 宿主 | 同上，每宿主独立 |
| `test_预设.light:286` | 反跑_可删判定 | 系统预设 | 反跑用例刻意用局部副本覆盖，避免污染模块级夹具 |
| `会话格式.light:41/45/321/560` | 非负整数 / 判版本 / 解码V3事件 / 记录助手轮 | 占 | 纯局部临时变量。**但模块级 L86-108 连续 `设 占 为 定词汇(...)` 把返回值丢进同一个名字**——`定词汇` 有副作用（写 `全局 事件词汇表`），返回值本就无用，属**命名污染**，建议改成不接返回值 |

### B. 判定为「潜在缺陷 / 需要立项」——**18 条**

#### B-1 `examples/test_启动配置.light:101/106/132`（3 条）→ 登记 **L-168**

注释（L48 / L78）明确写着：

```
# ---- 包装段落（无参；通过模块级变量 临时profile目录 / 临时缺失目录 传路径）----
# 模块级测试夹具（主() 内赋值；L-006 自动 nonlocal 允许跨段读写）
```

**实测：这条注释是错的。** 最小复现（`_r45_L006探针.light`）：

```
段内直接读 = [abc]     ← 主() 内的局部赋值
跨段读     = []        ← 另一个顶层段落读到的仍是模块级初值 ""
```

生成的 Python 里**没有任何** `nonlocal` / `global` 行——L-006「自动 nonlocal」
在该模式下**不成立**。

补充事实：`抛数组manifest`（L61）/ `抛缺失manifest`（L63）这两个消费方
**在全仓从未被调用**（`grep -rn` 只有定义、无调用）→ 是**死代码**。

**结论**：影子变量目前**没有造成实际的假绿**（因为消费方没被执行），但：
1. 注释与实现不符，会误导后来者；
2. 一旦有人启用这两个包装段落，就会立刻踩 L-165 的坑。

**处置**：登记 L-168，**不擅自改**（改了要么删死代码、要么加 `全局`，
都属行为变更，超出「加固」范围）。建议下轮立项：`主()` 内补三条 `全局` 声明
并启用/删除这两个死段落。

#### B-2 `light-merge/examples/games/snake.light`（5 条）
#### B-3 `light-merge/examples/games/主.light`（9 条）
#### B-4 `light-merge/examples/games/number_game.light`（3 条）
#### B-5 `light-merge/examples/{main,module_demo}.light`（2 条）
→ 合并登记 **L-169**

以 `snake.light` 为例（实证）：

```
L17-21  设 蛇身 为 [] / 设 方向 为 方向右 / 设 分数 为 0 / 设 游戏结束 为 假   ← 模块级
L25-34  段落 初始化游戏：蛇身 为 []。… 方向 为 方向右。分数 为 0。游戏结束 为 假。 ← 全部局部
L71/85  段落 移动蛇：游戏结束 / 分数                                        ← 全部局部
L151/162 段落 处理输入：方向 / 游戏结束                                     ← 全部局部
```

按作用域语义，`初始化游戏()` **不会重置任何模块级状态**；`移动蛇()` 里加的分
数、判的死亡标志也**不会写回** → 游戏状态在段落间完全不共享。

**结论**：light-merge 自带示例里存在 **L-165 同类的成片缺陷**（5 个文件 19 条）。
这些是**语言本体仓库的示例**，不是 lightharness 的代码。

**处置**：**登记立项，本轮不改**——① 跨仓（light-merge 独立 git）；② 改了会
改变示例的实际行为，属行为变更；③ 需要先确认这些示例是否被 light-merge 自己的
测试/文档引用。修复方案已明确：在各段落内补 `全局 声明`（或改造成显式传参/返回）。

## 3. 汇总

| 判定 | 文件数 | 条数 | 处置 |
|---|---|---|---|
| A 刻意局部遮蔽（合法） | 5 | 21 | 记录即可 |
| B-1 潜在缺陷（lightharness） | 1 | 3 | 登记 **L-168**，立项 |
| B-2~B-5 潜在缺陷（light-merge 示例） | 5 | 19 | 登记 **L-169**，立项 |

## 4. 对告警本身的评价

- **误报率**：本检查的「误报」= A 类（合法遮蔽），共 21/40 = 52%。
  这是**建议性告警**的固有代价——刻意遮蔽与漏写 `全局` 在语法上完全同形。
- **降噪措施已做**：形参不报、只读不报、嵌套函数不进、可用环境变量关闭。
- **价值**：40 条里**抓出 22 条潜在缺陷**（其中 19 条在 light-merge 自带示例里），
  且全部是此前**零报错零警告**的静默区。投入产出成立。
