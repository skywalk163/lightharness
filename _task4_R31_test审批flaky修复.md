# _task4_R31_test审批flaky修复 —— time.time() → time.monotonic()

> 第31轮 任务4（P0）｜ 只改 `examples/test_审批.light`，不碰其他文件

## 一、问题定位

`examples/test_审批.light` 的「用例等待超时默认拒绝」（`段落 用例等待超时默认拒绝`，
122-135 行）用 `time.time()` 计算耗时：

```light
设 开始 为 time.time()
设 结果 为 mgr.等待结果(aid4, 0.3)
设 耗时 为 time.time() - 开始
...
断言(耗时 >= 0.2, "用例12d: 至少等0.2s（实际" + 转字符串(耗时) + "）")
断言(耗时 < 1.5, "用例12e: 未超1.5s（实际" + 转字符串(耗时) + "）")
```

**flaky 根因**：`time.time()` 返回**系统墙上时钟**（wall clock）。两次采样之间若发生
NTP 校准、手动改时、夏令时切换或虚拟机时钟回拨/前跳，差值会被拉大或变成负数，
导致 用例12d / 用例12e 间歇性失败——这是典型的非确定性测试（flaky）形态，
与被测逻辑（审批超时默认拒绝）是否正确无关。

## 二、time.time() 使用点全量排查（4.1）

全文件 `time` 相关使用点复核（修改前）：

| 行 | 内容 | 用途 | 是否需改 |
|---|---|---|---|
| 3 | `导入 time` | 模块导入 | 否 |
| 125 | `设 开始 为 time.time()` | 耗时起点采样 | **是** |
| 127 | `设 耗时 为 time.time() - 开始` | 耗时差值计算 | **是** |

全文件仅此 2 处 `time.time()`，**全部**用于耗时计算，无其他用途（无时间戳落盘、
无格式化输出、无幂等键生成），故按任务书 4.2「只改用于计算耗时的」口径，
2 处均属修改范围，改动即全量，无遗漏。

## 三、修复内容（4.2）

```diff
@@ -122,9 +122,9 @@
 段落 用例等待超时默认拒绝:
   设 mgr 为 新建 审批管理器()
   设 aid4 为 mgr.请求审批("sessD", 2, "bash", ["命令": "rm x"])
-  设 开始 为 time.time()
+  设 开始 为 time.monotonic()
   设 结果 为 mgr.等待结果(aid4, 0.3)
-  设 耗时 为 time.time() - 开始
+  设 耗时 为 time.monotonic() - 开始
```

净效果：`git diff` 仅 2 行（+2/-2），`time.time()` 残留 0 处，
`time.monotonic()` 共 2 处；其余 163 行与修改前逐字节一致。

**为何 `time.monotonic()` 可行**：`导入 time` 走光明模块桥接，成员访问直通
Python 标准库 `time` 模块属性；`monotonic()` 为 stdlib 内建（PEP 418），
返回**单调递增**时钟（秒），不受系统时间调整影响。运行期无需在编译器/桥接层
做任何登记（本轮未触碰 `light-merge`，符合互斥约束）。

## 四、验证结果（4.3）

运行口径：`cd lightharness && python 运行.py examples/test_审批.light`
（`运行.py` 为 lightharness 既有启动器，负责把 `src/` 挂进模块搜索路径；
此前用 `light-merge/cli/light.py` 直跑报「模块未找到 审批」，系运行口径问题，
与本次修改无关。）

### 4.1 首次运行（改动后）

```
通过: 生成id格式 通过: 请求与列表 通过: 处理审批批准 通过: 处理审批幂等
通过: 处理不存在 通过: 处理审批拒绝 通过: 会话隔离 通过: 已处理过滤
通过: 清空 通过: 清理已处理 通过: 等待快速处理 通过: 等待超时 通过: 审批id唯一性
全部 13 用例通过 ✓
rc=0
```

13 用例全绿，含本轮目标用例「等待超时」（用例12a-12g 七条断言全过，
说明 `time.monotonic()` 在光明运行时可解析、可参与减法与比较运算，
且 用例12d 的 `耗时 >= 0.2` 成立——单调时钟与真实等待时长对齐）。

### 4.2 多次运行稳定性（≥5 次硬门槛）

连跑 7 次（超过任务书要求的 5 次），逐次留痕 `_r31_t4_run1..7.txt`：

| 次数 | rc | 输出尾部 |
|---|---|---|
| run1 | 0 | 全部 13 用例通过 ✓ |
| run2 | 0 | 全部 13 用例通过 ✓ |
| run3 | 0 | 全部 13 用例通过 ✓ |
| run4 | 0 | 全部 13 用例通过 ✓ |
| run5 | 0 | 全部 13 用例通过 ✓ |
| run6 | 0 | 全部 13 用例通过 ✓ |
| run7 | 0 | 全部 13 用例通过 ✓ |

**7/7 rc=0，输出逐字节一致，零波动。**

### 4.3 耗时计算逻辑正确性确认

- 用例12d `耗时 >= 0.2`：单调时钟起点采样于 `等待结果(aid4, 0.3)` 调用前，
  超时路径真实等待 ≥0.2s 后返回，断言成立 → 时钟读数与真实流逝时长对齐；
- 用例12e `耗时 < 1.5`：无时钟跳变时差值 ≈ 0.3s，上界余量充足；
- 两次采样同源（均为 `time.monotonic()`），差值恒为**非负的真实流逝时长**，
  不再受墙上时钟跳变影响 —— flaky 根因被移除。

### 4.4 未模拟时钟跳变的说明（诚实标注）

本轮**未**通过改系统时间来复现跳变场景（改系统时钟属破坏性系统操作，
不在任务授权范围）。flaky 修复的依据是 `time.monotonic()` 的 PEP 418 语义保证
（单调、不受系统时间调整影响）+ 7 次真实运行零波动，而非跳变注入复现。
如需注入级证据，建议后续用 `faketime`/时钟 mock 框架补做，属测试基建增强。

## 五、结论

- `examples/test_审批.light` 的 2 处耗时计算用 `time.time()` 已全部改为
  `time.monotonic()`，其余用途无（无需保留项）；
- 7 次连续运行 rc=0、输出一致，任务书 4.3 的 ≥5 次硬门槛达成；
- 本任务未触碰 `light-merge`，未触碰 `_EMBED` 表（任务1-3 互斥区），
  未新增 Python 绕语言缺陷的实现。
