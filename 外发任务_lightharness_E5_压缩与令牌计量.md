# 外发任务 E5：压缩与令牌计量

## 任务目标

实现上下文压缩和令牌计量，解决长对话爆上下文的问题。实现上下文摘要压缩 + 工具结果修剪 + token 使用统计。

参考 deepseek-harness `packages/compaction`（压缩能力 + basic provider + 工具结果修剪）、`packages/llm/token-meter`（令牌计量）的设计。

## 核心设计

### 1. 令牌计量

在 `src/令牌计量.light`（新建）中实现：

```光明
类 令牌计量器:
  属性 本轮输入 等于 0
  属性 本轮输出 等于 0
  属性 累计输入 等于 0
  属性 累计输出 等于 0
  属性 累计花费 等于 0.0    # 美元
  属性 模型单价 等于 {}      # 模型名 → {"输入":每百万token价格, "输出":...}
  
  段落 记录响应 接收 响应:
    # 从 OpenAI 响应的 usage 字段读取 prompt_tokens / completion_tokens
    # 更新本轮和累计计数
    # 计算花费 = (输入/1e6)*输入单价 + (输出/1e6)*输出单价
  
  段落 估算消息 接收 消息列表:
    # 估算消息列表的 token 数（无 API 响应时用启发式）
    # 中文约 1.5 字/token，英文约 4 字符/token，代码约 3 字符/token
    # 返回估算的总 token 数
  
  段落 本轮统计 接收:
    # 返回 {"输入":N,"输出":N,"总":N,"花费":$X}
  
  段落 累计统计 接收:
    # 返回 {"输入":N,"输出":N,"总":N,"花费":$X,"轮次":N}
  
  段落 重置本轮 接收:
    # 新一轮开始时重置本轮计数
```

模型单价表（内置常见模型，可配置）：
- deepseek-chat：输入 $0.27/M，输出 $1.10/M
- deepseek-reasoner：输入 $0.27/M，输出 $1.10/M
- gpt-4o：输入 $2.50/M，输出 $10.00/M
- gpt-4o-mini：输入 $0.15/M，输出 $0.60/M
- 未知模型：输入 $1.00/M，输出 $2.00/M（默认估算）

### 2. 工具结果修剪

在 agent 循环的 post_tool 钩子（契约 4）中注册工具结果修剪：

```光明
函数 修剪工具结果(工具名, 结果):
  # 结果是字符串
  设 最大长度 为 8000    # 字符数，约 2000-4000 token
  如果 结果的长度 <= 最大长度:
    返回 结果
  # 超长则截断，保留开头和结尾，中间加提示
  设 开头 为 结果截取(0, 最大长度 * 0.7)
  设 结尾 为 结果截取(结果长度 - 最大长度 * 0.3)
  设 提示 为 "\n... [结果已截断，原长 " + 结果长度 + " 字符，保留首尾] ...\n"
  返回 开头 + 提示 + 结尾
```

特殊处理：
- bash 工具：最大 8000 字符
- 读文件工具：最大 16000 字符（文件内容可能较长）
- 搜索文件工具：最大 4000 字符（搜索结果通常不需要全文）
- 其他工具：默认 8000 字符

### 3. 上下文摘要压缩

在 `src/压缩.light`（新建）中实现两种压缩策略：

#### 策略 A：滑动窗口（默认，免费）

当消息列表的估算 token 数超过阈值（默认 8000 token，约模型上下文的 1/2）时：
1. 保留系统提示（第一条）
2. 保留最近 N 轮对话（默认 6 轮 = 12 条消息）
3. 中间的旧消息丢弃
4. 在丢弃位置插入一条系统消息：`"[已省略 N 条历史消息，保留最近 6 轮]"`

优点：免费、快速、确定性；缺点：丢失旧上下文细节。

#### 策略 B：LLM 摘要（可选，需 API 调用）

当超过阈值时，用 LLM 对旧消息生成摘要：
1. 取前 M 条消息（超过窗口的部分）
2. 构造摘要 prompt："请用简洁的中文总结以下对话的关键信息，包括用户需求、已完成的操作、未解决的问题、重要的文件路径和决策。不超过 500 字。"
3. 调用 LLM 生成摘要
4. 用一条系统消息替换旧消息：`"[历史摘要] " + 摘要内容`
5. 保留最近 N 轮对话

优点：保留关键信息；缺点：消耗 token、增加延迟、摘要可能遗漏。

E5 实现策略 A（默认）+ 策略 B 的接口（`生成摘要(消息列表)` 函数，当前返回滑动窗口提示，后续可替换为 LLM 调用）。

### 4. Agent 循环集成（post_turn 钩子）

在 agent 循环的每轮结束后（post_turn 钩子）注册压缩检查：

```光明
函数 检查压缩(消息列表):
  设 估算token 为 令牌计量器.估算消息(消息列表)
  设 阈值 为 8000  # 可配置 HARNESS_COMPACTION_THRESHOLD
  如果 估算token <= 阈值:
    返回 消息列表  # 不压缩
  # 超过阈值，执行压缩
  返回 执行压缩(消息列表)
```

压缩在 post_turn 钩子中执行，修改消息列表后，下一轮 API 调用用压缩后的列表。

### 5. 统计展示

在 Web UI 顶栏或右侧面板显示 token 统计：
- 本轮：输入/输出 token 数
- 累计：总 token 数 + 估算花费
- 上下文使用率：当前消息估算 token / 模型上下文上限（百分比进度条）
- 压缩次数：本轮会话已触发压缩的次数

通过 SSE 流推送统计事件：
```
data: {"种类":"统计","本轮输入":N,"本轮输出":N,"累计总":N,"花费":$X,"上下文使用率":65%}
```

## 交付物

1. `src/令牌计量.light` — 令牌计量器（响应记录/消息估算/统计）
2. `src/压缩.light` — 压缩策略（滑动窗口 + LLM 摘要接口）
3. `src/agent.light`（修改）— 注册 post_tool 修剪钩子 + post_turn 压缩钩子
4. `src/web服务器.light`（修改）— SSE 推送统计事件
5. `webui/app.js`（修改）— 统计事件处理 + UI 展示
6. `examples/test_令牌计量.light` — 令牌计量最小复现
7. `examples/test_压缩.light` — 压缩触发/执行最小复现
8. 修复说明：改动点 + 验证结果

## 验收标准

- [ ] 令牌计量：API 响应的 usage 字段正确记录 prompt_tokens/completion_tokens
- [ ] 消息估算：中文/英文/代码的 token 估算在合理范围（误差 <30%）
- [ ] 花费计算：按模型单价计算累计花费，未知模型用默认价
- [ ] 本轮/累计统计：接口返回正确的计数和花费
- [ ] 工具结果修剪：超过 8000 字符的结果被截断，保留首尾 + 提示
- [ ] 不同工具阈值：bash 8000、读文件 16000、搜索 4000
- [ ] 短结果不修剪：<= 阈值的结果原样返回
- [ ] 压缩触发：消息估算 token 超过 8000 时触发压缩
- [ ] 滑动窗口：压缩后保留系统提示 + 最近 6 轮，中间插入省略提示
- [ ] 压缩后功能：压缩后的消息列表可正常继续对话，LLM 能看到省略提示
- [ ] 统计事件：SSE 流中每轮结束推送 `种类:"统计"` 事件
- [ ] UI 展示：顶栏或右侧面板显示本轮/累计 token、花费、上下文使用率
- [ ] 上下文使用率：进度条正确显示百分比，超过 80% 变黄色，超过 95% 变红色
- [ ] 压缩不丢工具结果：最近 6 轮内的工具调用和结果完整保留
- [ ] 全量回归不破坏（188 passed）

## 注意事项

- 与 E1 的 post_tool/post_turn 钩子机制（契约 4）严格对齐：E5 只注册钩子，不修改 agent 主循环
- 压缩阈值可配置：环境变量 `HARNESS_COMPACTION_THRESHOLD`（默认 8000），`HARNESS_COMPACTION_KEEP_ROUNDS`（默认 6）
- 模型上下文上限：环境变量 `HARNESS_MODEL_CONTEXT_WINDOW`（默认 32768），用于计算上下文使用率
- token 估算启发式：中文 1.5 字/token、英文 4 字符/token、代码 3 字符/token、工具结果按代码估算；这是估算不是精确值，用于触发压缩判断
- 工具结果修剪在 post_tool 钩子中执行，修剪后的结果存入消息列表和会话持久化（E2），原始结果不保留
- 压缩是有损操作：滑动窗口会丢失旧消息，LLM 摘要可能遗漏细节；在 UI 中明确提示"已触发上下文压缩，旧消息可能被省略"
- 压缩幂等性：已经压缩过的消息列表再次检查时，不会重复压缩（因为 token 数已降到阈值以下）
- 系统消息保护：压缩时第一条系统提示（agent 人设/工具定义）始终保留，不被窗口丢弃
- 工具调用完整性：滑动窗口以"轮"为单位丢弃（user+assistant+tool 为一轮），不丢弃半轮（避免工具调用没有结果的不一致状态）
- 与 E2 会话持久化的协调：压缩后的消息列表写入会话存储，旧消息从持久化中移除（压缩是持久的，不是仅内存）
- 不阻塞 E1：如果 E5 未合入，E1 的 agent 循环用空 post_tool/post_turn 钩子，功能正常但长对话可能爆上下文
- 性能：压缩检查每轮执行一次，消息估算 O(N)，1000 条消息估算 < 10ms，不影响响应速度
