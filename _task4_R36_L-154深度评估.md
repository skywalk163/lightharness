# 第36轮 任务4：L-154 深度评估 —— CCW 清零后合成名整词能力丧失

> 交付日期：2026-09-16 ｜ **只评估，不改代码**（本轮未修改任何 `.py`/`.light` 源码）
> 取证脚本：`lightharness/_task4_R36_L154_取证扫描.py`（可复跑）
> 语料规模：867 个 `.light` 文件全扫

---

## 〇、明确建议

> ## **不立项修复，维持现状（方案 D）。**
>
> 唯一的真实风险不是「名字被劈」，而是**报错不可诊断**；该风险已被两条既有测试钉住，
> 且修复代价（A/B）远高于收益（收益实测 ≈ 0）。
> 建议把「报错可诊断性增强」单独登记为低优先待办（方案 C 的子集），**不并入本轮、不占立项**。

一句话理由：**任何被正确声明/导入的名字，在全部 7 种位置形态下都不会被劈**（跨文件
实测 rc=0）。L-154 只在「名字从未在本文件声明」时咬人——而那本来就是「未定义名」，
只是报错信息被劈词污染成了误导性语法错误。

---

## 一、现状取证（全部基于实际扫描）

### 1.1 语料与口径

- 语料 8 个 glob：`lightharness/{examples,src,tests}` + `light-merge/{examples,stdlib,bootstrap,src,tests}`
  下全部 `.light`，去重后 **867 个文件**。
- tokenize 失败 2 个（已知既有错误，非本轮引入）：
  `light-merge/bootstrap/release/stdlib/集合.light`、`light-merge/examples/_test_nested_closure.light`
  （均为 `LexerError`）。
- 名全集口径：**只用 `tokenize()` 产出的 IDENTIFIER 值** + 每文件 `_scan_user_definitions`
  预扫描结果。不用原始文本正则扫——原始文本会把注释、字符串、docstring 里的中文
  片段（如 `断言标准库 - 临时文件模块`、`实际值.转小写()如果忽略大小写否则实际值`）
  当成名字，产生大量假命中（首轮扫描踩了这个坑，已修正口径）。

### 1.2 stdlib/bootstrap 内建名分类（759 个候选 × 7 形态实测）

候选 = 「词首命中关键字 + 整串非关键字 + 不在 `MERGE_WHOLE`/`CCW` 保护表」。
stdlib/bootstrap 名全集 6247 个，其中候选 **759 个**。

7 种位置形态：

| 代号 | 形态 | 构造 |
|---|---|---|
| F1 | 裸名语句起始 | `X。` |
| F2 | 赋值右值 | `设 甲 为 X。` |
| F3 | 调用位 | `X(1)` |
| F4 | 成员访问 | `设 r 为 对象.X` |
| F5 | 导出引用 | `导出 X` |
| F6 | 条件位 | `如果 X 那么 返回 1` |
| F7 | 定义位 | `段落 X: 返回 1` |

分类结果（每形态「未整词 = 被劈」的计数，共 759 个候选）：

| 形态 | 被劈 | 形态 | 被劈 |
|---|---|---|---|
| **F1 裸名语句起始** | **336/759** | F4 成员访问 | 3/759 |
| F2 赋值右值 | 55/759 | F5 导出引用 | 3/759 |
| F3 调用位 | 57/759 | F6 条件位 | 55/759 |
| | | F7 定义位 | 3/759 |

F1 是唯一的重灾区（336），其余六形态最高只有 57（F3，即 C 类）或 55（F2/F6，即 B 类）。

四分类结果：

| 类别 | 数量 | 含义 |
|---|---|---|
| **A 类：纯 L-154** | **269** | 仅 F1 劈；F2/F3/F4/F5/F6/F7 全部整词 |
| B 类：赋值尾巴/SEP | 10 | F2 劈（`为`+`空/真/假` 尾巴、`幂` 一元前缀），调用位安全 |
| C 类：最硬 | 57 | 连调用位都劈 —— 全部含拉丁字母/扩展名/SEP |
| D 类：全整词 | 423 | 七形态全整词 |

**A 类词首关键字分布（Top 5）**：`定义` 99 / `如果` 24 / `设` 23 / `并` 12 / `生成` 9。
A 类样本：`从列表中过滤匹配的文件`、`从右侧分割`、`使用标准句柄`、`关闭所有库`、
`函数体内`、`否则如果长度`、`回调函数`、`外部命令模块`……

**B 类全清单（10 个）**：`为假`/`为真`/`为空`/`幂次`/`断言不为空`/`断言不为空字符串`/
`断言为假`/`断言为真`/`断言为空`/`断言为空字符串`。
—— 属 R20「`X为` 赋值尾巴切分」与 `幂` 一元前缀规则的既定语义，不是 L-154。

**C 类 57 个的关键观察**：几乎全部含拉丁字母或文件扩展名或成员分隔符，
例如 `读取CSV`→`[读取, CSV]`、`生成UUID4`→`[生成, UUID4]`、`是Windows`→`[是, Windows]`、
`断言工具.py`→`[断言工具, ., py]`、`己.剩余体`→`[己, ., 剩余体]`、
`在文件末尾的模块级`→`[在, 文件末尾的模块级]`。
这些来自 stdlib 里的**遗留 Python 伪代码 `.light` 文件**（实测
`light-merge/bootstrap/release/stdlib/CSV读写器.light` L1 是 `def读取CSV(...)`、
L3 是 `return_读取CSV字典(...)`；`临时文件.light` L1 是散文标题
`段言标准库 - 临时文件模块`）。它们本来就不是光明原生标识符，
拉丁字母/句点游程切分是词法的既定行为，**不应计入 L-154**。

> 因此 L-154 的**真实打击面 = A 类 269 个名 × 1 种位置（F1 裸名语句起始）**。

### 1.3 任务书点名的 3 个例子

| 名字 | 语料命中 | 词首关键字 | 被劈的形态 |
|---|---|---|---|
| `异步读取文件` | 0 文件（stdlib/bootstrap 零命中） | 异步 | 仅 F1 |
| `并发等待` | 1 文件（`test_R23_CCW内建名.light`） | 并 | 仅 F1（A 类） |
| `常量时间比较` | 0 文件（stdlib/bootstrap 零命中） | 常量 | 仅 F1 |

三者逐形态实测（前两个 token，`ERR` 无）：

```
异步读取文件    F1=[异步, 读取文件]   F2=[设,甲,为,异步读取文件]  F3=[异步读取文件,(,1,)]
                 F4=[设,r,为,.,异步读取文件] F5=[导出,异步读取文件] F6/F7 同样整词
并发等待        F1=[并, 发, 等待]     F2/F3/F5/F6/F7 全部整词
常量时间比较    F1=[常量, 时间比较]   F2/F3/F5/F6/F7 全部整词
```

`并发等待` 的语料命中处 `lightharness/examples/test_R23_CCW内建名.light`
L13 `段落 并发等待 接收 输入:` + L31 `设 等待结果 为 并发等待(41)`，该文件 rc=0 全绿。

---

## 二、为什么「语句起始」是唯一打击面（代码级归因）

`lexer.py` 的合并链路按顺序有四道保护，只有语句起始位会同时绕开它们：

1. **`_P0A_MERGE_WHOLE` 整串精确匹配**（L2286，当前 2 条）：整串命中即整体成词，
   不看位置。
2. **`user_definitions` 预扫描检查**（L2276 / L2302 / L2406 / L2478 四处）：
   段落名、设名、导出名、**导入名**全部在册 → 整串作为标识符，不拆分。
3. **R21 上下文敏感切词闸门**（L2404 起）入口条件是
   `_ctx_call or not self._at_statement_start(source, pos)`
   —— **非语句起始位置或调用位都会走整词合并**，这就是 F2/F3/F4/F5/F6/F7 全绿的原因。
4. **语句起始位**：R21 不生效，走既有确定性切词——词首命中关键字即切分。

**跨文件实测（决定性证据）**：建临时模块 `l154_mod.light`
（`段落 异步读取文件 接收: 返回 42` + `导出 异步读取文件`），再从另一文件导入后调用：

| 用例 | 源码形态 | 结果 |
|---|---|---|
| 导入后 F1 裸名 | `异步读取文件。` | **rc=0**，打印 `F1_ran` |
| 导入后 F2 赋值右值 | `设 结果 为 异步读取文件。` | **rc=0**，返回 `<function 异步读取文件 at 0x...>` |
| 导入后 F3 调用 | `返回 异步读取文件()` | **rc=0** |
| 导入后 F5 导出 | `导出 异步读取文件` | **rc=0** |
| **不导入**直接写 F1 | `异步读取文件。` | **rc=1**（见 §2.1 报错） |
| 同文件内定义+调用 | `段落 异步读取文件 ...` + `异步读取文件(1)` | **rc=0** |

结论：**`从 X 导入 NAME` 会把 NAME 登记进导入方的 `user_definitions`**，
所以跨文件引用同样受保护。L-154 唯一的真实触发条件是「名字从未声明/导入」。

### 2.1 报错信息（真实编译输出，全部指向名字中间，无一提示根因）

| 输入（语句起始裸名） | 真实报错 |
|---|---|
| `异步读取文件。` | 行5列7：`期望'函数'（或兼容写法'段落'/'段'），但得到 标识符（名称）` |
| `并发等待。` | `「并」是保留关键字，不能直接作为语句开头。请检查语法是否正确` |
| `常量时间比较。` | `「常量」是常量修饰符，后面必须跟「设」。如：常量 设 圆周率 为 3.14159。` |
| `设置字段。` | `期望'为'或'等于'，但得到 「。」` |
| 未导入直接写 `异步读取文件。` | 同第 1 行 |

**4 种输入 → 4 种互不相同的报错**，且全部满足：① 列号指向名字**中间**；
② 无一提及「这个名字被词法切成了两段」；③ 无一提示「先声明或导入这个名字」。
这正是 L-154 的真实危害：**不是静默错误，而是误导性错误**。

对照实验：`甲。`（非关键字开头、裸名语句）在合法形态下 **rc=0**
（被编译为无参调用 `甲()`，运行期才报类型错误）。说明**裸名语句是光明合法语法**，
F1 位置确实可达——但只有当名字以关键字开头且未声明时，才退化为上述误导性报错。

---

## 三、四个方案的成本 / 风险 / 影响面

### 方案 A：重新引入白名单（恢复 CCW）—— ❌ 不采纳

- **成本**：低（加回 `COMMON_COMPOUND_WORDS` 条目即可）。
- **收益 ≈ 0（本次评估的核心结论）**：白名单要保护的集合是「已声明/已导入的名字」，
  而 `user_definitions` 预扫描 + 四处检查已 100% 覆盖它们（§2 跨文件实测 rc=0）。
  白名单只能额外覆盖「未声明的名字」——而那些名字本就该报未定义，
  把它们整体成词等于把「未定义名错误」换成「期望函数错误」，都是错。
- **风险**：高。① 违背保护表精简方向——CS/CCW/NEVER_SPLIT 已三表清零，
  CCW 曾在 R30 从 184 条降到 0；恢复即重新背表。② 白名单是**全局整串匹配、不分位置**，
  会把 F2/F3 的既定切分语义也改掉，其中包含 B/C 两类里真正依赖切分的行为
  （`为`+值字面量尾巴、`之/在/于/与` 成员/关系分隔符 R24 闸门4）。
- **影响面**：867 文件全量反跑必须重跑；等于回退 R30 的清零成果。

### 方案 B：扩展一元前缀通用规则（类 GR-2b）—— ❌ 不采纳

- **成本**：中。GR-2b 目前只对 `_P0A_UNARY_PREFIX_KW = {'非'}` 开例外；
  要覆盖 A 类 269 名，词首关键字 Top5 就有 `定义/如果/设/并/生成` 5 类，
  实质上是把「语句/控制/赋值关键字在语句起始也可并入」通用化。
- **风险**：高（致命冲突）。硬语句关键字**必须**词首切分，才能支撑
  无空格写法 `如果数…` / `段落阶乘…` / `否则如果…`（`lexer.py` L3309 附近的
  HARD_STMT 硬语句集合）。一旦允许 `如果忽略大小写否则实际值` 这类名词首并入，
  真语句 `如果 X 那么 …` 会被吞成一个标识符——**这是比 L-154 严重得多的解析歧义**。
  R21 的四道闸门（整串非关键字 / 词首更短关键字 / 运算符排除 / 分隔符）正是为防这个。
- **根本障碍**：`异步 读取文件`（两 token）与 `异步读取文件`（一 token）在词法层
  **无法区分**，光明词法没有「已声明名/已知复合名」之外的构词依据（任务书 4.3 的背景判断
  成立）。B 方案解决不了这个，只是把歧义从「词法」搬到「解析」。
- **影响面**：全部无空格写法语句——这是全语料主力形态，回归面最大。

### 方案 C：不修复，只改进报错 —— ⚠️ 部分采纳（独立低优先待办，不并入本轮）

- **成本**：低。在解析器「语句起始得到孤立标识符、且前一 token 是关键字」的报错分支
  补一句提示（如「该位置的名字疑似被切分，请确认已声明/导入」）。
- **风险**：低（只改错误文案，不动 token 流）。
- **收益**：有限。触发前提是「名字未声明」，用户本就该先声明；改进只是把误导性
  从「期望函数」降到「未定义」。
- **处置**：登记为低优先待办即可。**本轮铁律是只评估不改代码**，故本轮不动。

### 方案 D：不修复，维持现状 —— ✅ 采纳

- **成本**：零。
- **风险**：仅剩 §2.1 的报错可诊断性，且已被两条既有测试双钉住：
  - `lightharness/tests/test_R23_词法边界_token.py:96`
    `test_ccw_names_split_after_ccw_cleared` —— 钉住 3 例的**切分形态**
    （只钉「切成什么」不钉「是否整词」，未来若变会红，变更可感知）；
  - `lightharness/examples/test_R23_CCW内建名.light` —— 用「同名段落定义+调用」
    形态自校验 `并发等待`/`常量时间比较`/`创建任务`/`首个完成`/`低级关闭`，rc=0；
  - 同文件 `test_ccw_builtin_names_whole` 钉住 `低级关闭/创建任务/首个完成` 仍整词
    （契约未变），说明 CCW 清零**并未**让所有合成名整词能力都丧失，
    只有「词首关键字」这一类丧失。

---

## 四、后续监控方案

1. **缺陷账 L-154 状态 → 关闭（不修复）**，依据：真实影响面 = 269 名 × 1 位置，
   且该位置在正确声明/导入下 100% 受保护（跨文件实测 rc=0）。
2. **A 类 269 名清单已留存**（取证脚本可复跑导出）。监控触发条件：
   未来某轮新增「词首关键字」内建名时，先查该清单判断是否落入 A 类；
   一旦有**真实用户代码**在语句起始裸引用这类名字（而非测试夹具），立即重开 L-154。
3. **静默恶化的早期信号**：若 `test_ccw_names_split_after_ccw_cleared` 变红，
   说明有轮次改变了切分形态，需同步更新该测试并重新评估本结论。
4. **若日后确需修复，优先级排序**：
   C（报错增强，改动最小、无回归面）→ B 的极窄化版本
   （仅覆盖「词首关键字 + 余部非关键字 + 本文件未声明 + 全语料零命中」交集，
   且必须先解决 `如果…` 类歧义）→ A（最后手段，代价最大）。

---

## 五、附带发现（不在本任务范围，仅记录）

1. **stdlib 里存在遗留 Python 伪代码 `.light` 文件**：
   `light-merge/bootstrap/release/stdlib/` 下多个文件（`CSV读写器.light`、`临时文件.light`、
   `JSON.light`、`字符串处理.light` 等）内容是 `def读取CSV(...)` / `return_...` /
   `导入re` / 散文标题行。首轮扫描的 2588 个「语句起始假命中」几乎全部来自这类文件。
   它们能 tokenize 但不是合法光明代码，建议在后续轮次单独清理或标注（不是 L-154）。
2. **本轮工作区存在他人并行改动**：`light-merge/src/lexer.py` 有 R36 任务3 的
   `_OPERATOR_KEYWORDS_NO_UNARY_PREFIX` 预计算常量改动（含文末自校验断言④）。
   本评估的所有 token 级数据都在**含该改动的工作区**上采集，
   但该改动是语义等价的集合差预计算，不影响 §1 的分类结论。

---

## 附录：取证可复跑

```bash
# 形态分类 + 三例点名复核
python lightharness/_task4_R36_L154_取证扫描.py

# 编译级报错取证（临时探针文件，位于 .box-agent/scratch/，不入库）
#   q_C_time.light       -> 「常量」是常量修饰符…
#   q_set_field.light    -> 期望'为'或'等于'…
#   cf_F1_bare.light     -> rc=0 打印 F1_ran（导入后 F1 受保护）
#   cf_noimport.light    -> rc=1 期望'函数'…（未声明才红）
```

**七形态定义**：F1 `X。` ｜ F2 `设 甲 为 X。` ｜ F3 `X(1)` ｜
F4 `设 r 为 对象.X` ｜ F5 `导出 X` ｜ F6 `如果 X 那么 返回 1` ｜ F7 `段落 X: 返回 1`
