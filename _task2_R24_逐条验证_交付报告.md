# 第24轮 任务2 交付报告：30条单字逐条隔离中立验证 + 分类

> 日期：2026-09-14 ｜ 任务书：复刻_第24轮_任务prompt分发_阶段D单字通用化.md ｜ 状态：已完成
> 修改文件：无（任务书约束：仅验证脚本+报告）
> 交付物：_sweep_r24_t2_compound_safe_single.py（可复跑，输出 _r24_t2_sweep_out.txt，ALL OK）、
>        _task2_R24_单字逐条验证_证据.json（机读全量证据）、_task2_R24_单字分类清单.md（逐条场景）、本报告

---

## 一、方法与自检

- **判据③**（沿用第21-24轮决定性判据）：逐条临时移除 → 全语料 769 个 .light 文件 tokenize → 逐文件对比 token 序列哈希。零变化=冗余可删；有变化=真护栏。
- **双替换细节**：lexer 热路径既有读模块全局（`_match_keyword`/`_skip_compound_safe_and_match`）也有读类属性（`Lexer.compound_safe_single_keywords`，三处调用点），替换时二者同步，避免漏检。
- **正向控制**：清空整表后语料 token 变化 118 文件（>>0），harness 有效。
- 复跑脚本 rc=0（ALL OK）。

## 二、分类结论

| 分类 | 条数 | 单字 |
|---|---|---|
| ① 冗余可删 | 2 | **是、首** |
| ③ 守卫管辖且冗余（可直接删） | 4 | **余、减、空、除** |
| ③ 守卫管辖但真护栏（反馈任务1/3/4） | 3 | **乘、加、真** |
| ② 真护栏 | 21 | 例出列则到对常引接断末模段的类自试跳过配长 |

**冗余仅 6 条（20%）、真护栏 24 条（80%）**——单字表是当前保护表中最"实"的一张，与第22轮 CCW TierB 32条全冗余形成鲜明对比。任务书预设的"逐条验证后批量可删"预期不成立，通用规则替代（任务3/4）是必须路径。

## 三、真护栏保护场景分析（按任务书四类）

| 场景类型 | 条数 | 单字与例证 |
|---|---|---|
| 词尾并入 | 17 | 出(标准输出/本轮输出)、列(日期列)、则(规则/越界正则)、到(文件未找到)、常(首异常)、引(最后索引)、接(符号链接)、断(截断)、末(月末)、真(标准输出失真)、类(有界队列类)、试(测试)、跳(最后心跳)、过(通过)、长(块长之块长表)、配(配[0] L-119)、段(段[下标] L-119) |
| 词中并入 | 5 | 例(用例)、对(对于 归属敏感)、自(自定义表/自闭合标签集)、乘(乘阶乘)、加(加斐波那契) |
| 词首并入 | 1 | 模(模拟客户端 类名，22文件最大护栏) |
| 递归副作用实证 | 1 | 的(成绩的长度：移除后切成 成绩+的长度，暴露分段——`的` 在表内时由递归副作用整体成词) |

注：乘/加 的词中并入依赖 `_skip_compound_safe_and_match` 递归副作用（后随 compound-safe 单字+无真关键字→回报内层→调用方skip），归任务4规则化；`的` 的递归行为归任务1修复；其余 18 条的并入判定发生在 `_tokenize_chinese_sequence` 三处上下文判据（第一层单字动词词首跳过/探测循环/输出循环）+ P0-A 层，归任务3。

## 四、运算符/值字面量交叉验证（反馈任务1）

- `甲 加 乙`/`甲加乙`：_P0A_OP 正确切分（含空格/无空格均验证）。
- `减法`/`乘法` 词首：动词复合名头并入正确。
- `为 真`/`为 空` 值字面量：由最长匹配+位置判定保证。
- **守卫漏洞**：未发现运算符/值字面量层面的守卫缺口；`余`/`减`/`空`/`除` 的构词保护需求已为零（上下文切词覆盖）。
- **耦合警告**：`除` 的零冗余依赖 `空` 在表内的递归副作用（`去除空格` 保护链：除→空）。若任务3/4删除 `空`，须同时验证 `去除空格` 仍整体成词，否则 `除` 需回表。

## 五、给任务3/4的规则设计输入

1. 单字后缀规则须覆盖：词尾并入（17条）+ 词中并入（对/自/例）+ 词首并入（模）；
2. 必须保住的既有 KEYWORD 形态：`返回真`（值字面量词尾）、`配[0]`/`段[ ]`（单字别名+下标，L-119 判据四条件）、`遍历 块长 之 块长表`（之 前不入递归）、`如果 段[...]`（语句起始+下标）、`则`/`到`/`是` 词首语句语义；
3. `的` 修复判据（任务1）：`成绩的长度` 整体成词 与 `X 的 Y`（后随空白切分）并存；
4. 运算符/值字面量单字（乘加真 + 冗余的余减空除）按任务书不纳入单字通用化，仅删其中冗余者。
