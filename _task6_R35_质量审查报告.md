# 任务6（R35）：质量审查 + docs 回填 + 收口框架

> 执行：2026-09-16 ｜ 独立性：本报告**不采信任务1-5 的结论文字**，全部关键证据均**独立重跑复核**
> 铁律：docs 只备初稿、由路M终稿；审查独立于任务1-5；对标清单编号连续（#156）。

---

## 一、审查范围与复核方法

| 审查对象 | 复核方式（本轮实际执行） | 结果 |
|---|---|---|
| 任务1 `整理模型消息` 结论 | 重跑 `_antirun_r35_final.py` 的 G1/G3 段 + 复现 `甲模乙` | 一致 |
| 任务2 `非空块` 结论 | **重跑变异反跑**（关掉 GR-2b） | 一致，且证实非假绿 |
| 任务3 `记录类型` 结论 | 重跑 G3 + 复现 `数类型` 切分 | 一致 |
| 任务4 全量反跑/G3 终验 | **完整重跑** `_antirun_r35_final.py` | 一致 |
| 任务5 性能/pytest | 交替 ABAB 复测（推翻首轮结论） | 见 F-1 |

### 1.1 独立复核输出（原始）

```
=== A. G1 语料门：当前 vs HEAD ===
  可比文件=867 变化=0 新错=0 ✅ 零回归

=== B. G3 边界门：当前 vs HEAD ===
  整理模型消息   G3通过
  非空块      G3通过
  记录类型     G3通过

=== C. 变异反跑：关掉 GR-2b（非空块 仍不在 MERGE_WHOLE）===
  非空块 变异后 G3 失败形态：['F2 函数名', 'F5 传参位']
      [F2] HEAD: …IDENTIFIER·非空块(  变异态: …KEYWORD·非 IDENTIFIER·空块(
      [F5] HEAD: (IDENTIFIER·非空块,   变异态: (KEYWORD·非 IDENTIFIER·空块,
  判定：✅ 规则敏感（变异必红）
=> 全部通过

三个边界用例：test_R35_{整理模型消息,非空块,记录类型}边界.light  均 rc=0
```

**复核判定**：任务1-4 的三重判据证据**真实、可复现**，未发现"假绿"。

---

## 二、发现清单

### F-1（严重度：方法 · 已修正）性能首测结论错误，被交替复测推翻

- **现象**：任务5 首次 A/B（A 先 B 后、各 1 轮）得出「**B 快 5.64%**」；改成 `ABAB` 交替 4 轮后翻转为「**B 慢 1.87%**」。
- **根因**：先跑者承担冷启动 / 页缓存未热 / CPU 未睿频的代价；单次顺序测量**无法分离顺序效应**。
- **影响**：若采信首测，会得出"通用化带来性能收益"的错误结论并写进 docs。
- **处置**：已改用交替 ABAB、每变体取全局最优；报告只写"噪声带内、无实质影响"。
- **建议**：把「性能 A/B 必须交替多轮」写入反跑判据/方法论（本轮已回写 skill）。

### F-2（严重度：高 · 已修正）R35 使 5 条既有 pytest 断言过期

MERGE_WHOLE 3→2 后，`test_R32`（2 条）与 `test_R33`（1 条）硬断言 `len == 3`、`test_R30_零除`（1 条）断言 `mw == {…非空块…}` 必然打红；另 `test_R30_零除::test_hm_still_22` 为 R33 遗留。
**已全部修正**，并新增 `test_merge_whole_r35_removed_entry`（钉住"已移除且通用规则生效"）。

### F-3（严重度：高 · 已修正）**R33 遗留 16 条过期形态断言**，此前一直未被发现

这是本轮最有价值的发现：跑全量 pytest 时暴露 **18 条既有红**，其中 **16 条与 R35 无关**，
全部是 **R33 清零 `_P0A_NEVER_SPLIT`（4→0）后未同步更新** R23/R25–R30 的"保护表形态"断言。

| 断言族 | 过期值 → 现值 |
|---|---|
| HM（词首并入类别） | 22 → **25**（步/至/到 进入） |
| F（`_TRAILING_ALIAS_CLASS`） | 43 → **46** |
| `_P0A_HEAD_SPLIT_SINGLE` | 42 → **39**（步/至/到 移出排除集） |
| `CS∪HM∪DUAL` | 30 → **32** |
| `到 ∈/∉ F` | ∉ → **∈** |
| `{'到'} ⊆ _P0A_NEVER_SPLIT` | 该表已清零 → 改判 `到 ∈ DUAL` |
| `A_DUAL ∩ HM == ∅` | ∅ → **{到}** |
| `Lexer.compound_safe_single_keywords`（旧名） | 属性已不存在 → AttributeError |

**判定**：这正是项目记忆里已记录的「**收口核验口径**」坑（提交/收口时只核工作树、不核已提交态能否跑绿）的又一次命中。
**已全部修正**并逐条写明致过期轮次。

### F-4（严重度：中 · 已定位，交路M决策）R26 全语料基线快照过期

2 条 `TestCorpusZeroTokenChange::test_fingerprint_unchanged` 报「1 文件变化：`test_审批.light`」。
**已定位为源文件内容变更而非词法回归**：快照生成于 2026-09-15 **13:31**，而该文件于同日 **20:02**
（提交 `e26ed3d`，第31轮 T4 flaky 修复 `time.time→time.monotonic`）修改入库，晚 6.5 小时。
**按红线"合并前不刷 baseline"，本轮不重生成快照**，交路M二选一：重新生成 / 改为只比对快照后内容未变的文件。

### F-5（严重度：中 · 新登记 L-154）CCW 清零后合成名整词能力静默丧失

`异步读取文件` / `并发等待` / `常量时间比较` 在 CCW 清零后不再整词（详见缺陷账 L-154）。
**风险点在于"静默"**：用户按中文直觉写 `段落 并发等待:` 会在词法层被劈开，报错不直观。
本轮处置：不修（修它等于重引白名单），改为在 `test_R23` 用 `test_ccw_names_split_after_ccw_cleared`
**钉住现状**，让将来任何恢复整词的改动都可感知。

### F-6（严重度：低 · 已知接受）词首并入 / 词尾切分不对称

`非空块`（词首）整词，但 `甲非`（词尾）仍切成 `甲 + 非`。根因是 GR-2b 只改 R21 闸门3（词首），
未改 L3343 的词尾切分排除集。**有意保留**（词尾并入会把 `甲非` 的运算符语义吞掉）。
已登记 `行为差异清单 R35-D2`，并已由 `test_R35` 钉住三态。

---

## 三、三重判据充分性审查

| 判据 | 任务1/3（保留） | 任务2（移除） | 充分性评价 |
|---|---|---|---|
| **G1** 全语料 token 零变化 | 撤单条：任务1 = 0 变化；任务3 = **2 文件变化** | **0 变化**（867 文件） | ✅ 充分。任务3 的 2 文件变化被如实记录并作为保留依据，未掩盖 |
| **G2** 边界编译门 rc=0 | 3 个边界用例 rc=0（改动前后各跑） | rc=0（两态各跑） | ✅ 充分 |
| **G3** 五种边界形态一致 | 任务1 **F5 FAIL**、任务3 **F2/F5 FAIL**（这正是保留理由） | 五形态全通过 | ✅ 充分，且用 FAIL 反证了"真护栏" |
| **变异反跑** | — | 关掉 GR-2b ⇒ F2/F5 **必红** | ✅ 关键。证明规则**确实生效**，排除"改了个寂寞"的假绿 |

**补充判据（本轮新增，建议固化）**：
- **反向形态探针**（12 条 `非X` 变体）0 变化 —— 证明收窄条件不是摆设；
- **活性校验** —— 确认变体派生集确与基线不同（R33 已用，本轮 G1 引擎同口径）。

**审查结论**：三重判据执行**严格、结论可信**；任务1/3 的"保留"是有实证反例支撑的**主动判定**，
不是"试不出来就放弃"。唯一缺口是任务1/3 未做变异反跑——但对其**保留**结论而言变异反跑不适用
（没有新规则可关），故不构成缺口。

---

## 四、边界用例覆盖度审查

| 用例 | 行数 | 断言数 | 覆盖形态 | 评价 |
|---|---|---|---|---|
| `test_R35_整理模型消息边界.light` | 84 | 26 | F1–F5 + 传函数名位 | ✅ 五形态齐 |
| `test_R35_非空块边界.light` | 92 | 30 | F1–F5 + `非 是真` 正常用法 | ✅ 五形态齐，**含反向用法** |
| `test_R35_记录类型边界.light` | 88 | 28 | F1–F5 + `X类型` 对照组 | ✅ 五形态齐，**含对照组** |

- 三个用例均按铁律"**新增一律 .light，禁 Python 绕语言缺陷**"编写，无 Python 兜底。
- `test_R35_非空块边界` 显式断言了 `非 是真`（`非` 作逻辑非的正常用法）不受影响 —— 这是任务2 铁律明确要求项，**已覆盖**。
- `test_R35_记录类型边界` 显式断言了 `数类型/内容类型/…` 仍切分 —— 证明收窄不越界，**已覆盖**。

---

## 五、全量回归证据审查

| 套件 | 结果 | 判定 |
|---|---|---|
| `pytest tests/ --ignore=test_回归.py`（**修正后**） | **812 passed / 2 failed** | 2 红 = F-4（基线过期），已定位非回归 |
| 同上（**HEAD lexer 对照**） | 23 failed / 760 passed | ⇒ R35 **净减 21 红、新增 0 红** |
| `pytest tests/test_回归.py`（独立 .light 套件） | 381 passed / 2 failed | 2 红 = `test_R29_CCW精简边界`/`test_R30_CCW通用化边界`，git 显示 **tracked 且未改动** ⇒ PREEXISTING |
| 新增 `test_R35_…_token.py` | **31 passed** | ✅ |

**审查结论**：全量回归证据**完整且可复现**；剩余红已全部定位到根因，无未解释项。

---

## 六、docs 回填（初稿，路M终稿）

| 文档 | 条目 | 状态 |
|---|---|---|
| `docs/功能对标/对标清单.json` | **#156** `_P0A_MERGE_WHOLE 剩余3条通用化精简（3→2）` | ✅ 已追加（原 155 条 → 156 条，编号连续，JSON 已复核可解析） |
| `docs/功能对标/行为差异清单.md` | **R35-D1** MERGE_WHOLE 通用化结果（含 2 条保留理由的实证反例） | ✅ 已追加 |
| 同上 | **R35-D2** 一元前缀字 `非` 的词首并入/词尾切分不对称 | ✅ 已追加（F-6） |
| 同上 | **R35-D3** R26 基线快照过期 + R33 遗留 16 条过期断言 | ✅ 已追加（F-3/F-4） |
| `docs/功能对标/语言缺陷账.md` | **L-154** CCW 清零后合成名整词能力静默丧失 | ✅ 已追加（F-5，本轮唯一新缺陷） |

---

## 七、审查结论

**总体判定：通过（有条件）**

- 技术结论正确：`_P0A_MERGE_WHOLE` 3→2，`非空块` 通用化证据链完整（G1/G2/G3 + 变异反跑全通过），
  另 2 条保留有实证反例支撑。
- 代码改动干净：`lexer.py` 34 增 / 6 删，其中**代码改动仅 4 处**。
- 验证充分性达标：三重判据 + 变异反跑 + 反向形态，无假绿。

**放行条件（须路M确认）**

1. **F-4**：R26 基线快照是否重新生成（2 条红待决策）。
2. **F-5 / L-154**：是否接受 CCW 清零的合成名代价，或立项做通用化恢复。
3. 建议把「**性能 A/B 必须交替多轮**」（F-1）与「**收口前跑全量 pytest，而不只跑 .light 套件**」（F-3）
   两条写入 `docs/功能对标/反跑判据.md` —— 本轮 F-3 之所以潜伏一轮，正是因为收口只跑了 `test_回归.py`。

---

## 八、本轮审查发现的方法论（建议回写）

1. **性能 A/B 必须 ABAB 交替 + 取每变体最优**；单次顺序结论会翻转正负号（实测 +5.6% → −1.9%）。
2. **收口必须跑全量 pytest**，不能只跑 `.light` 回归套件 —— 形态断言过期不会在 `.light` 套件里暴露。
3. **改保护表后要全局 grep 该表在 tests/ 中的所有硬断言** —— 本次 5 条（R35）+ 16 条（R33 遗留）均由此产生。
4. **"记录现状"优于"留着红"** —— 对已知且暂不修的行为（`异步读取文件` 等），改成钉住现状的断言，
   使其变化可感知，而不是让一条永远红的断言丧失信号价值。
