# test_R26_词首并入反向.light —— 第26轮 任务4：词首必须切分场景（运行期自校验）
#
# 任务书要求：
#   · 硬语句关键字在词首必须切分为 KEYWORD，不并入标识符；
#   · 运算符在词首必须切分（后随空白时）；
#   · 值字面量（真/假/空）在词首必须切分（后随空白时）；
#   · 多字关键字最长匹配优先于 CS 单字词首并入（段落数/接口/跳出 仍成 KEYWORD）。
# 全部 rc=0 自校验。

段落 断言相等 接收 实际, 期望, 标签:
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

# ---- 一、条件关键字：如果/那么/否则/否则如果 ----
段落 测试_条件 接收 甲:
    如果 甲 == 3:
        返回 "如果命中"
    否则如果 甲 == 5:
        返回 "否则如果命中"
    否则:
        返回 "否则命中"

断言相等(测试_条件(3), "如果命中", "如果/否则如果/否则 词首切分")

# ---- 二、函数定义：段落（含嵌套定义）----
段落 测试_段落:
    段落 子函数 接收 参数:
        返回 参数 + 1
    返回 子函数(41)

断言相等(测试_段落(), 42, "段落 词首切分（含嵌套定义）")

# ---- 三、赋值关键字：设/为 ----
段落 测试_赋值:
    设 甲 为 10
    设 乙 为 20
    设 和 为 甲 + 乙
    断言相等(甲, 10, "设 词首切分")
    断言相等(和, 30, "为 词首切分")
    返回 真

# ---- 四、返回 ----
段落 斐波那契 接收 n:
    如果 n <= 1:
        返回 n
    结束
    返回 斐波那契(n - 1) + 斐波那契(n - 2)

段落 测试返回语句:
    返回 斐波那契(5)

断言相等(测试返回语句(), 5, "返回 词首切分")

# ---- 五、尝试/捕获/结束 ----
段落 测试_异常:
    尝试:
        设 结果 为 1 / 0
        返回 结果
    捕获 异常 错误:
        返回 "捕获到异常"

断言相等(测试_异常(), "捕获到异常", "尝试/捕获/结束 词首切分")

# ---- 六、打印 ----
段落 测试_打印:
    打印("打印语句")
    返回 真

# ---- 七、类型/函数 作变量名（词首切分后仍可作标识符）----
段落 测试_类型函数名:
    设 类型 为 "int"
    设 函数 为 "回调"
    断言相等(类型, "int", "类型 词首切分后作变量名")
    断言相等(函数, "回调", "函数 词首切分后作变量名")
    返回 真

# ---- 八、等待（异步段落内）----
异步 段落 假任务:
    返回 "完成"

异步 段落 测试_等待:
    设 结果 为 等待 假任务()
    断言相等(结果, "完成", "等待 词首切分（async）")
    返回 结果

异步 段落 主入口:
    等待 测试_等待()
    返回 真

# ---- 九、导入/导出（文件级关键字，口径钉住）----
# 说明：导入/导出 属文件头部语义，实际用例见 light-merge/examples/test_L* 系列；
#      此处以注释记录口径，避免在单元测试中制造真实模块依赖。

# ---- 十、运算符在词首必须切分（后随空白）----
段落 测试_运算符 接收 甲, 乙:
    设 加结果 为 甲 + 乙
    设 减结果 为 甲 - 乙
    设 乘结果 为 甲 * 乙
    设 除结果 为 甲 / 乙   # 中文除运算符（整数除）
    设 加结果中文 为 甲 加 乙
    设 减结果中文 为 甲 减 乙
    设 乘结果中文 为 甲 乘 乙
    设 除结果中文 为 甲 除 乙
    设 与结果 为 甲 与 乙
    设 或结果 为 甲 或 乙
    设 非结果 为 非 假
    设 相等结果 为 甲 等于 乙
    设 不等结果 为 甲 不等于 乙
    返回 加结果 + 减结果 + 乘结果

断言相等(测试_运算符(10, 3), 50, "运算符词首切分（+ - * / 加 减 乘 除 模 与 或 非 等于 不等于）")

# ---- 十一、值字面量在词首必须切分（后随空白）----
段落 测试_值字面量:
    设 真值 为 真
    设 假值 为 假
    设 空值 为 空
    断言相等(真值, 真, "真 词首切分")
    断言相等(假值, 假, "假 词首切分")
    返回 真

# ---- 十二、多字关键字最长匹配优先于 CS 单字词首并入 ----
段落 测试_多字关键字优先:
    设 段落数 为 5
    断言相等(段落数, 5, "段落数：段落(KEYWORD)+数(IDENTIFIER)，最长匹配优先")
    返回 真

# ---- 十三、主入口 ----
异步 段落 主:
    测试_赋值()
    测试_打印()
    测试_类型函数名()
    测试_运算符(10, 3)
    测试_值字面量()
    测试_多字关键字优先()
    等待 主入口()
    打印("词首并入反向测试全部通过（硬语句/运算符/值字面量/多字关键字优先）")
    返回 真

异步 运行 主()
