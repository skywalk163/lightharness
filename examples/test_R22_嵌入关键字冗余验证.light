# R22 任务1 验证样例：_EMBED_MAX_MATCH_KEYWORDS（为/返回/尝试）嵌入标识符保护
#
# 背景：第21轮收口时曾据「撤掉该集合后 L-084 仍绿」推断其冗余。
#       第22轮任务1 全语料隔离中立验证（770 文件）证明：撤掉该集合后
#       `行为`→`行`+`为`、`返回表`→`返回`+`表`、`尝试记录`→`尝试`+`记录` 等
#       17 个语料文件 / 10 条边界用例 token 序列变化。该集合**非冗余**，必须保留。
#
# 根因：`为`/`返回`/`尝试` 在 _OPERATOR_KEYWORDS 中（赋值/返回/尝试语句关键字），
#       被 R21 上下文敏感切词的「运算符关键字排除」挡在门外；其嵌入长汉字串的场景
#       只能由本集合兜底。本样例锁定这些标识符须整体成词（rc=0）。
段落 主:
    设 行为 为 "行为值"
    设 末位行为 为 "末位行为值"
    设 返回表 为 "返回表值"
    设 尝试记录 为 "尝试记录值"
    设 作为 为 "作为值"
    设 成为 为 "成为值"
    设 认为 为 "认为值"
    设 为了 为 "为了值"
    打印 行为
    打印 末位行为
    打印 返回表
    打印 尝试记录
    打印 作为
    打印 成为
    打印 认为
    打印 为了
