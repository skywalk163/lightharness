# _repro_L120.light —— L-120 最小复现（绕法形态，rc=0）
# 现象：标识符**不恰好等于**关键字，但能被关键字序列「完全切分」时，词法器仍会切开它。
#   实测（被破坏原写法，不参与编译）：
#     `尝试:`
#     `  抛出 新建 错误("boom")`
#     `捕获 异常 错误己:`          ← `错误己` 被切成 `错误`(关键字) + `己`(K_SELF 单字别名)，
#                                   解析报 **「期望'为'或'等于'，但得到「:」」**（L-084 同族）
#   对照组：`捕获 异常 错误甲:` **正常** —— `错误`+`甲`，而 `甲` 不是关键字，
#           整串无法被完全切分，故保留为单一标识符。
#   即判定条件是「整串可被关键字全覆盖切分」，而不是「含有关键字字样」：
#     · `错误甲` / `文本长` / `首条表` / `异常甲` → 不可全切分 → 安全
#     · `错误己`（错误+己） / `异常己`（异+常+己）→ 可全切分 → 被切碎
#   绕法：标识符后缀不用单字别名/关键字（这里改用拉丁字母后缀：错误A/错误B…）。
# 本文件按绕法写，断言改名后 捕获 块正常；被破坏原写法见上方注释。

段落 断言相等(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 新建 错误("断言失败: " + 标签)

段落 主:
  尝试:
    抛出 新建 错误("boom")
  捕获 异常 错误A:
    断言相等(转字符串(错误A), "boom", "捕获变量用 错误A（非全切分）可正常绑定")

  尝试:
    抛出 新建 错误("boom2")
  捕获 异常 错误甲:
    断言相等(转字符串(错误甲), "boom2", "捕获变量用 错误甲（后缀非关键字）可正常绑定")

  打印 "L-120 绕法形态复现通过（rc=0）"

主()
