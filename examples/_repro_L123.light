# _repro_L123.light —— L-123 最小复现（绕法形态，rc=0）
# 现象：`设 字典[键甲][键乙] 为 值` 双层下标赋值**静默失败**——编译产物不含内层键赋值，
#   无任何报错，子字典保持原样（单层下标赋值 `设 字典[键] 为 值` 正常）。
# 绕法：用 `字典设置(子字典, 键, 值)`（先单层取出子字典引用再设置），或先取子字典到
#   局部变量再单层赋值。本文件按绕法写，断言双层赋值绕法后键可读；
#   被破坏原写法见注释（不参与编译，可自行去掉注释验证静默失败）。
# （被破坏原写法：`设 外表["甲"]["乙"] 为 1` —— 静默失败，外表["甲"] 仍为 {}。）

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 主:
  设 外表 为 {}
  设 外表["甲"] 为 {}
  # 绕法：字典设置（子字典引用, 键, 值）
  字典设置(外表["甲"], "乙", 1)
  断言相等(外表["甲"]["乙"], 1, "绕法一：字典设置(子字典, 键, 值)")
  # 绕法二：先取子字典引用再单层赋值
  设 内表 为 外表["甲"]
  设 内表["丙"] 为 2
  断言相等(外表["甲"]["丙"], 2, "绕法二：子字典引用 + 单层赋值")
  # 对照（不参与编译）：
  #   设 外表["甲"]["丁"] 为 3   —— 双层下标赋值静默失败
  #   断言相等(外表["甲"]["丁"], 3, "双层赋值")  —— 会报 键错误
  打印 "L-123 绕法形态复现通过（rc=0）"

主()
