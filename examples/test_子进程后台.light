# test_子进程后台.light —— 后台进程执行测试（深对齐：Popen 句柄 + 超时等待）
# 覆盖：运行后台 返回句柄；等待后台 返回 {码,输出,错误,成功}；超时判定
# 反跑判据：把 输出 断言 done 改成 nope 即红；或把 成功 断言改反即红。

从 子进程 导入 运行后台, 等待后台

段落 断言真 接收 条件, 标签:
  如果 条件 == 假:
    抛出 "断言失败: " + 标签

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  # 正常后台进程：延迟后输出
  设 进程 为 运行后台("python -c \"import time; time.sleep(0.3); print('done')\"")
  设 结果 为 等待后台(进程, 5)
  断言真(结果["成功"] == 真, "后台进程成功")
  断言等于(结果["码"], 0, "退出码 0")
  断言真(结果["输出"].包含("done"), "输出含 done")

  # 超时场景：进程超时被终止，成功=假，码=-1
  设 慢进程 为 运行后台("python -c \"import time; time.sleep(3); print('slow')\"")
  设 超时结果 为 等待后台(慢进程, 1)
  断言真(超时结果["成功"] == 假, "超时判定失败")
  断言等于(超时结果["码"], -1, "超时码 -1")

  打印 "test_子进程后台 PASS"

主()
