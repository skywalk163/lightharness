# test_L085.light —— L-085 最小复现：e 指数浮点字面量
# 缺陷：`1e400` 被解析为标识符 `inf`（NameError: name 'inf' is not defined），
#       根因：词法层 float("1e400") 归一为 ±inf 后，代码生成 str(inf)=='inf' 是未定义名。
# 修复后：1e400 发射 float('inf')，与 Python 原生指数字面量语义一致；常规 1e3/2.5e-2 正常。

段落 主:
    设 x 为 1e400
    断言(x > 1e308, "1e400 应为超出 1e308 的无穷大")
    设 y 为 1e3
    断言(y == 1000, "1e3 应等于 1000")
    设 z 为 2.5e-2
    断言(z == 0.025, "2.5e-2 应等于 0.025")
    设 w 为 1.05e-34
    断言(w > 0 且 w < 1e-33, "1.05e-34 应为 1e-34 量级正数")
    设 n 为 -1e400
    断言(n < 0, "-1e400 应为负无穷")
    打印("test_L085 PASS")
