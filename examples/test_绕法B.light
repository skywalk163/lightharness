# 绕法B：己.X 为 空列表()（函数调用 RHS）
段落 空列表:
  返回 []

类 测试类B:
  属性 标识 等于 空
  属性 事件表 等于 空
  段落 构造 接收 标识:
    己.标识 为 标识
    己.事件表 为 空列表()
  段落 记录 接收 数据:
    己.事件表.追加(数据)
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类B("s1")
  对象.记录("x")
  对象.记录("y")
  打印 "绕法B数量: " 加上 转字符串(对象.数量())
  打印 "标识: " 加上 对象.标识
  打印 "--- 绕法B结束 ---"

主()
