# 缺陷: L-101 —— `回调` 是保留关键字，不能用作变量名
# 当前/修复后: `回调` 被 parser 用于 `外部 回调` FFI 语法，保留为保留字；
#             误用时报明确提示「回调是保留关键字，不能直接作为语句开头」（非静默失败），
#             满足任务3验收"报明确提示"分支。
# 说明: 本轮评估后保留为保留字（移除会破坏 FFI 语法），无代码改动；验收以"明确提示"达成。
段落 主:
    设 回调 为 空
    打印 回调
