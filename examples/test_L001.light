# 缺陷：L-001 跨目录点号导入不可用（点号导入 子目录.模块 的符号 编译期不内联、运行期 No module named）
# 当前：红——「No module named '_import_test.数据'」（即使 _import_test/__init__.py 存在也不解析）
# 修复后：绿——点号导入被解析/内联，造值() 返回 42，断言通过
从 _import_test.数据 导入 造值

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 值 为 造值()
  断言等于(值, 42, "L-001 点号导入后 造值() 应返回 42")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 值 为 造值()
  打印 "--- 测试L001 结束 ---"
  # 修复后启用：断言修复后()

主()
