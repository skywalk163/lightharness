# _repro_L066.light —— 语言缺陷 L-066 复现（原报告形态逐字复刻）
# 缺陷（历史）：函数调用实参为嵌套字面量/嵌套调用时，若前面存在前导语句，
#   报「期望 右花括号 }，但得到 右括号 )」。当前编译器已修复，本文件固化为防回归测试。
从 内置核心转换 导入 转字符串

段落 某函数(表):
  返回 表

段落 断言相等(实际, 期望, 标签):
  如果 (实际 != 期望):
    抛出 新建 错误("断言失败[" + 标签 + "]：实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 触发:
  设 占位 为 1
  断言相等(某函数({"a":[{"b":1}]})["a"][0]["b"], 1, "x")
  写 "--- L066 原报告形态通过（嵌套字面量实参往返=1）---"

触发()
