# _repro_L091.light —— L-091 最小复现（绕法形态，rc=0）
# 现象：匿名段落（段落 接收 x: ...）不能跨行内联在调用实参位置——
#   `应用(段落 接收 x:\n    返回 x + 1, 4)` 解析报错
#   （无法识别的语法元素：')'，期望冒号/缩进级联报错）。
# 绕法：先把匿名段落赋给变量，再作为实参传递（L-078 闭包能力正常）。
# 本文件按绕法写，断言匿名段落赋值后可传递调用；被破坏原写法见注释（不参与编译）。
# （被破坏原写法：`设 值 为 应用(段落 接收 x:` 换行 `返回 x + 1, 4)` —— 解析错误。）

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 应用 接收 函数, 值:
  返回 函数(值)

段落 主:
  # 绕法：先赋值，再传参
  设 加一 为 段落 接收 x:
    返回 x + 1
  断言相等(应用(加一, 4), 5, "匿名段落先赋值再传参")
  # 命名段落同样可传
  段落 乘二 接收 x:
    返回 x * 2
  断言相等(应用(乘二, 4), 8, "命名段落传参")
  打印 "L-091 绕法形态复现通过（rc=0）"

主()
