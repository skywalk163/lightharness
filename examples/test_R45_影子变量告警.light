# test_R45_影子变量告警.light —— L-166「影子变量」编译告警的验收载体
#
# 这个文件本身就是被检查的对象：编译它时编译器应当在 stderr 打出 1 条 L-166 告警
# （『坏_写不回』给模块级的『当前值』赋值却没声明 全局），而『好_写回』声明了
# 全局、不应告警。告警由 tests/test_R45_L166影子变量告警.py 断言。
#
# 运行期自校验部分：证明「没声明 全局 的赋值确实写不回模块级变量」——
# 这正是 L-165 的运行时症状（静默、无报错、症状离现场极远）。
# 反跑判据：把下面的『影子后仍为初值』断言改成『等于 1』即红。

设 当前值 为 0

段落 坏_写不回:
  设 当前值 为 1
  返回 当前值

段落 好_写回:
  全局 当前值
  设 当前值 为 2
  返回 当前值

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 坏返回 为 坏_写不回()
  断言等于(坏返回, 1, "坏_写不回 内部看到的是局部变量 1")

  # 关键：模块级 当前值 没有被写回（影子变量），仍是初值 0
  断言等于(当前值, 0, "影子后仍为初值：模块级 当前值 未被写回")

  设 好返回 为 好_写回()
  断言等于(好返回, 2, "好_写回 返回 2")
  断言等于(当前值, 2, "声明全局后模块级 当前值 被写回 2")

  打印 "test_R45_影子变量告警 PASS"

主()
