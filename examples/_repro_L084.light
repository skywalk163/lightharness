# _repro_L084.light —— L-084 最小复现：赋值关键字「为」切碎尾部中文标识符
# 现象：类属性 `属性 末位行为 等于 ""` 报「意外的标记: 换行」（列落在 "" 后）。
# 词法器实证：`行为` -> IDENTIFIER「行」+ KEYWORD「为」。
# 绕法：标识符避开「为」字（本文件故意不做绕法，保留报错以登记缺陷）。

类 复现类:
  属性 末位行为 等于 ""

段落 主:
  打印 "不应到达此处"
主()
