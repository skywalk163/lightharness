# _repro_L084.light —— L-084 最小复现（修复形态，rc=0）
# 现象：赋值关键字「为」在词法层恒为独立 KEYWORD，即使黏在中文标识符尾部也会被切碎——
#   `行为` -> IDENTIFIER「行」+ KEYWORD「为」；类属性 `属性 末位行为 等于 ""` 报「意外的标记」。
# 绕法：裸标识符避开「为」字（`末位行为` -> `末支`）。本文件按绕法写，断言类属性可正常定义与读取，
#   证明修复形态可用；被切碎的原写法见注释（不参与编译）。
# （被切碎原写法：`属性 末位行为 等于 ""` —— 词法器会切成 `末位行` + `为`，报解析错误。）

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

类 复现类:
  属性 末支 等于 ""

段落 主:
  设 实例 为 复现类()
  断言相等(实例.末支, "", "L-084 绕法：避开「为」字的标识符可正常定义与读取")
  打印 "L-084 修复形态复现通过（rc=0；绕法=裸标识符避开「为」字）"

主()
