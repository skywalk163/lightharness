# 缺陷复现套件 CI 汇总：逐条打印 L-* 复现状态（供 CI/人工按行核对）
# 状态取值：红=当前应红且已复现；绿=当前已缓解转绿；工具=工程层缺陷需文本替换脚本演示
# 每个用例文件头部有三行注释（缺陷/当前/修复后），本清单与其保持一致。
段落 主:
  打印 "L-001 红 test_L001.light 跨目录点号导入不可用"
  打印 "L-002 红 test_L002.light 多行字面量续行语法错误"
  打印 "L-003 绿 test_L003.light 成员空列表赋值已缓解转绿"
  打印 "L-004 红 test_L004.light 关键字边界拆分成员名（跨模块形态）"
  打印 "L-005 红 test_L005.light 非 self 成员下标被编译成调用"
  打印 "L-006 红 test_L006.light 闭包修改外层标量 UnboundLocalError"
  打印 "L-007 绿 test_L007.light 成员名拆分状态性已缓解转绿"
  打印 "L-007b 绿 test_L007b.light 跨模块局部变量污染已缓解转绿"
  打印 "L-007c 绿 test_L007c.light 属性名前缀重叠已缓解转绿"
  打印 "L-008 红 test_L008.light 序列化 非全局内置"
  打印 "L-009 工具 test_L009.light 注释内嵌导入字样被文本替换误伤"
  打印 "L-010 红 test_L010.light 返回码 成员访问被拆"
  打印 "L-011 红 test_L011.light 等待进程 成员访问被拆"
  打印 "L-012 红 test_L012.light 终端.终端会话 跨模块成员访问被拆"
  打印 "L-013 绿 test_L013.light 单行for循环已缓解转绿(编译器侧闭合)"
  打印 "L-014 红 test_L014.light 语句级下标调用 FunctionCallExpr"
  打印 "L-015 红 test_L015.light 模块名含关键字词根 from 解析失败"
  打印 "L-016 红 test_L016.light CancelledError 无法被捕获接住"
  打印 "--- 测试L缺陷清单 结束 ---"

主()
