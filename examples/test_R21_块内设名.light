# test_R21_块内设名.light —— 第21轮 任务4：块内 设 关键字前缀名 + 块外使用

段落 断言相等 接收 实际, 期望, 标签:
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 断言真 接收 条件, 标签:
    如果 条件 != 真:
        抛出 新建 错误("断言真失败: " + 标签)

段落 主:
    如果 真:
        设 函数值 为 1
        设 返回值 为 2
    遍历 项 之 [1, 2]:
        设 尝试结果 为 项 * 10
    当 假:
        设 永不到达 为 1
    断言相等(函数值, 1, "if块内设名")
    断言相等(返回值, 2, "if块内设名二")
    断言相等(尝试结果, 20, "遍历块内设名")
    尝试:
        设 定义变量 为 5
    捕获 异常 块内异常:
        抛出 新建 错误("不应进入捕获")
    断言相等(定义变量, 5, "尝试块内设名")
    打印 "test_R21_块内设名 PASS"

主()
