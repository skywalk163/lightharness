# 绕法C：构造有参数 + 设 己.事件表 为 []
类 测试类C:
  属性 标识 等于 空
  属性 事件表 等于 空
  段落 构造 接收 标识:
    己.标识 为 标识
    设 己.事件表 为 []
  段落 记录 接收 数据:
    己.事件表.追加(数据)
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类C("s1")
  对象.记录("x")
  打印 "绕法C数量: " 加上 转字符串(对象.数量())
  打印 "--- 绕法C结束 ---"

主()
