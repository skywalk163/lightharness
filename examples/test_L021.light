# 缺陷：L-021 「属性」全局保留关键字，普通代码块中不能作为变量名/表达式
#        （上下文感知缺失）。
#        `属性` 在类定义体内是关键字（`属性 名 等于 值`），但在函数体/模块级
#        等普通代码块中应允许作为变量名。当前 `设 属性 为 X` 赋值可通过，
#        但 `打印 属性` / `返回 属性` / `设 X 为 属性` 等表达式位置报
#        「属性」是保留关键字，不能直接作为语句开头。
# 修复前：红——下列用 `属性` 作变量名的场景编译失败。
# 修复后：绿——类体内 `属性` 仍是关键字（属性声明语法不变），普通代码块中
#        `属性` 是普通标识符，可赋值、读取、作为参数和返回值。
# 载体：自带最小用例，不依赖其它模块（最小复现可独立运行）。
#
# 防过度修复护栏：本文件中 `类 测试类` 内仍用 `属性 名称 等于 "test"` 作
#        类体属性声明；若修复误把类体内 `属性` 也降级为标识符，属性声明
#        语法将失效，编译报错或运行期属性缺失。

段落 取属性值 接收 输入:
  设 属性 为 输入
  返回 属性

段落 主:
  # 场景1：模块级变量名 `属性`
  设 属性 为 "module_level"
  断言等于(属性, "module_level", "L-021 模块级变量名 属性")

  # 场景2：函数内变量名 `属性`（赋值+读取+返回）
  断言等于(取属性值(42), 42, "L-021 函数内变量名 属性（赋值+返回）")

  # 场景3：`属性` 作为函数参数名
  段落 接收属性 接收 属性:
    返回 属性
  断言等于(接收属性("param"), "param", "L-021 函数参数名 属性")

  # 场景4：`属性` 作为右值表达式
  设 源属性 为 "right_value"
  设 目标 为 源属性
  断言等于(目标, "right_value", "L-021 属性名作右值表达式")

  # 场景5：类体内 `属性` 仍是关键字（防过度修复护栏）
  设 实例 为 新建 测试类()
  断言等于(实例.名称, "test", "L-021 类体内 属性 关键字声明不变")
  断言等于(实例.获取名(), "test", "L-021 类方法内访问属性")

  # 场景6：类方法内用 `属性` 作局部变量名
  断言等于(实例.方法内属性变量(), "local_in_method", "L-021 类方法内局部变量名 属性")

  打印 "--- 测试L021 通过 ---"

类 测试类:
  属性 名称 等于 "test"

  段落 获取名:
    返回 己.名称

  段落 方法内属性变量:
    设 属性 为 "local_in_method"
    返回 属性

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
