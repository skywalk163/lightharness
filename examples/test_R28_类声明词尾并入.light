# test_R28_类声明词尾并入.light —— 第28轮 任务4：类的类声明/词首并入/继承/使用处
#
# 任务书 4.2：
#   · 类声明关键字：`类 名称:` → 类为KEYWORD，名称为IDENTIFIER
#   · 类声明词尾并入：`类 独立类:` → 独立类整词成IDENTIFIER
#   · 类声明继承：`类 子类 继承 基类:` → 子类/基类整词成IDENTIFIER
#   · 类词首并入：`类别 为 "int"` → 类别整词成IDENTIFIER
#   · 使用处：`新建 独立类()` → 独立类整词（test_L013 打红形态钉住）
# 全部 rc=0 自校验。

段落 断言相等(实际, 期望, 标签):
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))


# ---- 一、词首并入：类别/类似 整词 ----

设 类别 为 "int"
设 类似 为 真
断言相等(类别, "int", "类：类别 词首并入整词")
断言相等(类似, 真, "类：类似 词首并入整词")

# ---- 二、类声明：独立类/测试类/有界队列类 词尾并入整词 ----

类 独立类:
    段落 取值:
        返回 42

类 测试类:
    段落 取值:
        返回 7

类 有界队列类:
    段落 取值:
        返回 99

# ---- 三、独立关键字：类 名称: 中类必须为KEYWORD ----

类 名称:
    段落 自报:
        返回 "名称类"

# ---- 四、类声明继承：子类/基类 整词 ----

类 基础类:
    段落 标识:
        返回 "base"

类 子类 继承 基础类:
    段落 标识:
        返回 "sub"

# ---- 五、使用处：新建 独立类() 词尾+`(` 整词（test_L013 打红形态）----

段落 主:
    设 实例一 为 新建 独立类()
    断言相等(实例一.取值(), 42, "类：新建 独立类() 整词（打红形态钉住）")

    设 实例二 为 新建 测试类()
    断言相等(实例二.取值(), 7, "类：新建 测试类() 整词")

    设 实例三 为 新建 有界队列类()
    断言相等(实例三.取值(), 99, "类：新建 有界队列类() 整词")

    设 命名实例 为 新建 名称()
    断言相等(命名实例.自报(), "名称类", "类：类 名称: 独立关键字形态正常")

    设 子实例 为 新建 子类()
    断言相等(子实例.标识(), "sub", "类：继承后子类方法正常")

    设 基实例 为 新建 基础类()
    断言相等(基实例.标识(), "base", "类：基类整词与实例化正常")

    打印("R28 类声明词尾并入测试全部通过（声明/关键字/继承/使用处）")
    返回 真

主()
