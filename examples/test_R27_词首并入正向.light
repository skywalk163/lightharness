# test_R27_词首并入正向.light —— 第27轮 任务4：CS表16字词首并入正向覆盖（运行期自校验）
#
# 任务书要求：对CS表16字（R27 任务3 后残部 列/类 仍在 CS；A类 8 字由
# `_P0A_HEAD_MERGE_DUAL` 词首/词尾正面规则覆盖、B类 6 字由 HM+L-119 等价覆盖），
# 每字构造词首并入场景，断言整体成 IDENTIFIER，不被切成关键字+余部。
# 口径：
#   · A类（乘减到加模真空除）：词首+后随汉字→并入（乘法/真空/到位…）；
#     独立/后随空白仍切分（反向文件覆盖）。
#   · B类（列对是段的自配 + 残部 列类）：词首并入由 `_P0A_HEAD_MERGE_SINGLE`
#     正面类别覆盖（列数/对立/是否/段落名/的确/类别/自己/配置）。
# 全部 rc=0 自校验。

段落 断言相等(实际, 期望, 标签):
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))


# ---- 一、A类 DUAL 8 字 × 2 名 ----

设 乘法 为 7
设 乘积 为 14
断言相等(乘法, 7, "乘：乘法")
断言相等(乘积, 14, "乘：乘积")

设 减法 为 3
设 减速 为 1
断言相等(减法, 3, "减：减法")
断言相等(减速, 1, "减：减速")

设 加法 为 8
设 加数 为 5
断言相等(加法, 8, "加：加法")
断言相等(加数, 5, "加：加数")

设 除法 为 2
设 除非名 为 0
断言相等(除法, 2, "除：除法")
断言相等(除非名, 0, "除：除非名")

设 模组 为 5
设 模型名 为 "m"
断言相等(模组, 5, "模：模组")
断言相等(模型名, "m", "模：模型名")

设 真空 为 1
设 真实值 为 2
断言相等(真空, 1, "真：真空")
断言相等(真实值, 2, "真：真实值")

设 空格 为 " "
设 空白页 为 2
断言相等(空格, " ", "空：空格")
断言相等(空白页, 2, "空：空白页")

设 到位 为 3
设 到达 为 "终点"
断言相等(到位, 3, "到：到位")
断言相等(到达, "终点", "到：到达")

# ---- 二、B类 6 字 × 2 名（R27 已撤出 CS，由 HM+L-119 等价覆盖）----

设 列数 为 4
设 列表项 为 1
断言相等(列数, 4, "列：列数")
断言相等(列表项, 1, "列：列表项")

设 对立 为 5
设 对象名 为 "o"
断言相等(对立, 5, "对：对立")
断言相等(对象名, "o", "对：对象名")

设 是否 为 6
设 是非 为 0
断言相等(是否, 6, "是：是否")
断言相等(是非, 0, "是：是非")

设 段落名 为 7
设 段首 为 9
断言相等(段落名, 7, "段：段落名")
断言相等(段首, 9, "段：段首")

设 的确 为 8
设 的确率 为 0.5
断言相等(的确, 8, "的：的确")
断言相等(的确率, 0.5, "的：的确率")

设 自己 为 10
设 自然数 为 11
断言相等(自己, 10, "自：自己")
断言相等(自然数, 11, "自：自然数")

设 配置 为 12
设 配对 为 13
断言相等(配置, 12, "配：配置")
断言相等(配对, 13, "配：配对")

# ---- 三、CS 残部 2 字（列/类 仍在 CS，词首并入由 CS 锚保证）----

设 序列号 为 15
设 分类号 为 16
断言相等(序列号, 15, "列：序列号（CS 残部）")
断言相等(分类号, 16, "类：分类号（CS 残部）")

# ---- 四、词首并入名可作运算参与方 / 列表元素 / 函数参数 ----

段落 组合(加数2, 乘数):
    返回 加数2 * 乘数

设 组合结果 为 组合(加数, 乘法)
断言相等(组合结果, 35, "词首并入名可作参数")

设 结果列表 为 []
结果列表.追加(乘法)
结果列表.追加(配置)
断言相等(长度(结果列表), 2, "词首并入名可作列表元素")

段落 主:
    打印("R27 词首并入正向测试全部通过（16字 × 2名 = 32 标识符）")
    返回 真

主()
