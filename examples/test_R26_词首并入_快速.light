# test_R26_词首并入_快速.light —— 第26轮 任务1/2：词首并入正面规则（运行期自校验）
#
# R26 把「单字关键字在【词首】并入标识符」从 CS 表逐词登记改为**正面规则**：
#     单字关键字 ∧ ∈ _P0A_HEAD_MERGE_SINGLE（= R25 词尾类别 F − 单字语句关键字）
#     ∧ 位于词首（scan_pos==0）∧ 后随汉字 ⇒ 并入标识符，不切出词首 KEYWORD。
# 有效类别 = CS 锚 ∪ 词首正面类别；CS 已按三重判据从 30 精简到 18
#（移除 12 字 余 例 出 则 常 引 接 末 试 跳 长 首，其词首并入改由正面类别提供）。
#
# 本用例覆盖：
#   一、正面类别 22 字的词首并入（每字一个词首复合名，整词成 IDENTIFIER 且可读写）；
#   二、CS 保留的运算符/值字面量/范围字词首并入（加法/减法/乘法/除法/模组/真空/到位）；
#   三、反向：单字语句关键字在词首必须切分（己/父/当/从/设/返回/类/如果/打印/加/空）；
# 全部 rc=0 自校验。

段落 断言相等(实际, 期望, 标签):
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 断言真(条件, 标签):
    如果 条件 != 真:
        抛出 新建 错误("断言真失败: " + 标签)


# ---- 一、正面类别 22 字：词首单字关键字后随汉字 → 整词成标识符 ----
设 余数 为 1
断言相等(余数, 1, "余：余数 词首并入")

设 例表 为 2
断言相等(例表, 2, "例：例表 词首并入")

设 出错 为 3
断言相等(出错, 3, "出：出错 词首并入")

设 列数 为 4
断言相等(列数, 4, "列：列数 词首并入")

设 则例 为 5
断言相等(则例, 5, "则：则例 词首并入")

设 对立 为 6
断言相等(对立, 6, "对：对立 词首并入")

设 常规 为 7
断言相等(常规, 7, "常：常规 词首并入")

设 引导 为 8
断言相等(引导, 8, "引：引导 词首并入")

设 接续 为 9
断言相等(接续, 9, "接：接续 词首并入")

设 断裂 为 10
断言相等(断裂, 10, "断：断裂 词首并入")

设 是否 为 真
断言相等(是否, 真, "是：是否 词首并入")

设 末项 为 11
断言相等(末项, 11, "末：末项 词首并入")

设 段数 为 12
断言相等(段数, 12, "段：段数 词首并入")

设 的确 为 13
断言相等(的确, 13, "的：的确 词首并入")

设 类别 为 14
断言相等(类别, 14, "类：类别 词首并入")

设 自然 为 15
断言相等(自然, 15, "自：自然 词首并入")

设 试探 为 16
断言相等(试探, 16, "试：试探 词首并入")

设 跳表 为 17
断言相等(跳表, 17, "跳：跳表 词首并入")

设 过载 为 18
断言相等(过载, 18, "过：过载 词首并入")

设 配置 为 19
断言相等(配置, 19, "配：配置 词首并入")

设 长度 为 20
断言相等(长度, 20, "长：长度 词首并入")

设 首项 为 21
断言相等(首项, 21, "首：首项 词首并入")

断言相等(余数 + 例表 + 出错 + 列数 + 则例 + 对立 + 常规 + 引导 + 接续 + 断裂, 55, "正面类别 前10字 求和")
断言相等(末项 + 段数 + 的确 + 类别 + 自然 + 试探 + 跳表 + 过载 + 配置 + 长度 + 首项, 176, "正面类别 后11字 求和")


# ---- 二、CS 保留的运算符/值字面量/范围字词首并入（DUAL 8）----
设 加法 为 30
断言相等(加法, 30, "加：加法 词首并入")
设 减法 为 31
断言相等(减法, 31, "减：减法 词首并入")
设 乘法 为 32
断言相等(乘法, 32, "乘：乘法 词首并入")
设 除法 为 33
断言相等(除法, 33, "除：除法 词首并入")
设 模组 为 34
断言相等(模组, 34, "模：模组 词首并入")
设 真空 为 35
断言相等(真空, 35, "真：真空 词首并入")
设 到位 为 36
断言相等(到位, 36, "到：到位 词首并入")


# ---- 三、反向：单字语句关键字在词首必须切分（不得并入）----
# 3.1 己 / 父：自身/父类语句关键字（无空格写法）
段落 取己名(己名参数):
    设 己成员 为 己名参数
    返回 己成员
断言相等(取己名(7), 7, "己：己成员 作为标识符前缀可读写")

# 3.2 设 / 为：赋值语句一字不差
设 目标 为 41
设 杂项 为 42
断言相等(目标 + 杂项, 83, "设/为 赋值语句切分正确")

# 3.3 类：类声明关键字词首切分（类名以 类 为前缀时仍整体成标识符）
设 类别名 为 "cls"
断言相等(类别名, "cls", "类：类别名 整词（类 后随汉字并入）")

# 3.4 加/减/乘 作为独立运算符必须切分
设 甲oper 为 5
设 乙oper 为 3
断言相等(甲oper + 乙oper, 8, "加：算术运算切分正确")
断言相等(甲oper - 乙oper, 2, "减：算术运算切分正确")
断言相等(甲oper * 乙oper, 15, "乘：算术运算切分正确")

# 3.5 值字面量真/假/空 独立成词
设 标记 为 真
断言相等(标记, 真, "真：值字面量独立成词")
设 空值 为 空
断言相等(空值 == 空, 真, "空：值字面量独立成词")

# 3.6 控制流关键字（无空格写法）
设 分支值 为 ""
如果 1 == 1 那么:
    设 分支值 为 "若路"
否则:
    设 分支值 为 "否路"
断言相等(分支值, "若路", "如果/那么/否则 控制流切分正确")

# 3.7 返回 / 打印：语句关键字词首切分
段落 取数(参数):
    返回 参数 + 1
断言相等(取数(41), 42, "返回 语句关键字切分正确")

打印 "test_R26_词首并入_快速 全部通过"
