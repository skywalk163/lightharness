# test_L036.light —— L-036 回归：标准库一等 取可选(字典, 键[, 缺省]) 助手
# 运行：python 运行.py examples/test_L036.light
#
# 缺陷 L-036：光明 `d[缺失键]` 直接抛 键错误，而原版 TS/JS 取缺失属性得 undefined（即 空）。
# 复刻「可选字段缺省为 空」语义必须守卫。本包在标准库提供一等 取可选 助手作为官方入口：
#   - 缺省默认 空
#   - 不改变 d[缺失键] 抛 键错误 的语义（语言层决策，超出本包）
#
# 三态：存在键取值 / 缺键返回缺省（默认 空）/ 自定义缺省生效
# 反向断言：d[缺失键] 仍抛 键错误（语义未改）

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 序列化JSON(实际) + " 期望=" + 序列化JSON(期望))

段落 断言真 接收 条件, 标签:
  如果 条件 == 假:
    抛出 新建 错误("断言失败: " + 标签)

段落 主:
  # 1. 存在键：返回值（不改语义）
  设 字典1 为 {"a": 1, "b": "二", "c": 空}
  断言相等(取可选(字典1, "a"), 1, "取可选 存在键返回原值")
  断言相等(取可选(字典1, "b"), "二", "取可选 存在键返回字符串")
  断言相等(取可选(字典1, "c"), 空, "取可选 存在键返回空值本身")

  # 2. 缺键：默认缺省 = 空 (None)
  断言相等(取可选(字典1, "不存在"), 空, "取可选 缺键默认空")
  断言真(取可选(字典1, "x") == 空, "取可选 缺键默认空2")

  # 3. 自定义缺省生效
  断言相等(取可选(字典1, "不存在", "后备"), "后备", "取可选 自定义缺省字符串")
  断言相等(取可选(字典1, "不存在", 0), 0, "取可选 自定义缺省数字")
  断言相等(取可选(字典1, "不存在", {"默认": 真}), {"默认": 真}, "取可选 自定义缺省对象")

  # 反向断言：d[缺失键] 仍抛 键错误（语言层语义不变，对照 JS undefined）
  设 抛了 为 假
  设 错误消息 为 ""
  尝试:
    设 会崩 为 字典1["缺失键"]
  捕获 错误 为 e:
    设 抛了 为 真
    设 错误消息 为 转字符串(e)
  断言真(抛了, "反向断言：d[缺失键] 仍抛 键错误（语义未改，对照 JS 返回 undefined/空）")
  断言真(字符串长度(错误消息) > 0, "反向断言：抛出的错误应带消息，实际='" + 错误消息 + "'")

  打印 "--- 测试L036 通过 ---"

主()
