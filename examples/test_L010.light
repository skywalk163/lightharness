# 缺陷：L-010 字段首字符为关键字（结果.返回码 被拆成 返回+码，成员访问报无属性）
# 当前：红——「'命令结果' object has no attribute '返回'」（返回码 被关键字边界拆分）
# 修复后：绿——返回码 整体成词可读取，断言通过
从 外部命令 导入 命令结果

段落 主:
  设 结果 为 新建 命令结果(0)
  打印 转字符串(结果.返回码)
  打印 "--- 测试L010 结束 ---"
  # 修复后启用：断言修复后()

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 结果 为 新建 命令结果(0)
  断言等于(结果.返回码, 0, "L-010 返回码 应整体成词并取值 0")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
