# test_R30_应当除非通用化_快速.light —— R30 任务2 快速验证
#
# 规则：词首前缀类 `_P0A_HEAD_MERGE_PREFIX = {位, 应, 除}`。
#   · 应当：关键字「当」在词中/词首会劈开 —— 以「应」为词首的前缀类整体并入。
#   · 除非：关键字「除」(DUAL/算术) + 「非」(逻辑) 双侧夹击 —— 以「除」为词首
#     的前缀类整体并入（调用语境 / 非语句起始位置）。
#   见 lexer.py L2390。
#
# 正向：应当 / 除非 作为函数名可定义、可调用（若被劈开为 应+当 / 除+非
#       则会直接语法错误 ⇒ rc≠0）。
# 反向：当 仍为循环关键字、非 仍为逻辑关键字、除 仍为算术/异常子句关键字。
#
# 运行：cd light-merge && python cli/light.py run ../lightharness/examples/test_R30_应当除非通用化_快速.light

段落 断言相等(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

# 以「应」「除」+ 关键字 开头命名的函数：词法若劈开则此定义处直接报错
段落 应当(x):
  返回 x

段落 除非(x):
  返回 x

段落 主:
  # 正向 1：调用语境整词成 IDENTIFIER
  设 a 为 应当(1)
  设 b 为 除非(2)
  断言相等(a, 1, "应当 整词可调用")
  断言相等(b, 2, "除非 整词可调用")

  # 正向 2：如果条件位（非语句起始）
  如果 除非(真) 那么:
    打印 "除非 在条件位整词成标识符"

  # 反向 1：当 仍为循环关键字（循环可正常运行）
  设 累计 为 0
  设 序号 为 0
  当 序号 < 3:
    设 累计 为 累计 + 1
    设 序号 为 序号 + 1
  断言相等(累计, 3, "当 仍为循环关键字")

  # 反向 2：非 仍为逻辑关键字
  设 翻转 为 非 真
  断言相等(翻转, 假, "非 仍为逻辑关键字")

  打印 "--- test_R30 应当除非通用化 通过 ---"

主()
