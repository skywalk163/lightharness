# test_R21_表达式上下文.light —— 第21轮 任务4：表达式上下文关键字前缀标识符
# 口径：定义登记后（模块级段落/设定义）的表达式调用；任务2 落地后孤立形态亦应整体成词。

段落 断言相等(实际, 期望, 标签):
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 断言真(条件, 标签):
    如果 条件 != 真:
        抛出 新建 错误("断言真失败: " + 标签)

段落 去重占位(输入):
    返回 输入 + 1

段落 作用域匹配(输入):
    返回 输入 + 2

段落 断言为真(输入):
    返回 输入 + 3

段落 判定为空(输入):
    返回 输入 + 4

段落 排序函数(输入):
    返回 输入 + 5

段落 主:
    设 结果甲 为 去重占位(41)
    断言相等(结果甲, 42, "去重占位表达式调用")
    断言相等(作用域匹配(1), 3, "赋值右侧调用")
    断言相等(断言为真(1), 4, "返回值位置")
    断言相等(判定为空(排序函数(1)), 10, "嵌套表达式")
    设 映射 为 {"读取器": 1, "写入器": 2}
    断言相等(映射["读取器"], 1, "字典键")
    设 列表项 为 [去重占位(0), 作用域匹配(0)]
    断言相等(列表项[0], 1, "列表元素一")
    断言相等(列表项[1], 2, "列表元素二")
    设 运算结果 为 去重占位(1) + 作用域匹配(1)
    断言相等(运算结果, 5, "运算符操作数")
    打印 "test_R21_表达式上下文 PASS"

主()
