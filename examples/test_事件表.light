类 测试类2:
  属性 事件表 等于 空
  段落 构造:
    己.事件表 为 []
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类2()
  打印 "事件表长度: " 加上 转字符串(对象.数量())
  对象.事件表.追加("x")
  打印 "追加后: " 加上 转字符串(对象.数量())
  打印 "--- 测试2结束 ---"

主()
