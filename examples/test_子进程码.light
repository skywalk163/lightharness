# test_子进程码.light —— 退出码精确化测试（T10-06）
# 覆盖验收 §5.1：exit(3)→3、exit(0)→0；反跑判据：把 码==3 改成 码==0 即红。

从 子进程 导入 运行进程

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  # 注：-c 的参数必须加引号。运行进程走 shell=True，POSIX sh 把未加引号的 ( )
  # 当元字符 → "Syntax error: ( unexpected"，sh 返回 2，而不是 python 的退出码 3。
  # cmd.exe 不把 ( 当元字符，故 Windows 上不加引号也能过——这正是本例只在 FreeBSD 红的原因。
  设 结果3 为 运行进程("python -c \"exit(3)\"", 空)
  断言等于(结果3["码"], 3, "exit(3) 退出码应为 3")
  断言等于(结果3["成功"], 假, "exit(3) 应非成功")

  设 结果0 为 运行进程("python -c \"exit(0)\"", 空)
  断言等于(结果0["码"], 0, "exit(0) 退出码应为 0")
  断言等于(结果0["成功"], 真, "exit(0) 应成功")

  打印 "test_子进程码 PASS"

主()