# R76 任务B - G-14 一等枚举 `枚举 名：成员1, 成员2, …`
# 编译为 `class 名(enum.Enum)` + 成员 `= enum.auto()`，天然获得四项语义：
#   取值 `颜色.红` / 构造 `颜色(2)`（按值反查）/ 相等（成员同一性）/ 哈希（可作字典键）
# 与「记录」（dataclass）的关系：记录=可变数据载体，枚举=封闭取值域，语义正交。
# 运行：python 运行.py examples/test_R76_G14_枚举.light   （期望 rc=0）

枚举 颜色：红, 绿, 蓝
枚举 状态：开, 关

段落 断言真(条件, 标签):
  如果 条件 != 真:
    抛出 "断言失败: " + 标签

段落 主:
  # 1. 取值 + 相等（同一性）
  断言真(颜色.红 == 颜色.红, "同成员相等")
  断言真(颜色.红 != 颜色.绿, "异成员不等")

  # 2. 构造：按值反查（enum.auto 按声明序编号 1,2,3）
  设 甲 为 颜色(2)
  断言真(甲 == 颜色.绿, "按值 2 构造得绿")

  # 3. 哈希：可作字典键
  设 表 为 {}
  表[颜色.红] 等于 "R"
  表[颜色.绿] 等于 "G"
  断言真(表[颜色.红] == "R", "枚举作字典键(红)")
  断言真(表[颜色.绿] == "G", "枚举作字典键(绿)")

  # 4. 不同枚举的同名成员互不相等
  断言真(颜色.红 != 状态.开, "跨枚举成员隔离")

  # 5. 取值打印
  打印 颜色.红

  打印 "--- G-14 一等枚举（构造/取值/相等/哈希）全部通过 ---"

主()
