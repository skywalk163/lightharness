# test_L119.light —— L-119 最小复现：L0单字别名「配」误用报错误导
# 缺陷：配 是「匹配」的 L0 单字别名保留字，`打印 配[0]` 词法把 配 判为关键字，
#       本行被解析成「匹配 [0]:」语句 → codegen 产出 match 空体 → 运行期报误导性缩进错误。
# 当前（修复前）：立红（rc≠0）
# 修复后：配 作为变量名正常使用，输出 1

设 配 为 [1, 2]
打印 配[0]
打印 "test_L119 PASS"
