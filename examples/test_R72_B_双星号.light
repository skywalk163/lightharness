# test_R72_B_双星号.light —— 反向哨兵（预期 rc!=0）
# R72 任务B（G-05）：一个解构目标表里最多只能有一个「*」星号 rest 目标
# （Python 同样只允许 `首, *余 = …` 这种单星形态）。
# 期望：编译期报「一个解构目标表里最多只能出现一个「*」星号解构」。
# 若未来解析器静默接受该写法（例如只取最后一个 *），本用例 rc 会变 0 →
# tests/test_回归.py 的 EXPECT_RED「断言 rc!=0」会失败，逼你复核词法/解析器。

段落 主:
  设 甲, *乙, *丙 为 [1, 2, 3]
  打印 转字符串(甲)
