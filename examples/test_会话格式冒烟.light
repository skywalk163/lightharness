# test_会话格式冒烟.light —— 语法与基本路径冒烟
从 会话格式 导入 造头, 头版本号, 版本文本, 当前目录, 目录恢复, 迁移会话, 标准链, 校验事件载荷, 已知事件类型, 造事件, 记录系统提示, 投影节点0, 记录助手轮, 投影内嵌消息, 投影节点0
从 会话 导入 新建会话
从 JSON 导入 解析JSON, 序列化JSON

段落 断言相等(实际, 期望, 标签):
  如果 (实际 != 期望):
    抛出 新建 错误("断言失败[" + 标签 + "]：实际=" + 序列化JSON(实际) + " 期望=" + 序列化JSON(期望))

段落 主程:
  设 头 为 造头(3, 0, "s1", 1000, ".", 空, 空, 假)
  断言相等(头版本号(头), "3.0", "头版本")
  断言相等(已知事件类型("user/message"), 真, "已知事件")
  设 占 为 校验事件载荷("user/message", ["消息": ["content": []]], 真)
  设 会 为 新建会话("s1", ".")
  设 占 为 记录系统提示(会, "你是助手", "append")
  设 占 为 记录助手轮(会, "m1", [], [["type": "text", "text": "hi"]])
  设 节点 为 投影节点0(会.事件表)
  断言相等(节点, "你是助手", "节点0投影")
  设 全 为 会.事件表
  设 消息 为 投影内嵌消息(全[2])
  断言相等(消息["content"][0]["text"], "hi", "内嵌流投影")
  写 "会话格式冒烟: 通过"
  返回 真

主程()
