# 缺陷：L-040 函数定义参数规范写法
#        函数定义参数必须用 `段落 NAME 接收 参数1, 参数2:` 形式。
#        `段落 NAME(PARAMS):`（括号包裹形参）属可选增强，本包以文档化的
#        `接收` 写法为准，不扩展括号形参语法。
# 载体：复用自复刻宿主 src/工作流错误.light 的 `接收` 规范写法，
#       自带最小用例，不依赖其它模块，可独立运行。

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

# 多参数 + 返回运算：规范 `接收` 写法
段落 求和 接收 甲, 乙, 丙:
  返回 甲 加 乙 加 丙

段落 主:
  # 场景1：`接收` 多参数定位调用
  断言等于(求和(1, 2, 3), 6, "L-040 接收 多参数求和")

  # 场景2：参数作为函数内局部变量
  段落 镜像 接收 值:
    返回 值
  断言等于(镜像("ok"), "ok", "L-040 接收 单参数透传")

  # 场景3：参数参与条件判断
  段落 判定符号 接收 数:
    如果 数 大于 0:
      返回 "正"
    否则:
      返回 "非正"
  断言等于(判定符号(5), "正", "L-040 接收 参数条件判断")

  打印 "--- 测试L040 通过 ---"

主()