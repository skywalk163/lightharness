# L-007b 辅助模块：定义 会话对象（危险成员名集合）
# 供 test_L007b.light 验证「调用方模块局部变量名是否污染跨模块成员访问」。
类 会话对象:
  属性 头信息 等于 空
  属性 轮次计数 等于 空
  属性 事件表 等于 空
  段落 构造:
    设 己.头信息 为 "首"
    设 己.轮次计数 为 0
    设 己.事件表 为 []
  段落 记录事件 接收 数据:
    己.事件表.追加(数据)
    返回 己.头信息
  段落 计数:
    返回 己.轮次计数
