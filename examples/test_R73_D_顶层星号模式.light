# test_R73_D_顶层星号模式.light —— R73 任务 D 反向哨兵（G-11）
#
# 任务书：docs/功能对标/光明语言语法缺陷清零_R71-R73三轮并发开发任务书.md §五 D
# 语义：rest 模式 `*余` 只允许出现在列表/元组模式的内部（`情况 [首, *余]:`），
#       顶层直接写 `情况 *余:` 应在编译期报明确中文错误 rc!=0。
#       若未来 parser 静默接受或误发非法 Python（case *rest），rc 会变 0 →
#       本 EXPECT_RED 断言失败，逼你复核 _parse_match_pattern 的 rest 分支。
# 登记：tests/test_回归.py EXPECT_RED['test_R73_D_顶层星号模式.light']
# 错误预期：「rest 模式 '*' 只能出现在列表 '[]' 或元组 '()' 模式内部」

段落 主:
  匹配 [1, 2, 3]:
    情况 *余: 打印 "不应到达"
    其他: 打印 "不匹配"