# _repro_L086.light —— L-086 最小复现（绕法形态，rc=0）
# 现象：光明字符串把 `{1,4}` 当重复语法——`"[0-9a-f]{1,4}"` 被编译破坏
#   （SyntaxWarning: 'int' object is not callable），正则无法用花括号量词。
# 绕法：等价无花括号量词 `[0-9a-f][0-9a-f]?[0-9a-f]?[0-9a-f]?`。本文件按绕法写，
#   断言 IPv6 十六进制段（1~4 位）可匹配；被破坏原写法见注释（不参与编译）。
# （被破坏原写法：`完全匹配("[0-9a-f]{1,4}", 段)` —— 花括号量词触发编译破坏。）

从 正则表达式 导入 完全匹配

段落 断言相等(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

段落 主:
  设 模式 为 "[0-9a-f][0-9a-f]?[0-9a-f]?[0-9a-f]?"
  断言相等(完全匹配(模式, "abcd"), 真, "L-086 绕法：4 位十六进制段可匹配")
  断言相等(完全匹配(模式, "a"), 真, "L-086 绕法：1 位十六进制段可匹配")
  断言相等(完全匹配(模式, "abcde"), 假, "L-086 绕法：5 位段应拒绝")
  打印 "L-086 绕法形态复现通过（rc=0；绕法=无花括号量词等价写法）"

主()
