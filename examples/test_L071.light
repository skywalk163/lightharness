# test_L071.light —— D批次验收：字典获取 安全原语（L-071）
# 缺陷：字典键访问无通用 .get() 安全原语。
# 验收结果：已修复——字典获取 已是内置原语（missing 键返回默认值，不抛异常）。

设 字典对象 为 {"甲": 1}
如果 字典获取(字典对象, "甲", 0) != 1:
    抛出 新建 错误("L-071 验收失败: 字典获取 存在键")
如果 字典获取(字典对象, "乙", 0) != 0:
    抛出 新建 错误("L-071 验收失败: 字典获取 缺失键应返回默认值")
打印 "test_L071 PASS"
