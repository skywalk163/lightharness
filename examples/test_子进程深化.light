# test_子进程深化.light —— 子进程 v2：运行并取行 + 批量运行
从 子进程 导入 运行进程, 运行并取行, 批量运行

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  # ===== 运行并取行 =====
  打印 "=== 运行并取行测试 ==="
  # echo 多行输出
  设 行表 为 运行并取行("echo line1&&echo line2&&echo line3", 10)
  断言等于(长度(行表), 3, "3行输出")
  断言等于(行表[0], "line1", "第1行")
  断言等于(行表[1], "line2", "第2行")
  断言等于(行表[2], "line3", "第3行")

  # 失败命令返回空表
  设 失败行 为 运行并取行("nonexistent_command_xyz_123", 5)
  断言等于(长度(失败行), 0, "失败命令返回空表")

  # ===== 批量运行 =====
  打印 "=== 批量运行测试 ==="
  设 命令表 为 ["echo batch1", "echo batch2", "exit 3"]
  设 批量结果 为 批量运行(命令表, 10)
  断言等于(长度(批量结果), 3, "批量运行3条")
  断言等于(批量结果[0]["成功"], 真, "第1条成功")
  断言等于(批量结果[1]["成功"], 真, "第2条成功")
  断言等于(批量结果[2]["成功"], 假, "第3条失败(exit3)")
  断言等于(批量结果[2]["码"], 3, "第3条退出码3")

  打印 "--- 子进程深化测试通过 ---"

主()
