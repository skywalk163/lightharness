# _repro_L085.light —— L-085 最小复现（绕法形态，rc=0）
# 现象：`e` 指数浮点字面量不被支持——`1e400` 被解析为标识符 `inf`
#   （NameError: name 'inf' is not defined），`1e3` 类写法同样不可靠。
# 绕法：`解析JSON("1e400")`（JSON 数字指数路径正常）。本文件按绕法写，
#   断言指数数值可正确解析为浮点；被破坏原写法见注释（不参与编译）。
# （被破坏原写法：`设 x 为 1e400` —— 解析为标识符 inf，NameError。）

从 JSON 导入 解析JSON

段落 断言相等(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

段落 主:
  设 大数 为 解析JSON("1e400")
  设 小数 为 解析JSON("1e3")
  断言相等(小数, 1000, "L-085 绕法：1e3 经 JSON 解析为 1000")
  如果 大数 <= 小数:
    抛出 "L-085 绕法：1e400 应远大于 1e3"
  打印 "L-085 绕法形态复现通过（rc=0；绕法=解析JSON(\"1e400\")，指数数值走 JSON 解析路径）"

主()
