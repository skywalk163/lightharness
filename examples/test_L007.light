# 缺陷：L-007 成员名拆分决策与符号表状态相关（部分/原因/类型 词根、方法内 temp 名与成员名差异可致 设 己.X 为 … 拆成 self.部分.表）
# 当前：已缓解转绿——预扫描已收集类属性与成员赋值目标，设 己.部分表 为 {} 按 self.部分表={} 编译（本用例可正常运行）
# 修复后：绿（即当前行为）——部分表 为真实字典，登记 生效，长度断言通过
类 测试类7:
  属性 部分表 等于 空
  段落 构造:
    设 己.部分表 为 {}
  段落 登记 接收 键, 值:
    己.部分表[键] 为 值
  段落 数量:
    返回 长度(己.部分表)

段落 主:
  设 对象 为 新建 测试类7()
  对象.登记("a", 1)
  打印 转字符串(对象.数量())
  打印 "--- 测试L007 结束 ---"
  # 修复后启用：断言修复后()

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 对象 为 新建 测试类7()
  对象.登记("a", 1)
  对象.登记("b", 2)
  断言等于(对象.数量(), 2, "L-007 部分表 登记两次后数量应为 2")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
