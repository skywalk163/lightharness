# R28 任务2 快速验证 —— 类的类声明上下文词尾并入
#
# 规则：类 在 user_definitions 前缀匹配路径中，余部为单字（列/类 等 HM∪DUAL 字）
#       时并入前缀 → 类名整词成 IDENTIFIER（如 `类 独立类:` 的 独立类）。
#       - 类的独立关键字（类 名称:）不受影响；
#       - 类的词首并入（类别/类型）不受影响。
# token 级断言：见 lightharness/_antirun_r28_t3_CS表清零验证.py（G3）
#
# 运行：cd light-merge && python cli/light.py run ../lightharness/examples/test_R28_类声明词尾并入_快速.light

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

类 独立类:
  段落 取值:
    返回 42

类 有界队列类:
  段落 容量:
    返回 7

段落 主:
  设 类别 为 "int"
  设 甲 为 新建 独立类()
  设 乙 为 新建 有界队列类()
  断言等于(类别, "int", "类别 词首并入成标识符")
  断言等于(甲.取值(), 42, "独立类 类声明后正常实例化")
  断言等于(乙.容量(), 7, "有界队列类 类声明后正常实例化")
  打印 "--- test_R28 类声明词尾并入 通过 ---"

主()
