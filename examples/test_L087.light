# 缺陷: L-087 —— `当前目录` 是编译器内置映射名，无条件发射为 当前目录() 调用，遮蔽用户变量
# 当前: 修复前 `设 当前目录 为 "/tmp"` 后 `打印 当前目录` 仍调用 os.getcwd()，输出真实目录而非 /tmp
# 修复后: 用户变量遮蔽内置映射，输出 /tmp（既有 _shadows_builtin 逻辑已覆盖；本轮确认绿，纳入反跑）
段落 主:
    设 当前目录 为 "/tmp"
    断言 当前目录 == "/tmp"，"当前目录应被用户变量遮蔽为 /tmp"
    打印 当前目录
