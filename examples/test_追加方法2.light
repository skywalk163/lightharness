类 测试类5:
  属性 标识 等于 空
  属性 事件表 等于 空
  段落 构造 接收 标识:
    己.标识 为 标识
    己.事件表 为 []
  段落 记录 接收 类型, 数据:
    设 事件 为 ["类型": 类型, "数据": 数据]
    己.事件表.追加(事件)
    返回 事件
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类5("s1")
  打印 "初始数量: " 加上 转字符串(对象.数量())
  对象.记录("user/message", "你好")
  打印 "记录后数量: " 加上 转字符串(对象.数量())
  打印 "--- 测试5（方法名改记录，仍调.追加）结束 ---"

主()
