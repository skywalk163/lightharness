# 缺陷: L-137 —— 变量名以`尝试`开头被断开（回归保护用例，本轮前已绿）
# 说明: 既有复合词硬化已覆盖`尝试`黏连场景，本轮新增嵌入最大匹配分支统一口径防回归。
# 当前: 修复前即绿（`尝试记录`作为整体标识符），本轮后保持绿。
# 修复后: `设 尝试记录 为 {}` + `尝试记录["甲"] 为 1` 正常，打印 1
段落 主:
    设 尝试记录 为 {}
    尝试记录["甲"] 为 1
    打印 尝试记录["甲"]
