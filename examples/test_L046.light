# test_L046.light —— L-046 最小复现：单字「出」不能作变量名/形参名
# 缺陷：词法器把单字 `出` 恒发 KEYWORD（导出 的 L0 别名），导致
#       `设 出 为 "x"` / `段落 乙 接收 出:` / `打印(出)` 全部解析失败。
# 修复后：`出` 仅在语句起始位置是导出语句关键字；其余位置（设目标/形参/
#         表达式/下标）是普通标识符。词内 `出`（输出/溢出/抛出）不受影响。

# 语句起始的 `出` 仍是导出语句（导出别名语义保持不变）
出 乙

段落 乙 接收 出:
    返回 出

段落 主:
    # 1. 单字 出 作变量名（设目标 + 表达式引用）
    设 出 为 "x"
    断言(出 == "x", "单字 出 应可作变量名")
    打印(出)

    # 2. 单字 出 作形参名
    断言(乙("y") == "y", "单字 出 应可作形参名")

    # 3. 单字 出 作列表变量 + 下标访问
    设 出 为 [1, 2, 3]
    断言(出[0] == 1 且 出[2] == 3, "变量 出 应支持下标访问")

    # 4. 词内 出（溢出）不受影响，保持整体标识符
    设 溢出 为 "词内出"
    断言(溢出 == "词内出", "词内 出（溢出）应保持整体")

    # 5. 成员访问中的 出（设 己.出 不在本次范围，验证字典键读法）
    设 表 为 {"出": 1}
    断言(表["出"] == 1, "字符串键 出 不受影响")

    打印("test_L046 PASS")
