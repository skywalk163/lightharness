# _repro_L083.light —— L-083 最小复现（修复形态，rc=0）
# 光明无深拷贝/冻结内置原语；消息构造按引用组装 content → 调用方改入参污染已构造消息。
# 上游 createXxxMessage 经 deep-freeze 脱钩；光明绕法：src/消息.light 造Xxx消息 统一用
# 拷贝内容块表(内容)（每块字典逐键拷贝）。本文件断言「修复形态」下调用方改入参不影响已构造消息。
从 消息 导入 造文本块, 造用户消息

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

段落 主:
  设 入参 为 [造文本块("原")]
  设 消息 为 造用户消息(入参)
  # 调用方后续改入参（修复前会污染已构造消息）
  列表追加(入参, 造文本块("污染"))
  入参[0]["text"] 为 "改了"
  断言相等(长度(消息["content"]), 1, "L-083 脱钩：调用方追加不影响已构造消息")
  断言相等(消息["content"][0]["text"], "原", "L-083 脱钩：调用方改块字段不影响已构造消息")
  打印 "L-083 修复形态复现通过（rc=0）"

主()
