# _repro_L090.light —— L-090 最小复现（绕法形态，rc=0）
# 现象：字符串原语裸名（`转小写/转大写/去除空白/截取/开头/结尾/查找子串/码位/
#   字符串包含/列表包含`）依赖编译器内置映射自动 `_light_builtin.` 前缀；被导入模块
#   内部再 import .light 字符串模块（字符串工具轻量/字符串处理）时，在运行.py
#   内联路径下产生名称解析错乱（实测 'int' object is not callable）。
# 绕法：字符串/列表原语一律用编译器内置映射裸名（不 import 字符串工具轻量/字符串处理）。
#   本文件按绕法写，断言裸名原语可用；被破坏原写法见注释（不参与编译）。
# （被破坏原写法：模块内 `从 字符串处理 导入 转小写` 后再用裸名 `转小写(...)` ——
#   名称解析错乱，'int' object is not callable。）

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

段落 主:
  设 文本 为 "  AbC dEf  "
  设 小写 为 转小写(文本)
  设 紧凑 为 去除空白(文本)
  断言相等(小写, "  abc def  ", "L-090 绕法：转小写 裸名可用")
  断言相等(紧凑, "AbC dEf", "L-090 绕法：去除空白 裸名可用")
  打印 "L-090 绕法形态复现通过（rc=0；绕法=字符串原语用编译器内置映射裸名，不 import 字符串 .light 模块）"

主()
