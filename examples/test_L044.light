# test_L044.light —— L-044 回归：文本(变量/索引) 类型转内置
# 运行：python 运行.py examples/test_L044.light
#
# 缺陷：`文本(数)` / `文本(表[1])`（变量/索引实参）被误编译为裸 `文本` 标识符，
#       运行期 name '文本' is not defined；`文本` 作变量/形参名也会冲突。
# 修复：与 `字符串`/`整数` 同表映射到 str()，字面量/变量/索引实参行为一致。

设 数 为 42
设 表 为 [1, 2, 3]

# 场景1：变量实参
设 a 为 文本(数)
如果 a != "42": 抛出 新建 错误("变量实参失败: " + a)

# 场景2：索引实参
设 b 为 文本(表[1])
如果 b != "2": 抛出 新建 错误("索引实参失败: " + b)

# 场景3：字面量实参（与变量/索引同路径）
设 c 为 文本(123)
如果 c != "123": 抛出 新建 错误("字面量实参失败: " + c)

# 场景4：`文本` 作变量名不受影响（遮蔽回归，_shadows_builtin 压过内置映射）
设 文本 为 "我是变量"
如果 文本 != "我是变量": 抛出 新建 错误("文本 变量被遮蔽破坏")
写 文本
写 a
写 b
写 c

写 "--- 测试L044 通过 ---"
