# test_L097.light —— L-097 最小复现：if块内嵌套段落提升错误
# 缺陷：如果块内定义的段落被编译器提升到模块级，导致其引用的
#       函数局部变量脱离闭包作用域，运行期 name '局部' is not defined。
# 当前（修复前）：立红
# 修复后：块内段落正确嵌套在块作用域内，闭包语义保持

段落 测试:
    设 局部 为 5
    如果 真:
        段落 内部函数():
            返回 局部
        打印 内部函数()
    打印 "块内完成"

测试()
打印 "test_L097 PASS"
