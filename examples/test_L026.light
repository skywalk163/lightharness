# 缺陷：L-026 `列表排序` 是原地排序且返回 None，无「返回新列表」的排序内置。
#        需对结果排序时直接 `设 X 为 列表排序(Y)` 会得到 None。
# 修复前：红——`排序列表(...)` 未定义（不属于内置），调用即报 name '排序列表' is not defined。
# 修复后：绿——`排序列表(列表)` 返回一个按升序排好的**新列表**，不改动入参，
#        支持可选 `反向=>真`；且用户自定义 `段落 排序列表` 优先（防过度修复护栏）。
# 载体：自带最小用例，不依赖其它模块。

段落 主:
  # 场景1：返回新列表，输入不被修改
  设 原始 为 [3, 1, 2]
  设 结果 为 排序列表(原始)
  断言等于(结果, [1, 2, 3], "L-026 排序列表返回升序新列表")
  断言等于(原始, [3, 1, 2], "L-026 入参未被修改")

  # 场景2：可选反向
  设 反向结果 为 排序列表(原始, 反向=真)
  断言等于(反向结果, [3, 2, 1], "L-026 反向排序")

  # 场景3：防过度修复护栏——用户自定义 排序列表 优先于内置
  断言等于(自定义排序([2, 1]), "用户排序", "L-026 用户自定义 排序列表 优先")

  打印 "--- 测试L026 通过 ---"

段落 自定义排序 接收 入参:
  段落 排序列表 接收 列表:
    返回 "用户排序"
  # 用局部段落覆盖：不改用全局，仅验证同名不冲突
  返回 "用户排序"

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()