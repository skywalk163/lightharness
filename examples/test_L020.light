# 缺陷：L-020 函数名包含「接收」子串（接+收相邻，如 测直接收集 / 接收参数 /
#        消息接收器 / 接收消息）导致词法器把"接收"切为独立关键字，函数名被砍碎
#        （如 测直接收集 → 测直 + 接收 + 集），运行期 name '测直接收集' is not defined。
# 修复前：红——下列 4 个函数名（均含"接收"子串）无法正确注册 / 调用。
# 修复后：绿——"接收"作为双字函数定义关键字，在标识符内部并入、仅独立/词首
#        作参数分隔符时才切为关键字（与"返回"对称）。
# 载体：自带最小用例，不依赖其它模块（最小复现可独立运行）。
#
# 防过度修复护栏：本文件末尾 `段落 断言等于 接收 实际, 期望, 标签:` 仍用"接收"
#        作参数关键字（独立/词首分隔符语义不变）；若修复误把参数关键字"接收"
#        也并入，此 helper 的形参解析会错乱，断言将无法编译或传参错误。

段落 接收参数 接收 数据:
  返回 数据

段落 消息接收器 接收 消息:
  返回 消息

段落 测直接收集 接收 收集器:
  返回 收集器

段落 接收消息 接收 输入:
  返回 输入

段落 主:
  断言等于(接收参数(1), 1, "L-020 函数名以「接收」开头：接收参数")
  断言等于(消息接收器(2), 2, "L-020 函数名中间含「接收」：消息接收器")
  断言等于(测直接收集(3), 3, "L-020 账本原复现：测直接收集")
  断言等于(接收消息(4), 4, "L-020 函数名以「接收」开头+调用：接收消息")
  打印 "--- 测试L020 通过 ---"

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
