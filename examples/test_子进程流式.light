从 子进程 导入 运行并流式输出, 运行进程

段落 主:
  # 测试1：流式输出多行，处理器逐行调用
  设 收集表 为 []
  段落 行处理器1 接收 行:
    收集表.追加(行)
  设 码1 为 运行并流式输出("echo line1 & echo line2 & echo line3", 行处理器1, 30)
  断言 码1 == 0, "退出码应为0"
  断言 长度(收集表) >= 3, "处理器应至少调用3次"
  打印("测试1通过：流式输出多行，处理器调用" + 转字符串(长度(收集表)) + "次")

  # 测试2：处理器为空时不崩溃
  设 码2 为 运行并流式输出("echo hello", 空, 30)
  断言 码2 == 0, "处理器为空时退出码应为0"
  打印("测试2通过：处理器为空时不崩溃")

  # 测试3：输出空内容时不崩溃
  设 收集表3 为 []
  段落 行处理器3 接收 行:
    收集表3.追加(行)
  设 码3 为 运行并流式输出("cd .", 行处理器3, 30)
  断言 码3 == 0, "空输出退出码应为0"
  打印("测试3通过：空输出时不崩溃")

  # 测试4：返回退出码正确（失败命令）
  设 收集表4 为 []
  段落 行处理器4 接收 行:
    收集表4.追加(行)
  设 码4 为 运行并流式输出("nonexistent_command_xyz", 行处理器4, 30)
  断言 码4 != 0, "失败命令退出码应非0"
  打印("测试4通过：失败命令退出码非0（码=" + 转字符串(码4) + "）")

  打印("--- 子进程流式输出v2测试通过 ---")

主()
