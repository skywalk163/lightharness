类 测试类:
  属性 表 等于 空
  段落 构造:
    己.表 为 []
  段落 空表:
    己.表 为 []
    返回 长度(己.表)
  段落 非空表:
    己.表 为 [1, 2]
    返回 长度(己.表)

段落 主:
  设 对象 为 新建 测试类()
  打印 "构造后长度: " 加上 转字符串(长度(对象.表))
  对象.表.追加("x")
  打印 "追加后长度: " 加上 转字符串(长度(对象.表))
  打印 "--- 测试结束 ---"

主()
