# _repro_L143.light —— L-143 最小复现（绕法形态，rc=0）
# 现象：`作用域` 是保留关键字——不能作为变量名：
#   `设 作用域 为 {}` 报「作用域是保留关键字，不能直接作为语句开头」；
#   `作用域[键] 为 值` 同样报错（L-119/L-120 关键字字样同族）。
# 绕法：变量名改 域标记（避开「作用域」关键字字样）。
# 本文件按绕法写，断言改名后形状字典可正常构造与读取；被破坏原写法见注释（不参与编译）。
# （被破坏原写法：`设 作用域 为 {}` 换行 `作用域["类型"] 为 "agent"` —— 解析错误。）

段落 断言相等(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 主:
  # 绕法：变量名 域标记
  设 域标记 为 {}
  域标记["类型"] 为 "agent"
  域标记["目标ID"] 为 "agent-1"
  断言相等(域标记["类型"], "agent", "改名后形状构造")
  断言相等(域标记["目标ID"], "agent-1", "改名后形状读取")
  # 对照（不参与编译）：
  #   设 作用域 为 {}           —— 解析错误
  #   作用域["类型"] 为 "agent"  —— 解析错误
  打印 "L-143 绕法形态复现通过（rc=0）"

主()
