# _repro_L081.light —— 最小复现：光明字典/列表字面量不支持尾随逗号
#
# L-081 缺陷表述：光明字典/列表字面量**不支持尾随逗号**（`[..., "y": 2,]` 或
# `[1, 2,]` 均报「意外的标记: ]」，报错定位在 `]` 而非逗号，误导排查）。
# 模块级多行字典字面量本身可用（无需函数包装）；真正不可写的是「末尾条目后的逗号」。
# 失败形态（取消下面两行注释即复现，rc=1）：
#   设 乙 为 [
#     "x": 1, "y": 2,
#   ]
# 本文件以「修复形态」落地（rc=0），保证进 CI 门禁（EXPECT_RED 必须为空）；
# 失败形态经路M 实测：段落内/模块级尾逗号均报「意外的标记: ]」。

段落 主:
  设 甲 为 [
    "x": 1,
    "y": 2
  ]
  写 "复现L-081（修复形态 rc=0）：无尾逗号可解析，甲y=" + 转字符串(甲["y"])

主()
