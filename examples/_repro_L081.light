# _repro_L081.light —— 最小复现：光明字典/列表字面量不支持尾随逗号（已修复）
#
# L-081 修复前：光明字典/列表字面量**不支持尾随逗号**（`[..., "y": 2,]` 或
# `[1, 2,]` 均报「意外的标记: ]」，报错定位在 `]` 而非逗号，误导排查）。
# 修复（light-merge/src/parser_expr.py _parse_list_literal 方括号字典分支）：
# 尾随逗号（"," 后直接 "]"）与 Python/JS 一致放行。
# 本文件以「修复形态」落地（rc=0）：带尾随逗号的字典/列表均正常解析。

段落 主:
  设 甲 为 [
    "x": 1,
    "y": 2,
  ]
  设 乙 为 [1, 2, 3,]
  写 "复现L-081（修复形态 rc=0）：尾随逗号可解析，甲y=" + 转字符串(甲["y"]) + " 乙末=" + 转字符串(乙[2])

主()
