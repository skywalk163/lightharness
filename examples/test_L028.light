# 缺陷：L-028 自动内置集不完整/不一致：不存在 `整数(x)` 强转，判型须用 `是整数(x)`，
#        且形似内置名被劈成「自动内置」与「须导入」两类，难以记忆。
# 本用例聚焦补齐 `整数(x)` 强转内置（与 `是整数` 判型对称）。
# 修复前：红——`整数(...)` 未定义，调用即报 name '整数' is not defined。
# 修复后：绿——`整数(...)` 作为强转内置可用：字符串→整数、浮点→截断整数、负数串正确。
# 载体：自带最小用例，不依赖其它模块。

段落 主:
  # 场景1：字符串 → 整数
  断言等于(整数("42"), 42, "L-028 整数(字符串)→整数")

  # 场景2：浮点 → 截断整数
  断言等于(整数(3.9), 3, "L-028 整数(浮点)→截断整数")

  # 场景3：负数字符串
  断言等于(整数("-5"), -5, "L-028 整数(负数串)→负数")

  # 场景4：与判型 `是整数` 对称（不破坏既有的判型内置）
  断言等于(是整数(整数("7")), True, "L-028 整数强转结果可被 是整数 判型")

  打印 "--- 测试L028 通过 ---"

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()