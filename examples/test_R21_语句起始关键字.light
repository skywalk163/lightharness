# test_R21_语句起始关键字.light —— 第21轮 任务4：语句起始位置关键字确保不受影响

段落 断言相等 接收 实际, 期望, 标签:
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 断言真 接收 条件, 标签:
    如果 条件 != 真:
        抛出 新建 错误("断言真失败: " + 标签)

段落 主:
    设 条件 为 真
    如果 条件 为 真:
        断言真(真, "如果语句")
    否则:
        断言真(假, "否则语句")
    如果 1 == 2:
        断言真(假, "不可达二")
    否则 如果 2 == 2:
        断言真(真, "否则若语句")
    遍历 元素 之 [1, 2]:
        断言真(元素 > 0, "遍历语句")
    当 条件:
        设 条件 为 假
    尝试:
        抛出 新建 错误("探测")
    捕获 异常 错误信息:
        断言真(转字符串(错误信息) != "", "捕获语句")
    段落 返回值测试 接收:
        返回 真
    断言真(返回值测试(), "返回语句")
    设 变量 为 "值占位"
    断言相等(变量, "值占位", "设语句")
    打印 "语句起始OK"
    断言真(真, "打印语句后继续")
    打印 "test_R21_语句起始关键字 PASS"

主()
