# 测试：局部变量改名「事件记录」后，事件表 是否不再被拆
段落 造事件 接收 类型:
  设 事件记录 为 ["类型": 类型]
  返回 事件记录

类 测试类E:
  属性 标识 等于 空
  属性 事件表 等于 空
  段落 构造 接收 标识:
    己.标识 为 标识
    设 己.事件表 为 []
  段落 记录 接收 数据:
    己.事件表.追加(数据)
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类E("s1")
  对象.记录("x")
  打印 "测试E数量: " 加上 转字符串(对象.数量())
  打印 "--- 测试E结束 ---"

主()
