# test_R85_E_附件归一.light —— R85 任务E 附件域：请求投影几何 + ImageRequestTarget
从 附件准入 导入 绳请求投影尺寸, 绳请求长边尺寸, 绳校验请求目标

段落 断言相等(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 断言包含(实际, 子串, 标签):
  如果 字符串包含(转字符串(实际), 子串) == 假:
    抛出 新建 错误("断言失败: " + 标签 + " 期望含 " + 子串)

段落 主:
  # 投影：1000x500, maxPixels=200000 → 632x316
  设 投 为 绳请求投影尺寸(1000, 500, 200000)
  断言相等(投["width"], 632, "投影宽")
  断言相等(投["height"], 316, "投影高")
  # 已小于预算 → 不变
  设 投小 为 绳请求投影尺寸(100, 50, 200000)
  断言相等(投小["width"], 100, "小数不变宽")
  断言相等(投小["height"], 50, "小数不变高")
  # 长边：800x600, longEdge=400 → 400x300
  设 长 为 绳请求长边尺寸(800, 600, 400)
  断言相等(长["width"], 400, "长边宽")
  断言相等(长["height"], 300, "长边高")
  # 长边超过源 → 不变
  设 长二 为 绳请求长边尺寸(800, 600, 2000)
  断言相等(长二["width"], 800, "长边超限宽")
  断言相等(长二["height"], 600, "长边超限高")
  # 校验目标
  如果 绳校验请求目标(["width": 10, "height": 10, "maxBytes": 100]) != 真:
    抛出 新建 错误("目标合法应过")
  尝试:
    绳校验请求目标(["width": 0, "height": 10, "maxBytes": 100])
    抛出 新建 错误("NO_THROW")
  捕获 异常 为 e:
    断言包含(e, "为正", "宽非正")

主()
写 "test_R85_E_附件归一 PASS"
