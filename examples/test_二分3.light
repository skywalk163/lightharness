# 二分法3：加模块级投影消息/是否表面事件
段落 是否表面事件 接收 类型:
  返回 类型 == "user/message"

段落 投影消息 接收 事件项:
  设 类型 为 事件项["类型"]
  返回 事件项["数据"]

类 测试类H:
  属性 事件表 等于 空
  段落 构造:
    设 己.事件表 为 []
  段落 记录事件 接收 类型:
    设 事件记录 为 ["类型": 类型]
    己.事件表.追加(事件记录)
    返回 事件记录
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类H()
  对象.记录事件("turn/start")
  打印 "H数量: " 加上 转字符串(对象.数量())
  打印 "--- 测试H结束 ---"

主()
