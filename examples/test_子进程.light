# test_子进程.light —— 子进程执行测试（P1）
# 反跑判据：把 输出 断言 "hi" 改成 "nope" 即红。

从 子进程 导入 运行进程, 进程成功

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 结果 为 运行进程("echo hi", 空)
  断言等于(结果["成功"], 真, "echo 应成功")
  断言等于(结果["输出"].去除空白(), "hi", "标准输出应为 hi")

  设 结果2 为 运行进程("echo bye", 空)
  断言等于(结果2["输出"].去除空白(), "bye", "第二次输出应为 bye")

  断言等于(进程成功("echo ok", 空), 真, "进程成功应返回真")

  打印 "test_子进程 PASS"

主()
