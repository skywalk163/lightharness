# 缺陷: L-092 —— 标识符含`返回`关键字子串（回归保护用例，本轮前已绿）
# 说明: 既有复合词硬化（_COMPOUND_SAFE / 嵌入关键字扫描）已覆盖`返回`黏连场景，
#       本轮新增的嵌入最大匹配分支把它与 L-084 一并纳入统一口径，防止回归。
# 当前: 修复前即绿（`返回值`作参数名整体识别），本轮后保持绿。
# 修复后: `段落 测试 接收 返回值:` 中 返回值 作为整体标识符参数，正常返回并打印 1
段落 测试 接收 返回值:
    返回 返回值
段落 主:
    打印 测试(1)
