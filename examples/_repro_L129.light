# _repro_L129.light —— L-129 最小复现（绕法形态，rc=0）
#
# 缺陷（两面同根：光明沿用 Python 语义，JS 语义不同）：
#   ① 条件真值：空列表 / 空字典**判假**（JS `Boolean([])` / `Boolean({})` 均为真）；
#      内建 `布尔(空字典)` 亦返回 False。
#   ② 相等语义：`0 == 假` 为真、`1 == 真` 为真（JS `0 === false` 为假、`true === 1` 为假），
#      故不能用 `== 真/假` 判布尔，必须用类型判别（`type(值) == bool`）。
# 影响面：上游 packages/preset/agent-presets/src/composition-inventory.ts 的 `Boolean(value)`
#   （组合行 `disabled` 节点的真值判定）直译到光明会静默翻转 [] / {} 两种取值。
# 绕法：显式实现 JS 真值表（布尔），布尔判别用 type(值) == bool。
#
# 运行：cd lightharness && python 运行.py examples/_repro_L129.light   → 期望 rc=0

# JS `Boolean(value)` 真值表
段落 布尔(值):
  如果 值 == 空:
    返回 假
  如果 type(值) == bool:
    返回 值
  如果 是整数(值):
    返回 值 != 0
  如果 是浮点(值):
    返回 值 != 0
  如果 是字符串(值):
    返回 字符串长度(值) != 0
  返回 真

段落 主:
  设 空表 为 []
  设 空字典 为 {}
  设 原生空表判真 为 假
  如果 空表:
    设 原生空表判真 为 真
  如果 原生空表判真 == 真:
    抛出 新建 错误("L-129 前提失效：空列表已被判真")
  设 原生空字典判真 为 假
  如果 空字典:
    设 原生空字典判真 为 真
  如果 原生空字典判真 == 真:
    抛出 新建 错误("L-129 前提失效：空字典已被判真")
  如果 0 == 假:
    设 混同成立 为 真
  否则:
    抛出 新建 错误("L-129 前提失效：0 与 假 不再相等")
  如果 布尔([]) != 真:
    抛出 新建 错误("绕法失效：布尔(空列表) 应为真")
  如果 布尔({}) != 真:
    抛出 新建 错误("绕法失效：布尔(空字典) 应为真")
  如果 布尔(0) != 假:
    抛出 新建 错误("绕法失效：布尔(0) 应为假")
  如果 布尔("") != 假:
    抛出 新建 错误("绕法失效：布尔(空串) 应为假")
  打印 "L-129 复现通过：空列表/空字典原生判假（JS 判真）、0==假 为真；绕法 布尔 复刻 JS 真值表"
