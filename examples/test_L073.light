# test_L073.light —— D批次验收：运行时错误翻译（L-073）
# 缺陷：运行时 AttributeError/KeyError 未翻译为光明层可读提示。
# 验收结果：已修复——键错误顶层输出翻译为「键错误: 字典缺少键 '乙'（文件:行）」+ 光明建议。
# 验证方式：①不捕获时顶层输出光明层错误提示（含文件名/行号/建议）；②异常可被捕获不崩溃。
# 注意：「捕获 异常 变量:」绑定变量语法（L-088）尚不支持，本用例使用「捕获 错误:」不绑定变量。

段落 主:
  设 字典对象 为 {"甲": 1}
  设 已捕获 为 假
  尝试:
    打印 字典对象["乙"]
  捕获 错误:
    设 已捕获 为 真
  如果 已捕获 != 真:
    抛出 新建 错误("L-073 验收失败: 键错误未被捕获")
  打印 "键错误已捕获，异常不崩溃"
  打印 "test_L073 PASS"

主()
