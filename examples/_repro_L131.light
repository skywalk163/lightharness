# _repro_L131.light —— 第16轮 任务1 语言缺陷复现（rc=0）
# L-131：明亮除法「x / 0」抛 除零错误，而非产生 ±Infinity / NaN。
# 上游 deepseek-harness 为 TS/JS：1/0 === Infinity、0/0 === NaN（可被 isFinite/isNaN 判定）。
# 复刻任何含「可能除零」的数值逻辑时，Light 直接抛错，需调用点 尝试/捕获 兜底，
# 无法用值语义表达「无穷 / 非数」。

段落 主:
  设 整数除零抛了 为 假
  设 浮点除零抛了 为 假
  尝试:
    设 甲 为 1 / 0
  捕获 异常 为 e:
    设 整数除零抛了 为 真
  尝试:
    设 乙 为 1.0 / 0.0
  捕获 异常 为 e:
    设 浮点除零抛了 为 真
  如果 整数除零抛了 != 真:
    抛出 新建 错误("L-131 前提失效：整数 1/0 应抛 除零错误（JS 为 Infinity）")
  如果 浮点除零抛了 != 真:
    抛出 新建 错误("L-131 前提失效：浮点 1.0/0.0 应抛 除零错误（JS 为 Infinity）")
  打印 "L-131 复现通过：明亮除法 x/0 抛 除零错误（JS 为 Infinity/NaN），复刻数值逻辑须 尝试/捕获 兜底"
