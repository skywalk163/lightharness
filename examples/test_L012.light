# 缺陷：L-012 模块名与类名共享「首字符为关键字」的同一前缀，跨模块 `新建 终端.终端会话(...)` 被拆成 `终端().终(端会话(...))`（L-004/L-010 组合新实例）
# 当前：红——「module '终端' is not callable」（`终端.终端会话` 整段被关键字边界拆分）
# 修复后：绿——`新建 终端.终端会话(...)` 按整段标识符解析，正常构造会话（未启动态），断言通过
导入 终端

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 会话 为 新建 终端.终端会话("python", [], 空, 空, 空)
  设 状态表 为 会话.状态()
  断言等于(状态表["状态"], "未启动", "L-012 修复后 终端.终端会话 应能直接构造")
  会话.关闭()

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 会话 为 新建 终端.终端会话("python", [], 空, 空, 空)
  打印 "--- 测试L012 结束 ---"
  # 修复后启用：断言修复后()

主()
