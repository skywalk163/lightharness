类 测试类4:
  属性 标识 等于 空
  属性 事件表 等于 空
  属性 下一序号 等于 空
  段落 构造 接收 标识:
    己.标识 为 标识
    己.事件表 为 []
    己.下一序号 为 0
  段落 追加 接收 类型, 数据:
    设 事件 为 ["类型": 类型, "序号": 己.下一序号, "数据": 数据]
    己.事件表.追加(事件)
    己.下一序号 为 己.下一序号 + 1
    返回 事件
  段落 事件数:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类4("s1")
  打印 "初始事件数: " 加上 转字符串(对象.事件数())
  对象.追加("user/message", "你好")
  对象.追加("assistant/message", "你好呀")
  打印 "追加后事件数: " 加上 转字符串(对象.事件数())
  打印 "事件1: " 加上 对象.事件表[1]["数据"]
  打印 "--- 测试4结束 ---"

主()
