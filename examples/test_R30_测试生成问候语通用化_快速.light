# test_R30_测试生成问候语通用化_快速.light —— 第30轮 任务4：测试_生成问候语 通用化快速验证
#
# 任务书 4.1 要求：`返回 测试_生成问候语(1)` → 测试_生成问候语 整词成 IDENTIFIER。
# 本文件覆盖三类形态，全部 rc=0 自校验：
#   ① 段落声明形态（预扫描注册完整名 → 已整词，CCW 无关）
#   ② 函数调用形态（`测试_生成问候语(1)` —— 方案A ASCII下划线+Han 粘连通用规则救回）
#   ③ 设 为 赋值形态（`设 问候语 为 测试_生成问候语()` —— 非语句起始位置）
# 若方案A 失效劈成 测试_+生成问候语，②③ 在编译期将报 NameError（生成问候语
# 非保留关键字、无此用户定义）→ 运行便失败，本文件即红。

段落 测试_生成问候语:
    返回 1

# ② 函数调用形态（任务书快速验证的同一形态：表达式起始位置）
设 数值 为 测试_生成问候语()

# ③ 断言调用（实参位置）
段落 断言相等 接收 实际, 期望, 标签:
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签)

断言相等(测试_生成问候语(), 1, "测试_生成问候语 调用整词（实参位置）")
断言相等(数值, 1, "测试_生成问候语 调用整词（赋值形态）")

段落 主:
    打印("R30 测试_生成问候语 通用化快速验证通过（CCW 已清空，方案A 整词并入）")
    返回 真

主()