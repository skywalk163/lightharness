# 缺陷：L-023 行尾二元运算符的续行被拒绝——`返回 "a" 加上` 换行后继续写 `"b"。`
#       编译器报「意外的标记：换行」，长表达式无法跨行续写。
# 修复后：绿——行尾二元运算符（加/减/乘/比较等）后换行继续书写右操作数（Python
#       风格隐式续行），模块级与块内同缩进均可用；无运算符的行尾仍是语句边界，
#       不吞后续语句、不破坏缩进语义（L-013 单行 for 不回归）。

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

# 场景1：字符串加法续行（返回语句，块内同缩进）
段落 场景1:
  返回 "a" 加上
  "b"。

# 场景2：数值加法续行（设语句，块内）
段落 场景2:
  设 甲 为 1 加上
  2
  返回 甲

# 场景3：乘法续行
段落 场景3:
  返回 2 乘以
  3。

# 场景4：比较续行
段落 场景4:
  返回 3 大于
  2。

# 场景5：模块级（顶格）续行
设 乙 为 5 加上
6

# 场景6：三段连加
段落 场景6:
  返回 1 加上
  2 加上
  3。

段落 主:
  断言等于(场景1(), "ab", "L-023 场景1 字符串加法续行")
  断言等于(场景2(), 3, "L-023 场景2 数值加法续行")
  断言等于(场景3(), 6, "L-023 场景3 乘法续行")
  断言等于(场景4(), 真, "L-023 场景4 比较续行")
  断言等于(乙, 11, "L-023 场景5 模块级续行")
  断言等于(场景6(), 6, "L-023 场景6 三段连加")
  打印 "--- 测试L023 通过 ---"

主()
