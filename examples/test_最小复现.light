# 最小复现：己.记录事件 调用触发 事件表 拆分
类 测试类I:
  属性 事件表 等于 空
  段落 构造:
    设 己.事件表 为 []
  段落 记录事件 接收 类型:
    设 事件记录 为 ["类型": 类型]
    己.事件表.追加(事件记录)
    返回 事件记录
  段落 记录消息事件 接收 类型, 数据:
    返回 己.记录事件(类型)
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类I()
  对象.记录消息事件("user/message", "x")
  打印 "I数量: " 加上 转字符串(对象.数量())
  打印 "--- 测试I结束 ---"

主()
