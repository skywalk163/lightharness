# 缺陷：L-024 缺少「循环 对于 X 在 Y:」遍历写法——`循环` 只支持 C 风格
#       `循环(初始化;条件;更新) { ... }`，写「循环 对于 元素 在 值:」被报
#       语法错误（用户期望与 while「当 X:」风格一致的遍历写法）。
# 修复后：绿——`循环 对于 X 在 Y:` 与 `遍历 X 之 Y:` 等价（遍历循环的语法别名），
#       两种写法求和结果一致；`遍历 X 之 Y:` 既有写法不回归；`循环` 作为普通
#       标识符的用法（如变量名）不受影响。

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

# 写法A：循环 对于 X 在 Y:
段落 用循环对于 接收 值:
  设 累计 为 0
  循环 对于 元素 在 值:
    设 累计 为 累计 加上 元素
  返回 累计

# 写法B：遍历 X 之 Y:（既有写法，防回归）
段落 用遍历之 接收 值:
  设 累计 为 0
  遍历 元素 之 值:
    设 累计 为 累计 加上 元素
  返回 累计

# 写法C：循环 对于 X 在 Y: 加跳出
段落 用循环对于带跳出 接收 值:
  设 累计 为 0
  循环 对于 元素 在 值:
    如果 元素 > 3:
      跳出
    设 累计 为 累计 加上 元素
  返回 累计

段落 主:
  设 表 为 [1, 2, 3, 4]
  断言等于(用循环对于(表), 10, "L-024 循环 对于 X 在 Y: 求和")
  断言等于(用遍历之(表), 10, "L-024 遍历 X 之 Y: 求和（回归）")
  断言等于(用循环对于(表), 用遍历之(表), "L-024 两种写法等价")
  断言等于(用循环对于带跳出(表), 6, "L-024 循环 对于 X 在 Y: 可跳出")
  设 循环 为 5
  断言等于(循环, 5, "L-024 循环 作为普通标识符不受影响")
  打印 "--- 测试L024 通过 ---"

主()
