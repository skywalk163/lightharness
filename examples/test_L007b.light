# 缺陷：L-007b 模块级局部变量名污染跨模块成员访问（a.light 局部变量 头/事件/轮次 使 b.light 对象成员 头信息/记录事件 被拆成 头+…/记录(事件…)）
# 当前：已缓解转绿——调用方局部变量不再影响成员访问拆分（本用例跨模块调用可正常运行）
# 修复后：绿（即当前行为）——头信息/记录事件 整体成词，取值与返回值断言通过
从 _helper_L007b 导入 会话对象

段落 主:
  设 对象 为 新建 会话对象()
  设 头 为 1
  设 事件 为 2
  设 轮次 为 3
  设 甲 为 对象.头信息
  设 丙 为 对象.记录事件("x")
  设 丁 为 对象.计数()
  打印 甲 加上 丙 加上 转字符串(丁)
  打印 "--- 测试L007b 结束 ---"
  # 修复后启用：断言修复后()

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 对象 为 新建 会话对象()
  设 头 为 1
  设 事件 为 2
  设 轮次 为 3
  断言等于(对象.头信息, "首", "L-007b 头信息 整体成词并取值")
  断言等于(对象.记录事件("x"), "首", "L-007b 记录事件 整体成词并返回")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
