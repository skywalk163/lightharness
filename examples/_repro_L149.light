# _repro_L149.light —— 第 17 轮任务 5 语言缺陷 L-149 最小复现
# 缺陷：光明「模块级 段落 函数」无法读取/共享「模块级 设 变量」（无全局捕获/闭包）
# 现象：模块级 设 接收表 为 []；模块级 段落 IO事件处理器 对 接收表 做 .append；
#       主 调用处理器后读取 接收表 仍是 []（处理器内的 append 落到独立绑定，不反映到主作用域）。
# 实测（探针 _probe_io.light）：H sees 接收表= [] / main 接收表= [] —— 处理器侧修改对调用方不可见。
# 绕法：状态改用「传入的字典」承载（如 总线["历史"]），避开模块级共享变量。
# 运行：cd lightharness && python 运行.py examples/_repro_L149.light  → 期望 rc=0（缺陷被稳定复现）

设 接收表 为 []

段落 IO事件处理器 接收 数据:
  接收表.追加(数据)   # 期望追加到模块级 接收表，实则无效（无闭包捕获）

段落 断言相等 接收 实际, 期望, 信息:
  如果 实际 != 期望:
    抛出 新建 错误("断言失败：" + 信息 + " | 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))
  返回 真

段落 主:
  设 占位 为 真
  IO事件处理器({"请求ID": "x"})
  断言相等(列表长度(接收表), 0, "L-149: 模块级段对模块级设变量的 append 不反映到主作用域（无闭包捕获）")
  打印 "L-149 复现成功：调用 IO事件处理器 后 接收表 长度=" + 文本(列表长度(接收表)) + "（应为 1 而实际 0）"
  打印 "绕法：状态改用传入的字典承载（如 总线['历史']），避开模块级共享变量"
