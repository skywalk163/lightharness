# _repro_L088.light —— L-088 最小复现（绕法形态，rc=0）
# 现象：`捕获 错误 as 变量` 不支持——异常捕获无法绑定变量，
#   `尝试 ... 捕获 错误 as e:` 语法不可用。
# 绕法：`尝试/捕获 错误:` + 布尔标志（抛错置标志，捕获块内读标志）。
#   本文件按绕法写，断言抛错可被捕获且标志正确；被破坏原写法见注释（不参与编译）。
# （被破坏原写法：`尝试: 抛出 "x" 捕获 错误 as 变量: 打印 变量` —— 解析错误。）

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

段落 抛一个错:
  抛出 "模拟异常"

段落 主:
  设 已捕获 为 假
  尝试:
    抛一个错()
  捕获 错误:
    已捕获 为 真
  断言相等(已捕获, 真, "L-088 绕法：尝试/捕获 + 布尔标志可判定抛错")
  设 未触发 为 假
  尝试:
    设 平稳 为 1 + 1
  捕获 错误:
    未触发 为 真
  断言相等(未触发, 假, "L-088 绕法：无异常时不置标志")
  打印 "L-088 绕法形态复现通过（rc=0；绕法=尝试/捕获 错误 + 布尔标志）"

主()
