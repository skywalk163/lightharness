# 缺陷：L-008 模块级 JSON 序列化函数并非全局内置（序列化(...) 未定义，需显式引入 JSON 模块的 序列化JSON）
# 当前：红——「name '序列化' is not defined」
# 修复后：绿——可直接调用 序列化/反序列化（或语言内置等价动词），断言通过
段落 主:
  设 文本 为 序列化(["a": 1])
  打印 文本
  打印 "--- 测试L008 结束 ---"
  # 修复后启用：断言修复后()

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 文本 为 序列化(["a": 1])
  断言等于(长度(文本) > 0, 真, "L-008 序列化 应返回非空 JSON 文本")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
