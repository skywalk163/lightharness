# 安全前缀批量测试（成员访问方法名）
类 测试前缀:
  属性 数据 等于 空
  段落 构造:
    设 己.数据 为 []
  段落 生成列表:
    返回 己.数据
  段落 汇集列表:
    返回 己.数据
  段落 整理列表:
    返回 己.数据
  段落 分析列表:
    返回 己.数据
  段落 计算列表:
    返回 己.数据
  段落 汇总列表:
    返回 己.数据

段落 主:
  设 对象 为 新建 测试前缀()
  打印 "生成: " 加上 转字符串(长度(对象.生成列表()))
  打印 "汇集: " 加上 转字符串(长度(对象.汇集列表()))
  打印 "整理: " 加上 转字符串(长度(对象.整理列表()))
  打印 "分析: " 加上 转字符串(长度(对象.分析列表()))
  打印 "计算: " 加上 转字符串(长度(对象.计算列表()))
  打印 "汇总: " 加上 转字符串(长度(对象.汇总列表()))
  打印 "--- 前缀测试结束 ---"

主()
