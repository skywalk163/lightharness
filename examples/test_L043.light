# 缺陷：L-043 布尔链 + 行内 `否则 返回` 同行解析失败
#        `如果 (a == 真) 且 (b == 真): 返回 "都真" 否则 返回 "有假"`
#        当前报「期望冒号」（行内 `否则` 结合 `且/或` 的解析分支）。
#        单独 `如果 条件: 返回 X 否则 返回 Y`（无 且/或）已可解析。
# 修复前：红——布尔链 + 行内否则 编译失败。
# 修复后：绿——`且`/`或` 布尔链可接行内 `否则 返回`。

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 判定且 接收 甲, 乙:
  如果 (甲 == 真) 且 (乙 == 真): 返回 "都真" 否则 返回 "有假"

段落 判定或 接收 甲, 乙:
  如果 (甲 == 真) 或 (乙 == 真): 返回 "有真" 否则 返回 "都假"

段落 主:
  断言等于(判定且(真, 真), "都真", "L-043 且 两真")
  断言等于(判定且(真, 假), "有假", "L-043 且 一假")
  断言等于(判定或(真, 假), "有真", "L-043 或 一真")
  断言等于(判定或(假, 假), "都假", "L-043 或 两假")

  打印 "--- 测试L043 通过 ---"

主()