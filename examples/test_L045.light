# test_L045.light —— L-045 回归：写 参数为拼接表达式应整体写出
# 运行：python 运行.py examples/test_L045.light
#
# 缺陷：`写 "通过 [" + 标签 + "]"` 被 parser 拆成 BinaryOp(写("通过 [") + 标签)，
#       只吃首个字面量串操作数，后续 `+ 标签` 成悬空表达式，
#       运行期 TypeError: NoneType + str（test_令牌估算.light 首版报错）。
# 修复：codegen 识别「输出类内置调用 + 拼接尾巴」形态，合并为 输出(整条表达式)
#       一次写出（覆盖 写/印/显示 族；`打印` 由 parser 特判本就不受影响）。

设 标签 为 "PASS"
设 数 为 42

# 场景1：字面量 + 变量 拼接（修复前此处 TypeError 崩溃）
写 "通过 [" + 标签 + "]"

# 场景2：变量首参 + 字面量尾巴
写 标签 + " 结束"

# 场景3：内置调用结果 + 字面量尾巴
写 (文本(数)) + " 个"

# 场景4：纯字面量多段连加
写 "a" + "b" + "c"

# 场景5：写 单变量不回归
写 标签

# 场景6：写 括号整体表达式不回归
写 ("x=" + 标签)

# 场景7：印/显示 族同缺陷一并修复
印 "印族:" + 标签
显示 "显示族:" + 标签

写 "--- 测试L045 通过 ---"
