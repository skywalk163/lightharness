# 二分法2：加压缩替换方法
类 测试类G:
  属性 事件表 等于 空
  段落 构造 接收 标识, 工作目录:
    设 己.事件表 为 []
  段落 记录事件 接收 类型:
    设 事件记录 为 ["类型": 类型]
    己.事件表.追加(事件记录)
    返回 事件记录
  段落 压缩替换 接收 旧开始, 旧结束, 新事件表:
    遍历 旧事件 之 己.事件表:
      如果 旧事件["序号"] >= 旧开始 且 旧事件["序号"] <= 旧结束:
        旧事件["遮蔽"] 为 真
    遍历 新事件 之 新事件表:
      设 事件记录 为 己.记录事件(新事件["类型"])
      如果 新事件.包含("源序号表"):
        事件记录["来源事件序号"] 为 新事件["源序号表"]
    返回 己.生成模型消息()
  段落 生成模型消息:
    设 结果 为 []
    遍历 事件项 之 己.事件表:
      设 被遮蔽 为 事件项.包含("遮蔽") 且 事件项["遮蔽"]
      如果 被遮蔽 == 假:
        结果.追加(事件项)
    返回 结果
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类G("s1", "G:\\x")
  对象.记录事件("turn/start")
  对象.压缩替换(0, 0, [["类型": "x", "数据": "d", "源序号表": [0]]])
  打印 "G数量: " 加上 转字符串(对象.数量())
  打印 "--- 测试G结束 ---"

主()
