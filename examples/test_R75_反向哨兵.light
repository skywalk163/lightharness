# R75 任务B - 反向哨兵（G-06 / G-10 / G-15）
# 本文件包含三项语法的非法形态，编译时必须报中文错误（非 Python 原生异常泄漏）。
# 预期错误见每条注释。运行方式：单独编译每条，确认 rc!=0 且错误信息为中文。

# === G-06 推导式：缺少可迭代对象 ===
# 预期：解析错误 "意外的标记: 「]」"
# 设 x 为 [1 遍历 x 之]

# === G-10 数据类：空记录（无字段）===
# 预期：解析错误 "记录至少需要一个字段"
# 记录 空记录:

# === G-15 with：缺少上下文表达式 ===
# 预期：解析错误 "意外的标记: 「:」"
# 使用:
#   打印 1

段落 主:
  打印 "反向哨兵文件——不应直接运行（非法形态已注释）"

主()
