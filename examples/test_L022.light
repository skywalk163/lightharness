# 缺陷：L-022 匿名闭包赋值被拒绝——`设 X 为 段落 接收 参数: 体` 无法把内联回调
#       直接赋值给变量（编译器把表达式位置的 `段落 接收` 误判为段落定义，报
#       「无法识别的语法元素：':'」；或把 `返回 段落 接收 参数:` 静默错编成
#       「返回空 + 定义名为接收参数的段落」，运行期 NoneType 不可调用）。
# 修复后：绿——`段落 接收 参数: 体` 作为匿名闭包字面量，可赋值给变量、捕获外层
#       局部变量、作为返回值返回（高阶函数）、多层嵌套、支持多参数与单行体，
#       所有场景运行结果正确。

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

# 场景1：基础内联闭包赋值（多行块体）
段落 场景1:
  设 加倍 为 段落 接收 参数:
    返回 参数 * 2
  断言等于(加倍(5), 10, "L-022 场景1 基础闭包赋值")

# 场景2：闭包捕获外层局部变量
段落 场景2:
  设 基数 为 10
  设 加基数 为 段落 接收 参数:
    返回 参数 + 基数
  断言等于(加基数(5), 15, "L-022 场景2 捕获外层变量")

# 场景3：返回闭包（高阶函数）
段落 造加法器 接收 增量:
  返回 段落 接收 参数:
    返回 参数 + 增量

# 场景4：多层闭包（外层返回内层）
段落 场景4:
  设 外层 为 段落 接收 甲:
    设 内层 为 段落 接收 乙:
      返回 甲 + 乙
    返回 内层
  设 内 为 外层(10)
  断言等于(内(20), 30, "L-022 场景4 多层闭包")

# 场景5：多参数闭包
段落 场景5:
  设 相加 为 段落 接收 甲, 乙:
    返回 甲 + 乙
  断言等于(相加(3, 4), 7, "L-022 场景5 多参数闭包")

# 场景6：单行体闭包
段落 场景6:
  设 加倍 为 段落 接收 参数: 返回 参数 * 3
  断言等于(加倍(4), 12, "L-022 场景6 单行体闭包")

段落 主:
  场景1()
  场景2()
  设 加三 为 造加法器(3)
  断言等于(加三(7), 10, "L-022 场景3 返回闭包")
  场景4()
  场景5()
  场景6()
  打印 "--- 测试L022 通过 ---"

主()
