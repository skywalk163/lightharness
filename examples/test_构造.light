类 测试类3:
  属性 标识 等于 空
  属性 事件表 等于 空
  属性 下一序号 等于 空
  属性 头部 等于 空
  段落 构造 接收 标识, 工作目录, 父会话, 种子长度:
    己.标识 为 标识
    己.事件表 为 []
    己.下一序号 为 0
    己.头部 为 ["版本": 1, "创建时间": 时间戳(), "工作目录": 工作目录, "父会话": 父会话, "种子长度": 种子长度]
  段落 事件数:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类3("s1", "G:\\x", 空, 0)
  打印 "事件数: " 加上 转字符串(对象.事件数())
  打印 "标识: " 加上 对象.标识
  打印 "头部版本: " 加上 转字符串(对象.头部["版本"])
  打印 "--- 测试3结束 ---"

主()
