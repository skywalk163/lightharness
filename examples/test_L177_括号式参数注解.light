# test_L177_括号式参数注解.light
#
# L-177 复核（R71-R73 收口遗留）：R73-B 记录的「括号式参数注解解析期递归死循环」
# 经复核不成立。本用例锁定「括号式参数注解必须正常可用」这一事实，
# 防止该误判再次被当作遗留缺陷登记。
#
# 覆盖：单参 / 双参 / 三参 / 无返回注解 / 泛型 / 多行括号注解 / 方法括号注解
#       / 联合注解 / 可空注解 / 与接收式共存

# ---- 任务书原文形式：双参括号注解 + 返回注解 ----
段落 加法(a: 整数, b: 整数) -> 整数:
    返回 a 加 b

# ---- 单参 ----
段落 加一(a: 整数) -> 整数:
    返回 a 加 1

# ---- 三参 + 无返回注解 ----
段落 三数之和(a: 整数, b: 整数, c: 整数):
    返回 a 加 b 加 c

# ---- 泛型注解（括号式） ----
段落 首元素(a: 列表<整数>) -> 整数:
    返回 a[0]

# ---- 多行括号注解 ----
段落 相乘(
    a: 整数,
    b: 整数
) -> 整数:
    返回 a 乘 b

# ---- 联合 / 可空注解（括号式） ----
段落 描述(a: 整数|字符串) -> 字符串:
    返回 "有值"

段落 判空(a: 可空 字符串) -> 整数:
    若 a == 空:
        返回 0
    返回 1

# ---- 与接收式注解共存 ----
段落 减一(a: 整数) -> 整数:
    返回 a 减 1

# ---- 方法括号注解 ----
类 计数器:
    段落 加值(己, a: 整数) -> 整数:
        返回 a 加 100

段落 主:
    # 括号式注解各形态
    断言(加法(1, 2) == 3, "括号式双参注解：加法(1,2)=3")
    断言(加法(10, 20) == 30, "括号式双参注解：加法(10,20)=30")
    断言(加一(5) == 6, "括号式单参注解：加一(5)=6")
    断言(三数之和(1, 2, 3) == 6, "括号式三参无返回注解：=6")
    断言(首元素([10, 20, 30]) == 10, "括号式泛型注解：首元素=10")
    断言(相乘(3, 4) == 12, "多行括号注解：相乘(3,4)=12")
    断言(描述(7) == "有值", "括号式联合注解")
    断言(判空(空) == 0, "括号式可空注解：空→0")
    断言(判空("hi") == 1, "括号式可空注解：非空→1")
    断言(减一(10) == 9, "接收式注解共存：减一(10)=9")
    断言(计数器().加值(1) == 101, "方法括号注解：加值(1)=101")

    # 无挂死哨兵：本用例整体 rc=0 即证明括号式注解不走死循环
    打印 "--- test_L177 括号式参数注解 通过（无递归死循环）---"

主()
