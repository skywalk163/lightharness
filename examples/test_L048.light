# test_L048.light —— L-048 复现/回归测试
# 动词名变量（与 VERB_ARITY 动词同名，如 读取/排序/求和）后紧跟 [ 下标访问，
# 必须按下标解析，不得误当动词调用把 [] 比较吞成实参。
从 JSON 导入 序列化JSON

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望: 抛出 新建 错误("断言失败[" + 标签 + "]：实际=" + 序列化JSON(实际) + " 期望=" + 序列化JSON(期望))

# 动词名变量 + 下标 + 比较（bash渲染 #50 根因场景）
设 读取 为 {"lossy": 真, "stdoutSpillPath": "/a"}
如果 读取["lossy"] == 真:
  断言相等(读取["stdoutSpillPath"], "/a", "读取-下标-比较-命中")
否则:
  断言相等(真, 假, "读取-不应走否则")

# 缺失键下标（须先 字典包含键 守卫，见 L-036：缺失键直接下标抛 键错误）
设 求和 为 {"k": 5}
断言相等(求和["k"], 5, "求和-下标-命中")
如果 字典包含键(求和, "missing") == 真:
  断言相等(真, 假, "求和-缺失键不应命中")

# 动词名变量 + 列表下标
设 排序 为 [10, 20, 30]
断言相等(排序[0], 10, "排序-列表下标0")
断言相等(排序[2], 30, "排序-列表下标2")

打印 "L-048 测试通过"
