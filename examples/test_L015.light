# 缺陷：L-015 模块名含关键字词根（如 `异步`）导致 `从 X 导入 Y` 解析失败；整模块 `导入 X` 可加载但成员访问被拆（L-004/L-010 家族新实例）
# 当前：红——`从 异步样例 导入 样例函数` 语法错误「期望关键字但得到标识符（附近 '样例'）」（模块名按关键字边界拆分）
# 修复后：绿——模块名以整段为单位解析，`从 异步样例 导入 样例函数` 成功，样例函数() 返回 7，断言通过
从 异步样例 导入 样例函数

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 值 为 样例函数()
  断言等于(值, 7, "L-015 修复后 从含关键字词根模块导入应成功")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 值 为 样例函数()
  打印 "--- 测试L015 结束 ---"
  # 修复后启用：断言修复后()

主()
