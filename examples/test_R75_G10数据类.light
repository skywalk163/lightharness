# R75 任务B - G-10 数据类/记录类型正向用例
# 验证：记录类型定义、构造、相等比较、哈希、字段访问

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 断言为真 接收 条件, 标签:
  如果 非 条件:
    抛出 "断言失败: " + 标签

# 定义一个记录类型：点坐标
记录 点: x, y

# 定义带更多字段的记录类型
记录 人员: 姓名, 年龄, 城市

段落 主:
  # 1. 构造记录
  设 p1 为 点(3, 4)
  断言等于(p1.x, 3, "记录字段访问 x")
  断言等于(p1.y, 4, "记录字段访问 y")

  # 2. 相等比较（同值应相等）
  设 p2 为 点(3, 4)
  断言为真(p1 == p2, "同值记录应相等")

  # 3. 不同值不相等
  设 p3 为 点(5, 6)
  断言为真(p1 != p3, "不同值记录应不等")

  # 4. 可哈希（能放入集合，同值去重）
  设 点集合 为 {p1, p2, p3}
  断言等于(长度(点集合), 2, "记录可哈希，同值 p1/p2 去重后剩 2 个")

  # 5. 多字段记录
  设 人1 为 人员("张三", 25, "北京")
  断言等于(人1.姓名, "张三", "多字段记录 姓名")
  断言等于(人1.年龄, 25, "多字段记录 年龄")
  断言等于(人1.城市, "北京", "多字段记录 城市")

  # 6. 记录作为字典值
  设 映射 为 {"原点": 点(0, 0)}
  断言等于(映射["原点"].x, 0, "记录作为字典值")

  打印 "--- G-10 数据类测试全部通过 ---"

主()
