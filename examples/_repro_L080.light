# _repro_L080.light —— 最小复现：光明 / 为浮点除法，切片下标需 int
#
# L-080 缺陷表述：光明 `/` 对应 Python 浮点除法，字符串/列表切片下标要求 int；
#   浮点结果直接作下标报 `slice indices must be integers or None or have an __index__ method`，
#   且光明包装提示「文本和数字不能直接进行运算」与真实根因无关（误导）。
#
# 失败形态（取消下面两行注释即复现，rc=1）：
#   设 头部长红 为 (长 * 70) / 100        # 14000.0（float）
#   设 头红 为 结果[0:头部长红]            # slice indices must be integers
#
# 本文件以「修复形态」落地（rc=0），保证进 CI 门禁（EXPECT_RED 必须为空）；
# 失败形态经任务1 实测：`(20000*70)/100` → 14000.0（float），切片报错。

段落 主:
  设 长 为 20000
  设 头部长 为 整数((长 * 70) / 100)   # 修复：显式转 int（等价备选：(长 * 70) // 100）
  设 结果 为 ""
  设 i 为 0
  当 i < 长:
    设 结果 为 结果 + "x"
    设 i 为 i + 1
  设 头 为 结果[0:头部长]
  写 "复现L-080（修复形态 rc=0）：头部长=" + 转字符串(头部长) + " 切片长度=" + 转字符串(长度(头))

主()
