# R27 任务1+2 快速验证（词法/语义双层）
# 目标：证明 A类 DUAL 8字（乘 减 加 除 模 真 空 到）与 B类 F∩CS 8字
#       （列 对 是 段 的 类 自 配）经「词首并入正面规则」后，可整词成标识符。
# 运行：python -m light ... 或 lightharness 测试入口（见各 test_L0xx.light）
段落 断言相等(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 测试A类DUAL词首并入:
  设 乘法 为 7
  设 减法 为 3
  设 加法 为 5
  设 除法 为 2
  设 模组 为 9
  设 真空 为 真
  设 空格 为 " "
  设 到位 为 10
  断言相等(乘法, 7, "乘法 整词成标识符")
  断言相等(减法, 3, "减法 整词成标识符")
  断言相等(加法, 5, "加法 整词成标识符")
  断言相等(除法, 2, "除法 整词成标识符")
  断言相等(模组, 9, "模组 整词成标识符")
  断言相等(真空, 真, "真空 整词成标识符")
  断言相等(空格, " ", "空格 整词成标识符")
  断言相等(到位, 10, "到位 整词成标识符")
  # 反向：运算符/值字面量仍按语句语义切分（不并入）
  设 甲 为 1
  设 乙 为 2
  断言相等(甲 加 乙, 3, "甲 加 乙 仍切分为运算符")
  断言相等(甲 减 乙, -1, "甲 减 乙 仍切分为运算符")

段落 测试B类词首并入:
  设 列数 为 5
  设 对立 为 1
  设 是否 为 真
  设 段数 为 3
  设 的确 为 真
  设 类别 为 "动物"
  设 自然 为 2
  设 配置 为 {}
  断言相等(列数, 5, "列数 整词成标识符")
  断言相等(对立, 1, "对立 整词成标识符")
  断言相等(是否, 真, "是否 整词成标识符")
  断言相等(段数, 3, "段数 整词成标识符")
  断言相等(的确, 真, "的确 整词成标识符")
  断言相等(类别, "动物", "类别 整词成标识符")
  断言相等(自然, 2, "自然 整词成标识符")
  断言相等(配置, {}, "配置 整词成标识符")

段落 主:
  测试A类DUAL词首并入()
  测试B类词首并入()
  打印 "--- R27 任务1+2 词首并入规则验证通过 ---"

主()
