# R28 任务1 快速验证 —— 列 的词尾切出反向规则
#
# 规则：列 在【嵌入输出循环】中位于标识符【词尾】时切出为 KEYWORD
#       （复刻 CS 基线行为，如 `对于元素在序列:` 的 序 + 列）；
#       - 词首并入（列数/列名）不受影响；
#       - 裸 序列 / 甲序列（段内无真关键字）仍整词成 IDENTIFIER。
# token 级断言：见 lightharness/_antirun_r28_t3_CS表清零验证.py（G3）
#
# 运行：cd light-merge && python cli/light.py run ../lightharness/examples/test_R28_列词尾切出_快速.light

段落 断言等于(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

段落 主:
  设 列数 为 3
  设 列名 为 "id"
  设 序列 为 [10, 20, 30]
  断言等于(列数, 3, "列数 词首并入成标识符")
  断言等于(列名, "id", "列名 词首并入成标识符")
  断言等于(序列[2], 30, "序列 词尾并入整词成标识符")
  打印 "--- test_R28 列词尾切出 通过 ---"

主()
