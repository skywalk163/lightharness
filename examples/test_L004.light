# 缺陷：L-004 关键字边界拆分中文标识符（跨模块调用 导出 开头方法名时被拆成 导出+事件表）
# 当前：红——「'会话对象' object has no attribute '导出'」（调用方模块不认识成员名，词法按关键字边界拆分）
# 修复后：绿——方法名整体成词，导出事件表() 返回追加后的表，断言通过
从 _helper_L004 导入 会话对象

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 对象 为 新建 会话对象()
  设 结果 为 对象.导出事件表("x")
  断言等于(长度(结果), 1, "L-004 跨模块调用 导出事件表 后表长度应为 1")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 对象 为 新建 会话对象()
  设 结果 为 对象.导出事件表("x")
  打印 转字符串(长度(结果))
  打印 "--- 测试L004 结束 ---"
  # 修复后启用：断言修复后()

主()
