# _repro_L087.light —— L-087 最小复现（绕法形态，rc=0）
# 现象：`当前目录` 是编译器内置映射名（builtins os.getcwd()），编译器把源码里的
#   `当前目录` 无条件发射为 `当前目录()`（带括号调用）；`从 会话格式 导入 当前目录`
#   （字典常量）后使用该名报 'dict' object is not callable。
# 绕法：不 import 该名，改用其它变量名存放目录字典。本文件按绕法写，
#   断言自建目录字典可正常读取；被破坏原写法见注释（不参与编译）。
# （被破坏原写法：`从 会话格式 导入 当前目录` 后 `当前目录["x"]` ——
#   编译器把 `当前目录` 发射为 `当前目录()`，'dict' object is not callable。）

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签

段落 主:
  设 工作目录 为 ["路径": "/sessions/abc"]
  断言相等(工作目录["路径"], "/sessions/abc", "L-087 绕法：改用「工作目录」存放目录字典可正常读取")
  打印 "L-087 绕法形态复现通过（rc=0；绕法=不 import 内置映射名「当前目录」，改用其它名）"

主()
