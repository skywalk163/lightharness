# test_R72_B_空目标.light —— 反向哨兵（预期 rc!=0）
# R72 任务B（G-05）：解构赋值的目标表不能为空。
# 期望：编译期报「解构赋值的目标表不能为空，至少要有一个变量名」。
# 改前 `设 () 为 [1, 2]` 生成的 Python 是 ` = [1, 2]`（语法非法的残码），
# 报错指向不明；本单改为明确的中文诊断。
# 若未来解析器静默产出残码或空元组目标，本用例 rc 可能变 0 →
# tests/test_回归.py 的 EXPECT_RED「断言 rc!=0」会失败，逼你复核解析器。

段落 主:
  设 () 为 [1, 2]
  打印 "不应到达"
