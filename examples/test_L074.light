# test_L074.light —— D批次验收：类型判断内置（L-074）
# 缺陷：是字典/是字符串/是列表 等类型判断函数非内置。
# 验收结果：已修复——均为内置原语，输出真。

如果 是字典({"甲": 1}) != 真:
    抛出 新建 错误("L-074 验收失败: 是字典")
如果 是字符串("甲") != 真:
    抛出 新建 错误("L-074 验收失败: 是字符串")
如果 是列表([1, 2]) != 真:
    抛出 新建 错误("L-074 验收失败: 是列表")
如果 是整数(5) != 真:
    抛出 新建 错误("L-074 验收失败: 是整数")
打印 "test_L074 PASS"
