# test_R73_B_类型注解.light —— G-09 类型注解（R73 任务B）
#
# 覆盖：接收式参数注解、括号式参数注解、泛型注解（列表<整数>）、
#       联合类型注解（整数|字符串）、默认值+注解、返回泛型注解、
#       可空注解、返回类型箭头注解。
# 类型注解运行时不检查（仅文档/IDE 用途）；断言验证「语义正确」而非「注解存在」。
#
# 运行：python 运行.py examples/test_R73_B_类型注解.light

段落 加一 接收 x: 整数 -> 整数:
    返回 x 加 1

段落 问候 接收 名字: 字符串 -> 字符串:
    返回 名字

段落 首元素 接收 xs: 列表<整数> -> 整数:
    返回 xs[0]

段落 描述 接收 v: 整数|字符串 -> 字符串:
    若 v == 空:
        返回 "空"
    否则:
        返回 "有值"

段落 乘 接收 x: 整数 等于 2 -> 整数:
    返回 x 乘 10

段落 包装 接收 s: 字符串 -> 列表<字符串>:
    返回 [s]

段落 长度 接收 可能: 可空 字符串 -> 整数:
    若 可能 == 空:
        返回 0
    否则:
        返回 1

段落 主:
    断言(加一(5) == 6, "参数/返回注解：加一(5)=6")
    断言(问候("光") == "光", "字符串参数/返回注解")
    断言(首元素([10, 20, 30]) == 10, "泛型注解 列表<整数>")
    断言(描述(7) == "有值", "联合类型注解 整数|字符串")
    断言(乘() == 20, "默认值+注解：乘()=20")
    断言(乘(3) == 30, "默认值可被覆盖：乘(3)=30")
    断言(包装("x") == ["x"], "返回泛型注解 列表<字符串>")
    断言(长度(空) == 0, "可空注解：空 → 0")
    断言(长度("hi") == 1, "可空注解：非空 → 1")

    打印 "--- test_R73_B 类型注解 通过 ---"

主()
