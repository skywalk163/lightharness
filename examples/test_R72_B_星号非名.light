# test_R72_B_星号非名.light —— 反向哨兵（预期 rc!=0）
# R72 任务B（G-05）：`*` 星号解构后面必须跟一个变量名，不能跟分组。
# （Python 里 `a, *(b, c) = …` 也是 SyntaxError：starred assignment target
#   必须是一个裸名字，不能是列表/元组形态。）
# 期望：编译期报「「*」星号解构后面必须跟一个变量名」。
# 若未来解析器静默丢弃 `*` 或把 `(乙, 丙)` 当整体目标，本用例 rc 会变 0 →
# tests/test_回归.py 的 EXPECT_RED「断言 rc!=0」会失败，逼你复核解析器。

段落 主:
  设 甲, *(乙, 丙) 为 [1, 2, 3]
  打印 转字符串(甲)
