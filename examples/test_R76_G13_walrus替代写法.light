# R76 任务B - G-13 walrus（海象运算符 :=）
# 判定：**有意不支持**，非缺口。判据见 docs/功能对标/语言缺陷账.md 的 G-13 条目：
#   src/parser_expr.py 在 _parse_expr 与 _parse_logical_expr 两处主动抛出中文错误
#   「光明不支持海象运算符 ':='。请使用「设」语句声明变量后再使用。」
#   —— 属设计内（原标注 P1-3），且已给出替代写法，故不实现。
# 本文件是**替代写法**的正向用例（全文不含 :=）；反向哨兵见
# light-merge/tests/test_R76_syntax_gaps.py::test_G13_walrus_哨兵_中文错误。
# 运行：python 运行.py examples/test_R76_G13_walrus替代写法.light   （期望 rc=0）

段落 主:
  设 甲 为 0
  设 乙 为 5
  甲 等于 乙
  设 命中 为 假
  如果 甲 大于 1:
    设 命中 为 真
  如果 命中 != 真:
    抛出 "G-13 替代写法失败：设 语句赋值后条件判断未命中"
  打印 "--- G-13 walrus：用「设」语句声明/赋值，替代 := 全部通过 ---"

主()
