# test_L068.light —— D批次验收：类方法定义边界（L-068）
# 缺陷：类方法定义边界不清晰（多类连续定义时方法归属错误）。
# 验收结果：已修复/正常——两个连续类的方法归属正确，多态行为正常。
# 修复来源：light-merge 已实现类结束标记与编译器警告（cli/light.py L-068 警告透出）。

类 动物:
    段落 叫():
        打印 "动物叫"

类 狗 承 动物:
    段落 叫():
        打印 "汪汪"

设 旺财 为 新建 狗()
旺财.叫()
设 大黄 为 新建 动物()
大黄.叫()
打印 "test_L068 PASS"
