# _repro_L092.light —— L-092 最小复现（绕法形态，rc=0）
# 现象：标识符含关键字子串被词法切碎（L-084「为」字同族扩展）——
#   形参名以「返回」开头：`段落 甲 接收 返回表:` 报「期望 冒号，但得到 关键字」；
#   段落名含「返回」：`段落 注册未声明返回:` 报「期望 冒号，但得到 关键字」。
#   即「返回」「为」等关键字字样出现在标识符任意位置都会被切碎。
# 绕法：裸标识符避开全部关键字字样（返回/为/如果/遍历/当/设/段落/抛出/尝试/捕获/跳出/
#   导入/导出/从/且/或/非/空 等），命名改用同义字（如 结果表/注册未声明键）。
# 本文件按绕法写，断言改名后形参/段落名可正常定义调用；被破坏原写法见注释（不参与编译）。
# （被破坏原写法一：`段落 甲 接收 返回表:` 换行 `返回 返回表` —— 解析错误。）
# （被破坏原写法二：`段落 注册未声明返回:` —— 解析错误。）

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 新建 错误("断言失败: " + 标签)

# 绕法：形参用 结果表（避开「返回」字样）
段落 造恒定 接收 结果表:
  段落 取值 接收 执行上下文:
    返回 结果表
  返回 取值

# 绕法：段落名用 注册未声明键（避开「返回」字样）
段落 注册未声明键:
  返回 "注册未声明键 可正常定义"

段落 主:
  设 取值 为 造恒定(["甲": "1"])
  断言相等(取值(空), ["甲": "1"], "形参避开关键字字样")
  断言相等(注册未声明键(), "注册未声明键 可正常定义", "段落名避开关键字字样")
  打印 "L-092 绕法形态复现通过（rc=0）"

主()
