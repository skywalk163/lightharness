# test_web服务器E5.light —— E5 web服务器 统计事件测试
从 web服务器 导入 Web服务端
从 JSON 导入 序列化JSON

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望: 抛出 新建 错误("断言失败[" + 标签 + "]：实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 断言包含 接收 文本, 片段, 标签:
  如果 文本.查找(片段) < 0: 抛出 新建 错误("断言失败[" + 标签 + "]：文本不含片段 " + 片段 + " 全文=" + 文本)

段落 用例计量器存在:
  设 服务 为 新建 Web服务端()
  如果 服务.计量器 == 空:
    抛出 新建 错误("计量器应初始化")
  如果 服务.压缩管理器 == 空:
    抛出 新建 错误("压缩管理器应初始化")
  如果 服务.上下文窗口 <= 0:
    抛出 新建 错误("上下文窗口应>0")
  写 "通过: 计量器存在"

段落 用例生成统计事件:
  设 服务 为 新建 Web服务端()
  服务.计量器.记录响应(["输入token": 100, "输出token": 50])
  设 统计 为 服务.生成统计事件()
  断言相等(统计["种类"], "统计", "种类=统计")
  断言相等(统计["本轮输入"], 100, "本轮输入=100")
  断言相等(统计["本轮输出"], 50, "本轮输出=50")
  断言相等(统计["累计总"], 150, "累计总=150")
  如果 统计["花费"] < 0:
    抛出 新建 错误("花费应>=0")
  如果 统计["上下文使用率"] < 0:
    抛出 新建 错误("使用率应>=0")
  写 "通过: 生成统计事件"

段落 用例重置本轮:
  设 服务 为 新建 Web服务端()
  服务.计量器.记录响应(["输入token": 100, "输出token": 50])
  服务.计量器.重置本轮()
  设 统计 为 服务.生成统计事件()
  断言相等(统计["本轮输入"], 0, "重置后本轮输入=0")
  断言相等(统计["累计总"], 150, "累计总保持=150")
  写 "通过: 重置本轮"

用例计量器存在()
用例生成统计事件()
用例重置本轮()
写 "--- E5 web服务器统计事件测试通过 ---"
