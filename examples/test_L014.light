# 缺陷：L-014 语句级下标调用 `字典["键"](参数)` 被解析成 FunctionCallExpr，统一后端代码生成器在语句层（含表达式层）均不处理，必报「未知语句类型 (节点类型: FunctionCallExpr)」
# 当前：红——代码生成错误「未知语句类型 (节点类型: FunctionCallExpr)」（`表["甲"](1)` 作语句）
# 修复后：绿——`表["甲"](1)` 被识别为合法调用语句并执行，断言通过
段落 计数 接收 值:
  返回 值 + 1

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 表 为 ["甲": 计数]
  设 结果 为 表["甲"](1)
  断言等于(结果, 2, "L-014 修复后 下标调用应返回 2")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 表 为 ["甲": 计数]
  表["甲"](1)
  打印 "--- 测试L014 结束 ---"
  # 修复后启用：断言修复后()

主()
