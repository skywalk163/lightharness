# 缺陷: L-143 —— `作用域` 是保留关键字，不能用作变量名
# 当前/修复后: `作用域` 被 parser 用于 `异步作用域` 语法，保留为保留字；
#             误用时报明确提示「作用域是保留关键字，不能直接作为语句开头」（非静默失败），
#             满足任务3验收"报明确提示"分支。
# 说明: 本轮评估后保留为保留字（移除会破坏异步作用域语法），无代码改动；验收以"明确提示"达成。
段落 主:
    设 作用域 为 {}
    打印 作用域
