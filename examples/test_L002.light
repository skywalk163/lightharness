# 缺陷：L-002 不支持多行表达式/字面量续行（字典/列表跨行即「意外的标记: 换行」）
# 当前：红——解析错误「意外的标记: 换行」（第 4 行逗号后换行即触发，整文件无法解析）
# 修复后：绿——支持括号内换行续行（Python 风格），长度断言通过

# 复现段：多行字典字面量（当前语法错误，整文件无法解析）
段落 主:
  设 表 为 [
    "a": 1,
    "b": 2
  ]
  打印 转字符串(长度(表))

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 表2 为 [
    "a": 1,
    "b": 2
  ]
  断言等于(长度(表2), 2, "L-002 多行字面量续行后长度应为 2")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
