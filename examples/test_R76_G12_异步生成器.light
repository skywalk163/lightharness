# R76 任务B - G-12 异步生成器 / 异步遍历消费
# 实测结论：本项**已支持**（R74-R75 期间已落地），本文件固化为回归护栏，防止将来退化。
#   异步 段落 + 生成        → async def + yield
#   异步 遍历 项 之 G        → async for 消费 async 生成器（examples/harness 生产代码已在用）
# 运行：python 运行.py examples/test_R76_G12_异步生成器.light   （期望 rc=0）

异步 段落 计数(上界):
  设 序号 为 0
  当 序号 小于 上界:
    生成 序号
    序号 等于 序号 加 1

异步 段落 主测试:
  设 累计 为 0
  设 个数 为 0
  异步 遍历 甲 之 计数(4):
    累计 等于 累计 加 甲
    个数 等于 个数 加 1
  如果 累计 != 6:
    抛出 "异步生成器求和失败: " + 转字符串(累计)
  如果 个数 != 4:
    抛出 "异步生成器元素个数失败: " + 转字符串(个数)
  打印 "--- G-12 异步生成器（async def + yield + 异步遍历 消费）全部通过 ---"

异步 运行 主测试()
