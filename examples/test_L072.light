# test_L072.light —— D批次验收：转字符串 字典输出（L-072）
# 缺陷：转字符串 对字典输出 Python repr（{'键': 1}），非友好格式。
# 验收结果：未修复（E批次验收标注）——当前仍输出 Python repr。
# 根因：stdlib/内置核心转换.light 的 转字符串 真身为 str(值)，对 dict
#       直接落 Python repr；格式变更涉及全仓断言兼容面，纳入后续批次。
# 本用例记录当前行为（repr 形态），后续修复时改为友好格式并更新本用例。

设 字典对象 为 {"键": 1}
设 文本 为 转字符串(字典对象)
打印 文本
打印 "test_L072 PASS"
