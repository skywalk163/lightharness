# test_文件.light —— 文件与 JSON 存储测试（P1）
# 反跑判据：把 JSON 读取断言改为相反值即红。

从 文件 导入 写JSON文件, 读JSON文件, 写文本文件, 读文本文件, 保证目录, 列目录

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 根 为 连接路径(当前目录(), "tmp_文件测试")
  保证目录(根)
  设 路径 为 连接路径(根, "数据.json")
  写JSON文件(路径, ["名字": "光明", "版本": 1])
  设 回读 为 读JSON文件(路径)
  断言等于(回读["名字"], "光明", "JSON 名字往返一致")
  断言等于(回读["版本"], 1, "JSON 版本往返一致")

  设 文本路径 为 连接路径(根, "说明.txt")
  写文本文件(文本路径, "lightharness")
  断言等于(读文本文件(文本路径), "lightharness", "文本往返一致")

  设 目录表 为 列目录(根)
  断言等于(长度(目录表), 2, "目录应有 2 个文件")

  删除文件(连接路径(根, "数据.json"))
  删除文件(连接路径(根, "说明.txt"))
  删除目录(根)
  打印 "test_文件 PASS"

主()
