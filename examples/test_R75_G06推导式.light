# R75 任务B - G-06 列表/字典推导式正向用例
# 验证：列表推导、字典推导、带条件推导、多重遍历推导

段落 断言等于(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  # 1. 基础列表推导
  设 平方 为 [x * x 遍历 x 之 范围(1, 6)]
  断言等于(平方, [1, 4, 9, 16, 25], "基础列表推导")

  # 2. 带条件的列表推导
  设 偶数 为 [x 遍历 x 之 范围(1, 11) 若 x % 2 == 0]
  断言等于(偶数, [2, 4, 6, 8, 10], "带条件列表推导")

  # 3. 字典推导
  设 平方字典 为 {x: x * x 遍历 x 之 范围(1, 4)}
  断言等于(平方字典, {1: 1, 2: 4, 3: 9}, "基础字典推导")

  # 4. 带条件的字典推导
  设 偶数字典 为 {x: x * x 遍历 x 之 范围(1, 6) 若 x % 2 == 0}
  断言等于(偶数字典, {2: 4, 4: 16}, "带条件字典推导")

  # 5. 推导式中使用函数调用
  设 长度列表 为 [长度(s) 遍历 s 之 ["a", "bb", "ccc"]]
  断言等于(长度列表, [1, 2, 3], "推导式中调用函数")

  # 6. 嵌套推导（多重遍历）
  设 配对 为 [(i, j) 遍历 i 之 范围(1, 3) 遍历 j 之 范围(4, 6)]
  断言等于(配对, [(1, 4), (1, 5), (2, 4), (2, 5)], "多重遍历推导")

  打印 "--- G-06 推导式测试全部通过 ---"

主()
