# test_R29_CCW精简边界.light —— CCW 37→10精简后边界测试
# 覆盖：保留10条真护栏的保护语义 + 删除27条的token序列不变

# === 保留条目的保护语义（10条真护栏）===

# 位运算符4条：撤后会被关键字劈开，必须保留
设 r1 为 位与(3, 1)
设 r2 为 位异或(3, 1)
设 r3 为 位或(3, 1)
设 r4 为 位非(3)

# 零除错误：撤后被CHINESE_NUM劈开，必须保留
捕获 零除错误:
    打印 "捕获零除错误"

# 应当："当"关键字在词首劈开，必须保留
如果 应当 那么:
    打印 "应当为真"

# 除非：双侧关键字夹击，必须保留
设 r5 为 除非(条件)

# 测试_生成问候语：ASCII下划线+Han粘连整词依赖CCW，必须保留
段落 测试_生成问候语:
    返回 "你好"

# 幂次：被关键字"幂"劈开，必须保留
设 r6 为 幂次(2, 10)

# 记录类型：语料0命中但边界形态G3失败，必须保留
设 r7 为 记录类型(用户)

# === 删除条目的token序列不变（27条已删，验证仍可编译）===

# A批删除5条（异步相关）
段落 异步写入文件测试:
    设 异步写入文件 为 真
    设 异步睡眠 为 真
    设 异步读取文件 为 真
    设 异步追加文件 为 真
    设 并发等待 为 真

# B批删除9条（错误类型+设置）
段落 设置相关测试:
    设 导入错误 为 真
    设 类型错误 为 真
    设 设指针 为 真
    设 设指针值 为 真
    设 设系统 为 真
    设 设系统错误码 为 真
    设 设置 为 真
    设 设置数组 为 真
    设 低级关闭 为 真

# C批删除7条（测试/生成+关键字）
段落 关键字相关测试:
    设 生成分析 为 真
    设 生成问候语 为 真
    设 最终答案 为 真
    设 当然 为 真
    设 当前 为 真
    设 终点 为 真
    设 开启调试 为 真

# D批删除6条（其他类型）
段落 其他类型测试:
    设 函数对象 为 真
    设 常量时间比较 为 真
    设 枚举值 为 真
    设 正则匹配 为 真
    设 环境枚举 为 真
    设 结构体值 为 真

打印 "CCW精简边界测试通过"
