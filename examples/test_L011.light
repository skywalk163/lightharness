# 缺陷：L-011 关键字根「等待」拆分成员访问（外部命令.等待进程 被拆成 外部命令.等待(...)）
# 当前：红——「module '外部命令' has no attribute '等待'」（等待进程 被关键字根拆分）
# 修复后：绿——等待进程 整体成词可调用，断言通过（此处允许使用 stdlib 外部命令 作复现载体）
导入 外部命令

段落 主:
  设 结果 为 外部命令.等待进程(空, 5)
  打印 转字符串(结果.标准输出)
  打印 "--- 测试L011 结束 ---"
  # 修复后启用：断言修复后()

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 进程 为 外部命令.后台执行("echo hi")
  设 结果 为 外部命令.等待进程(进程, 5)
  断言等于(长度(结果.标准输出) > 0, 真, "L-011 等待进程 应整体成词并捕获输出")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
