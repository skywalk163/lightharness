# 缺陷：L-011 关键字根「等待」拆分成员访问（外部命令.等待进程 被拆成 外部命令.等待(...)）
# 当前：绿（第11轮松绑）——L1 编译器修复后 等待进程 整段成词；主段落入参由 空 改为真实进程
#   （原 空 触发 NoneType AttributeError，属用例自身问题）
# 修复后：绿——等待进程 整体成词可调用，断言通过
导入 外部命令

段落 主:
  设 进程 为 外部命令.后台执行("echo hi")
  设 结果 为 外部命令.等待进程(进程, 5)
  打印 转字符串(结果.标准输出)
  打印 "--- 测试L011 结束 ---"
  断言修复后()

# 修复后断言段——当前已启用（第11轮松绑转绿）
段落 断言修复后:
  设 进程 为 外部命令.后台执行("echo hi")
  设 结果 为 外部命令.等待进程(进程, 5)
  断言等于(长度(结果.标准输出) > 0, 真, "L-011 等待进程 应整体成词并捕获输出")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()
