段落 主:
  设 映射 为 ["a": 1, "b": 2]
  打印 "包含键a: " 加上 转字符串(映射.包含("a"))
  打印 "包含键c: " 加上 转字符串(映射.包含("c"))
  打印 "获取b: " 加上 转字符串(映射.获取("b", 空))
  设 映射["c"] 为 3
  打印 "设后c: " 加上 转字符串(映射["c"])
  设 映射 为 ["标记": 假]
  如果 映射.包含("标记") 且 映射["标记"] == 假:
    打印 "条件倒置可用"
  打印 "--- 字典方法测试通过 ---"

主()
