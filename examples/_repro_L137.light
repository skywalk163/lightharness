# _repro_L137.light —— L-137 最小复现（绕法形态，rc=0）
# 现象：局部变量名以关键字「尝试」开头时，后续下标赋值/读取在关键字边界被断开——
#   `尝试["甲"] 为 1` 报「期望 '='、':' 或 '为'」（「尝试」被当关键字语句，
#   `["甲"]...` 悬空）；赋值静默失败的形态亦有。与 L-120（可被关键字序列完全切分）
#   / L-101（「回调」保留字）同族。
# 绕法：变量名完全避开「尝试」等关键字字样（改 活动项/尝试态不可用、用 运行项）。
# 本文件按绕法写，断言改名后字典下标读写正常；被破坏原写法见注释（不参与编译）。
# （被破坏原写法：`设 尝试 为 {}` 换行 `尝试["甲"] 为 1` —— 解析错误「期望 '='、':' 或 '为'」。）

段落 断言相等(实际, 期望, 标签):
  如果 实际 != 期望:
    抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

段落 主:
  # 绕法：变量名避开「尝试」关键字字样
  设 活动项 为 {}
  活动项["甲"] 为 1
  断言相等(活动项["甲"], 1, "改名后下标赋值")
  活动项["乙"] 为 活动项["甲"] + 1
  断言相等(活动项["乙"], 2, "改名后下标读取与运算")
  # 对照（不参与编译）：
  #   设 尝试 为 {}
  #   尝试["甲"] 为 1   —— 解析错误
  打印 "L-137 绕法形态复现通过（rc=0）"

主()
