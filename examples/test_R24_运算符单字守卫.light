# test_R24_运算符单字守卫.light —— 第24轮 任务1：运算符单字守卫覆盖（运行期自校验）
#
# 单字运算符（加/减/乘/除/模）的两侧语义必须同时成立：
#   · 表达式位置（无空格 / 带空格）：`甲加乙`、`甲 加 乙` 都要切出运算符；
#   · 构词位置（词首/词中/词尾）：`加法`、`减法`、`乘法表`、`余数`、`除号`、
#     `增加量` 必须整体成词并可直接当变量名。
# 本文件覆盖运行期结果；token 层精确序列由 lightharness/tests/test_R24_的递归_token.py 覆盖。
#
# 已记录、本轮**不修**的缺口（见交付报告「运算符守卫验证」表）：
#   · `甲取余乙`：`取余` 是多字运算符，未登记在 OPERATOR_VERBS（无空格取余会被
#     当作复合词并入标识符）；带空格写法 `甲 取余 乙` 正确。故本文件不写该断言。
#   · `甲余乙`：`余` 是 arity=1 的列表动词（取余下元素），不是中缀运算符，
#     故 `甲余2` 合并为标识符属预期行为，不构成守卫漏洞。
# 全部 rc=0 自校验。

段落 断言相等 接收 实际, 期望, 标签:
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))


设 甲 为 3
设 乙 为 4

# ---- 一、无空格二元运算符（本语言一等写法）----
断言相等(甲加乙, 7, "甲加乙 无空格加法")
断言相等(甲减乙, -1, "甲减乙 无空格减法")
断言相等(甲乘乙, 12, "甲乘乙 无空格乘法")
断言相等(甲除乙, 0.75, "甲除乙 无空格除法")
断言相等(甲模乙, 3, "甲模乙 无空格取模")

# ---- 二、带空格写法等价 ----
断言相等(甲 加 乙, 7, "甲 加 乙 带空格加法")
断言相等(甲 减 乙, -1, "甲 减 乙 带空格减法")
断言相等(甲 乘 乙, 12, "甲 乘 乙 带空格乘法")
断言相等(甲 除 乙, 0.75, "甲 除 乙 带空格除法")
断言相等(甲 取余 乙, 3, "甲 取余 乙 带空格取余")
断言相等(甲 加 乙, 甲加乙, "两种写法结果一致")

# ---- 三、ASCII/中文数字操作数：汉字段后紧跟数字时必须切出运算符 ----
断言相等(甲加1, 4, "甲加1 数字右操作数")
断言相等(甲减一, 2, "甲减一 中文数字右操作数")
断言相等(甲乘2, 6, "甲乘2 数字右操作数")
设 基 为 甲加1
断言相等(基, 4, "运算符表达式可赋给变量")

# ---- 四、运算符单字作构词成分：整体成词且可当变量名 ----
设 加法 为 10
设 减法 为 20
设 乘法表 为 30
设 余数 为 40
设 除号 为 50
设 除数 为 60
设 增加量 为 7
断言相等(加法 + 减法 + 乘法表 + 余数 + 除号 + 除数 + 增加量, 217, "运算符构词标识符可参与运算")

设 算式表 为 [加法, 减法, 乘法表]
断言相等(算式表[2], 30, "运算符构词标识符可作列表元素")

# ---- 五、值字面量单字与运算符相邻时不得互相吞并 ----
设 是否为零 为 假
断言相等(是否为零, 假, "是否为零 整体成词")

打印 "test_R24_运算符单字守卫 全部通过"
