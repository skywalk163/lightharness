# 缺陷：L-005 非 self 成员下标访问被误编译成函数调用（对象.表[键] → 对象.表([键])）
# 当前：红——「'dict' object is not callable」（监听器表["点击"] 被编译成 监听器表(["点击"])）
# 修复后：绿——下标读取按字面语义执行，取回已挂载的值，断言通过
类 总线:
  属性 监听器表 等于 空
  段落 构造:
    设 己.监听器表 为 {}
  段落 挂载 接收 事件名, 处理器:
    己.监听器表[事件名] 为 处理器

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  设 总线对象 为 新建 总线()
  总线对象.挂载("点击", 42)
  设 表 为 总线对象.监听器表["点击"]
  断言等于(表, 42, "L-005 下标读取应取回已挂载的值 42")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  设 总线对象 为 新建 总线()
  总线对象.挂载("点击", 42)
  设 表 为 总线对象.监听器表["点击"]
  打印 转字符串(表)
  打印 "--- 测试L005 结束 ---"
  # 修复后启用：断言修复后()

主()
