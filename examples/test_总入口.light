# test_总入口.light —— CLI 主入口测试（P0）
#
# 覆盖：
#   a) 执行bash 工具真实执行外部命令并回传输出
#   b) 组装注册表 注册 bash 工具
#
# 反跑判据：把 (a) 的输出断言 "hello-bash" 改成 "world" 即红。

从 总入口 导入 执行bash, 组装注册表

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  # a) 执行bash 真实执行
  设 结果 为 执行bash(["command": "echo hello-bash"])
  断言等于(结果[0]["text"].去除空白(), "hello-bash", "bash 工具应返回命令输出")

  # b) 注册表
  设 注册表 为 组装注册表()
  断言等于(长度(注册表.名单()), 1, "注册表应有 1 个工具")
  断言等于(注册表.名单()[0], "bash", "工具名应为 bash")

  打印 "test_总入口 PASS"

主()
