# test_L049.light —— L-049 回归：捕获 错误 as 变量 绑定子句
# 运行：python 运行.py examples/test_L049.light
#
# 缺陷 L-049：光明 `捕获 错误 as 变量:` 的 `as 变量` 绑定形式不生效，
# 变量未正常绑定。修复后 `as` 与 `为` 均可绑定异常对象到变量。
#
# 测试覆盖：
#   1. 捕获 错误 as 变量: —— as 绑定 ASCII 名
#   2. 捕获 错误 as 错误对象: —— as 绑定中文名
#   3. 捕获 异常 as exc: —— 异常类型 + as 绑定
#   4. 为 绑定仍兼容（回归）
#   5. 绑定变量在捕获体内可正常使用

段落 断言相等 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 新建 错误("断言失败[" + 标签 + "]：实际=" + 序列化JSON(实际) + " 期望=" + 序列化JSON(期望))

段落 断言真 接收 条件, 标签:
  如果 条件 == 假:
    抛出 新建 错误("断言失败: " + 标签)

段落 主:
  # 1. 捕获 错误 as 变量: ASCII 变量名
  设 捕获到 为 假
  设 错误文本 为 ""
  尝试:
    抛出 新建 错误("as绑定ASCII")
  捕获 错误 as e:
    设 捕获到 为 真
    设 错误文本 为 转字符串(e)
  断言真(捕获到, "as ASCII: 应捕获到异常")
  断言相等(错误文本, "as绑定ASCII", "as ASCII: 错误消息应正确绑定")

  # 2. 捕获 错误 as 错误对象: 中文变量名
  设 捕获到2 为 假
  设 错误文本2 为 ""
  尝试:
    抛出 新建 错误("as绑定中文")
  捕获 错误 as 错误对象:
    设 捕获到2 为 真
    设 错误文本2 为 转字符串(错误对象)
  断言真(捕获到2, "as 中文: 应捕获到异常")
  断言相等(错误文本2, "as绑定中文", "as 中文: 错误消息应正确绑定")

  # 3. 捕获 异常 as exc: 异常类型 + as 绑定
  设 捕获到3 为 假
  尝试:
    抛出 新建 错误("异常类型as")
  捕获 异常 as exc:
    设 捕获到3 为 真
  断言真(捕获到3, "异常 as: 应捕获到异常")

  # 4. 为 绑定仍兼容（回归）
  设 捕获到4 为 假
  设 错误文本4 为 ""
  尝试:
    抛出 新建 错误("为绑定回归")
  捕获 错误 为 err:
    设 捕获到4 为 真
    设 错误文本4 为 转字符串(err)
  断言真(捕获到4, "为 绑定: 回归应兼容")
  断言相等(错误文本4, "为绑定回归", "为 绑定: 错误消息应正确")

  # 5. 无绑定捕获仍兼容（回归）
  设 捕获到5 为 假
  尝试:
    抛出 新建 错误("无绑定")
  捕获 错误:
    设 捕获到5 为 真
  断言真(捕获到5, "无绑定: 回归应兼容")

  打印 "L-049 测试通过"

主()
