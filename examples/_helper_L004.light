# L-004 辅助模块：定义含关键字开头成员名的类
# 定义侧（本模块）预扫描把「导出事件表」整体收进白名单，编译正常；
# 调用方模块不认识该成员名，词法器按关键字边界把「导出事件表」拆成 导出+事件表。
类 会话对象:
  属性 事件表 等于 空
  段落 构造:
    设 己.事件表 为 []
  段落 导出事件表 接收 数据:
    己.事件表.追加(数据)
    返回 己.事件表
