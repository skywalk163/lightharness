# 缺陷：L-032 `父` 是 super() 关键字，作普通变量名被注入 super 调用导致运行期崩溃。
#        `段落` 内 `设 父 为 取目录(当前)` 被编译成 super(...) 调用，
#        运行期报 super(): no arguments / __class__ cell not found。
# 修复前：红——下列用 `父` 作局部变量的场景编译失败或运行期崩溃。
# 修复后：绿——普通段落/赋值目标里 `父` 是普通标识符，可赋值、读取；类方法内
#        `父.方法(...)` 的 super 语义保持不变（防过度修复护栏见下方测试类）。
# 载体：自带最小用例，不依赖其它模块。

段落 取父值 接收 当前:
  设 父 为 当前
  返回 父

段落 主:
  # 场景1：普通函数内 `父` 作局部变量（赋值+读取+返回）
  断言等于(取父值("root"), "root", "L-032 函数内 父 作局部变量")

  # 场景2：`父` 作为右值（读取一个名为 父 的已定义变量）
  设 父 为 "module_level"
  断言等于(父, "module_level", "L-032 模块级变量名 父")

  # 场景3：类方法内 `父.方法(...)` 的 super 语义不被破坏（防过度修复护栏）
  设 子实例 为 新建 子类()
  断言等于(子实例.打招呼(), "父类问候-子类", "L-032 类内 父.方法 super 语义不变")

  打印 "--- 测试L032 通过 ---"

类 测试基类:
  段落 问候:
    返回 "父类问候"

类 子类 继承 测试基类:
  段落 打招呼:
    设 前缀 为 父.问候()
    返回 前缀 + "-子类"

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

主()