从 文件 导入 原子写文本, 读文本文件, 存在, 删除路径, 建临时目录, 拼路径, 列目录

段落 主:
  设 临时目录 为 建临时目录()
  设 测试路径 为 拼路径(临时目录, "test_atomic.txt")

  # 测试1：原子写入新文件
  设 结果1 为 原子写文本(测试路径, "hello world")
  断言 结果1 == 真, "原子写入应返回真"
  断言 存在(测试路径) == 真, "文件应存在"
  设 内容1 为 读文本文件(测试路径)
  断言 内容1 == "hello world", "文件内容应为hello world"
  打印("测试1通过：原子写入新文件")

  # 测试2：原子覆盖已有文件
  设 结果2 为 原子写文本(测试路径, "updated content")
  断言 结果2 == 真, "原子覆盖应返回真"
  设 内容2 为 读文本文件(测试路径)
  断言 内容2 == "updated content", "文件内容应为updated content"
  打印("测试2通过：原子覆盖已有文件")

  # 测试3：临时文件应被清理（检查没有.tmp文件残留）
  设 目录内容 为 列目录(临时目录)
  遍历 名 之 目录内容:
    如果 名.包含(".tmp."):
      断言 假, "不应有临时文件残留: " + 名
  打印("测试3通过：临时文件已清理")

  # 测试4：写入空内容
  设 空路径 为 拼路径(临时目录, "empty.txt")
  设 结果4 为 原子写文本(空路径, "")
  断言 结果4 == 真, "空内容写入应返回真"
  设 内容4 为 读文本文件(空路径)
  断言 内容4 == "", "空文件内容应为空"
  打印("测试4通过：写入空内容")

  # 测试5：写入中文内容
  设 中文路径 为 拼路径(临时目录, "中文.txt")
  设 结果5 为 原子写文本(中文路径, "你好世界，这是中文内容")
  断言 结果5 == 真, "中文写入应返回真"
  设 内容5 为 读文本文件(中文路径)
  断言 内容5 == "你好世界，这是中文内容", "中文内容应正确"
  打印("测试5通过：写入中文内容")

  # 清理
  删除路径(测试路径)
  删除路径(空路径)
  删除路径(中文路径)
  删除路径(临时目录)

  打印("--- 原子写文本v2测试通过 ---")

主()
