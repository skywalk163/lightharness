# test_L120.light —— L-120 最小复现：标识符可被关键字序列完全切分
# 缺陷：捕获 异常 错误己: 中 错误己 被切成 错误+己（己 是 L0 单字别名）而解析报错；
#       错误甲（甲 非关键字）正常。全切分判定应让整串优先成标识符。
# 当前（修复前）：立红（rc≠0）
# 修复后：错误己 作为异常变量名正常使用

段落 主:
  尝试:
    抛出 新建 错误("测试错误")
  捕获 异常 错误己:
    打印 转字符串(错误己)
  打印 "test_L120 PASS"

主()
