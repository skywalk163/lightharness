# test_R23_己词尾并入.light —— 第23轮 任务1/任务4：己 词尾并入标识符（运行期自校验）
#
# 背景：_TRAILING_ALIAS_MERGE（R23任务1处置）仅存 `己`——单字语句别名在汉字段
#       词尾并入标识符（L-120 家族）。本文件验证词尾并入行为及其反例（词首仍作语句别名）。
# 全部 rc=0 自校验。

段落 断言相等(实际, 期望, 标签):
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))

# ---- 一、词尾并入：含 己 的复合标识符整体成词 ----
设 自己 为 "me"
断言相等(自己, "me", "自己 词尾己并入")

设 错误己 为 "err"
断言相等(错误己, "err", "错误己 词尾己并入")

设 本己录 为 [1, 2, 3]
断言相等(本己录[1], 2, "本己录 词尾己并入且可下标")

# ---- 二、词中/词尾组合 ----
段落 记己(输入):
    返回 输入 + 1

设 记己结果 为 记己(41)
断言相等(记己结果, 42, "记己 段名词尾己并入且可调用")

# ---- 三、词首反例：己 作为语句别名单字仍受语句语义管辖 ----
# （己 词首是语句别名关键字；本用例验证普通名字不受影响即可，
#   词首语句形态由 pytest token 层覆盖，此处不写会改变控制流的语句）

# ---- 四、边界混合：自己 出现在表达式/实参/列表 ----
设 问候 为 "你好, " + 自己
断言相等(问候, "你好, me", "自己 参与字符串拼接")

设 名单 为 [自己, 错误己]
断言相等(名单[0] + 名单[1], "meerr", "列表元素含己尾复合名")

打印 "test_R23_己词尾并入 全部通过"
