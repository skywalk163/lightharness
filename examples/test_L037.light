# test_L037.light —— L-037 回归：断言(条件, 标签) 必须真正生效
# 运行：python 运行.py examples/test_L037.light
#
# 缺陷：`断言(条件, 标签)` 括号写法被 parser 解析成 AssertStmt(条件=元组, 消息=空)，
#       旧生成 emit `assert (条件, 标签)` —— 非空元组恒真，断言永不抛错，
#       所有基于 断言/断言抛出 的判据都是静默 no-op。
# 修复：生成 `assert 条件, 标签`（逗号置于括号外），条件为假时抛 AssertionError 附标签。

# 真正生效的抛错检查：捕获并核对错误信息是否含标签
段落 检查抛错 接收 标签, 运算:
  设 抛了 为 假
  设 消息 为 ""
  尝试:
    运算()
  捕获 错误 为 e:
    设 抛了 为 真
    设 消息 为 转字符串(e)
  如果 抛了 == 假: 抛出 新建 错误("应抛未抛: " + 标签)
  返回 消息

# 场景1：条件为真，括号两参不抛
断言(真, "不该抛")

# 场景2：条件为假，必须抛错，且标签进入错误信息（修复前的元组恒真 bug 在此暴露）
段落 抛假带标签:
  断言(假, "标签A")
设 消息1 为 检查抛错("假断言应抛且带标签", 抛假带标签)
如果 字符串包含(消息1, "标签A"):
  写 "OK: 假断言抛错且带标签"
否则:
  抛出 新建 错误("标签未进入错误信息: " + 消息1)

# 场景3：单参断言（真）不回归
断言(1 == 1)

# 场景4：单参断言（假）仍会抛
段落 抛假单参:
  断言(假)
设 消息2 为 检查抛错("单参假断言应抛", 抛假单参)

# 场景5：无括号两参写法不回归
断言 1 + 1 == 2, "无括号两参"

写 "--- 测试L037 通过 ---"
