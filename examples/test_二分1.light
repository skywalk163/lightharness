# 二分法：定位 事件表 拆分的触发源
类 测试类F:
  属性 事件表 等于 空
  段落 构造:
    设 己.事件表 为 []
  段落 记录事件 接收 类型:
    设 事件记录 为 ["类型": 类型]
    己.事件表.追加(事件记录)
    返回 事件记录
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类F()
  对象.记录事件("turn/start")
  打印 "F数量: " 加上 转字符串(对象.数量())
  打印 "--- 测试F结束 ---"

主()
