# test_R27_词首并入混合.light —— 第27轮 任务4：16字词首并入 + 切分 混合场景（运行期自校验）
#
# 任务书 4.3 要求：同一作用域内 正向（整词成 IDENTIFIER）与 反向（运算符/值字面量
# /范围/下标切分）交织，验证两条规则互不串扰。
# 全部 rc=0 自校验。

段落 断言相等(实际, 期望, 标签):
    如果 实际 != 期望:
        抛出 新建 错误("断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望))


# ===== 1. 乘法 为 7; 如果 乘法 大于 5 那么 返回 乘法 =====

段落 测试_乘法混合:
    设 乘法 为 7
    如果 乘法 大于 5:
        返回 乘法
    返回 0

断言相等(测试_乘法混合(), 7, "乘法：整词与语句切分混合")

# ===== 2. 真空 为 对; 返回 真 =====

段落 测试_真空混合:
    设 真空 为 1
    如果 真空:
        返回 真
    返回 空

断言相等(测试_真空混合(), 真, "真空/真：并入与值字面量共存")

# ===== 3. 配置 为 段; 配置[0] 为 1 =====

段落 测试_配置下标:
    设 段 为 [5, 6]
    设 配置 为 段
    返回 配置[0] 加 配置[1]

断言相等(测试_配置下标(), 11, "段/配置：下标访问与整词混合")

# ===== 4. 加法 为 甲 加 乙; 返回 加法 =====

段落 测试_加法混合:
    设 甲 为 4
    设 乙 为 3
    设 加法 为 甲 加 乙
    返回 加法

断言相等(测试_加法混合(), 7, "加法/加：并入名与运算符同表达式")

# ===== 5. 段落名与下标、类别与类声明混合 =====

类 类别:
    段落 取值:
        返回 99

段落 测试_类别混合:
    设 分类值 为 新建 类别()
    设 列表值 为 [分类值.取值()]
    返回 列表值[0]

断言相等(测试_类别混合(), 99, "类别：类名/类声明/下标混合")

# ===== 6. 是否 与 真假值 混合 =====

段落 测试_是否混合:
    设 是否 为 真
    如果 是否:
        设 的确 为 "是"
    否则:
        设 的确 为 "否"
    返回 的确

断言相等(测试_是否混合(), "是", "是否/的确：B类并入名与控制流混合")

# ===== 7. 模组 与 取模 混合 =====

段落 测试_模混合:
    设 模组 为 10
    设 余数 为 模组 模 3
    返回 余数

断言相等(测试_模混合(), 1, "模：模组整词 + 模 运算符切分")

# ===== 8. 到位/到达 与 范围遍历 混合 =====

段落 测试_到混合:
    设 到位计数 为 0
    遍历 到位序号 于 1 至 3:
        设 到位计数 为 到位计数 加 到位序号
    返回 到位计数

断言相等(测试_到混合(), 6, "到：到位系列整词与范围切分混合")

# ===== 9. 自己/自然 与 成员访问 之 混合 =====

类 自己类:
    段落 初始化(命名):
        己.命名 = 命名
    段落 自报:
        返回 己.命名

段落 测试_自混合:
    设 实例 为 新建 自己类("测试")
    返回 实例.自报()

断言相等(测试_自混合(), "测试", "自：己/之 语义不受并入影响")

段落 主:
    打印("R27 词首并入混合场景测试全部通过（9 组）")
    返回 真

主()
