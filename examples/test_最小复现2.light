# 最小复现2：多个方法调用 己.记录事件
类 测试类J:
  属性 事件表 等于 空
  段落 构造:
    设 己.事件表 为 []
  段落 记录事件 接收 类型:
    设 事件记录 为 ["类型": 类型]
    己.事件表.追加(事件记录)
    返回 事件记录
  段落 记录消息事件 接收 类型, 数据:
    返回 己.记录事件(类型)
  段落 记录工具结果 接收 类型:
    返回 己.记录事件(类型)
  段落 记录轮次开始:
    返回 己.记录事件("turn/start")
  段落 数量:
    返回 长度(己.事件表)

段落 主:
  设 对象 为 新建 测试类J()
  对象.记录消息事件("user/message", "x")
  对象.记录工具结果("tool/result")
  对象.记录轮次开始()
  打印 "J数量: " 加上 转字符串(对象.数量())
  打印 "--- 测试J结束 ---"

主()
