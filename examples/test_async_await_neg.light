# R70 任务B 反向用例：在「同步 段落」里误用「等待」应报中文编译错误
# 期望：编译失败，且报错含「只能写在 异步 段落 里面」
#
# 运行：python 运行.py examples/test_async_await_neg.light
# 期望：非零退出码 + 报错「「等待」只能写在 异步 段落 里面；当前位于同步 段落...」

段落 主:
    设 x 为 1
    等待 异步睡眠(0.1)   # ← 错误：同步段落里不能用 等待
    打印(x)
