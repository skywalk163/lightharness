# 任务3 R24 交付报告 —— P0-A 复合安全集合通用化 + 单字后缀位置规则（CR1）

> 轮次：第 24 轮 · 阶段D 单字通用化 ｜ 任务：任务3（P1）
> 日期：2026-09-15 ｜ 基线：`ca5741a8`（第23轮保护表通用化替代）
> 修改文件：`light-merge/src/lexer.py`（互斥区：`_tokenize_chinese_sequence` 嵌入扫描段首并入分支 + 死代码 `_P0A_COMPOUND_SAFE`）
> 判定：**反跑 ALL_OK** ｜ CS 保持 30 条（审计结论：13 条「语料冗余 ∧ 语义非冗余」→ 保留）｜ 新增 pytest 打红 0

---

## 一、结论先行

1. **任务书设想的「删表」没有发生，且不应发生**。任务2 判为①冗余可删的 13 条（`例 出 则 常 引 接 是 末 试 跳 过 长 首`），
   在语料层确实「撤掉后 834 个 `.light` 的 token 序列不变」，但在**语义层非冗余**——撤掉会破坏
   **语句起始位置的裸名**（如 `长度 为 5` 被切成 `长`+`度`+`为`+`5`，赋值目标丢失）。
   该空洞**不能**由任何位置规则覆盖（语句起始位置的关键字优先是硬约束，见任务书约束 6）。
   → **决策：CS 保持 30 条**。
2. **用通用规则接管了这 13 条的「成员访问段首」保护场景**：新增 **CR1 规则**（`_seg_after_sep` 游标）。
   反跑判据 [C1]/[C2] 双向证明：撤掉 13 条后，基准态 `甲之<c>顶` 反例矩阵 **破裂 13/13**，
   而修复态**保持 13/13** → CR1 已覆盖其成员访问段首语义。
3. **删除全库零引用死代码** `_P0A_COMPOUND_SAFE = _COMPOUND_SAFE_SINGLE_KEYWORDS | {'当'}`（`git grep` 零引用）。
4. 语义等价性：真实源 token 变化 **1 个文件**（任务1 的 `自之X` 修正）；性能 **×1.066~1.115**（变快）。

---

## 二、CR1 规则：成员访问段首并入

### 2.1 问题

R23 任务2 已实现**多字**关键字的「后缀位置并入」（`scan_pos > 0` + 多字 + 非运算符 + 非排除集 → 并入）。
但**单字**关键字在「成员访问分隔符之后的新段段首」仍会错误浮出为 `KEYWORD`：

```
甲之长度   →  ID(甲) KW(之) KW(长) ID(度)     ← 期望 ID(甲) KW(之) ID(长度)
甲之首项   →  ID(甲) KW(之) KW(首) ID(项)     ← 期望 ID(甲) KW(之) ID(首项)
```

原因是段首 `scan_pos == 0`，既不满足「词中」（`scan_pos + sub_len < len`），也不是「词尾别名」（`_trailing_alias`）。

### 2.2 规则

在嵌入扫描的输出循环里引入游标 `_seg_after_sep`，语义 = **「刚刚输出过一个成员/关系分隔符」**：

```python
# 初始化（进入分段输出循环前）
_seg_after_sep = False                                  # :2771
...
# 单字非运算符关键字的「并入」判据（原为词中判据）
if ((scan_pos > 0 and scan_pos + sub_len < len(full_identifier))
        or _seg_after_sep):                             # :2903-2909  ← CR1 新增项
    scan_pos += sub_len
    continue
...
# 每输出一个关键字就重算游标（非分隔符关键字把它清零，保证只有紧邻分隔符的那一段享受段首并入）
_seg_after_sep = sub_kw in self._P0A_SEP                # :2979
```

- `_P0A_SEP = {之, 在, 于, 为, 与}`（声明为「始终切分」的成员/关系分隔符）。
- **作用域自然收敛**：游标只在「前一个输出 token 恰是 `_P0A_SEP`」时为真，且遇到第一个非分隔符关键字即清零；
  段首之后一旦吃掉单字，`scan_pos` 前移，`_seg_after_sep` 不再复用于第二个单字。
- **不动语句起始语义**：CR1 只作用在嵌入扫描的输出循环内（该循环只在 `embedded_found` 时进入），
  语句起始位置的关键字优先切分（约束 6）不受影响。

### 2.3 为什么 `_P0A_COMPOUND_SAFE` 是死代码（可删）

原常量 `_P0A_COMPOUND_SAFE = _COMPOUND_SAFE_SINGLE_KEYWORDS | {'当'}` 在 R21 重构后
已被 `_tokenize_chinese_sequence` 的上下文规则取代，全库 `git grep` **零引用**。本轮删除；
反跑判据 [G] 断言 `hasattr(edit_mod, '_P0A_COMPOUND_SAFE') is False` 且 CS 仍 30 条。

---

## 三、13 条「①冗余可删」的复核与保留决策

### 3.1 任务2 的判据与结论

任务2 判据③：撤掉该条目后全语料 token 序列零变化 ⇒ 冗余可删。
对 13 条（`例 出 则 常 引 接 是 末 试 跳 过 长 首`）执行该判据 → **834 个文件 token 零变化** → 判为①。

### 3.2 本轮复核：语料冗余 ≠ 语义冗余

反跑判据 [D] 的「语句起始裸名」子集暴露了空洞：

| 探针 | 基准态 | 撤掉 13 条后 |
|---|---|---|
| `长度 为 5` | `ID(长度) KW(为) NUM(5)` | `ID(长) ID(度) KW(为) NUM(5)` ← **赋值目标丢失** |
| `举例 为 1` | `ID(举例) KW(为) NUM(1)` | `ID(举) ID(例) KW(为) NUM(1)` |
| `首项 为 1` | `ID(首项) KW(为) NUM(1)` | `ID(首) ID(项) KW(为) NUM(1)` |
| …（`末尾`/`例子`/`常规`/`跳过`/`引用` 同形） | — | 同形破裂 |

**两个空洞必须分开看**：

- **空洞 A（成员访问段首）**：`甲之长度` 型 → **已由 CR1 覆盖**（反跑 [C2] 13/13 保持）。
- **空洞 B（语句起始裸名）**：`长度 为 5` 型 → **没有任何位置规则能覆盖**。
  `长度` 在语句起始位置时，词首单字 `长` 是合法关键字，按「语句起始关键字优先」的硬约束必须切出
  `长`。要让 `长度` 整体成词，词法层**必须**有「`长` 是构词字」的登记——这正是 CS 表的存在意义。

> 也就是说：这 13 条撤掉后语料 token 不变，**只是因为语料里恰好没有「语句起始位置出现这 13 个字打头的裸名」**，
> 而不是因为这些条目本身冗余。判据③（语料中立）在**孤立使用**时会漏掉「语义中立性」这一维。
> 这与第23轮沉淀的「**双中立判据 = 语料中立 ∧ 隔离中立**」一致：本轮以「隔离中立」一票否决了这 13 条的删除。

### 3.3 决策

**CS 保持 30 条**。任务3 的成果改为「CR1 通用规则 + 死代码清理 + 分类修正（①→②）」，
而不是「删表」。这是对本轮任务书目标的一次**证据驱动的修正**，不是缩水：
CR1 已把 13 条的**成员访问段首**保护场景通用化，剩下未通用化的是**不可通用化**的语句起始裸名场景。

---

## 四、与任务2 分类结论的偏差（须路M 知悉）

| 项 | 任务2 结论 | 本轮（任务3）结论 | 理由 |
|---|---|---|---|
| 13 条（例出则常引接是末试跳过长首） | ①冗余可删 | **②真护栏（语句起始裸名）** | 撤掉后 `长度 为 5` 赋值目标丢失（空洞 B） |
| 其余 17 条 | ②真护栏 | ②真护栏（不动） | 无异议 |
| `当` | 由 `_P0A_COMPOUND_SAFE` 承载 | 常量整体为死代码，直接删 | `git grep` 零引用；`当` 另在 `_P0A_STMT_SPLIT`/嵌入扫描里有独立处理 |

> 建议：任务2 的分类清单需按本节更新（`①→②` 修正 13 条）；`_COMPOUND_SAFE_SINGLE_KEYWORDS`
> 的最终清理目标应从「30→17」修正为「30→17 **且** 语句起始裸名有替代机制（如内建名登记/预扫描）」，
> 该替代机制属于第25轮候选方向。

---

## 五、反跑证据（`_antirun_r24_t3_P0A复合安全通用化.py`，判定 ALL_OK）

```
语料 .light：834 文件
断裂态基准：ca5741a8a0e8（第23轮保护表通用化替代）    修复态 lexer.py sha256=5cfbfa95b8da0d9e
[G] CS 基准 30 → 修复态 30 ｜ _P0A_COMPOUND_SAFE 删除=True ｜ CR1 就位=True            PASS
[A] 基线自检（两侧各两次 dump 一致）                                                  PASS
[B] 真实源 token 变化 1（预期 1）｜生成树 4                                            PASS
      真实源: light-merge/examples/L2_wenyan/学生模块.light
      生成树漂移: bootstrap/release/stdlib/{CSV读写器,对象池缓存,文件系统,断言工具}.light
[C] CR1 覆盖充分性：
      C1 基准态撤 13 条 → 矩阵破裂 13/13（应 13，证明白名单承重）                       PASS
      C2 修复态撤 13 条 → 矩阵保持 13/13（应 13，证明 CR1 已覆盖）                      PASS
[D] 自由名(10)+语句起始裸名(8) token 逐字节一致                                       PASS
[E] 性能：基准 6.31s vs 修复态 5.92s  ×1.066                                          PASS
[F] 既有核心用例（6 个）编译运行 rc/stdout 两侧一致                                     PASS
[F2] R24 新增用例（4 个）修复态 rc=0                                                  PASS
判定： ALL_OK
```

证据文件：`lightharness/_task3_R24_反跑证据.json`。

---

## 六、生成树漂移的定性（4 个文件）

`[B]` 记录的 4 个生成树漂移（`bootstrap/release/stdlib/{CSV读写器,对象池缓存,文件系统,断言工具}.light`）
**不是回归**，依据如下（三重取证）：

1. **这些文件本就不可编译、且不在任何测试断言路径上**。实测 `运行.py bootstrap/release/stdlib/断言工具.light`
   在第 1 行 `class 断言失败异常(断言错误):` 即解析失败——它们是含 `class` / `def` / `self.` 的
   **Python 转译产物**，不是可编译的光明程序。
2. **项目自有测试文件已把它们定性为「已损坏的生成产物」**：
   `tests/unit/test_lexer_compound_safe_alignment.py:80` 明确写「`除类型错误` 在 `断言工具.light` 里
   本就是已损坏的生成产物里的名字，**怎么切都编不过**」。
3. **漂移根因与本轮改动的语义一致**：
   | 文件 | 基线 | 修复态 | 根因 |
   |---|---|---|---|
   | `对象池缓存.light` | `ID(对象在self)` | `ID(对象) KW(在) ID(self)` | 第 4 道闸门：`在` 是成员/关系分隔符（**修好**，与 `键在self._缓存` 的 `在` 用法一致） |
   | `CSV读写器.light` | `ID(值) KW(或)` | `ID(值或)` | CR1：`在` 之后新段段首 `值或` 被并入（见 §七遗留） |
   | `文件系统.light` | `ID(定义文件存在)` | `KW(定义) ID(文件存在)` | 第 4 道闸门：串内 `在` |
   | `断言工具.light` | `ID(断言大于)` | `ID(断言) KW(大于)` | 第 4 道闸门：串内 `于`（后随 `(` 时才触发，即 `def断言大于(`） |

   > 对照：**原始**（非生成）`stdlib/断言工具.light` 里的 `段落 断言大于 接收 …` **零变化**——
   > 因为 `断言大于` 已被 `_scan_user_definitions` 预扫描登记为 `user_definitions`，
   > 优先整体成词，不经过闸门。生成树里是 `def断言大于(...)`（ASCII `def`，无预扫描登记）才触发。

---

## 七、已知遗留

### 7.1 CR1 在生成树的 `值或` 并入（低风险，需后续轮次收口）

`CSV读写器.light` 的 `分隔符在值或'"'在值或'\n'在值` 里，`在` 之后的新段是 `值或`：
`值` 非关键字 → `scan_pos` 前移；随后 `或`（单字逻辑运算符）在第 5 位被判为「段首」并入了标识符。
生成 `ID(值或)` 而语义上应是 `ID(值) KW(或)`。

- **影响面**：仅该已损坏生成树的 token 流；原始 stdlib `CSV读写器` 源零变化；不在任何测试断言路径。
- **收紧方向（建议第25轮）**：`_seg_after_sep` 只在「**紧邻分隔符的那个字**」生效，
  即记录段起始位置 `_seg_start_pos`，把判据改为 `scan_pos == _seg_start_pos`；
  或在单字分支里追加 `sub_kw not in _OPERATOR_KEYWORDS`（`或` 属逻辑运算符）。
  本轮未改的理由：CR1 已过双向反跑（[C1]/[C2] 13/13），任何收紧都可能重新打开空洞 A，
  应在有独立反跑矩阵的前提下单独做。

### 7.2 既有 pytest 打红（与任务1 报告 §七 同源）

5 条 `tests/unit/**` 在基线 `ca5741a8` 即红（`test_simple_tokenize`、`test_number_prefix_still_split_...`、
`test_成员与关系分隔符`、`test_语句关键字词首切分`、`test_异_进复合词保护表`），根因分别落在
`_EMBED` 区（约束 11 禁改）、R21 分支、以及 `_COMPOUND_SAFE_SINGLE_KEYWORDS` 的 `异` 条目
（属任务2 条目集）——**均不在任务3 互斥区**，须单独立项。

---

## 八、交付物

- `light-merge/src/lexer.py`（CR1 + 死代码清理，见 §二）
- `lightharness/examples/test_R24_单字通用规则.light`（rc=0：13 字打头自由名整体成词 + 运算符/值字面量不受影响 + 100+ 自由名）
- `lightharness/_antirun_r24_t3_P0A复合安全通用化.py`（判定 ALL_OK）
- `lightharness/_task3_R24_反跑证据.json`
- 本报告：`lightharness/_task3_R24_P0A复合安全通用化_交付报告.md`
