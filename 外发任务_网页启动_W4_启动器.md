# 外发任务 W4：lightharness 启动器升级（运行Web服务器.light 独占）

> 定位：本任务是"lightharness 网页启动"四路并行中的 **W4 启动器**。把
> `examples/运行Web服务器.light` 从"打印 URL 的 API 服务"升级成"一条命令 → 打印带 token 的
> 登录 URL → 自动打开浏览器"的完整启动体验。只改白名单文件。
>
> 并行依赖：按 `总览.md` 契约 2 调用 W2 的 `认证.light`；按契约 4 给 W3 的 `配置` 传新参数。
> 两路未合入时可用本地桩（同签名）先行，集成时换真。

## 白名单文件（只改这一个）

- 独占修改 `examples/运行Web服务器.light`

## 功能要求（按总览契约 2/4/6）

### 1. 认证接入（契约 2）

- `从 认证 导入 初始化认证, 取令牌`。
- 主入口装配阶段调用 `初始化认证()`（在起服务前），随后 `取令牌()` 得到令牌。
- 令牌为空（理论不出现，防御）→ 按未启用认证处理，打印原格式 URL。

### 2. 装配新签名（契约 4）

- `服务.配置(客户端对象, 注册表, 根, 超时值, 界面目录)` —— 新增第 5 参。
- `界面目录` 解析优先级：
  1. 环境变量 `HARNESS_WEB_UI_DIR`（非空直接用）；
  2. `获取脚本路径()` 推导：脚本在 `examples/`，取目录名后拼 `../webui`（或"脚本上级目录 + webui"），
     用 `文件系统` 的 `绝对路径`/`规范化路径` 归一；`文件存在` 校验；
  3. 都失败 → 打印警告"未找到 webui 目录，静态页面不启用"，传 `""`（不崩、不阻断 API 服务）。
- 注意：**不要用 `当前目录()` 定位 webui**（回归测试在临时 cwd 跑）。

### 3. 登录 URL 输出（契约 6）

- 启用认证：打印 `web 服务已启动: http://127.0.0.1:<实际端口>/?token=<令牌>`。
- 未启用：打印原格式 `web 服务已启动: http://127.0.0.1:<实际端口>`。
- 保留 `HARNESS_PORT` 广告端口逻辑与 `HARNESS_PORT_FILE` 端口文件机制（回归门禁依赖）。

### 4. 自动打开浏览器（契约 6）

- 默认开。用 `从 外部命令 导入 执行命令` 调系统命令：
  - Windows（`是否Windows()`）：`start "" "http://127.0.0.1:<端口>/?token=<令牌>"`（注意 start 首参是标题占位）。
  - 非 Windows：`xdg-open`（Linux）/ `open`（Mac）—— 若 `命令存在` 判断失败则跳过。
- `HARNESS_WEB_NO_OPEN=1` → 不打开（**门禁必须设**，否则 CI 弹浏览器）。
- 打开失败 → 打印提示"自动打开浏览器失败，请手动访问 <URL>"，**不崩**。
- 打开动作放在"打印 URL 之后、进入常驻循环之前"。

### 5. 保留既有行为（回归门禁兼容，红线）

- mock/真实双模式（`HARNESS_WEB_MOCK`）、密钥解析（`.env`）、`HARNESS_WEB_LIFETIME` 常驻/寿命
  逻辑**一律不动**。`RESIDENT_CASES` 注入 `HARNESS_WEB_LIFETIME=3` 时必须能正常停机 rc=0。
- 新增环境变量只做增量（HARNESS_WEB_UI_DIR / HARNESS_WEB_TOKEN / HARNESS_WEB_NO_OPEN / HARNESS_WEB_CORS），
  不改变既有变量的语义。

## 语言坑提醒（历批踩过，直接规避）

- `外部命令.light` 已有 `执行命令`、`执行命令并获取输出`、`命令存在`，直接导入用。
- `获取脚本路径()`（`从 系统接口 导入`）返回当前入口脚本路径——先用它推 `examples/` 上级目录。
- PowerShell 里调 `start` 的引号注意转义；光明字符串里写 `\"` 前先确认字面量语法（必要时
  用拼接绕开嵌套引号）。
- 发现"写不出/写错/绕不过"→ 登记 `docs/功能对标/语言缺陷账.md`，别顺手改编译器。

## 验收判据（每条立得住反跑）

1. 门禁兼容（**最重要**）：
   `HARNESS_WEB_MOCK=1; HARNESS_WEB_NO_OPEN=1; HARNESS_WEB_LIFETIME=3` →
   `python 运行.py examples/运行Web服务器.light` 启动 → 打印带 token 的登录 URL → 3 秒后
   正常停机，退出码 0，**全程无浏览器弹出**。
2. 输出格式：启用认证时 URL 含 `?token=`；`取令牌()` 与 URL 中 token 一致；未认证时无 `?token=`。
3. 手动验收（不设 NO_OPEN，真机）：启动 → 默认浏览器自动打开带 token 的 URL；页面加载后
   关闭服务（Ctrl+C 或设 LIFETIME）。
4. webui 定位：`HARNESS_WEB_UI_DIR` 指向正确目录 → 静态路由生效（`GET /` 200）；不设置且
   `webui/` 存在 → 同样生效；两者都拿不到 → 打印警告并继续起 API（不崩）。
5. **反跑**：把"应打印 token URL"改成不打、或把"NO_OPEN=1 不打开"改成照样打开，测试/日志必须能分辨。

## 交付物

- `examples/运行Web服务器.light` 改动。
- 交付报告：新环境变量清单及默认值、登录 URL 实际输出样例（脱敏）、门禁兼容验收记录
  （LIFETIME 停机 rc=0）、浏览器自动打开手动验收记录、webui 定位三种路径的实测结果。

## 运行通道

```powershell
cd G:\dswork\duan-light-merge\lightharness
# 门禁兼容验证（无浏览器、自动退出）：
$env:HARNESS_WEB_MOCK='1'; $env:HARNESS_WEB_NO_OPEN='1'; $env:HARNESS_WEB_LIFETIME='3'
python 运行.py examples/运行Web服务器.light
# 手动体验（会弹浏览器）：
$env:HARNESS_WEB_MOCK='1'; python 运行.py examples/运行Web服务器.light
```
