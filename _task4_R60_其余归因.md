# 第60轮 任务4 · 只读归因（R61 底表）交付报告

> 范围：`复刻_第60轮_任务prompt分发_compact词法与异步报错与截取族轮.md` **任务4**
> 纪律：**只读归因，不改代码**（4A）；定向复跑确认（4B）
> 基线：`lightharness/reports/082_lightmerge基线_2026-09-18-172617.json`（13 红）
> 配套明细：`lightharness/_task4_R60_其余归因明细.json`

---

## 一、结论速览

| 项 | 基线 172617 状态 | 当前状态 | 处置 |
|---|---|---|---|
| 4A-1 `_test_null_safety.py::TestBackwardsCompatibility::test_paragraph_call` | 红 | **仍红**（同症状） | 归因 → R61（见二） |
| 4A-2 `_test_null_safety.py::TestNullSafetyFunctionCall::test_func_non_nullable_param_with_nullable_arg` | 红 | **已转绿**（任务1 连带） | 归因存档，R61 无需处理（见三） |
| 4B 钉桩 R19 / T6C 参数解析 / T6C URL | 红（R59 已修） | **绿** | 双侧定向复跑确认（见四） |

---

## 二、4A-1 `test_paragraph_call`（仍红，R61 修复项）

### 2.1 复现证据（本机，R60 工作区词法器）

```
ParseError: 发现 2 个语法错误
  错误 1/2: 行 1, 列 11 — 无法识别的语法元素：'：'
  错误 2/2: 行 2, 列 1 — 缩进不正确：这一行的缩进层级没有对应的语句块
用例源码: 段落相加(甲, 乙)：\n    返回甲加乙。\n打印相加(1, 2)。\n
```

### 2.2 根因复核（token 级取证，修正 R59 归因线索）

行 1 的词法 token（R60 词法器与基线一致，本形态未被任务1 判据触及）：

```
IDENTIFIER '段落相加'   ← 语句关键字「段落」被吞进标识符
LPAREN '('   IDENTIFIER '甲'   COMMA ','   IDENTIFIER '乙'   RPAREN ')'
COLON '：'
```

**根因：R21 上下文敏感切词的 `_ctx_call` 分支**（`src/lexer.py` :2517 附近，「整串后紧随 `(` → 函数调用语境 → 关键字前缀整串并入」）。紧凑定义 `段落相加(...)` 的**形参表左括号 `(` 被误判为调用语境**，`段落`+`相加` 并成单标识符 `段落相加`；解析器遂把 `段落相加(甲, 乙)` 当普通调用表达式处理，语句级尾随 `：` 无法识别，连带行 2 缩进错误。这与 R59 任务1 已修的 `设结果为段落计算(甲，乙)`（`设` 头触发豁免）**完全同族**——`段落` 缺同款头触发豁免。

**对照实验（决定性）**：

| 变体 | 结果 |
|---|---|
| `段落相加(甲, 乙)：`（原用例，紧凑+半角逗号） | FAIL（列 11 `：`） |
| `段落相加(甲，乙)：`（紧凑+全角逗号） | FAIL（列 10，同机制） |
| `段落 相加(甲, 乙)：`（**空格**+半角逗号） | **OK** |
| `段落 相加(甲，乙)：`（空格+全角逗号） | OK |
| `段落 相加(甲，乙)：` + `打印相加(1，2)。` | OK |

→ **修正 R59 归因线索**：半角逗号（实参/形参表）与半角冒号**均非根因**——空格分隔后半角逗号全程可解析；唯一阻塞是紧凑形态下 `段落` 关键字被 `_ctx_call` 吞并。

### 2.3 R61 修复路径（建议）

与 R59 任务1 `设` 头触发同款（改面最小、口径统一）：把定义头语句关键字 `段落`（可一并评估 `函数`/`类型`/`接口`/`结构体`）加入 R21 `_ctx_call` 与词首触发豁免（`_emb_head_trigger` 同位）。护栏流程照 R60 任务1：全语料 token A/B（38091 文件，变化集逐条取证）+ 互举反跑 0 新增 + 本机/0.82 定向。
注意：需排查语料中是否存在**名为 `段落X` 的合法标识符调用**（`_scan_user_definitions` 已定义者优先整体成词，理论上已被 `_match_mixed_user_definition` 前置保护，A/B 落证即可）。

### 2.4 工作量 / 风险

- 工作量：**小**（判据 ~10 行 + 护栏验证半天内；复用 R60 任务1 的 A/B 脚本与流程）
- 风险：低——`段落` 为定义头高频关键字，紧凑 `段落名(` 形态在语料中命中应集中；已有 user_definitions 前置保护兜底。

---

## 三、4A-2 `test_func_non_nullable_param_with_nullable_arg`（基线红 → **已随任务1 转绿**）

### 3.1 基线根因（HEAD 词法器复现取证）

用例首行 `段落展示内容接收内容:数：`，基线 token：

```
KEYWORD '段落'  IDENTIFIER '展示内容'  IDENTIFIER '接收内容'  COLON ':'  IDENTIFIER '数'  COLON '：'
```

R21 词首多字动词并入把 `接收`+形参 `内容` 并成单标识符 `接收内容` → `接收` 关键字丢失 → 解析器无法识别其后的半角冒号类型标注 `:数` → ParseError 行 1 列 13（R59 线索中「半角冒号类型标注存疑」可排除：类型标注本身被解析器支持，阻塞仅在 `接收` 被吞）。

### 3.2 转绿机制（R60 任务1 修复B 连带）

R60 词法器 token：`段落 展示内容 接收(KW) 内容(ID) : 数 ：` → 旧式段落 + 半角冒号类型标注正常解析，可空推断链路（形参非可空 ← 实参为空）按测试期望产出可空错误，断言通过。

**双态复验**：HEAD 词法器 = FAIL（基线复现）；R60 词法器 = PASS。该红随 R60 任务1 交付转绿，**R61 无需处理**；其修复判据与护栏证据见 `light-merge/_task1_R60_compact词法与examples连带.md` 2.2 节。

---

## 四、4B：R59 3 个新增红已转绿确认（双侧定向复跑，不改代码）

| # | 测试 | 本机（py3.13 venv） | 0.82（/tmp/r44-20260918-190439，py3.12） |
|---|---|---|---|
| 1 | `tests/test_R40_语言支撑配套.py::Test现状钉桩_既有词法回归::test_现状钉桩_R19_为字整串合并` | passed | passed |
| 2 | `tests/unit/test_原生腿_T6C_向量复数颜色参数编码.py::test_O0_参数解析_选项默认类型_对拍` | passed | passed |
| 3 | `tests/unit/test_原生腿_T6C_向量复数颜色参数编码.py::test_O0_编码解码_URL_对拍` | passed | passed |

两次运行均 **3 passed / 0 failed**（钉桩 R19 已随 R59 转正更新，T6C 两处 `长(词)` 截取修复稳定）。

---

## 五、声明

- 本任务**未改任何代码**（4A 只读；4B 仅复跑）；验证期间为隔离任务1 影响曾临时切换词法器文件，均已原样恢复。
- 4A-2 的转绿归属任务1（连带改善），如路M 对拍 172617 预期「剩 2 条存量红」，实际预期应修正为**剩 1 条**（4A-1）；门判据「新增红 0」不受影响。
- 明细 JSON（id/分类/根因复核/取证证据/R61 修复路径/工作量/风险）：`lightharness/_task4_R60_其余归因明细.json`。
