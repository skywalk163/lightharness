# 第18轮路M收口说明（缺陷修复轮）

> 日期：2026-09-13 ｜ 编译器：`G:\dswork\duan-light-merge\light-merge` ｜ 工作区：`G:\dswork\duan-light-merge\lightharness`（branch main）
> 前置：第17轮收口 `b9aea94`（对标115条 / 缺陷L-001~L-143 / pytest 304 passed）
> 本轮定位：**缺陷修复轮**——前17轮累计登记32个真实语言缺陷，本轮建设6个标准化工具库，对32个缺陷提供A级修复（7个）/B级标准化绕法（10个）/C级检查工具+绕法指南（15个）。

---

## 一、6路交付查收

| 任务 | 工具库模块 | 代码行数 | 测试断言数 | 反跑 | 新缺陷 |
|---|---|---|---|---|---|
| 1 | src/字典增强.light | ~170行 | 33断言 | ALL OK | 无 |
| 2 | src/数值兼容.light | ~140行 | 28断言 | ALL OK | 无 |
| 3 | src/错误增强.light | ~130行 | 25断言 | ALL OK | 无 |
| 4 | src/字符串增强.light | ~140行 | 30断言 | ALL OK | 无 |
| 5 | src/函数增强.light | ~160行 | 26断言 | ALL OK | 无 |
| 6 | src/词法安全.light | ~320行 | 30断言 | ALL OK | 无 |
| **合计** | **6模块** | **~1060行** | **172断言** | **6/6 ALL OK** | **0个新缺陷** |

**文件互斥**：零越界。6路均只新增文件（6 src模块 + 6测试 + 6反跑 + 6报告），**未修改任何已有 src/ 模块**（本轮铁律：已有模块的绕法保持原样，后续轮次逐步迁移）。

---

## 二、6测试单独跑结果

全部 PASS rc=0：
- test_字典增强.light — 33断言 PASS
- test_数值兼容.light — 28断言 PASS
- test_错误增强.light — 25断言 PASS
- test_字符串增强.light — 30断言 PASS
- test_函数增强.light — 26断言 PASS
- test_词法安全.light — 30断言 PASS

---

## 三、6反跑结果

全部 ALL OK（A/B/C三项判据全过）：
- T1 字典增强：A/B正常组合+边界 PASS；C变异（深度设置键路径改错）立红；字节级恢复；恢复后绿
- T2 数值兼容：A/B正常+边界 PASS；C变异（取整半数上用银行家舍入）立红；恢复后绿
- T3 错误增强：A/B round-trip+边界 PASS；C变异（错误消息格式化不含码）立红；恢复后绿
- T4 字符串增强：A/B正常+NUL/量词边界 PASS；C变异（码转字符码偏移改错）立红；恢复后绿
- T5 函数增强：A/B默认参数+计数器持久 PASS；C变异（闭包状态递增改错不持久）立红；恢复后绿
- T6 词法安全：A/B保留字检查+可切分/生成名持久 PASS；C变异（单字别名串移除配字）立红；恢复后绿

---

## 四、工具库协同验证（本轮特有）

编写 `examples/_taskM_integration_r18.light`，同时导入6个工具库，构建6个端到端场景验证协同工作：

| 场景 | 验证内容 | 检查数 |
|---|---|---|
| 1 安全配置加载器 | 6库联动：安全标识符→深度设置→取整半数上→安全除→尝试执行→构造JSON→码转字符→带默认参数→管道 | 16 |
| 2 计数器+身份令牌+序列化 | 造计数器→造身份令牌→序列化JSON排序 | 5 |
| 3 词法安全全面检查 | 保留字表→是保留字→安全标识符→是内置映射名 | 10 |
| 4 数值兼容边界 | JS真值→JS相等→指数解析→钳位→线性插值 | 11 |
| 5 错误增强round-trip | 造结构化错误→抛出→捕获→解析→错误码/错误消息→是错误 | 9 |
| 6 字符串增强工具 | 码转字符→字符转码→字符串重复→填充左/右→截断→转义花括号 | 9 |
| **合计** | | **54检查** |

**结果：54检查全PASS，rc=0。**

### 协同验证过程中遇到的问题及解决

1. **`断言` 与光明内置函数冲突**：自定义 `段落 断言` 被内置 `断言` 函数遮蔽/冲突，改名为 `检查` 解决。
2. **L-098 闭包修改外层变量不生效**：`检查` 函数内修改模块级计数器变量触发 UnboundLocalError，改用任务5的 `造计数器` 函数（字典承载状态）解决。
3. **`带默认参数` 设计约定**：目标函数必须接收字典参数而非多位置参数，修正 `原始加载器` 定义和调用方式。
4. **整数与字符串拼接**：`超时` 参数为整数30，需用 `文本()` 转换后拼接。
5. **L-090 字符串原语裸名作为函数参数失效**：`管道([去除空白, 转大写])` 中内置函数作为参数传递时失效，改用自定义包装函数 `去空白`/`转大写文本` 解决。
6. **L-089 字符串插值**：`"值是 {x}"` 中 `{x}` 被插值处理触发 NameError，改用字符串拼接 `"值是 " + "{" + "x" + "}"` 构造。
7. **词法安全模块设计限制**：①`是保留字("正常变量")` 返回真（因含单字别名"常"字，49字单字别名导致多数中文标识符被保守判定），改用英文标识符测试；②`内置映射名表` 不含"当前目录"（是特殊内置映射而非普通字符串原语），改用"转小写"测试。

---

## 五、缺陷修复状态汇总

32个已登记缺陷的修复层级分布：

| 修复层级 | 数量 | 缺陷编号 |
|---|---|---|
| **A级（真正修复）** | 7 | L-133(码转字符) / L-125(取整半数上) / L-131(安全除) / L-129(JS真值/JS相等) / L-099(结构化错误) / L-102(带默认参数) / L-098(闭包包装/造计数器) |
| **B级（标准化绕法工具）** | 10 | L-135/L-103(字典获取) / L-123(深度设置) / L-116(序列化JSON排序) / L-127(造身份令牌) / L-100(空字典) / L-085(指数解析) / L-088(获取最后异常) / L-089(构造JSON/转义花括号) / L-091(偏应用) |
| **C级（检查工具+绕法指南）** | 15 | L-084/L-092/L-101/L-119/L-120/L-137/L-143(词法关键字) / L-087(内置映射名) / L-086(正则花括号) / L-090(字符串原语导入) / L-093(异常位置) / L-094(模块遮蔽) / L-095(HMAC) / L-096(字典属性访问) / L-097(if块嵌套段落) |

**本轮未发现新缺陷**（0个新repro），L-151~L-160全部空缺。

---

## 六、docs 三件回填

| 文档 | 变更 |
|---|---|
| 对标清单.json | 115→121条，追加 #116~#121（6个工具库，标注"缺陷修复轮工具库（非上游复刻）"） |
| 语言缺陷账.md | 追加"第18轮缺陷修复状态汇总"（A级7/B级10/C级15，含后续迁移计划） |
| 行为差异清单.md | 追加 R18-D1~D6（6个工具库与上游JS/TS的差异说明） |

---

## 七、全量CI结果

**CI全绿**（2026-09-13，复跑后，耗时 7分05秒）：

| 项目 | 结果 | 详情 |
|---|---|---|
| pytest | **311 passed** | 304基线 + 6新测试 + 1协同验证(_taskM_integration_r18) = 311；rc=0 |
| smoke | **5/5 通过** | test_会话/test_代理/test_工具/test_消息/test_流 全通过 |
| 总耗时 | 425.33s (0:07:05) | pytest 400.5s + smoke ~6s |

**CI首轮红1项（已知flaky，复跑全绿）**：首轮 `test_example_exit_code[test_审批.light]` 失败（assert 1==0）。这是第14轮发现的已知flaky测试（全量pytest偶发rc=1，单独跑全绿，疑似全量顺序依赖/系统资源竞争）。单独跑确认13用例全PASS rc=0，复跑全量CI全绿。不阻塞收口。

---

## 八、git 提交链

```
<收口提交>  收口(第18轮): 对标回填 #116~#121(缺陷修复轮6工具库) / 缺陷修复状态汇总(A7+B10+C15) / 行为差异 R18-D1~D6 + 收口说明
5df4964  任务6: 词法安全工具库
57936fb  任务5: 函数增强工具库
57f0c01  任务4: 字符串增强工具库
2bc5482  任务3: 错误增强工具库
0a26bfa  任务2: 数值兼容工具库
ff1ef10  任务1: 字典增强工具库
b9aea94  收口(第17轮)
```

---

## 九、后续方向建议

### 方向A：已有模块绕法迁移轮
将已有115个模块中的手写绕法逐步迁移到本轮新建的6个工具库。优先迁移高频缺陷绕法（L-135字典获取/L-125取整半数上/L-131安全除/L-099结构化错误），每个模块迁移后跑对应测试确认无回归。

### 方向B：宿主接线深化轮
在第17轮宿主接线原型基础上，利用本轮新建的工具库（特别是错误增强/函数增强/词法安全）深化宿主接线实现，将原型模块升级为可实际使用的宿主运行时。

### 方向C：上游剩余模块复刻轮
继续复刻上游 deepseek-harness 中尚未覆盖的模块（如 packages/ 下剩余的纯逻辑面模块），利用本轮工具库简化绕法代码。

### 方向D：编译器缺陷反馈轮
整理C级15个编译器层面缺陷（词法/语法/闭包/异常），形成详细的缺陷反馈报告提交给光明编译器团队，推动编译器层面的真正修复。

---

## 十、教训与注意事项

1. **`断言` 是光明内置函数**：自定义测试框架函数不要命名为 `断言`，会与内置函数冲突。建议用 `检查`/`验证` 等名称。
2. **L-098 闭包缺陷影响范围广**：不仅是"段落对象存入列表再取出调用"，模块级段落函数内修改模块级变量也会触发 UnboundLocalError（Python闭包语义）。计数器/状态累积必须用字典承载或 `造计数器` 函数。
3. **`带默认参数` 设计约定**：目标函数必须接收字典参数，这是光明不支持 `**kwargs` 的折中设计。使用前需确认目标函数的参数协议。
4. **L-090 字符串原语裸名作为函数参数失效**：`管道`/`偏应用` 等函数式工具接收函数参数时，内置字符串原语（去除空白/转大写等）不能直接传递，需用自定义包装函数。
5. **词法安全 `是保留字` 保守判定**：单字别名49字导致多数中文标识符被判定为"含保留字子串"，`安全标识符` 对多数中文标识符返回备选名。这是保守设计（宁可误报不可漏报），使用时需注意。
6. **缺陷修复轮不修改已有模块**：本轮铁律是只新增工具库，不修改已有模块的绕法代码。迁移工作留待后续轮次，避免本轮引入回归风险。
