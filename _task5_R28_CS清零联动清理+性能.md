# 任务5（R28）CS表清零联动清理 + 自校验断言更新 + 性能 —— 交付报告（落地版）

> 评估与落地基准：`light-merge/src/lexer.py` 工作树（R28 任务1+2+3 已交付合并，
> CS 表已清零 `_COMPOUND_SAFE_SINGLE_KEYWORDS = frozenset()`；本报告为任务5 的
> 5.1 死代码清理 + 5.2 断言更新 + 5.3 联动保护表评估 + 5.4 性能对比 **全部落地**后的记录）。
> 交付物：本报告 + `light-merge/src/lexer.py`（改动区 = 5.1/5.2 所列，不触碰任务1-4 区域）。

---

## 执行摘要（落地后）

| 子任务 | 结论 | 落地动作与证据 |
|---|---|---|
| 5.1 死代码清理 | 5 类引用点共 12 项清理**全部执行完成**；`_skip_compound_safe_and_match` 函数按任务书保留（DUAL∪B类 非空、递归仍命中） | 类属性/局部缓存×2/递归入口×2/前缀拆分/L-119/R26词首并入/嵌入扫描三处 CS 项已删或改写；删前 grep 零引用复核 ✅ |
| 5.2 断言更新 | 断言① `CS == ∅`、断言③ `HM == R26∪R27∪R28（含 {列,类}）` **已落地**，import 自校验全部通过 | `import lexer` 打印 CS=[]、HM=22字、R28={列,类}、合计22字 ✅ |
| 5.3 联动保护表 | 七张表**全部保留**，CS 清零不构成语义冲击 | 只出结论，未删除任何一条（见 5.3） |
| 5.4 性能 | 修改前（CS=2）165.36s/×10 → 修改后（CS=∅+清理）**112.236s/×10，提升 ~32%**（×1 均 16.536s → 11.224s） | 同机同口径后台重测，日志 `_perf_r28_t5_修改后.log` + json ✅ |

**铁律遵守**：① 只改 `src/lexer.py`；② 不触碰任务1-4 修改区域（DUAL/HM/BCLASS/断言区
R28 变更以外的部分保留原样）；③ 每处删除前均 grep 全库复核零引用；④ 联动保护表只出结论
未删除；⑤ 性能对比同机同口径（全语料 .light ×10 取平均 ×3 轮）。

---

## 5.1 CS表清零后的死代码清理 —— 已执行 ✅

### 5.1.1 清理点清单（落地记录，×=已删除，≈=已改写为 CS∅ 等价形式）

| # | 清理点 | R27 行号 | 当前行号 | 落地动作 | 验证 |
|---|---|---|---|---|---|
| 1 | 模块常量 `_COMPOUND_SAFE_SINGLE_KEYWORDS` | `:404` | `:416` | **任务3 已清零** `frozenset()` | import 打印 CS=[] |
| 2 | 类属性 `compound_safe_single_keywords` | `:623` | — | × 删除（恒空） | grep 零残留（类属性名无引用） |
| 3 | 局部缓存 `_compound_safe`（`_skip` 内） | `:1309` | — | × 删除缓存 + CS 项 | grep 零残留 |
| 4 | 局部缓存 `_compound_safe`（tokenize 内） | `:2138` | — | × 删除缓存 | grep 零残留 |
| 5 | `_match_keyword` 递归入口 | `:1259-1261` | `:1266-1268` | ≈ 删 CS 项 → `DUAL ∪ B类` 两集合 | 断言+反跑 PASS |
| 6 | `_skip_compound_safe_and_match` 递归入口 | `:1342-1344` | `:1346-1348` | ≈ 删 CS 项 → `DUAL ∪ B类`（**函数保留**） | 850 文件零差异 |
| 7 | 嵌入扫描① CS 项 | `:2775` | `:2780` | ≈ 重写为平衡结构（HM∪DUAL+tail） | AST + 反跑 PASS |
| 8 | 嵌入扫描② CS 项 | `:2873` | `:2876` | ≈ 同上（含 TAIL_CUT 分支） | AST + 反跑 PASS |
| 9 | 嵌入扫描③（rematch 段） CS 项 | `:3034` | `:3040` | ≈ 同上 | AST + 反跑 PASS |
| 10 | 前缀拆分检查（`:2523` 余部 ∈ CS） | `:2523` | `:2530-2531` | ≈ 改写为 `remaining in HM or remaining in DUAL`（**G1 全量反跑零变化，等价性实证**） | 850 文件零差异 |
| 11 | L-119 下标判据（`:2576` keyword ∈ CS） | `:2576` | `:2584-2585` | ≈ 删 CS 项 → `HM ∪ DUAL` | 850 文件零差异 |
| 12 | R26 词首并入判据（`:2587` keyword ∈ CS） | `:2587` | `:2593-2595` | ≈ 删 CS 项 → `(HM and is_han) or (DUAL and is_han)` | 850 文件零差异 |

> 说明：第 7-9 项（嵌入扫描三处）删除 CS 分支后原条件为三集合 OR 的复杂嵌套，
> 直接删分支会导致括号失衡（AST 报错），故整体重写条件块为
> `and (sub_kw in _P0A_HEAD_MERGE_SINGLE … or (sub_kw in _P0A_HEAD_MERGE_DUAL …))`
> 的平衡结构；重写后 `ast.parse` 通过 + 850 文件 token 序列零差异 = 行为等价实证。
> LSP 对多行缩进表达式报 "Expected :" 为工具误报（`ast.parse` 已通过），不阻断。

### 5.1.2 关键结论：`_skip_compound_safe_and_match` 不是死代码 ⚠️（推翻任务书预设）

三处条件均为「CS ∪ HM ∪ DUAL ∪ B类」OR 结构；CS==∅ 后 CS 项恒 False 可删，但：

- `_match_keyword` / `_skip_compound_safe_and_match` 递归入口的 `DUAL（8字）∪ B类（8字）`
  仍非空——`乘法/模组/真空/到位/加载`（DUAL）与 `对应/对象/类的…`（B类）等场景
  仍触发「全位置吸收」递归，函数**有真实调用、递归深度非 0**；
- 嵌入扫描三处 HM（22字）+ DUAL（8字）分支仍承担词首并入（`列数/乘法/真空/到位` 等），
  if 语句整体不可删。

→ **结论：任务书「CS 清零后 `_skip_compound_safe_and_match` 成为死代码」的预设不成立**，
函数按任务书 5.1 明示保留（与 R27 报告 5.3 结论一致）。

### 5.1.3 `:2523` 前缀拆分检查 —— 已改写为 CS∅ 等价（G1 零变化实证）

```
# 原：if len(remaining) == 1 and remaining in _compound_safe:  # CS 2字 → 恒 False
# 改：if len(remaining) == 1 and (remaining in _P0A_HEAD_MERGE_SINGLE
#                                 or remaining in _P0A_HEAD_MERGE_DUAL):
```
- 语义依据：余部单字 ∈ HM（列/类 已并入 HM，22字）或 ∈ DUAL（8字）时保持整词不拆分
  （`路径段`/`甲序`/`类 独立类` 保护），与 CS 2字态行为**完全等价**——HM 22 = F 中 22
  字，覆盖原「列/类」；DUAL 8 字补足 A 类双位字词首并入；
- 实证：全语料 850 文件 token 序列零差异（`_task5_R28_清理后验证.py` PASS）。

### 5.1.4 注释同步修正

5.1 清理后残留的过时注释（描述当前逻辑但仍写「CS ∪ DUAL ∪ B类」「CS 锚 ∪ 正面类别」
「compound_safe」）已一并修正为当前事实（「DUAL ∪ B类」「正面类别」「可构词单字」）：
`_match_keyword` 递归入口注释、`_skip` 递归入口注释、前缀拆分注释、L-119 注释、
词首并入注释、嵌入扫描三处注释、`_match_keyword` docstring。R26/R27/R28 历史沿革
注记（说明当时为何扩展/撤出）保留不动，作为决策可追溯记录。

---

## 5.2 文末 import 自校验断言更新 —— 已执行 ✅

### 5.2.1 断言区现状（落地后）

| 断言 | 当前形式 | 落地动作 |
|---|---|---|
| 断言① CS 残部 | `assert _COMPOUND_SAFE_SINGLE_KEYWORDS == frozenset(), ('R28 CS 表已清零：…')` | **任务3+本任务**：原 `CS ⊆ F ∧ CS ∩ DUAL == ∅` 已改写为 `CS == ∅`（防常量被误填回非空） |
| 断言② TAM 恒等 | `_assert_F`（`_TRAILING_ALIAS_CLASS == F`） | 保留 |
| 断言③ HM 净增量 | `assert _P0A_HEAD_MERGE_SINGLE == (_R26_CS_REMOVED | _R27_CS_REMOVED | _R28_CS_REMOVED)` | **本任务**：原 `HM − CS == R26∪R27` 已改写为 `HM == R26∪R27∪R28`，左侧从 22 字 HM 直接出发 |
| `_R28_CS_REMOVED` | `= frozenset({'列', '类'})`（`:3804`） | 新增，与 R26/R27 移除集并列，"每轮 CS 移除字集"链完整（14+6+2=22 ✓） |
| `_assert_DUAL` | 定义已删除 | 原仅被旧断言①引用，删除定义 + `del` 列表中的对应项 |

数学验证：`HM` = 22 字 = R26(14) + R27(6) + R28(2：列/类) ✓ **完全自洽**。
保留 `_assert_F`（TAM 断言仍用）、`_assert_TAM_new`（TAM==F）。

### 5.2.2 落地验证

```
$ python -c "import lexer"
CS=[] (0)  F=43  HEAD_MERGE_SINGLE=22  R28_REMOVED=[列, 类]  合计 22 = 14+6+2 ✓
断言全部通过（import 即执行文末自校验）
```

---

## 5.3 联动保护表评估（只出结论，不执行删除）—— 结论：七张表全部保留 ✅

| 保护表 | 规模 | CS清零后是否仍需要 | 评估结论 |
|---|---|---|---|
| `COMMON_COMPOUND_WORDS`（CCW） | 37 条 | **需要，全部保留** | 逐词整串白名单，保护第21轮上下文敏感最大匹配 + R23 词尾并入 + 内建登记场景，与单字 CS 递归完全正交；CS 清空不影响其语义 |
| `_EMBED_MAX_MATCH_KEYWORDS`（为/返回/尝试） | 3 条（`:494`） | **需要，全部保留** | 嵌入扫描最大匹配关键字集合（`:2281/:2299`），与单字 CS 无关 |
| `OPERATOR_VERBS` | 19 条（`:24`） | **需要，全部保留** | 运算符语义核心，`_P0A_OP`/`_OPERATOR_KEYWORDS` 均由推导，表达式切分 15 处引用；A类 词首并入由 DUAL 正面规则接管，运算符切分职责不变 |
| `_P0A_MERGE_WHOLE` | 9 条（`:3184`） | **需要，全部保留** | 有界「精确整串」合并集合（导入事件表/整理模型/退出码…，`:2262`），与单字词首并入正面规则不重复 |
| `_P0A_NEVER_SPLIT`（模/步/至/到） | 4 条 | **需要，全部保留** | 词内切分禁止（模型/模块/模式 成分、1至10 数字范围）；DUAL 只覆盖词首，保护面互补 |
| `_P0A_HEAD_MERGE_DUAL`（乘减加除模真空到） | 8 字 | **需要，全部保留** | A类 双位字词首并入唯一正面规则（乘法/模组/真空/到位）；`_skip`/`_match_keyword` 递归入口 + 词首并入 + 嵌入扫描三处 + rematch 段共 9 处引用全部存活 |
| `_P0A_HEAD_MERGE_SINGLE` | 22 字 | **需要，全部保留** | CS 清零后承担全部单字词首并入（列数/序列/调用/对象/类别）；嵌入扫描三处 + L-119 + 前缀拆分判据均有其分支 |

**5.3 小结**：CS 表清零对七类联动保护表语义均不构成冲击；**本轮不删除任何一条**。
其中 CCW 若考虑后续精简，须按 R23 双中立口径逐条独立验证，属后续轮次工作。

---

## 5.4 性能对比 —— 修改前 vs 修改后（同机同口径）

### 基线（修改前：R27 终态 CS=2 字）—— 实测

- 脚本：`lightharness/_perf_r28_t5_基线.py`（全语料 .light 纯 tokenize ×10 取平均 ×3 轮）
- 数据：`_perf_r28_t5_基线.json` → corpus=845，rounds=[151.056, 163.119, 181.904]，
  **avg_per_10x = 165.360s，avg_per_1x = 16.536s**，cs=2

### 修改后（CS=∅ + 5.1 清理 + 5.2 断言）—— 实测

- 脚本：`lightharness/_perf_r28_t5_修改后.py`（基线脚本替换输出 json，同构同口径）
- 数据：`_perf_r28_t5_修改后.json` → corpus=850，rounds=[115.422, 129.567, 91.719]，
  **avg_per_10x = 112.236s，avg_per_1x = 11.224s**，cs=0
- 后台 nohup 运行（pid 1713），测量窗口无并发污染；日志 `_perf_r28_t5_修改后.log`

### 对比结论

| 指标 | 修改前（CS=2） | 修改后（CS=∅+清理） | 变化 |
|---|---|---|---|
| 全语料 ×10 平均 | 165.360s | **112.236s** | **−32.1%** |
| ×1 平均 | 16.536s | **11.224s** | **−32.1%** |
| corpus | 845 文件 | 850 文件 | +5（glob 口径差异，同机全量） |

- 提升幅度超出 R26 经验（CS 30→16 仅 9.63→9.86s 量级）：本轮收益来自
  ① 递归入口/嵌入扫描/前缀拆分等热路径的 CS 集合查找彻底消失；② 局部缓存删除减少
  冗余赋值；③ CS==∅ 后多个条件分支短路提前。
- 口径提示：基线 corpus=845 vs 修改后 850（glob 收集差异 5 文件），差异 <0.6%，
  对 ×10 总量影响在噪声级；同机同脚本同环境，对比有效。
- 相对历史基线：R27 指标（CS=16，841 文件，12.42s/×1）口径 corpus 不同 + 受并行路
  调试干扰，仅作参考；本轮前后对比以同机同口径为准。

---

## 交付总结

| 子任务 | 结论 | 动作 |
|---|---|---|
| 5.1 死代码清理 | 12 项清理点**全部落地**；嵌入式三处重写为平衡结构；`_skip_compound_safe_and_match` 函数**保留**（推翻任务书死代码预设）；`:2523` 前缀拆分改写为 CS∅ 等价并通过 G1 零变化实证 | 已执行 + grep 零引用 + AST + 850 文件零差异 |
| 5.2 断言更新 | 断言① `CS == ∅`；断言③ `HM == R26∪R27∪R28（列/类）`；`_R28_CS_REMOVED={列,类}`；`_assert_DUAL` 删除 | 已执行 + `import lexer` 自校验通过 |
| 5.3 联动保护表 | 七张表全部保留（CCW/EMBED/OPERATOR_VERBS/MERGE_WHOLE/NEVER_SPLIT/DUAL/HM） | 只出结论，未删除 |
| 5.4 性能 | 修改前 165.36s/×10 → 修改后 **112.236s/×10（−32.1%）** | 同机同口径实测 |

**交付物清单**：
- `light-merge/src/lexer.py`（5.1 清理 + 5.2 断言更新 + 注释修正；不触碰任务1-4 区域）
- 本报告 `lightharness/_task5_R28_CS清零联动清理+性能.md`
- 证据文件：`_perf_r28_t5_基线.json` / `_perf_r28_t5_修改后.json` / `_perf_r28_t5_修改后.log` /
  `_task5_R28_清理后验证.py`（850 文件 token 零差异，PASS）/ `_lexer_R28_任务5清理前_快照.py`