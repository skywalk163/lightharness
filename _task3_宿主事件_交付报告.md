# 任务3 交付报告：宿主事件接线原型（src/宿主事件.light）

> 任务来源：`G:\dswork\duan-light-merge\复刻_第17轮_任务prompt分发_宿主接线原型 (1).md` 任务3
> 日期：2026-09-13 ｜ 对标 #112（**原型设计**，上游参考 Cordis Events 事件类型声明模式、@mode emit/waterfall、core/session scope-filtered dispatch——非逐行复刻）
> 命名预检查：src/ 下无 宿主事件.light 同名模块 ✓
> 与既有模块关系：只读导入 src/事件.light（事件总线实现）与 src/宿主上下文.light（作用域匹配），不修改。

---

## 一、接口设计（设计决策登记，R17-D3）

| 接口 | 形状 | 设计决策 |
|---|---|---|
| 事件类型注册表 | 注册事件类型(定义 {事件名, 模式, 必填键表?, 描述?}) / 获取事件类型 / 列举事件类型 | 事件名 `域/动作` 规范（恰一个 `/`、两侧非空）；重名抛 `事件类型重复注册: X`；模式限 emit\|waterfall（`事件模式必须为 emit 或 waterfall: X`）；名称不规范抛 `事件名必须为 域/动作 格式: X`；未注册获取抛 `事件类型未注册: X` |
| 参数形状校验 | 派发前必填键存在性校验 | 缺失抛 `事件参数缺少必填键 K: E`（简化校验，登记） |
| 绑定器 | 绑定事件(接线, 插件名, 声明 {事件名, 监听器, 作用域?, 一次性?, 优先级?}) / 解绑事件 / 解绑全部 / 插件停止解绑 | 绑定未注册类型抛；解绑调取消器+从记录表移除+重订；插件停止解绑=解绑全部（供插件停止钩子调用） |
| 包装处理器 | 一次性判定 → 作用域过滤（参数["事件作用域"] vs 声明.作用域，复用 作用域匹配）→ 调用计数 → 错误隔离（吞错记录，waterfall 返回 假=委托下一个）→ 透传监听器返回值 | 一次性由接线层 已触发 标志管理（不依赖总线一次性表）；错误记录 {事件名, 插件名, 错误消息, 时间, 监听器索引} |
| 优先级 | 绑定后按（优先级降序、绑定序）**取消全部并重订**到总线 | 保证高优先级先调用、同优先级按绑定顺序（利用 总线.订阅 返回的取消器实现重排，设计决策） |
| 派发 | 派发事件(接线, 事件名, 参数, 事件作用域?, 时间) | 未注册事件→记警告（警告表）返回 假 不崩溃；emit→总线.发布；waterfall→总线.瀑布（收尾 空——消费停链语义由包装返回值承载）；派发计数+1 |
| 统计 | 获取统计 {派发: {事件名: 次数}, 调用: {插件名/事件名: 次数}} / 重置统计 | 调用计数在包装内累加 |

## 二、测试验证

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"; python 运行.py examples/test_宿主事件.light
test_宿主事件 PASS   （rc=0）
```

`examples/test_宿主事件.light` 覆盖 9 组 ≥50 断言：事件类型注册表（注册/列举/重名/名称不规范/双斜杠/模式非法/未注册获取抛）、emit 派发（两监听器按序）、waterfall（优先级重排：最高先调、次优消费停链、低优不调）、错误隔离（坏监听吞错记录+后续仍调+日志字段（插件名/消息/时间）+清空）、参数形状校验（必填键缺失抛）、一次性监听器（首派发触发/二次不触发，含坏监听吞错计数核对）、作用域过滤（空事件作用域不匹配 agent 监听/同作用域接收/祖先匹配/其他 agent 不接收）、解绑与停止解绑（解绑甲后乙仍调/多事件插件独立记录器验证两事件均解绑/其余监听器仍调用）、未注册派发返回假、统计（派发计数/调用计数/重置）。

## 三、反跑结果（3/3 ALL OK）

`lightharness/_antirun_t3_宿主事件.py`（BASE 自定位；字节级备份→变异→跑本路测试→断红→恢复→断绿；恢复置于 try/finally）：

```
PASS C=变异 插件停止时不解绑 -> 红 (变异后运行 rc=1（期望非 0，真红）)
PASS 字节级恢复校验 (sha256 aa68351c6d51)
PASS A/B 判据（emit 两监听器 + waterfall 消费停链）回归绿 (rc=0)
ALL OK
```

A=正常（注册类型→绑定 2 监听器→emit 派发按序调用）与 B=边界（waterfall 三监听器按优先级、第二个消费停链第三个不调）为测试内场景，随回归绿验证；C=变异（插件停止解绑 的 解绑全部 调用替换为占位——停止不解绑）立红（多事件插件独立记录器仍收到事件）、恢复后绿。

## 四、未移植项 / 与上游 Cordis 的架构差异（R17-D3）

- Cordis Events 的 TypeScript 类型层（Events 接口声明合并/this: Scoped<T> 绑定）——以注册表+包装承载。
- waterfall 的参数可变性（监听器可修改参数字典透传下游）——原型参数为共享字典（监听器可直接改 参数["键"]，未做逐监听器隔离）。
- 异步监听器/Promise 排干——光明顺序执行。
- @dshScopeScan 装饰器——复用 宿主上下文.作用域匹配。
- 事件总线的一次性表——接线层自带 已触发 标志（重排兼容）。

## 五、语言差异（本轮新缺陷登记）

- 无新缺陷（L-145~L-146 编号空缺，路M 顺延）。开发中踩到并按既有缺陷绕法处理：L-120 同族（`定义` 为常量声明关键字、`占为emit` 撞 `为` 字、import `从` 后需空格）、L-119 同族（`收到` 以单字别名 `到` 结尾被切碎）、段落内 `设 X` 局部化遮蔽全局（改独立变量名）。

## 六、移交清单

- `lightharness/src/宿主事件.light`（新增，≈230 行）
- `lightharness/examples/test_宿主事件.light`（新增，PASS rc=0）
- `lightharness/_antirun_t3_宿主事件.py`（反跑 3/3 ALL OK，BASE 自定位）
- 本报告。
- 移交路M：对标清单新增 **#112**（宿主事件接线原型，标注「原型设计」）；行为差异清单 **R17-D3**；CI 增 1 个 test_ 文件（302+ 占 1）。
