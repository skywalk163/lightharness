# 第21轮 docs 更新草案（任务6）

> 说明：本文件为 docs 三件的**具体修改内容草案**，归路M收口时统一写入。
> 依赖标注：#131/#132 与 L-152 状态取决于任务1（预扫描重构）/任务2（上下文敏感切词）的落地结果；
> 草案按「任务1/2 已合并且验证绿」的目标态撰写，若收口时未达成，按文末「未达成态措辞」降级。

---

## 一、对标清单.json（docs/功能对标/对标清单.json）

追加 3 条（现总数 130 → 133），并把版本字段更新：

```json
{"编号": 131, "功能": "词法预扫描重构：基于缩进/嵌套层级的定义收集（修复L-152）", "原版包": "词法确定性切词重构轮（第21轮）", "光明模块": "light-merge/src/lexer.py _scan_user_definitions", "状态": "已完成", "完成轮次": "第21轮", "备注": "P0；嵌套段落段名/形参名/块内设名正确入definitions（只增不减）；L-152复现用例转绿；模块级收集行为兼容"},
{"编号": 132, "功能": "词法上下文敏感切词：非语句起始位置最大匹配", "原版包": "词法确定性切词重构轮（第21轮）", "光明模块": "light-merge/src/lexer.py _tokenize_chinese_sequence + _tokenize_identifier_or_keyword", "状态": "已完成", "完成轮次": "第21轮", "备注": "P0；表达式上下文关键字前缀标识符（去重占位/作用域匹配/断言为真等）整体成词；语句起始位置与运算符关键字（与/或/非/等于/大于等）切分不变"},
{"编号": 133, "功能": "词法确定性测试套件（嵌套定义/块内设名/表达式上下文/语句起始/边界混合/运算符 6大类）", "原版包": "词法确定性切词重构轮（第21轮）", "光明模块": "lightharness/examples/test_R21_*.light + lightharness/tests/test_R21_*.py", "状态": "已完成", "完成轮次": "第21轮", "备注": "P1；运行期自校验.light + pytest token层验证双层；为词法重构提供长期回归保障"}
```

版本字段更新：

```json
"版本": "0.1.5-rc.2（a305303422，2026-09-10）；覆盖记录至第21轮（2026-09-14）"
```

## 二、语言缺陷账.md

### L-152 状态更新（将现有条目末尾的「状态」段替换）

**达成态（任务1 落地且复现绿）**：

> **状态**：**已修复（2026-09-14；第21轮任务1）**。预扫描 `_scan_user_definitions` 重构为基于
> 缩进/嵌套层级的定义收集：嵌套段落的段名与形参名、块内 `设` 名正确入 definitions（只增不减）；
> L-152 复现用例 `examples/test_R21_L152嵌套段落形参.light` rc=0，token 层验证 `函数值` 为单个
> IDENTIFIER；模块级同形态行为不变（definitions 超集验证通过）。

**未达成态（任务1 未合并或部分场景失败）**：

> **状态**：**部分修复（2026-09-14；第21轮任务1）**。已修复〈列出已验证场景〉；
> 〈列出未通过场景〉待后续轮次。绕法不变：嵌套段落形参避免关键字前缀。

### L-151 状态确认

维持「已修复（第20轮任务1）」不变；第21轮重构未破坏（第20轮反跑 `test_R20_*` 与既有反跑全部 ALL OK，验证证据见 `_task5_R21_全量回归扫描报告.md`）。

### 新缺陷预留（如任务1 实施中发现预扫描盲区，按此模板登记 L-153）

> ## L-153（第21轮任务1 发现，2026-09-14）〈标题：预扫描盲区简述〉
> **现象**：〈复现现象〉；**最小复现**：〈代码块〉；**期望**：〈期望行为〉；
> **状态**：已知未修复/已修复（依实施结果）。

## 三、行为差异清单.md（追加 R21 段，并订正 R20-D1）

```markdown
### R21-D1 预扫描基于缩进/嵌套层级与上游差异
- **差异**：上游 deepseek-harness（TS/JS）标识符以 ASCII 构成、无关键字粘连问题，
  名称收集无需预扫描；光明语言标识符与关键字共用汉字字符集，名称收集依赖
  `_scan_user_definitions` 预扫描。第21轮将其从线性文本扫描重构为基于缩进/嵌套层级的
  结构化收集（段落定义行按缩进级别建层级，段名/形参名/块内设名按层级正确收集）。
- **影响**：嵌套段落形参名、块内设名进入 definitions；预扫描结果为重构前超集。
- **上游对齐**：语义上对齐上游「标识符即名称」的无限制行为；实现路径为光明特有。

### R21-D2 词法上下文敏感切词与上游差异
- **差异**：上游 ASCII 词法以空白/符号为天然边界；光明汉字串与关键字共享字符集，
  第21轮引入上下文敏感策略：非语句起始位置（表达式上下文）优先最大匹配整体成
  IDENTIFIER，仅整串精确等于关键字时作 KEYWORD；语句起始位置切分逻辑不变；
  运算符关键字（与/或/非/等于/大于/小于等）在表达式上下文仍按关键字切分。
- **影响**：`去重占位(名单)`/`作用域匹配(...)`/`断言为真(...)` 等表达式形态不再依赖
  预扫描兜底；减少对 COMMON_COMPOUND_WORDS 等保护表的依赖（见 R21-D3）。
- **订正 R20-D1**：第20轮的「收敛化最大匹配」（_EMBED_MAX_MATCH_KEYWORDS 三字集合）
  是本策略的过渡形态，第21轮已由「非语句起始位置最大匹配」通用策略替代，
  该集合的作用范围由新策略覆盖（详见对标 #132）。

### R21-D3 保护表精简与上游差异
- **差异**：上游无此类保护表；光明第21轮对 6+ 个复合标识符保护表完成审计
  （A类=新策略已覆盖可删/B类=部分覆盖精简/C类=必须保留），精简幅度与验证结果
  见 `_task3_R21_保护表审计报告.md`（若任务3 仅产出审计未精简，则本条改为
  「审计完成，精简待第22轮」）。
- **影响**：词法维护面收敛，减少逐词白名单生长。
```

## 四、执行核对清单（路M用）

1. [ ] 对标清单.json：追加 #131~#133（JSON 可解析校验 `python -c "import json;json.load(open(...))"`)
2. [ ] 版本字段：覆盖记录至第21轮
3. [ ] 缺陷账 L-152：按任务1 实际结果选「达成态/未达成态」措辞
4. [ ] 行为差异：追加 R21-D1~D3（任务3 未精简则 D3 改审计说明）
5. [ ] R20-D1 订正说明已在 R21-D2 内体现
