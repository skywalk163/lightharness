# 第 39 轮 任务 3 交付报告 —— system-prompt 核心模块复刻

> 日期：2026-09-16 ｜ 方向：上游复刻——@deepseek-ai/dsh-system-prompt（节/上下文注册 + assemble + 瀑布流）
> 上游基线：deepseek-harness 0.1.5-rc.2
> 上游源码：`packages/core/system-prompt/src/index.ts`
> 上游测试：`packages/core/system-prompt/tests/system-prompt.spec.ts`
> 交付物：`src/系统提示.light`（新增模块，与任务 4 合并写同一文件）、`examples/test_R39_系统提示.light`（单元测试，rc=0）

---

## 一、上游源码精读

| 关键点 | 上游实现 | 语义 |
|---|---|---|
| `PromptSection` | `{name, order, text, complete?}` | 系统提示节，text 静态或函数 |
| `PromptContext` | `{name, order, text}` | 动态上下文节 |
| `PromptAssembly` | `{sections, contexts, tools, variables}` | assemble 结果 |
| `AssembleContext` | `{scope, signal}` | 组装上下文（scope 作用域键，signal 中止信号） |
| `section()` | 注册节，重复 name 抛错，返回 undo | 幂等 undo |
| `context()` | 同上 | 幂等 undo |
| `tool()` | 注册工具提供者，返回 undo | 提供者返回 `{schemas, knownNames}` |
| `variable()` | 变量名合法校验（`^[a-z][a-z0-9_]*$`），返回 undo | 幂等 undo |
| `assemble(ctx)` | 节+上下文+工具+变量 → 排序 → 解析文本 → 处理 complete → 瀑布流 | 完整流程 |
| `PERSONA_PREFIX_SECTION` | `'deployment:persona-prefix'` | 可被 agent preset 替换 |
| `PERSONA_SUFFIX_SECTION` | `'deployment:persona-suffix'` | 部署后缀 |
| complete 语义 | 0 个正常，1 个恢复为唯一节，>1 个抛错 | 严格 |
| 瀑布流 | cordis 事件 `system-prompt/assemble`，监听器可修改结果 | 允许变更 |

**关键设计**：
1. name 唯一（重复即抛错），由调用方负责诊断上下文。
2. order 升序，同 order 按 name 代码单元序（tie-break 稳定）。
3. complete 处理是 assemble 的独立步骤，先于瀑布流。
4. 瀑布流是最终步骤，监听器拿到的组装结果可能已被前一个监听器修改。
5. 幂等 undo：注册函数返回 `撤销X` 高阶引用，撤销幂等（多次调用安全）。

---

## 二、光明适配设计

**模块名**：`src/系统提示.light`（命名预检查：src/ 下无同名，无单字别名风险）。

**核心数据结构**：
```
系统提示注册表 = {
  "节值表": {},            # {名字: 节}
  "节顺序": [],            # 插入顺序
  "上下文值表": {},
  "上下文顺序": [],
  "工具提供者": [],        # 匿名条目
  "变量表": {},
  "组装监听器": [],
  "形态": "系统提示注册表"
}
```

**光明适配点**：
1. **无闭包**：上游 section/context 返回的 undo 是闭包；光明改为「返回高阶函数引用（撤销节/撤销上下文/…），调用时显式传 (注册表, 名字)」。
2. **无 AbortSignal**：`AssembleContext.signal` 留空，字段保留占位。
3. **无原生正则**：变量名/引用用字符集 + 手动查找实现（见任务 4 报告）。
4. **无 NamedEntries 复用**：任务 2 的 NamedEntries 类在 `src/作用域.light`，本模块用「值表 + 顺序列表」组合（等价语义：插入有序、重复抛错、撤销幂等）。
5. **无 AbortSignal 依赖**：`文本(组装上下文)` 调用带 `尝试/捕获`，若函数不接受参数则退化到 `文本()`。

**导出清单**（本任务负责的核心 12 项 + 辅助）：
1. `建系统提示注册表()`
2. `注册节(注册表, 名字, 顺序, 文本, 完整=假)` —— 返回 `撤销节` 引用
3. `注册上下文(注册表, 名字, 顺序, 文本)` —— 返回 `撤销上下文`
4. `注册工具提供者(注册表, 提供者函数)` —— 返回 `撤销工具提供者`
5. `注册变量(注册表, 名字, 值)` —— 名字必须合法，返回 `撤销变量`
6. `注册组装监听器(注册表, 监听器函数)` —— 返回 `撤销组装监听器`
7. `组装系统提示(注册表, 组装上下文=空)` —— 核心 assemble
8. `排序节(节列表)` —— 按 order 升序、同 order 按 name
9. `排序上下文(上下文列表)` —— 同上
10. `解析节文本(节, 组装上下文)` —— 字符串直接返回，函数调用
11. `解析上下文文本(上下文, 组装上下文)` —— 同上
12. `处理完整节(组装结果)` —— 0/1/>1 完整节语义
13. `取节(注册表, 名字)` / `取上下文(注册表, 名字)`
14. `撤销节/上下文/工具提供者/变量/组装监听器`（撤销高阶函数，独立可调用）

任务 4 追加：`节顺序常量表`、`上下文顺序常量表`、`工具排序未列出标记`、`部署角色前缀节名/后缀节名`、`比较工具名字`、`校验工具排序`、`排序工具`、`校验变量名`、`扫描变量引用`、`插值`、`渲染提示`、`渲染上下文快照`。

---

## 三、关键实现

### 3.1 节/上下文注册（重复抛错 + 幂等 undo）
- 注册时用 `字典包含键` 检测重名，重复即抛错 `DUPLICATE_SECTION / DUPLICATE_CONTEXT`。
- 撤销函数是**高阶函数引用**：`撤销节(注册表, 名字)`。幂等：若已无该节则返回 `假`。
- 撤销时同步从「节顺序」列表移除，保持插入顺序一致性。

### 3.2 assemble 流程（`组装系统提示`）
1. **收集节**：按 `节顺序` 迭代，构造节对象 `{名字, 顺序, 文本, 完整}`。
2. **排序节**：按 `order` 升序、同 order 按 name 代码单元序（`比较节顺序`）。
3. **解析节文本**：字符串直接返回，函数调用 `文本(组装上下文)`；异常降级为 `文本()`。
4. **收集上下文**：同上。
5. **收集工具**：遍历工具提供者列表，调用后合并 `schemas`，聚合 `knownNames`。
6. **应用 toolOrder**：调用 `排序工具(schemas, 上下文["工具排序"], knownNames)`。
7. **收集变量**：复制 `注册表["变量表"]`。
8. **注入上下文**：把 `组装上下文` 放入结果（供监听器/后续使用）。
9. **处理 complete 节**：0 个不变，1 个恢复为唯一节，>1 个抛错。
10. **瀑布流**：依次调用监听器 `监听器(结果, 上下文)`，返回列表作为新结果（允许监听器修改）。

### 3.3 complete 处理（0/1/>1 语义）
- 0 个：直接返回原结果。
- 1 个：把该节文本作为唯一节，其它节清空，`完整=真` 保留。
- >1 个：抛错 `MULTIPLE_COMPLETE_SECTIONS`。

### 3.4 瀑布流监听器
- 注册表维护 `组装监听器` 列表。
- assemble 末尾按顺序调用，签名 `(组装结果, 组装上下文) -> 组装结果`。
- 监听器可原地修改返回（Light 中返回新字典，避免可变引用陷阱）。

---

## 四、单元测试覆盖（examples/test_R39_系统提示.light，rc=0，20 组）

| 编号 | 覆盖点 |
|---|---|
| #00 | 断言助手自检（真能报错） |
| #01 | 建注册表：形态、字段完整性 |
| #02 | 注册节 + 取节（字符串文本） |
| #03 | 重复节名抛错 |
| #04 | 注册上下文 + 取上下文 |
| #05 | 重复上下文名抛错 |
| #06 | 注册变量（合法/非法） |
| #07 | 组装：静态文本节 |
| #08 | 组装：函数文本节（动态上下文） |
| #09 | order 排序（升序 + tie-break by name） |
| #10 | 处理完整节：0 个 |
| #11 | 处理完整节：1 个（恢复为唯一节） |
| #12 | 处理完整节：>1 个抛错 |
| #13 | 瀑布流监听器修改组装结果 |
| #14 | 撤销（节/上下文/变量） |
| #15 | 撤销幂等（多次调用安全） |
| #16 | 工具提供者 + 默认按字典序排序 |
| #17 | 渲染提示（简单变量替换） |
| #18 | 渲染上下文快照（含头部） |
| #19 | 撤销组装监听器 |

**运行命令**：
```
cd lightharness && python 运行.py examples/test_R39_系统提示.light
```

**运行结果**：
```
OK #00
OK #01-01d
OK #02-02d
OK #03
OK #04-04c
OK #05
OK #06-06b
OK #07-07f
OK #08-08b
OK #09-09d
OK #10
OK #11-11c
OK #12
OK #13-13c
OK #14-14e
OK #15
OK #16-16c
OK #17
OK #18-18b
OK #19-19b
系统提示 判据通过：20 组
```

---

## 五、铁律合规检查

- ✅ **命名预检查**：src/ 下无「系统提示」同名模块，无单字别名冲突。
- ✅ **新增一律 .light**：`src/系统提示.light` + `examples/test_R39_系统提示.light`，无 Python 绕语言缺陷。
- ✅ **name 唯一（重复抛错）**：#03/#05 验证。
- ✅ **order 排序严格（升序 + tie-break by name）**：#09 验证。
- ✅ **complete 处理严格（0/1/>1）**：#10/#11/#12 验证。
- ✅ **瀑布流监听器可修改组装结果**：#13 验证。
- ✅ **测试用例 rc=0**：`python 运行.py examples/test_R39_系统提示.light` → EXIT=0。

---

## 六、光明适配差异（记录在案）

| 上游 | 光明适配 | 差异影响 |
|---|---|---|
| 闭包式 undo | 高阶函数引用 + 显式参数 | 调用形态不同，语义等价 |
| AbortSignal | `signal` 字段留空 | 中止能力未复刻（本轮不做） |
| 正则 `/^[a-z][a-z0-9_]*$/` | 字符集 + 手动循环 | 语义等价 |
| cordis 事件系统 | 简单监听器列表 | 事件订阅/解绑能力简化 |
| NamedEntries 类 | 值表 + 顺序列表组合 | 语义等价（插入有序 + 重复抛错 + 撤销幂等） |
| Python dict 迭代顺序 | Light 字典 `字典包含键` + 显式顺序列表 | 顺序由 `节顺序/上下文顺序` 显式维护 |

---

## 七、遗留项 / 与任务 5 接口

- **任务 4 已合并**：工具排序、变量插值、节顺序常量已合入 `src/系统提示.light`，分工边界明确。
- **任务 5 集成测试接口**：本模块导出 `组装系统提示`、`渲染提示`、`渲染上下文快照`、`节顺序常量表`、`上下文顺序常量表`、`排序工具`、`插值`，供 scope+system-prompt 联动使用。
- **agent-default-model 联动（R37）**：本模块的 `注册上下文` + `上下文顺序常量表` 支持把默认模型配置注册为系统提示上下文节。
