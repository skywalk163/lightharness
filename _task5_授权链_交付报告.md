# 第13轮·任务5｜授权链域 —— 交付报告

- 分支：任务5（授权链域），工作区 `G:\dswork\duan-light-merge\lightharness`
- 对照基准：上游 `G:\github\deepseek-harness` @ `a305303422`（0.1.5-rc.2，只读参考）
- 涉及文件：**新建** `src/授权链.light`（349 行）+ `examples/test_授权链.light`（266 行）+ `_antirun_授权链.py`（68 行）
- 任务书：`复刻_第13轮_任务prompt分发.md` §任务5（授权链域：authorization 注册表 + 校验链 + 单键单尝试状态机）
- 只读遵守：`src/会话查询.light` / `src/会话查询过滤.light` / `src/会话存储.light` / `src/会话冷读.light` / `src/查询工具.light` / `docs/功能对标/` / `scripts/` / `运行.py` / `stdlib/` 全部只读，零改动

## 1. 上游对应表

| 上游源文件（a305303422） | 上游语义 | 本路实现位置 |
|---|---|---|
| `packages/credentials/authorization/src/types.ts` | 类型形状：方法/通知/选项/提问(文本\|机密\|选择)/状态/结算/结果/条目 | `src/授权链.light` 构造方法/构造通知/构造选项/构造提问文本/构造提问机密/构造提问选择 |
| `packages/credentials/authorization/src/index.ts`（534 行） | `AuthorizationService`：注册流程（重复键抛 DUPLICATE_FLOW）/ list / describe / cancel / begin（校验链 NO_FLOW→UNKNOWN_METHOD→ALREADY_IN_FLIGHT→已中止→占槽→attempt→释放→settle 广播）/ attempt（中止竞态+提交观察+notify/prompt 包装+提交契约）/ settle（监听器失败隔离+INVARIANT 全跑完后重抛） | `src/授权链.light` 注册流程/列出流程/描述流程/取消流程/开始授权/执行尝试/结算广播/添加结算监听器 |
| `packages/credentials/authorization/src/invariant.ts` | Cordis 配套插件：settled 时校验 key 已释放（inFlight=false），否则 fail（INVARIANT 码） | 宿主面剔除；INVARIANT 码语义由结算广播的监听器失败隔离+重抛覆盖 |
| （跳过）`ctx.effect` / `events.dispatch` / `credentials` 事件 / `AbortSignal` | 宿主胶水：Cordis 上下文、事件总线、凭据记录读写、中止信号 | 未移植（见 §6 未移植项） |

## 2. 实现要点

### 2.1 错误传递约定（关键适配）

光明语言 `抛出` 会把字典/自定义类对象包装成 Python Exception，自定义属性（如 `.码`）丢失。本模块统一采用：
- 抛出：`抛出 新建 错误("[" + 码 + "] " + 消息)`
- 捕获：`转字符串(错误)` + `解析授权错误(文本)` 还原 `{码, 消息}`
- 错误族：授权错误（码 NO_FLOW/UNKNOWN_METHOD/ALREADY_IN_FLIGHT/NOT_COMMITTED/DUPLICATE_FLOW）、授权被拒错误（码 DECLINED，默认文案"授权提示被拒绝"）

### 2.2 注册表（对齐 registerFlow）

- `注册流程(服务, 流程)`：重复键抛 DUPLICATE_FLOW；登记 + 返回注销函数（注销时移除流程，若该键在跑则置中止标志 + 调用中止处理器）
- `列出流程(服务)` / `描述流程(服务, 键)`：条目形状 `{键, 标签, 方法表, 在飞}`
- `取消流程(服务, 键)`：置运行上下文中止标志 + 调用中止处理器表

### 2.3 入口 开始授权（校验链顺序不可乱）

对齐上游 `begin` 的校验链顺序：
1. **NO_FLOW**：无流程 → 抛 `没有为 "X" 注册授权流程`
2. **UNKNOWN_METHOD**：方法解析（默认流程首方法）+ 方法集校验 → 抛 `授权流程 "X" 不提供方法 "Y"`
3. **ALREADY_IN_FLIGHT**：单键单尝试，运行表含键 → 抛 `"X" 的授权尝试已在运行`
4. **已中止**：请求已中止 → 直接返回 `{状态:"cancelled"}`（不占槽，校验仍先执行）
5. **占槽**：运行上下文 `{中止标志:假, 中止处理器表:[]}` 写入运行表
6. **注册外部中止处理器**：模拟 `request.signal` abort 事件
7. **执行尝试** + finally 模拟（释放槽 → 结算广播 → 重抛尝试错误）

### 2.4 执行尝试（对齐 attempt）

- **中止竞态**：流程运行前/后检查中止标志，已中止 → cancelled
- **提交观察**：模拟 `credentials/record-updated` 事件，注册提交观察器到服务提交观察表，流程内 `会话.提交(键)` 触发
- **notify 包装**：`交互.通知` 抛错吞掉记日志（fire-and-forget 语义）
- **prompt 包装**：`交互.提问` 抛被拒错误 → 置已拒绝标记 + 重抛
- **运行抛错分类**：被拒错误/已中止 → cancelled，其他错误 → 重抛（→ failed）
- **提交契约**：运行返回后未提交 → 抛 NOT_COMMITTED `授权流程 "X" 在此尝试中解析但未提交凭据记录`
- 提交且未中止 → authorized

### 2.5 结算广播（对齐 settle）

- 遍历监听器表，每个监听器 `(键, 结算)` 调用
- 监听器失败隔离：普通失败记日志不改变结果；码 INVARIANT 的失败存为 `不变失败`，全部监听器跑完后重抛
- 结算值：authorized / cancelled / failed（尝试抛错时结算保持 "failed"）

### 2.6 中止模拟（光明无 AbortSignal）

- 运行上下文 `{中止标志, 中止处理器表}` 替代 AbortController
- 请求可选 `注册中止处理器` 段落（外部取消时调用），内部 `外部中止` 段落置中止标志 + 调用中止处理器表
- 流程内通过 `会话.是否中止()` 读取中止标志（同步简化：前置检查拦截，流程运行中无法被外部异步中止）

## 3. 测试与验证

### 3.1 定向测试（新增，全绿 RC=0）

`examples/test_授权链.light` → 输出 `test_授权链 PASS`，覆盖 11 组场景：

| 组 | 覆盖 |
|---|---|
| 1. 类型形状 | 三种提问(文本/机密/选择) / 方法 / 通知 / 选项 / 状态 / 结算 |
| 2. NO_FLOW | 未注册流程 → 抛 NO_FLOW |
| 3. 注册流程 + UNKNOWN_METHOD | 重复键 DUPLICATE_FLOW（注册时验证）/ 方法不在集 → UNKNOWN_METHOD / 列出流程 / 描述流程条目形状 |
| 4. 成功授权 | 流程提交凭据 → authorized / 成功后槽已释放 |
| 5. NOT_COMMITTED | 流程运行但未提交 → 抛 NOT_COMMITTED（反跑点B） |
| 6. DECLINED | 交互提问抛被拒错误 → cancelled |
| 7. 已中止 | 请求已中止 → 直接 cancelled（不占槽） |
| 8. ALREADY_IN_FLIGHT | 第一个尝试运行中发起第二个 → 第二个抛 ALREADY_IN_FLIGHT（反跑点A）；第一个尝试 authorized |
| 9. 注销 | 注销后流程消失 / 外部中止处理器触发后 cancelled |
| 10. 结算广播 | failed 结算不改变调用方结果（调用方仍收到内部错误码）/ authorized 结算 / INVARIANT 监听器全跑完后重抛（反跑点C，普通监听器在 INVARIANT 前已执行） |
| 11. 取消流程 | 取消流程置中止标志 → cancelled |

### 3.2 反跑判据（机器验证）

`python _antirun_授权链.py` → **3/3 PASS（改反即红 rc=1，恢复即绿 rc=0），输出 ALL OK**：

1. **A｜ALREADY_IN_FLIGHT 校验失效**：`如果 字典包含键(服务["运行表"], 键)` 改为 `如果 假` → 单键单尝试断言红；恢复 → 绿
2. **B｜NOT_COMMITTED 不再抛**：`抛授权错误(..., 码未提交)` 改为 `返回 ["状态":"authorized"]` → 提交契约断言红；恢复 → 绿
3. **C｜结算广播去掉 INVARIANT 重抛**：`抛出 不变失败` 改为 `设 占位 为 0` → INVARIANT重抛断言红；恢复 → 绿

脚本自含字节级备份/恢复 src，恢复后源文件与备份逐字节一致。

### 3.3 既有回归（全绿）

- `examples/test_会话查询1.5.light`（PASS，RC=0）
- `examples/test_会话查询过滤.light`（通过，RC=0）
- `examples/test_修复_repair接入.light`（通过，RC=0）
- `examples/test_会话冷读.light`（通过，RC=0）
- `examples/test_查询工具.light`（通过，RC=0）

按铁律 6 只跑本路新增测试 + 直接相关既有回归，未跑全量 CI（由路M 统一执行）。

## 4. 光明语言适配与发现的缺陷（本次实测记录）

本次任务在实现过程中发现并绕过了以下光明语言特性/限制，建议路M 统一评估是否登记语言缺陷账：

| # | 现象 | 绕过方式 | 严重度 |
|---|---|---|---|
| D1 | **字典属性访问不可用**：字典普通值必须用下标 `obj["key"]`，不能用 `obj.key`（Python dict 无属性访问）；方法调用也必须用 `obj["method"]()` | 全模块统一用下标访问 | 高（全模块适配） |
| D2 | **if 块内段落定义导致缩进跟踪错误**：在 `如果` 块内定义嵌套段落，编译器会把段落提升并导致后续代码被编译到函数外部（模块级），引发 `name '服务' is not defined` | 嵌套段落定义移到 `如果` 块之前（函数级） | 高（编译器 bug） |
| D3 | **段落从列表/字典取出调用时 nonlocal 绑定丢失**：段落对象存入列表再取出调用时，闭包修改外层变量（`设 外层 为 新值`）不生效；但字典属性修改（`设 状态["键"] 为 值`）生效 | 监听器/回调状态统一用字典对象存储，不用闭包修改外层变量 | 高（闭包 bug） |
| D4 | **`抛出` 包装字典/自定义类丢失属性**：`抛出 {码:"X"}` 或 `抛出 新建 自定义错误(...)` 被转成 Python Exception，`.码` 访问失败 | 统一用 `[CODE] 消息` 前缀 + `转字符串(错误)` + 解析函数 | 中（已形成约定） |
| D5 | **空字典构造**：`{}` 和 `[:]` 均不支持，空字典须用 `字典()` 内置 | 用 `字典()` | 低 |
| D6 | **`回调` 是保留关键字**：不能用作变量名/遍历变量名 | 用 `处理器` / `监听器` / `观察器` | 低 |
| D7 | **`返回` 关键字作变量名前缀被词法拆分**：如 `返回x段落` 被拆成 `返回` + `x段落` | 用 `给出x段落` 等避开 | 低 |
| D8 | **不支持默认参数值**：所有函数参数必须显式传值 | 构造函数可选参数调用方传 `空`，函数内判空处理 | 低 |
| D9 | **可选字典键访问**：`obj["key"]` 对不存在的键抛 KeyError；可选字段须用 `取可选(obj, "key")` 或 `字典包含键` 先检查 | `开始授权` 的可选请求字段（方法/已中止/注册中止处理器/交互）统一用 `取可选` | 低 |

**src 内部闭包已全部用字典属性修改**（`观察["已提交"]` / `观察["已拒绝"]`），不受 D3 影响。

## 5. 未移植项（明确保留）

- **宿主胶水**：`ctx.effect`（Cordis 上下文注入）、`events.dispatch`（事件总线）、`credentials` 事件（凭据记录读写）全部剔除，查询工具保持纯逻辑面。
- **AbortSignal**：光明无异步中止信号，用运行上下文 `{中止标志, 中止处理器表}` 同步模拟；流程运行中无法被外部异步中止（同步简化限制，已在测试中适配）。
- **invariant.ts Cordis 插件**：宿主配套插件不移植；INVARIANT 码语义由结算广播的监听器失败隔离+重抛覆盖。
- **工具注册**：授权链不注册进任何工具注册表（纯逻辑面，由宿主/路M 统一决策接入点）。

## 6. 移交清单

- `src/授权链.light`（新建，349 行）——任务5 授权链核心（导出 构造授权服务/注册流程/列出流程/描述流程/取消流程/添加结算监听器/开始授权/执行尝试/结算广播/构造方法/构造通知/构造选项/构造提问文本/构造提问机密/构造提问选择/解析授权错误/抛授权错误/抛被拒错误/错误码常量）
- `examples/test_授权链.light`（新建，266 行）——11 组场景，`test_授权链 PASS`
- `_antirun_授权链.py`（新建，68 行）——反跑判据 3/3 PASS（ALL OK）
- 临时文件：开发过程中产生的 `_tmp_*.light` / `_tmp_*.py` / `_tmp_*.txt` 探针文件已全部清理；无测试数据目录残留

## 7. 语言缺陷账判定

**本次发现 9 项光明语言特性/限制（D1-D9，见 §4），其中 D1（字典属性访问不可用）、D2（if 块内段落定义缩进错误）、D3（段落从列表取出调用 nonlocal 丢失）为高严重度编译器/运行时 bug，建议路M 统一评估是否登记 L-0xx 及最小复现。**

按第13轮铁律「docs/功能对标/ 保持只读（路M 统一）」，本路未修改语言缺陷账，仅在本报告标注。所有发现的缺陷均已在本路实现中绕过，不影响交付物功能。
