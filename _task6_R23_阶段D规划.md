# 第23轮 阶段D规划 —— _COMPOUND_SAFE_SINGLE_KEYWORDS 单字通用化

> 日期：2026-09-14 ｜ 供第24轮或后续轮次参考
> 背景：第23轮完成保护表通用化替代阶段A+B+C，3表清零（_TRAILING_ALIAS_MERGE/IDENTIFIER_SAFE_KEYWORDS/IDENTIFIER_SAFE_SUFFIX_ONLY_KEYWORDS），CCW 184→37条。剩余_COMPOUND_SAFE_SINGLE_KEYWORDS（30条单字）为阶段D，风险最大，须先解决已知独立缺陷再做全仓token A/B。

---

## 一、_COMPOUND_SAFE_SINGLE_KEYWORDS 现状

### 30条单字清单
乘/余/例/减/出/列/则/到/加/对/常/引/接/断/是/末/模/段/的/真/空/类/自/试/跳/过/配/长/除/首

### 引用位置
- `_match_keyword`：关键字匹配时的单字构词保护
- `_skip_compound_safe`：跳过复合安全单字
- `_tokenize_chinese_sequence`：汉字序列切词时的单字保护
- `_P0A_COMPOUND_SAFE`：P0-A确定性切词的复合安全集合

### 保护场景分类
1. **运算符单字**：乘/减/加/除/余（与OPERATOR_VERBS重叠，运算符语义核心）
2. **数据结构单字**：列/类/模/段（列表/类名/模块/段落等高频后缀）
3. **控制流单字**：则/到/是/真/空（如果...则/从...到/是/真值/空值）
4. **动作单字**：出/接/断/试/跳/过/配/引/首/末/长/对/常/自/的（输出/接收/中断/尝试/跳过/通过/配置/引用/首先/末尾/长度/对应/常量/自己/的）

---

## 二、已知独立缺陷（须先解决）

### 缺陷1：`的`递归吞分隔符
- **现象**：`的`作为结构助词（如`我的书`/`函数的参数`）在某些上下文会递归吞入后续分隔符，导致切词错误
- **影响**：`的`是30条中引用频率最高的单字，直接通用化风险最大
- **解决方案**：
  1. 分析`的`在词法中的所有引用分支
  2. 限定`的`的并入条件：仅当`的`位于汉字词词中（前后均为汉字/字母）时并入，词尾（后随空白/符号/换行）保持单字切分
  3. 建设`的`专项测试套件（`我的`/`函数的`/`的参数`/`红色的花`等）
  4. 全语料token A/B验证

### 缺陷2：运算符单字的表达式上下文歧义
- **现象**：乘/减/加/除/余既是运算符（`甲 加 乙`），又可作标识符一部分（`加法`/`减法`/`乘法表`）
- **影响**：直接通用化可能导致运算符表达式被误并为标识符
- **解决方案**：
  1. 运算符单字已在OPERATOR_VERBS中，表达式上下文有运算符守卫
  2. 验证运算符守卫是否覆盖所有场景（`甲加乙`无空格时是否正确切分）
  3. 建设运算符单字专项测试（`加法`/`减法`/`甲 加 乙`/`甲加乙`等）

### 缺陷3：`真`/`空`值字面量关键字
- **现象**：`真`/`空`是值字面量关键字（KEYWORDS_SPECIAL），同时可作标识符一部分（`真实`/`空白`/`空值`）
- **影响**：直接通用化可能导致值字面量被误并
- **解决方案**：
  1. 值字面量关键字已有_VALUE_LITERAL_KEYWORDS守卫
  2. 验证守卫是否覆盖词中/词尾场景（`真实`/`空白`应并入，`设 甲 为 真`应保持关键字）
  3. 建设值字面量专项测试

---

## 三、阶段D实施路径

### 步骤1：已知缺陷修复（独立子轮或任务1-2）
- 修复`的`递归吞分隔符缺陷
- 验证运算符单字/值字面量守卫覆盖
- 建设专项测试套件
- 全量CI无回归

### 步骤2：逐条隔离中立验证（任务3-4）
- 对30条单字逐条执行判据③（撤掉后全语料token零变化=冗余可删）
- 分类：①冗余可删 ②真护栏（需通用规则覆盖） ③运算符/值字面量（不动）
- 对分类②的单字，分析保护场景，设计通用规则

### 步骤3：通用规则设计与实现（任务5）
- 基于步骤2的分析，设计单字通用化规则
- 可能的规则方向：
  - 非语句起始位置 + 单字关键字位于汉字词词中（前后均为汉字/字母）→ 并入标识符
  - 词尾位置（后随空白/符号）保持单字切分（运算符/值字面量语义）
  - 排除集：硬语句头/运算符/值字面量在特定上下文保持切分
- 实现后全语料token A/B验证

### 步骤4：全仓token A/B与回归（任务6+路M）
- 全量776文件token序列对比
- 编译结果对比
- codegen输出对比
- 性能对比
- 全量CI无回归
- 反跑ALL OK

---

## 四、风险评估与缓解

| 风险 | 说明 | 缓解 |
|---|---|---|
| `的`递归吞分隔符 | `的`引用频率最高，直接通用化可能引发大量回归 | 步骤1先修复缺陷，建设专项测试 |
| 运算符表达式误并 | 乘/加/减等运算符单字通用化可能导致`甲加乙`被误并 | 运算符守卫已存在，验证覆盖后再动 |
| 值字面量误并 | `真`/`空`通用化可能导致`设甲为真`被误并 | 值字面量守卫已存在，验证覆盖后再动 |
| 合法语法破坏 | 单字关键字在语句起始位置的语义（`则`/`到`/`是`等） | 规则限定"非语句起始位置"，语句起始保持关键字优先 |
| 性能回退 | 单字通用化规则可能增加词法判定开销 | P0-A热路径优化，每步跑性能对比 |

---

## 五、收益与目标

- 最终目标：**消除所有逐词白名单**，词法层只保留"上下文规则 + 内建名登记 + 运算符/值字面量关键字"三类结构，与上游deepseek-harness「无保护表」形态完全对齐
- 阶段D完成后，逐词白名单从当前约73条（CCW 37 + _COMPOUND_SAFE_SINGLE 30 + _EMBED 3 + OPERATOR_VERBS 19派生）降至约40条（CCW真护栏 + _EMBED + OPERATOR_VERBS，其中CCW真护栏可继续评估）
- 建议阶段D单独立轮（第24轮或更后），不与其他方向混合

---

## 六、证据链

- 第21轮：_task3_R21_保护表审计报告.md（_COMPOUND_SAFE_SINGLE 52条审计）
- 第22轮：_task3_R22_COMPOUND_SAFE_SINGLE精简_交付报告.md（52→30条，删22条冗余）
- 第23轮：_task2_R23_IDENTIFIER_SAFE清表_交付报告.md（后缀位置上下文规则设计，可复用于单字通用化）
- 已知缺陷：lexer.py中`的`/`幂`等单字的注释（递归吞分隔符风险）
