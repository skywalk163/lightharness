# 第22轮 任务4 交付报告：_TRAILING_ALIAS_MERGE + COMMON_COMPOUND_WORDS TierB 护栏精简

> 日期：2026-09-14 ｜ 任务书：复刻_第22轮_任务prompt分发_保护表深度精简轮.md ｜ 状态：已完成
> 修改文件：light-merge/src/lexer.py（_TRAILING_ALIAS_MERGE 定义 + COMMON_COMPOUND_WORDS 定义，仅增删条目与注释，未改引用代码）

---

## 一、精简结论

| 表 | 精简前 | 删除 | 精简后 | 幅度 |
|---|---|---|---|---|
| _TRAILING_ALIAS_MERGE | 10 条 | 9 条 | 1 条 | 90% |
| COMMON_COMPOUND_WORDS（TierB 32 条重估） | 216 条 | 32 条 | 184 条 | 表整体 14.8% |
| 合计 | | 41 条 | | |

## 二、_TRAILING_ALIAS_MERGE 逐条验证（10 条）

机读证据：lightharness/_sweep_r22_t4_tam_results.json；可复跑脚本：lightharness/_sweep_r22_t4_trailing_alias.py。

| 条目 | 补回后语料变化 | 判定 |
|---|---|---|
| 己 | —（保留） | **护栏**：删除后 lightharness/examples/test_L120.light（L-120 复现用例）token 变化 |
| 遍/捕/抛/终/返/设/承/宏/掷 | 均 0 文件 | 冗余，删除 |

L-120 关联验证：精简后 test_L120.light 经 lightharness/运行.py 编译运行 rc=0（反跑 D 判据），token 层 `己` 护栏仍有效（反跑 C1 判据）。L-120 修复的主体逻辑（词尾单字别名并入标识符）不受影响——被删 9 条的场景已被第21轮上下文敏感切词覆盖。

说明：`设` 虽是赋值关键字，但语料级验证显示「词尾并入」分支对现有 763 文件零影响（`设` 作语句关键字仅出现在词首，走关键字分支），删除中立成立。

## 三、COMMON_COMPOUND_WORDS TierB 32 条重估

背景：第21轮任务3把这 32 条「语料未命中但隔离不中立」条目标记为 TierB 保留待评估；第21轮任务2「非语句起始位置上下文敏感最大匹配」落地后，本轮逐条重估。

机读证据：lightharness/_sweep_r22_t4_ccw_results.json；可复跑脚本：lightharness/_sweep_r22_t4_ccw_guard.py。

| 分组 | 条目 | 判定 |
|---|---|---|
| 工程/承载/压缩 | 弹性模量、承载力、承载能力、压缩模量 | 冗余，删除 |
| 农业含字族 | 当前含、当前含水、当前环节、当前环节数 | 冗余，删除 |
| 数据各乘除族 | 各乘数据、各减数据、各加数据、各除数据 | 冗余，删除 |
| 滚动统计族 | 滚动乘积、滚动加权、滚动求和 | 冗余，删除 |
| 过滤模族 | 过滤模零、过滤模二零、过滤模二非零、过滤模十零、过滤模零集合 | 冗余，删除 |
| 文本去重族 | 去重行、文本去重行 | 冗余，删除 |
| 其余 | 当家、当选、格式模式、清除率、清除速率、生成分析阶段、甲位与乙、终期折现因子、联合体成员、跳过迭代 | 冗余，删除 |

**32 条全部冗余**：每条补回后全语料 763 文件 token 序列零变化。第21轮 D 判据（隔离单词 tokenize 变化）与第22轮重估（全语料级）结论不同，原因即第21轮任务2上下文敏感切词落地——原先在合成串里会被切碎的场景，现按最大匹配整体成 IDENTIFIER。典型例证：第21轮的正向控制 `弹性模量` 本轮已中立。

正向控制：清空 CCW 整表后语料 token 变化远大于 0（_r22_t4_ccw_sweep_out.txt），证明扫描 harness 有效性；即剩余 184 条中仍有真护栏（如 `异步读取文件`/`并发等待` 等有 codegen 映射的复合名、`低级关闭`/`常量时间比较` 等系统原语），本轮不动。

### 通用替代策略建议（详见 lightharness/_task6_R22_保护表通用化策略建议.md）

TierB 32 条全军覆没的直接教训：**逐词白名单是上下文规则缺位时的补丁，规则一旦补齐补丁即失效**。建议后续轮次以「非语句起始位置 + 非运算符关键字前缀 + 后随汉字/字母 → 整体成标识符」的通用规则逐步替代剩余 184 条 CCW 与 5 条 IDENTIFIER_SAFE；有 codegen 映射的复合名应迁移到内建表（builtin_map）而非词法保护表。

## 四、验证与回归

1. **逐条隔离中立验证**：9（TAM）+32（CCW）条删除条目补回后全语料 token 零变化。
2. **精简后全语料对比**：与任务2合并后的最终态，全语料 763 文件 token 序列零变化。
3. **单测**：词法相关 5 个测试文件与 HEAD 基线完全一致（9 failed 均为既有噪音）。
4. **全量 CI**：light-merge pytest 全量 + lightharness smoke 见本轮收口记录；第22轮任务5全量回归扫描覆盖。
5. **反跑**：lightharness/_antirun_r22_t4_trailing_alias+ccw精简.py —— A 基线自检 / B 删除中立（41 条补回零变化）/ C1 `己` 护栏正向控制 / C2 CCW 清空正向控制 / D test_L120.light 编译 rc=0，ALL OK（输出 _r22_t4_antirun_out.txt）；finally 恢复模块属性，不落盘修改。

## 五、通用约束遵守情况

- 文件互斥：仅改 _TRAILING_ALIAS_MERGE 定义与 COMMON_COMPOUND_WORDS 定义（任务4互斥区域），未触碰 _EMBED_MAX_MATCH_KEYWORDS（任务1区域，其定义行 539 与本表相邻不重叠）与引用代码。
- 只删不增；逐条验证未批量删除；保留条目（己）有依赖证据。
