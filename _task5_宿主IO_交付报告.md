# 第17轮任务5 交付报告 —— 宿主IO抽象层原型

> 对标编号：#114 ｜ 差异编号：R17-D5 ｜ 缺陷预分配：L-149
> 模块：`src/宿主IO.light`（410行 / 59段）｜ 测试：`examples/test_宿主IO.light`（133行）
> 反跑：`_antirun_t5_宿主IO.py` ｜ 日期：2026-09-13

## 一、上游对应与实现映射

自包含内存模拟原型（不依赖 Node fs/child_process/net/stream），接口层定义 + 内存模拟实现：

| 设计面（任务书） | 光明实现（`宿主IO.light`） | 说明 |
|---|---|---|
| IO结果统一形状 | `造成功结果`/`造失败结果`/`是成功结果`/`是失败结果`/`IO错误码表`(8项) | 成功 `{成功,数据,元数据}` / 失败 `{成功,错误码,错误消息,元数据}` |
| IO取消令牌 | `造取消令牌`/`取消令牌`/`监听取消`/`令牌已取消`/`校验未取消` | 令牌 `{是否已取消,取消原因,监听表,请求ID}`，取消触发全部监听 |
| IO超时与重试 | `带重试执行`/`退避延迟`/`合并元数据` | 重试策略 `{最大重试次数,初始延迟,延迟倍增因子,可重试错误码列表}`；原型不真睡，仅计 `重试次数` |
| 文件系统提供者（内存） | `造内存文件系统`+`写/读/删除/列目录/创建目录/重命名/获取文件信息` | 字典<路径,节点> 模拟，支持目录层级 |
| 路径操作 | `拼接路径`/`规范化路径`/`获取父目录`/`获取文件名`/`获取扩展名`/`是绝对路径` | 规范化支持 `.`/`..` |
| 进程提供者（内存） | `造内存进程表`+`预设进程结果`/`启动进程`/`取进程结果`/`终止进程`/`列举进程` | 字典<进程ID,句柄>，等待时返回预设退出码/输出 |
| IO事件（轻量） | `造IO事件总线`/`派发IO事件`/`监听IO事件` | 监听器表 + `历史` 字段记录派发 |

## 二、实现要点

1. **IO结果形状**：统一 `{成功,数据/错误码/错误消息,元数据}`，判别用 `是成功结果`/`是失败结果`（显式布尔，规避 L-129 空对象判真）。
2. **取消令牌**：`取消令牌(令牌,原因)` 标记后遍历 `监听表` 逐个触发回调（catch 兜底，单监听异常不阻断）。
3. **重试判定**：`带重试执行` 先试执行→成功即返回；失败看 `可重试表.包含(码)`：不可重试或超上限立即返回，可重试则按 `退避延迟` 继续（不真睡）。
4. **内存 FS/进程**：路径规范化后以字典承载；进程 `等待` 读 `预设` 表回填退出码/输出（测试可断言）。
5. **IO事件**：`派发IO事件` 把 `{事件名,数据}` 追加进 `总线["历史"]`，再触发注册监听器——**事件接收记录承载在总线字典上**，不依赖模块级共享变量。

## 三、验证

- **测试 `examples/test_宿主IO.light`**：**44 断言**（34 断言相等 + 10 断言真），`test_宿主IO PASS`（RC=0）
  - 覆盖 §1 结果形状 / §2 取消令牌 / §3 路径操作 / §4 内存文件系统 / §5 内存进程 / §6 超时重试 / §7 IO事件，正常·边界·异常三态。
- **反跑 `_antirun_t5_宿主IO.py`**：**3/3 ALL OK**（sha256 `87815a329b2b`）
  - A 文件系统读错内容 → 4c 红；B 进程退出码被改 → 5b 红；C 重试判定翻转 → 6c 红；恢复后回归绿。

## 四、语言缺陷处置（L-149 空缺）

**本轮未遇到真实新语言缺陷，L-149 空缺顺延。**

排查过程特别说明（避免误报缺陷）：
- 初判「模块级 `段落` 无法捕获模块级 `设` 变量」——经最小探针隔离，实为**标识符笔误**：handler 参数声明为中文 `数据`，函数体误写为英文 `data`，导致 `data` 未定义。修正为 `数据` 后，模块级 `段落` 对模块级 `设` 列表的 `.append` 正常生效（复现 length=1）。**故非语言缺陷，不登记。**
- 已规避的保留字陷阱（属既有大类，不占新编号）：
  - `等待` 编译为 `await`（全局正则 `等待\s+<词>`→`__await__`，注释/变量名亦触发）→ 段名/变量名避开 `等待`（改 `取进程结果`）。
  - `通过` 在 `捕获` 块非有效空操作（被当未定义变量）→ 改 `设 占位 为 真`。
  - `定义`/`回调` 为保留字 → 已改 `条目`/`处理器`（同任务4 大类）。

## 五、剔除与偏差

- 上游 `stream` 的背压/pipe、net/http 的 connection/timeout 等**未逐行复刻**（任务书明确定位为接口层原型，仅定义接口 + 内存模拟）。
- 超时控制为「原型不真阻塞」：操作函数自身决定返回 TIMEOUT/CANCELLED，`带超时执行` 仅做取消校验与透传；不实现真实计时器。
- 进程 `等待` 为同步回填预设结果，不模拟真实子进程生命周期调度。

## 六、移交

- 对标 #114 / 差异 R17-D5，交付物：模块 + 测试（44断言） + 反跑（3/3） + 本报告。
- 缺陷账 L-149 空缺，由路M 统一回填（铁律③ docs 归路M）。
- 全程零越界：仅新增互斥文件表内文件，未修改任何既有 `src/` 模块，未 commit。

---
*纪律：中文回复 / 反跑自定位 BASE=__file__ / 测试≥12断言 / 反跑 A/B/C 三判据 / 不碰 docs 与既有模块。*
