# 第20轮 任务6 · docs 更新草案（供路M收口统一写入）

> 本文件为**草案**，不直接改 `docs/`（docs 三件归路M收口统一写入）。
> 每条给出「文件 → 定位锚点 → 拟写入内容 → 理由」，可直接复制执行。
> 生成日期：2026-09-14 ｜ 提交人：第20轮任务6

---

## 一、`docs/功能对标/对标清单.json` —— 追加 #128 ~ #130

当前：`版本 = 0.1.5-rc.2（a305303422，2026-09-10）；覆盖记录至第7轮（2026-09-11）`，`条目` 长度 **127**（末条 #127）。
拟：`条目` 追加 3 条 → 长度 **130**；`版本` 字段建议改为
`0.1.5-rc.2（a305303422，2026-09-10）；覆盖记录至第20轮（2026-09-14）`。

```json
{"编号": 128, "功能": "编译器词法回归修复（关键字前缀标识符 + `X为` 赋值尾巴）", "原版包": "编译器回归修复轮（第20轮）；修复 L-151/L-152", "光明模块": "light-merge/src/lexer.py", "状态": "已完成", "完成轮次": "第20轮", "备注": "P0；修复 `设 合并为 {}` 的 `为` 丢失（例外①②真根因）；新增函数调用语境守卫（整串后紧随 '(' 不切分），修复 6 个 `断言为真(...)` 类解析回归；全量 examples 296 文件 284 绿→修复后 293 绿+2 EXPECT_RED+1 既有红"},
{"编号": 129, "功能": "编译器 HMAC-SHA256 标准实现对拍（RFC 2104）", "原版包": "编译器回归修复轮（第20轮）；L-095 由「部分修复」订正为已修复", "光明模块": "light-merge/stdlib/哈希.py + lightharness/stdlib/哈希.py", "状态": "已完成", "完成轮次": "第20轮", "备注": "P0；7 组对拍（标准 2 + 边界 5：空密钥/空消息/长密钥>64B/中文）与 Python hmac 逐字节一致；反跑含 A2 直接 import 双份哈希.py 对拍 + B 其他哈希不受影响 + C 变异立红"},
{"编号": 130, "功能": "词法边界测试套件（关键字前缀标识符 100+ 场景）", "原版包": "编译器回归修复轮（第20轮）", "光明模块": "lightharness/examples/test_R20_关键字前缀标识符.light + test_R20_词法边界综合.light + tests/test_R20_词法边界.py", "状态": "已完成", "完成轮次": "第20轮", "备注": "P1；三层覆盖（运行期自校验 .light / pytest token 层 / 全项目扫描脚本）；防「关键字前缀标识符被切碎」家族回归"}
```

⚠️ 第 128 条的「293 绿」需以路M收口时的**最终全量 CI 数字**为准替换（本草案数字取自 2026-09-14 全量 examples 扫描）。

---

## 二、`docs/功能对标/语言缺陷账.md` —— 状态订正 + 新增 2 条

### 2.1 订正①：汇总表 L-095 行（现第 554 行附近）

**现状**
```
| L-095 | HMAC_SHA256非标准HMAC | 绕法指南（自洽链路内使用，对外宿主校验） | 词法安全  **第19轮状态更新：已修复（2026-09-14；第19轮任务4字符串哈希，HMAC改为标准RFC 2104双轮哈希实现，复现test_L095绿+反跑ALL OK）** |
```
**拟改为**（补第20轮复核结论，消除「已修复 vs 收口登记 EXPECT_RED」的自相矛盾）
```
| L-095 | HMAC_SHA256非标准HMAC | 绕法指南（自洽链路内使用，对外宿主校验） | 词法安全  **第20轮订正：已修复（2026-09-14；第19轮任务4已把实现改为 `hmac.new(key,msg,sha256)`，第20轮任务2 以 7 组对拍逐字节复核确认；第19轮收口登记的 EXPECT_RED「部分修复」系状态滞后——其观察值 5b94b938… 正是修复前单轮 PBKDF2 输出。test_L095 rc=0，EXPECT_RED 已移除）** |
```

### 2.2 订正②：汇总表 L-097 行

**现状**（措辞偏强，「修复」实为「验收确认」）
```
| L-097 | if块内嵌套段落提升 | 绕法指南（段落定义放模块级） | 函数增强  **第19轮状态更新：已修复（2026-09-14；第19轮任务6字典与函数，if块嵌套段落解析修复，复现test_L097绿+反跑ALL OK）** |
```
**拟改为**
```
| L-097 | if块内嵌套段落提升 | 绕法指南（段落定义放模块级） | 函数增强  **第20轮订正：已修复（既有实现已覆盖，第19轮任务6为「验收确认」而非新修——`49319306` 未触及嵌套定义路径；test_L097 rc=0）。残留限制见 L-152** |
```

### 2.3 L-095 详述段（第 264~278 行附近）追加

在「**裁定**：L-095 为**编译器侧缺陷**…」之后追加：
```
**第20轮订正（2026-09-14）**：实现侧已在**第19轮任务4**改为 Python 标准库 `hmac.new(key, msg, hashlib.sha256)`，
标准 RFC 2104 语义（`H(K⊕opad, H(K⊕ipad, text))`；密钥 <64B 补 0x00、>64B 先 SHA256）。
第20轮任务2 补 7 组对拍（含空密钥/空消息/长密钥>64B/中文），与 Python `hmac` **逐字节一致**；
反跑 `_antirun_r20_t2_HMAC标准实现.py` A1/A2/B/C 全 PASS。
> 注：`.light` 语言层仍缺「原始字节哈希」原语（stdlib `SHA256` 只返回 hex），故 .light 侧**仍无法手写 HMAC**，
> 本修复是把标准实现下沉到 stdlib(.py) 侧；「编译器侧提供原始字节哈希接口」的诉求继续挂 A9。
```

### 2.4 新增 L-151：`设 X为<值起始>` 赋值尾巴并坏

插在汇总表之后（或详述段末尾），编号顺延（当前最大 **L-150**）：

```
## L-151（第20轮任务1，2026-09-14）赋值尾巴 `X为<值>` 的 `为` 被并入标识符（报错位置误导）

**现象**：`设 合并为 {}` 中 `合并为` 被词法整体成 IDENTIFIER，赋值关键字 `为` 丢失 →
解析报「期望'为'或'等于'，但得到 「{」」；且经跨模块导入时 `format_error` 会把位置
错锚到入口文件的其它行（如 `宿主上下文.light:100` 被报成 `test_宿主上下文.light:100`），
形成「明明写的是 `==` 断言却报 `{}`」的误导。

**最小复现**：`设 合并为 {}`（词法应产 `设 / 合并 / 为 / { / }`）。
`examples/test_宿主上下文.light`（rc=0）、`examples/test_宿主事件.light`（rc=0）为其回归哨兵。

**期望**：赋值关键字 `为`（及 `返回`/`尝试`）在「词尾 + 值起始（`{ [ "` 或数字）」或
「紧邻值字面量 真/假/空」时必须切回 KEYWORD；而**函数调用语境（整串后紧随 `(`）绝不切分**。

**第20轮修复**：`light-merge/src/lexer.py` 在 `_EMBED_MAX_MATCH_KEYWORDS`（`为/返回/尝试`）分支
新增精确切分（情形 a/b）与调用语境守卫（情形 c）；
第19轮的「关键字前缀标识符被切碎」诊断经扫描证伪（见本账第 3 节扫描结论），
`去重占位`/`作用域匹配` 由 `_scan_user_definitions` 预扫描整体还原，**从未被切碎**。
```

### 2.5 新增 L-152：嵌套段落定义的形参名不被预扫描覆盖

```
## L-152（第20轮任务1 附带发现，2026-09-14）嵌套段落（段落体内定义段落）的形参名不被 `_scan_user_definitions` 覆盖

**现象**：`段落 主:` 体内再定义 `段落 内层返回 接收 函数值:`，形参 `函数值` 以关键字 `函数` 开头，
因预扫描只收模块级 `段落 X` / `设 X` / 导出名，**不含嵌套段落的形参**，`函数值` 被切成 `函数` + `值`
→ 运行期 `name '值' is not defined`。模块级同形态（`段落 测试段 接收 函数值:`）正常。

**最小复现**：
```
段落 小于判断 接收 甲:
  返回 甲 + 1

段落 主:
  段落 内层返回 接收 函数值:
      返回 函数值(9)
  断言相等(内层返回(小于判断), 10, "x")

主()
```
→ `name '值' is not defined`；把形参改名 `算子` 即绿。

**期望**：预扫描应覆盖嵌套段落的形参名（以及块内 `设` 名），或词法层对「以关键字开头但非关键字、
且不在合法语句起始位置」的汉字串按最大匹配整体成词。

**第20轮处置**：**未修**（属 P0-A「词法确定性切词重构」范畴；泛化合并关键字已实测引发 104 个解析回归）。
`examples/test_R20_词法边界综合.light` 该处形参改用 `算子` 并就地加注；本账登记待后续轮次处理。
```

---

### 2.6 本轮新发现（非语言缺陷）：`test_审批.light` 计时断言 wall-clock 抖动 —— R20-C（仅登记，不入缺陷账）

> **说明**：此条**不写入 `语言缺陷账.md`**（非语言/编译器缺陷，属测试健壮性问题）。仅在收口说明与本报告中登记，供路M知悉，避免误判为编译器回归。

```
## R20-C（第20轮全量CI附带发现，2026-09-14）test_审批.light 用例12d wall-clock 计时断言在负载下偶发红

**现象**：全量CI（pytest -n 8）出现 1 次 `examples/test_审批.light` rc=1：
  用例12d `断言(耗时 >= 0.2, ...)` 实际 0.1961195468902588（低于下界 0.004s），
  同一用例其余断言（12a/12b/12c/12e/12f/12g）均通过。

**归因（非本轮编译器改动）**：
  ① test_审批.light 由第19轮 E4 提交（1572bb8，2026-09-10），本轮**未改动**（git 无 M）；
  ② 单独运行 13 次（3+5+5）全部 rc=0；并发 10 份同跑亦全部 rc=0；
  ③ 本机 `3×sleep(0.1)`（复刻 `等待结果` 轮询）实测 0.389s（Windows 定时器**过冲**），
     即本机正常值远超 0.2s 下界；CI 记录 0.196s（**偏小**）只可能来自测量期间墙钟被校时
     步进（`等待结果` 用 `time.time()` 而非 `time.monotonic()` 计耗时）→ 属环境侧抖动。

**期望/建议**：`等待结果` 类耗时测量改用 `time.monotonic()`；或测试下界放宽（0.2 → 0.1）并加注
  「仅验证未立即返回」。**本轮未改**该测试（超出任务1文件互斥范围），建议路M或后续轮次处置。

**结论**：**非回归**、非编译器缺陷；本轮 4 个临时例外与编译器改动均不受其影响。
```

---

## 三、`docs/功能对标/行为差异清单.md` —— 追加 R20 段（R20-D1 ~ R20-D3）

追加在 R19-D6 之后（文件末尾）：

```markdown
## R20 第20轮（编译器回归修复轮，2026-09-14）

### R20-D1 词法最大匹配「收敛化」与上游差异
- **差异**：上游 TS/JS 词法器按 ASCII 标识符规则切词，不存在中文关键字切词歧义。光明语言因中文无空格，
  必须靠「最大匹配 vs 关键字边界」启发式；第20轮把该策略**收敛**为
  「`为/返回/尝试` 三关键字在赋值/返回语境精确切分 + 函数调用语境（整串后紧随 `(`）绝不切分」，
  并明确**拒绝**把全部非运算符关键字泛化合并（实测会引发 104 个解析回归：`接收参数`/`如果条件` 等合法「关键字+名」结构被误并）。
- **影响**：`设 合并为 {}` / `设 甲为空` 等无空格赋值尾巴可用；`断言为真(...)`/`判定为空(...)` 等
  含关键字尾的函数名不被切碎；`去重占位`/`作用域匹配` 依赖 `_scan_user_definitions` 预扫描整体还原
  （**仅在名称有定义登记时成立**）。
- **绕法**：正常写法即可；**嵌套段落形参**等无预扫描覆盖的位置仍须避开关键字前缀（见 R20-D3 / 缺陷账 L-152）。

### R20-D2 HMAC 标准实现与上游差异（订正 R19-D5）
- **差异**：上游 Node.js `crypto.createHmac` 遵循 RFC 2104。光明语言第19轮已把 stdlib `HMAC_SHA256`
  改为标准 `hmac.new`，但**第19轮收口误登记为「部分修复/EXPECT_RED」**；第20轮以 7 组对拍逐字节复核：
  与 Python `hmac` **完全一致**，EXPECT_RED 移除。
- **影响**：可与 openssl / GitHub `x-hub-signature-256` 等外部实现直接对拍验签（不再需要宿主侧二次校验）。
- **绕法**：无需绕法。**语言层仍无原始字节哈希原语**，.light 侧仍不能手写 HMAC（标准实现下沉在 stdlib .py 侧）。

### R20-D3 关键字前缀标识符行为与上游差异
- **差异**：上游标识符为 ASCII，任意组合不歧义；光明语言中「以关键字开头/含关键字的汉字标识符」是否被切碎，
  取决于**是否有定义登记**：模块级 `段落 X`/`设 X`/导出名会被 `_scan_user_definitions` 整体还原（`去重占位(...)` 正常）；
  而**嵌套段落形参名**等无登记位置仍会被切碎（`函数值` → `函数`+`值`）。
- **影响**：写 `.light` 时「函数名可自由用关键字前缀，形参/局部名在嵌套段落中需规避」。
- **绕法**：① 把嵌套段落提升到模块级；② 形参/局部名避开关键字前缀（用 `算子`/`异常甲`/`错误甲` 等不可全切分名）；
  ③ 用 `词法安全` 工具库的 `安全标识符(候选, 备选)` 生成安全名。（根治方向：P0-A 词法确定性切词重构）
```

---

## 四、路M执行清单（可直接照做）

1. 追加对标清单 #128~#130（§一 的 JSON 三条），确认 `条目` 长度变为 130。
2. 缺陷账：改 L-095 行 + L-097 行（§2.1/§2.2）；L-095 详述段追加 §2.3；新增 L-151、L-152（§2.4/§2.5）。
3. 行为差异清单：末尾追加 §三 的 R20 段（R20-D1~D3）。
4. `lightharness/tests/test_回归.py`：`EXPECT_RED` 删除 `test_L095.light`；`SKIP` 删除 `_taskM_integration_r17.light`
   （该文件第20轮实测 rc=0）；同步更新 31~35 行注释。
5. 全量 CI，确认 `EXPECT_RED = {test_L101, test_L143}`（合规保留字预期红）、`SKIP = {_helper_L004, _helper_L007b}`。
