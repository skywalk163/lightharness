# 外发任务 W3：lightharness 服务端路由扩展（web服务器.light 独占）

> 定位：本任务是"lightharness 网页启动"四路并行中的 **W3 服务端**。在现有
> `src/web服务器.light`（OpenAI 兼容 API 壳，已实测可用）之上补三件事：**静态页面路由、
> CORS 头、认证挂载点**。只改白名单文件，`HTTP服务端.light` 等核心共享文件一律不碰。
>
> 并行依赖：本任务按 `总览.md` 契约 2 调用 W2 的 `src/认证.light`（签名已定死）。
> W2 未合入时可用**本地临时桩**（同签名、固定返回）开发与测试，集成时换成真模块，
> 保证两路互不阻塞。

## 白名单文件（只改/只新建这些）

- 独占修改 `src/web服务器.light`（唯一现有文件）
- 可新建 `examples/test_web路由.light`（反跑测试，建议新建）

## 功能要求

### 1. 静态页面路由（总览契约 1、4）

- `Web服务端.配置` 增加第 5 参 `界面目录`（字符串；`空`/`""` 表示不启用静态路由，向后兼容）。
- 启用后路由处理器新增：
  - `GET /` → `webui/index.html`（`text/html; charset=utf-8`）
  - `GET /app.js` → `webui/app.js`（`application/javascript; charset=utf-8`）
  - `GET /style.css` → `webui/style.css`（`text/css; charset=utf-8`）
  - 其它静态路径 → 404 JSON（沿用现有 404 格式）
- 文件读取用 `从 文件系统 导入 读取文件, 文件存在`；**文件不存在 → 404**（不抛 500）。
- **只支持文本资源**：二进制请求返回 404（现有 `构造响应字节` 只输出文本体，勿改底层）。
- 静态目录定位：调用方（W4 / 本任务测试文件）负责传绝对路径；本模块内部**不做**路径自推导，
  只按传入目录拼接文件名（用 `从 文件系统 导入 路径连接`）。
- 提示：`web服务器.light` 现有 `取会话` 里用了 `当前目录()`，静态路由**不要**用 `当前目录()`
  定位（回归测试在临时 cwd 跑），一律用传入的 `界面目录`。

### 2. CORS 头（总览契约 5）

- 所有响应统一加 `Access-Control-Allow-Origin: *` 头。
- 开关：`环境变量("HARNESS_WEB_CORS", "1")`；`"0"` 时不加。
- 实现位置：路由处理器返回的响应字典里加头即可（`"头": {..., "Access-Control-Allow-Origin": "*"}`），
  不改 `HTTP服务端.light`。

### 3. 认证挂载点（总览契约 2、3，S2 语义）

- 从 `认证` 导入 `初始化认证, 取令牌, 校验令牌`。
- **启用判定**：`取令牌() == ""` → 未启用（维持 S1 全放行，向后兼容）；非空 → 启用。
  `初始化认证()` 由调用方（W4 启动器 / 测试）调用，本模块只负责**校验**，不在路由层初始化。
- 启用后校验规则：
  - 白名单放行：`GET /health`；静态资源 `GET /`、`/app.js`、`/style.css`（建议放行——
    让页面能先打开、前端 JS 再带 token 调 API；最终取舍在交付报告写明）。
  - 其余请求（如 `POST /v1/chat/completions`）校验 `X-Auth-Token` 头 **或** URL query `?token=`；
    二选一命中即通过。
  - 失败 → `401` + `{"error":{"message":"未认证","type":"unauthorized"}}`。
- **不做 /login 端点**（登录 = URL 带 `?token=`，见总览契约 3）。

## 语言坑提醒（历批踩过，直接规避）

- 光明类体不能放 `异步 段落`：异步路由继续走现有 `路由处理器`（模块级段落）+ `全局服务` 单例模式。
- 成员赋值 `设 己.X 为 …`；`配置` 里新增字段要初始化默认值（`属性 界面目录 等于 ""`）。
- 头字典是光明字典，直接 `["Access-Control-Allow-Origin"] 为 "*"` 追加。
- 字符串拼接用 `+`；`路径连接(界面目录, "index.html")`。
- 发现"写不出/写错/绕不过"→ 登记 `docs/功能对标/语言缺陷账.md`，别顺手改编译器。

## 验收判据（每条立得住反跑）

1. `python 运行.py examples/test_web路由.light` 退出码 0，断言全过。
2. 测试覆盖（测试文件内自起服务 + 内核端口）：
   - 未配置 `界面目录`（传 `""`）→ `GET /` 返回 404（**向后兼容**：老调用行为不变）；
   - 配置后 `GET /` → 200 + `Content-Type: text/html`，体含 `index.html` 内容标记；
     `GET /app.js`、`GET /style.css` → 200 + 正确 Content-Type；`GET /nope.js` → 404；
   - CORS：响应头含 `Access-Control-Allow-Origin: *`；`HARNESS_WEB_CORS=0` 时不含；
   - 认证：本地桩令牌（如固定 "test-token"）下，无 token 访问 `/v1/chat/completions` → 401；
     带 `X-Auth-Token: test-token` 头 → 正常走通（mock 客户端）；带错 token → 401；
     URL query `?token=test-token` 同样通过；`/health` 无 token → 200；
   - 桩切换：不调用 `初始化认证`（`取令牌()==""`）→ 全部放行（S1 行为）。
3. **反跑**：把"未认证应 401"改成 200、或把静态 200 改成 404，测试必须立红。
4. **不动核心**：`git diff` 确认 `stdlib/HTTP服务端.light`、`examples/运行Web服务器.light`、
   `src/认证.light` 均未被本任务改动（运行Web服务器 与 认证 由 W4/W2 负责）。

## 交付物

- `src/web服务器.light` 改动 + `examples/test_web路由.light`。
- 交付报告：新 `配置` 签名、静态路由表、CORS 开关行为、认证白名单最终取舍（静态资源放行与否 +
  理由）、测试输出 + 反跑记录。

## 运行通道

```powershell
cd G:\dswork\duan-light-merge\lightharness
python 运行.py examples/test_web路由.light
# 手验（可选，需 W4 合入后）：
# $env:HARNESS_WEB_MOCK='1'; python 运行.py examples/运行Web服务器.light
```
