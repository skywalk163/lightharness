# 第18轮任务1 交付报告 —— 字典操作增强库

- **对标编号**：#116（光明语言工具库·缺陷修复轮，非上游复刻）
- **差异编号**：R18-D1
- **语言缺陷预分配**：L-151~L-152（**空缺顺延**，见下）
- **可写文件**（全部新增，零越界）：
  - `src/字典增强.light`（106 行 / 13 段落）
  - `examples/test_字典增强.light`（138 行 / 41 断言）
  - `_antirun_t1_字典增强.py`
  - `_task1_字典增强_交付报告.md`

---

## 一、定位

前 17 轮复刻中，字典操作绕法散落各模块（如 `网关协议.light` 到处 `字典获取(值,键,空)`、`宿主IO.light` 手写深层赋值）。本轮建设标准化字典工具库，把 7 个字典操作缺陷（L-135/L-103/L-123/L-116/L-127/L-100/L-096）的绕法统一到一处。

## 二、10 个函数与修复层级

| # | 函数 | 修复层级 | 对应缺陷 | 说明 |
|---|---|---|---|---|
| 1 | `字典获取(字典,键,默认值=空)` | A | L-135/L-103 | 委托内置 `字典获取`（stdlib/内置核心字典.light:44 已支持默认值），缺失键返默认不抛 KeyError |
| 2 | `深度设置(字典,键路径,值)` | A | L-123 | 多层下标赋值，中间层自动建字典；空路径/非字典中间层抛错 |
| 3 | `深度获取(字典,键路径,默认值=空)` | B | L-123 | 任一层缺失返默认 |
| 4 | `键规范化(字典)` | B | L-116 | 键名排序返回新字典，嵌套递归 |
| 5 | `序列化JSON排序(字典)` | A | L-116 | 先规范化再序列化，键序无关 |
| 6 | `造身份令牌()` | A | L-127 | 唯一令牌字符串，替代对象身份键 |
| 7 | `字典合并(字典A,字典B,覆盖=真)` | B | — | 覆盖/不覆盖两种模式 |
| 8 | `字典深拷贝(字典)` | B | — | 递归深拷贝嵌套字典/列表 |
| 9 | `属性获取(对象,键,默认值=空)` | C | L-096 | 统一下标访问（光明不支持 `对象.键`） |
| 10 | `空字典()` | B | L-100 | 包装内置 `字典()` 的语义化别名 |

## 三、验证

- **测试**：`examples/test_字典增强.light` 共 **41 断言**（含 2 个 `应抛错` 异常态），覆盖 10 个函数的正常/边界/异常三态，终行 `test_字典增强 PASS`，`rc=0`。
- **反跑**：`_antirun_t1_字典增强.py` **3/3 ALL OK**
  - A=正常：字典获取 + 深度设置 + 序列化JSON排序 组合运行 PASS
  - B=边界：深度设置中间层自动建 / 深度获取缺层返默认 在干净运行全过
  - C=变异：把深度设置末键由 `键路径[长-1]` 改成 `键路径[0]`（只设第一层），双层赋值断言（2a/2c）立红；恢复后 sha256 校验一致（c948d107…）、回归 PASS

## 四、语言缺陷处理

- **新缺陷**：本轮为修复轮，**未遇真实新语言缺陷**。L-151~L-152 预分配编号**空缺顺延**。
- **被修复缺陷标注**（供路M回填缺陷账"修复状态"列）：
  - L-135 / L-103「字典缺失键抛 KeyError」→ 已通过 `字典获取` 统一修复（委托内置默认值）
  - L-123「双层下标赋值静默失败」→ 已通过 `深度设置`/`深度获取` 修复
  - L-116「序列化JSON键序敏感」→ 已通过 `键规范化`/`序列化JSON排序` 修复
  - L-127「对象身份键不可表达」→ 已通过 `造身份令牌` 修复
  - L-100「空字典 `{}` 不支持」→ 已通过 `空字典` 别名修复
  - L-096「对象.键 属性访问不可用」→ 已通过 `属性获取` 统一下标访问修复

## 五、两步踩坑（已就地解决，非新缺陷）

1. **`空` 关键字的误用**：初版 `空字典` 段写 `设 空 为 字典()`，触发"「空」是关键字不能作变量名"。改为 `空字典实例`。属关键字误用，非语言缺陷。
2. **L-098 闭包计数现代表现**：`造身份令牌` 初版在函数内 `设 令牌计数 为 令牌计数 + 1`，因光明闭包不能重绑外层变量（L-098），该变量在函数内被视为局部 → `UnboundLocalError`。改用模块级字典 `令牌状态` 承载计数、原地 `字典设置` mutate 绕开（与 L-098 标准绕法一致），不登记新缺陷。

## 六、纪律

- 仅新增本路互斥文件，未修改任何既有 `src/` 模块；未 commit，收口归路M。
- docs（对标清单/缺陷账/行为差异）由路M 统一回填。
- 交付文件：`src/字典增强.light`、`examples/test_字典增强.light`、`_antirun_t1_字典增强.py`、`_task1_字典增强_交付报告.md`。
