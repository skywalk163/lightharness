# 文件.light —— 文件系统与存储（对齐原版 fs/storage，加深版）
#
# 提供安全的文件读写、目录保证、JSON 存取、文件操作（删除/重命名/复制）、
# stat 信息、glob 查找、临时目录、二进制读写。
# 字段/函数名避开光明关键字。

# 保证目录存在（不存在则创建）
段落 保证目录 接收 路径:
  如果 目录存在(路径) == 假:
    创建目录(路径)

# 写 JSON 文件（覆盖式；自动保证父目录）
段落 写JSON文件 接收 路径, 数据:
  设 父目录 为 目录名(路径)
  如果 父目录 != "" 且 目录存在(父目录) == 假:
    创建目录(父目录)
  设 文本 为 序列化JSON(数据)
  写入文件(路径, 文本)

# 读 JSON 文件；文件不存在或解析失败返回 空
段落 读JSON文件 接收 路径:
  如果 文件存在(路径) == 假:
    返回 空
  设 文本 为 读取文件(路径)
  返回 解析JSON(文本)

# 写文本文件（覆盖式）
段落 写文本文件 接收 路径, 内容:
  写入文件(路径, 内容)

# 读文本文件（不存在返回空）
段落 读文本文件 接收 路径:
  如果 文件存在(路径) == 假:
    返回 空
  返回 读取文件(路径)

# 拼接路径段
段落 拼路径 接收 根, 段:
  返回 连接路径(根, 段)

# 列出目录下的文件名（不存在返回空表）
段落 列目录 接收 路径:
  如果 目录存在(路径) == 假:
    返回 []
  返回 列出目录(路径)

# === 加深：文件系统操作 ===

# 删除路径（文件或目录；目录递归删除）
段落 删除路径 接收 路径:
  如果 文件存在(路径):
    删除文件(路径)
  否则如果 目录存在(路径):
    删目录树(路径)

# 重命名文件或目录
段落 重命名路径 接收 旧路径, 新路径:
  重命名(旧路径, 新路径)

# 复制路径（自动判断文件或目录）
段落 复制路径 接收 源路径, 目标路径:
  如果 文件存在(源路径):
    复制文件(源路径, 目标路径)
  否则如果 目录存在(源路径):
    复制目录(源路径, 目标路径)

# 检查路径是否存在（文件或目录）
段落 存在 接收 路径:
  返回 路径存在(路径)

# 获取文件信息（大小、类别、是否存在）
段落 文件信息 接收 路径:
  如果 文件存在(路径):
    返回 ["大小": 文件大小(路径), "类别": "文件", "存在": 真]
  如果 目录存在(路径):
    返回 ["大小": 0, "类别": "目录", "存在": 真]
  返回 ["大小": 0, "类别": "无", "存在": 假]

# 按通配符查找文件（样式如 *.txt；递归为真时搜索子目录）
段落 查找文件 接收 目录, 样式, 递归:
  如果 目录存在(目录) == 假:
    返回 []
  返回 查找目录列表(目录, 样式, 递归)

# 创建临时目录，返回路径
段落 建临时目录:
  返回 创建临时目录()

# 写入二进制文件
段落 写二进制 接收 路径, 数据:
  写入二进制文件(路径, 数据)

# v2：原子写文本——先写临时文件再重命名，避免中途损坏
段落 原子写文本 接收 路径, 内容:
  设 临时路径 为 路径 + ".tmp"
  写入文件(临时路径, 内容)
  重命名(临时路径, 路径)

# v2：追加文本——在文件末尾追加内容（文件不存在则创建）
段落 追加文本 接收 路径, 内容:
  设 已有 为 ""
  如果 文件存在(路径):
    设 已有 为 读取文件(路径)
  写入文件(路径, 已有 + 内容)

# 读取二进制文件
段落 读二进制 接收 路径:
  如果 文件存在(路径) == 假:
    返回 空
  返回 读二进制文件(路径)


# 原子写文本（先写临时文件再重命名，确保写入过程中崩溃不破坏原文件）
段落 原子写文本 接收 路径, 内容:
  设 临时路径 为 路径 + ".tmp." + 转字符串(时间戳())
  尝试:
    写入文件(临时路径, 内容)
    # Windows 下重命名不能覆盖已存在文件，先删除
    如果 文件存在(路径):
      删除文件(路径)
    重命名(临时路径, 路径)
    返回 真
  捕获 错误:
    # 失败时清理临时文件
    如果 文件存在(临时路径):
      删除文件(临时路径)
    返回 假
