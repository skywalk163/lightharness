# 文件.light —— 文件与 JSON 存储（对齐原版 fs/storage，P1 版）
#
# 提供安全的文件读写、目录保证、JSON 存取。字段/函数名避开光明关键字。

# 保证目录存在（不存在则创建）
段落 保证目录 接收 路径:
  如果 目录存在(路径) == 假:
    创建目录(路径)

# 写 JSON 文件（覆盖式；自动保证父目录）
段落 写JSON文件 接收 路径, 数据:
  设 父目录 为 目录名(路径)
  如果 父目录 != "" 且 目录存在(父目录) == 假:
    创建目录(父目录)
  设 文本 为 序列化JSON(数据)
  写入文件(路径, 文本)

# 读 JSON 文件；文件不存在或解析失败返回 空
段落 读JSON文件 接收 路径:
  如果 文件存在(路径) == 假:
    返回 空
  设 文本 为 读取文件(路径)
  返回 解析JSON(文本)

# 写文本文件（覆盖式）
段落 写文本文件 接收 路径, 内容:
  写入文件(路径, 内容)

# 读文本文件（不存在返回空）
段落 读文本文件 接收 路径:
  如果 文件存在(路径) == 假:
    返回 空
  返回 读取文件(路径)

# 拼接路径段
段落 拼路径 接收 根, 段:
  返回 连接路径(根, 段)

# 列出目录下的文件名（不存在返回空表）
段落 列目录 接收 路径:
  如果 目录存在(路径) == 假:
    返回 []
  返回 列出目录(路径)
