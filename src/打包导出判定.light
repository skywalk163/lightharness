# 打包导出判定.light —— 对标上游 deepseek-harness experimental/webworker-packer（增量 ddefc45fbc..877717787c，0.1.7）
# 纯逻辑面移植（R87 任务 B3 第 3 组：boot + experimental）。
#
# 上游源码：packages/experimental/webworker-packer/src/pack.ts
#   * :353 isDeclarationOnlyExport —— 一个 exports 条件树是否「仅类型声明」，不带运行时根
#   * :400 sweepImage 的运行时根选取 —— 从 package.json 的 exports 里挑出非通配、非仅声明的面
#
# 本模块只搬「AST/条件节点的类型判定 + 面过滤」纯逻辑，不做真实打包、tar、gzip、文件 IO、
# 也不跑模块加载器；真实 reachability sweep 仍归宿主面。
#
# 条件树语义（isDeclarationOnlyExport）：
#   * 非对象/null → 假（字符串路径、URL 等真实目标不是声明-only）
#   * 数组 → 非空且每个元素都是声明-only
#   * 对象 → 非空，且每个条件键要么是 "types"/以 "types@" 开头，要么其目标是声明-only
#   满足者整棵不贡献运行时根（显式 import 仍照常解析，那是 loader 的事）。
#
# 命名铁律：避开「导出」词根作标识符，故用「外露」指代 export face；文件名为任务书指定。

导出 判仅声明外露, 列运行外露面

# isDeclarationOnlyExport：一个 exports 条件节点是否仅声明类型、不带运行时面
段落 判仅声明外露(值):
  如果 值 == 空:
    返回 假
  如果 是列表(值) == 真:
    如果 列表长度(值) == 0:
      返回 假
    遍历 子 之 值:
      如果 判仅声明外露(子) == 假:
        返回 假
    返回 真
  如果 是字典(值) == 假:
    返回 假
  设 键表 为 字典键列表(值)
  如果 列表长度(键表) == 0:
    返回 假
  遍历 键 之 键表:
    如果 键 == "types":
      继续
    如果 开头(键, "types@"):
      继续
    如果 判仅声明外露(字典获取(值, 键, 空)) == 假:
      返回 假
  返回 真

# sweepImage 的运行时根选取：exports 缺省返回 ["."]；否则挑出
# 键以 "." 开头、不含 "*"、且目标不是仅声明外露面 的那些子路径。
段落 列运行外露面(出口):
  如果 出口 == 空:
    返回 ["."]
  设 面表 为 []
  遍历 键 之 字典键列表(出口):
    如果 开头(键, ".") == 假:
      继续
    如果 字符串包含(键, "*"):
      继续
    如果 判仅声明外露(字典获取(出口, 键, 空)):
      继续
    列表追加(面表, 键)
  返回 面表
