# 子进程.light —— 子进程执行（对齐原版 subprocess，P1 v1）
#
# 封装 stdlib/外部命令 的执行命令，提供安全字段名的进程运行接口。
#
# ⚠️ 光明命名规范 v1（缺陷 L-010）：`命令结果.返回码` 因「返回」关键字被拆，
#    故 v1 只暴露 输出/错误/成功，退出码以 成功?0:-1 表示，不保留精确码；
#    若后续需要精确退出码，需编译器支持「返回码」属性访问后再补。

导入 外部命令

# 运行一条命令（shell 字符串），返回 { 码, 输出, 错误, 成功 }
段落 运行进程 接收 命令, 超时:
  设 结果 为 外部命令.执行命令(命令, 真, 空, 空, 超时)
  设 成功 为 结果.是否成功
  设 码 为 0
  如果 成功 == 假:
    码 为 -1
  返回 ["码": 码, "输出": 结果.标准输出, "错误": 结果.标准错误, "成功": 成功]

# 运行命令并只返回标准输出（去尾空白）；失败时返回空
段落 运行并取输出 接收 命令, 超时:
  设 结果 为 运行进程(命令, 超时)
  如果 结果["成功"] == 假:
    返回 ""
  返回 结果["输出"].去除空白()

# 命令是否成功（返回 真/假）
段落 进程成功 接收 命令, 超时:
  设 结果 为 运行进程(命令, 超时)
  返回 结果["成功"]
