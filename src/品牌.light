# 品牌.light —— 对标原版 packages/util/brand/src/index.ts
# 纯逻辑核心：名义（nominal）品牌辅助。上游在类型层把结构相同的字符串/数字区分成
# 不同域类型（Branded<B> / BrandedNumber<B>），运行期 brandString/brandNumber 是
# 恒等转换（不改值、不建运行期身份）。
# 差异：光明无编译期类型层，品牌在运行期本就是恒等函数；这里保留同名导出以便调用面
# 与上游词表对齐——调用方据此表达「该值已过某域准入」的约定，比较/序列化仍走原值。

# 给字符串打上域品牌（运行期恒等）
段落 打品牌字符串 接收 值:
  返回 值

# 给数字打上域品牌（运行期恒等）
段落 打品牌数字 接收 值:
  返回 值
