# 函数增强.light —— 第18轮 缺陷修复轮：函数与闭包增强工具库（对标 #120）
# 修复/统一 4 个函数闭包缺陷的绕法（L-102/L-098/L-091/L-097）：
#   1. 带默认参数（L-102，A级）：包装「字典参数函数」，调用时缺键填默认值。
#      设计约定（R18-D5）：光明函数参数个数固定，无法变长展开——默认参数/偏应用/应用
#      统一采用「字典参数函数 / 列表参数函数」约定（原函数接收一个字典或列表）。
#   2. 柯里化（B级）：多参列表函数 -> 单参函数链（嵌套段落返回，利用 L-078 闭包能力）。
#   3. 闭包包装（L-098，A级）：函数通过共享状态字典读写状态（字典引用跨调用共享，
#      绕开闭包外层变量修改不生效）。
#   4. 造计数器：闭包包装的应用范例（递增/获取值/重置）。
#   5-10. 通用工具：管道/组合/记忆化/应用/偏应用/断言包装。
# 绕法：内置裸名（L-090）、无 且/或 行内链（L-043）、标识符避关键字与单字别名切分（L-119/L-120/L-084）、
#   变量名避「尝试」（L-137）、段落定义放模块级或函数体顶层（L-097）。

导出 带默认参数 柯里化 闭包包装 造计数器 管道 组合函数 记忆化 应用参数 偏应用 断言包装。

# ---------------- 带默认参数（L-102，A级） ----------------
# 约定：原函数为「字典参数函数」（接收一个参数字典）。
# 返回包装函数：接收参数字典 -> 合并默认值（调用方显式传的键优先）-> 调用原函数(合并后字典)。
段落 带默认参数(目标函数, 默认参数字典):
  段落 包装(参数字典):
    设 合并表 为 {}
    遍历 键 之 字典键列表(默认参数字典):
      字典设置(合并表, 键, 默认参数字典[键])
    遍历 键 之 字典键列表(参数字典):
      字典设置(合并表, 键, 参数字典[键])
    返回 目标函数(合并表)
  返回 包装

# ---------------- 柯里化（B级） ----------------
# 列表参数函数 -> 单参函数链：每步返回收集函数，收齐 参数个数 个后调用原函数(收集列表)
段落 柯里化(目标函数, 参数个数):
  段落 收集步(已收集表):
    段落 收下一个(值):
      设 新表 为 副本(已收集表)
      列表追加(新表, 值)
      如果 列表长度(新表) == 参数个数:
        返回 目标函数(新表)
      返回 收集步(新表)
    返回 收下一个
  返回 收集步([])

# ---------------- 闭包包装（L-098，A级） ----------------
# 约定：原函数接收 (参数, 状态字典) 两参，函数内通过 状态["键"] 读写状态
# （字典引用跨调用共享，绕开 L-098 闭包外层变量修改不生效）。
# 返回 {调用: 段落(参数), 状态: 状态字典}
段落 闭包包装(目标函数, 状态字典):
  段落 包装(参数):
    返回 目标函数(参数, 状态字典)
  设 结果 为 {}
  结果["调用"] 为 包装
  结果["状态"] 为 状态字典
  返回 结果

# 造计数器：闭包包装的应用范例（状态字典计数持久）
段落 造计数器(初始值):
  段落 计数逻辑(参数, 状态):
    设 动作 为 字典获取(参数, "动作", "递增")
    如果 动作 == "递增":
      设 状态["值"] 为 字典获取(状态, "值", 初始值) + 1
    如果 动作 == "重置":
      设 状态["值"] 为 初始值
    返回 字典获取(状态, "值", 初始值)
  设 包装果 为 闭包包装(计数逻辑, ["值": 初始值])
  设 计数器 为 {}
  段落 递增:
    设 递增参数 为 ["动作": "递增"]
    设 调用器 为 包装果["调用"]
    返回 调用器(递增参数)
  段落 获取值:
    设 读取参数 为 ["动作": "获取"]
    设 调用器乙 为 包装果["调用"]
    返回 调用器乙(读取参数)
  段落 重置:
    设 重置参数 为 ["动作": "重置"]
    设 调用器丙 为 包装果["调用"]
    调用器丙(重置参数)
    返回 真
  计数器["递增"] 为 递增
  计数器["获取值"] 为 获取值
  计数器["重置"] 为 重置
  计数器["状态"] 为 包装果["状态"]
  返回 计数器

# ---------------- 通用工具 ----------------
# 管道：值依次通过函数列表
段落 管道(值, 函数列表):
  设 结果 为 值
  遍历 函数 之 函数列表:
    设 结果 为 函数(结果)
  返回 结果

# 组合：返回 函数甲(函数乙(x))
段落 组合函数(函数甲, 函数乙):
  段落 组合体(值):
    返回 函数甲(函数乙(值))
  返回 组合体

# 记忆化：单参数函数的字典缓存（键=转字符串(参数)）
段落 记忆化(目标函数):
  设 缓存表 为 {}
  段落 包装(参数):
    设 键 为 转字符串(参数)
    如果 字典包含键(缓存表, 键):
      返回 字典获取(缓存表, 键, 空)
    设 结果 为 目标函数(参数)
    字典设置(缓存表, 键, 结果)
    返回 结果
  返回 包装

# 应用：列表参数函数直通（约定：目标函数接收列表）
段落 应用参数(目标函数, 参数列表):
  返回 目标函数(参数列表)

# 偏应用：固定列表参数函数的前 N 个参数，返回接收剩余参数的函数
段落 偏应用(目标函数, 固定参数列表):
  段落 包装(剩余列表):
    设 全表 为 []
    遍历 项 之 固定参数列表:
      列表追加(全表, 项)
    遍历 项 之 剩余列表:
      列表追加(全表, 项)
    返回 目标函数(全表)
  返回 包装

# 断言包装：执行前后检查条件（前置(参数)->布尔；后置(结果)->布尔；失败抛）
段落 断言包装(目标函数, 前置条件, 后置条件):
  段落 包装(参数):
    设 前果 为 前置条件(参数)
    如果 前果 == 假:
      抛出 新建 错误("断言包装：前置条件失败")
    设 结果 为 目标函数(参数)
    设 后果 为 后置条件(结果)
    如果 后果 == 假:
      抛出 新建 错误("断言包装：后置条件失败")
    返回 结果
  返回 包装
