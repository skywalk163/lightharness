# 异步代理.light —— 任务书交付名保留文件（指向可导入实现）
#
# 任务书要求本文件名承载「异步代理循环」。但光明编译器对 `从 异步代理 导入 …`
# 会把模块名「异步代理」按关键字边界拆成「异步」+「代理」→ 解析失败（L-004 家族
# 缺陷，`导入 异步代理` 可加载但成员访问同样被拆，模块不可用）。
#
# 因此实际可导入实现位于：src/协程代理.light（模块名「协程代理」），类名仍为
# 「异步代理循环」。使用方式：
#   从 协程代理 导入 异步代理循环
#   设 代理 为 新建 异步代理循环(会话, 客户端, 注册表, 选项, 策略, 超时秒)
#   设 结果 为 等待 代理.驱动("你好", 令牌)
#
# 详见 docs/功能对标/语言缺陷账.md 本轮登记（模块名含关键字「异步」不可被 from 引用）。
