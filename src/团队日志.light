# 团队日志.light —— 对标 #70：TeamJournal 纯逻辑复刻
# 原版：packages/experimental/agent-team/src/journal.ts（TeamJournal，71 行）
#
# 原版职责：
#   - tails: Map<SessionId, Promise<void>>            —— 每 Lead 事务串行化
#   - state(root) = foldTeam(root.id, root.session.events)
#                                                  —— 回放权威团队状态
#   - transact(rootId, operation)                   —— 串行化一个读-查-追加操作
#   - appendAndFlush(root, type, data)              —— 经 session.append /
#                                                     ctx.sessions.flush / onCommit
#                                                     追加并落盘一个 root 拥有的团队事件
#
# 纯逻辑化：宿主 session.append / sessions.flush / onCommit 投影为入参能力函数
#   {追加(rootId, 种类, 数据), 落盘(rootId), 提交(rootId)}；不 IO、不网络、不起子进程、
#   不实例化宿主服务。光明为同步模型，原版异步串行化（tails Promise 队列）在本仓折叠为
#   「per-root 尾序号」保序：同一 rootId 的操作按调用序记入 尾[rootId]，不同 rootId 互不
#   干扰——语义等价（同步单线程天然无并发重叠）。

从 团队折叠 导入 折叠团队
从 内置核心转换 导入 转字符串

# 可变动团队事件种类（原版 MutableTeamEventType）：团队事件不进入对话面，窄化本地能力。
设 可变动种类 为 ["team/member", "team/task", "team/message/queued", "team/message/delivered"]

# 模块级全局能力变量 + 全局状态字典（绕 L-057：类方法体内不可调函数值，改用顶层段落 + 全局）
设 日志能力 为 空
设 日志状态 为 空

# 构造日志，注入宿主能力。
段落 造日志 接收 能力:
  全局 日志能力, 日志状态
  设 日志能力 为 能力
  设 日志状态 为 {}
  设 日志状态["尾"] 为 {}

# 回放某 Lead 的权威团队状态（复用 #67 折叠团队）。
段落 回放 接收 根标识, 事件表:
  返回 折叠团队(根标识, 事件表)

# 按 rootId 串行化一个读-查-追加操作，返回操作结果；记录 per-root 尾序号保序。
段落 事务 接收 根标识, 操作:
  全局 日志状态
  设 序 为 0
  如果 (字典包含键(日志状态["尾"], 根标识) == 真):
    设 序 为 日志状态["尾"][根标识]
  设 序 为 序 + 1
  设 日志状态["尾"][根标识] 为 序
  设 结果 为 操作()
  返回 结果

# 追加一个 root 拥有的团队事件，并在发布前落盘 + 触发提交回调。
段落 追加落盘 接收 根标识, 种类, 数据:
  全局 日志能力, 日志状态
  如果 (列表包含(可变动种类, 种类) == 假):
    抛出 新建 错误("非法团队事件种类 [TEAM_INVALID_EVENT_TYPE]: " + 转字符串(种类))
  日志能力["追加"](根标识, 种类, 数据)
  日志能力["落盘"](根标识)
  日志能力["提交"](根标识)
  设 序 为 0
  如果 (字典包含键(日志状态["尾"], 根标识) == 真):
    设 序 为 日志状态["尾"][根标识]
  设 日志状态["尾"][根标识] 为 序 + 1

# 读取某 rootId 的当前尾序号（供测试观测 per-root 串行化；纯只读）。
段落 取尾序号 接收 根标识:
  全局 日志状态
  如果 (字典包含键(日志状态["尾"], 根标识) == 真):
    返回 日志状态["尾"][根标识]
  返回 0

导出 造日志
导出 回放
导出 事务
导出 追加落盘
导出 可变动种类
导出 取尾序号
