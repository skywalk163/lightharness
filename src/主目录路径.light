# 主目录路径.light —— 对标原版 packages/util/home-paths
# 纯逻辑核心：~ 展开、DSH_HOME 解析（配置 > 环境变量 > 默认 ~/.dsh）、主目录拼接、显示名。
# 原版用 Node homedir()，此处把「操作系统主目录」作为显式参数 主目录 注入，使其纯函数化、可测试。
# 无法 1:1 复刻之处：原版依赖 process.platform/真实 fs 的 realpath/canonicalizeWatchPath，
#           纯逻辑层不涉及，已省略（参见 不可移植缺口）。

从 字符串工具 导入 去除首尾空白

设 DSH主目录名 为 ".dsh"
设 默认主目录显示 为 "~/.dsh"
设 DSH主目录环境 为 "DSH_HOME"
设 DSH主目录环境显示 为 "$DSH_HOME"

# 默认 DeepSeek Harness 主目录（操作系统主目录下的 .dsh）
段落 默认主目录 接收 主目录:
  返回 连接路径(主目录, DSH主目录名)

# 展开受支持的 ~ 前缀：~ 本身、~/ 与 ~\（Windows 反斜杠）
段落 展开主目录路径 接收 路径, 主目录:
  如果 路径 == "~":
    返回 主目录
  如果 开头(路径, "~/") 或 开头(路径, "~\\"):
    返回 连接路径(主目录, 截取(路径, 2, 字符串长度(路径)))
  返回 路径

# 解析单一根的 DeepSeek Harness 主目录。
# 优先级（从高到低）：显式配置 > $DSH_HOME（非空且非纯空白）> 默认 ~/.dsh。
# 空串或仅空白的 $DSH_HOME 视为未设置，绝不解析到当前工作目录。
段落 解析主目录 接收 配置, 环境, 主目录:
  如果 环境 == 空:
    设 环境 为 {}
  设 来自环境 为 字典获取(环境, DSH主目录环境)
  设 选中 为 配置
  如果 选中 == 空:
    如果 来自环境 != 空 且 字符串长度(去除首尾空白(来自环境)) > 0:
      设 选中 为 来自环境
    否则:
      设 选中 为 默认主目录(主目录)
  返回 解析路径(展开主目录路径(选中, 主目录))

# 把若干段拼接到解析后的主目录上。段列表为空时返回主目录本身。
段落 主目录路径 接收 主目录, 段列表, 配置=空, 环境=空:
  设 根 为 解析主目录(配置, 环境, 主目录)
  设 结果 为 根
  遍历 段 之 段列表:
    设 结果 为 连接路径(结果, 段)
  返回 结果

# 给用户看的象征性显示名：默认主目录标为 ~/.dsh，其余一律标为 $DSH_HOME（绝不回显绝对机器路径）。
段落 主目录显示 接收 已解析主目录, 主目录:
  如果 已解析主目录 == 解析主目录(空, {}, 主目录):
    返回 默认主目录显示
  返回 DSH主目录环境显示

# 最小规范化：去掉尾随斜杠（根 "/" 除外）。
段落 解析路径 接收 路径:
  如果 路径 == "/":
    返回 "/"
  当 字符串长度(路径) > 1 且 结尾(路径, "/"):
    设 路径 为 截取(路径, 0, 字符串长度(路径) - 1)
  返回 路径
