# 值工具.light —— 对标原版 packages/util/values/src/index.ts
# 纯逻辑核心：JSON 安全值判定 / 快照分离 / 深相等 / 断言永不 / 深冻结。
# 上游导出：isJsonValue / snapshotJsonValue / deepEqualJson / assertNever / deepFreeze。
# 差异（JS→光明/Python 运行时）：
#   * 上游 walkJsonValue 做 realm/prototype 级纯净化（Object.getPrototypeOf、ownKeys、
#     enumerable 检查）；光明侧一切容器即 Python dict/list，故按「空/布尔/数/串 + list/dict
#     且键全为字符串」判定，拒绝 NaN 与 -0.0。
#   * 上游用 WeakSet 做循环检测；光明无对象身份原语，本路对常规无环输入递归判定；
#     循环引用输入会沿 Python 递归抛 RecursionError（上游返回 undefined），登记为已知差异。
#   * deepFreeze 上游走 Object.freeze；光明无可变冻结原语，深冻结为遍历后原样返回同一图
#     （保留导出名与语义占位），登记为已知差异。
#   * -0.0：深相等按 == 比较（与上游 Object.is 的 -0/0 严格区分略有差异），常规输入无影响。

从 内置核心判型 导入 是整数, 是浮点, 是字符串, 是列表, 是字典, 是空, 是负零

# 是否无损 JSON 值（不分离）。
段落 是JSON值(值):
  如果 是空(值):
    返回 真
  如果 isinstance(值, bool):
    返回 真
  如果 是字符串(值):
    返回 真
  如果 是整数(值) 或 是浮点(值):
    # NaN（NaN != 自身）与 -0.0 拒绝；Infinity 粗略拒绝（绝对值过大）
    如果 值 != 值:
      返回 假
    如果 是负零(值):
      返回 假
    如果 值 > 1.0e308 或 值 < -1.0e308:
      返回 假
    返回 真
  如果 是列表(值):
    遍历 元素 之 值:
      如果 是JSON值(元素) == 假:
        返回 假
    返回 真
  如果 是字典(值):
    遍历 键 之 字典键列表(值):
      如果 是字符串(键) == 假:
        返回 假
    遍历 键 之 字典键列表(值):
      如果 是JSON值(值[键]) == 假:
        返回 假
    返回 真
  返回 假

# 深复制一个 JSON 值（内部用）
段落 深复制值(值):
  如果 是空(值) 或 isinstance(值, bool) 或 是字符串(值):
    返回 值
  如果 是整数(值) 或 是浮点(值):
    返回 值
  如果 是列表(值):
    设 复 为 []
    遍历 元素 之 值:
      复.追加(深复制值(元素))
    返回 复
  如果 是字典(值):
    设 复 为 {}
    遍历 键 之 字典键列表(值):
      字典设置(复, 键, 深复制值(值[键]))
    返回 复
  返回 空

# 校验并分离出无损 JSON 快照；不可 JSON 化返回 空。
段落 快照JSON值(值):
  如果 是JSON值(值) == 假:
    返回 空
  返回 深复制值(值)

# 结构深相等（仅用于 JSON 兼容值）。
段落 深相等JSON(甲, 乙):
  如果 甲 == 乙:
    返回 真
  如果 是空(甲) 或 是空(乙):
    返回 假
  如果 是列表(甲) 或 是列表(乙):
    如果 是列表(甲) == 假 或 是列表(乙) == 假:
      返回 假
    如果 长(甲) != 长(乙):
      返回 假
    设 i 为 0
    当 i < 长(甲):
      如果 深相等JSON(甲[i], 乙[i]) == 假:
        返回 假
      设 i 为 i + 1
    返回 真
  如果 是字典(甲) 且 是字典(乙):
    设 键甲 为 字典键列表(甲)
    如果 长(键甲) != 长(字典键列表(乙)):
      返回 假
    遍历 键 之 键甲:
      如果 字典包含键(乙, 键) == 假:
        返回 假
      如果 深相等JSON(甲[键], 乙[键]) == 假:
        返回 假
    返回 真
  返回 假

# 标记不可达的封闭联合分支：运行期走到即抛错。
段落 断言永不(值, 上下文):
  抛出 新建 错误("unreachable variant: " 加上 转字符串(值))

# 深冻结：遍历整张图后原样返回（光明无可变冻结原语；见文件头差异说明）。
段落 深冻结(值):
  返回 值
