# 时间工具.light —— 对标原版 packages/util/time/src/index.ts
# 纯逻辑核心：canonicalClientTimeZone —— 在边界处校验并归一化调用方上报的 IANA 时区。
# 上游导出：canonicalClientTimeZone。
# 与既有模块关系（先核对后对齐）：src/时间上下文.light 已实现 是IANA时区（同一套
# IANA 形态正则），本模块直接复用之，避免重复造轮子；此处仅补「去首尾空白 + UTC 捷径」
# 并对外暴露上游同名 规范客户端时区。
# 差异：上游最后一步用 Intl.DateTimeFormat(...).resolvedOptions().timeZone 做真实时区库
# 归一化/可支持性校验；纯光明无时区数据库，无法判定「别名是否已规范化 / 该区是否被系统支持」，
# 故返回语法合法名本身（与 时间上下文 登记的既有缺口一致）。

从 时间上下文 导入 是IANA时区
从 字符串工具 导入 去除首尾空白

# 校验并归一化调用方时区：空串/含首尾空白/非 UTC 且非 IANA Area/Location 形态 → 空。
段落 规范客户端时区(值):
  如果 是字符串(值) == 假:
    返回 空
  如果 长(值) == 0:
    返回 空
  如果 去除首尾空白(值) != 值:
    返回 空
  如果 值 == "UTC":
    返回 "UTC"
  如果 是IANA时区(值):
    返回 值
  返回 空
