# 存储.light —— 键值存储（对齐原版 storage，命名空间 + 持久化）
#
# 提供基于 JSON 文件的键值存储：命名空间隔离、设值/读值/删值/键名表、
# 关闭后重开能读回（持久化到 JSON 文件）。
# 成员名避开光明危险词根（取/生成/导出/类型/部分/原因/全部/模式/等待/返回/异步）。

从 文件 导入 写JSON文件, 读JSON文件

类 存储:
  属性 仓路径 等于 空
  属性 空间名 等于 空
  属性 数据表 等于 空

  段落 构造 接收 存储路径, 空间名:
    己.仓路径 为 存储路径
    己.空间名 为 空间名
    设 己.数据表 为 {}
    己.载入()

  段落 载入:
    如果 文件存在(己.仓路径) == 假:
      返回
    设 原始 为 读JSON文件(己.仓路径)
    如果 原始 不等于 空 且 原始.包含(己.空间名):
      己.数据表 为 原始[己.空间名]

  段落 存值 接收 键, 值:
    己.数据表[键] 为 值
    己.保存()

  段落 读值 接收 键:
    如果 己.数据表.包含(键):
      返回 己.数据表[键]
    返回 空

  段落 删值 接收 键:
    如果 己.数据表.包含(键):
      己.数据表.pop(键)
      己.保存()

  # v2：批量存值——一次设置多个键值对，只持久化一次
  段落 批量存值 接收 键值表:
    遍历 键 之 键值表:
      己.数据表[键] 为 键值表[键]
    己.保存()

  # v2：清空命名空间——删除所有键
  段落 清空:
    设 己.数据表 为 {}
    己.保存()

  # v2：条目数
  段落 条目数:
    返回 长度(己.数据表)

  段落 键名表:
    返回 字典键列表(己.数据表)

  段落 保存:
    设 全量 为 {}
    如果 文件存在(己.仓路径):
      设 原始 为 读JSON文件(己.仓路径)
      如果 原始 不等于 空:
        全量 为 原始
    全量[己.空间名] 为 己.数据表
    写JSON文件(己.仓路径, 全量)
