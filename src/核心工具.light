# 核心工具.light —— 4 个核心工具聚合注册
#
# 把 E1 的 4 个核心工具（bash / 读文件 / 写文件 / 搜索文件）统一注册进
# 工具注册表，供 agent 循环与测试一键装载。

从 工具 导入 工具注册表
从 工具_bash 导入 造bash工具定义
从 工具_读文件 导入 造读文件工具定义
从 工具_写文件 导入 造写文件工具定义
从 工具_搜索文件 导入 造搜索文件工具定义

# ---- 注册全部核心工具到给定注册表 ----
段落 注册全部核心工具 接收 注册表:
  注册表.注册(造bash工具定义())
  注册表.注册(造读文件工具定义())
  注册表.注册(造写文件工具定义())
  注册表.注册(造搜索文件工具定义())
  返回 真

# ---- 新建一个已装载核心工具的注册表 ----
段落 默认核心注册表 接收:
  设 表 为 新建 工具注册表()
  注册全部核心工具(表)
  返回 表
