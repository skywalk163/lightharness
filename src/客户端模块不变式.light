# 客户端模块不变式.light —— R87 B4 第3组：client/modules/src/invariant.ts 纯逻辑
#
# 上游（锚点 877717787c）：packages/client/modules/src/invariant.ts
#   不变式：node 半 boot 条目图必须自洽——每一行 row.id 都能解析出
#     plugins/<id>/client.js 客户端包路径（否则刚收到图的浏览器取包会 404）。
#   graph() 与 clientPath() 读同一张表，故任意瞬间关系成立，无需等待微任务刷新。
#
# 纯逻辑剥离：Cordis internal/plugin 事件、ctx.get('clientModules')、invariants.register
#   全属宿主接线；这里把 图条目表 与 解析客户端路径 函数作为入参，做逐行自洽校验。

# 校验模块图自洽：逐行要求 row.id 能被 解析客户端路径 解析出非空包路径
# 入参：图条目表 [{id, url}]；解析客户端路径(id) → 路径串/空
# 回 {ok: 真} 或 {ok: 假, 失败表: [文案...]}
段落 校验模块图自洽(图条目表, 解析客户端路径):
  设 失败表 为 []
  遍历 行 之 图条目表:
    设 包路径 为 解析客户端路径(行["id"])
    如果 包路径 == 空:
      设 文案 为 "web 插件图行 \"" + 行["id"] + "\" 宣告 " + 行["url"] + " 但解析不到客户端包路径——__DSH_BOOT__ 取包会 404"
      列表追加(失败表, 文案)
  如果 列表长度(失败表) > 0:
    返回 ["ok": 假, "失败表": 失败表]
  返回 ["ok": 真]

导出 校验模块图自洽
