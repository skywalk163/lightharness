# 第46轮 任务2：L-169 修复 —— light-merge 自带示例的 30 条影子变量

## 缺陷回顾（第45轮登记，立项未修）

第45轮给 L-166 告警做全语料审定时扫到 light-merge 自带示例成片的影子变量。第46轮把
扫描范围扩到**两层子目录 + lightharness/stdlib**（791 个 .light，比第45轮的 702 更全），
light-merge 侧命中从 19 条涨到 **30 条 / 7 个文件**。

## 先做的决定性实证：`全局` 到底管不管用

在动手改任何示例之前，必须先确认修法成立——否则 30 处改动全是无用功。
探针 `_r46_全局探针.light`：

```light
设 甲 为 0。
设 乙 为 0。

段落 写甲:
  全局 甲。
  甲 为 42。
段落 写乙:
  乙 为 99。        # 无 全局 声明
```

结果：

```
甲(有全局声明，期望42) = 42      ← 写回成功
乙(无全局声明，期望0或99) = 0    ← 未写回
```

生成的 Python 里只有一行 `global 甲`。**`全局` 是有效修法，且告警只报未声明的那一个**
（检查器的正/反向判定都正确）。

## 逐条审定：29 条真缺陷 + 1 条合法遮蔽

| 文件 | 条数 | 段落 / 变量 | 判定 | 处理 |
|---|---|---|---|---|
| `examples/snake_game/主.light` | 10 | 加载最高分(最高分)、初始化游戏(蛇身/方向/分数/游戏结束)、移动蛇(分数/游戏结束)、显示游戏结束(最高分)、处理输入(方向/游戏结束) | **真缺陷**：游戏状态在段落间完全不共享，最高分永不更新 | 加 `全局` |
| `examples/games/snake.light` | 8 | 初始化游戏(蛇身/方向/分数/游戏结束)、移动蛇(分数/游戏结束)、处理输入(方向/游戏结束) | **真缺陷**：同上 | 加 `全局` |
| `examples/file_tools/batch_rename/主.light` | 5 | 执行批量重命名(计数器)、解析参数(当前目录/要添加前缀/要添加后缀/预览模式) | **真缺陷**：命令行解析结果全部丢失（写进局部，后续读模块级默认值）；计数恒为 0 | 加 `全局` |
| `examples/kids/number_game.light` | 3 | 玩一局(总游戏次数/总猜测次数/最高分) | **真缺陷**：跨局统计永远停在 0，最高分永不刷新 | 加 `全局` |
| `examples/calculator_app/主.light` | 2 | 计算(当前值)、主程序(当前值) | **真缺陷**：计算器累加器跨调用不保留 | 加 `全局` |
| `examples/blog_app/main.light` | 1 | 处理创建文章(下一文章ID) | **真缺陷**：自增 ID 失效，所有文章 id 都是 1 | 加 `全局` |
| `examples/module_demo.light` | 1 | 求和(总和) | **合法遮蔽**：局部累加器；模块级 `总和` 是第 21 行接收本函数结果的变量 | 局部变量改名 `总和` → `累加` |

## 改动方式

写脚本 `_r46_加全局.py`（先 dry-run 打印插入点、人工核对，再 `--apply` 写入），
在每个目标段落**体首行之前**插入 `全局 X。`，缩进与体首行一致，句末句号风格跟随所在文件。

> 为什么用脚本而不是手改 30 处：插入点规则统一（段落体首行前）、缩进/句号风格可自动跟随，
> dry-run 输出可逐条 review；手改 30 处反而容易漏和错。

## 验证（三层）

| 层 | 方法 | 结果 |
|---|---|---|
| 语法/作用域 | 7 个文件全部 `LightParser().parse()` + `PythonCodeGenerator().generate()` | **7/7 通过，0 异常** |
| 语义正确 | 统计生成的 Python 里 `global` 语句条数，与预期逐文件比对 | **29 = 29，逐文件精确匹配**（snake 8 / snake_game 10 / number_game 3 / calculator 2 / batch_rename 5 / blog_app 1 / module_demo 0） |
| 运行正确 | `module_demo.light` 实跑 | **rc=0，输出 `总和: 15 / 平均值: 3.0`**（改名后仍正确） |
| 告警清零 | L-166 全语料重扫（带抑制） | **light-merge 侧 0 命中** |

> `calculator_app` / `snake` / `number_game` 是交互式程序，不实跑（会挂住）。
> `calculator_app` 的实参传递被 CLI 的 argparse 拦截（`unrecognized arguments: + 5`），
> 改用「生成代码里 global 条数 + 编译通过」作为判据，已足够。

## 交付物

- 7 个示例文件（改）
- `_r46_加全局.py`（工具，已随探针移档）
- `_r46_验证L169.py`（三层验证脚本，已随探针移档）
- 缺陷账 L-169 标记为**已修复**
