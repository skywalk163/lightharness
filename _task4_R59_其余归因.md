# 任务4 · R59 存量红只读归因（给 R60 的底表）

- 轮次：R59（任务1-3 由 WorkBuddy 完成；任务4 归因 + 路M 由主 agent 收口）
- 基线：133701（R58 终跑，64 红）
- 归因方式：只读（不改 src/），本机定向复跑 + 钩子组合扫雷 + 0.82 全量对拍
- 状态：**R13B/R13C 已在本轮路M预修中修复转绿（属 L-176 魔数同族 + 截取语义误用族，不在任务书原范围，见 §5）**；其余 10 条为真存量红，留给 R60

## 1. 12 条归因明细

| # | 红名（基线上无类名前缀） | 分类 | 现状 | 根因复核 | R60 修复路径 | 工作量 | 风险 |
|---|---|---|---|---|---|---|---|
| 1-5 | test_p0_异步修饰符不许当值用[异步读取二进制/异步写入二进制/异步任务等待/异步任务取消/异步取数] | 异步修饰符 5 | 仍红 | 运行时 NameError（`name '异步读取二进制' is not defined`），期望错误信息含「修饰符」字样（test_frontend_blockers_run.py:1049）——编译期未识别异步修饰符当值，直接当普通标识符编译 | 编译期识别「异步修饰符被当值使用」并报友好错误（含「修饰符」字样）；一条修复全清 5 条 | 中 | 低（仅编译期报错路径） |
| 6-7 | test_paragraph_call / test_func_non_nullable_param_with_nullable_arg | 旧式语法 2 | 仍红 | ParseError：`段落相加(甲,乙)：`（半角冒号+缩进/语法元素无法识别）、`段落展示内容接收内容:数：`（「接收」旧式 + 半角冒号类型标注存疑）。均为旧式语法解析问题，非语义缺陷 | 语法解析兼容半角冒号类型标注与旧式「段落 名 接收 参数」行内写法（或在报错信息中明确指引升级写法） | 中 | 中（改解析器影响面大） |
| 8 | test_compact_binary_expr_with_call | compact 1 | 仍红 | `n乘阶乘(n减1)` 无空格运算符 `乘` 与标识符粘连 → 词法器并入标识符 → 解析为 ParagraphCall 而非 BinaryOp。与 L-174 变体同族，但任务1 的修复（`_at_statement_start`/`_emb_head_trigger`/`_assign_tail`）未覆盖**表达式内**无空格二元运算符 | 词法层支持表达式内无空格二元运算符（`X乘Y` 连写）的切分；一条修复全清 | 中高 | 高（词法改动影响全量） |
| 9 | test_O0_行政区划代码_对拍与扩展（R13B） | 魔数同族 | **已修** | 中国行政区划.light 首行带魔数 → 钩子环境优先加载 .light（旧形态，无 `_region_code_map`）→ AttributeError；无钩子单跑 .py 正常。R58 归因「本地绿 0.82 红=平台差异」为误判 | 已预修：去中国行政区划.light 魔数（.py 优先），钩子组合验证绿 | - | - |
| 10 | test_URL编码解码（R13C） | 魔数同族 + 截取语义误用 | **已修** | 双根因：① 编码解码.light 的 `编码URL编码核心` 调用 `截取(hx, k*2, 2)` 按 (start,len) 意图，而 `截取` 语义为 [start:end]（内置核心字符串.light L14-15 `返回 文本[起始:结束]`）→ 每字节仅得 1 个 `%xx`，中文损坏为 `%E5%%`；② 编码解码.light 无任何 import 却调用 `截取`（隐式全局）→ LLVM 编译期未绑定，输出错位 `%E5%8589%89`。R58 归因「Python 原版乱码奥楥」为误判（oracle 被钩子劫持成缺陷 .light） | 已预修：L306 改传 end 参数（`k*2+2`）+ 顶部补 `从 内置核心字符串 导入 截取`；LLVM O0 与 Python 后端钩子均输出 `%E5%85%89` | - | - |
| 11 | test_十个段落全部导出且两版都可调用（地板搬迁） | 地板搬迁 1 | 仍红 | 内置核心列表.light 编译 `__all__` 时混入非本模块段落名（首 extra '冻结'/'副本'）——地板搬迁时段落名收集越界 | 修地板搬迁的 `__all__` 收集（按模块边界过滤非本模块段落） | 小 | 低 |
| 12 | test_all_examples_output（examples 聚合） | examples 1 | 仍红 | 失败内容已从「hello.light 期望'为'或'等于'」（L-174 症状）变为 `hello.light name 'n乘阶乘' is not defined`（compact 症状）——L-174 症状已消除，**compact 修复可连带本用例** | 与 #8 同源，compact 修复后复跑验证 | - | - |

## 2. 分类汇总

| 分类 | 条数 | 性质 |
|---|---|---|
| 异步修饰符当值 | 5 | 真缺陷（编译期报错缺失），一条修复全清 |
| 旧式语法 | 2 | 真缺陷（解析器兼容/报错指引） |
| compact 无空格二元运算符 | 1 | 真缺陷（词法层），连带 examples 1 条 |
| 地板搬迁 __all__ | 1 | 真缺陷（小） |
| L-176 魔数同族（本轮预修转绿） | 2 | 钩子/装载顺序缺陷（R13B/R13C） |
| **合计** | **12** | 存量真红 10 条 → R60；预修转绿 2 条 |

## 3. R60 优先级建议

1. **P0 compact 无空格二元运算符**（1 条 + 连带 examples 1 条 = 2 条转绿）：词法层改动，风险高需全量验证，但收益最大
2. **P0 异步修饰符当值**（5 条）：编译期友好报错，范围收敛
3. **P1 地板搬迁 __all__**（1 条）：小修
4. **P2 旧式语法**（2 条）：解析器兼容，需评估影响面
5. **P2 截取语义误用族**（新发现潜在雷，非 133701 红）：身份证校验.light L198-200 等 6 处 `截取(x, start, len)` 形态（含 L198 `截取(号码,6,4)`、L199 `截取(号码,10,2)`、L200 `截取(号码,12,2)`、L235 `截取(号码,14,3)`、手机号校验.light L92/L156、中文数字转换.light L188 等）——`截取` 语义为 [start:end]，这些调用会得空串/错值，**当前无测试覆盖所以不红，一旦覆盖即炸**，建议 R60 统一修正（改传 end 或引入 `截取N` 新函数）

## 4. 取证证据链（探针存档）

- `_r59_t4_evidence.py` + `_out.txt`：7 组定向（异步 5/旧式 2/compact 1/R13B/R13C/地板/examples）
- `_r59_diag_r13c.py` + `_out.txt`：R13C 钩子 oracle 劫持铁证（无钩子 `%E5%85%89%E6%98%8E` vs 钩子 `%E5%%%E6%%`、MOD=字符串工具.light）
- `_r59_enc_probe2.py` + `_out.txt`：钩子加载 编码解码.light 损坏编码（修复前 `%E5%%`）
- `_r59_enc_mid.py` + `_out.txt`：中间量（编码编UTF8=E58589 正确、截取 [start:end] 语义）
- `_r59_slice_o0.py` + `_out.txt`：LLVM O0 截取语义 + 损坏编码（修复前）
- `_r59_urldecode_probe.py` + `_out.txt`：修复后双后端全对（光/光明）
- `_r59_url_dump.py` + `_out.txt`：LLVM 未绑定截取错位输出（`%E5%8589%89`）
- `_r59_scan_jiequ.py` + `_out.txt`：全 stdlib 截取三参调用扫描（误用族清单）
- `_r59_dep_scan.py` + `_out.txt`：编码解码 ↔ 内置核心字符串 依赖（无循环）

## 5. 本轮路M预修（超出任务书范围，如实声明）

任务3 的 3A「网络请求 5 条实测已绿未改源码」经复核为**假绿**（stdlib/网络请求.light 首行带魔数，钩子环境优先旧形态 .light，无视 .py 修复形态）。以此为契机，钩子全家桶扫雷（bootstrap+phase3+phase4+phase9+pure_light_hook+R13B+R13C）发现 **L-176 魔数同族共 3 处**：

| 文件 | 缺陷 | 预修 |
|---|---|---|
| stdlib/网络请求.light | 魔数优先 → 旧形态遮蔽 .py 修复（5 条红） | 去魔数（`.py` 优先）✓ |
| stdlib/中国行政区划.light | 魔数优先 → 旧形态无 `_region_code_map`（R13B 红） | 去魔数 ✓ |
| stdlib/编码解码.light | 截取语义误用（L306）+ 缺 import（LLVM 未绑定）→ URL编码 中文损坏（R13C 红） | L306 传 end + 补 import ✓ |

预修后钩子组合（bootstrap+护栏+R13B+R13C）132 passed / 0 failed。三处 .light 修复将在路M sync 后带入 0.82 全量。
