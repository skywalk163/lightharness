# 第17轮任务4 交付报告：宿主工具接线原型

> 对标编号：#113 ｜ 差异编号：R17-D4 ｜ 缺陷编号预分配：L-147~L-148（**本轮空缺顺延**）
> 模块：`src/宿主工具.light`（295 行 / 自包含原型，不依赖尚未实现的 宿主运行时.light 等）
> 测试：`examples/test_宿主工具.light`（161 行 / **43 断言**：29 断言相等 + 14 断言真）
> 反跑：`_antirun_t4_宿主工具.py`（**3/3 ALL OK**）
> 状态：测试 rc=0 全绿；反跑 A/B/C 三判据均真红，恢复后回归绿 + 字节级恢复校验通过。

---

## 一、上游对应 / 设计灵感映射

本任务**不是逐行复刻**（上游 `defineTool` 依赖 Cordis Context + Node 执行面），而是设计等价的光照接线层。设计灵感来源与落地对照：

| 上游概念（设计灵感） | 本路落地（宿主工具.light） | 说明 |
|---|---|---|
| `defineTool` {name, description, parameters, execute} | `注册工具` 的「工具声明」形状 + 运行时工具表 | 声明即注册，全名=`插件名/工具名` |
| 工具注册表（注册/注销/命名空间） | §1 工具发现器（注册/注销/注销全部/插件启动注册/插件停止注销） | 自包含字典注册表 |
| 执行上下文注入 {Context, 会话, 取消令牌, 作用域, 请求ID} | §2 造执行上下文 + 造取消令牌 + 监听取消 | 上下文含 运行时引用/取消令牌/请求ID/调用者 |
| 调用审计 {请求ID, 工具名, 调用者, 参数摘要, 结果状态, 耗时, 时间} | §3 记审计 + 获取审计日志 | 每次调用落审计表 |
| 可见性 public/internal/private | §3 检查调用权限 | private 仅注册者可调用 |
| 速率限制 | §5 检查限流/记录限流/重置限流 | 计数器+上限简版 |
| 参数校验（prepare→校验→execute→finalize） | `调用工具` 复用 `工具.light` 的 `校验参数` | **唯一只读导入的既有模块函数** |
| 工具分组/命名空间 | §4 解析工具名/列举工具（按分组/插件/可见性过滤） | 全名+短名解析 |

---

## 二、实现要点（接口契约）

1. **运行时载体（自包含）**：`造接线运行时` 返回 `{工具表, 短名表, 审计日志, 请求计数, 事件监听表, 令牌表, 限流表}`。因任务1/2/3 的 `宿主运行时.light` 本轮尚未实现，本路以字典形状承载运行时（整合验证时再接到真实模块）。
2. **工具发现器**：`注册工具` 以 `插件名/工具名` 为全名入库；同插件同名冲突抛错；跨插件同名以全名区分，短名表标记歧义（`""`）。
3. **执行上下文注入**：`调用工具` 自动造 `{运行时引用, 参数, 调用者插件名, 作用域, 取消令牌, 请求ID}` 并传给 execute 函数（首个参数）。测试 `上下文探测执行` 验证五个字段均注入。
4. **取消令牌**：`造取消令牌`/`取消令牌`/`监听取消`/`令牌已取消`/`校验未取消`——取消时触发所有监听回调（回调异常被隔离不中断）。
5. **权限控制 + 审计**：`检查调用权限`（public/internal/private）；每次调用写审计日志（含请求ID/调用者/结果状态/错误码/耗时）。
6. **调用协议**：`调用工具` 流程 = 解析全名 → 权限检查 → 限流检查 → 造上下文 → 派发 tool/called → **参数校验（复用 工具.light 校验参数）** → 执行 → 派发 tool/completed 或 tool/failed → 写审计 → 记录限流 → 返回统一结果 `{成功, 数据, 错误码, 错误消息, 元数据}`。
7. **批量调用**：`批量调用` 顺序执行，单失败不影响其余（返回结果列表）。

---

## 三、验证

- **测试**：`python 运行.py examples/test_宿主工具.light` → `test_宿主工具 PASS`（rc=0），**43 断言**。
- **反跑**：`python _antirun_t4_宿主工具.py` → **ALL OK**（sha256 `d927a0a05d1d`）：
  - A 调用协议跳过执行（`函数(上下文)` 改 `空`）→ 2b「执行返回计算结果==10」立红；恢复 → 绿。
  - B 权限控制全放行（`检查调用权限` 恒返回 真）→ 3a「跨插件调 private 失败」立红；恢复 → 绿。
  - C 停止不自动注销（`插件停止注销工具` 不再调用 `注销全部`）→ 6b「停止后工具不可调用」立红；恢复 → 绿。

---

## 四、缺陷登记

**L-147 / L-148 预分配，本轮空缺顺延（无新语言缺陷）。**

本轮未触发任何真正语言缺陷。排查到的两处摩擦均为既有纪律，非新缺陷：

- **`校验参数` 返回错误列表而非抛异常**：这是 `工具.light` 的**库 API 约定**（对齐上游 json-schema.ts 返回结构化错误列表），非语言缺陷。本路在 `调用工具` 中显式捕获 `校验参数` 返回值并判非空 → 前置返回 `PARAM_SCHEMA_INVALID` 失败结果。属正确使用，不登记。
- **`定义` / `回调` 被保留字拦截**：属光明**一般关键字报错**（解析期直接报错，非静默陷阱），按一般关键字规则规避（改名 `条目`/`处理器`）。与 L-119（单字别名静默保留字）性质不同，不登记。

---

## 五、剔除与偏差（设计决策，建议路M 订正/采纳）

1. **自包含注册表 vs 复用 `工具.light` 注册表类**：任务书说「只读导入 工具.light 的工具注册表和执行函数」，但 `工具.light` 的 `工具注册表` 类以 flat 字符串名索引、无插件命名空间/可见性/短名歧义概念。本路的「工具接线层」核心是命名空间+可见性+分组，故在运行时字典上自建注册表，仅复用其 `校验参数`（参数校验）。这是原型设计决策，接口契约与任务书一致。
2. **`参数模式` 须为英文键 JSON Schema**：`校验参数` 的 `校验器` 只识别英文键 `properties`/`required`/`type`（见 `工具.light` 第58-293行）。故测试构造 `参数模式` 用 `{"type":"object","properties":{"x":{"type":"number"}},"required":["x"]}`，**不可**用 `造工具模式` 的 `{name,description,parameters}` 包装（其 `parameters` 內层也需是英文键 schema，否则校验器跳过校验）。
3. **限流计数时机**：仅在「放行调用」后 `记录限流`，RATE_LIMITED 提前返回不计数（避免上限被自身永久锁死）。
4. **事件派发**：`tool/called`/`tool/completed`/`tool/failed` 用本地轻量 `事件监听表` 实现（不依赖任务3 的 `宿主事件.light`，避免跨任务耦合）。

---

## 六、移交（#113 / R17-D4）

- 可写文件（全部新增，零越界，未修改任何既有 `src/` 模块）：
  - `src/宿主工具.light`
  - `examples/test_宿主工具.light`
  - `_antirun_t4_宿主工具.py`
  - `_task4_宿主工具_交付报告.md`（本文件）
- 只读复用：`工具.light` 的 `校验参数`（唯一）。
- 缺陷留档：L-147 / L-148 空缺顺延。
- 未 commit，收口归路M。
