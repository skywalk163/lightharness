# _task4_R32_MERGE_WHOLE第二批精简 —— 后 5 条逐条验证（G1∧G2∧G3）

> R32 任务4（P0）｜ 修改区域：`light-merge/src/lexer.py` `_P0A_MERGE_WHOLE`
> 条目：外部命令 / 排序依据 / 输出块表 / 返回码 / 记录类型（R30 新增）

## 一、结论

| 条目 | 语料命中 | G1 语料门 | G2 编译门 | G3 边界门 | 判定 |
|---|---|---|---|---|---|
| 外部命令 | 36 处/13 文件 | ✅ 0 变化 | ✅ rc=0 | ✅ 5 形态一致 | **删除** |
| 排序依据 | 6 处/2 文件 | ✅ 0 变化 | ✅ rc=0 | ✅ 5 形态一致 | **删除** |
| 输出块表 | 5 处/1 文件 | ✅ 0 变化 | ✅ rc=0 | ✅ 5 形态一致 | **删除** |
| 返回码 | 55 处/16 文件 | ✅ 0 变化 | ✅ rc=0 | ✅ 5 形态一致 | **删除** |
| 记录类型 | 15 处/3 文件 | ❌ 2 文件变化 | ✅ rc=0 | ❌ F2/F5 劈开 | **保留（真护栏）** |

第二批 5 → 4 删 1 留；两批合计 10 → 3（精简 70%）。

## 二、场景定位（4.1）

- `外部命令`：13 文件 36 处（bootstrap/release/stdlib/外部命令.light、
  stdlib/外部命令.light、代理工具集、进程树 等），设名位与成员访问；
- `排序依据`：data_cleaner 示例 2 文件 6 处，设名位；
- `输出块表`：stdlib/伪终端.light 5 处，设名位/成员访问；
- `返回码`：16 文件 55 处（进程.light、test_L010、test_e2b 等），
  **全部为成员访问形态 `结果.返回码` 或设名位**（R30 注释预判的
  「成员访问规则整体成词」在此实证）；
- `记录类型`：test_R29_CCW精简边界.light(2)、test_R30_CCW通用化边界.light(2)、
  test_R30_零除幂次记录类型通用化_快速.light(11) —— R30 通用化的**语料钉住形态**。

## 三、逐条验证（4.2/4.3）

### G1 语料判据

变体构造与比对口径同任务3（编辑前快照行级手术 + 锚点断言）：

```
G1 外部命令   通过  变化文件=0  新增错误=0
G1 排序依据   通过  变化文件=0  新增错误=0
G1 输出块表   通过  变化文件=0  新增错误=0
G1 返回码     通过  变化文件=0  新增错误=0
G1 记录类型   失败  变化文件=2
    lightharness/examples/test_R29_CCW精简边界.light
    lightharness/examples/test_R30_CCW通用化边界.light
第二批并集删除(4 条)  变化文件=0  ✅ token 零变化
```

`记录类型` 打红与 R30 侧判断一致：`类型` 是 `_P0A_HARD_STMT` /
`_P0A_SUFFIX_SPLIT_KW` 成员，撤掉整串登记后 `设 r7 为 记录类型(用户)` 被劈成
`记录 + 类型`，两个 R29/R30 边界语料文件 token 序列变化。

### G3 边界门

| 形态 | 外部命令 | 排序依据 | 输出块表 | 返回码 | 记录类型 |
|---|---|---|---|---|---|
| F1 设名位 | 一致 | 一致 | 一致 | 一致 | 一致 |
| F2 函数名 | 一致 | 一致 | 一致 | 一致 | **变化** |
| F3 成员访问 | 一致 | 一致 | 一致 | 一致 | 一致 |
| F4 段落名 | 一致 | 一致 | 一致 | 一致 | 一致 |
| F5 传参 | 一致 | 一致 | 一致 | 一致 | **变化** |

`记录类型` 打红明细：F2/F5 由 `IDENTIFIER:记录类型` 变为
`IDENTIFIER:记录 + KEYWORD:类型`（类型 为硬语句关键字）。

### G2 编译门

`lightharness/examples/test_R32_MERGE_WHOLE精简边界.light`：
移除前 rc=0、移除后 rc=0（含 保留条目 整理模型消息/非空块/记录类型 的
函数名整词断言，防回归）。

## 四、执行删除（4.4）

- lexer.py `_P0A_MERGE_WHOLE`：移除 外部命令/排序依据/输出块表/返回码
  （记录类型 保留，R30 注释原样保留），同步 :2268 调用点与类 docstring 注释；
- 删除后全量反跑（与任务3 合并口径）：pre-R32 HEAD ↔ 工作区，
  **857 文件 token 序列零变化、token 总数差 +0**。

## 五、`记录类型` 保留理由（铁律要求详录）

1. **G1 硬打红**：撤单条即变化 2 个真实语料文件（非人工构造），
   不满足「变化文件数=0」；
2. **G3 打红**：函数名/传参位被 `类型` 劈开，而 `类型` 在语料中是
   `_P0A_MERGE_WHOLE` 之外大量 `X类型` 复合词（数类型/内容类型/分类内容类型/
   注册事件类型）的后缀切分关键字，任何「类型 前向并入」类泛化都会波及它们
   （R30 已实证），故只能以「精确整串」口径保留；
3. **下游依赖**：`外部 调试 {…记录类型…}`（parser_stmt.py:6653 FFI 调试配置）
   按 `IDENTIFIER|KEYWORD` 双口径接收，整串成 IDENTIFIER 是其现状契约。

## 六、交付物

- `light-merge/src/lexer.py`（_P0A_MERGE_WHOLE 10→3，本批贡献 删4）
- `lightharness/_antirun_r32_t4_MERGE_WHOLE第二批.py`（本批逐条反跑，t4 口径）
- `lightharness/_r32_t4_g1_结果.json` / `_r32_t4_g1.log`（G1 证据）
- `lightharness/_r32_t34_g3_边界门.py`（G3 证据，两任务共用）
- `lightharness/examples/test_R32_MERGE_WHOLE精简边界.light`（G2 用例）
- 全量反跑证据（见任务3 文档第六节，同一套快照）
