# 第45轮 任务2：L-166「影子变量」编译告警落地

## 1. 根因（为什么必须做）

第44轮修复的 **L-165** 是这类缺陷的标本：

```light
设 当前实例 为 空                      # 模块级

段落 新建Mock服务 接收 选项:
  设 实例 为 新建 Mock大模型服务器(选项)
  设 当前实例 为 实例                  # ← 意图写回模块级
  返回 实例
```

按作用域语义，`设 当前实例 为 实例` 创建的是**局部**变量，模块级 `当前实例` 不受影响。
想写回必须显式 `全局 当前实例。`。漏写时：

- 不报 `NameError`（局部赋值完全合法）；
- 不报警告；
- 运行期症状是「模块级变量恒为初值」——本例表现为真实 HTTP 服务永远返回
  `500 MOCK_NOT_READY`，症状离现场极远，排查成本极高。

**这是唯一能用静态分析抓到的模式**，此前语言层完全没有对应信号。

## 2. 做了什么

### 2.1 新增检查器 `light-merge/src/scope_shadow_check.py`

解析后对 AST 做一次扫描：

| 步骤 | 内容 |
|---|---|
| 收集 | 模块级被赋值的名字 |
| 对每个顶层 `段落` | 形参名 / `全局` 声明的名字 / 函数体内被赋值的名字 |
| 判定 | `函数内赋值名 ∈ 模块级名` 且 `∉ 全局声明` 且 `∉ 形参` → 告警 |

刻意不做的事（避免噪音与误报）：

- **不进入**嵌套 `段落` / 类 / 方法体（各自独立作用域，应各自判定）；
- **不告警**只读不写的名字（读模块级变量是合法且常见的）；
- **不告警**形参遮蔽（正常写法）；
- 纯函数、容错，任何异常都不能阻断编译。

### 2.2 接入点：必须接在**真正生效**的编译入口

> ⚠️ **踩坑记录**：`light run` **不走** `LightCompiler.compile()`，而是
> `cli/light.py::_compile_src`（主文件）+ `_resolve_local_imports`（依赖模块）。
> 一开始接在 `compiler.py` 里，实测**完全不生效**（0 条告警）。
> L-165 恰好发生在**被导入的** `mock大模型服务器.light` 里，所以
> `_resolve_local_imports` 那个点**必须**接，只接主文件会漏掉它。

- `cli/light.py::_warn_shadow_scope(module, 标签)`：输出到 **stderr**（stdout 是程序输出，
  绝不能被污染），`LIGHT_WARN_GLOBAL_SHADOW=0` 可关。
- 调用点：`_compile_src`（主文件）与 `_resolve_local_imports`（依赖模块）。

### 2.3 顺带补齐：`LightCompiler` 的告警此前**只进不出**

`compile()` 的返回字典里**根本没有 `warnings` 键**——也就是说 L-068 那批告警
（类结束标记 / 模块级函数与类成员同名）**从来没人看得见**。本次补上
`'warnings'` 键（纯新增键，不影响既有调用方），并按次重置 `self.warnings`
（否则重复编译同一实例会累积重复告警）。

## 3. 验收

| 判据 | 结果 |
|---|---|
| 人造 L-165 样例必报警 | ✅ 命中 `坏_写不回` |
| 声明 `全局` 后必消失 | ✅ `好_写回` 不报 |
| 形参同名不误报 | ✅ |
| 端到端浮出 stderr | ✅ 且 `rc=0` |
| stdout 不被污染 | ✅ `PASS` 在 stdout，`L-166` 只在 stderr |
| `LIGHT_WARN_GLOBAL_SHADOW=0` 静默 | ✅ |

新增：
- `lightharness/examples/test_R45_影子变量告警.light`（运行期自校验：影子后模块级仍为初值
  `0`；声明 `全局` 后写回为 `2`）
- `lightharness/tests/test_R45_L166影子变量告警.py`（5 项：**5 passed**）

## 4. 噪声实测

全语料（lightharness examples+src ∪ light-merge examples）**702 个文件**：
命中 **12 个文件 / 40 条**（1.7% 文件）。详见 `_task3_R45_影子变量清单.md`。

## 5. 交付物

- `light-merge/src/scope_shadow_check.py`（新增）
- `light-merge/cli/light.py`（接入 + 透出）
- `light-merge/src/compiler.py`（补 `warnings` 键 + 按次重置）
- `lightharness/examples/test_R45_影子变量告警.light`
- `lightharness/tests/test_R45_L166影子变量告警.py`
