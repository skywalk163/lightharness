# E1 工具系统与核心工具 —— 交付说明

> 任务文档：`外发任务_lightharness_E1_工具系统与核心工具.md`
> 仓库：`G:\dswork\duan-light-merge\lightharness`
> 运行方式：在本目录执行 `python 运行.py examples/test_工具系统.light` 与 `python 运行.py examples/test_agent工具调用.light`

## 一、交付清单

| 交付物 | 文件 | 状态 |
|---|---|---|
| 工具注册框架 | `src/工具.light`（函数 `工具注册表`，含 注册/查找/描述表/执行 + JSON Schema 校验） | 已存在，未改动，直接复用 |
| 核心工具聚合 | `src/核心工具.light`（`注册全部核心工具` / `默认核心注册表`） | 新增 |
| bash 工具 | `src/工具_bash.light` | 新增 |
| 读文件工具 | `src/工具_读文件.light` | 新增 |
| 写文件工具 | `src/工具_写文件.light` | 新增 |
| 搜索文件工具 | `src/工具_搜索文件.light` | 新增 |
| 路径安全护栏 | `src/工具_路径安全.light`（`取根目录` / `解析安全路径`，越界拒绝） | 新增 |
| Agent 工具调用循环 | `src/agent.light`（`运行智能体` + 钩子管理器 `钩子管理器`） | 新增 |
| 交付测试 1（工具系统） | `examples/test_工具系统.light` | 新增，通过 |
| 交付测试 2（Agent 端到端） | `examples/test_agent工具调用.light` | 新增，通过 |

## 二、功能点对照（任务文档 → 实现）

- **工具注册表**：`工具注册表` 已实现 `注册 / 有无 / 查找 / 名单 / 描述表 / 执行 / 批量执行`；`执行` 走「准备(prepare)→校验(schema)→派发(execute)→收尾(finalize)」流水线，未知工具返回 `TOOL_NOT_FOUND`，参数非法返回 `PARAM_SCHEMA_INVALID`。
- **4 个核心工具**：bash（固定工作目录 + cd 越界拒绝 + 30s 超时）、读文件（行范围 + 超大文件截断）、写文件（自动建父目录 + 覆盖提示）、搜索文件（文件名/内容两模式 + 跳目录/二进制）。
- **Agent 循环集成**：`运行智能体(初始消息, 注册表, 聊天函数, 事件推送, 钩子, 最大轮次, 模型)`：
  - 每轮把 `注册表.描述表()` 作为 tools 传给 `聊天函数`；
  - 检测 `响应["工具调用"]` → 逐个 `事件推送` 契约1「工具调用」事件 → 执行 → 提取内容/错误 → `事件推送` 契约1「工具结果」事件 → 回传 tool 消息 → 继续；
  - 无 tool_calls → 推送「文本」事件并 return；
  - 达 `最大轮次` → return `"工具调用轮次超限"`。
- **中间件钩子（契约4）**：`钩子管理器` 实现 `注册预工具 / 注册后工具 / 注册后轮次` 注册机制；`运行智能体` 在 pre_tool（批准/拒绝/需审批）、post_tool（修剪结果）、post_turn 处调用。E1 按要求仅实现空注册机制。

## 三、根因与修复（本次开发踩到的光明语言/框架坑）

### 坑 1：`工具_搜索文件.light` 全模块解析失败
**现象**：导入即报「解析错误」，且行号虚报（5 行文件报「行47」）。
**根因**：标准库 `文件系统` 模块的若干导出名含**词法伪关键字**，被词法器在标识符内部切开，无法从光明源码调用：
- `筛选目录列表`、`文件行列表` 含 `列`（`keywords.py:355` `'列': -1`，可变参数/列表构造）；
- `遍历目录(树)`、`目录列表`、`文件列表` 含 `遍历`（`keywords.py:77`）；
- `按扩展名筛选`、`筛选目录列表` 含 `筛选`（`keywords.py:372` `'筛选': 2`）。

**修复**：搜索文件工具改用 `导入 os` + `os.walk` + `读取文件` 实现遍历，彻底绕开全部坏名字。

### 坑 2：工具运行时 `name '获取环境变量' is not defined` 等
**现象**：bash/读/写/搜索工具调用即 `TOOL_EXECUTION_FAILED`。
**根因**：lightharness 仅把 `stdlib/builtins.py` 的 `__all__` 清单注入为自动可见函数（`读取文件`/`写入文件`/`文件存在`/`目录存在`/`文件大小`/`绝对路径`/`连接路径`/`目录名`/`当前目录`/`环境变量`/`连接字符串`/`分割字符串`/`替换字符串`/`解析JSON`/`长` 等）。而工具文件写的是 `文件系统.py` 的**另一套名字**，且部分根本不在注入清单：
- `获取环境变量` → `环境变量`
- `当前工作目录` → `当前目录`
- `路径连接` → `连接路径`
- `规范化路径` → 直接用 `绝对路径`（其底层 `os.path.abspath` 已归一化 `..`，无需再套）
- `连接` → `连接字符串`
- `文件行列表` → `读取文件` + `分割字符串`（避开含 `列` 的坏名）

**修复**：`工具_路径安全.light`、`工具_读文件.light` 统一改用注入名。

### 坑 3：`从 外部命令 导入 执行命令` 失效
**现象**：`name '外部命令' is not defined`。
**根因**：光明对 `.py` 模块的 `from X import Y` 不生效；但 `导入 外部命令`（整模块导入）可用，随后以 `外部命令.执行命令(...)` 访问。已验证 `src/子进程.light` 即用此写法。
**修复**：`工具_bash.light` 改为 `导入 外部命令`。

### 坑 4：行内函数体不被解析
**现象**：`段落 推送 接收 e: 事件.追加(e)` 报「期望冒号但得到点号」。
**根因**：光明不支持「`段落 定义 接收 参数: 语句`」写在同一行，函数体必须换行缩进。
**修复**：`examples/test_agent工具调用.light` 中 3 处行内体改为多行。

### 坑 5：编译器行号/列号偏移误报
**现象**：真实 5 行文件报「行47 列38」；长串含中文+转义引号时列号按字节而非字符计。
**影响**：定位解析错误时行号不可信，需靠**隔离二分实验**定位。本次通过逐段剥离（模式/跳过/循环/筛选）锁定真凶。

## 四、路径偏差说明（相对任务文档）

任务文档建议把 4 个工具放在 `src/工具/bash.light` 等**子目录**下。但：
1. 现有 `src/工具.light` 已是注册表模块，子目录 `工具/` 会与它**命名冲突**；
2. 光明点号导入（`从 工具.bash 导入 ...`）仅相对导入文件所在目录解析，`examples/` 测试无法 import `src/` 内子包，会导致回归 `examples/*.light` 全红。

**决策**：4 个核心工具实现为**扁平顶层模块** `src/工具_bash.light` / `工具_读文件.light` / `工具_写文件.light` / `工具_搜索文件.light`，全项目可导入、可被回归引用，功能与文档完全一致。

## 五、验收状态

- [x] 注册表 + 4 工具可注册/执行，返回格式正确
- [x] 路径/越界安全护栏生效（读越界、`..`、bash `cd ..` 均被拒）
- [x] Agent 循环：tool_calls 检测→执行→结果回传→继续；pre_tool 拒绝、post_tool 修剪、契约1 事件、轮次上限均验证
- [x] 两个交付测试通过（`python 运行.py examples/test_工具系统.light`、`examples/test_agent工具调用.light` 均打印「通过」）
- [~] 全量回归：`pytest tests/test_回归.py` 结果 **190 passed / 2 failed**（约 7 分钟）。

### 回归 2 个失败项与 E1 无关（既有问题，非本次引入）

| 用例 | 报错 | 根因 | 与 E1 关系 |
|---|---|---|---|
| `test_web路由.light` | `Web服务端.处理对话() takes 2 positional arguments but 4 were given` | `src/web服务器.light`、`examples/运行Web服务器.light` 存在未提交 WIP 改动（Task B / W3 在制），`处理对话` 调用/定义签名不符 | 无。本文件未被 E1 触碰（`git status` 可证） |
| `examples/__exp_e2e.light` | 同上 | 它是 `src/web服务器.light` 的副本，由 web 路由示例在回归运行中动态拷贝进 `examples/` 再执行，因此连带失败 | 无。E1 未创建/修改该文件（其 `mtime` 在回归运行中，非本次会话产物） |

**结论**：E1 的改动只涉及工具子系统（`工具_路径安全`/`工具_读文件`/`工具_bash`/`工具_搜索文件`）+ 2 个新增测试 + `agent`/`核心工具`；未改动任何 web 路由相关文件，也未改动其它既有示例。回归中 190 个用例通过（含 E1 新增的 2 个）；2 个失败均源自既有的、与 E1 无关的 web 服务器在制改动，**E1 未破坏任何原有通过用例**。

## 六、遗留/建议

- 标准库 `文件系统` 模块的「含伪关键字导出名」是系统性坑（影响任何想直接调用目录遍历 API 的光明代码）。建议：要么在 `builtins.py` 注入 `遍历目录`/`筛选目录列表`/`文件行列表` 等别名，要么在 lightharness 文档标明「目录遍历请用 `导入 os`」。本次 E1 已在 `工具_搜索文件.light` 顶部注释该坑。
- 编译器行号误报建议在 light-merge 侧修复（属语言本体问题，超出 E1 范围）。
