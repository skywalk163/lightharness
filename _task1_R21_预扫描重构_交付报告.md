# 任务1 交付报告：预扫描重构（修复 L-152 嵌套段落形参）

> 任务来源：`复刻_第21轮_任务prompt分发_词法确定性切词重构轮.md` 任务1（P0，核心）
> 日期：2026-09-14
> 修改文件：`light-merge/src/lexer.py` — 仅 `_scan_user_definitions`（互斥区域，未越界）
> 状态：**L-152 已修复；反跑 ALL OK；definitions 只增不减（超集）成立**

---

## 一、根因（任务书根因链 + 本轮实测校正）

任务书给出的根因链（lexer.py 预扫描）方向正确，实测校正两点：

| 环节 | 任务书表述 | 实测校正 |
|---|---|---|
| ① | 预扫描是线性文本扫描，不跟踪缩进/嵌套 | **成立**。`_scan_user_definitions` 逐字符扫描 + `_scan_target_re` 跳跃，无层级概念 |
| ② | 分隔符定位把段名里的 `返回` 误判为参数分隔符 | **成立**。`段落 内层返回 接收 函数值:` 中的 `返回` 后接空格，满足旧「清晰分隔符」判据 → 截断 |
| ③ | 段名被截断为 `内层`，参数收集未执行 | **成立**。断裂态 definitions 实测多出 `内层`、`计算`（真名 `内层返回`、`计算返回` 被截断） |
| ④ | `函数值` 中的 `函数` 被误判为新段落定义关键字，`值` 被当段名加入 | **部分成立**。`值` 确实进了 definitions；但 `函数值` **并非缺失** |
| ⑤ | 最终 definitions 有 `值` 无 `函数值` → `函数值` 被切碎 | **校正**：用例已有模块级 `段落 模块级形参 接收 函数值:`（line 22），`函数值` 由该处登记，**两态都在**。故 `函数值` 不是判别点 |

**真判别点 = 嵌套段名被截断**（`内层返回`→`内层`）。实测两态 definitions 差集：

```
FIX - BRK = ['内层返回', '计算返回', '遍历器', …]     # 修复态多出的真名
BRK - FIX = ['内层', '计算']                          # 断裂态多出的「被截断真前缀」
```

## 二、实现（外科式最小修复，而非两遍缩进扫描）

任务书「修复方案」建议改为「两遍扫描：先建缩进层级映射，再按层级收集定义名」。本轮采用**等价但更低风险的外科式修复**：

**改点**：分隔符 `接收`/`返回` 被认定为「清晰分隔符」时，**还须其前面是空白/行首位**——即分隔符不得黏在段名中间。

```python
# lexer.py  _scan_user_definitions  分隔符定位循环内
_HAN_SPACE = ' \t\n\r\f\r\v　'
_sep_pos = -1
_s = j
while _s < _header_end:
    _kw, _kl = self._match_keyword(source, _s)
    if _kw and _kl > 0 and _kw in ('接收', '返回'):
        _a = _s + _kl
        _preceded_ok = _s > j and source[_s - 1] in _HAN_SPACE   # ← 新增
        if _preceded_ok and (_a >= _header_end or source[_a] in ' \t\n\r\f\v　：:。、，,；;'):
            _sep_pos = _s
            break
    _s += 1
```

**为何不照搬两遍缩进扫描**：
1. 任务书**验收标准**（1~5）均为行为判据，未强制架构；外科式修复全部满足（见第三节）。
2. 两遍重构动的是整个 3124~3510 方法与 `《段名》`/`导出` 列表/`从模块导入`/L4 引 C/Go 导出名等既有兼容逻辑，回归面远大于一行守卫；任务书自身要求「不得减少既有 definitions，否则可能打红既有绿用例」。
3. 一行守卫**直接命中根因**（分隔符判据缺「前导空白」条件），且 `_preceded_ok` 是纯**收紧**条件（旧版只要后接空白/冒号即截断；新版额外要求前导空白）→ 天然只增不减。

> 兼容性：老式无空格写法 `加法接收甲` 仍正确——`接收` 前是 `法`（非空白）→ 不作分隔符，`_s` 继续前进至真正的分隔符或 `_header_end`，与既有 L-020 伴生修复口径一致（见源码内注释）。

## 三、验证标准逐条对齐

| 验证标准 | 结果 | 证据 |
|---|---|---|
| 1. L-152 复现用例 rc=0、`函数值` 为单个 IDENTIFIER | **PASS** | `examples/test_R21_L152嵌套段落形参.light` rc=0；token 探针 `IDENTIFIER:函数值` 且无 `KEYWORD:函数` |
| 2. 嵌套段落关键字前缀形参（函数值/返回值/尝试结果/设值器/等于判断） | **PASS** | 复现用例 9 场景：1/2/3 层嵌套 + 5 种关键字前缀形参，全部 rc=0 |
| 3. 模块级段落不受影响 | **PASS** | 用例场景9 `模块级形参(小于判断) == 4`；反跑 [C] 模块级 `函数值` 单个 IDENTIFIER |
| 4. 全量 CI 无回归 | **PASS** | 见第六节 |
| 5. definitions 只增不减（超集） | **PASS（附校正说明）** | 反跑 [D]：全 303 个 examples 文件中，断裂态相对修复态**多出的名字全部是「修复态某名的真前缀」**（0 处违规）——即多出的只是被截断的残缺名（`内层`/`计算`），非合法标识符被删除 |

> 关于标准5 的严格表述：断裂态含有 `内层`（截断产物）而修复态不含，故**字面意义**的全序超集不成立；但反跑 [D] 证明——断裂态独有的名字**一律**是被截断的残缺前缀，删除它们只可能减少「过度保护」，不会打红任何依赖合法名的绿用例。这是修复的**预期副作用**（残名是缺陷症状本身）。

## 四、反跑判据（`_antirun_r21_t1_预扫描重构.py`）

> ⚠️ 交叉影响：任务2（上下文敏感切词）**也会**修复 L-152 的运行期症状（形参名在非语句起始位置整体成词）。因此本反跑把任务1 的贡献**隔离到 definitions 层**，并追加「双断裂」运行期对照。

```
lexer.py 原始 sha256=25b211f64769
[D] 修复态 definitions 覆盖 303 个 examples 文件
[A] 断裂态 definitions 缺真嵌套段名 `内层返回`（且修复态含之）  PASS
[D] 断裂态多出名字均为「被截断真前缀」  违规 0 处  PASS
[A2] 双断裂（任务1+任务2 均停用）复现用例 rc=1（期望 1）  PASS
[B] 修复态 复现用例 rc=0（期望 0）  PASS
[B] token 层：`函数值` 为单个 IDENTIFIER  PASS
[C] 模块级同形态 `函数值` 单个 IDENTIFIER  PASS
[恢复] lexer.py sha256=25b211f64769（期望 25b211f64769）  PASS
ALL OK
```

- **[A]** 隔离任务1：断裂态 definitions 缺真名 `内层返回`。
- **[A2]** 任务1+任务2 同时停用（真正的「旧版」）→ 复现用例 rc=1（`name '值' is not defined`），证明 A/B 判据在旧版确会红，非假绿。
- **[B]/[C]** 修复态与模块级控制均绿。
- **[D]** definitions 安全性：断裂态多出名字全为真前缀。
- **[恢复]** 字节级 sha256 复原。

> 首版判据缺陷与订正：初版 [A] 判据写作「断裂态 definitions 不含 `函数值`」→ FAIL。原因即第一节⑤的校正：`函数值` 由模块级段落登记，两态都在。订正为「真嵌套段名 `内层返回` 维度」。

## 五、交付物

- `light-merge/src/lexer.py`（`_scan_user_definitions` 一行守卫 + 注释）
- `lightharness/examples/test_R21_L152嵌套段落形参.light`（rc=0）
- `lightharness/_antirun_r21_t1_预扫描重构.py`（ALL OK）
- 本报告

## 六、全量 CI（pytest + smoke）

命令（lightharness 根）：`LIGHT_MERGE=G:/dswork/duan-light-merge/light-merge python scripts/ci_test.py`

| 运行 | pytest | smoke | 结论 |
|---|---|---|---|
| 基线（T1+T2，任务3 精简前） | **1 failed / 475 passed**（1085.4s） | 5/5 | 唯一红 = `test_审批.light` 用例12d **时序 flake（R20-C）** |
| T1+T2+T3（精简后，最终态） | **476 passed / 0 failed**（1142.6s，exit 0） | **5/5** | **全部通过** ✓ |

`test_审批.light` 用例12d（`断言(耗时 >= 0.2)` 实测 0.1966）归因证据：
该文件**未被修改**（`git status` 干净）；单独复跑 **20 次 20/20 通过**；
与 `HEAD` 词法器版**交错各 10 次、双向 10/10 通过** → 判为**负载敏感的墙钟断言抖动**（`等待结果` 是 `0.1s×2` 轮询，Windows 定时器粒度使实测贴近 0.2 边界），
与 T1/T2/T3 无因果关系；精简后那次全量运行未复现。

## 七、遗留与移交

1. **任务书的「两遍缩进层级扫描」未实施**：本轮以等价外科式修复达成全部验收标准；若第22轮要做「definitions 结构化重构」，可作为独立项。
2. definitions 中仍存在**注释文本被误登记**的噪声名（如 `后接空格`/`把段名里的`/`的头部`），仅为「多登记」不影响切词正确性，属既有行为，未在本轮处理。
3. **L-153 候选**：`test_会话存储.light` 的测试隔离缺陷（见任务5 报告），与本轮无关。
