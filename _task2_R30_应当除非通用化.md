# R30 任务2：应当 / 除非 通用化

> 交付：`light-merge/src/lexer.py`（复用任务1 的词首前缀类 + CCW 删 2 条）
> 证据：`lightharness/_task1t2_R30_证据.json`、`lightharness/_r30v2_iso.py`
> 基线对照：`lightharness/_r29_baseline_tokens.json`（CCW=10 态全语料 token）
> lexer sha（二进制）：`96f63c114746`

## 1. 目标

为 `应当` / `除非` 提供通用词法规则，使其在语句起始 / 表达式位置**不被关键字劈开**
（`应当` → 应+当；`除非` → 除+非），从而把这 2 条从 CCW 移除。

## 2. 场景定位（全语料 .light）

| 场景 | 文件:行 | 形态 | 分类 |
|---|---|---|---|
| 赋值（复合名） | `lightharness/examples/test_R27_词首并入正向.light:36` | `设 除非名 为 0` | 赋值 / user_definition |
| 表达式断言 | `同上:38` | `断言相等(除非名, 0, "除：除非名")` | 函数调用（实参） |
| 表达式（调用） | `lightharness/examples/test_R29_CCW精简边界.light:21` | `设 r5 为 除非(条件)` | 赋值 / 调用 |
| 条件位 | `同上:17` | `如果 应当 那么:` | 条件语句（非语句起始） |
| 注释（伪命中） | `lightharness/src/待办.light:9,75`、`认证.light:18`、`团队服务.light:346`、`examples/test_待办.light:87`、`test_文件深.light:67,69` | `# … 除非 …` / `"删除非空目录"` | 注释 / 字符串，**不产生 token** |

**结论**：`除非` 的真实代码形态全部落在**赋值 / 调用 / 实参 / 条件位**（即「非语句起始
或调用语境」），`应当` 仅出现在条件位；**语句起始非调用形态零命中**。

分类小结：**赋值 2**、**函数调用/实参 2**、**条件语句 1**、**注释/字符串 8**（伪命中）。

## 3. 机制订正（任务书的表名机制不成立）

- **应当**：任务书称「方案 B：把『应』加入 HM」。实测 `应` **不是关键字**
  （`_match_keyword` 返回 `None`），HM 由关键字推导 ⇒ 无法加入。剥开看：
  `应当` 被劈的真正原因是词中第二字 `当`（循环关键字）在连续汉字串里被识别 ——
  以 `应` 为词首的**前缀类**整体并入即可绕过。
- **除非**：任务书判断「`除`∈DUAL，`非` 是逻辑关键字，双侧夹击」——**方向正确**。
  实测 DUAL 机制（lexer.py L1254-1265）在 `除` 命中 DUAL 且后随关键字时返回
  `None,0`（把 `除` 抑制成非关键字），但**不吸收**后随的 `非` ⇒ `除非` 仍劈成 `除`+`非`。
  以 `除` 为词首的前缀类整体并入即解决。

两例均归入任务1 建立的同一张**词首前缀类** `_P0A_HEAD_MERGE_PREFIX`（见
`_task1_R30_位运算符通用化.md` §4），无需新增独立规则。

## 4. 实现

复用 `_P0A_HEAD_MERGE_PREFIX = frozenset({'位', '应', '除'})`（lexer.py L3910）
与主循环判定（lexer.py L2387-2394）。CCW 删除 2 条：`应当`、`除非`。

**位置口径**：调用语境（后随 `(`）或非语句起始位置；语句起始非调用被排除（见 §6）。

## 5. 三重判据结果

| 判据 | 口径 | 结果 |
|---|---|---|
| **G1 语料门** | 全语料对照 `_r29_baseline_tokens.json` | ✅ **848 可比文件零变化**（排除 2 个基线既有错误文件） |
| **G2 编译门** | 含应当/除非的语句 rc=0 | ✅ `examples/test_R30_应当除非通用化_快速.light`（rc=0） |
| **G3 边界门** | 6 类边界形态 + 反向形态 | ✅ 见下表 |

G3 边界形态：

| 词 | 设名 | 段落名 | 返回调用 | 条件位 | 列表元素 | 字符串态 |
|---|---|---|---|---|---|---|
| 应当 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 除非 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

反向形态（不受影响）：

| 形态 | token 流 |
|---|---|
| `当 x > 0:` | `KEYWORD 当` …（循环关键字） |
| `设 y 为 非 真` | `KEYWORD 非` / `KEYWORD 真`（逻辑关键字） |
| `设 q 为 甲 除 乙` | `KEYWORD 除`（算术关键字；DUAL 命中且后随空白，正常切出） |

## 6. 关键发现：语句起始「除」与异常子句冲突（与任务1 同源）

把 `除` 的前缀类判定提升到语句起始会**破坏** `bootstrap/release/stdlib/断言工具.light`
的异常子句（`除异常(e):` / `除类型错误:` —— `除` = except 关键字）。
故 `除非 条件:`（语句起始非调用）形态**主动排除**；该形态在语料中零命中。
详见 `_task1_R30_位运算符通用化.md` §6。

## 7. 结论

- CCW `应当` / `除非` 2 条**删除**（本任务净减 2）。
- 与任务1 共用通用规则 `_P0A_HEAD_MERGE_PREFIX`，无独立特化分支。
- 三重判据全通过；快速验证 `examples/test_R30_应当除非通用化_快速.light` **rc=0**。
