# 第55轮任务6交付：docs 回填 + 探针移档 + 长期记忆更新

> 日期：2026-09-18 ｜ 优先级 P2 ｜ 依赖任务1~5 的实测数据

---

## 一、docs 回填：对标清单 #194（全量测试基线固化）

文件：`docs/功能对标/对标清单.json`

| 字段 | 内容 |
|---|---|
| 编号 | **194** |
| 功能 | 全量测试基线固化 + 跨平台回归门（0.82 跑 light-merge 全量 / 本机跑 lightharness 全量） |
| 原模块 | 回归验证基建（此前无全量基线，靠口述「零新增红」判定） |
| 光明模块 | `scripts/082全量回归.py` + `scripts/回归基线.py` + `scripts/多平台矩阵.py` |
| 状态 | 已完成 |
| 完成轮次 | 第55轮 |
| 证据 | 6 份 `_task*_R55_*.md` + 3 个脚本 + 2 份基线 JSON |

备注里写的是实测结论（7806/117、1272/7、跨平台核对结果、slow 未纳入），不是套话。
`版本` 字段同步更新为「覆盖记录至第55轮（全量测试基线固化：0.82 light-merge + 本机 lightharness 双平台门）」。

### ⚠️ 这里踩到一个坑，值得单独记

第一版脚本直接用 `json.dumps(indent=2)` 回写，**整个文件 2744 行全被重排**（git diff 显示 +2764 / −2742），
193 条历史条目无辜受影响。

实測定为该仓库的序列化风格是：**`ensure_ascii=False` + `indent=1` + CRLF 行尾 + 无文件尾换行**（不是常见的 indent=2）。

改后的做法是「**先自证再落盘**」：

```python
before = dump(d)                      # 用待验证的格式化参数序列化未修改的内容
if before != raw:
    raise SystemExit("❌ 序列化风格与原文件不一致（会波及全文），已中止")
items.append(ITEM)                    # 只在通过自证后才改
```

改完 git diff 收敛为 **+23 / −1**（1 行是被替换的 `版本` 字段）。
这个「先自证无损往返、再写」的动作，以后凡是碰这类机器+人工混写的 JSON 都该做。

---

## 二、探针移档

`docs/历史存档/R55探针/`（新建），共 10 个文件：

| 文件 | 用途 |
|---|---|
| `_r55_probe082.py` ~ `_r55_probe082e.py` | 0.82 环境探测链：Python 连通性 → pytest 插件清单 → 收集规模（8106/7803/303）→ 本机失败项跨平台复跑 |
| `_r55_probe_crlf.py` | 验证「4 条 example 红是不是 CRLF 行尾导致的」→ **结论：否** |
| `_r55_analyze.py` / `_r55_analyze.out` | 117 条失败按文件/原因归类 |
| `_r55_add194.py` | 对标清单 #194 追加（含格式自证） |

---

## 三、长期记忆更新

已更新 `.workbuddy/memory/MEMORY.md`，新增「**全量测试的跑法与回归门（R55 固化）**」一节，要点：

1. **light-merge 全量 pytest 统一去 0.82 跑，本机 Windows 不跑**（8106 用例；本机 xdist 不稳 + 串行 1h+）；
2. **0.82 只有 pytest 9.1.1，无 xdist / 无 pytest-timeout** → 远端必须 `-o addopts=` 置空，
   用 FreeBSD 自带 `timeout(1)` 兜硬超时；
3. 本机 lightharness 全量**实测 25 分钟**（不是任务书说的 6-7 分钟）；
   `-o addopts=` 会连带清掉 `pytest.ini` 的 `--timeout=60`，必须显式补回，否则跑到 84% 整轮 hang；
4. 三件套脚本与判据（新增红 = 本轮失败集合 − 基线失败集合；首次跑记为基线）；
5. `对标清单.json` 的序列化风格（indent=1 + CRLF + 无尾换行）与「先自证再落盘」的要求。

---

## 四、收遗漏 / 待办（给下一轮）

| # | 事项 | 出处 |
|---|---|---|
| 1 | `test_distributed_eval_light.py::test_重派与心跳_杀节点后重派且无静默丢条` 是 **flaky**，两轮跑出红/绿翻转 | 任务5 §二 |
| 2 | 4 条 example 红是 **Windows 独占**（0.82 绿），已排除 CRLF，剩两个候选：py3.13 vs py3.11 / OS 路径差异，未隔离 | 任务3 §4.2 |
| 3 | light-merge 117 条红**归因未做**：需要在 0.82 上对 R53 基线再跑一次全量与本轮互比 | 任务1 §七 |
| 4 | slow 用例 303 条未纳入基线（脚本已支持 `--mode full`） | 任务1/2 |
| 5 | gate 长跑期间无实时进度输出（`capture_output=True`），建议改 `Popen` 逐行转发 | 任务5 §五 |
| 6 | 0.82 装上 `pytest-xdist` 后可用 `--parallel` 把 25 分钟压到 5 分钟量级，脚本已留开关 | 任务2 §七 |
