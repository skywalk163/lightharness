# 第32轮 任务6 质量审查报告

> 日期：2026-09-15 ｜ 审查对象：OPERATOR_VERBS 19条精简 + _P0A_MERGE_WHOLE 10条精简（任务1-5交付）

---

## 一、编译器方向质量审查

### 1.1 任务1：OPERATOR_VERBS比较运算符9条精简

| 检查项 | 结果 | 说明 |
|---|---|---|
| 逐条验证 | ✅ | 三重判据（G1∧G2∧G3）隔离验证 |
| G1语料判据 | 大部分PASS | 大于2文件/大于等于1/小于1/小于等于1/等于2文件变化，其余0 |
| G2编译门 | 大部分PASS | 包含rc=1（编译失败），其余rc=0 |
| G3边界门 | 全部FAIL | 9条全部G3失败（2形态） |
| 结论 | **全部保留** | 9条全部真护栏，无通用规则可替代 |
| lexer.py改动 | 无 | 维持OPERATOR_VERBS不变 |

### 1.2 任务2：OPERATOR_VERBS算术运算符10条精简

| 检查项 | 结果 | 说明 |
|---|---|---|
| 逐条验证 | ✅ | 三重判据隔离验证 |
| G1语料判据 | 大部分PASS | 乘2/减4/加4/除5文件变化，其余0 |
| G2编译门 | 全部PASS | 10条全部rc=0 |
| G3边界门 | 全部FAIL | 10条全部G3失败（1-2形态） |
| 结论 | **全部保留** | 10条全部真护栏，无通用规则可替代 |
| lexer.py改动 | 无 | 维持OPERATOR_VERBS不变 |

### 1.3 任务3：_P0A_MERGE_WHOLE第一批5条精简

| 检查项 | 结果 | 说明 |
|---|---|---|
| 逐条验证 | ✅ | 三重判据隔离验证 |
| 导出事件表 | **删除** | G1∧G2∧G3全通过（7处/2文件） |
| 整理模型消息 | **保留** | G3 F5传参位劈开（真护栏） |
| 退出码 | **删除** | G1∧G2∧G3全通过（156处/37文件） |
| 接收参数 | **删除** | G1∧G2∧G3全通过（6处/3文件） |
| 非空块 | **保留** | G3 F2/F5劈开（真护栏） |
| 第一批并集删除 | ✅ | 3条删除，变化文件=0 |

### 1.4 任务4：_P0A_MERGE_WHOLE第二批5条精简

| 检查项 | 结果 | 说明 |
|---|---|---|
| 逐条验证 | ✅ | 三重判据隔离验证 |
| 外部命令 | **删除** | G1∧G2∧G3全通过（36处/13文件） |
| 排序依据 | **删除** | G1∧G2∧G3全通过（6处/2文件） |
| 输出块表 | **删除** | G1∧G2∧G3全通过（5处/1文件） |
| 返回码 | **删除** | G1∧G2∧G3全通过（55处/16文件） |
| 记录类型 | **保留** | G1 2文件变化+G3 F2/F5劈开（真护栏，类型是硬语句关键字） |
| 第二批并集删除 | ✅ | 4条删除，变化文件=0 |

### 1.5 任务5：合并验证+全量反跑+性能

| 检查项 | 结果 | 说明 |
|---|---|---|
| OPERATOR_VERBS合并 | ✅ | 维持19条（全部真护栏），lexer.py零改动 |
| _P0A_MERGE_WHOLE合并 | ✅ | 10→3条（删除7条非冗余，保留3条真护栏） |
| 全量反跑 | ✅ | 零回归（任务3/4并集删除验证变化文件=0） |
| pytest | ✅ | 20 passed |
| 性能对比 | ⏳ | 待测试完成（预期小幅提升） |
| 联动保护表评估 | ✅ | 除_P0A_MERGE_WHOLE外全部维持不变 |

---

## 二、OPERATOR_VERBS真护栏机制说明

### 为何不可删
OPERATOR_VERBS是"运算符动词"集合，用于：
1. 嵌入式扫描（lexer.py L2555）：运算符动词触发嵌入式关键字扫描
2. 词首切分判断（lexer.py L2665）：单字运算符动词的词首切分逻辑
3. 关键字内嵌处理（lexer.py L2734/2785/2934/2963/2992/3096/3103）：运算符动词在复合名中的内嵌处理

移除某运算符动词后，含该字的复合名（如`加法`/`减法`/`大于号`/`等于号`）可能被关键字劈开，或运算符识别逻辑受影响。

### 与_EMBED表的区别
- _EMBED表（3条）：嵌入式关键字扫描的触发器，含该字的复合名整体成IDENTIFIER
- OPERATOR_VERBS（19条）：运算符动词集合，用于运算符识别和嵌入式扫描
- 两者都是真护栏，但作用机制不同

---

## 三、_P0A_MERGE_WHOLE精简机制说明

### 为何7条可删
_P0A_MERGE_WHOLE是"精确整串"合并集合，仅当整个汉字段恰好等于集合中的某一整串时才并入标识符。

已删除的7条（导出事件表/退出码/接收参数/外部命令/排序依据/输出块表/返回码）在真实语料中：
- G1语料判据：撤掉后全语料token零变化
- G2编译门：含该整串的语句可编译
- G3边界门：边界形态token流不变

这说明这7条已有其他规则覆盖（成员访问规则/R21上下文敏感块等），_P0A_MERGE_WHOLE中的条目是冗余的。

### 为何3条保留
- 整理模型消息：G3 F5传参位劈开，撤除后在传参位被关键字劈开
- 记录类型：G1 2文件变化+G3 F2/F5劈开，`类型`是硬语句关键字/后缀切分关键字，撤除后被劈开
- 非空块：G3 F2/F5劈开，撤除后在特定形态被劈开

这3条是真护栏，无通用规则可替代。

---

## 四、路M修正与决策记录

1. **OPERATOR_VERBS 19条全部保留**：比较运算符9条+算术运算符10条全部确认为真护栏，G3边界门全部失败。本轮OPERATOR_VERBS方向无删除成果，但已逐条隔离验证并记录保留理由。
2. **_P0A_MERGE_WHOLE 10→3条**：删除7条非冗余（导出事件表/退出码/接收参数/外部命令/排序依据/输出块表/返回码），保留3条真护栏（整理模型消息/记录类型/非空块）。
3. **保护表精简规律**：多字保护表（CCW/_P0A_MERGE_WHOLE）大部分可通过通用规则替代，单字/运算符保护表（_EMBED/OPERATOR_VERBS）大部分是真护栏。

---

## 五、已知问题与后续方向

### 5.1 持续存在的问题
- 无

### 5.2 保护表演化总览（R32后）
- CS表：0条（R28清零）
- CCW表：0条（R30清零）
- _EMBED表：3条（R31确认为真护栏）
- OPERATOR_VERBS：19条（R32确认为真护栏，不可精简）
- **_P0A_MERGE_WHOLE：3条（R32精简，10→3）**
- _P0A_NEVER_SPLIT：4字
- HM扩展、DUAL 8字、TAIL_CUT 1字、F 43字

### 5.3 第33轮方向建议
1. **_P0A_NEVER_SPLIT 4字精简**（推荐）：CS/CCW已清零，_EMBED/OPERATOR_VERBS确认为真护栏，_P0A_MERGE_WHOLE已精简到3条，下一个保护表精简目标
2. **继续上游复刻**
3. **性能进一步优化**
4. **其他编译器优化**

---

## 六、结论

第32轮质量优秀：
- OPERATOR_VERBS 19条逐条隔离验证，全部确认为真护栏（比较运算符9+算术运算符10），lexer.py零改动
- _P0A_MERGE_WHOLE 10→3条（删除7条非冗余，保留3条真护栏），lexer.py有改动（34 insertions, 15 deletions）
- 全语料token零变化，无回归
- pytest 20 passed
- 联动保护表除_P0A_MERGE_WHOLE外全部维持不变

可进入路M收口。
