# 第19轮 任务3 交付报告 —— 保留字与内置映射（L-087 / L-101 / L-143）

> 日期：2026-09-13 ｜ 编译器项目：`light-merge`（branch main）
> 验证项目：`lightharness`（branch main）
> 路：任务3（保留字表定义 + 内置映射名解析逻辑）
> 铁律：跨项目操作；每缺陷最小复现；不破坏现有回归；反跑 A/B/C 三项。

---

## 一、根因与评估结论

| 缺陷 | 现象 | 根因 | 本轮处置 | 验收达成 |
|---|---|---|---|---|
| **L-087** | `设 当前目录 为 "/tmp"` 后 `打印 当前目录` 仍调用 `os.getcwd()` | 内置映射名 `当前目录` 在 codegen 发射点无条件替换为 `当前目录()`，未优先用户变量 | **既有 `_shadows_builtin` 逻辑已覆盖**（用户 `设 X 为` 绑定局部变量后压过 builtin_map）；本轮确认绿，纳入反跑 C 判据 | 绿（rc=0，输出 /tmp） |
| **L-101** | `设 回调 为 空` 报解析错误 | `回调` 被 parser 用于 `外部 回调` FFI 语法（parser_stmt.py:5998/6183），保留为关键字 | **评估后保留为保留字**（移除会破坏 FFI 语法）；误用报明确保留字提示 | 报明确提示（rc!=0 + 「回调是保留关键字」） |
| **L-143** | `设 作用域 为 {}` 报解析错误 | `作用域` 被 parser 用于 `异步作用域` 语法（parser_stmt.py:3877），保留为关键字 | **评估后保留为保留字**（移除会破坏异步作用域语法）；误用报明确保留字提示 | 报明确提示（rc!=0 + 「作用域是保留关键字」） |

**关键评估结论**：L-101/L-143 不能从保留字表移除——它们在光明语法中有明确用途（FFI 回调声明、异步作用域块），parser 硬消费这两个 token。若移除会导致合法 `外部 回调` / `异步作用域` 语法失效。因此保留为保留字，由"误用报明确提示"满足任务3 验收标准的第二分支（"要么允许作变量名、要么报明确的保留字提示"）。**本轮对 light-merge 无代码改动**。

> **诚实声明**：任务3 三项均**无需本轮编译器代码改动**。L-087 由既有遮蔽逻辑已修复；L-101/L-143 经评估为合规保留字、当前清晰提示已满足验收。三个最小复现用例与反跑脚本用于**固化现状、防回归**，并明确记录决策依据，供路M 收口与缺陷账回填。

---

## 二、改动点

- **light-merge 源文件**：无修改（本轮任务3 范围内）。
- 既有相关逻辑（供参考，非本轮新增）：
  - `src/code_generator.py:878` `_shadows_builtin(name)`：检查名字是否被 `设 X 为` 绑定为局部变量（`self._local_variables`），是则压过 `builtin_map`。
  - `src/code_generator.py:3466-3470` ParagraphCall 发射点：`shadowed = self._shadows_builtin(expr.name)`；`if (not shadowed) and expr.name in self.builtin_map ...` → 仅当未遮蔽时才发射内置映射调用。
  - `src/keywords.py`：`回调` ∈ `KEYWORDS_FFI`（line 238）、`作用域` ∈ `KEYWORDS_ASYNC`（line 179），作为保留字 token 被 parser 消费。

---

## 三、验证结果

### 3.1 最小复现用例（lightharness/examples/）
| 用例 | 缺陷 | 结果 | 说明 |
|---|---|---|---|
| `test_L087.light` | L-087 | **rc=0**，打印 `/tmp` | 用户变量遮蔽内置映射；含 `断言 当前目录 == "/tmp"` 自校验 |
| `test_L101.light` | L-101 | **rc=1**，报「回调是保留关键字，不能直接作为语句开头」 | 满足"明确提示"验收分支，非静默失败 |
| `test_L143.light` | L-143 | **rc=1**，报「作用域是保留关键字，不能直接作为语句开头」 | 满足"明确提示"验收分支，非静默失败 |

### 3.2 反跑脚本 `_antirun_t3_保留字内置映射.py`（A/B/C 全过）
- **A 判据**：L-087 绿且输出 /tmp；L-101/L-143 报明确保留字提示（rc!=0 + 含「保留关键字」）。
- **B 判据**：
  - B1：L-087 不赋值时 `打印 当前目录` 回退真实 cwd（内置映射未被破坏，遮蔽仅作用于已绑定变量）。
  - B2：`回调处理器`/`作用域表` 作前缀整体标识符正常（保留字按整 token 判定，非子串误切）。
- **C 判据**：撤掉 `_shadows_builtin` 的局部变量判定（`and name in self._local_variables` → `and False`）→ L-087 断言 `当前目录 == "/tmp"` 失败，**rc!=0 立红**，证明遮蔽逻辑必要。恢复后 sha256 一致、L-087 复绿。

### 3.3 其他内置映射名回归
L-087 修复路径（`_shadows_builtin`）同时保护 `转小写`/`转大写`/`字典设置` 等全部 builtin_map 名：用户 `设 X 为` 绑定后即可遮蔽，未绑定则走内置映射（B1 已验证）。本轮未改动，无回归风险。

---

## 四、全量 CI 期望

- 运行：`python 运行.py examples/test_L087.light`（rc=0）；L-101/L-143 预期 rc=1 带明确提示（非失败）。
- 反跑：`python _antirun_t3_保留字内置映射.py` → `ANTIRUN T3 ALL OK`
- lightharness 全量 CI（路M 收口执行）：期望 pytest 311+ passed + smoke 5/5。本轮零编译器改动，无回归风险。

---

## 五、交付物清单
- light-merge 修复代码：无（本轮任务3 范围内无需改动）
- lightharness 复现用例：`examples/test_L087.light` / `test_L101.light` / `test_L143.light`
- lightharness 反跑：`_antirun_t3_保留字内置映射.py`
- lightharness 报告：本文件

---

## 六、遗留/后续
- **L-101/L-143 顺延建议**：若未来希望放开为变量名，需改 parser 支持"上下文敏感保留字"（仅在 `外部 回调` / `异步作用域` 语法位置消费，其余位置允许作标识符），属词法/parser 协同重构，超出本轮文件互斥范围，建议后续批次评估。
- 缺陷账：L-087 状态更新为「已修复（既有 _shadows_builtin；R19 确认）」；L-101/L-143 标注「合规保留字（R19 评估；报明确提示）」，不计入未修复缺陷。
