# 任务5 交付报告 —— code-runtime-python 协议层（第11轮遗留）

- **轮次**：第12轮 任务5（复刻续批）
- **日期**：2026-09-12
- **仓库**：`G:\dswork\duan-light-merge\lightharness`（验证层）；编译器 `light-merge`
- **文件互斥**：仅新建 `src/代码运行时协议.light` + `examples/test_代码运行时协议.light` + 本报告 + 反跑脚本；未触碰任何其他 `src`/`examples`/`docs`/`stdlib`/`scripts`（遵守铁律 1、7）。

---

## 1. 上游依据（只读参考，本地无 checkout，已从 pinned commit `a305303422` 的 `packages/experimental/code-runtime-python/src/protocol.ts` 取得完整源码）

| 上游符号 | 语义 | 光明落地 |
|---|---|---|
| `PROTOCOL_FD = 3` | fd 3 上的 JSON-lines 帧通道；stdout/stderr 留给程序自身 | `协议文件描述符()` 返回 3 |
| `BootMessage/RunMessage` | 宿主→子进程（boot/run） | `造启动消息` / `造运行消息` |
| `BootAckMessage/CallMessage/LogMessage/DoneMessage` | 子进程→宿主 | `造启动确认消息` / `造调用消息` / `造日志消息` / `造完成消息` |
| `ReplyMessage = ReplyOk｜ReplyErr` | 宿主→子进程回复 | `造回复成功` / `造回复失败` |
| `WIRE_FRAME_FIELDS` | 每帧必填/可选字段名册 | `线框字段名册()`（必填/可选分离、字段名按字母序，与上游投影一致） |
| `validateChildFrame` | 入站 fd-3 帧重建/净化（伪造字段不随行、非数值 id/缺 args/非无损 args 丢弃） | `校验子帧()` |
| `logTruncationMarker(maxBytes)` | 日志截断标记文本 | `日志截断标记()` |
| `encodeJsonPlain` / `scalarJson` | 无损紧凑 JSON 编码（超安全范围整数取精确位） | `编码JSON纯量` / `标量JSON` |
| `checkDoneValue` | Done 值计量：超预算/非无损（非有限/负零）拒绝 | `校验完成值()` |
| `hasNonLosslessNumber` | 任一内嵌数非有限或负零 → 真 | `含非无损数()` |
| `hasUnsafeIntegerToken` | 原始 JSON 行里、字符串外、超安全范围整数令牌 → 真 | `含不安全整数令牌()` |

---

## 2. 实现要点

**新增模块 `src/代码运行时协议.light`**（纯协议逻辑，无宿主导入），与 `src/代码运行时.light`（只读，子进程执行）词汇对齐：前者是线缆帧语义，后者是执行引擎。

导出的段落（均经 `test_代码运行时协议.light` 覆盖）：
- 常量：`协议文件描述符`
- 消息构造：`造启动消息` `造运行消息` `造启动确认消息` `造调用消息` `造日志消息` `造完成消息` `造错误字段` `造回复成功` `造回复失败`
- 字段名册与校验：`线框字段名册` `校验帧必填`
- 方向判别：`判别方向`（子到宿主 / 回复 / 宿主到子）
- 无损编码与计量：`编码JSON纯量` `标量JSON` `字节长度` `校验完成值` `含非无损数`
- 入站帧净化：`校验子帧`
- 令牌扫描：`跳过字符串字面量` `是纯整数` `是不安全整数` `含不安全整数令牌`

**关键实现细节（与上游逐一对齐）**：
- `校验子帧` 重建而非透传：伪造 `truncated`/`open` 仅当字面 `真` 才计入（避免程序打印同名串或伪造值静默关闭捕获）；`call` 要求 id 为有限数值且非负零、global/name 为字符串、args 必填且不含非无损数；`done` 的 error.kind 须在 `{exception, invalid-output, output-limit}` 内，并保留伪造同时携带的 value/error 两份字段（消费方须先判 error）。
- `校验完成值` 单次遍历同时算字节长与无损性，超预算优先于非无损（二者皆违规时判 `over-budget`，与上游一致）；UTF-8 字节长用 `字节长度`（早停，对齐 `jsonStringBytesUpTo`）。
- `编码JSON纯量`/`标量JSON`：超安全范围整数经 `转整数` 取精确位（光明后端直连 Python，double 精确位本就无损，与原版 BigInt 路径等价）；键序按插入序，保证与 `解析JSON` 往返一致。

---

## 3. 测试与 CI

**新增 `examples/test_代码运行时协议.light`**（9 段、共 **30+ 断言**，远超 ≥12 要求）：
1. 常量与构造（PROTOCOL_FD=3、各消息构造往返）
2. 帧编码往返 / 大整数精确位（1152921504606846976）/ 负零编码 `-0.0`
3. 非无损检测（负零 / Infinity / 正常数 / 嵌套）
4. 完成值计量（正常 ok、超预算 over-budget、Infinity non-lossless、负零 non-lossless）
5. 方向判别（call/log/done→子到宿主、reply→回复、boot/run→宿主到子）
6. 字段名册必填（完整过、缺 id 拒）
7. 截断标记文本
8. 子帧校验（合法重建丢伪造字段、缺 args 丢、非数值 id 丢、负零 id 丢、非无损 args 丢、伪造 truncated=1 不计入、非法 error kind 丢、done 正常保留）
9. 不安全整数令牌（超范围整数、20 位、安全小整数不计、字符串内数字不计、分数/指数不计）

**结果**：
- `test_代码运行时协议` → **PASS**（rc=0）
- 相关回归 `test_代码运行时` / `test_代码运行时深化` → **均 PASS**（rc=0，本次改动为纯新增模块，零回归面）

---

## 4. 反跑判据（`_antirun_protocol.py`，字节级备份/恢复 `src/代码运行时协议.light`）

| 项 | 变异 | 期望 | 实际 |
|---|---|---|---|
| A | 字段名册必填校验放宽（`返回 假`→`返回 真`） | 缺字段不再被拒 → 红 | ✅ rc=1 |
| B | 无损检测失效（`设 非无损 为 真`→`为 假`） | 含非无损值不再被拒 → 红 | ✅ rc=1 |
| C | 截断标记改词（`[dsh-code-runtime-python]…`→`[dsh]…`） | 标记字符串不符 → 红 | ✅ rc=1 |

还原后复跑 rc=0，且源文件与备份**逐字节一致**（还原校验 OK）。**反跑成立 3/3。**

---

## 5. 未移植项

无。上游 `protocol.ts` 为纯协议逻辑、零宿主导入，本次**完整移植**；无宿主传输/进程装配依赖被引入。

---

## 6. 移交清单（建议 路M 统一处理）

1. **对标清单**：新增对标卡 **#84** `experimental/code-runtime-python 协议` → `src/代码运行时协议.light`（状态：已复刻，纯协议层；运行时执行仍由 `src/代码运行时.light` 负责）。
2. **语言缺陷账（建议登记）**：实现中确认一处光明限制 —— **`e` 指数浮点字面量不被支持**：源码中 `1e400` 被解析为标识符 `inf`（`NameError: name 'inf' is not defined`）。最小复现 `设 x 为 1e400`。本轮以 `解析JSON("1e400")`（JSON 数字指数路径正常）绕开，未阻塞。建议以 **L-084** 登记（因文件互斥表把 `docs/` 标为「路M 统一」，本路未直接改 ledger）。
3. 全量 CI 由 路M 统一跑（预期维持全绿，本路为纯新增）。

---

## 7. 光明约束（实现中踩到、已绕开，供后续任务参考）

- **L-043**：`如果 ... 且/或 ... :` 禁止行内布尔链，一律拆成顺序 `如果`（本模块所有条件均遵守）。
- **e 指数浮点字面量**：`1e400`/`1e308` 等被解析为 `inf` 标识符；需无穷大/NaN 时经 `解析JSON("1e400")` 取得 `inf`。
- **零除**：光明 `/` 对 `0`/`0.0`/`-0.0` 均抛 `ZeroDivisionError`（不同于 Python 对 `0.0` 给 `inf`）。负零判定因此改用 `转文本(当前) == "-0.0"`（已避开除法）。
- **布尔判等陷阱**：光明 `==` 底层为 Python 语意，`0 == 假`、`-0.0 == 假`、`1 == 真` 均为真。凡 `值 == 真`/`值 == 假` 的语义判断必须先 `是布尔(值)` 守卫（本模块 `编码JSON纯量`/`标量JSON`/`校验子帧` 的 log 分支均已加守卫）。
- 新原语 `副本/深拷贝/浅拷贝/冻结`（第10轮 L-082/L-083）未在此任务直接使用（协议层无需深拷贝），但运行时可用，按需取用即可。
