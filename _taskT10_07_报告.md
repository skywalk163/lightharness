# T10-07 · 缺陷复现套件 —— 交付报告

> 仓库：`g:\dswork\duan-light-merge\lightharness`（独立 git 仓库）
> 任务书：`light-merge/任务书/任务T10_07_缺陷复现套件.md`
> 运行：`python 运行.py examples/test_L0XX.light`（默认 SRC 后端，与 运行.py 一致）
> 日期：2026-08-27

## 1. 改了哪些文件（逐个 + 一句意图）

| 文件 | 动作 | 意图 |
|---|---|---|
| `examples/test_L001.light` ~ `test_L011.light`（13 个，含 007b/007c/011） | 新建 | 每缺陷一个用例：三段注释头 + 复现段 + 「修复后断言」段 + `主()`；当前 8 红 4 绿（缓解）1 工具（工程层）。 |
| `examples/_helper_L004.light` | 新建（辅助） | L-004 复现载体：定义含关键字开头成员名 `导出事件表` 的类（调用方模块不认识该成员名才会被拆）。 |
| `examples/_helper_L007b.light` | 新建（辅助） | L-007b 复现载体：定义 `头信息/记录事件/轮次计数` 危险成员名的类（跨模块形态）。 |
| `examples/_import_test/`（`__init__.py` + `数据.light`） | 新建（辅助） | L-001 复现载体：有 `__init__.py` 的包目录 + 点号导入目标模块（证明即使包目录正确也不解析）。 |
| `examples/test_L缺陷清单.light` | 新建（可选汇总） | 逐条打印 L-* 复现状态，供 CI 按行核对。 |
| `docs/功能对标/语言缺陷账.md` | 修改 | L-001~L-011 各行补「复现套件」列（用例文件名 + 当前红/绿状态）；状态列标注「用例已固化」（8 项 `新发现（用例已固化）`、4 项 `已缓解（用例已固化转绿，T10-07 实测）`、L-009 `新发现（用例已固化）`（工具层））。 |
| `docs/功能对标/反跑判据.md` | 新建 | 写明「修复后断言」段的写法、启用方式与判红/判绿规则（见 §5）。 |
| `_taskT10_07_输出/*.out.txt` | 新建 | 全部用例定向运行输出落盘，证明「当前红/绿」。 |

> 提交边界：本提交只含上述交付物。工作区中 T10-06 遗留的未提交改动（`src/子进程.light`、`src/总入口.light`、`src/沙箱.light`、`stdlib/外部命令.py`、`docs/功能对标/对标清单.json` 等）**不在本提交范围内**；`语言缺陷账.md` 因同一文件无法按行拆分，提交时连同 T10-06 遗留的 L-017 登记行一并入库（见 §7）。

## 2. 复现矩阵（13 用例当前状态）

| 编号 | 用例文件 | 当前 | 复现现象 / 说明 |
|---|---|---|---|
| L-001 | `test_L001.light` | **红** | `No module named '_import_test.数据'`（有 `__init__.py` 也失败） |
| L-002 | `test_L002.light` | **红** | 解析错误：多行字面量「意外的标记: 换行」 |
| L-003 | `test_L003.light` | 绿（缓解） | 成员空列表赋值在当前预扫描下已稳定编译为赋值，无法复现 |
| L-004 | `test_L004.light` | **红** | `'会话对象' object has no attribute '导出'`（跨模块调用关键字开头方法名被拆） |
| L-005 | `test_L005.light` | **红** | `'dict' object is not callable`（`对象.表[键]` 被编译成 `对象.表([键])`） |
| L-006 | `test_L006.light` | **红** | `UnboundLocalError: cannot access local variable '计数'`（闭包改外层标量） |
| L-007 | `test_L007.light` | 绿（缓解） | 成员名拆分状态性在当前预扫描下未复现（属性/赋值目标已入白名单） |
| L-007b | `test_L007b.light` | 绿（缓解） | 跨模块局部变量污染在当前词法器下未复现 |
| L-007c | `test_L007c.light` | 绿（缓解） | 属性名与参数名前缀重叠在当前预扫描下未复现 |
| L-008 | `test_L008.light` | **红** | `name '序列化' is not defined`（JSON 序列化非全局内置） |
| L-009 | `test_L009.light` | 工具 | 直接运行绿；`-replace '导入 builtins'` 后注释被劈开 → 伪词法错误（见 §4.3） |
| L-010 | `test_L010.light` | **红** | `'命令结果' object has no attribute '返回'`（`返回码` 被拆成 `返回`+`码`） |
| L-011 | `test_L011.light` | **红** | `module '外部命令' has no attribute '等待'`（`等待进程` 被关键字根 `等待` 拆分） |

> 红 = `python 运行.py examples/test_L0XX.light` 退出码 1 且报缺陷账一致错误；绿 = 退出码 0 且输出正确结果。
> L-003/L-007/L-007b/L-007c 的「已缓解」结论基于多组形态实测：单文件、跨模块、含/不含 `属性` 声明、危险局部变量广扫，均不再复现（当前 `lexer._scan_user_definitions` 预扫描把类属性/成员赋值目标/局部变量名统一收进白名单，拆分决策已基本去状态化）。

## 3. 用例文件结构约定

每个用例统一结构（以 `test_L004.light` 为模板）：

```
# 缺陷：L-004 <一句话现象>
# 当前：<预期红/报错现象>
# 修复后：<应绿的行为断言>
（复现段：触发缺陷的最小源码，当前运行即红）

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  断言等于(…, …, "L-004 …")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  <复现段>
  打印 "--- 测试L004 结束 ---"
  # 修复后启用：断言修复后()

主()
```

- **三段注释头**：`# 缺陷：` / `# 当前：` / `# 修复后：` 各一行，机器可读。
- **「修复后断言」段**：统一命名 `断言修复后`，`主()` 里只保留注释掉的调用 `# 修复后启用：断言修复后()`（当前刻意不调用）。
- **断言原语**：复用仓内既有惯例（`test_事件.light` 等）的 `段落 断言等于 接收 实际, 期望, 标签:`，失败即 `抛出`。

## 4. 逐缺陷复现说明

### 4.1 已复现（8 个，当前红）

| 缺陷 | 复现机制 | 运行输出（落盘） |
|---|---|---|
| L-001 | `从 _import_test.数据 导入 造值`：`_resolve_local_imports` 只认同目录模块，点号导入不内联，运行期 `from _import_test.数据 import 造值` → ModuleNotFoundError（`_import_test/__init__.py` 存在也不解析） | `_taskT10_07_输出/test_L001.out.txt` |
| L-002 | 字典字面量 `[` 后逗号 + 换行 → 语法错误「意外的标记: 换行」 | `_taskT10_07_输出/test_L002.out.txt` |
| L-004 | 调用方模块预扫描不认识 `导出事件表`，词法按关键字边界拆成 `导出`+`事件表` → `对象.导出(事件表(...))` → AttributeError | `_taskT10_07_输出/test_L004.out.txt` |
| L-005 | `对象.监听器表["点击"]` 被编译成 `对象.监听器表(["点击"])` → `'dict' object is not callable` | `_taskT10_07_输出/test_L005.out.txt` |
| L-006 | 内层 `段落 处理器` 内 `计数 = 计数 + 1` → 生成 Python 后 `计数` 变内层局部 → `UnboundLocalError` | `_taskT10_07_输出/test_L006.out.txt` |
| L-008 | 模块级直接调用 `序列化(...)` → `name '序列化' is not defined`（须 `从 JSON 导入 序列化JSON`） | `_taskT10_07_输出/test_L008.out.txt` |
| L-010 | `结果.返回码` 被拆成 `返回`+`码` → `'命令结果' object has no attribute '返回'`（模块名 `外部命令` 形态已被词法器修复，字段形态仍红） | `_taskT10_07_输出/test_L010.out.txt` |
| L-011 | `外部命令.等待进程(空, 5)` 被拆成 `外部命令.等待(...)` → `module '外部命令' has no attribute '等待'` | `_taskT10_07_输出/test_L011.out.txt` |

### 4.2 已缓解（4 个，当前绿，缺陷账状态同步为「已缓解」）

- **L-003**：裸 `己.X 为 []`（含 `追加` 方法、`己.X.追加(...)` 调用、局部变量 `事件` 等形态）均已按赋值编译，`self.事件表=[]` 正常；`X 保持 None` 的缺陷形态未复现。
- **L-007**：`设 己.部分表 为 {}`、`设 己.原因值 为 空`、方法内 temp 名与成员名不同的组合均已按 `self.部分表={}` 编译；拆分状态性未复现。
- **L-007b**：调用方模块局部变量 `头/事件/轮次/部分/记录` 广扫下，`对象.头信息`、`对象.记录事件`、`对象.轮次计数` 均整体成词；跨模块污染未复现。
- **L-007c**：`属性 轮次计数` + 方法参数 `轮次` 共存时，`设 己.轮次计数 为 0` 已按 `self.轮次计数=0` 编译；前缀重叠拆分未复现。

> 说明：这 4 项按缺陷账描述的多形态最小复现逐一实测均转绿，判断为当前 `lexer.py`（含 `_scan_user_definitions` 预扫描、`IDENTIFIER_SAFE_KEYWORDS`、`_after_dot` 成员合并、L-010 修复的复合词剩余段判定）已大幅缓解 L-004 家族。用例文件仍保留完整复现段与「修复后断言」段，作为回归哨兵；若未来编译器回归（拆分逻辑退化），启用 `断言修复后()` 即可立红。

### 4.3 L-009（工程层，需脚本演示）

语言运行时层面注释本就被词法忽略（直接运行 `test_L009.light` 为绿）；缺陷在**文本级批量替换**层：
对文件执行 `-replace '导入 builtins', "导入 builtins`n从 JSON 导入 序列化JSON"` 后，注释行被拦腰劈开、注释尾巴变成代码行，替换产物报「词法错误: 字符串未闭合」（伪错误，误导排查）。

- 原始文件：`examples/test_L009.light`（绿）
- 替换产物：`_taskT10_07_输出/test_L009_替换后.light`（红，见该文件输出 `_taskT10_07_输出/test_L009_替换后.out.txt` 或重跑复现）

## 5. 反跑判据（已写入 `docs/功能对标/反跑判据.md`）

- **判红**：`python 运行.py examples/test_L0XX.light` 退出码 ≠ 0 且错误信息与文件头「# 当前：」一致 → 缺陷在复现状态（8 项当前满足）。
- **判绿（修复后）**：语言团队修好缺陷后，把 `主()` 里 `# 修复后启用：断言修复后()` 的注释去掉，`断言修复后()` 内断言全部通过且退出码 = 0 → 修复成立。
- **断言段写法**：见 §3 模板；断言失败会 `抛出 "断言失败: …"`，运行器将错误分类为运行期异常，退出码非 0，CI 可据此判定。
- **缓解项**：L-003/L-007/L-007b/L-007c 当前已绿，启用断言后应保持绿（回归哨兵）；L-009 需按 §4.3 的替换脚本演示红，直接运行判绿。

## 6. 验证方式与结果

1. 全部 13 用例 + 汇总用例均已用 `python 运行.py` 实际运行，输出落盘 `_taskT10_07_输出/`。
2. 8 个红用例退出码均为 1，错误正文与缺陷账「现象」逐条核对一致（见 §4.1 表）。
3. 5 个绿用例（L-003/L-007/L-007b/L-007c/L-009）退出码 0 且输出符合预期（L-003:1、L-007:1、L-007b:首首0、L-007c:2、L-009:演示文本）。
4. L-009 工程层复现：`-replace` 脚本作用于副本后，替换产物无法编译（词法错误），已落盘留证。
5. 复用 `python 运行.py` 默认 SRC 后端——与任务书「用 运行.py 可独立运行」的判定通道一致，未引入其他运行器。

## 7. 已知缺口 / 边界

1. **L-003/L-007/L-007b/L-007c 无法复现**（已缓解转绿）：用例仍保留，作为回归哨兵；缺陷账状态已标「已缓解」。
2. **L-009 为工程层缺陷**：用例直接运行判绿，需脚本演示红（§4.3），非语言运行时缺陷。
3. **L-001 需包目录辅助文件**：`examples/_import_test/` 含一个空的 `__init__.py`（包标记，非用例逻辑），其余用例文件均为纯 `.light`。
4. **L-004/L-007b 需跨模块辅助**：`_helper_L004.light`、`_helper_L007b.light` 为复现载体；用例间互不依赖（仅各自依赖自己的辅助模块与 stdlib）。
5. **提交边界**：本提交不含 T10-06 遗留未提交改动（见 §1 边界说明）；`语言缺陷账.md` 提交时含 T10-06 遗留的 L-017 登记行（同一文件）。
6. L-010 的「模块名 `外部命令` 形态」已被词法器修复（`git log c0c1962c` 曾登记修复），本套件按缺陷账聚焦仍红的「字段 `返回码` 形态」。
