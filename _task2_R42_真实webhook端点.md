# 任务2（R42·P0）交付报告 —— 真实webhook端点

> 日期：2026-09-16 ｜ 负责人：WorkBuddy ｜ 状态：**完成（本机 Windows rc=0）**
> 目标：把 `网络钩子GitHub.light`（十一段门禁链 + HMAC 验签）挂到 R41 web服务器，形成**真实 webhook 端点**：`/webhook/github` → 真实 POST 流入 → 门禁 + 验签 → 派发。**零修改**纯逻辑门禁层。

---

## 一、根因 / 本轮回溯（最重要的修正点）

### 1.1 「秘密未配置 → 503」闸一度失效，误判成 401
初版验收脚本对「秘密未配置」用例断言 503，实测**得 401**。
- **根因**：门禁链以 `秘密值 == 空` 判定「未配置」（→ 503）。web 服务器默认从环境变量取秘密，未配置时得到的是**空串 `""`**；而光明里**空串 ≠ 空**（`空` 等价 null/undefined）。于是 `秘密值 = ""` 不触发 503，直接落到签名校验（用空密钥算 HMAC）→ 与请求签名不符 → 401。
- **修正**：`钩子取秘密` 把空串归一为 `空`——`如果 值 == "" 或 值 == 空: 返回 空`。未配置 → 门禁判 `== 空` → 503。
> 这是**接线层的语义缝隙**（空串/空 在光明不是一回事），非门禁链缺陷。已在 `钩子取秘密` 注释固化。

### 1.2 两处形状转换（HTTP 请求字典 → 门禁链入参）
`HTTP服务端` 交来的请求字典是 `{"方法","路径","头":{原名:单值},"体":bytes}`；门禁链 `处理请求` 要的是 `{"方法","内容类型","内容长度","块表","头表"}`，其中：
- **头表**：`必需头()` 按**小写名**查 `x-hub-signature-256` / `x-github-delivery` / `x-github-event`，且要求值是**长度恰 1 的列表**；而 `HTTP服务端.解析请求` 保留头名**原始大小写**、单值。→ `造钩子头表` 统一小写键 + 包成单元素列表。
- **块表**：`读有界UTF8体` 吃 `[{字节:int, 文本:str}]`（字节数显式给出，避免光明串按码点计长偏差）。→ 由 `体字节` 构造单块：`{"字节": 长(体字节), "文本": 字符串(体字节,"utf-8")}`。

### 1.3 HMAC 近似边界已消解（澄清本轮「关键路径风险#2」）
任务书按 `网络钩子GitHub.light` 的**旧注释**（`stdlib 哈希.HMAC_SHA256 为 pbkdf2 近似`）预判"验签对拍需宿主面标准 hmac 参照"。**实测该注释已过时**：
- `stdlib/哈希.py` 的 `HMAC_SHA256` 早在 **L-095 已修复**为 `hmac.new(key, msg, hashlib.sha256)`——标准 RFC 2104 实现（注释原文：输出与 RFC 4231 / `openssl dgst -sha256 -hmac` 完全一致）。
- **实测对拍**：light `HMAC_SHA256("secret","hello")` = `88aab3ede8d3adf94d26ab90d3bafd4a2083070c3bcce9c014ee04a443847c0b`，与 Python `hmac.new(b"secret",b"hello",sha256)` **逐字节一致**。
- **互操作断言**：验收用例 A7 用一个**外部算出**的标准签名（`sha256=d42142b5…`，由 Python 对 `{"action":"opened"}` 计算）POST 进来 → 端点 **202 通过**。即端点接受标准 HMAC-SHA256 签名，**与真实 GitHub `X-Hub-Signature-256` 标准一致、可互操作**。
> 结论：验签面**不存在近似边界**；`网络钩子GitHub.light` 的注释过时。按铁律「零修改 网络钩子GitHub.light」，**本轮未改其注释**，登记为 task6 docs 回填项。

---

## 二、做了什么（交付物 + 设计）

### 2.1 交付物（三项，全部到位）
| 文件 | 类型 | 说明 |
|---|---|---|
| `lightharness/src/web服务器.light` | 修改（最小） | 挂 `/webhook/github` 路由 + 门禁桥接 |
| `lightharness/examples/test_R42_真实webhook.light` | 新增 | 全链验收，rc=0 |
| `lightharness/_task2_R42_真实webhook端点.md` | 新增 | 本报告 |

**铁律遵守**：零修改 `网络钩子GitHub.light`；派发默认只记录不外联（不外呼 GitHub）；继承 R41 安全铁律（非 loopback 无 token 拒启动）。

### 2.2 改动点（web服务器.light，逐条）
1. `从 网络钩子GitHub 导入 处理请求`。
2. 类属性：`钩子秘密=""` / `钩子来源="github:webhook:lightharness"` / `钩子体上限=1000000` / `钩子派发记录=空`。
3. `构造` 装配：`钩子秘密` ← `环境变量("HARNESS_WEBHOOK_SECRET", 环境变量("GITHUB_WEBHOOK_SECRET",""))`；`钩子体上限` ← `转整数(环境变量("HARNESS_WEBHOOK_MAX_BODY","1000000"))`；`钩子派发记录` ← `[]`。
4. 方法 `处理webhook(请求)`：形状转换（1.2）→ `处理请求(钩子请求, 钩子响应, 造钩子能力(己))` → 状态回映射为 HTTP 响应（JSON 体 `{"状态","消息"}`）。
5. 装配接口（沿用既有 `设置会话库` 风格）：`设置钩子秘密` / `设置钩子体上限` / `清空钩子派发` / `钩子派发数`。
6. 模块级辅助：`造钩子头表` / `取头值`（大小写不敏感）/ `钩子取秘密`（空串归一 空）/ `钩子当前时间` / `钩子派发`（只追加进 `钩子派发记录`）/ `造钩子能力`。
   能力字典：`{"取秘密":钩子取秘密, "当前时间":钩子当前时间, "来源":服务.钩子来源, "派发":钩子派发, "体上限":服务.钩子体上限}`（光明支持把段落当字典值并 `能力["派发"](投递)` 调用，同 `test_网络钩子GitHub` 手法）。
7. 路由分派：在兜底 404 之前加 `如果 路径 == "/webhook/github": 返回 服务.处理webhook(请求)`——**不判方法**，让门禁链自行对非 POST 返 405；该分支位于 R41 非 loopback token 门禁**之后**，故**天然继承**该门禁。

### 2.3 状态映射（门禁链决定，逐条）
| 门禁场景 | HTTP 状态 |
|---|---|
| 验签通过 + 载荷合法 → 派发 | **202** |
| 非 POST | 405 |
| 非 `application/json` | 415 |
| 缺/多值 `X-Hub-*` 头 | 400 |
| 声明或累计 body 超限 | 413 |
| 秘密未配置 | 503 |
| 错误签名 | 401 |

### 2.4 light 内建探查（缺则登记）
本轮**未暴露新的 light 内建缺失**；socket/字符串方法/HMAC 全部可用。唯一语义缝隙（空串 ≠ 空）已在接线层堵口，非语言缺陷，登记为 docs 注意点。

---

## 三、验证结果（本机 Windows）

运行：`cd lightharness && python 运行.py examples/test_R42_真实webhook.light` → **EXIT_CODE=0**
（输出：`任务2 全部断言通过（真实webhook：好签名202派发/错签名401/非POST405/超大413/秘密空503；非loopback令牌门禁继承 rc=0）`）

| 会话 | 用例 | 断言 | 结果 |
|---|---|---|---|
| A（loopback 无令牌） | 好签名 POST | `状态==202` 且派发一次（kind/事件名/载荷/deliveryId 全对） | ✅ |
| A | 错误签名 | `状态==401` | ✅ |
| A | 非 POST（GET） | `状态==405` | ✅ |
| A | 超大 body（上限压到 200，发 259B） | `状态==413` | ✅ |
| A | 秘密未配置（置空串） | `状态==503` | ✅ |
| A | 未知路径 | `状态==404`（兜底未受影响） | ✅ |
| A | 外部标准签名（Python 计算） | `状态==202` 且派发一次（**互操作性**） | ✅ |
| B（0.0.0.0 + token） | 无 token | `状态==401`（R41 门禁先于 webhook） | ✅ |
| B | 带 token + 好签名 | `状态==202` 且派发一次 | ✅ |
| C | 非 loopback 无 token 调 `配置()` | 抛错（安全铁律拒启动） | ✅ |

**向后兼容回归**：既有 `examples/test_web路由.light` → rc=0；纯逻辑 `examples/test_网络钩子GitHub.light` → PASS（未改动亦无回流）。

---

## 四、现在 / 待办（任务5/6 衔接）

1. **互操作性（已证）**：端点验签 = 标准 RFC 2104 HMAC-SHA256，可与真实 GitHub 签名对拍。task6 回填时**顺带更正** `网络钩子GitHub.light` 顶部「pbkdf2 近似」的过时注释（本轮受「零修改」铁律约束未动）。
2. **任务5**：把真实 webhook 接收串进端到端真实工具链。
3. **0.82 复测**：本机绿；0.82（POSIX）复测归任务4/5（socket 行为已按 R41 H1/H4 教训对齐：显式 `setblocking(假)` + monotonic 截止）。
4. **缺陷账**：本轮任务2 未暴露新语言缺陷（含 HMAC 面——L-095 已修）。

---

## 五、运行方式（copy-paste）
```bash
cd G:/dswork/duan-light-merge/lightharness
G:/dswork/duan-light-merge/light-merge/.venv/Scripts/python.exe 运行.py examples/test_R42_真实webhook.light
# 期望：任务2 全部断言通过（...）  退出码 0
```
