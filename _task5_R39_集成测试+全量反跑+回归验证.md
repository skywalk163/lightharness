# R39 任务5 验证报告 — scope 作用域 + system-prompt 系统提示 复刻

> 仓库：`lightharness/`  复刻源：`deepseek-harness 0.1.5-rc.2`
> 交付时间：第 39 轮任务 5 完成时

## 1. 交付清单

| 文件 | 角色 | 行数 | 备注 |
| ---- | ---- | ---- | ---- |
| `src/作用域.light` | 核心模块 | 730+ | 建作用域键 / 父链绑定 / 循环检测 / 上下文扩展 / 命名条目表 / 匿名条目表 / 作用域层组 |
| `src/系统提示.light` | 核心模块 | — | 节/上下文/变量注册 / 组装 / 渲染 / 工具排序 / 变量插值 / 监听器瀑布流 / SECTION_ORDERS |
| `examples/test_R39_作用域.light` | 单元测试 | — | 14 组 |
| `examples/test_R39_作用域存储.light` | 单元测试 | — | 19 组 |
| `examples/test_R39_系统提示.light` | 单元测试 | — | 20 组 |
| `examples/test_R39_系统提示工具排序变量插值.light` | 单元测试 | — | 19 组 |
| `examples/test_R39_集成测试.light` | 集成测试 | 400+ | 8 大组、23 组小断言 |
| `_antirun_r39_final.py` | 反跑脚本 | — | 6 阶段：基准/变异/解析/三环/还原 |

## 2. 运行结果

### 2.1 基准全量（阶段0 + 阶段6 还原复跑）

```
[GREEN] test_R39_作用域.light                     rc=0  14 组
[GREEN] test_R39_作用域存储.light                 rc=0  19 组
[GREEN] test_R39_系统提示.light                   rc=0  20 组
[GREEN] test_R39_系统提示工具排序变量插值.light   rc=0  19 组
[GREEN] test_R39_集成测试.light                   rc=0  23 组
-------------------------------------------------------
合计 95 组小断言，全部 GREEN（基准 + 还原各跑一次）
```

### 2.2 集成测试覆盖矩阵（test_R39_集成测试.light）

| § | 覆盖主题 | 代表断言 |
| - | -------- | -------- |
| 1 | scope 核心 | 键判型、父链长度/方向、循环检测、一次性绑定、重绑句柄、重绑后仍可循环检测 |
| 2 | scope 存储层 | 命名条目表重复抛错、注销幂等、匿名条目表追加/注销幂等 |
| 3 | 作用域层组 | 全局节对所有作用域可见、域A/B 专属作用域隔离、根作用域看不到子专属 |
| 4 | system-prompt 核心 | 节重复抛错、order 排序（100/500/900）、context order、函数文本节 |
| 5 | complete + 瀑布流 | complete 节存在、多 complete 冲突抛错、监听器修改装配 |
| 6 | 工具排序 + 变量插值 | 字典序、REST 插入未列出、重复名抛错、REST 必填、`{{var}}` 插值、变量名校验、SECTION_ORDERS 常量 |
| 7 | scope + system-prompt 联动 | 域A/B 从作用域层组合并命名 → 注册到 system prompt → 变量插值 → 渲染；域隔离 + 全局继承 + 变量插值三重叠加 |
| 8 | agent-default-model 端到端 | 取当前选择 → 注册为系统提示变量 → 渲染；更新默认模型 → 撤销变量 → 重新注册 → 渲染 |

### 2.3 反跑（变异断链必须红）

| 判据 | 变异类型 | 期望 | 实际 |
| ---- | -------- | ---- | ---- |
| §7.1b 域A专属期望值改反 | 值反 | rc≠0 + "断言失败" | PASS |
| §7.1 全局身份期望值改反 | 值反 | rc≠0 + "断言失败" | PASS |
| §7.2b 域B专属期望值改反 | 值反 | rc≠0 + "断言失败" | PASS |
| §5.2 去掉第二个 complete 标记 | 功能断链 | rc≠0 + "断言失败" | PASS |
| §1.3 循环键换成新键 | 逻辑断链 | rc≠0 + "断言失败" | PASS |
| §6.5 变量插值字面量改反 | 值反 | rc≠0 + "断言失败" | PASS |

### 2.4 新模块词法/语法回归（阶段4）

```
[PARSE-OK] src/作用域.light
[PARSE-OK] src/系统提示.light
```

### 2.5 三环导入路径联通（阶段5）

临时生成 glue 程序（作用域 → 系统提示 → 代理默认模型）：

```
[PASS] rc=0  三环联通（作用域→系统提示→默认模型）
       输出：三环联通 OK
```

## 3. 关键实现对标（上游 → 光明落地）

| 上游 deepseek-harness 概念 | 光明落地 | 说明 |
| -------------------------- | -------- | ---- |
| `ScopeKey` 不透明对象 | `建作用域键()` 返回 `{"__scope_key__": id}` | 字典身份比较模拟 `object` 标识 |
| `ScopeParentBinding` 句柄 | `绑定父作用域` 返回 `{键, 父键}`；`重绑父作用域` 仅通过句柄移动 | 一次性绑定 + 句柄授权 |
| `ScopedEffect` 附着/撤销 | `作用域层组.附着(上下文, 动作)` 返回含 `"撤销"` 段落的句柄 | 支持幂等注销 |
| `NamedEntries` 插入顺序 + 重复抛错 | `类 命名条目表` + 内部有序列表 + 字典重复检测 | 与上游 Python dict 有序性对齐 |
| `AnonymousEntries` | `类 匿名条目表` 列表容器 + 句柄索引 | 支持幂等注销 |
| `SECTION_ORDERS` 常量 | 字典常量，含 `HARNESS_IDENTITY=-1000` 到 `DEPLOYMENT_PERSONA_SUFFIX=10200` 三十余节 | 节序硬编码复刻 |
| `TOOL_ORDER_REST="<unlisted-tools>"` | `工具排序未列出标记` 常量 + REST 位置插入未列出工具 | 对齐上游语义 |
| 变量插值 `/\{\{([a-z][a-z0-9_]*)\}\}/` | 同 regex + `校验变量名` + 节渲染时插值 | 严格 `{{var}}` 语法 |
| complete 节唯一性（只能 1 个） | `组装系统提示` 收集时抛错 | 多 complete → 冲突 |
| 监听器瀑布流 | `注册组装监听器` 回调列表，按注册顺序依次修改装配 | 支持修改节列表 |
| 作用域过滤 | `作用域层组.合并命名(作用域键)` 链上合并；不同作用域生成不同 prompt | §7 联动验证 |

## 4. 已知约束与技术债

- 光明语言不支持 inline lambda，§3/§5/§7 用到的动作段落在文件顶部定义为命名段落（无功能差异，仅风格）。
- `作用域键ID` 基于自增计数器；如后续引入跨进程存储，需要 UUID 替换。
- `系统提示.light` 渲染器对未插值变量保留字面量（上游也类似），不强制 fail-fast。
- 本任务未改动 Python 宿主层（`agent-default-model` 宿主桥接仍是既有实现）。

## 5. 结论

R39 任务5 交付达成：

- [x] 2 个核心 `.light` 模块复刻（作用域 + 系统提示）
- [x] 4 个单元测试文件合计 72 组断言全部 GREEN
- [x] 1 个集成测试覆盖 scope + system-prompt + agent-default-model 三环联动，8 大组 23 组小断言全部 GREEN
- [x] 6 条变异反跑全部红
- [x] 2 个新模块词法/语法回归通过
- [x] 三环导入路径联通
- [x] 反跑过程未污染 src/（还原基准复跑仍是全 GREEN）

**验证状态：ALL PASS，可交付。**
