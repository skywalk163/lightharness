# 任务1（R34）：agent-loop 多轮循环核心复刻 —— 交付报告

> 日期：2026-09-15 ｜ 轮次：第34轮 ｜ 优先级：P0
> 交付物：`src/代理循环.light`、`examples/test_代理循环.light`、本报告
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）`packages/agent-loop/src/`

---

## 1. 做了什么

新增纯逻辑模块 `代理循环.light`（491 行 / 44 段落 / 60 导出），复刻 agent-loop 四个核心面：

| 上游文件 | 复刻内容 | 本模块章节 |
|---|---|---|
| `turn.ts` | Turn 状态机（pending/running/completed/failed/aborted） | §1 |
| `step.ts` | Step 状态机（多一个 skipped）、步骤类型 llm/tool/user_input | §1 |
| `checkpoint.ts` | 检查点时机（pre-turn/pre-step/post-step/post-turn）+ 幂等恢复 | §2 |
| `abort.ts` | AbortSignal、中止原因、恢复续跑 | §3 |
| —（事件面） | 事件驱动 + 监听器注册/注销/错误隔离 + 事件投影 | §4 §5 |

### 1.1 Turn/Step 状态机（严格转换）

- **Turn**：`pending → running → {completed, failed, aborted}`；`pending → aborted`。终态无后继。
- **Step**：`pending → {running, skipped, aborted}`；`running → {completed, failed, skipped, aborted}`。
- 合法后继由 `循轮次后继 / 循步骤后继` 显式给出，**非法转换一律抛错**并带错误码
  （`ILLEGAL_TURN_TRANSITION` / `ILLEGAL_STEP_TRANSITION` / `UNKNOWN_TURN_STATE` / `UNKNOWN_STEP_TYPE`）。
- 每次转换记入 `轨迹`（状态迁移历史），便于回放与投影校验。
- 轮次含多步骤：`轮次追加步骤` / `轮次当前步骤` / `轮次是否跑完`。
- **轮次限制**：`最大轮次`（默认 3），`是否超限` 判定；超限后 `开始轮次` 返回空并以 `max_rounds` 中止。

### 1.2 检查点（Checkpoint）

- 检查点形状：`{序号, 轮次序号, 步骤序号, 策略, 快照, 待处理事件}`。
- 四种时机：`pre-turn`（开轮前）、`pre-step`、`post-step`、`post-turn`。
- **序号严格递增**（追加-only），`记检查点` 自动分配序号。
- **幂等恢复**：`从检查点恢复(驱动, 序号)` 只写 `恢复序号/恢复快照` 两个游标字段，
  **不追加任何检查点**；同一序号重复恢复返回同一快照（测试用 `序列化JSON` 比对相等）。
  不存在序号返回 `空` 而非抛错。

### 1.3 中止与恢复（Abort/Resume）

- 中止信号 `{中止, 原因}`；`请求中止` 校验原因 ∈ `user_abort/timeout/error/max_rounds`，
  **首次返回真、重复返回假**（幂等，且首次原因不被覆盖）。
- `中止循环`：标记当前 step 为 `aborted`、当前 turn 为 `aborted`，发 `abort` 事件。
- `恢复循环`：取最近检查点 → 统计并跳过已终态步骤 → 重建中止信号 → 发 `resume` 事件。
  未中止时返回 `{"决定": "无需恢复"}`，无检查点时返回 `{"决定": "无检查点"}`。

### 1.4 事件驱动与投影

- 事件类型：`turn_start / turn_end / step_start / step_end / checkpoint / abort / resume`，未知类型抛错。
- 监听器：`注册监听`（重名抛错 `DUPLICATE_LISTENER`）/ `注销监听` / `监听器数`。
- **错误隔离**：`派发监听` 逐个 `尝试/捕获`，单个监听器抛错只记入 `监听器错误表`（含名称与消息），
  不影响后续监听器与循环推进。
- 事件投影 `投影循环(事件表, 起始序号)`：重建 `{轮次数, 步骤数, 检查点数, 中止数, 恢复数, 轮次状态, 步骤状态}`；
  `校验投影` 校验不变量；`增量投影` 只处理序号 > 起始的新事件（不重复计入）。

---

## 2. 验证结果

| 项 | 结果 |
|---|---|
| `运行.py examples/test_代理循环.light` | **rc=0，打印 `test_代理循环 PASS`** |
| 测试规模 | 222 行 / 76 条断言 / 7 组应抛校验 / 9 个用例段 |
| 反跑（值反 + 抛反） | 2/2 变异体**变红**，还原后**转绿**（详见 §3） |
| 命名冲突扫描 | 与既有 `src/*.light` **0 冲突** |

测试用例段覆盖：
1. 轮次状态机合法/非法转换、终态无后继、抛错后状态不变
2. 步骤状态机（含 skipped）、未知步骤类型、终态判定
3. 一轮多步骤推进与「跑完」判定
4. 检查点四时机、序号严格递增、**幂等恢复**、未知策略抛错
5. 中止信号幂等 + 未知原因抛错
6. 轮次上限（maxRounds）触发 `max_rounds` 中止
7. 中止 → 恢复，跳过已终态步骤
8. 事件驱动 + 监听器错误隔离（坏监听被隔离，好监听仍被调用）
9. 事件投影幂等 + 增量投影不重复计入

---

## 3. 反跑判据（先跑通再写断言，手动改反确认真红再改回）

脚本：`_antirun_r34_t123_反跑.py`（变异写在仓库根目录 `_red_tmp.light`，**不放 `examples/`**，跑完即删）。

| 判据 | 变异 | 变异后 rc | 结论 |
|---|---|---|---|
| A 值反 | `长(循轮次后继(轮次完成))` 期望 `0` → `1` | 1（红） | OK |
| B 抛反 | `建步骤(0, "未知类型")` → `建步骤(0, "llm")`（应当抛错改成不抛） | 1（红，报「应抛错未抛」） | OK |

还原后三个用例复跑全绿（rc=0）。反跑与用例执行**严格串行**，未并发。

---

## 4. 与上游的差异（纯逻辑取舍）

| 差异 | 原因 |
|---|---|
| 无 LLM 流式 I/O、无真实工具分派 | 纯逻辑复刻，副作用外置 |
| 检查点/事件落内存结构而非写盘 | 持久化属 I/O，本轮剔除；已提供 `取检查点/取最近检查点` 供外部持久化 |
| 事件无时间戳，只带递增 `序号` | 避免墙钟不确定性，保证可重放（投影幂等的前提） |
| 监听器同步派发 | 解释器路径无异步 |
| 新增 `aborted` 状态 | 上游中止语义在本模块显式成状态，避免「failed + reason」两处判中止 |

---

## 5. 铁律自检

- [x] 只创建 `代理循环.light`，未修改任何既有模块
- [x] 新增一律 `.light`，未用 Python 绕语言缺陷
- [x] 状态机转换严格，非法转换抛错（7 组应抛校验）
- [x] 检查点恢复幂等（两次恢复快照 `序列化JSON` 相等，且不新增检查点）
- [x] 段落名一律 `循` 前缀，与既有模块 0 冲突

---

## 6. 本轮踩到的语言/工具坑（供缺陷账参考）

1. **`\` 行续行不被支持**：`从 X 导入 a, \` 换行续写 → 解析错误。导入清单必须写成**单行**。
2. **段落内 `设 模块级标量 为 ...` 只建局部名**：测试里计数器改不出去（表现为监听器「没被调用」）。
   改用具状态：`设 计数盘 为 {"值": 0}` + `字典设置(计数盘, "值", ...)`。
3. **`等待` 疑似关键字**：`设 等待 为 x * y / z` 后 `等待 + 1` 报「意外的标记 `+`」，改名 `需补` 即通过
   （同批模块 `工具执行.light` 第 295 列实证）。
4. **并行 agent 命名冲突**：扫描发现 `校验投影` 与并行路（任务4 `会话日志深化.light`）撞名 →
   本模块改名为 `校验循环投影`，冲突归零。
