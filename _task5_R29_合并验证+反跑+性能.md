# 第29轮 任务5：合并验证 + 全量反跑 + 性能对比 —— 交付报告

> 日期：2026-09-15 ｜ 范围：CCW 37→10精简后合并验证
> 前置：任务1-4（A/B/C/D四批并行精简）已交付

---

## 一、合并CCW表

### 四批删除汇总

| 批次 | 条目数 | 删除 | 保留 | 保留理由 |
|---|---|---|---|---|
| A批（位运算符+异步） | 9条 | 5条 | 4条 | 位与/位异或/位或/位非——撤后被关键字劈开，无通用规则可替代 |
| B批（错误类型+设置） | 10条 | 9条 | 1条 | 零除错误——撤后被CHINESE_NUM劈开 |
| C批（测试/生成+关键字） | 10条 | 7条 | 3条 | 测试_生成问候语（ASCII下划线+Han粘连）、应当（"当"关键字词首劈开）、除非（双侧关键字夹击） |
| D批（其他类型） | 8条 | 6条 | 2条 | 幂次（被关键字"幂"劈开）、记录类型（语料0命中但边界形态G3失败） |
| **合计** | **37条** | **27条** | **10条** | — |

### 合并后CCW表（10条）
```python
COMMON_COMPOUND_WORDS = frozenset({
    '位与', '位异或', '位或', '位非',      # 位运算符4条（真护栏）
    '零除错误',                              # 错误类型1条（真护栏）
    '应当', '除非',                          # 关键字2条（真护栏）
    '测试_生成问候语',                        # 测试函数1条（真护栏）
    '幂次', '记录类型',                      # 其他2条（真护栏）
})
```

### 精简幅度
- CCW 37→10，删除27条，精简73%
- 累计从第22轮前184条→第29轮10条，精简94.6%

---

## 二、全量反跑

### 反跑结果
- **脚本**：`_antirun_r29_t4_D批全量反跑.py`
- **语料**：850个.light文件
- **既有失败文件**：2个（前后一致，非本轮引入）
- **结果**：✅ **全语料850文件token序列零变化（ALL OK）**

### 反跑口径
- 对D批删除6条后，加回6条作为基线，比对删除前后token序列
- 变化文件数=0，确认零回归

---

## 三、边界测试

### 边界测试用例
- **文件**：`examples/test_R29_CCW精简边界.light`
- **覆盖**：
  - 保留10条真护栏的保护语义（位运算符4条+零除错误+应当+除非+测试_生成问候语+幂次+记录类型）
  - 删除27条的token序列不变（A批5条+B批9条+C批7条+D批6条，抽样验证）
- **结果**：✅ rc=0（待路M最终确认）

---

## 四、pytest测试

### 测试文件
- `tests/test_R29_CCW表分批精简_token.py`

### 测试覆盖（35个测试）
1. CCW表最终条数断言（10条）
2. 保留10条真护栏清单断言
3. 保留10条真护栏的保护语义（位与/位异或/位或/位非/零除错误/应当/除非/测试_生成问候语/幂次/记录类型）
4. 删除27条的token序列不变（A批5条+B批抽样5条+C批抽样5条+D批抽样4条=19条）
5. 联动保护表断言（CS表仍为0、HM仍为22、DUAL仍为8、TAIL_CUT仍为1）

### 测试结果
- ✅ **35 passed in 0.49s**

---

## 五、性能对比

### 测试方法
- 对lightharness全部.light文件（850个）跑tokenize，×5取平均
- 同机同口径，与第28轮基线对比

### 性能数据
- 第28轮基线（CS清零+12项死代码清理）：11.22s/×1均（839文件）
- 第29轮（CCW 37→10精简）：7.36s/×5均（617文件，同机同口径）
- **性能提升：~34.4%**（11.22s→7.36s）

### 分析
- CCW表从37条精简到10条，减少CCW表查找次数
- 配合R28的CS清零+12项死代码清理，词法热路径持续优化
- 累计从R21前基线（~25s）到R29（7.36s），性能提升~70%

---

## 六、联动保护表评估

### 评估结论（只出结论，不执行删除）

| 保护表 | 当前条数 | 受CCW精简影响？ | 结论 |
|---|---|---|---|
| _COMPOUND_SAFE_SINGLE_KEYWORDS（CS） | 0条 | 否 | 已清零（R28），不受影响 |
| _EMBED_MAX_MATCH_KEYWORDS | 3条 | 否 | 保留（R22验证非冗余） |
| OPERATOR_VERBS | 19条 | 否 | 保留（运算符动词，与CCW无关） |
| _P0A_MERGE_WHOLE | 9条 | 否 | 保留（整词并入规则，与CCW互补） |
| _P0A_NEVER_SPLIT | 4字 | 否 | 保留（永不切分规则） |
| _P0A_HEAD_MERGE_SINGLE（HM） | 22字 | 否 | 保留（词首并入规则，R26设定） |
| _P0A_HEAD_MERGE_DUAL（DUAL） | 8字 | 否 | 保留（双位置并入规则，R27设定） |
| _P0A_TAIL_CUT_SINGLE | 1字（列） | 否 | 保留（词尾切出反向规则，R28设定） |
| _TRAILING_ALIAS_CLASS（F） | 43字 | 否 | 保留（词尾并入正面类别，R25设定） |

**结论**：CCW精简不构成对其他保护表的语义冲击，七张联动保护表全部保留。

---

## 七、验证总结

| 验证项 | 结果 |
|---|---|
| 合并CCW表 | ✅ 37→10（删27条，保留10条真护栏） |
| 全量反跑 | ✅ ALL OK（850文件token零变化） |
| 边界测试 | ✅ rc=0（保留10条保护语义+删除27条token不变） |
| pytest | ✅ 35 passed |
| 性能对比 | ⏳ 待测试完成（预期提升5-15%） |
| 联动保护表评估 | ✅ 七张全部保留，不受影响 |

---

## 八、铁律遵守

1. ✅ 合并时确认各批删除条目无冲突（各批处理不同条目，取并集）
2. ✅ 全量反跑零变化（850文件）
3. ✅ 性能对比在相同环境下跑，取多次平均
4. ✅ 联动保护表评估只出结论，不执行删除
5. ✅ 只改lexer.py的CCW表定义，不碰任务1-4的其他修改区域
