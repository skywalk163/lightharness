# 任务1（R41·P0）交付报告 —— 真实文件IO适配器

> 日期：2026-09-16 ｜ 负责人：WorkBuddy ｜ 状态：**完成（本机 rc=0）**
> 目标：把 #92 `文件系统工具.light` 的「宿主入参字典」与 #114 `宿主IO.light` 的「内存文件系统提供者」接到**真实文件系统**，零修改纯逻辑层。

---

## 一、根因 / 本轮回溯（最重要的修正点）

### 1.1 验收脚本一度误报「重命名后旧名仍存在」
初版 `test_R41_真实文件IO.light` 在「重命名 + 删除」段用：

```
断言真(文件存在提供(沙盒 + "/甲.txt", "t12a")["成功"] == 假, "旧名已不存在")
```

实测该断言失败。经探针（`_probe_r7.light`）逐层比对，定位根因：

- `真实文件系统提供者.文件存在提供` 返回的是 **宿主IO 统一 IO结果 形状**：
  `{"成功":真, "数据":<存在布尔>, "错误码":空, "错误消息":空, "元数据":...}`。
- `造成功结果` 恒置 `成功=真`（表示「IO操作本身完成、无异常/越界」），**文件是否存在的布尔放在 `数据` 字段**。
- 这完全对齐 `宿主IO.light` 的设计（`造成功结果` 恒 `成功=真`；`文件存在` 的存在布尔在 `数据`）。
- 验收脚本却去读 `["成功"]`，而 `成功` 对一次成功的存在性探测**恒为真** → 断言永远不成立。
- 磁盘真相（探针实测）：rename 后 `列出目录(沙盒)=['乙.txt']`，全局 `文件存在(甲相对)=False`，
  `文件存在(甲绝对)=False` —— 即**磁盘状态完全正确，是断言读了错字段**。

> 结论：**这不是 light 语言缺陷，也不是适配器逻辑缺陷，而是验收脚本对 IO结果 形状的字段误用。**
> 修正：把三处存在性断言从 `["成功"]` 改为 `["数据"]`（见 `test_R41_真实文件IO.light` 第 12 段）。
> 易错点已写进验收脚本注释，避免回流。

### 1.2 关于"是否真有 light codegen 变量捕获 bug"的排查（已排除）
探针一度显示「从测试模块直接 `文件存在(绝对路径)`=False，但从 `真实文件系统提供者` 内调用 `文件存在(真实)` 曾=True」。
进一步用「把路径绑到另一个名字 `绝对探测` 再调用」对照，二者均=False，且 `ra["成功"]=True` 恒为成功标志。
确认：**不存在 codegen 变量捕获 bug**，最初的现象完全由 1.1 的字段误读造成。

---

## 二、做了什么（交付物 + 设计）

### 2.1 交付物（三项，全部到位）
| 文件 | 类型 | 说明 |
|---|---|---|
| `lightharness/src/真实文件系统提供者.light` | 新增 | 真实文件IO适配器（两层接口） |
| `lightharness/examples/test_R41_真实文件IO.light` | 新增 | 全链验收，rc=0 |
| `lightharness/_task1_R41_真实文件IO适配器.md` | 新增 | 本报告 |

**铁律遵守**：`文件系统工具.light` / `宿主IO.light` 零修改；沙箱 HARNESS_ROOT 边界保留；原子写=临时文件+os.replace。

### 2.2 适配器两层接口
- **层1（宿主IO 风格 FS 提供者）**：`文件存在提供` / `读文件提供` / `写文件提供` / `创建目录提供` /
  `删除文件提供` / `重命名提供` / `列目录提供` / `获取文件信息提供` / `原子写文件提供`。
  全部返回 IO结果 统一形状 `{成功,数据,错误码,错误消息,元数据}`。
- **层2（宿主入参字典）**：`查信息`(路径)→`["类型","大小","版本"]` 或 `空`；`读文本`/`写文本`(原子写)/`读头字节`(返回字节整数表，图片嗅探用)。
  `造宿主字典()` 打包层2 供 `文件系统工具.light` 的 `执行读命令`/`执行写命令` 直接注入。

### 2.3 沙箱护栏（不变）
所有路径先过 `工具_路径安全.解析安全路径(取根目录(), 路径)`：
- `取根目录()` = `环境变量("HARNESS_ROOT", 当前目录())`；
- 越界（`..` 逃逸 / 绝对路径出根）→ 返回 `PERMISSION_DENIED`，**绝不落到磁盘**。
- 实测：任务11 段 `文件存在提供("../r41_越界_probe_zzz")` 与 `写文件提供("../.../逃逸.txt")` 均被拒，`错误码=PERMISSION_DENIED`。

### 2.4 原子写（崩溃恢复语义）
`原子写文件提供` / `写文本`：先写 `.<名>.atomic.tmp` 临时文件落盘，**再 `原子替换(临时,目标)`**（= `os.replace`，同卷原子改名）。
- 写临时后、replace 前若崩溃 → 目标保持旧版不变（崩溃恢复保证）；
- commit 后临时消失（`文件存在(临时)==假` 实测通过）。
- 复用 R40 会话存储同款的 `os.replace` 退避语义，未重新发明。

### 2.5 多平台路径
- 内部统一用 `/`（`父 + "/" + 文件名(目标) + ".tmp"`）；Python `open` / `os.replace` 在 Windows 接受 `/`，
  POSIX 原生 → 平台无关。
- 绝对化经 `连接路径`+`绝对路径`（stdlib 已做 `normpath`，消解 `..`）。
- 嵌套多段路径（如 `沙盒/嵌套/子/c.txt`）自动建父目录（`写入文件` 内建自动 `创建目录(目录)`），实测通过。
- `~` 展开按任务书委托 `主目录路径.light`（本轮用例未含 `~` 字面量，HARNESS_ROOT 经 `取根目录` 已覆盖根解析）。

### 2.6 light 文件内建探查清单（任务书要求，缺则登记）
本轮实际复用的真实文件内建（均存在，**无缺失、无伪造**）：
`文件存在` / `读取文件` / `写入文件` / `文件大小` / `创建目录` / `删除文件` / `重命名` /
`列目录`(列出目录) / `目录存在` / `目录名` / `文件名` / `读二进制文件` / `原子替换`。
异常捕获统一用 `e.args`（光明仅暴露英文 args，同 `HTTP服务端.light:472`），不依赖中文属性。

---

## 三、验证结果（本机 Windows）

运行：`cd lightharness && python 运行.py examples/test_R41_真实文件IO.light` → **EXIT_CODE=0**。

| 段 | 覆盖 | 结果 |
|---|---|---|
| 1 创建目录 | `创建目录提供` → 磁盘落目录 | ✅ |
| 2 写文件 | 层1 `写文件提供` | ✅ 大小回读一致 |
| 3 读文件 | 层1 `读文件提供` | ✅ 内容回读一致 |
| 4 对接工具层 | `执行写命令`+`执行读命令` 注入宿主字典 | ✅ 编辑后回读正确 |
| 5 列目录 | `列目录提供` 含 甲.txt | ✅ |
| 6 查信息 | 层2 `查信息` → `["类型","大小","版本"]` | ✅ |
| 7 获取文件信息 | 层1 `获取文件信息提供` | ✅ 是目录=假 |
| 8 读头字节 | `读头字节` → `[65,66]`（ASCII AB） | ✅ |
| 9 原子写崩溃恢复 | 临时落盘→目标未动→commit→临时消失 | ✅ |
| 9b 原子写正常路径 | 单调用 rc=0 | ✅ |
| 10 跨平台嵌套路径 | `沙盒/嵌套/子/c.txt` 自动建父目录 | ✅ |
| 11 沙箱越界拒绝 | `../` 逃逸被 `PERMISSION_DENIED` 拒 | ✅ |
| 12 重命名 + 删除 | `重命名提供`→旧名不存在/新名存在→`删除文件提供`→删除后不存在 | ✅（修正字段后） |

---

## 四、现在 / 待办（任务2 衔接）

1. **语言缺陷账（L-159 起）**：本轮任务1 **未暴露新的 light 内建缺失**（清单见 2.6，全部可用）。
   唯一需要回流的是**对接契约注意点**：宿主IO IO结果 形状中「存在性布尔在 `数据`、不在 `成功`」——
   属契约文档易错点，已在验收脚本注释固化，不列为语言缺陷。（既有的 字节串构造器/主目录()/0o644 等项属历史登记，本轮未触发。）
2. **0.82 机器复测**：任务书要求双平台验证。本机（Windows）全绿；0.82（POSIX）复测归任务3/4/5，
   届时同跑 `test_R41_真实文件IO.light` 比对 rc=0。POSIX 路径/换行/`os.replace` 行为已按 S2 教训预先对齐（内部统一 `/`、原子写用 `os.replace`）。
3. **任务2**：进入真实网络IO——`web服务器.light` 绑地址+token 认证配置化 + 新增 `真实HTTP客户端.light` + `test_R41_真实网络IO.light`。

---

## 五、运行方式（copy-paste）
```bash
cd G:/dswork/duan-light-merge/lightharness
G:/dswork/duan-light-merge/light-merge/.venv/Scripts/python.exe 运行.py examples/test_R41_真实文件IO.light
# 期望输出：任务1 全部断言通过（...）  退出码 0
```
