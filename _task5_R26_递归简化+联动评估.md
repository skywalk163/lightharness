# 第26轮 任务5 交付报告：递归简化评估 + 联动保护表评估 + 性能对比

日期：2026-09-15 ｜ 状态：✅ 评估完成 + 注释修正落地
铁律口径：联动保护表**只出结论不执行删除**（删除需后续轮次逐条验证）；
删除前必须 grep 全库确认零引用。

---

## 一、5.1 `_skip_compound_safe_and_match` 简化评估（lexer.py:1248）

### 引用现状（grep 全库）
```
src/lexer.py:1239   _match_keyword 内调用（唯一外部调用方）
src/lexer.py:1319   递归自调用
（.scratch/ 下为历史快照，不计）
```

### 是否仍被调用？
**是。** 任务1 将词首并入正面规则实现在**调用方**（`_tokenize_chinese_sequence`
第一层 skip_verb 分支 + 嵌入扫描三处，:2558/:2746/:2830/:2983），但
`_match_keyword` 内部的 compound-safe 递归路径（:1238–1244）原样保留，其递归
副作用语义仍被依赖：

- **「最内层 CS 单字回报」语义**：`除空格` → 递归回报 ('空',1) → 外层判
  kw≠'之' → 返回 (None,0) → `去除空格` 整体保留为 stdlib 函数名标识符。
  若改纯最长匹配，`除` 作为运算符浮现，`去除空格` 被切成 去+除+空格
  （R22 前实测 A/B 抓到 100+ 处同类回归：10的幂/索引/种类/阶乘…）。
- **`之` 唯一例外**（v7 新单 B）：`自之姓名` → 内层 ('之',1) → 外层返
  ('自',1)，保住 self 语义；收窄到 `之` 后 A/B 仅 16 处修正，全部为票内目标。

### 可否转迭代？
理论上可（显式栈 + 三态传播：内层 None/'之'→回报本层候选，其他→(None,0)）。
已推演 10+ 边界用例（`除空格`/`自之姓名`/`对于`/`对是`/`段段`/`除除为`/`段`/
`对子`/`对之`/`除除之`），转换逻辑可行但**传播规则三态交织，出错即静默错译**，
且递归深度实际仅 1–3 层（CS 16字），性能收益 ~0（`_match_keyword` 已内联
第一层快速路径）。

### 结论：**保留现状，不简化**
理由：转换风险（静默错译）> 收益（省 1–3 次函数调用/词首命中）；该函数是
多轮 A/B 验证过的正确性锚点。若未来轮次需要，建议携带「边界用例矩阵 +
全量 A/B」再动。

## 二、5.2 `compound_safe_single_keywords` 类属性评估（lexer.py:606）

### 引用现状（grep src/）
```
:606   类属性定义
:2745 / :2829 / :2982   嵌入式扫描三处 self.compound_safe_single_keywords
:1284–1286 / :2112   局部缓存 _compound_safe = _COMPOUND_SAFE_SINGLE_KEYWORDS
```

### 结论：**不可删除**
嵌入式扫描三处仍以「CS ∪ `_P0A_HEAD_MERGE_SINGLE`」作为词首并入有效类别
（:2745 等），类属性是这三处的取值来源。局部缓存（:1286/:2112）是 64K+ 次
热路径的性能优化（消 self 属性查找），非死代码，保留。

## 三、5.3 联动保护表逐项评估（只出结论，不执行删除）

### 3.1 CCW（`COMMON_COMPOUND_WORDS`，37条）→ **保留**
- 机制：多字复合词整串保护（内建名/语料雷区），作用在**多字**整串匹配层。
- 与本轮关系：词首并入正面规则只作用于**单字**关键字 + 词首 + 后随汉字场景，
  与 CCW 的多字整串保护机制互补、无重叠。本轮 843 文件 token 零变化也反证
  CCW 撤条会打红（CCW 条目在语料中有真实命中场景，如 设置/当前/应当/除非…）。
- 后续建议：沿用 R23 双中立判据（语料中立∧隔离中立）逐条复验，非本轮范围。

### 3.2 `_EMBED_MAX_MATCH_KEYWORDS`（3条：为/尝试/返回）→ **保留**
- 机制：嵌入扫描子串中这三个关键字要求**最大长度优先**匹配（L-027 返回系）。
- 与本轮关系：词首并入改的是「单字关键字是否并入标识符」的类别判定，不改
  「关键字内部最大匹配优先级」。第22轮已验证不冗余，本轮无新证据推翻。
- 三字均为 `_P0A_HEAD_SPLIT_SINGLE` 成员（词首必须切分），与并入规则正交。

### 3.3 `OPERATOR_VERBS`（19条）→ **保留**
- 机制：运算符语义核心（大于/小于/等于/加/减/乘/除/模…），驱动
  `_OPERATOR_KEYWORDS`、嵌入扫描运算符分支（:2705/:2835/:2864/:2988/:2995）。
- 与本轮关系：运算符被 `_P0A_HEAD_SPLIT_SINGLE` 排除出词首并入正面类别；
  其中 DUAL 8字（乘减加除模真空到）以 CS 保留作词首锚（后随汉字并入、
  后随空白切分）。表本身语义与结构未受影响。

### 3.4 `_P0A_MERGE_WHOLE`（9条）→ **保留**
- 机制：有界「精确整串」合并集合（外部命令/导出事件表/排序依据/接收参数/
  整理模型消息/输出块表/返回码/退出码/非空块），升级计划 §8.4 点名的
  6 个历史雷区之一，替代逐词生长白名单。
- 与本轮关系：作用层为**多字整串**（deterministic 模式 :2234），与单字词首
  并入规则无交集；9 条均为多字词，不在正面类别作用域内。

### 评估汇总表

| 保护表 | 条数 | 结论 | 一句话理由 |
|---|---|---|---|
| CCW | 37 | 保留 | 多字整串保护，与单字词首并入互补；撤条打红 |
| _EMBED_MAX_MATCH_KEYWORDS | 3 | 保留 | 最大匹配优先级，与类别判定正交 |
| OPERATOR_VERBS | 19 | 保留 | 运算符语义核心，已被排除集正确隔离 |
| _P0A_MERGE_WHOLE | 9 | 保留 | 多字精确整串，不在单字规则作用域 |

## 四、5.5 死代码清理 + 注释修正

### grep 全库死代码扫描
- `_P0A_COMPOUND_SAFE`：仅存于 :3130 历史注释（R24 任务3 已清理的记录），
  无实际定义/引用，**无需处理**。
- TODO/FIXME/DEAD/UNUSED/deprecated 标记：0 处。
- `_R26_CS_REMOVED`、`_R26_STMT_HEAD_SINGLE`：均被文末自校验断言引用，
  非死代码。

### 本轮执行的注释修正（lexer.py，2处）
1. **:3664** 自校验断言注释 `字面量 30 字` → `字面量 16 字`（补 R26 任务3
   后 30→16 口径）。
2. **:3716** 净增量断言注释 `移除的 12 字` → `移除的 14 字`（与
   `_R26_CS_REMOVED` 实际 14 字对齐：余例出则常引接断末试跳过长首）。

修正后验证：`import lexer` 通过，全部文末自校验断言通过
（CS=16，HM=22，净增量==14字，HM⊆F）。

### 已核对无误、无需修正的注释
- CS 表上方文档（:355–382）：已正确写明 30→16、移除14字、A类8字/B类8字。
- `_R26_CS_REMOVED` 定义注释（:3710）：已正确写明 14 字。

## 五、5.4 性能对比

### 口径
与第25轮一致：全语料 843 文件纯 `Lexer(text, deterministic=True).tokenize()`，
×10 次取平均，不含 json/sha 后处理；同机同环境。

### 实测

| 轮次 | CS表 | 平均耗时 | 备注 |
|---|---|---|---|
| R25 基线 | 30字 | 9.63s | 第25轮报告口径 |
| R26 实测① | 16字 | 8.62s（中位 8.82s） | 反跑首轮 |
| R26 实测② | 16字 | 9.86s（中位 9.75s） | 写基线轮 |
| R26 实测③ | 16字 | 12.04s（中位 12.83s） | 同机有后台 pytest 并发 |

### 结论
- 三轮实测 8.62s–12.04s，中位 8.82s–12.83s，与 R25 基线 9.63s 处于**同一
  波动带**；CS 30→16 + 词首并入正面规则（新增 frozenset membership 判断）
  **未引入可测性能退化**。
- 波动主因：同机后台并发（pytest/反跑互相挤占）；单轮极值 6.0s（静置）—
  提示 R26 静置态可能**优于** R25 基线，但需任务6独立静置复测确认。
- 预期中的收益（CS 表 -46.7% 条目、词首判定从逐词白名单改 frozenset 正面
  类别）在热路径上体现为 `_P0A_HEAD_MERGE_SINGLE` 的 O(1) 成员判断，
  与 CS 判断同阶，故总量持平属合理结果。

## 六、铁律自检

| 铁律 | 自检 |
|---|---|
| 删除前 grep 确认零引用 | ✅ 本轮未删任何函数/常量（评估结论均为保留） |
| 性能对比同环境多次平均 | ✅ 同机 ×10 取平均 ×3 轮，波动原因已标注 |
| 联动表只出结论不删除 | ✅ 4 表全部「保留」结论，未动一行表定义 |
| 文末自校验断言更新通过 | ✅ 2 处注释修正后 import 断言全过 |
| 只改 lexer.py，不碰任务1-4区域 | ✅ 仅 :3664/:3716 两行注释，均为评估/断言文档层 |
