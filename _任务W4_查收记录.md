# W4 主线查收记录（2026-09-09）

> 范围：外发任务_网页启动_W4_启动器（examples/运行Web服务器.light 独占）

## 查收结论：通过，无需修复

与 W4 交付报告（`_任务W4_启动器_交付报告.md`，含 22 项复现脚本 `_verify_webui.py`）互证，
主线独立复验全部一致。

## 主线独立复验（实测记录）

| 判据 | 主线实测 | 结果 |
|------|----------|------|
| 门禁兼容 | `MOCK=1 + NO_OPEN=1 + TOKEN=gate-check-token-456 + LIFETIME=3` → 打印 `http://127.0.0.1:52174/?token=gate-check-token-456`，rc=0，端口文件 52174 一致，无浏览器弹出，无进程残留 | ✅ |
| 登录 URL 格式 | token 与固定值一致、含 `?token=`；反跑（破坏拼接）后输出 `http://127.0.0.1:52263`（无 token），与正常可分辨；还原后恢复 `/?token=tk-anti` | ✅ |
| 自动开浏览器 | 默认模式（不设 NO_OPEN）启动：无"自动打开浏览器失败"提示（`start` 返回码 0），rc=0 | ✅ |
| webui 定位·S3 | `HARNESS_WEB_UI_DIR` 指向不存在目录 → GET / 404 + POST API 200（降级不崩，无警告=按契约优先级①） | ✅ |
| webui 定位·S4 | 临时重命名 webui → 打印 `未找到 webui 目录，静态页面不启用` + GET / 404 + API 正常 + rc=0；恢复后复核通过 | ✅ |
| webui 定位·S2 | 默认脚本推导（examples/../webui）→ GET / 200（W3 联调中已实测） | ✅ |

## 文件归属

- `examples/运行Web服务器.light` diff：66 insertions / 3 deletions，全部为 W4 职责内容
  （认证装配、界面目录解析、登录 URL、自动开浏览器、环境变量），无越界改动。
- 交付物：`_任务W4_启动器_交付报告.md` + `_verify_webui.py`（22 项复现脚本，交付说明已声明）。
- 全量回归 `188 passed`（交付报告自述；主线在集成阶段统一复跑确认）。

## 遗留说明

- `_verify_webui.py` 是 W4 的验证辅助（非白名单，交付报告已声明），保留供复现；如不需要可删。
- 与 W3 交接确认：`配置` 第 5 参 `界面目录` 传参、`初始化认证()` 调用时机、登录 URL 契约 6
  均按总览执行，实测端到端可用。
