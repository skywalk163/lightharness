# 任务6（R54·P2）交付报告 —— 缺陷账登记 + docs回填 + 探针移档

> 日期：2026-09-17 ｜ 状态：**完成**
> 交付物：本报告 ｜ 铁律遵守：docs定稿归路M。
> 关联：L-170编译器静默修复（核心arity-2动词作变量名下标/成员赋值静默变比较）

---

## 一、缺陷账登记：L-170/L-171标记已修

**文件**：`lightharness/docs/功能对标/语言缺陷账.md`

**新增条目**：

| 编号 | 现象 | 状态 |
|---|---|---|
| **L-170** | 核心arity-2动词作变量名 → 下标/成员赋值静默变比较（`映射[k] 为 99` → `(映射[k] == 99)`） | ✅ 已修复（第54轮任务2） |
| **L-171** | `写 X + Y` 静默丢实参 | ✅ 已修复（L-055已覆盖，第54轮任务1复核确认） |

**L-170根因**：`parser_stmt.py` 两处缺陷叠加——
1. KEYWORD路径缺赋值守卫：核心动词在语句首被无条件当动词调用；
2. IDENTIFIER路径完全漏管：lexer把已声明动词名重分类为IDENTIFIER后，L-058只覆盖STDLIB动词不覆盖核心VERB_ARITY → 落到默认表达式语句 → 静默变比较。

**L-170修复**：`parser_stmt.py` 两处——
1. 动词调用分支守卫：动词名（KEYWORD/IDENTIFIER均可）后跟 `[`/`.`/`之`/`的`/`为`/`等于`/`=` 时改走赋值；
2. 赋值目标名接收VERB_ARITY动词。

---

## 二、docs回填：对标清单 #193

**文件**：`lightharness/docs/功能对标/对标清单.json`

**新增条目 #193**：

| 字段 | 值 |
|---|---|
| 编号 | 193 |
| 功能 | L-170编译器静默修复（核心arity-2动词作变量名下标/成员赋值静默变比较） |
| 原模块 | 编译器parser_stmt语句解析层 |
| 光明模块 | light-merge/src/parser_stmt.py |
| 状态 | 已完成 |
| 完成轮次 | 第54轮 |
| 备注 | 任务1/2。修复parser_stmt动词调用分支缺赋值守卫+IDENTIFIER路径漏管的问题。L-171写X+Y经复核已由L-055覆盖。 |

**当前对标清单总条目数**：193（原192 + 新增1）

---

## 三、探针移档

**本轮无独立探针脚本目录**（不像R53有_l172probe/）。
- 任务1诊断用例直接入库为 `examples/test_L170.light`
- 诊断/修复报告直接在 light-merge 根目录（_task1/_task2_R54_*.md）
- 无临时探针脚本需要移档

**结论**：本轮无需移档操作。

---

## 四、本轮改动文件清单

| 文件 | 操作 |
|---|---|
| `lightharness/docs/功能对标/语言缺陷账.md` | 追加L-170/L-171条目 |
| `lightharness/docs/功能对标/对标清单.json` | 新增#193条目 |

---

## 五、结论

1. 缺陷账L-170条目已登记并标记**已修复**；L-171经复核确认已修（L-055覆盖）；
2. 对标清单#193已新增，与L-170修复对应；
3. 本轮无独立探针脚本需移档；
4. docs三件套（缺陷账+对标清单）完成，归路M定稿。
