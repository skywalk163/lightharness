# 第43轮 任务2 交付报告 —— 真实 agent 循环（R40 互举接线换真实 LLM 客户端，工具真调）

> 日期：2026-09-16 ｜ 优先级 P0 ｜ 修改区域：examples（零修改 互举接线.light / 客户端.light / mock大模型服务器.light）

## 1. 根因 / 背景

第43轮把「大脑」接真。任务1 已验证**真实 LLM 往返**（客户端真 POST + SSE 解析 + wire 映射）。
任务2 在任务1 之上把 **R40 互举接线**用的大模型**回放**换成**真实 LLM 客户端**（同打本机 mock 服务端），
并让 agent 循环里的工具**真调**：bash（经 `工具_bash` + `工具执行` 注册表真跑 shell）、
真实 fetch（`真实抓取提供`）、真实文件读写（`真实文件系统提供者`）。

即：LLM 回文本 → 回工具调用 → 真调 bash → 工具结果回 LLM → 最终文本，**多轮流转全程真实**，不退放。

### 1.1 复刻中暴露的装配坑（均已解决/登记）

**坑A（死锁，同任务1 绕行）**：`客户端.对话响应` 阻塞（urllib）。复用任务1 的
「阻塞客户端丢 OS 线程 + 主协程 `异步睡眠` 轮询让出事件循环 + `处理循环` 跑主循环任务」并发模型。

**坑B（L-162，mock handler 响应形状缺陷）**：同任务1——测试自写 handler 复用 `分派` 行为逻辑，
仅做 `[状态,头,体]`→`{"状态","头","体"}` 转换。本轮未动源码。

**坑C（L-163，工具_bash ↔ 工具执行 装配契约两处摩擦，本轮新发现）**：
1. `注册bash工具(注册表)` 调 `注册表.注册(...)`，但 `建注册表()` 返回 dict 无 `.注册` 方法
   → `AttributeError`。改用 `具注册工具(执行注册, "bash", 级公开, 定义["execute"], 空)`。
2. `执行工具` 以 `处理(参数, 令牌)` 调 handler，而 `bash处理` 仅一参 → `takes 1 positional argument but 2 were given`
   （光明对形参数量严格）。测试内写 2 参适配器 `我的bash处理(参数, 取消令牌)` 转调 `bash处理(参数)`。

**坑D（L-164，执行工具 状态字面值 "ok"/"failed"）**：`执行工具` 成功返 `{"状态": 调正常}`，
`调正常` 字面值是 `"ok"`（非直觉 "normal"）。断言改用导出的 `调正常` 常量。

> 坑C / 坑D 均为「模块 API 契约摩擦」，非语言本体 bug；本轮在测试层绕行（零修改铁律），
> 源码侧登记 L-163 / L-164 待 Alignment 修复。

## 2. 交付物

| 文件 | 说明 |
|---|---|
| `examples/test_R43_真实agent循环.light` | 真实 agent 循环验收，rc=0 |
| `examples/test_R43_真实LLM往返.light` | 任务1 真实 LLM 往返验收，rc=0（复验通过） |
| `lightharness/_task2_R43_真实agent循环.md` | 本报告 |
| `lightharness/_task1_R43_真实LLM往返.md` | 任务1 报告（既有） |
| `docs/功能对标/语言缺陷账.md` | 追加 L-162 / L-163 / L-164 |

## 3. 设计要点

- **mock 脚本化（确定性）**：`新建Mock服务(["序列": ["success","tool_call_success","success"], "工具名": "bash", "工具参数": "{\"命令\":\"echo 你好R43\"}"])`。
  第一轮回文本 / 第二轮回工具调用（name=bash, arguments=`{"命令":"echo 你好R43"}`）/ 第三轮回最终文本。
- **agent 循环（R40 互举接线结构复用，零改 `互举接线.light`）**：`当 轮 < 3` 多轮；
  `等往返` 经客户端线程真打 mock；`块组装器` 重装 → `组装消息("client")` 追加为 assistant 消息；
  完成原因 `tool-calls` 时遍历 tool-call 块 `执行真实工具(名字, 参数)` → `造工具消息` 回灌 → 续跑。
- **工具结果回 LLM 形状（对齐 `客户端.映射网络消息` 的 tool-result）**：
  `造工具消息(调用块, 结果文本)` 返回
  `{"role":"tool","tool_call_id": 块["id"], "content":[{"type":"tool-result","toolCallId": 块["id"],"content":[{"type":"text","text": 结果文本}]}]}`，
  形状与上游 `映射网络消息` 的 `tool-result` 一致，保证续跑时 LLM 能消费工具结果。
- **真实工具三方验证（同一 `执行真实工具` 分发）**：
  - `bash` → `工具执行.执行工具(执行器, "bash", 级公开, "agent-main", 参数, 空)` 真跑 `echo 你好R43`；
  - `fetch`/`web_fetch` → `真实抓取(参数["网址"])` 真发起 HTTP（B1 GET 本机 mock → 真实 405，证明真发了请求）；
  - 文件 → `写文本`/`读文本`（受 HARNESS_ROOT 沙箱护栏）真落盘回环（B2）。
- **超时兜底**：HTTP 超时 5.0s，线程轮询有界，不会无限挂起。

## 4. 验证结果（本机 Windows 实跑）

```
python 运行.py examples/test_R43_真实agent循环.light   →   EXIT_CODE=0
python 运行.py examples/test_R43_真实LLM往返.light      →   EXIT_CODE=0  (复验)
```

任务2 逐断言全绿：
- agent 循环 轮数=3（回文本 / 工具调用 / 最终文本 三态齐）；
- 真调了工具（第二轮 `调真实工具=bash 参数={"命令":"echo 你好R43"}`）；
- bash 输出含 `你好R43`（真实 shell 回显命中）；
- 最终文本含 `mock response recovered`（工具结果回灌后续跑生效）；
- B1 真实 fetch：`状态=405`（GET 本机 mock 端点，仅 POST 受支持，证明真实 HTTP 往返）；
- B2 真实文件读写回环：`写文本`→`读文本` 一致（`R43 真实文件IO ok`），受 HARNESS_ROOT 沙箱。

## 5. 待办 / 风险提示

- **L-162 / L-163 / L-164**：本轮新增三项缺陷（mock handler 形状、bash 装配契约、执行状态字面值），
  均已登记 `docs/功能对标/语言缺陷账.md`，零修改铁律下测试层绕行，未动源码。
- 任务2 验证的是**本机 mock 服务端**的真实链路（不依赖外部 API/key），满足「真实 LLM 用本机 mock 服务端」铁律。
- 任务3（0.82 FreeBSD 复测）/ 任务4（0.82 pytest）/ 任务5（文件 IO 深化）/ 任务6（质量审查）待后续；
  0.82 机器信息（192.168.0.82，FreeBSD 15.1，用户 ai，凭据见 .env，只读执行 /tmp 临时副本）已备。
- 注：本轮 `examples` 仅新增两个测试文件 + 本/任务1 报告 + 缺陷账追加；纯逻辑层
  （互举接线 / 客户端 / mock大模型服务器 / 文件系统工具）零修改，符合铁律。
