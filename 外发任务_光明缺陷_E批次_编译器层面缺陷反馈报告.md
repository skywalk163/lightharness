# 光明语言编译器缺陷反馈报告 E 批次（第19轮）

> **移交方**：lightharness（光明复刻 deepseek-harness 项目）
> **接收方**：光明语言团队（light-merge 编译器）
> **日期**：2026-09-13
> **缺陷来源**：第1~18轮复刻过程中累计登记，第18轮缺陷修复轮分类为C级（编译器层面，.light层无法真正修复）
> **关联文档**：`docs/功能对标/语言缺陷账.md`（L-001~L-143）、`docs/功能对标/编译器缺陷验收判据.md`（L-001~L-018）、`外发任务_光明缺陷_D批次_总览.md`（L-068~L-074）

---

## 一、概述

本批次共 **15个编译器层面缺陷**（C级），均为.light层无法真正修复、必须修改编译器/运行时才能解决的问题。此外，D批次（L-068~L-074，2026-09-09外发）的7个缺陷修复状态待验收确认。

### 1.1 缺陷分布

| 领域 | 数量 | 缺陷编号 | 优先级 |
|---|---|---|---|
| 词法分析（关键字/保留字/标识符切碎） | 7 | L-084/L-092/L-101/L-119/L-120/L-137/L-143 | **最高**（家族性根因，影响所有中文标识符命名） |
| 字符串与内置映射 | 3 | L-086/L-090/L-087 | 高 |
| 异常与错误处理 | 1 | L-093 | 中 |
| 模块与导入 | 2 | L-094/L-095 | 中 |
| 字典与对象访问 | 1 | L-096 | 中 |
| 函数与闭包 | 1 | L-097 | 中 |

### 1.2 与已修复缺陷的关系

- L-001~L-018（第10轮移交）：**17/18已修复**（仅L-009工程层未修），见`编译器缺陷验收判据.md`。
- L-066/L-067/L-075/L-076/L-079/L-080/L-081/L-082/L-083：light-merge git log确认已修复。
- D批次L-068~L-074：**修复状态待验收确认**（见附录A）。
- 本批次15个C级缺陷：**均未修复**，.light层仅提供检查工具+绕法指南（第18轮词法安全工具库）。

---

## 二、词法分析缺陷（7个，最高优先）

这7个缺陷共享同一根因：**光明词法分析器对中文关键字/保留字的处理过于激进，导致包含关键字子串的中文标识符被错误切碎**。L-004（关键字边界拆分）曾修复了成员访问场景，但标识符声明/变量名/参数名场景仍存在大量问题。

### L-084：`为`字黏在标识符尾部被切碎

- **现象**：赋值关键字`为`在词法层恒为独立KEYWORD，即使黏在中文标识符尾部也会被切碎。`行为`→`行`+`为`、`末位行为`→`末位行`+`为`、`全部行为`→`全部行`+`为`。类属性默认值`属性 末位行为 等于 ""`解析为属性名=`末位行`、再把`为`当赋值符，后续`等于`变成无主表达式。
- **影响面**：所有含"为"字的中文标识符（行为/作为/成为/认为等），在属性声明/变量赋值/参数名场景均触发。
- **最小复现**：`设 末位行为 为 "test"` → 解析错误
- **期望**：词法分析器应识别`为`仅在独立作为赋值关键字时才是KEYWORD，黏在标识符尾部时应视为标识符的一部分。参考L-004修复方案（成员访问场景的关键字边界识别），扩展到标识符声明/赋值/参数名场景。
- **优先级**：P0

### L-092：标识符含`返回`关键字子串被切碎

- **现象**：形参名以`返回`开头（`段落 甲 接收 返回表:`）报「期望冒号，但得到关键字」；段落名含`返回`（`段落 注册未声明返回:`）同样解析错误。`返回``为`等关键字字样出现在标识符任意位置都会被切碎。
- **影响面**：所有含"返回"的标识符（返回值/返回码/返回表/未返回等）。
- **最小复现**：`段落 测试 接收 返回值:` → 解析错误
- **期望**：同L-084，关键字仅在独立语法位置时才是KEYWORD。
- **优先级**：P0

### L-101：`回调`是保留关键字

- **现象**：`回调`是保留关键字，不能用作变量名/遍历变量名。`设 回调 为 函数` → 报错。
- **影响面**：事件驱动/异步编程中"回调"是极常用变量名。
- **最小复现**：`设 回调 为 空` → 报错
- **期望**：评估`回调`是否需要作为保留关键字。若需保留（如未来语法`回调 函数`），应在错误提示中明确说明并给出替代建议；若无需保留，应从保留字表移除。
- **优先级**：P1

### L-119：L0单字别名静默保留字（49字）

- **现象**：v4.0「L0单字别名」是静默保留字，且误用报错完全误导。L0单字别名在词法层把常见中文单字吞成关键字。实测：`设 配 为 [10, 20]`合法不报错；`打印 配[0]`词法把`配`判为K_MATCH（`匹配`的单字别名）→ 本行被解析成「匹配 [0]:」→ codegen产出`print()` + `match [0]:`空体 → 运行期报「缩进错误」。
- **49字清单**：若则否是段出导遍对跳过试捕抛终返配己自设为从当空长首末余父常并与且或的到现例步断接承宏引掷跃非真
- **影响面**：这49个常见中文单字不能作为变量名/标识符，且误用时报错完全误导（不提示"配是保留字"，而是报缩进错误）。
- **最小复现**：`设 配 为 [1,2]` 然后 `打印 配[0]` → 缩进错误（误导）
- **期望**：①L0单字别名应仅在明确的语法上下文（如`配 模式:`）中才解析为关键字，独立作为变量名时应允许或给出明确的"保留字"错误提示；②误用报错应指向真实原因（"配是匹配的单字别名保留字"），而非误导性的缩进错误。
- **优先级**：P0（49字覆盖极广，报错误导性极强）

### L-120：标识符可被关键字序列完全切分

- **现象**：标识符并不等于关键字，但若整串可被关键字序列完全覆盖切分，词法器仍会切开它。实测：`捕获 异常 错误己:` ← `错误己`被切成`错误`(关键字)+`己`(K_SELF单字别名)→解析报错；而`捕获 异常 错误甲:`正常（`错误`+`甲`，`甲`不是关键字→整串不可全切分）。
- **影响面**：由保留字+单字别名组成的标识符（错误己/错误的/返回为等）。
- **最小复现**：`设 错误己 为 1` → 解析错误
- **期望**：词法分析器应优先尝试将整串识别为标识符，仅在整串确实是关键字（而非可切分组合）时才作为关键字处理。即"最大匹配"应优先于"关键字序列切分"。
- **优先级**：P1

### L-137：变量名以`尝试`开头被断开

- **现象**：局部变量名以关键字`尝试`开头时，后续下标赋值/读取在关键字边界被断开。`尝试["甲"] 为 1`报「期望'='、':'或'为'」（`尝试`被当关键字语句，`["甲"]...`悬空）。
- **影响面**：含"尝试"的变量名（尝试记录/尝试结果/尝试次数等）。
- **最小复现**：`设 尝试记录 为 {}` 然后 `尝试记录["甲"] 为 1` → 解析错误
- **期望**：同L-084/L-092，`尝试`仅在独立作为`尝试...捕获...`语句开头时才是关键字，作为变量名前缀时应视为标识符一部分。
- **优先级**：P0

### L-143：`作用域`是保留关键字

- **现象**：`作用域`是光明保留关键字，不能作为变量名。`设 作用域 为 {}`报「作用域是保留关键字，不能直接作为语句开头」。
- **影响面**：作用域是编程常用概念，宿主接线/插件系统中频繁使用。
- **最小复现**：`设 作用域 为 {}` → 报错
- **期望**：同L-101，评估是否需要保留。若需保留，给出明确替代建议；若无需保留，移除。
- **优先级**：P1

### 词法缺陷家族修复建议

这7个缺陷的根因是词法分析器的"关键字优先"策略。建议采用**"最大匹配+上下文感知"**策略：
1. 词法分析器应优先尝试将连续中文字符识别为一个标识符（最大匹配）。
2. 仅当整串精确等于关键字/保留字时，才作为关键字处理；包含关键字子串但整串不是关键字的，应作为标识符。
3. L0单字别名（49字）应仅在明确的语法上下文（如`配 模式:`、`若 条件:`）中才解析为关键字，独立作为变量名时应允许或给出明确的"保留字"错误。
4. 误用保留字时的错误提示应指向真实原因（"X是保留字/单字别名"），而非误导性的语法错误。

---

## 三、字符串与内置映射缺陷（3个，高优先）

### L-086：正则`{1,4}`花括号被破坏

- **现象**：光明字符串把`{1,4}`当重复语法（字符串乘法），`"[0-9a-f]{1,4}"`被编译破坏（SyntaxWarning: 'int' object is not callable），正则表达式无法使用花括号量词。
- **影响面**：所有使用正则表达式花括号量词（`{n}`/`{n,}`/`{n,m}`）的代码。
- **最小复现**：`设 模式 为 "[0-9a-f]{1,4}"` → 编译警告/错误
- **期望**：字符串字面量中的`{...}`应仅在明确的插值语法（如`{变量名}`）中才触发插值/重复，`{数字,数字}`形式应视为普通文本。或提供原始字符串语法（如`r"..."`）禁用插值。
- **优先级**：P1

### L-090：字符串原语裸名导入失败

- **现象**：字符串原语裸名（`转小写/转大写/去除空白/截取/开头/结尾/查找子串/码位/字符串包含/列表包含`）依赖编译器内置映射自动`_light_builtin.`前缀；被导入模块内部再import这些.light模块时，在运行.py内联路径下产生名称解析错乱（实测'int' object is not callable）。
- **影响面**：所有导入字符串处理模块后使用字符串原语的代码。
- **最小复现**：模块A `从 字符串处理 导入 转小写`，模块B `从 A 导入 某函数`，某函数内用`转小写` → 运行期错误
- **期望**：字符串原语应作为真正的内置函数（不依赖模块导入），或在导入时正确解析名称。建议将字符串原语统一到`_light_builtin`命名空间，并在所有导入路径下保持一致。
- **优先级**：P1

### L-087：`当前目录`内置映射名遮蔽

- **现象**：`当前目录`是编译器内置映射名（builtins `os.getcwd()`），编译器把源码里的`当前目录`无条件发射为`当前目录()`（带括号调用）；`从 会话格式 导入 当前目录`（字典常量）后使用该名报`'dict' object is not callable`。
- **影响面**：所有想使用`当前目录`作为变量名/字典键/导入名的代码。
- **最小复现**：`设 当前目录 为 "/tmp"` → 实际被编译为`当前目录()`调用
- **期望**：内置映射名应仅在未被用户显式赋值/导入时才使用内置映射；用户显式`设 当前目录 为 ...`或`从 X 导入 当前目录`后，应使用用户定义的值。或在用户遮蔽内置映射名时给出编译警告。
- **优先级**：P2

---

## 四、异常与错误处理缺陷（1个，中优先）

### L-093：跨模块异常位置锚定错误

- **现象**：异常从另一个.light模块抛出、且调用点未被`尝试`包住时，顶层异常格式化器给出的「位置块」不指向真实调用语句，而是恒锚定到函数体末尾附近。message本身正确，但行号/caret错配。实测两种落地：①真实抛点在函数第2条语句→位置块显示函数末尾收尾语句；②真实抛点是中段普通语句→位置块指向其后若干语句的尝试体。
- **影响面**：所有跨模块调用的异常调试，开发者无法根据错误位置快速定位真实抛点。
- **最小复现**：模块A `段落 抛错 接收: 抛出 "测试错误"`，模块B `从 A 导入 抛错` 然后 `抛错()` → 位置块指向A模块函数末尾而非抛出语句
- **期望**：异常格式化器的位置块应指向真实的抛出语句（或调用语句），而非函数体末尾。需检查py→light行号映射机制（L-061已修复enhanced_errors，但跨模块场景仍有问题）。
- **优先级**：P2

---

## 五、模块与导入缺陷（2个，中优先）

### L-094：入口目录遮蔽src同名模块（静默）

- **现象**：入口文件所在目录会遮蔽`src/`同名模块，且遮蔽失败是静默的。若`examples/<X>.light`存在，某用例`从 <X> 导入 ...`（本意导入`src/<X>.light`）会解析到examples下的同名文件（Python `sys.path[0]`=脚本目录），导出为空→运行期报`name '...' is not defined`，无任何警告。
- **影响面**：所有测试文件与src模块同名的场景，开发者困惑"为什么导入成功但函数不存在"。
- **最小复现**：`examples/工具.light`存在，另一测试`从 工具 导入 某函数` → 实际导入examples/工具.light而非src/工具.light
- **期望**：①模块解析应优先`src/`目录而非入口文件目录；②或在解析到非src目录的同名模块时给出编译警告；③或在导入的模块导出为空时给出提示"模块X导出为空，是否导入了错误的文件？"
- **优先级**：P2

### L-095：HMAC_SHA256非标准HMAC

- **现象**：`从 哈希 导入 HMAC_SHA256`实为`hashlib.pbkdf2_hmac('sha256', 文本, 密钥, 1)`单轮PBKDF2（消息尾部含`b'\x00\x00\x00\x01'`），非标准HMAC-SHA256——密钥未走ipad/opad内层处理；同一输入与Python标准库`hmac`输出不一致。
- **影响面**：所有需要标准HMAC-SHA256的场景（webhook验签/JWT/OAuth等），与外部系统对拍失败。
- **最小复现**：`HMAC_SHA256("消息", "密钥")` 与 Python `hmac.new("密钥", "消息", hashlib.sha256).hexdigest()` 对比 → 不一致
- **期望**：`HMAC_SHA256`应实现标准HMAC算法（RFC 2104），使用ipad/opad内层处理。或重命名为`PBKDF2_SHA256_单轮`以避免误导，并新增标准`HMAC_SHA256`。
- **优先级**：P1（安全相关，与外部系统对拍必须正确）

---

## 六、字典与对象访问缺陷（1个，中优先）

### L-096：字典属性访问不可用

- **现象**：字典普通值必须用下标`对象["键"]`，不能用`对象.键`（Python dict无属性访问）；方法调用也必须用`对象["方法"]()`。
- **影响面**：所有想使用`对象.键`语法的代码，尤其是从JS/TS迁移的代码（JS对象原生支持属性访问）。
- **最小复现**：`设 对象 为 {键: 1}` 然后 `打印 对象.键` → AttributeError
- **期望**：评估是否支持字典属性访问（`对象.键`等价于`对象["键"]`）。Python dict不支持属性访问，但可通过自定义dict子类（`class AttrDict(dict): __getattr__ = dict.get`）实现。建议在光明运行时层提供`AttrDict`或支持字典属性访问语法糖。
- **优先级**：P2（.light层已提供`属性获取`函数作为绕法）

---

## 七、函数与闭包缺陷（1个，中优先）

### L-097：if块内嵌套段落提升错误

- **现象**：在`如果`块内定义嵌套段落，编译器会把段落提升并导致后续代码被编译到函数外部（模块级），引发`name '服务' is not defined`（缩进跟踪错误）。
- **影响面**：所有在if/for/while等块内定义函数的代码。
- **最小复现**：
  ```
  如果 条件:
      段落 内部函数 接收:
          返回 1
      打印 内部函数()
  打印 "后续代码"  # ← 被编译到函数外部
  ```
- **期望**：块内定义的段落函数应正确嵌套在块作用域内，不应被提升到模块级。需检查编译器的段落函数提升逻辑（可能是将所有段落函数提升到模块级，忽略了块作用域）。
- **优先级**：P2

---

## 八、修复优先级总览

| 优先级 | 缺陷编号 | 数量 | 说明 |
|---|---|---|---|
| **P0（立即修复）** | L-084/L-092/L-119/L-137 | 4 | 词法家族核心，影响所有中文标识符，报错误导性强 |
| **P1（本批次修复）** | L-101/L-120/L-143/L-086/L-090/L-095 | 6 | 常用保留字/正则/字符串原语/HMAC安全 |
| **P2（后续修复）** | L-087/L-093/L-094/L-096/L-097 | 5 | 内置映射/异常位置/模块遮蔽/属性访问/嵌套段落 |

---

## 九、验收标准

1. **每个缺陷必须有最小复现用例**（`examples/test_LXXX.light`），修复前立红、修复后变绿。
2. **不破坏现有回归**：修复后 lightharness 全量CI（`python scripts/ci_test.py`）全绿（当前基线 pytest 311 passed + smoke 5/5）。
3. **向后兼容**：现有绕法（词法安全工具库/字典增强工具库等）必须继续可用，不得因修复而失效。
4. **词法家族修复需回归保护**：L-004/L-010/L-011/L-012/L-015等已修复的词法缺陷必须保持绿，防止修复回潮。
5. **交付物**：修复代码 + 最小复现用例 + 修复说明（根因+改动点+验证结果）。
6. **缺陷账状态更新**：修复后将对应条目的状态从「有绕法」改为「已修复（日期；验证绿）」。

---

## 附录A：D批次（L-068~L-074）修复状态验收清单

D批次于2026-09-09外发，7个缺陷的修复状态待验收确认：

| 编号 | 缺陷 | 领域 | 验收状态 | 备注 |
|---|---|---|---|---|
| L-068 | 类方法定义边界不清晰 | 编译器/语法 | 待验收 | 需确认是否实现类结束标记/编译器警告 |
| L-069 | `序列化JSON`不支持布尔值`真`/`假` | 序列化 | 待验收 | 需确认序列化JSON(真)是否输出true |
| L-070 | 默认参数不支持`{}`/`[]`字面量 | 编译器/语法 | 待验收 | 需确认`段落 甲 接收 选项 = {}`是否可用 |
| L-071 | 字典键访问无通用`.get()`安全原语 | 标准库 | 待验收 | 需确认`字典获取`是否已成为内置原语 |
| L-072 | `转字符串`对字典输出Python repr | 序列化/字符串化 | 待验收 | 需确认转字符串({a:1})是否输出友好格式 |
| L-073 | 运行时AttributeError/KeyError未翻译 | 运行时/错误提示 | 待验收 | 需确认AttributeError是否输出光明层可读提示 |
| L-074 | 类型判断函数`是字典`等非内置 | 标准库/内置 | 待验收 | 需确认`是字典`/`是字符串`/`是列表`是否已成为内置 |

**验收方法**：对每个缺陷编写最小复现用例，在当前light-merge编译器下运行，确认是否已修复。未修复的纳入本批次E批次一并修复。

---

## 附录B：.light层已有绕法工具（第18轮建设，修复后可逐步移除）

- `src/词法安全.light`：保留字表/是保留字/安全标识符/标识符可切分检查/绕法指南（覆盖L-084/L-092/L-101/L-119/L-120/L-137/L-143/L-087）
- `src/字符串增强.light`：码转字符/构造JSON字符串/转义花括号/安全正则匹配（覆盖L-086/L-089）
- `src/字典增强.light`：字典获取/属性获取（覆盖L-096/L-135）
- `src/函数增强.light`：闭包包装/造计数器（覆盖L-098）

编译器修复后，这些绕法工具可保留作为兼容层，或逐步从已有模块中移除绕法代码。
