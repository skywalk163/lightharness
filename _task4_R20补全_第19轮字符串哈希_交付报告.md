# 第19轮 任务4 交付报告（补全版）：字符串与哈希 L-086 / L-095

> 补记说明：本报告为**第20轮任务5 补齐**——第19轮任务4 的交付物（修复代码+复现用例+反跑脚本）齐备，但交付报告缺失。
> 依据：① `light-merge` 提交 `49319306` 与 `lightharness` 提交 `afdc26a` 的真实 diff、② 反跑脚本 `_antirun_t4_字符串哈希.py`、
> ③ 复现用例 `examples/test_L086.light` / `examples/test_L095.light`、④ 第20轮实测复核。补记日期：2026-09-14 ｜ 补记路：第20轮任务5

---

## 1. 任务定位

- 轮次：第19轮（编译器缺陷修复轮）
- 任务：任务4 —— 字符串与哈希
- 修复文件：
  - `light-merge/src/parser_expr.py`（L-086 字符串插值判定）
  - `light-merge/stdlib/哈希.py` + `lightharness/stdlib/哈希.py`（L-095 HMAC，**两份同步**）

## 2. L-086：正则 `{n,m}` 花括号量词被插值破坏

### 2.1 现象与根因

光明字符串字面量把 `{...}` 当插值（f-string）处理。`"[0-9a-f]{1,4}"` 中的 `{1,4}` 被交给表达式插值子解析，
子解析把 `1,4` 消费成「字面量调用」节点 `NUM(1) COMMA NUM(4)` → 生成 `1(4)` → 产物变成 `f"[0-9a-f]{1(4)}"`，
运行期 `'int' object is not callable`，**正则花括号量词彻底不可用**。

### 2.2 修复（两处，均在 `_parse_string_interpolation`）

1. **前置拦截**：`if re.match(r'^\d+\s*,\s*\d+$', expr_text):` → 该 `{...}` 直接作为**普通文本**片段 append，
   不进入插值解析。`{n,m}` 作为插值没有任何合法语义。
2. **兜底拒绝**：插值子解析返回节点后，若为 `FunctionCall` 且 `callee` 是 `NumberLiteral`/`StringLiteral`
   （即「字面量调用」畸形节点）→ 返回 `None`，让整串按普通文本处理。

### 2.3 反跑判据（C1 锚点）

```python
C1_OLD = "if re.match(r'^\\d+\\s*,\\s*\\d+$', expr_text):"
C1_NEW = "if re.match(r'^\\d+\\s*,\\s*\\d+NEVER$', expr_text):  # antirun C1"
```
→ 变异后 `test_L086` 立红，恢复后绿（`try/finally` + 字节级 sha256 校验）。
B 判据（直接调 `_parse_string_interpolation`）：`{甲}` 仍为插值、`[0-9a-f]{1,4}` 不触发插值、`{名:格式}` 格式插值不受影响 —— 3/3 PASS。

## 3. L-095：HMAC_SHA256 非标准实现

### 3.1 现象与根因（历史）

`HMAC_SHA256` 旧真身为 `hashlib.pbkdf2_hmac('sha256', text, key, 1)` —— **单轮 PBKDF2**：
密钥未走 ipad/opad 内层处理，输出与标准 HMAC-SHA256 不一致，无法与外部系统（openssl / GitHub `x-hub-signature-256`）对拍。

- 旧输出实证：`HMAC_SHA256("密钥","消息")` = `5b94b9380c64bfc99022c0999f08761768799b19d68ea1d8161a59bee91200d5`
  = `hashlib.pbkdf2_hmac('sha256', b'消息', b'密钥', 1).hex()`（第20轮已离线复核，**逐字节相同**）。
- 标准期望：`hmac.new('密钥'.encode(), '消息'.encode(), hashlib.sha256).hexdigest()`
  = `51785c6051c3d60392c441ada800662e64a027761ff96d71237c51da0eb25020`。

### 3.2 修复

```python
import hmac
...
def HMAC_SHA256(key: str, text: str, encoding: str = 'utf-8') -> str:
    return hmac.new(
        key.encode(encoding), text.encode(encoding), hashlib.sha256
    ).hexdigest()
```

- 委托 Python 标准库 `hmac`（本身就是 RFC 2104 实现：`H(K⊕opad, H(K⊕ipad, text))`，密钥 <64B 补 0x00、>64B 先 SHA256），
  从根上消除自实现偏差（密钥填充/轮次/字节序/编码全部由标准库保证）。
- **两份同步**：`light-merge/stdlib/哈希.py` 与 `lightharness/stdlib/哈希.py` 同改；反跑脚本的变异/恢复对两份一起做，
  并在终态校验两份 sha256 一致。

### 3.3 反跑判据（C2 锚点）

```python
C2_OLD = "    return hmac.new(\r\n        key.encode(encoding), text.encode(encoding), hashlib.sha256\r\n    ).hexdigest()"
C2_NEW = "    return hashlib.pbkdf2_hmac('sha256', text.encode(encoding), key.encode(encoding), 1).hex()  # antirun C2"
```
→ 变异回单轮 PBKDF2 后 `test_L095` 立红，恢复后绿；终态双份 sha256 一致校验 PASS。

### 3.4 第20轮复核（7/7 逐字节对拍）

| # | 用例 | 类别 | 与 Python `hmac.new(...).hexdigest()` |
|---|---|---|---|
| 1 | `HMAC_SHA256("密钥","消息")` | 标准 | ✅ 一致 |
| 2 | `HMAC_SHA256("key","The quick brown fox jumps over the lazy dog")` | RFC 经典 | ✅ 一致 |
| 3 | `HMAC_SHA256("","")` | 空密钥+空消息 | ✅ 一致 |
| 4 | `HMAC_SHA256("","消息")` | 空密钥 | ✅ 一致 |
| 5 | `HMAC_SHA256("密钥","")` | 空消息 | ✅ 一致 |
| 6 | `HMAC_SHA256("k"×100, "长密钥测试")` | 长密钥（>64B，走「先哈希密钥」分支） | ✅ 一致 |
| 7 | `HMAC_SHA256("中文密钥甲乙丙","中文消息丁戊己")` | 中文（多字节 UTF-8） | ✅ 一致 |

结论：`examples/test_L095.light` rc=0，可**从 EXPECT_RED 移除**。

### 3.5 ⚠️ 重要订正：第19轮「HMAC 部分修复」是**收口误判**

- 事实链：`lightharness` 提交 `afdc26a`（2026-09-14 02:57，第19轮任务4）已把 HMAC 改为 `hmac.new`；
  `stdlib/哈希.py` 此后**无任何未提交改动**（`git diff` 干净）；同一提交里 `examples/test_L095.light` 写入的期望值
  **就是标准值**（`51785c60…` / `f7bc83f4…`）。
- 但第19轮收口提交 `9724e99` 仍把 test_L095 登记为 `EXPECT_RED`「HMAC部分修复：双轮哈希后与标准RFC2104对拍仍不一致」。
- 该登记的「现象值」`5b94b938…` 恰恰是**修复前单轮 PBKDF2 的输出**（本轮已离线复算证实）。
- 结论：**代码侧无残留缺陷；上游收口状态登记滞后**（观察值取自修复前，或收口 CI 期间与 `_antirun_t4_字符串哈希.py`
  的原地变异窗口重叠 —— 该反跑会把 哈希.py 临时写成 pbkdf2，与全量测试**不可并发**）。
  处置：以「代码事实 > 登记状态」裁定为已修复，R20 任务6 清理该 EXPECT_RED 项并订正 docs 表述。

## 4. 复现用例

- `examples/test_L086.light`（rc=0）：`"[0-9a-f]{1,4}"` 不再触发插值，模式串原样可用。
- `examples/test_L095.light`（rc=0）：7 组对拍（标准 2 + 边界 5），期望值全部由 Python `hmac` 离线预计算。

## 5. 已知局限

1. `{n,m}` 拦截只认「纯数字,数字」形态（含空白）。若将来需要 `{变量,变量}` 作插值（无此合法语义）需另议。
2. HMAC 现完全依赖宿主 `hmac` 模块；**光明语言层仍无「原始字节哈希」原语**——
   即 .light 侧无法手写 HMAC（stdlib `SHA256` 仅返回 hex、无 bytes 接口，码点 >127 会被 UTF-8 多字节化，
   无法构造 64 字节 ipad/opad 原始块，见缺陷账 L-095「第13轮决策4」）。本修复是**把标准实现下沉到 stdlib (.py) 侧**，
   语言层缺口仍待 A9 处理。
3. 与 L-095 相关的「对外验签」链路（GitHub `x-hub-signature-256`）现在可直接用本函数对拍（此前需宿主侧二次校验）。
