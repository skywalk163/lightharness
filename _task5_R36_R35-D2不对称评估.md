# 任务5（R36）评估报告：R35-D2 词首并入 / 词尾切分不对称

> 评估性质：**只评估，不改代码**（铁律）。
> 评估对象：一元前缀字 `非` 在「词首并入」（R21 闸门3）与「词尾切分」（嵌入扫描 `scan_pos>0` 分支 + `_P0A_SUFFIX_SPLIT_SINGLE` 排除集）两处的不对称。
> 结论先行：**维持现状（方案B）**——不对称符合中文构词/语法，且对称化会破坏现有代码，风险 > 收益。

---

## 一、不对称的两侧逻辑定位（基于实际代码）

### 侧A：词首并入（`非` 在词首被并入复合名）
- 位置：`_tokenize_chinese_sequence` 的 R21 词首闸门3，当前 `src/lexer.py` **L2445–2456**。
- 判据：`_lead_kw not in _OPERATOR_KEYWORDS_NO_UNARY_PREFIX`（L2452，R36 预计算常量）
  且 `_lead_kw ∈ _P0A_UNARY_PREFIX_KW={'非'}`（L3309）时，若余部非已声明名字则整体并入 IDENTIFIER。
- 效果：`非X`（非空/非法/非零/非值/非负整数…）在词首**整体成 IDENTIFIER（复合名）**。

### 侧B：词尾切分（`非` 在词尾被切出）
- 位置：嵌入扫描的 `scan_pos > 0`（词中/词尾并入）分支，当前 `src/lexer.py` **L2872–2890**（其中 `and scan_pos > 0` 并入门槛见 L2948）；
  并入前提 `∉ _P0A_SUFFIX_SPLIT_SINGLE`。
- `_P0A_SUFFIX_SPLIT_SINGLE` 定义于 **L3342–3345**，其单字排除集来自
  `_P0A_OP | _OPERATOR_KEYWORDS | _P0A_NEVER_SPLIT | _AWAIT_KEYWORDS | _VALUE_LITERAL_KEYWORDS`
  中**单字**成员。
- 因 `非 ∈ _OPERATOR_KEYWORDS`（L37–41，逻辑运算符），故 `非` 被纳入词尾排除集 →
  **词尾 `非` 不并入，被切分为 `X` + `非`(KEYWORD)**。

### 不对称表现
| 位置 | `非` 行为 | 例 |
|---|---|---|
| 词首 | 并入复合名（IDENTIFIER） | `非空`→IDENTIFIER(非空) |
| 词尾 | 切出为独立 KEYWORD | `长度非`→`长度`(ID) + `非`(KW) |

---

## 二、全语料实证（676 个 .light 文件）

扫描 `lightharness/{examples,src}` + `light-merge/{examples,src/stdlib}` 共 676 文件：

**含 `非` 的 token 分布**（仅汉字 token）：
- IDENTIFIER·中（非在词中）：258 —— `非空非负`/`非零值` 等，前缀规则逐段并入，正常。
- IDENTIFIER·首（非在词首）：114 —— `非空`/`非法`/`非零` 等复合名，正常并入。
- IDENTIFIER·尾（非在词尾但仍整体成 ID）：6 —— 极少，均为 `非` 后随非汉字/下标等使其保持并入的边界形态。
- KEYWORD·首：41 —— `非` 作独立逻辑非前缀（如 `非 甲`）。

**`非` 在词尾（len>1）被切分的串：81 个去重 run，其中 50 个被切分为 `X`+`非`(KEYWORD)**。
逐一核验切分形态，典型样例：

```
长度非     -> 长度(ID) + 非(KW)       # 长度 非 0  = 长度≠0
而非       -> 而(ID) + 非(KW)         # X 而非 Y 连接词
是非       -> 是(ID) + 非(KW)
除非       -> 除(ID) + 非(KW)
位非       -> 位(ID) + 非(KW)
输出真实目录而非 -> 输出真实目录而(ID) + 非(KW)
常量而非   -> 常量(KW) + 而(ID) + 非(KW)
```

**关键判读**：这 50 例被切分全部是 `非` 作为**独立逻辑非运算符**（`长度 非 0`）或 **`而非` 连接词**的合法用法，**无任何一例是「以 非 结尾的复合名词被误切」**。即语料中 `非` 在词尾的真实语义 100% = 运算符/连接词，与「词首并入（复合名前缀）」形成**语义自洽**而非矛盾。

---

## 三、方案对比

### 方案A：同步修改词尾切分逻辑，使词尾 `非` 也并入
- 实施：把 `非` 从 `_P0A_SUFFIX_SPLIT_SINGLE` 排除（即不再因 `_OPERATOR_KEYWORDS` 把它当词尾排除字）。
- 成本：中（需改 L3342–3345 排除集构造 + 重跑全量反跑/pytest）。
- 风险（致命）：**会破坏现有代码**。`长度非0` 现 tokenize 为 `长度`(ID) `非`(KW) `0`(NUM) = `长度≠0`；
  若词尾并入，则变 `长度非`(ID) + `0` → `长度非` 成未定义名 → **NameError**。语料中 50 例均依赖当前切分，
  对称化后全部打红。
- 额外风险：动摇 `_P0A_SUFFIX_SPLIT_SINGLE` 五类语义排除完整性（L3328–3341 明文：排除集不得超出五类，
  否则 `_TRAILING_ALIAS_CLASS==F` 的 import 自校验 L3876 失效），需同步重审全部 5 类排除。

### 方案B：维持不对称（现状）
- 成本：零。
- 风险：仅理论上 `莫非`/`是非`/`无非`/`并非` 等以 `非` 结尾的**复合名词**若作标识符会被切分；
  但语料 676 文件**零命中**这类用法（现有 `是非`/`除非` 全为运算符/连接词语境）。属低概率静默风险。
- 收益：保留 `非` 在词尾作逻辑非运算符的正确行为，与 `非空` 在词首作复合名前缀的中文构词法一致。

---

## 四、明确建议

**建议：维持现状（方案B），不立项对称化。**

理由（逐条）：
1. **语义正确**：词首 `非` 是复合名前缀（非空/非法），词尾 `非` 是独立逻辑非/连接词（长度非0/而非），
   不对称恰是中文本构词与语法的真实反映，非缺陷。
2. **实证零回归**：全语料 676 文件，50 个词尾 `非` 串切分无一误伤，全为合法运算符用法。
3. **对称化代价高且危险**：方案A 会令现有 `长度 非 0` 等代码 NameError（50 例全红），并动摇
   `_P0A_SUFFIX_SPLIT_SINGLE` 五类排除完整性（import 自校验可能失败）。
4. **影响面低**：以 `非` 结尾的复合名词作标识符在语料与上游 deepseek-harness 复刻中均未出现，风险静默且无实例。

**后续监控方案**：
- 词法层已由 `tests/test_R30_CCW保留10条通用化_token.py` / `_OPERATOR_KEYWORDS` 相关 token 用例钉住
  `非` 等运算符的切分行为；保持该钉桩。
- 若未来用户报告 `莫非`/`是非` 类名字被切分致 NameError，再单独立项评估（届时可考虑「仅当 `非` 后随 `(`
  或特定后缀时并入」的窄规则，而非全局对称化）。

---

## 五、附：与任务3的边界
任务3（R36）在 L47 新增 `_OPERATOR_KEYWORDS_NO_UNARY_PREFIX` 预计算常量，仅替换侧A 闸门3 的集合差运算，
**未触碰**侧B 的 `_P0A_SUFFIX_SPLIT_SINGLE` 排除集，故本评估的不对称结构不受任务3影响，结论独立成立。
