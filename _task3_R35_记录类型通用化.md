# 任务3（R35）：记录类型 通用化替代 —— 结论：不可通用化，保留真护栏

> 日期：2026-09-16 ｜ 范围：light-merge/src/lexer.py + lightharness 验证
> 判据：G1 全语料 token 零变化 ∧ G2 边界 .light rc=0 ∧ G3 五种边界形态 token 一致

---

## 一、结论

| 项 | 结果 |
|---|---|
| 通用化是否成功 | **否** |
| `_P0A_MERGE_WHOLE` 中该条目 | **保留**（3 → 2 后仍含 `记录类型`） |
| 撤单条 G1 | **★ 打红 2 文件**（三条中最严重） |
| G3 五种形态 | **F2 函数名 / F5 传参位 FAIL** |
| 不可通用化的根因 | 与 `期望类型`/`参数类型` **结构不可分** |

---

## 二、根因分析

### 2.1 劈开机制

撤除条目后，`记录类型` 在**词尾**被 `类型` 劈开：

```
基线（含条目）: IDENTIFIER·记录类型
撤除后        : IDENTIFIER·记录  KEYWORD·类型
```

定位：嵌入扫描探测循环的**后缀位置规则**分支（lexer.py 原 L2840）：

```python
elif (sub_len > 1 and sub_kw not in self._P0A_OP
        and sub_kw not in self._P0A_SUFFIX_SPLIT_KW):   # ← 类型 在此集合内，分支不成立
    ...
```

`类型 ∈ _P0A_HARD_STMT`，而 `_P0A_SUFFIX_SPLIT_KW = (_P0A_HARD_STMT - {'函数'}) | {'定义'}`
⇒ `类型` 被排除在「多字关键字词中/词尾并入」之外 ⇒ 走 `embedded_found` ⇒ 分段输出。
`记录` 本身不是关键字，故 R21「词首关键字」三道闸门无从生效。

### 2.2 G1 打红的 2 个文件（重点分析）

| 文件 | 位置 | 形态 |
|---|---|---|
| `examples/test_R29_CCW精简边界.light` | L31 | `设 r7 为 记录类型(用户)` |
| `examples/test_R30_CCW通用化边界.light` | L19 | `设 r7 为 记录类型(用户)` |

两处**同一形态**：`记录类型` 后紧跟 `(`，即**函数调用语境**。
首个差异 token：

```
基线: … KEYWORD·为 IDENTIFIER·记录类型 LPAREN·( IDENTIFIER·用户 RPAREN·)
撤除: … KEYWORD·为 IDENTIFIER·记录  KEYWORD·类型 LPAREN·( IDENTIFIER·用户 RPAREN·)
```

（差异 token 数 134 / 33 是**位移放大**：首 token 一切就整体错位，真实差异就这 1 处。）

### 2.3 语料实态

`记录类型` 命中 **19 处 / 4 文件**：
`test_R29_CCW精简边界`(2)、`test_R30_CCW通用化边界`(2)、
`test_R30_零除幂次记录类型通用化_快速`(11)、`test_R32_MERGE_WHOLE精简边界`(4)。
形态为：段落名（`段落 记录类型 接收 目标:`）、调用（`记录类型(用户)`）、
F2/F5 合成边界（`设 r7 为 记录类型(用户)`）。

---

## 三、通用规则尝试与实测

### GR-3：词尾多字硬语句关键字在「非语句起始 ∨ 调用」语境并入

思路：R21 分支补一条「词尾」接受路径——词首非关键字时，若整串词尾恰为
`_P0A_SUFFIX_SPLIT_KW` 成员，则整体并入（与既有「词首关键字」路径并列）。

**G1 结果：打红 4 文件（全部是 bootstrap stdlib）**

| 文件 | 现状（切分） | 变更后（并入） |
|---|---|---|
| bootstrap/…/参数解析.light | 参数 + **定义** | 参数定义 |
| bootstrap/…/断言工具.light | 期望 + **类型** | 期望类型 |
| bootstrap/…/装饰器.light | 参数 + **类型** | 参数类型 |
| bootstrap/…/集合工具.light | 创建命名元组 + **类型** | 创建命名元组类型 |

**关键结论**：`记录类型` 与 `期望类型` / `参数类型` 是**同一个结构**
（非关键字前缀 + `类型`），词法层**不可区分**。
要么一起并入（打红 3 个 stdlib 文件的既有切分），要么都不并入（撤条目即打红 2 文件）。
⇒ 本条只能靠「精确整串」白名单区分，**不存在不误伤的通用规则**。

### GR-3c：收窄到仅「函数调用语境 `X(`」

思路：参照 `_emb_iscall` / 第一层 skip_verb 既有口径，只在整串后紧邻 `(` 时放行。
这恰好覆盖 G1 那 2 个文件的形态（`记录类型(用户)`）。

**G1 结果：0 变化 / 0 新错 ✅**（867 可比文件）
**G3 结果：F2 通过，F5 传参位仍 FAIL**

```
[F5 传参位] 基线 ( IDENTIFIER·记录类型 ,   →  变体 ( IDENTIFIER·记录 KEYWORD·类型 ,
```

按铁律「**三重判据全通过才可移除，任一失败即保留**」⇒ **保留**。

---

## 四、三重判据

| 判据 | 结果 |
|---|---|
| **G1 语料门** | 撤单条 **打红 2 文件**；GR-3 打红 4 文件；GR-3c 0 变化但 G3 不全过 |
| **G2 编译门** | `examples/test_R35_记录类型边界.light` **rc=0**（保留态） |
| **G3 边界门** | 撤单条 **F2/F5 FAIL**；GR-3c 下 **F5 仍 FAIL** |

⇒ **保留条目**。

---

## 五、为什么 GR-3c 的 G1=0 也不足以删除

1. **铁律优先**：任务书明确「三重判据必须全通过才可移除，任一失败即保留」，
   G3-F5 未过即不得移除。
2. **G3-F5 不是纯合成噪音**：`X类型` 在传参位的真实形态就是
   `def断言类型(对象,期望类型,消息='')`（断言工具.light）—— 传参位
   是这类复合名**真实存在**的语境；若放行「传参位 `X类型` 并入」，
   该文件同样会变。而 `记录类型` 与 `期望类型` 结构一致，放行前者必然放行后者。
3. 因此 GR-3c 只是**把 G1 的矛盾推迟到 G3**，并未真正消解。

---

## 六、交付物

- `light-merge/src/lexer.py` —— 该条目**保留**，注释补充 R35 两版通用化实测证据
  （GR-3 打红 4 文件 / GR-3c G1=0 但 G3-F5 FAIL）
- `lightharness/examples/test_R35_记录类型边界.light` —— 边界测试（rc=0），
  含「对照：期望类型 / 参数类型 复合名整词」两条，固化「结构不可分」的证据
- 证据脚本：`_r35_probe_记录类型diff.py`（2 文件逐 token 定位）、
  `_r35_probe_变体diff.py`（GR-3 的 4 文件定位）、`_r35_g1_engine.py`、`_r35_g3_边界门.py`
- 变体定义：`_r35_variants.py`（`gr3_only` / `gr3c_only` / `gr3c_mw_no3`）

---

## 七、遗留建议（不执行，仅记录）

1. 若接受 `期望类型`/`参数类型` 也整词并入（语义上更正确，当前切分疑似潜在缺陷），
   则 GR-3 可上线、`记录类型` 可删 —— 但须先验证 4 个 bootstrap stdlib 文件
   **编译并运行**通过，且作为**独立缺陷修复**立项，不混在本轮精简里。
2. 长远看，`类型` 既是类型声明关键字又是高频构词后缀，属于语言设计层面
   的歧义；可考虑把类型声明改用 `类型 X = …` 之外的形态（如 `定义类型 X`），
   从根上把 `类型` 从 `_P0A_HARD_STMT` 摘出。
