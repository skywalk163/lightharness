# 第18轮任务3 交付报告 —— 错误处理增强库

- **对标编号**：#118（光明语言工具库·缺陷修复轮，非上游复刻）
- **差异编号**：R18-D3
- **语言缺陷预分配**：L-155~L-156（**空缺顺延**，见下）
- **可写文件**（全部新增，零越界）：
  - `src/错误增强.light`（126 行 / 13 段落）
  - `examples/test_错误增强.light`（138 行 / 40 断言）
  - `_antirun_t3_错误增强.py`
  - `_task3_错误增强_交付报告.md`

---

## 一、定位

前 17 轮复刻中，错误处理绕法散落各模块（如 `抛出 {码:"X"}` 丢属性、跨模块异常靠 message 判断）。本轮建设标准化错误处理库，把 3 个错误处理缺陷（L-099/L-088/L-093）的绕法统一。

## 二、10 个函数与修复层级

| # | 函数 | 修复层级 | 对应缺陷 | 说明 |
|---|---|---|---|---|
| 1 | `造结构化错误(码,消息,详情=空)` | A | L-099 | 构造 {码,消息,详情,时间} 字典 |
| 2 | `抛出结构化错误(码,消息,详情=空)` | A | L-099 | 序列化 JSON 字符串再抛（避免 `抛出` 字典丢属性），并记最后异常 |
| 3 | `解析错误消息(消息字符串)` | B | L-099 | 从格式化消息还原结构化错误字典，非法返空 |
| 4 | `获取最后异常()` | B | L-088 | 返回最后抛出的结构化错误消息串（模块级字典承载，绕 L-098） |
| 5 | `错误码(异常)` | B | L-099 | 从错误对象/消息提取码 |
| 6 | `错误消息(异常)` | B | L-099 | 从错误对象/消息提取消息 |
| 7 | `尝试执行(函数,参数=空,默认返回值=空)` | B | — | 安全执行，异常返默认不传播 |
| 8 | `尝试执行详情(函数,参数=空)` | B | — | 返回 {成功,结果,错误} |
| 9 | `错误链(错误列表)` | B | — | 组合多错误为链消息 |
| 10 | `是错误(值)` | B | — | 判断值是否为错误对象/消息 |

## 三、验证

- **测试**：`examples/test_错误增强.light` 共 **40 断言**，覆盖 10 个函数正常/边界/异常三态，终行 `test_错误增强 PASS`，`rc=0`。
- **反跑**：`_antirun_t3_错误增强.py` **3/3 ALL OK**
  - A=正常：造结构化错误 → 抛出 → 捕获 → 解析，码/消息/详情 round-trip 一致
  - B=边界：尝试执行抛异常函数返回默认不传播 / 尝试执行详情 失败带错误 在干净运行全过
  - C=变异：抛出结构化错误抹掉码字段（序列化不含码），解析错误码断言（2a/5c）立红；恢复后 sha256 校验一致（71b470ae…）、回归 PASS

## 四、语言缺陷处理

- **新缺陷**：本轮为修复轮，**未遇真实新语言缺陷**。L-155~L-156 预分配编号**空缺顺延**。
- **被修复缺陷标注**（供路M回填缺陷账"修复状态"列）：
  - L-099「抛出字典/自定义类丢属性」→ 已通过 `造结构化错误`/`抛出结构化错误`/`解析错误消息` 修复（序列化 JSON 字符串承载，避免属性丢失）
  - L-088「捕获错误 as 变量不支持」→ 已通过 `获取最后异常` 修复（模块级字典承载最后异常消息）
  - L-093「跨模块异常位置锚定错误」→ C级：本轮提供 `错误码`/`错误消息` 提取工具 + 绕法指南（依赖错误消息 message 而非行号；跨模块位置错位时按 message 判定，与第14轮 L-093 登记一致）

## 五、两步踩坑（已就地解决，非新缺陷）

1. **L-092 关键字子串切碎**：测试段名 `返回42` 含 `返回` 关键字子串被词法切碎（"期望冒号，但得到数字"）。改段名为 `得42`。
2. **`1.0e309` 同类浮点上限（属数值库，已记入任务2报告）**未在本模块触发；本模块用模块级字典 `异常状态` 承载最后异常，绕开 L-098 闭包重绑（与任务1 `令牌状态` 同款绕法）。

## 六、纪律

- 仅新增本路互斥文件，未修改任何既有 `src/` 模块；未 commit，收口归路M。
- docs（对标清单/缺陷账/行为差异）由路M 统一回填。
- 交付文件：`src/错误增强.light`、`examples/test_错误增强.light`、`_antirun_t3_错误增强.py`、`_task3_错误增强_交付报告.md`。
