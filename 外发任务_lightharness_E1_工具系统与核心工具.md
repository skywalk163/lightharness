# 外发任务 E1：工具系统框架 + 核心工具

## 任务目标

在 lightharness 中实现工具调用系统，让 agent 从"纯对话"升级为"能调用工具做事"。实现工具注册/调度框架 + agent 循环工具调用 + 4 个核心工具（bash/读文件/写文件/搜索文件）。

参考 deepseek-harness `packages/core/tools`（工具注册框架）、`packages/shell`（bash 工具）、`packages/fs`（文件系统工具）的设计。

## 核心设计

### 1. 工具注册框架

在 `src/工具.light`（新建）中实现工具注册表：

```光明
类 工具注册表:
  属性 工具 等于 {}    # 工具名 → 工具定义字典
  段落 注册 接收 名字, 描述, 参数模式, 处理函数:
    工具[名字] 为 {"描述":描述,"参数模式":参数模式,"处理":处理函数}
  段落 获取定义 接收 名字:
    返回 工具[名字]
  段落 所有定义 接收:
    # 返回 OpenAI tools 格式的列表，供 API 调用时传入
    设 列表 为 []
    遍历 工具 的 每个 名, 定义:
      列表追加({"类型":"function","function":{"name":名,"description":定义["描述"],"parameters":定义["参数模式"]}})
    返回 列表
  段落 执行 接收 名字, 参数:
    设 定义 为 获取定义(名字)
    如果 定义 == 空:
      返回 {"错误":"未知工具: " + 名字}
    返回 定义["处理"](参数)
```

### 2. 核心工具实现

在 `src/工具/` 目录下新建 4 个工具模块：

#### bash 工具（`src/工具/bash.light`）
- 工具名：`bash`
- 描述：在 shell 中执行命令，返回 stdout 和 stderr
- 参数：`{"命令": "要执行的 shell 命令"}`
- 实现：调用 `运行命令`（光明内置或 subprocess），超时 30 秒，返回 `{"输出":"...","错误":"...","退出码":N}`
- 安全：工作目录限制在项目根目录（`HARNESS_ROOT`），不允许 `cd` 到上级目录

#### 读文件工具（`src/工具/读文件.light`）
- 工具名：`读文件`
- 描述：读取文件内容，支持行范围
- 参数：`{"路径":"文件路径","起始行":1,"结束行":100}`（起始行/结束行可选）
- 实现：读取文件，超出范围截断，大文件（>100KB）只返回前 1000 行 + 提示
- 安全：路径必须在 `HARNESS_ROOT` 下，拒绝 `..` 跨目录

#### 写文件工具（`src/工具/写文件.light`）
- 工具名：`写文件`
- 描述：写入文件内容（覆盖或新建）
- 参数：`{"路径":"文件路径","内容":"要写入的内容"}`
- 实现：创建父目录，写入文件，返回 `{"写入字节数":N,"路径":"..."}`
- 安全：路径必须在 `HARNESS_ROOT` 下；覆盖已有文件时返回提示（不阻止，但 agent 应知道）

#### 搜索文件工具（`src/工具/搜索文件.light`）
- 工具名：`搜索文件`
- 描述：按文件名或内容搜索文件
- 参数：`{"模式":"文件名|内容","查询":"搜索词","路径":"起始目录（可选，默认根目录）","最大结果":20}`
- 实现：文件名搜索用 glob/遍历；内容搜索用逐文件 grep（跳过 .git、node_modules、二进制文件）
- 返回：`{"结果":[{"路径":"...","匹配行":"...（内容搜索时）"}]}`

### 3. Agent 循环工具调用

修改 `src/agent.light`（或现有 agent 循环模块）：

1. **请求时传入工具定义**：调用 OpenAI API 时，把 `工具注册表.所有定义()` 作为 `tools` 参数传入
2. **检测工具调用**：API 返回 `tool_calls` 时，解析每个工具调用（id/name/arguments）
3. **执行工具**：调用 `工具注册表.执行(名字, 参数)`，获取结果
4. **结果回传**：把工具结果作为 `tool` 角色消息加入上下文，继续调用 API
5. **SSE 推送**：工具调用请求和结果都按契约 1 的格式推送到 SSE 流
6. **循环终止**：当 API 返回不再有 tool_calls 时，结束本轮

### 4. 中间件钩子机制

在 agent 循环中实现三个钩子点（契约 4）：
- `pre_tool_hooks`：工具执行前调用，返回 `{"批准":真/假,"需审批":真/假,"理由":""}`
- `post_tool_hooks`：工具执行后调用，可修剪结果
- `post_turn_hooks`：每轮结束后调用，可压缩上下文

E1 只实现钩子注册机制（空钩子列表），不实现具体逻辑（E4/E5 会注册）。

## 交付物

1. `src/工具.light` — 工具注册表类
2. `src/工具/bash.light` — bash 工具
3. `src/工具/读文件.light` — 读文件工具
4. `src/工具/写文件.light` — 写文件工具
5. `src/工具/搜索文件.light` — 搜索文件工具
6. `src/agent.light`（修改）— agent 循环加入工具调用 + 钩子机制
7. `examples/test_工具系统.light` — 工具注册/执行最小复现
8. `examples/test_agent工具调用.light` — agent 循环工具调用端到端（mock API）
9. 修复说明：根因 + 改动点 + 验证结果

## 验收标准

- [ ] `工具注册表` 可注册/查询/执行工具，未知工具返回错误
- [ ] 4 个核心工具均可正常执行，返回格式符合预期
- [ ] bash 工具执行 `echo hello` 返回 `{"输出":"hello\n","错误":"","退出码":0}`
- [ ] 读文件工具读取现有文件返回内容，不存在文件返回错误
- [ ] 写文件工具写入后文件存在且内容正确
- [ ] 搜索文件工具按文件名和内容搜索均返回正确结果
- [ ] agent 循环：mock API 返回 tool_calls → 执行工具 → 结果回传 → 继续循环 → 最终返回文本
- [ ] SSE 流中工具调用请求和结果按契约 1 格式推送
- [ ] 钩子机制：注册 pre_tool/post_tool/post_turn 钩子可被正确调用
- [ ] 安全：所有工具路径限制在 HARNESS_ROOT 下，`..` 跨目录被拒绝
- [ ] 全量回归不破坏（188 passed）

## 注意事项

- 工具参数模式（JSON Schema）必须正确，OpenAI API 会校验；参考 deepseek-harness `packages/core/tools` 的参数定义格式
- bash 工具的超时处理：用 `运行命令` 的超时参数，超时后返回 `{"错误":"命令超时（30秒）","退出码":-1}`
- 大文件读取：>100KB 或 >1000 行时截断，返回提示"文件过大，已截断"，避免爆上下文
- 搜索文件：内容搜索时跳过 `.git`、`node_modules`、`__pycache__`、二进制文件（.exe/.dll/.so/.pyc 等）
- agent 循环的工具调用轮次上限：最多 20 轮工具调用，超过后强制结束并返回"工具调用轮次超限"
- mock API 测试：用 `HARNESS_WEB_MOCK=1` 模式，mock 返回固定的 tool_calls 响应，验证工具调度逻辑
- 与现有 SSE 流的兼容：工具事件和文本事件在同一 SSE 流中，前端靠 `种类` 字段区分
