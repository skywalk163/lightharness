# 任务2（R34）：工具执行流水线完善 —— 交付报告

> 日期：2026-09-15 ｜ 轮次：第34轮 ｜ 优先级：P0
> 交付物：`src/工具执行.light`、`examples/test_工具执行.light`、本报告
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）`packages/tools/src/`

---

## 1. 做了什么

新增纯逻辑模块 `工具执行.light`（460 行 / 37 段落 / 56 导出），复刻五个核心面 + 一条执行流水线：

| 上游文件 | 复刻内容 | 本模块章节 |
|---|---|---|
| `registry.ts` | ToolRegistry：注册/注销/列举/插件归属 | §1 |
| `permission.ts` | 权限级别 public/internal/private + 可见性矩阵 + 同插件豁免 | §2 |
| `cancel.ts` | CancellationToken：取消/检查/清理回调 | §3 |
| `audit.ts` | AuditLog：5 类审计事件 + 环形缓冲 + 过滤查询 + 导出导入 | §4 |
| `ratelimit.ts` | RateLimiter：令牌桶 + 滑动窗口，三维度 | §5 |
| `executor.ts` | 执行流水线：权限 → 限流 → 取消 → 执行 → 审计 | §6 |

### 2.1 权限控制

- 级别：`public`（对三级可见）/ `internal`（对 internal、private 可见）/ `private`（仅 private 或同插件）。
- `权限判定` 返回 `{可见, 可调用, 错误码, 消息}`；拒绝错误码 `PERMISSION_DENIED`，工具不存在 `TOOL_NOT_FOUND`。
- **权限继承**：`注册插件工具(注册, 插件, 级别, 名称表)` —— 插件级登记一次，其下工具**全部继承该级别**。
- 列举按调用者级别过滤（`列出工具`）。

### 2.2 取消令牌

- `建取消令牌(信号)`：可绑定外部中止信号，信号中止时 `是否取消` 即真。
- `请求取消` 幂等（首次真、重复假，首次原因不被覆盖）；`检查取消` 已取消时抛 `TOOL_CANCELLED`。
- `注册清理 / 执行清理`：资源清理钩子**只可追加不可删**，保证清理不被绕过；仅在取消路径触发。

### 2.3 审计日志（追加-only）

- 事件类型：`tool_call_start / tool_call_end / tool_call_failed / permission_denied / rate_limited`，未知类型抛错。
- 字段：`序号, 类型, 工具名, 调用者, 参数摘要, 状态, 时刻, 耗时`；`序号` 全局单调。
- **追加-only**：对外只暴露 `记审计`；**没有任何改写/删除单条的接口**。
- 内存**环形缓冲**（默认容量 128）：超容量只丢弃最旧条目，不改写已有条目。
- 查询：按 工具名 / 调用者 / 状态 / 起始时刻 任意组合过滤。
- 持久化接口：`审计导出` / `审计导入`（整体替换快照，非单条改写）。

### 2.4 速率限制

- 两种策略：`策略令牌桶`（按时间比例补令牌，桶容量 = burstSize，为 0 时取 maxRequests）、
  `策略滑动窗`（保留窗口内命中时刻）。
- 三维度：**全局 / 每工具 / 每调用者**，任一维度超限即拒绝，返回命中的 `维度`。
- 超限返回 `RATE_LIMITED` + `重试毫秒`（令牌桶按缺口折算，滑动窗口按最早命中过期时间折算）。
- 配置：`maxRequests` / `windowMs` / `burstSize`，非法配置抛错。

### 2.5 执行流水线

`执行工具(执行器, 工具名, 调用者级别, 调用者, 参数, 调用者插件)` 严格按序：

1. **权限**（先于一切，不可绕过）—— 拒绝则记 `permission_denied` 审计并**不产生 start 审计**；
2. **限流** —— 拒绝则记 `rate_limited` 审计，消息带重试建议；
3. **取消** —— 执行前检测令牌，已取消直接返回 `cancelled`；
4. **执行** —— 处理函数签名 `处理(参数, 令牌)`，取消令牌注入上下文；
5. **审计** —— 成功记 `tool_call_end`；抛错记 `tool_call_failed`；消息含 `TOOL_CANCELLED` 归为 `cancelled` 并触发清理。

---

## 2. 验证结果

| 项 | 结果 |
|---|---|
| `运行.py examples/test_工具执行.light` | **rc=0，打印 `test_工具执行 PASS`** |
| 测试规模 | 240 行 / 85 条断言 / 6 组应抛校验 / 6 个用例段 |
| 反跑（值反 + 抛反） | 2/2 变异体**变红**，还原后**转绿** |
| 命名冲突扫描 | 与既有 `src/*.light` **0 冲突** |

测试用例段覆盖：
1. 可见性矩阵（9 条）+ 未知级别双向拒绝
2. 注册/重名/坏级别/空名抛错、插件权限继承、按级别列举、权限判定（含同插件豁免、`TOOL_NOT_FOUND`）、注销
3. 取消令牌：幂等、信号关联、清理钩子
4. 审计：环形缓冲丢弃最旧、序号单调、四种维度过滤、导出导入一致、未知类型抛错
5. 限流：滑动窗口超限/维度/窗口滑过恢复/每工具独立；令牌桶补令牌
6. 流水线：正常 / 权限拒绝（且无 start 审计）/ 工具抛错 / 工具内取消（触发清理）/ 执行前已取消 / 限流 / 时钟推进后恢复 / 时钟不可倒退 / 摘要裁剪

---

## 3. 反跑判据

脚本：`_antirun_r34_t123_反跑.py`（变异文件 `_red_tmp.light`，根目录，跑完即删）。

| 判据 | 变异 | 变异后 rc | 结论 |
|---|---|---|---|
| A 值反 | `长(具可见级别(级公开))` 期望 `3` → `2` | 1（红） | OK |
| B 抛反 | 重名注册 `注册工具(注册, "读文件", ...)` → 改名 `"读文件2"`（不再抛重名） | 1（红，报「应抛错未抛」） | OK |

还原后三个用例复跑全绿。反跑与用例执行严格串行。

---

## 4. 与上游的差异（纯逻辑取舍）

| 差异 | 原因 |
|---|---|
| 时间用**逻辑时钟**（`推时钟` 推进）而非 `Date.now()` | 墙钟会导致限流窗口不可重放、测试不稳定；逻辑时钟保证确定性 |
| 审计落内存环形缓冲 | 持久化属 I/O；已留 `审计导出/审计导入` 接口 |
| 权限模型简化为三级可见性矩阵 | 上游含审批流，审批属 I/O 与 UI，本轮只复刻判定面 |
| 无 `AbortSignal` 原生对象 | 用字典 `{中止, 原因}` 等价承载，语义一致 |
| 监听器为同步派发 | 解释器路径无异步 |

---

## 5. 铁律自检

- [x] 只创建 `工具执行.light`，未修改任何既有模块
- [x] 新增一律 `.light`，未用 Python 绕语言缺陷
- [x] 权限检查在执行**之前**完成（流水线第 1 步，拒绝后不进入限流/执行）
- [x] 取消令牌可传递到工具执行上下文（处理函数第二参）
- [x] 审计日志不可篡改（只暴露追加接口，环形缓冲只丢弃不改写）
- [x] 段落名一律 `具` 前缀，与既有模块 0 冲突

---

## 6. 命名冲突处置（并行 agent 场景）

初版与既有模块撞名 4 个，已全部改名，冲突归零：

| 原名 | 冲突对象 | 现名 |
|---|---|---|
| `注册工具` | `宿主工具.light` | `具注册工具` |
| `注销工具` | `宿主工具.light` | `具注销工具` |
| `记审计` | `宿主工具.light` | `具记审计` |
| `摘要` | `加密.light` | `具摘要` |

> 改名时对 `参数摘要` 做了保护（只替换独立出现的 `摘要`，未误伤字典键）。

## 7. 本轮踩到的坑

1. **`等待` 疑似关键字**：`设 等待 为 缺口 * 窗口 / 最大请求` 后写 `等待 + 1` → 解析错误「意外的标记 `+`」（列 42）。
   改名 `需补` 即通过。**写 .light 时避免用 `等待` 作变量名。**
2. **`\` 行续行不被支持**：跨行拼接字符串/参数须写成单行。
3. 环形缓冲初版用「复制整表再截尾」的写法冗余且易错，改成 `具截尾(表, n)` 直接返回新表。
