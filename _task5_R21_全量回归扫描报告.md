# 任务5 交付报告：全项目回归扫描与反跑（第21轮）

> 任务来源：`复刻_第21轮_任务prompt分发_词法确定性切词重构轮.md` 任务5
> 日期：2026-09-14 ｜ 本路仅新增脚本与报告，未修改编译器源码与既有测试 ✓
> 执行状态：任务1 已落盘（L-152 已修复）、任务2 未落盘——本报告按「重构态（任务1）/基线态对照」双口径输出

---

## 一、扫描框架（`_antirun_r21_t5_全量回归扫描.py`）

- **对比基线**：`git archive HEAD src` 提取重构前完整 src（保证依赖模块齐备）vs 工作区版 `light-merge/src`。
- **token 全量对比**：lightharness/examples 340 个 .light 逐文件双版 tokenize、序列级对比。
- **判据**：A=L-152 断裂态→修复态；B=表达式上下文（依赖任务2，软标注）；C=语句起始不变性；D=运算符不变性；E=token 全量对比；F=性能（阈值 20%）；G=编译运行快照（`--run`）。
- **恢复逻辑**：旧版运行于临时目录副本，全程不触碰工作区文件（无需 finally 恢复源码）。

## 二、扫描结果（重构态实测）

| 判据 | 结果 | 数据 |
|---|---|---|
| A=L-152 修复 | **PASS** | HEAD 断裂态成立（`函数值` → `内层返回`+`值`）；工作区已修复（`函数值` 整体成词，复现用例 rc=0） |
| B=表达式上下文 | **WAIT（待任务2）** | HEAD 断裂；工作区尚未实现上下文敏感切词（`去重占位([1,1,2])` 孤立形态仍切碎；定义登记路径已绿——见任务4 套件③） |
| C=语句起始不变 | **PASS** | `如果 条件 为 真:` → 如果=KEYWORD、条件=IDENTIFIER、为=KEYWORD、真=KEYWORD |
| D=运算符不变 | **PASS** | `设 结果 为 甲 与 乙` → `与`=KEYWORD |
| E=token 全量对比 | **1/340 差异，已归因** | 唯一差异文件 `test_R21_L152嵌套段落形参.light`（任务1 新增用例，HEAD 版切碎 vs 工作区整体成词）＝**预期变化（L-152 修复）**；意外回归 **0** |
| F=性能 | **PASS** | 旧版 4.66s → 新版 4.50s（**-3.5%**，未超 20% 阈值；多轮实测 -3.5%~+0.9% 区间） |
| G=编译运行快照（--run） | **抽样 60/341：57 绿 / 3 红，3 红全部归因为已知非回归**（全量 `--run` 留待路M收口复跑） | 详见第四节 |

## 三、回归分类结论

- **预期变化**：1 个文件（`test_R21_L152嵌套段落形参.light`，L-152 修复使 `函数值` 从切碎变整体）。
- **意外回归**：0 个。
- **任务2 预期变化预留**：任务2（上下文敏感切词）落地后，`去重占位`/`作用域匹配` 等孤立表达式形态将从切碎变整体，token 差异文件数将增加——均属预期，重跑本脚本即可自动对比与归因。

## 四、编译运行快照（抽样 60/341，含全部 R20/R21/L 系列用例）

```
快照: 57 绿 / 3 红
红用例: test_L101.light, test_L143.light, test_会话存储.light
```

3 红逐一归因（**均非第21轮词法重构回归**）：

1. `test_L101.light` / `test_L143.light`：第19轮任务3 保留字评估的**登记预期红**（`回调`/`作用域` 评估为保留，用例断言转绿条件未达成）——第19轮收口说明「20 复现用例 18 绿 + 2 预期红」即此二者。
2. `test_会话存储.light`：**环境残留**。断言「初始会话列表为空 实际=4」——存储根目录 `sessions/--G-dswork-…lightharness--/` 被 CLI 侧会话写入 6+ 个 `cli_*` 目录（时间戳早于本轮），测试与 CLI 共享根目录、无清理步骤，属**测试隔离缺陷**（运行期数据断言，无解析错误，与词法重构无关）。建议：登记测试隔离缺陷（L-153 候选），修复方向=测试改用独立临时根目录，或运行前清理 `cli_*` 残留；移交路M/测试维护方。

## 五、性能对比明细

- tokenize 全量 340 文件：HEAD 版与工作区版三轮交叉实测 **-3.5% ~ +0.9%**（任务1 重构未引入显著开销；预扫描按缩进层级扫描的开销被 `_scan_target_re` 跳跃扫描抵消）。
- 结论：性能可接受，无需第22轮专项优化。

## 六、验证命令

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"
python _antirun_r21_t5_全量回归扫描.py          # token+性能+探针判据（基线/重构态自适应）
python _antirun_r21_t5_全量回归扫描.py --run    # 追加 340 文件编译运行快照（约 9 分钟）
```

## 七、移交路M

1. 任务2 合并后重跑本脚本：E 差异文件清单将增加（预期变化），B 判据应由 WAIT 转 PASS。
2. 全量 CI（pytest + smoke）由路M按任务书第6节执行；本报告 G 快照为补充证据。
3. 性能基线数据（4.66s/4.50s）供第22轮性能轮（如需）对照。
