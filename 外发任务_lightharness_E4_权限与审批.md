# 外发任务 E4：权限与审批框架

## 任务目标

实现工具调用的权限控制与用户审批机制，让 agent 执行危险操作前需要用户确认。实现审批队列 + 权限预设 + Web 审批 UI。

参考 deepseek-harness `packages/interaction/user-approval`（审批框架）、`packages/interaction/permission-presets`（权限预设）、`packages/guard`（循环卫生/超时）的设计。

## 核心设计

### 1. 权限预设

在 `src/权限.light`（新建）中定义 4 种权限预设：

| 预设 | 行为 | 适用场景 |
|------|------|----------|
| `全放行` | 所有工具调用直接执行，不审批 | 信任环境/自动化 |
| `全拒绝` | 所有工具调用直接拒绝 | 只读模式/演示 |
| `按类别`（默认） | 危险类别需审批，安全类别放行 | 日常使用 |
| `自定义` | 用户配置每个工具的权限（放行/审批/拒绝） | 高级用户 |

"按类别"预设的工具分类：
- **安全（自动放行）**：读文件、搜索文件、bash 只读命令（ls/cat/grep/whoami/pwd/echo）
- **需审批**：写文件、bash 修改命令（rm/mv/cp/mkdir/chmod/git/npm install/pip install）、bash 网络命令（curl/wget/ssh）
- **拒绝**：bash 危险命令（rm -rf /、mkfs、dd if=/dev/zero、:(){ :|:& };:）

### 2. 审批队列

在 `src/审批.light`（新建）中实现审批队列：

```光明
类 审批管理器:
  属性 待审批 等于 {}    # 审批id → 审批请求
  属性 已处理 等于 {}    # 审批id → 审批结果
  
  段落 请求审批 接收 会话id, 轮次, 工具名, 参数:
    # 生成审批id，加入待审批队列，返回审批id
    # agent 循环暂停，等待审批结果
  
  段落 处理审批 接收 审批id, 批准, 理由:
    # 从待审批移除，加入已处理，设置结果
    # 唤醒等待的 agent 循环
  
  段落 等待结果 接收 审批id, 超时:
    # 轮询等待审批结果，超时后默认拒绝
    # 返回 {"批准":真/假,"理由":"..."}
  
  段落 待审批列表 接收 会话id:
    # 返回该会话的所有待审批项
```

### 3. 审批 API

在 `src/web服务器.light` 中新增路由：

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/approvals?session=xxx` | 待审批列表（SSE 轮询或 REST） |
| POST | `/api/approvals/:id` | 处理审批（body: `{"批准":true,"理由":""}`） |

审批事件也通过 SSE 流推送（契约 3）：
```
data: {"种类":"审批请求","审批id":"...","工具名":"bash","参数":{"命令":"rm -rf /"},"会话id":"...","轮次":3}
```

### 4. Agent 循环集成（pre_tool 钩子）

E1 实现了 pre_tool 钩子机制，E4 注册审批钩子：

```光明
# 在 agent 循环启动时注册
预工具钩子.追加(函数(工具名, 参数):
  设 权限 为 权限检查(工具名, 参数)
  如果 权限 == "放行":
    返回 {"批准":真,"需审批":假}
  如果 权限 == "拒绝":
    返回 {"批准":假,"需审批":假,"理由":"工具被权限策略拒绝: " + 工具名}
  # 需审批
  设 审批id 为 审批管理器.请求审批(会话id, 轮次, 工具名, 参数)
  设 结果 为 审批管理器.等待结果(审批id, 300)  # 5分钟超时
  返回 {"批准":结果["批准"],"需审批":真,"理由":结果["理由"]}
)
```

agent 循环在工具执行前调用所有 pre_tool 钩子，如果任一返回 `批准:假`，跳过工具执行，把拒绝理由作为工具结果回传给 LLM。

### 5. Web 审批 UI（E3 右侧面板扩展）

在右侧面板中新增"审批队列"区域：
- 待审批项卡片：工具名（红色标签）+ 参数摘要 + 危险等级 + 批准/拒绝按钮
- 批准按钮：绿色，点击调用 POST /api/approvals/:id `{"批准":true}`
- 拒绝按钮：红色，点击调用 POST /api/approvals/:id `{"批准":false,"理由":"..."}`（理由可选，弹窗输入）
- 已处理项：灰色显示结果（已批准/已拒绝 + 理由）
- 声音提示：新审批请求时播放提示音（可选，默认关）
- 自动聚焦：新审批请求时右侧面板自动展开并滚动到该项

### 6. 权限配置

在配置面板（已有）中新增"权限"标签页：
- 权限预设选择：全放行/按类别（默认）/全拒绝/自定义
- 自定义模式：每个工具的权限下拉（放行/审批/拒绝）
- 危险命令列表：可编辑的危险命令正则列表（匹配 bash 命令时拒绝）
- 审批超时：默认 300 秒，可配置

## 交付物

1. `src/权限.light` — 权限预设 + 工具分类 + 危险命令检测
2. `src/审批.light` — 审批队列 + 等待/处理机制
3. `src/web服务器.light`（修改）— 新增审批 API 路由 + SSE 审批事件
4. `src/agent.light`（修改）— 注册 pre_tool 审批钩子
5. `webui/app.js`（修改）— 审批事件处理 + 审批 UI 交互
6. `webui/style.css`（修改）— 审批卡片样式
7. `examples/test_权限.light` — 权限检查最小复现
8. `examples/test_审批.light` — 审批流程最小复现
9. 修复说明：改动点 + 验证结果

## 验收标准

- [ ] 权限预设：4 种预设可切换，"按类别"默认
- [ ] 工具分类：读文件/搜索自动放行，写文件/bash 修改命令需审批，危险命令拒绝
- [ ] 危险命令检测：`rm -rf /`、`mkfs` 等被直接拒绝，不进入审批
- [ ] 审批请求：需审批的工具调用生成审批项，SSE 推送审批请求事件
- [ ] 审批等待：agent 循环暂停等待审批，超时（300s）后默认拒绝
- [ ] 审批处理：POST /api/approvals/:id 批准/拒绝，agent 循环恢复执行
- [ ] 批准后执行：审批通过的工具正常执行，结果回传 LLM
- [ ] 拒绝后跳过：审批拒绝的工具不执行，拒绝理由作为工具结果回传 LLM
- [ ] 审批 UI：右侧面板显示待审批项，批准/拒绝按钮可点击
- [ ] 审批历史：已处理项显示结果和理由
- [ ] 全放行模式：所有工具直接执行，不生成审批项
- [ ] 全拒绝模式：所有工具直接拒绝，不生成审批项
- [ ] 权限配置：配置面板可切换预设，自定义模式可配置每个工具
- [ ] 并发安全：多个会话的审批项隔离，不混淆
- [ ] 全量回归不破坏（188 passed）

## 注意事项

- 与 E1 的 pre_tool 钩子机制（契约 4）严格对齐：E4 只注册钩子，不修改 agent 主循环
- 与 E3 的右侧面板协调：审批队列是右侧面板的一个区域（工具调用历史之上），E3 预留位置
- bash 命令分类：用正则匹配命令首词和参数，`git commit`/`npm install`/`pip install` 需审批，`git status`/`git log`/`ls`/`cat` 放行
- 危险命令列表：默认包含 `rm -rf /`、`mkfs`、`dd if=/dev/zero`、`fork bomb`（`:(){ :|:& };:`）、`chmod 777 /`、`> /dev/sda` 等，可配置
- 审批超时：默认 300 秒（5 分钟），超时后默认拒绝并返回"审批超时"，避免 agent 无限等待
- 审批 id 格式：`approval_时间戳_6位随机串`
- 审批队列持久化：待审批项在内存中（重启后丢失，agent 循环也会重启），不需要持久化
- 拒绝理由回传：工具结果格式 `{"错误":"用户拒绝: " + 理由}`，LLM 看到后可调整策略
- 权限配置存储：在 .env 中新增 `HARNESS_PERMISSION_MODE`（全放行/按类别/全拒绝/自定义），自定义配置存在 `data/permissions.json`
- 不阻塞 E1：如果 E4 未合入，E1 的 agent 循环用空 pre_tool 钩子（所有工具放行），功能正常
- 安全第一：默认预设是"按类别"，不是"全放行"，确保开箱即有基本安全保障
