# 任务5 R24 交付报告 —— 全量回归扫描与反跑

> 轮次：第 24 轮 · 阶段D 单字通用化 ｜ 任务：任务5（P1）
> 日期：2026-09-15 ｜ 对比基准：`ca5741a8`（第23轮，脚本自动上溯得到）
> 修改文件：无（仅扫描脚本 + 报告 + 证据）
> 判定：**ALL_OK**（新增红用例 0；预期变化 1；100+ 自由名全保护；性能提升）

---

## 一、扫描框架

| 项 | 值 |
|---|---|
| 扫描模式 | 8 个 glob（`lightharness/{examples,src,tests}` + `light-merge/{examples,stdlib,bootstrap,src,tests}`） |
| 语料规模 | **834 个 `.light`**（真实源 781 + 生成产物树 53） |
| 对比口径 | **内存态双 lexer**：断裂态 = `git show <基准>:src/lexer.py`；修复态 = 工作区 `src/lexer.py`（**不改任何文件**即可对拍） |
| 编译对比 | 原地替换 `src/lexer.py` 走真 CLI（含 **sha256 校验还原** + 「外部写入检测」：文件被并发改动则放弃替换、不覆盖他人改动） |
| 迭代 | token 全量 dump × 3 轮取中位数（性能）；语料内容**一次性快照**（防多 agent 并发写导致假红） |
| 脚本 | `lightharness/_antirun_r24_t5_全量回归扫描.py` |
| 证据 | `lightharness/_task5_R24_全量回归证据.json` |

---

## 二、判据结果一览

| 判据 | 内容 | 结果 |
|---|---|---|
| [A] | 基线自检（两侧各两次 dump 一致，证明确定性） | **PASS** |
| [B] | 真实源 token 变化 **1**（预期 1）｜生成树漂移 **4** | **PASS** |
| [C] | 性能：基准中位 **6.58s** vs 修复态中位 **6.00s** → **×1.098** | **PASS** |
| [D] | 100+ 自由名专项：**175 条**，全部整体成词且两侧一致 | **PASS** |
| [D2] | 分隔符契约串：**29 条**两侧逐字节一致 | **PASS** |
| [E] | 语句起始裸名/关键字切分：**11 例**两侧逐字节一致（约束 6） | **PASS** |
| [F] | 核心用例 **8 个**（L-084/L-092/L-119/L-120/L-152×2 + 2 探针）编译运行 rc/stdout 两侧一致 | **PASS** |
| [G] | R24 新增 **4 个** `.light` 用例修复态 rc=0 | **PASS** |
| [H] | 真实变化文件（`学生模块.light`）编译 A/B：`base_rc=0 → edit_rc=0`（不劣于基准） | **PASS** |
| [I] | OLD 路径（`deterministic=False`）真实源变化 **1**（遗留模式，不门控） | 记录 |

**判定：`ALL_OK`**

---

## 三、token 变化文件清单与差异分类（验证标准 1 / 2）

### 3.1 预期变化（1 个真实源文件）

| 文件 | 基线 | 修复态 | 定性 |
|---|---|---|---|
| `light-merge/examples/L2_wenyan/学生模块.light` | `ID(自之取平均分)` ×3 处 | `KW(自) KW(之) ID(取平均分)` ×3 处 | ✅ **任务1 P0 修复**（`自之X` 成员访问在表达式位置被吞并 → NameError） |

该文件编译 A/B：`base_rc=0 → edit_rc=0`，**修复态不劣于基准**（[H] PASS）。
`unexpected_real_files = []` —— **没有任何意外回归**。

### 3.2 生成产物树漂移（4 个，记录不门控）

`light-merge/bootstrap/release/stdlib/{CSV读写器, 对象池缓存, 文件系统, 断言工具}.light`

**定性依据（三重）**：
1. 这些 `.light` 内含 `class` / `def` / `self.`，是 **Python 转译产物**；实测 `运行.py` 在其第 1 行
   `class 断言失败异常(断言错误):` 即解析失败 ⇒ 本就不是可编译的光明程序，**不在任何测试断言路径**。
2. 项目自有测试 `tests/unit/test_lexer_compound_safe_alignment.py:80` 已明文定性为
   「**已损坏的生成产物**……怎么切都编不过」。
3. 漂移根因与改动语义自洽：`对象池缓存`/`文件系统`/`断言工具` 是第 4 道闸门对串内
   `在`/`于` 的修正（`键在self` → `键`+`在`+`self`，与源语料正确用法一致）；`CSV读写器` 是 CR1
   在 `在` 后新段把 `值或` 并入（见 §六遗留）。
   **对照**：**原始** `stdlib/断言工具.light`（`段落 断言大于 接收 …`）**零变化**——因 `断言大于`
   已被 `_scan_user_definitions` 预扫描登记，优先整体成词，不经闸门。

---

## 四、100+ 自由名专项验证（验证标准 3）

| 类别 | 条数 | 判据 | 结果 |
|---|---|---|---|
| `FREE_NAMES`（须整体成词） | **175** | 两侧均为**单** `IDENTIFIER` 且逐字节一致 | ✅ 0 条异常 |
| `CONTRACT_CASES`（多 token 契约串） | **29** | 两侧 token 序列逐字节一致 | ✅ 0 条异常 |

`CONTRACT_CASES` 里的是**本来就不是单标识符**的串（在基线上即如此），例如
`对于 → ID(对)+KW(于)`、`10的幂 → NUM(10)+ID(的幂)`、`自之姓名 → KW(自)+KW(之)+ID(姓名)`——
本轮只要求「两侧一致」，不要求成词（后者属口径 9 / 任务4）。

175 条自由名覆盖：任务书点名（`去除空格`/`索引`/`种类`/`阶乘`）、单字表真实护栏场景
（`对象`/`列表`/`加法`/`减法`/`乘法表`/`余数`/`除法`/`真实`/`空白`/`空值`/`真空`/`错误己`/`自己`/`爱己`/`知己`、
`输出结果`/`输出格式`/`输出块表`/`可打印`/`返回码`/`退出码`/`接收参数`/`非空块`/`导出事件表`/`整理模型消息`…）、
`的` 契约（`我的书`/`红色的花`/`小的大的`）、L-119/L-120 安全对照（`错误甲`/`文本长`/`首条表`/`占位A`/`伪终端会话`）、
内建名（`平方根`/`四舍五入`/`十六进制`/`序列化JSON`/`字典键列表`…）、
算法/系统/网络/具身域词（`冒泡排序`/`快速排序`/`梯度下降`/`注意力机制`/`状态估计`/`路径规划`/
`负载均衡`/`蓝绿部署`/`机械臂关节角`/`末端执行器`/`力矩传感器`…）。

---

## 五、性能对比（验证标准 4）

| 侧 | 3 轮耗时（s） | 中位（s） |
|---|---|---|
| 基准 `ca5741a8` | 8.47 / 6.58 / 5.82 | **6.58** |
| 修复态 | 7.62 / 5.94 / 6.00 | **6.00** |

**×1.098（变快）**，无回退；任务3 反跑独立测得 ×1.066，两次一致方向。

---

## 六、结论与遗留

### 6.1 结论（对照任务书验证标准）

| 验证标准 | 结论 |
|---|---|
| 1. 全量 examples 无新增红用例 | ✅ 意外回归 **0**；`unexpected_real_files = []` |
| 2. token 序列零变化或预期变化 | ✅ 真实源变化仅 1 个 = 任务1 预期修正 |
| 3. 100+ 自由名全部保护 | ✅ 175 条单标识符全一致 + 29 条契约串全一致 |
| 4. 性能提升或持平 | ✅ ×1.098 |
| 5. 反跑 ALL OK | ✅ `verdict = ALL_OK` |

### 6.2 遗留

1. **CR1 在生成树 `CSV读写器.light` 把 `值或` 并入**（`ID(值或)` 应为 `ID(值) KW(或)`）：
   仅影响该已损坏生成产物的 token 流，不在测试断言路径；收紧方案见任务3 报告 §七.1，
   建议第25轮在独立反跑矩阵保护下做。
2. **5 条既有 `tests/unit/**` pytest 打红**（基线 `ca5741a8` 即红，非本轮引入；
   其中 `test_自之X_仍是单B修好的样子` 本轮已修好，5 条为剩余量）：根因落在任务书禁改的
   `_EMBED` 区与任务4 互斥区，须单独立项。详见任务1 报告 §七.1。
3. **CI 现状**：gitea CI run 182（HEAD = `ca5741a8`）为 `failure`（41m57s），
   即本轮起点 CI 就是红的；`tests/ci_baseline_failures.txt`（12 条 e2e）已过期。
   故本轮口径为「**相对 HEAD 的新增打红 = 0**」。

---

## 七、交付物

- `lightharness/_antirun_r24_t5_全量回归扫描.py`（含 finally 恢复 + sha256 校验 + 外部写入检测）
- `lightharness/_task5_R24_全量回归扫描报告.md`（本文件）
- `lightharness/_task5_R24_全量回归证据.json`（`verdict = ALL_OK`）
