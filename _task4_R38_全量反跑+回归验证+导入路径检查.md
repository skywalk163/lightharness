# 任务4 交付报告 —— 全量反跑 + 回归验证 + 导入路径检查（第 38 轮）

> 轮次：第 38 轮 ｜ 任务：4（P1）｜ 日期：2026-09-16
> 执行基线：任务1（`src/工具展示.light`，单元测试 14 组全绿）+ 任务3（`examples/test_R38_集成测试.light`，11 组全绿）合并后的工作区
> 工具：`_antirun_r38_final.py`（新增，四模式：snapshot/compare/tests/imports，口径继承 `_antirun_r37_final.py`）
> 工作区 git 说明：执行时 `git rev-parse` 返回 not a git repository（用户手工管理 git），改动范围以文件系统证据 + tokenize 确定性论证。

## 一、§A 全量反跑（tokenize）

| 指标 | 结果 |
|----|----|
| 语料文件数 | 873（R37 终态 870 + 本轮新增 `src/工具展示.light`、`examples/test_R38_工具展示.light`、`examples/test_R38_集成测试.light`） |
| 解析错误文件 | 2 —— 与 R30 起登记的既有红完全一致（`light-merge/bootstrap/release/stdlib/集合.light`、`light-merge/examples/_test_nested_closure.light`） |
| 新增解析错误 / 自愈 | **0 / 0** ✅ |

**零回归论证**：本轮代码改动 = 3 个纯新增文件（工具展示模块 + 2 个测试），既有文件零改动（既有文件均不引用 工具展示，见 §三）；lexer 本轮零改动 → 既有文件 token 序列由确定性保证不变。基线快照（任务1 交付前 870 文件）与终态快照（873 文件）均已生成留档，新增 3 文件全部可解析。

## 二、§B 回归验证（`_antirun_r38_final.py tests`，347 文件全量）

**全量扫描：347 个 `examples/test_*.light`，rc=0 共 344，rc≠0 共 3 —— 逐一取证后有效回归为 0。**

| 红文件 | 红因取证 | 判定 |
|----|----|----|
| `test_L101.light` | 「回调」是保留关键字，解析期报错 | **EXPECT_RED 门禁内钉子**（`tests/test_回归.py` 登记「应 rc!=0」） |
| `test_L143.light` | 「作用域」是保留关键字，解析期报错 | **EXPECT_RED 门禁内钉子**（同上） |
| `test_会话存储.light` | 见下方专项取证 | **既有模块偶发 Windows 竞态 + 残留放大**，复跑 rc=0 |

**test_会话存储 专项取证**（红因链完整还原）：
1. 真红因：`src/会话存储.light:117` `os.replace(_index.json.tmp, _index.json)` 偶发 `WinError 5 拒绝访问`（Windows 目标文件被短暂占用的经典竞态；本轮并行交付期间文件活动频繁，触发率约 50%，间隔退避后稳定通过）。
2. 放大效应：os.replace 失败 → 测试中断 → 清理段（该测试自述"尽力而为，失败不阻断验收"）不执行 → `_e2_会话存储_tmp` 残留 4 条会话 → 下一次运行命中「初始会话列表为空 实际=4 期望=0」。
3. **与本轮改动无关的实证**：`test_会话存储.light`（mtime 2026-09-09，早于本轮）与 `src/会话存储.light` 本轮零改动（tokenize 零变化）；清除残留并退避后复跑 **rc=0**。
4. **移交建议**（非本路文件，不越权修改）：① `会话存储.light:117` 的 os.replace 加短退避重试（Windows 竞态标准处置）；② 该测试的根目录改为随机子目录（消除残留放大）；该测试 L217 起已有"尽力而为清理"的已知缺口（R37 报告 §四.3 已登记）。

重点用例（任务书 4.2）：`test_工具`、`test_工具执行`、`test_工具系统`、`test_代理循环`、`test_代理`、`test_代码运行时`、`test_R38_工具展示`（任务1 单元测试 14 组）、`test_R38_集成测试`（任务3，11 组）——**全部 rc=0**。

## 三、§C 导入路径检查（`_antirun_r38_final.py imports`，全语料 873 文件）

| 检查项 | 结果 |
|----|----|
| `从 工具展示 导入` | 引用方仅 2 个：`test_R38_工具展示.light`（任务1 单元测试）、`test_R38_集成测试.light`（任务3）——两者均 rc=0，导入符号与 `src/工具展示.light` 的 18 项导出逐一比对一致（模块自身在排除口径内） |
| 其他引用方 | **0** —— 与任务1「零修改适配层」设计一致：`src/工具.light`/`工具执行.light`/`代理循环.light`/`代码运行时.light` 均未改动、未反向引用（host-plane 不变的语义成立） |
| 同名冲突 | src/ 与 stdlib/ 下均无第二份 `工具展示.light`；`代理循环/代理运行时` 的 R37 重命名格局未被触碰 |
| 模块搜索路径 | `运行.py` 实测 **SRC > ROOT > STDLIB**（与 R37 任务1/任务4 报告的证伪结论一致），新增模块位于 src/，解析优先级正确 |
| `从 工具执行 导入` | 2 处（`test_R34_集成测试`、`test_工具执行`），均 rc=0，未受影响 |

## 四、交付物
- `lightharness/_antirun_r38_final.py`（反跑脚本：snapshot/compare/tests/imports 四模式）
- 本报告 `_task4_R38_全量反跑+回归验证+导入路径检查.md`
- 证据：873 文件快照（新增 ERR=0）、347 测试全量扫描（344 绿 + 3 红逐一取证）、导入路径全表

## 五、遗留登记（非本路文件，移交路M/后续轮次）
1. `会话存储.light:117` os.replace Windows 竞态 → 建议加退避重试（§二.4）。
2. `test_会话存储.light` 随机子目录根改造（消除残留放大，R37 起两次验证其放大效应）。
3. 上轮遗留：`test_R37_集成测试` 测试5 SKIP 待补（条件已具备）；`light-merge/stdlib/代理循环.light` 边界登记。
