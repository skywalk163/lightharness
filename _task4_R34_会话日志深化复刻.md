# 任务4（P0）会话事件日志完善 · 第 34 轮交付报告

> 上游基线：deepseek-harness 0.1.5-rc.2（`a305303422`）`packages/session/src/`
> 复刻对象：`event-log.ts` / `projection.ts` / `incremental-scan.ts` / `watermark.ts`
> 交付日期：2026-09-15 ｜ 执行器：商汤小浣熊 ｜ 编译/运行：`lightharness/运行.py`

---

## 一、交付物清单

| # | 文件 | 规模 | 说明 |
|---|---|---|---|
| 1 | `lightharness/src/会话日志深化.light` | 764 行 / 37 段落 / 7 条导出 | 核心模块，4 段复刻 + 整链装配 |
| 2 | `lightharness/examples/test_会话日志深化.light` | 608 行 / 155 断言 | 判据用例，`rc=0` 通过 38 组 |
| 3 | `lightharness/_task4_R34_会话日志深化复刻.md` | 本文 | 交付报告 |

未修改任何既有文件；未触碰 `src/会话日志增量.light`（R13 已交付的 delivery-accepted 链路）；未触碰 `light-merge`（本轮零编译器改动）。

---

## 二、§1 AppendOnly Log（`event-log.ts`）

| 要求 | 落地 |
|---|---|
| 日志结构 seq+类型+载荷+时间戳 | 事件 = `{"类型","序号","时间","数据"}`，日志 = `{"会话标识","格式版本","事件表","末尾序号","锁持有者",...}` |
| 严格 seq 递增 | `追加事件` 只用 `末尾序号 + 1`，不接受外部传入序号；`校验日志` 逐条断言 `序号 == 期望` |
| 并发安全 | `获取写锁/释放写锁`：持锁期间 `追加事件` 拒绝写入（判据 #14），错释放者抛错（#15） |
| 按 seq 范围读取 | `读取范围(日志, 起, 止)`，负起序号 / 越界止序号抛错（#17） |
| 全量读取 | `读取全部` |
| 不可篡改 | 追加返回 `记录副本`，读取返回 `记录表副本`（逐条深拷贝），外部改返回值不污染日志（判据 #40a） |
| seq 连续校验 | `校验日志` → `序号断点：期望 N，实际 M`（#18c/#30d） |
| 类型已知校验 | 不在 56 项 `已知类型集` → `类型未知`（#12） |
| 载荷必选键 | `载荷模式表` 14 类必选键，缺键 → `缺必选键`（#06d） |
| 多余键拒绝 | 不在必选键且不在 8 项 `可选载荷键集` → `多余键`（#11/#06f/#18d） |

设计取舍：**类型已知但无载荷模式**（如 `todo/write`、`hook/invoked`）视为透传，不校验键集合——否则上游允许携带任意数据的事件类型会被误判为多余键（首跑即暴露，已修正）。

---

## 三、§2 事件投影（`projection.ts`）

四类投影目标一次折叠完成：

- **会话元数据**：`标题` / `模型` / `轮次数` / `步次数` / `接受数` / `未处理数` / `孤儿结果数` / `孤儿裁决数`
- **消息列表**：`角色`（用户/助手/系统）+ `片段数`
- **工具调用记录**：`工具名` / `参数摘要` / `状态`（进行中→成功/失败）/ `结果序号`，结果按最近未完成任务匹配，无主结果计入 `孤儿结果数`
- **审批状态**：`等待` → `已批准/已拒绝/已取消`，无主裁决计入 `孤儿裁决数`

| 要求 | 落地 |
|---|---|
| 从事件流重建会话状态 | `投影全部(事件表, 起始序号)` 从零状态全量重建，两次重建逐字段一致（#22d/#22e） |
| 从指定 seq 开始投影 | `投影全部(..., 12)` 只投影 seq≥12（#22a），`投影增量` 用 `已投影至` 续投（#24a/#24c） |
| 不重复处理 | `投影增量` 重复调用 `本次新增 == 0`，投影数/消息表长度不变（#23a-#23d） |
| 状态一致性 / 不变量验证 | `校验投影`：消息序号递增、工具状态合法、审批状态合法、结果序号不倒挂、进度不越界（#25a/#25c/#25d） |

幂等性证明口径：`投影全部` 是纯函数（同输入同状态），`投影增量` 只推进 `序号 > 已投影至`，因此全量重建与分批续投收敛到同一状态（#24e 交叉验证 `接受数`）。

---

## 四、§3 增量扫描（`incremental-scan.ts`）

- **扫描起点**：`扫描点` 记录上次扫描到的序号，初值 `-1`（`判定扫描点` 允许 `-1` 哨兵或安全整数，首跑暴露过 `-1` 被安全整数校验误拒的问题，已单列判定函数）
- **只处理新增**：`序号 > 起点` 才处理；扫描完再扫返回空批、计数不重复累加（#27i/#27j）
- **分批 + 缺口容忍**：默认批量 64，可指定；序号缺口记录到 `问题表`（`序号缺口：起点+1→实际`）但不中断扫描（#30a/#30c）
- **进度持久化（内存持久化）**：`扫描快照` 导出纯数据、`从快照恢复` 重建扫描器，往返后快照再被外部篡改不影响已恢复对象（#28a-#28e）
- **崩溃恢复**：缺键 / 非法扫描点快照直接抛错拒绝恢复（#29）

判据 #27 覆盖：两批 4 条 → 扫描点 7；循环 5 条/批扫完 17 条 → 累计 9 条、扫描点 16、已扫描数 17、批次 5。

---

## 五、§4 水印折叠（`watermark.ts`）

- **水印 = 已接受的最大 seq**，初值 `-1` 表示未接受
- **增量扫描起点**：`下一扫描起点 = 水印 + 1`（水印 `-1` → 起点 0，#37a；水印 29 → 起点 30，#37b）
- **事件接受后更新**：只认 `delivery-accepted` 类型，格式版本必须等于水印头版本、`sessionId` 必须等于会话标识、`throughSeq` 必须小于自身序号，取最大值推进（#31/#32）
- **非幂等回退保护**：重复折叠同一事件不再推进、`接受数` 不重复累加（#32a-#32c）
- **跳过计数**：版本/会话不匹配记入 `跳过数` 而非报错（#33）
- **畸形拒绝**：格式版本非安全整数 → `malformed acceptance format version`；水印 `-0` / `>=` 自身序号 / 空会话标识 → `malformed acceptance watermark`（#34-#35）
- **水印校验**：`校验水印` 要求水印为 `-1` 或安全整数（含 -0 检查）、格式版本安全整数、会话标识非空、计数合法（#36a-#36f）

复用 R13 的 `delivery-accepted` 事件形状（`sessionId` / `sessionFormatVersion` / `throughSeq`），字段名与 R13 `会话日志增量.light` 完全对齐，两条链路可互认。

---

## 六、§5 整链装配

`整链回放(日志)` = `读取全部 → 投影全部(0) → 增量扫描 → 水印折叠`，输出 `状态 / 扫描器 / 水印 / 扫描总数`；`整链校验(回放, 日志)` 聚合 `校验日志 + 校验投影 + 校验扫描器 + 校验水印`。判据 #38 全链 17 事件回放通过（投影数 17、扫描点 16、扫描总数 17、水印 15、接受数 1），#39 对缺口日志与空回放均正确报 `有效=假`。

---

## 七、6 条铁律逐条对照

| # | 铁律 | 证据 |
|---|---|---|
| 1 | 只创建 `src/会话日志深化.light`，不修改其他模块 | 本轮仅新增 3 个文件（模块/判据/报告），`git status` 无既有文件改动 |
| 2 | 新增一律 .light，禁 Python 绕语言缺陷 | 模块与判据均为 .light；Python 只用于既有反跑工具调用，不参与业务逻辑 |
| 3 | AppendOnly Log 严格 seq 递增，不得修改已有事件 | `末尾序号+1` 唯一序号源；读取/追加均返回深拷贝（#09/#40a） |
| 4 | 事件投影幂等，重复投影结果一致 | 全量重建可重复（#22d/#22e）、增量重复调用零新增（#23a-#23d）、分批续投收敛（#24e） |
| 5 | 增量扫描记录进度，崩溃后可恢复 | 快照导出/恢复往返一致（#28）、非法快照拒收（#29）、扫完再扫零新增（#27i/#27j） |
| 6 | 水印必须安全整数，含 -0 检查 | 布尔/空/浮点/复合/负数/超限一律拒收（#02），字符串形态 `-0` / `-0.0` / 带空白 `-0` 全部拒收（#03a-#03c） |

---

## 八、验证结果

### 8.1 判据用例

```
cd lightharness && python 运行.py examples/test_会话日志深化.light
→ OK #00 … OK #40（38 组，含断言助手自检 #00）
→ 会话日志深化 判据通过：38 组      rc=0
```

155 个断言全部通过；判据内自建 `#00` 自检项先验证「断言助手真能报错」，避免断言被空转。

### 8.2 全量语料反跑（硬门槛：零变化）

```
cd lightharness && python _antirun_r30_t3_全量反跑.py snapshot lexer <out.json>
→ 语料文件数：863    CCW 条数：0    错误文件 2
```

| 项 | 结果 |
|---|---|
| 新增文件纳入语料 | `lightharness/src/会话日志深化.light` = 5515 token，无词法错误 |
| 基线文件 token 变化 | **仅 1 个**：`lightharness/examples/test_审批.light` —— R31 已交付的 `time.time → time.monotonic` 修复，非本轮引入 |
| 错误文件 | 仍为既有 2 个（`light-merge/bootstrap/release/stdlib/集合.light`、`light-merge/examples/_test_nested_closure.light`），未新增 |
| 本轮净影响 | 语料 851 → 863（纯新增），既有文件 token 序列零变化 |

### 8.3 既有会话链路回归

| 用例 | 结果 |
|---|---|
| `test_会话日志增量.light`（R13 delivery-accepted 链路） | rc=0，PASS |
| `test_会话深化.light` | rc=0，通过 |
| `test_会话格式.light` | rc=0，全部用例通过 |

### 8.4 light-merge 未改动

本轮未修改 `light-merge` 任何文件，编译器 pytest 结果与 R32 收口状态一致（496 passed / 7 failed，7 个失败已归因于 R23/R25/R26 与 R31 预期变化，与本轮无关）。

---

## 九、语言层发现与建议（供路 M 回填 docs）

1. **`-0` 与 `-0.0` 在光明语言中被折叠为 `0`**：`是浮点(-0.0) == 假`，因此「安全整数 + 含 -0 检查」必须走**字符串形态**判定（`-0` / `-0.0` / 含空白变体）。已用 `是负零` 单独实现字符串负零识别，并在 `判定安全整数` 的字符串分支中先判负零再判数值。建议登记为 `L-R34-01`，供上游对齐 `watermark.ts` 时参考。
2. **`断言` 是内建关键字**：会被编译器改写为 Python `assert (...)`，与自定义同名函数冲突（自定义 `断言` 函数被静默屏蔽，调用不报错也不校验）。判据文件已改用 `必等` 命名。**建议全局排查是否有自定义 `断言` 函数被屏蔽**——这是比 `-0` 更危险的静默失效模式。
3. **`追加事件` 的第四参是时间戳不是序号**：序号由日志内部单调分配。判据 #18 首跑因此误构造「序号缺口」场景（实际是两条 seq 连续事件），已改为手工注入事件来验证校验逻辑；公开 API 无法产生缺口这一性质本身已在 #30 断言中固化。
4. **`字符串包含` 返回布尔，不能直接参与字符串拼接**（光明有类型检查），需要 `转字符串` 后再拼；判据文件已按此写法。

---

## 十、未覆盖项与已知边界

1. **文件级持久化**：扫描进度目前只做内存快照（`扫描快照`/`从快照恢复`），未接磁盘 JSONL。任务书明确「至少内存持久化」，磁盘落盘留给 §5/§6 的集成层。
2. **真实并发**：`获取写锁/释放写锁` 是单进程内的协作式锁，未做跨进程互斥；光明语言当前无并发原语，无法在纯 .light 层实现。
3. **投影的 `replace` 类表面操作**：R13 已实现 `surfaceOp` 的 append/replace 分发，本轮投影只折叠事件本身，未按 `surfaceOp=replace` 做消息重写；如需对齐上游完整语义，建议在 §6 集成层合并。
4. **跨会话水印比较**：`折叠一个` 对会话标识不匹配记 `跳过数` 并跳过，不做「跨会话水印聚合」；上游该能力归属会话树层，不在本轮范围。
5. **语料基线快照文件**：`_antirun_r32_head_快照.json` / `_antirun_r32_cur_快照.json` 在 lightharness 根目录已不存在（R32 会话的 scratch 产物），本轮以 R30 基线快照 + 现场全量快照比对替代，结论不受影响。
