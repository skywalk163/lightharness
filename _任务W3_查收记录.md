# W3 主线查收记录（2026-09-09）

> 范围：外发任务_网页启动_W3_服务端路由（src/web服务器.light 独占 + examples/test_web路由.light）

## 一、查收结论：通过（含 1 处主线修复）

W3 交付的静态路由 / CORS / 认证挂载功能完整，实测全绿。查收中发现 1 个违反任务书判据的
缺陷，已由主线修复并补测试锁住。

## 二、独立验证清单（全部实测）

### 静态路由（真实 HTTP 联调，走 `运行Web服务器.light` 入口 + 真实 webui）
- GET / → 200，Content-Type text/html，1260B（= webui/index.html）✅
- GET /app.js → 200 application/javascript，11179B ✅
- GET /style.css → 200 text/css ✅
- GET /nope.js → 404 ✅（修复后）
- GET /favicon.ico → 404 ✅（修复后）

### 认证分支（HARNESS_WEB_TOKEN=test-token-123，body 走文件避免 shell 转义）
- POST /v1/chat/completions 无 token → 401 ✅
- 正确 X-Auth-Token 头 → 200 + OpenAI 兼容 JSON（mock 回复）✅
- 错误 token → 401 ✅
- URL query ?token= → 200 ✅
- GET /health 无 token → 200（白名单）✅
- 所有响应含 Access-Control-Allow-Origin: * ✅

### SSE 流式
- data: 角色块 → 内容增量块 → stop 块 → [DONE] 完整 ✅

### 测试三种模式（python 运行.py examples/test_web路由.light）
- 默认（随机令牌 + CORS 开）→ 退出码 0 ✅
- HARNESS_WEB_TOKEN=test-token（固定令牌）→ 退出码 0 ✅
- HARNESS_WEB_CORS=0（CORS 关）→ 退出码 0 ✅

### 白名单红线
- stdlib/HTTP服务端.light 零改动 ✅
- src/认证.light 只由 W2 建立（未被 W3 触碰）✅

## 三、主线修复（1 处）

**缺陷**：认证白名单只精确放行 GET /、/app.js、/style.css，导致未知静态路径在认证启用时
返回 401 而非 404（任务书判据要求 `GET /nope.js → 404`）。实际影响：浏览器自动请求的
`/favicon.ico` 也会被 401 拦下。

**修复**：`src/web服务器.light` 路由处理器认证段——白名单改为"所有 GET 放行，认证只保护
POST agent API"。理由：GET 无副作用，未知路径由静态路由 404 兜底；安全边界不变（仅本机
127.0.0.1 + token 保护对话 API）。

**测试锁定**：`examples/test_web路由.light` 阶段 3（认证启用）补断言 `S2_未知静态_404`
（GET /nope.js → 404）。修复后三种模式仍全绿。

## 四、遗留说明

- 测试清理临时目录时 `_w3_sess` 删除失败（会话根含嵌套子目录，WinError 145），测试已捕获
  不阻断；主线查收后已人工清理。
- 与 W4 的交接：`运行Web服务器.light` 已按契约 4 传 `界面目录`（W4 版本已在工作区），
  真实 webui 定位（脚本路径推导 → examples/../webui）实测有效。
