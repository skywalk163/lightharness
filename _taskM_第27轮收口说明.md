# 第27轮路M收口说明 —— CS表剩余16字逐字通用化

> 日期：2026-09-15 ｜ 方向：阶段F——CS表剩余16字逐字通用化（CS表完整通用化关键一步，16→2）
> 跨项目操作：light-merge（编译器规则扩展+修复）+ lightharness（验证+测试+反跑）
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）

---

## 一、本轮概述

第26轮将CS表从30条缩减为16条（删14字），剩余16字分为两类：
- **A类DUAL双位字（8字）**：乘/减/到/加/模/真/空/除 — 在排除集中，词首后随汉字须并入但正面规则不覆盖
- **B类F∩CS（8字）**：列/对/是/段/的/类/自/配 — 已在正面类别中，但G1打红说明实现有遗漏

本轮逐字通用化这16字，最终CS表从16条缩减为2条（列/类保留为真护栏），精简96%。

### 核心成果

| 指标 | 结果 |
|---|---|
| A类DUAL规则 | `_P0A_HEAD_MERGE_DUAL`（8字），词首+词尾双位置并入 |
| B类6字修复 | L-119下标边界+def边界协同+词首并入+rematch段 |
| CS表变化 | **16→2**（净删14字，精简96%） |
| 删除14字 | A类8（乘/减/到/加/模/真/空/除）+ B类6（对/是/段/的/自/配） |
| 保留2字 | 列/类（真护栏，行为互为镜像） |
| 全语料token | **0/839文件变化** |
| pytest | 113 passed（含保护表形态断言CS=2） |
| .light边界测试 | 3个 rc=0（正向/反向/混合） |

---

## 二、A类DUAL双位字通用化（任务1）

### 规则定义
- **`_P0A_HEAD_MERGE_DUAL`**（8字）：乘/减/到/加/模/真/空/除
- **词首并入**：scan_pos==0 + 单字关键字 ∈ DUAL + 后随汉字 → 并入标识符
- **词尾并入**：scan_pos+sub_len==len + 单字关键字 ∈ DUAL + 前导汉字 → 并入标识符
- **切分**：后随空白/数字/操作数 → 运算符/值字面量/范围结束符切分

### 关键区分
| 场景 | 后随 | 行为 | 示例 |
|---|---|---|---|
| 运算符词首并入 | 汉字 | 并入标识符 | 加法/乘法/减法 |
| 运算符独立切分 | 空白 | 切分为KEYWORD | 甲 加 乙 |
| 值字面量词首并入 | 汉字 | 并入标识符 | 真空/空格 |
| 值字面量独立切分 | 空白 | 切分为值字面量 | 返回 真 |
| 范围结束符词首并入 | 汉字 | 并入标识符 | 到位/到底 |
| 范围结束符独立切分 | 数字 | 切分为范围结束符 | 从1到10 |

### 实现站点
- 第一层skip_verb（lexer.py:2553附近）
- 嵌入式扫描三处（探测/输出/rematch），与B类HM规则同口径
- DUAL词尾并入：探测/输出循环词尾分支

### 与_P0A_NEVER_SPLIT的互补
模/到虽入DUAL类别，但_NEVER_SPLIT语义是词内切分禁止（模型/模块/1至10），DUAL只覆盖词首/词尾并入，两者保护面互补不重叠。

---

## 三、B类F∩CS八字G1打红修复（任务2）

### 删除6字（对/是/段/的/自/配）

| 字 | G1打红场景 | 修复方案 |
|---|---|---|
| 对 | `对[0]`下标→KEYWORD(对)（4文件） | L-119嵌入扫描等价：独立单字+后随`[`→不标记内嵌 |
| 是 | `def是否夏令时`→def+是否夏令时分离（2文件） | 词首并入 + def边界协同 |
| 段 | `段[x]`下标→KEYWORD(段)（9文件） | L-119嵌入扫描等价 |
| 的 | `应失败的长度`→应失败+的长度（5文件） | 词首并入分支（词中由整串路径覆盖） |
| 自 | `自定义表`→切成 自+定义+表（1文件） | 词首并入（rematch段） |
| 配 | `配[0]`下标（4文件） | L-119嵌入扫描等价 |

### L-119嵌入扫描等价
独立单字（`sub_len==len(full_identifier)`）+ 后随`[` → 不标记内嵌，与第一层skip_verb同口径。覆盖段[1]/配[0]/对[0]等下标访问场景。

### 保留2字（列/类）

| 字 | 场景 | 基线行为 | 撤CS后行为 | 不可删原因 |
|---|---|---|---|---|
| 列 | `对于 元素 在 序列:` | IDENTIFIER(序)+KEYWORD(列)+COLON | 列∈F词尾并入→`序列`整词 | 词尾+前导汉字→**切出**KEYWORD，与R25词尾F并入方向相反；列∈F不可加入词尾排除集（否则HM词首`列数`打红） |
| 类 | `类 独立类:` | IDENTIFIER(独立类)整词 | 切成`独立+类:` | 基线整词依赖CS与类声明上下文协同；撤CS后词尾并入被`独立+冒号`边界打断 |

**列/类行为互为镜像**：列=词尾切出、类=词尾整词，说明"词尾+CS残部字"的token行为由上下文（前导关键字"在"/"类"声明、后随冒号、user_definitions共同决定），单条通用条件无法同时刻画，需后续轮次以`_seg_after_sep`/user_definitions协同口径逐字定位。

---

## 四、CS表逐条验证删除（任务3）

### 删除14字（G1∧G2∧G3全通过）
- A类8字：乘/减/到/加/模/真/空/除（由DUAL规则覆盖）
- B类6字：对/是/段/的/自/配（由L-119等价+词首并入+def边界协同覆盖）

### 保留2字（真护栏）
- 列：词尾切出，与R25词尾F并入方向相反
- 类：词尾整词，依赖类声明上下文协同

### 自校验断言更新
| 断言 | 旧口径（R26） | 新口径（R27） |
|---|---|---|
| ① | CS==(F−TAM)∪DUAL（16字） | CS⊆F ∧ CS∩DUAL==∅（残部列/类） |
| ② | _TRAILING_ALIAS_CLASS==F | 不变（仍43字） |
| ③（新） | — | HM−CS==_R26_CS_REMOVED∪_R27_CS_REMOVED |

---

## 五、边界测试与反跑（任务4）

- **正向.light**：`test_R27_词首并入正向.light` rc=0（16字×2名=32标识符）
- **反向.light**：`test_R27_词首并入反向.light` rc=0（运算符5/值字面量3/范围/硬语句/下标）
- **混合.light**：`test_R27_词首并入混合.light` rc=0（9组）
- **pytest**：`test_R27_CS表16字词首并入_token.py` 113 passed（含保护表形态断言CS=2）
- **全量反跑**：ALL OK，839文件token零变化

---

## 六、排除集精简与联动评估（任务5）

### 排除集
- **方案A落地**：新增`_P0A_HEAD_MERGE_DUAL`类别，`_P0A_HEAD_SPLIT_SINGLE`保持42字不变
- **词尾排除集**：`_P0A_SUFFIX_SPLIT_SINGLE`（21字）不需要同步修改（派生自五类词法依据，与CS无耦合）

### 联动保护表（五张全部保留）
| 保护表 | 条数 | 结论 | 理由 |
|---|---|---|---|
| CCW | 37 | 保留 | 与单字CS递归正交，R22已逐条验证保留 |
| _EMBED_MAX_MATCH_KEYWORDS | 3 | 保留 | 与单字CS递归无关 |
| OPERATOR_VERBS | 19 | 保留 | 运算符切分职责不变 |
| _P0A_MERGE_WHOLE | 9 | 保留 | 2-4字整词，与单字词首并入不重复 |
| _P0A_NEVER_SPLIT | 4 | 保留 | 词内切分禁止，与DUAL词首并入互补 |

### _skip_compound_safe_and_match（非死代码，保留）
任务书5.3预设CS清零后该函数成为死代码可删除。实测任务1+2主动扩展递归入口条件为`CS ∪ DUAL ∪ B类`三集合并集，CS清零后DUAL/B类仍非空，递归仍被调用。**不得按任务书原预设删除**。

---

## 七、验证结果

### 7.1 编译器方向反跑
- T3 CS表删除：ALL OK（839文件token零变化）
- T4 全量反跑：ALL OK（839文件token零变化）

### 7.2 全量CI
- **pytest**：待确认（CI运行中）
- **smoke**：待确认

### 7.3 数据集方向
- 第25轮数据集（2200条）不受本轮影响（编译器修改为词法层规则扩展，全语料token零变化）

---

## 八、路M修正与决策记录

1. **CS表未完全清零**：任务书目标"CS表清零"，实际16→2（列/类保留）。路M认可——列/类的基线行为互为镜像（列=词尾切出、类=词尾整词），单条通用条件无法同时刻画，需后续轮次以`_seg_after_sep`/user_definitions协同口径逐字定位。CS表从第22轮52条→第27轮2条，已精简96%。

2. **_skip_compound_safe_and_match非死代码**：任务书5.3预设CS清零后该函数成为死代码可删除。实测任务1+2扩展递归入口为CS∪DUAL∪B类，CS清零后DUAL/B类仍非空。路M认可保留。

3. **排除集保持42字**：任务书预期排除集精简，实际采用方案A（新增DUAL类别，排除集不变）。路M认可——方案A语义更清晰（运算符/值字面量/范围仍需在非词首位置切分），排除集的语义不依赖CS表存在。

4. **联动保护表五张全部保留**：CCW/_EMBED/OPERATOR_VERBS/_P0A_MERGE_WHOLE/_P0A_NEVER_SPLIT评估后全部保留。路M认可——这些表与单字CS递归正交或互补，CS清零不影响其语义。

---

## 九、已知问题与后续方向

### 9.1 CS表剩余2字（列/类）的进一步通用化
- 列：`对于 元素 在 序列:`中列在词尾切出KEYWORD，与R25词尾F并入方向相反。需独立刻画"词尾+前导汉字→切出KEYWORD"的反向规则。
- 类：`类 独立类:`中独立类整词依赖CS与类声明上下文协同。需`_seg_after_sep`/user_definitions协同口径。
- 两字行为互为镜像，单条通用条件无法同时刻画，需后续轮次逐字定位。

### 9.2 持续存在的问题
- test_审批.light flaky（R20-C）：从第20轮持续存在，`等待结果`用`time.time()`而非`time.monotonic()`，非编译器缺陷。

### 9.3 第28轮方向建议
1. **CS表剩余2字（列/类）逐字通用化**（推荐）：CS表完整清零的最后一步
2. **CCW 37条分批精简**
3. **test_审批.light flaky修复**
4. **继续上游复刻**

---

## 十、docs三件回填

1. **对标清单**（docs/功能对标/对标清单.json）：追加#145（CS表16字逐字通用化，CS表16→2），总数145条。
2. **缺陷账**（docs/功能对标/语言缺陷账.md）：本轮无新发现缺陷，未更新。
3. **行为差异清单**（docs/功能对标/行为差异清单.md）：追加R27-D1（CS表16字逐字通用化，CS表16→2，剩余列/类，订正R26-D1）、R27-D2（A类DUAL双位字词首/词尾双位置并入规则）、R27-D3（递归入口扩展与排除集方案A）。

---

## 十一、git提交

### light-merge项目（1个提交）
- 待提交：src/lexer.py（156+/31-，DUAL规则+B类修复+CS表16→2+自校验断言更新）

### lightharness项目（6路分路提交 + 收口提交）
- 任务1：A类DUAL双位字词首并入规则设计+实现
- 任务2：B类F∩CS八字G1打红场景定位与修复
- 任务3：CS表16字逐条验证删除（16→2）
- 任务4：边界测试套件+全量反跑
- 任务5：排除集精简+联动保护表评估+性能
- 任务6：质量审查+docs回填
- 收口提交：docs三件 + 质量审查报告 + 收口说明
