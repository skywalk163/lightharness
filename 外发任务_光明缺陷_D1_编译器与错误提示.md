# 外发任务 D1：编译器与错误提示（L-068 + L-073）

## 任务目标

修复光明语言编译器/运行时的两个缺陷：类方法定义边界不清晰（L-068）、运行时错误未翻译为光明层可读提示（L-073）。

---

## 缺陷 L-068：类方法定义边界不清晰，方法易写在类外

### 现象

光明的类方法与模块级函数语法完全相同（均为 `段落 XXX 接收 ...`），仅靠缩进区分。类的最后一个方法与后续模块级函数之间无明确结束标记。新增类方法时若位置判断失误（放在类外、或嵌套进前一个方法体），**编译不报错**，运行期报 `'类名' object has no attribute '方法名'`，且错误无光明源码行号指引，极难定位。

### 最小复现

```光明
类 测试类:
  属性 值 等于 0
  段落 方法A 接收:
    返回 1

段落 方法B 接收:    # ← 误写在类外（缩进与类同级）
  返回 2

设 实例 为 新建 测试类()
打印 实例.方法B()   # ← 运行期：'测试类' object has no attribute '方法B'
```

### 期望能力（任选其一或组合）

1. **类结束标记**：支持 `类结束`/`结束类` 显式标记类定义结束（类似 `结束如果`/`结束遍历`），编译器据此校验方法归属。
2. **编译器警告**：检测到「类定义区域外、但缩进与类方法相同的 `段落`」时，给出编译警告「该段落不在任何类内，是否漏写缩进？方法名与某类同名时尤需注意」。
3. **运行期错误增强**：AttributeError 附带光明源码行号与类名（与 L-061 行号映射联动）。

### 验收标准

- [ ] 最小复现用例 `examples/test_L068.light`：修复前运行期 AttributeError，修复后编译器给出明确警告或类结束标记可用。
- [ ] 现有类定义全部正常编译运行（全量回归不破坏）。
- [ ] 若实现类结束标记，`类结束` 标记后的段落正确归为模块级函数。

---

## 缺陷 L-073：运行时 AttributeError/KeyError 未翻译为光明层可读提示

### 现象

L-061 已修复 `enhanced_errors` 的 message/行号归因，但运行期以下错误仍是 Python 层原始报错，未翻译成光明层可读提示：

- **AttributeError**：`'Web服务端' object has no attribute '处理获取配置'` → 应翻译为「类 Web服务端 缺少方法/属性 处理获取配置，请检查方法定义位置」
- **KeyError**：`'消息'` → 应翻译为「字典缺少键 '消息'」（当前 KeyError 只打印键名，无上下文）

用户看到 Python 风格的错误消息，需自行理解 `object has no attribute` 对应"类缺少方法"、`'键'` 对应"字典缺键"。

### 最小复现

```光明
类 测试类:
  段落 方法A 接收:
    返回 1

设 实例 为 新建 测试类()
打印 实例.不存在的方法()   # ← Python: '测试类' object has no attribute '不存在的方法'
                              # 期望光明: 类 测试类 缺少方法 不存在的方法（src/xxx.light:行号）

设 字典 为 {"a": 1}
打印 字典["b"]              # ← Python: 'b'
                              # 期望光明: 字典缺少键 'b'（src/xxx.light:行号）
```

### 期望能力

运行期错误翻译为光明层可读提示：

| Python 原始错误 | 光明层期望提示 |
|-----------------|---------------|
| `'X' object has no attribute 'Y'` | 类 X 缺少方法/属性 Y（文件:行号） |
| `'Y'`（KeyError） | 字典缺少键 'Y'（文件:行号） |
| `'Y'`（IndexError） | 列表索引越界: Y（文件:行号） |
| `unsupported operand type(s) for +: 'NoneType' and 'str'` | 类型错误: 不能对 空值 与 字符串 做 + 运算（文件:行号） |

附带光明源码行号（复用 L-061 的 py→light 行映射机制）。

### 验收标准

- [ ] 最小复现用例 `examples/test_L073.light`：AttributeError 和 KeyError 均输出光明层可读提示 + 行号。
- [ ] L-061 已修复的 message/行号归因不回归。
- [ ] 全量回归不破坏（错误提示格式变化不应影响依赖原始错误消息的测试）。
- [ ] 错误提示中英文一致（项目用中文提示）。

---

## 交付物

1. 修复代码（编译器/运行时）
2. `examples/test_L068.light` + `examples/test_L073.light` 最小复现用例
3. 修复说明：根因 + 改动点 + 验证结果（含全量回归结果）
4. 更新 `docs/功能对标/语言缺陷账.md` 中 L-068/L-073 的状态为「已修复」

## 注意事项

- L-061（`enhanced_errors` 行号映射）的修复代码在 `src/enhanced_errors.py` / `src/error_formatter.py`，L-073 可复用其 py→light 行映射机制。
- 错误提示翻译应在 `enhanced_errors` 层做（不修改 Python 原生异常类型，只改展示层），避免影响依赖异常类型的代码。
- L-068 若实现编译器警告，注意不要误报合法的模块级函数（缩进与类同级的段落本就应是模块级函数）——警告应针对「方法名与某类成员同名」或「段落紧跟类定义末尾且缩进模糊」的场景。
