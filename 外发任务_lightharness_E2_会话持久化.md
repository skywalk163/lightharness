# 外发任务 E2：会话持久化

## 任务目标

实现会话持久化，让用户的对话历史在刷新页面/重启服务后不丢失。实现 JSONL 会话存储 + 会话管理 API + 会话标题生成。

参考 deepseek-harness `packages/session/session-persistence-jsonl`（JSONL 持久化）、`packages/session/session-title`（标题生成）、`packages/session/session-query`（会话查询）的设计。

## 核心设计

### 1. 会话存储格式

在 `data/sessions/` 目录下，每个会话一个 JSONL 文件：

```
data/sessions/
  ├── session_20260909_120000_abc123.jsonl   # 每个会话一个文件
  ├── session_20260909_130000_def456.jsonl
  └── _index.json                              # 会话索引（元数据）
```

每条消息一行 JSON：
```json
{"角色":"user","内容":"你好","时间":"2026-09-09T12:00:00","工具调用":null}
{"角色":"assistant","内容":"你好！有什么可以帮你？","时间":"2026-09-09T12:00:01","工具调用":null}
{"角色":"assistant","内容":null,"时间":"2026-09-09T12:00:05","工具调用":[{"id":"call_1","名字":"bash","参数":{"命令":"ls"}}]}
{"角色":"tool","内容":"file1\nfile2","时间":"2026-09-09T12:00:06","工具调用id":"call_1","工具名":"bash"}
```

索引文件 `_index.json`：
```json
{
  "会话": [
    {"id":"session_20260909_120000_abc123","标题":"新会话","创建时间":"2026-09-09T12:00:00","最后更新":"2026-09-09T12:05:00","消息数":10,"文件":"session_20260909_120000_abc123.jsonl"}
  ]
}
```

### 2. 会话管理模块

在 `src/会话存储.light`（新建）中实现：

```光明
类 会话存储:
  属性 根目录 等于 "data/sessions"
  属性 索引 等于 {}
  
  段落 初始化 接收:
    # 创建目录，加载索引
  
  段落 新建会话 接收 标题:
    # 生成 id（时间戳+随机串），创建空 JSONL 文件，更新索引，返回会话元数据
  
  段落 获取会话 接收 id:
    # 读取 JSONL 文件，返回完整消息列表
  
  段落 追加消息 接收 id, 消息:
    # 追加一行到 JSONL 文件，更新索引的最后更新时间和消息数
  
  段落 列表会话 接收:
    # 返回索引中的所有会话元数据，按最后更新时间倒序
  
  段落 删除会话 接收 id:
    # 删除 JSONL 文件，从索引移除
  
  段落 更新标题 接收 id, 标题:
    # 更新索引中的标题
```

### 3. 会话管理 API（契约 2）

在 `src/web服务器.light` 中新增路由：

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/sessions` | 会话列表（按最后更新倒序） |
| GET | `/api/sessions/current` | 当前会话（从 token 或查询参数获取） |
| GET | `/api/sessions/:id` | 会话详情（完整消息历史） |
| POST | `/api/sessions` | 新建会话（body 可选 `{"标题":"..."}`） |
| DELETE | `/api/sessions/:id` | 删除会话 |
| PUT | `/api/sessions/:id/标题` | 更新会话标题 |

当前会话标识：通过登录 URL 的 token 关联会话 id（`HARNESS_WEB_TOKEN` 生成时绑定会话），或前端通过 `?session=xxx` 指定。无会话时自动新建。

### 4. 会话标题生成

参考 deepseek-harness `session-title` 的两种策略：
- **首条消息策略**（默认，免费）：取用户第一条消息的前 30 个字符作为标题
- **LLM 生成策略**（可选，需 API 调用）：用 LLM 根据前几条消息生成简洁标题（≤20 字）

E2 实现首条消息策略（默认），LLM 策略预留接口（`生成标题(消息列表)` 函数，当前返回首条消息前 30 字，后续可替换为 LLM 调用）。

### 5. 与 agent 循环的集成

- agent 循环开始时：从当前会话加载历史消息作为上下文
- 每轮消息（user/assistant/tool）：调用 `会话存储.追加消息()` 持久化
- 会话恢复：前端切换会话时，加载该会话的历史消息，继续对话

## 交付物

1. `src/会话存储.light` — 会话存储类（JSONL + 索引）
2. `src/web服务器.light`（修改）— 新增 6 个会话管理 API 路由
3. `src/agent.light`（修改）— agent 循环集成会话加载/追加
4. `examples/运行Web服务器.light`（修改）— 启动时初始化会话存储
5. `examples/test_会话存储.light` — 会话 CRUD 最小复现
6. 修复说明：根因 + 改动点 + 验证结果

## 验收标准

- [ ] 新建会话：POST /api/sessions 返回 `{"id","标题"}`，JSONL 文件创建
- [ ] 追加消息：消息写入 JSONL，索引的消息数+1、最后更新时间更新
- [ ] 会话列表：GET /api/sessions 返回所有会话，按最后更新倒序
- [ ] 会话详情：GET /api/sessions/:id 返回完整消息历史，顺序正确
- [ ] 删除会话：DELETE /api/sessions/:id 删除文件和索引条目
- [ ] 更新标题：PUT /api/sessions/:id/标题 更新索引标题
- [ ] 标题生成：首条消息前 30 字作为默认标题
- [ ] 会话恢复：重启服务后，历史会话仍在列表中，消息历史完整
- [ ] 并发安全：追加消息用追加模式（`a`），不覆盖已有内容
- [ ] 大会话性能：1000 条消息的会话加载 < 1 秒
- [ ] 全量回归不破坏（188 passed）

## 注意事项

- JSONL 格式：每行一条完整 JSON，不允许多行 JSON；消息中的换行符转义为 `\n`
- 索引文件：每次修改后原子写入（先写临时文件再 rename），避免崩溃导致索引损坏
- 会话 id 格式：`session_YYYYMMDD_HHMMSS_6位随机串`，全局唯一
- 目录创建：`data/sessions/` 不存在时自动创建，权限 755
- 与现有认证的集成：当前 token 绑定会话 id，无 token 时自动新建会话并返回新 token
- 消息格式兼容：现有 agent 循环的消息格式（`角色`/`内容`）不变，新增 `工具调用`/`工具调用id`/`工具名` 字段（E1 会用到）
- 旧数据迁移：首次运行时如果 `data/sessions/` 不存在，创建空索引；不涉及迁移
- 错误处理：会话文件不存在时返回 404 JSON，不抛异常
