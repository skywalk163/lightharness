# 安全策略

## 支持的版本

本项目按轮次（Rxx）推进，并以 `release/rxx` 分支 + `rxx-dev` 标签作为发布面。

| 版本 | 是否支持安全修复 |
|------|------------------|
| `r95-dev` 及之后的发布分支 | 支持 |
| `r95-dev` 之前的轮次（如 r94-dev、r87） | 不支持，请升级到最新发布分支 |

报告的漏洞会在最新的发布分支上修复；必要时维护者会决定是否向后移植。

## 报告方式

**不要**在公开 Issue 里贴漏洞细节、PoC 或利用步骤。请使用以下任一私密渠道：

1. **GitHub 仓库的 Security advisories**（"Report a vulnerability"）——首选，支持私密讨论与后续披露协调。
2. **私密 Issue**——若目标仓库未开启 Security advisories，可通过仓库维护渠道发起私密反馈；仍在整理而暂不方便公开的内容，请在标题中注明「私密 / security」。

本仓**不提供专门的安全邮箱**，任何声称来自本项目的安全邮箱地址都可视为不实。

### 请提供

- 影响的模块或文件路径（例如 `src/运行层/` 下某个文件）
- 漏洞类型（如路径穿越、命令注入、令牌泄漏、越权）
- 复现步骤与最小 PoC
- 影响面与利用前提（是否需要认证、是否需要本地访问）
- 已知的缓解方式（如有）

## 响应期望

- **首次响应**：尽力在 7 个工作日内给出答复（确认收到并给出初步判断）。这不是硬性 SLA，实际时间受维护者投入影响。
- **进展同步**：一旦定性为安全问题，会与你同步修复计划和发布时间。
- **披露节奏**：我们会先发布修复，再协调公开披露；在修复可用之前请勿公开细节。

感谢负责任地披露安全问题。
