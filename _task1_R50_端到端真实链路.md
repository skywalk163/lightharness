# 任务1（R50/P0）交付报告 —— 端到端真实链路终验

> 日期：2026-09-17 ｜ 第50轮 ｜ 交付物：`examples/test_R50_端到端真实链路.light`
> 铁律遵守：**零修改 src 纯逻辑层**，只新增 examples；mock 服务端 loopback，不依赖外部 API/key。

---

## 1. 做了什么

新增 `lightharness/examples/test_R50_端到端真实链路.light`（装配 + 断言，不含任何业务逻辑）。
把 R40 互举接线 / R43 真实 LLM 客户端 / R41-42 真实 IO 三条已交付能力**串成一个完整 agent 任务**：
5 轮真 HTTP 往返 + 4 次真工具调用，全程零退放。

### 链路时序（行为队列 FIFO，mock 脚本长度 = 5）

| 轮 | mock 行为 | 工具名 | 工具参数 | 真调落点 |
|---|---|---|---|---|
| R1 | `tool_call_success` | `bash` | `{"命令":"echo 你好R50"}` | 真实 shell 回显 → `结果["输出"]` |
| R2 | `tool_call_success` | `写文件` | `{"路径":"r50_e2e_note.txt","内容":"R50 端到端真实文件IO ok"}` | 工具_写文件 → 光明 `写入文件` 真落盘 |
| R3 | `tool_call_success` | `读文件` | `{"路径":"r50_e2e_note.txt"}` | 工具_读文件 → 光明 `读取文件` 真读回 |
| R4 | `tool_call_success` | `bash` | `{"命令":"cat r50_e2e_note.txt"}` | 真实 shell 看见 R2 写的字节 |
| R5 | `success` | — | — | 最终文本 `mock response recovered` |

每轮：真客户端 POST → mock 服务端（真 socket）SSE 流 → `块组装器` 重建 → 若有 `tool-call` 则
真调工具 → 工具结果按 `tool-result` 形状回灌消息 → 下一轮。

### 关键装配手法（本轮新增，可复用）

1. **单 mock 服务端吐多种工具调用**：`Mock大模型服务器.工具名 / 工具参数` 是实例**公开属性**，
   逐轮改写即可让同一个真实服务端先后吐 `bash / 写文件 / 读文件`（上游 `tool_call_success`
   只支持一个固定工具）。改写发生在「上一轮往返结束之后、下一轮发起之前」，与服务端处理
   时序不重叠（服务端 `处理循环` 在主协程任务里跑，客户端在独立线程里阻塞 POST）。
   → **不需要**为一个测试起 3 个 mock 服务端，也**不需要**改 mock 模块。
2. **读/写文件工具进 `工具执行` 流水线**：`读文件处理/写文件处理` 是一参签名，而
   `工具执行.执行工具` 以 `处理(参数, 取消令牌)` 调用 → 用例内写两参适配器
   （`读文件适配/写文件适配`，第二参带默认值），再 `具注册工具(执行器["注册"], …)`。
   bash 仍走已修好的 `注册bash工具`（L-163 后契约已对齐）。
3. **每次真调新建执行器**：`建执行器()` 内置限速（默认 10/1000ms），同实例连调 4 次会累积
   计时状态；新建即状态清零，既避免限流抖动，也保留「权限 → 限流 → 取消 → 执行 → 审计」全流程。

---

## 2. 现在（实测证据）

### 本机（Windows / light-merge venv python 3.13.14）

```
cd G:/dswork/duan-light-merge/lightharness
../light-merge/.venv/Scripts/python.exe 运行.py examples/test_R50_端到端真实链路.light
```

```
mock 服务端已起，端口=65207
轮1 调真实工具=bash 参数={"命令":"echo 你好R50"}
轮2 调真实工具=写文件 参数={"路径":"r50_e2e_note.txt","内容":"R50 端到端真实文件IO ok"}
轮3 调真实工具=读文件 参数={"路径":"r50_e2e_note.txt"}
轮4 调真实工具=bash 参数={"命令":"cat r50_e2e_note.txt"}
A1 FIFO 行为队列 = ['tool_call_success', 'tool_call_success', 'tool_call_success', 'tool_call_success', 'success']
A5 三层一致：读文件工具=R50 端到端真实文件IO ok | bash cat=R50 端到端真实文件IO ok | R41提供层=R50 端到端真实文件IO ok
A6 最终文本=mock response recovered
任务1 全部断言通过（端到端真实链路：真LLM+真HTTP+真工具5轮4调，rc=0）
rc=0
```

**连跑 3 次 rc=0**（无 flaky，端口由内核分配互不冲突）；临时文件 `r50_e2e_note.txt` 已被
`删除文件提供(路径,"cleanup")` 清理，工作树无残留。

### 0.82（FreeBSD 15.1，全量重同步副本 `/tmp/r44-20260917-123931`）

```
python scripts/同步0.82.py run python 运行.py examples/test_R50_端到端真实链路.light
[同步0.82] rc=0
```
远端输出与本机**逐行一致**（含 A1/A5/A6 全部断言），仅端口号不同。

---

## 3. 断言清单（A1–A6，共 12 条硬断言）

| 编号 | 断言 | 判据 |
|---|---|---|
| A1 | `agent轮数=5` / `真调工具次数=4` / `工具调用序号=4` / `mock请求记录=5` | 循环推进正确、FIFO 无跳号 |
| A1 | `FIFO脚本行为[i] == 脚本[i]`（i=0..4） | **行为队列严格 FIFO**（任务书关键风险#1 已覆盖） |
| A1 | `轮N_工具名对齐脚本` | 每轮吐出的工具名与脚本序位一一对应 |
| A2 | `bash真回显含你好R50` | 真实 shell stdout，非回放常量 |
| A3 | `写文件工具返回写入字节数` | 工具层返回值（经 `调正常` 状态闸） |
| A4 | `读文件工具读回一致` | 读回 == 写入内容 |
| A5 | `bash cat 看见写文件工具的字节` | **跨工具同 cwd**（HARNESS_ROOT）一致 |
| A5 | `R41真实文件系统提供者读回一致` | **三层交叉**（工具层读 / shell cat / 宿主提供层） |
| A6 | `最终文本 == mock response recovered` | 末轮文本经 SSE → 组装器完整还原 |
| — | 每次真调 `结果["状态"] == 调正常`（bash/写文件/读文件 各 1） | 全部经 `工具执行` 流水线放行 |

`工具执行` 的 `调正常 = "ok"`（L-164 常量）在断言中统一用常量比对，未硬编码字面值。

---

## 4. 未做 / 边界说明

- **未改任何 src**：`git status` 只剩新增 `examples/test_R50_端到端真实链路.light`。
- **mock 的 `stall/connection_reset` 等传输级行为未纳入**：本用例聚焦「多轮工具调用」，
  传输失败族已由 `examples/test_mock大模型服务器.light`（29 处契约断言）覆盖，不重复。
- **0.82 同步脚本一处罚疑**：`scripts/同步0.82.py` docstring 写 `run -- CMD`，但
  `cmd` 用 `nargs=REMAINDER` 会把字面 `--` 一起带进远端命令 → 实际必须写成 `run CMD`
  （本次首次调用即因此 rc=127，去掉 `--` 后 rc=0）。属文档与实现不一致，留待任务4/路M处置。
- 本机与 0.82 输出**逐行一致**，无平台差异。

---

## 5. 结论

端到端真实链路终验**达成**：真 LLM（本机 mock 服务端，无外部依赖）→ 真 HTTP/SSE → 真 agent 循环
→ 4 次真工具调用（bash / 写文件 / 读文件 / bash cat）→ 三层交叉一致 → 最终文本。
双平台 rc=0，三连跑稳定。可用于验收标准第 1 项「端到端真实链路跑通（多轮工具调用全真实）」。
