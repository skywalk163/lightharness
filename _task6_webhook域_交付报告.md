# 第13轮·任务6｜webhook 域：webhook-github → 网络钩子GitHub.light —— 交付报告

- 分支/工作区：`G:\dswork\duan-light-merge\lightharness`（本轮 worktree）
- 对照基准：上游 `packages/webhook/webhook-github/src/{body.ts, handler.ts, index.ts, types.ts}`（258 行），仓库 `G:\github\deepseek-harness`（只读参考）
- 任务书：`复刻_第13轮_任务prompt分发.md` §任务6（webhook 域；对标 #91 网络钩子GitHub）
- 改动文件：`src/网络钩子GitHub.light`（新建）、`examples/test_网络钩子GitHub.light`（新建）、`examples/_repro_L091.light`（新建，L-091 复现）、`_antirun_webhookgh.py`（反跑判据）、`_task6_webhook域_交付报告.md`（本报告）
- 验收硬标准：`test_网络钩子GitHub PASS` 文案精确匹配 + rc=0；上游四个源文件名保留不改；探针验证内建能力通过；反跑判据 3 项改反必红 → 恢复必绿。

## 1. 上游对应表（文件头标准化标注）

上游四个源文件名**原样保留**（不改、不合并）：`body.ts`、`handler.ts`、`index.ts`、`types.ts`。`src/网络钩子GitHub.light` 头部注释逐文件登记行数（70/136/34/18 = 258 行）与所有权映射：

| 上游文件 | 上游语义 | 本路实现位置（网络钩子GitHub.light） |
|---|---|---|
| `body.ts` | `WebhookHttpError(status, message)` 错误族（状态 ∈ {400,401,405,413,415,503}） | `网络钩子HTTP错误(状态, 消息)`：返回错误文本 `消息 + " [WH_状态]"`；`读错误码(文本)` 从 `[WH_NNN]` 回读状态（默认 503 兜底） |
| `body.ts` | `contentLength(header)`：`^(0\|[1-9]\d*)$` 十进制、超安全整数→413、缺省 undefined→无 | `解析内容长度(值)`（手写十进制判定，L-086 禁花括号量词）+ `是十进制串`/`是十六进制字符` 辅助；超 `安全整数上限=9007199254740991` → 413 |
| `body.ts` | `readBoundedUtf8Body(ctx, req, maxBytes)`：声明长度>上限→413；逐块累计>上限→413；返回拼接文本 | `读有界UTF8体(块表, 上限)`：块表逐项校验、累计超限→413、拼接块 "文本"；"字节" 键显式承载 UTF-8 字节数（光明按码点计长的偏差见 §语言差异 3） |
| `handler.ts` | 十一段门禁链：方法/类型/体/头/凭据/签名/载荷/派发（136 行） | `校验方法`（405）、`是JSON内容类型`（415）、`读有界UTF8体`（413）、`必需头`（400）、`验证签名`（401）/`恒定时间相等`、`解析载荷`（400）、`构造交付`、`处理请求`（链路总装） |
| `handler.ts` | `headersDistinct[name]`：值列表长度恰 1 且 trim 非空 | `必需头(头表, 名)`：`列表长度(值表) != 1` 或空 → 400 `缺少 X 头` |
| `handler.ts` | `isJsonContentType(value)`：`application/json` + 至多一个 charset 参数 | `是JSON内容类型(值)`：转小写比较 + 手写分段检查 `charset=utf-8`/`charset="utf-8"`（无正则）；多余参数含 `;` → 415 |
| `handler.ts` | octokit `Webhooks.verify()` 时间安全 HMAC-SHA256 | `验证签名(秘密, 体, 签名)`：`sha256=` 前缀 + 64 位十六进制校验 + `恒定时间相等`（max 长 次固定循环 + 差异累计，长度差不短路）；失败吞错返回 假，401 由链外层抛 |
| `index.ts` | `assertConfig(config)`：source 非空且 trim 不变；path 绝对、非根、无尾斜杠、无 `?`/`#`（34 行） | `校验配置(配置)`：[WH_CONFIG] 六闸（非串/空/trim 变/非 `/` 开头/根/尾斜杠/`?`/`#`） |
| `types.ts` | `GitHubJsonObject / GitHubWebhookEvent / WebhookEventMap`（18 行） | `构造函数`（无自定义类）：`构造交付(来源, 交付id, 事件名, 载荷, 接收时间)` → `{kind:"github", source, deliveryId, event:{name, payload}, receivedAt}`（对齐 VerifiedWebhookDelivery） |

### 宿主绑定剥离表
| 上游宿主设施 | 本路投影 | 登记 |
|---|---|---|
| `webServer.register(route)` 路由注册 | `处理请求(请求, 响应, 能力)` 入参投影 + `响应["状态"]`/`响应["消息"]` | 剔除（光明 web服务器.light 宿主面） |
| 真实 HTTP 流（req body 流式读取） | 块表（"字节"/"文本" 键）注入 | 剔除（测试注入） |
| `credentials` 凭据读取 | 能力 字典 的 `取秘密` 键（测试 `假取秘密` 注入） | 剔除（测试注入） |
| `receiveTime` 当前时间 | 能力 字典 的 `当前时间` 键（测试 `假当前时间` 注入） | 剔除（测试注入） |
| `dispatch` 派发 | 能力 字典 的 `派发` 键（测试 `假派发` 注入） | 剔除（测试注入） |
| octokit 标准 HMAC-SHA256 | `从 哈希 导入 HMAC_SHA256`（**pbkdf2 单轮近似，非标准**）→ §语言差异 L-091 | 宿主面差异登记 |

## 2. 实现要点

### 2.1 src/网络钩子GitHub.light（15 段）
- **错误族**：`网络钩子HTTP错误(状态, 消息)` 返回错误文本（`新建 错误` 仅限 `抛出` 语境编译——`返回 新建 错误` 不可用，错误对象经 `抛出` 构造后由 `读错误码` 从文本回读状态，503 兜底）。
- **内容长度**：`^0|([1-9]\d*)$` 等价手写判定（L-086 禁花括号量词）；缺省 → 空（`解析内容长度("")` → 空，对应 undefined→无）；超 `2^53-1` → 413。
- **有界读体**：块表逐块累计，声明长度/实际累计均受 `默认体上限=1000000` 约束；UTF-8 多字节代价由块 "字节" 键显式计数。
- **门禁链顺序**（`处理请求`）：方法 405 → 内容类型 415 → 声明长度 413 → 有界读体 413 → 必需头 400（X-GitHub-Event/X-Hub-Signature-256）→ 秘密存在性（空 → 401）→ 验证签名 401 → 解析载荷 400 → 配置 400 → 构造交付 → 派发。
- **配置校验**：source 非串/空/trim 变化 → 400；path 非 `/` 开头/`/` 根/尾斜杠/含 `?`/`#` → 400（文案对齐 index.ts assertConfig）。
- **载荷归一**：`解析JSON` 抛异常 → 400；空/数组/非字典（光明 `null`/`undefined` 同为 `空`）→ 400 `GitHub webhook 载荷必须是 JSON 对象`。
- 顶层常量：`默认体上限 = 1000000`、`安全整数上限 = 9007199254740991`。

### 2.2 examples/test_网络钩子GitHub.light（44 断言相等 + 19 检查抛码，全部硬断言）
- 用例分组：内容长度（400 非法串/413 超界/缺省）、读有界（0/超上限/精确边界）、必需头（缺失/多值 400）、内容类型（正确/大写/带合法 charset 通过；多余参数 415）、方法（POST 通过 405）、恒定时间（等长/异长前缀差）、签名（正确通过、错误秘密/错误签名/前缀错/非十六进制 401）、载荷（坏 JSON/数组/null 400、对象通过）、配置（全六闸 + trim 变化）、构造交付（形状对齐）、处理请求（全链路成功 → 响应 200 + 派发记录；secret 空 → 401）。
- 测试互斥：只读参考 `src/webhook会话.light` 的断言框架约定（`断言相等`/`检查抛码`/全局装配槽模式），未触碰禁区文件。

### 2.3 反跑判据 `_antirun_webhookgh.py`（3/3 ALL OK）

| # | 变异形态 | 变红断言 | 结果 |
|---|---|---|---|
| A | `恒定时间相等` 变异为恒真（签名比较被短路） | 错误秘密/错误签名 `验证签名` 必须 401 的用例红 | 反跑 rc=1；恢复绿 ✓ |
| B | `必需头` 的 `列表长度(值表) != 1` 变异为 `< 1`（放行多值） | 同名头双值 → 400 用例红 | 反跑 rc=1；恢复绿 ✓ |
| C | `是JSON内容类型` 删除「参数区含分号 → 假」分支（放行多余参数） | `application/json; charset=utf-8; x=1` → 415 用例红 | 反跑 rc=1；恢复绿 ✓ |

逐字节还原验证：变异/恢复后文件与备份完全一致（字节级比对 PASS）。

## 3. 测试与 CI

- 定向测试：`LIGHT_MERGE=... python 运行.py examples/test_网络钩子GitHub.light` → 输出 **`test_网络钩子GitHub PASS`**（文案精确）、rc=0。
- L-091 复现：`python 运行.py examples/_repro_L091.light` → `L091 PASS：...`、rc=0。
- 无残留软断言：`打印 "ok"` 风格全部硬化为 `断言相等`/`检查抛码`，确保反跑判据各自动红。
- 行尾/编码：src、test、_repro_L091 均为 CRLF 无 BOM（UTF-8）。

## 4. 未移植项（宿主面维持登记）

- **路由注册**：`effect(() => webServer.register(route))` → 光明 web服务器.light 宿主面，本域不实现。
- **真实 HTTP 流**：流式读体、HTTP 解析、`res.statusCode/setHeader` → 宿主面（`block-表` 注入 + `响应` 字典投影承接）。
- **octokit 标准 HMAC-SHA256 校验**：宿主入库时芯必须用标准 `hmac`（Python 标准库/宿主实现）对拍 GitHub 真实签名；`哈希.HMAC_SHA256`（pbkdf2 近似）仅能在本域自洽链路内使用（§语言差异 L-091）。
- **凭据读取**（credentials provider）、**收到时刻**（receiveTime）：能力注入，宿主接线。

## 5. 语言差异（本轮新登记）

| 编号 | 严重程度 | 现象 | 绕法（光明语法） |
|---|---|---|---|
| **L-091** | 高（宿主面差异） | `从 哈希 导入 HMAC_SHA256` 实为 `hashlib.pbkdf2_hmac('sha256', 文本, 密钥, 1)` 单轮 PBKDF2（消息尾部含 `b'\x00\x00\x00\x01'`），**非标准 HMAC-SHA256**（key 未走 ipad/opad 内层处理）；同一输入与 Python `hmac` 标准库输出不一致（实测：`"supersecret"/"payload-body"` → 光明 `ec8ebaf2...` vs 标准 `36a6b169...`） | 自洽链路内可用（测试用同一函数生成签名再验证）；外部对拍（GitHub 真实签名）必须宿主面标准校验。复现：`examples/_repro_L091.light`（绕法形态 rc=0） |

沿用既有 L-037（`断言(条件,标签)` 降级 no-op，一律 `断言相等`/`检查抛码`）、L-051（字面量单行）、L-054（嵌套字面量不写 `\n`）、L-055（`写`/`打印` 只吃字面量串）、L-086（禁花括号量词）、L-088（`捕获 as` 不可用，`尝试/捕获` + 布尔标志）、L-089（手写 JSON 不可靠，一律 `序列化JSON` 构造）。

### 其他光明降级点（口径明确，勿当 bug）
1. `null` 与 `undefined` 同在光明为 `空`：Content-Length 缺省与秘密未配置一致以 `空` 判定；`解析JSON("null")` → 空 → 与非字典同走 400 闸。
2. 恒定时间比较无外部时间可观测：实现为 `max(长)` 固定循环 + 差异累计（长度差不短路）；反跑用失败注入断言（错误秘密/错误签名必须返回 假）。
3. UTF-8 字节计数：光明串按码点计长，多字节代价由块表 "字节" 键显式注入。

## 6. 移交清单

- `src/网络钩子GitHub.light`（新建 15 段：错误族/读错误码/十六进制判定/十进制判定/内容长度/有界读体/必需头/内容类型/方法门禁/恒定时间/签名验证/载荷归一/配置校验/交付构造/处理请求）
- `examples/test_网络钩子GitHub.light`（新建，44 断言相等 + 19 检查抛码，PASS 文案精确、rc=0）
- `examples/_repro_L091.light`（新建，L-091 复现，rc=0）
- `_antirun_webhookgh.py`（反跑判据 3/3 ALL OK，逐字节还原一致）
- `_task6_webhook域_交付报告.md`（本报告）

## 7. 自检铁律核验

- 文件互斥：仅写本任务 4 个新文件 + 交付报告；只读 `src/webhook会话.light`（参考断言框架）；未触碰任何禁区文件。
- 无代写报告（本报告由任务执行过程实况整理，所有数字可复核）。
- 无新增二进制/锁文件（`_antirun_webhookgh.py` 备份/恢复使用字节级原语，不产生残留）。
- import 白名单：src 仅导入 `内置核心转换`/`内置核心字符串`/`内置核心判型`/`内置核心列表`/`内置核心字典`/`字符串工具轻量`/`JSON`/`哈希`（均在 stdlib 白名单内）；测试另导入 `内置核心列表`/`内置核心字典`（框架所需）。
- 反跑判据 3 项全部「改反必红 → 恢复必绿 → 逐字节一致」。
- 遗留延续（本轮不接）：工具注册表接入点、approval/* 词汇注册、宿主面（agent 接线/HTTP 路由/凭据事件）——均在上文 §4 登记。