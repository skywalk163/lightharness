# 光明语言专项升级 — 并行任务总览（R70 批次）

> **背景**：R66~R69 系统级开发（socket/事件循环/沙箱/异步子进程/异步PTY）过程中，光明语言的多处不足成为开发瓶颈。最突出的是**无匿名闭包**（回调只能用"实例属性暂存+无参派发方法"绕）、**无 async/await 协程语法**（异步只能回调式）。本批次对光明语言做专项升级补缺。
>
> **编译器主目录**：`G:\dswork\duan-light-merge\light-merge\`
> **运行时/测试目录**：`G:\dswork\duan-light-merge\lightharness\`
> **语言缺陷账**：`lightharness/docs/功能对标/语言缺陷账.md`（L-001~L-165+）
>
> **任务划分原则**：每个任务独立可并行，改动面不重叠（词法/解析/代码生成/运行时 分层隔离）。任务间通过"全量回归门"统一收口。

---

## 任务总览

| 任务 | 主题 | 核心改动层 | 优先级 | 预计产出 |
|------|------|-----------|--------|---------|
| **A** | 匿名函数与闭包捕获深化 | 解析器 + 代码生成 | P0 | lambda 语法 + 闭包捕获 + 测试 |
| **B** | async/await 协程语法 | 解析器 + 代码生成 + 运行时 | P0 | 异步段落/函数 + 等待表达式 + 事件循环集成 + 测试 |
| **C** | 块级作用域与嵌套段落加固 | 代码生成（作用域分析） | P1 | let/const 语义 + 块级作用域 + 测试 |
| **D** | 高优先级缺陷批量修复 | 词法 + 解析 + 内置 | P1 | L-084/L-085/L-086 等 5~8 条修复 + 测试 |
| **E** | 全量回归门与文档收口 | 测试 + 文档 | P2 | 回归报告 + 缺陷账更新 + 升级说明 |

> 任务 A/B/C/D 可完全并行；任务 E 在 A/B/C/D 全部合入后执行收口。

---

## 任务 A：匿名函数与闭包捕获深化

### 背景与现象

L-078（第11轮）已实现**命名嵌套段落**（函数内定义函数）+ `nonlocal` 标量捕获，6 场景测试全绿。但在 R69 异步子进程/异步PTY 开发中，实际瓶颈是**无法在回调位置直接写匿名函数捕获外层变量**：

```光明
# 当前写法（被迫绕）：
类 异步子进程:
  段落 启动 接收 完成回调:
    设 己.待派发结果 为 空      # ← 用实例属性暂存回调参数
    设 己.完成回调 为 完成回调
    注册定时器(循环, 30, 己.派发完成)  # ← 无参方法读取实例属性后调用

  段落 派发完成:  # ← 额外的无参派发方法
    设 结果 为 己.待派发结果
    己.完成回调(结果)
```

期望能直接写：

```光明
# 期望写法（匿名闭包）：
段落 启动 接收 完成回调:
  设 结果 为 执行命令()
  注册定时器(循环, 30, 函数(): 完成回调(结果))  # ← 匿名函数捕获 结果 和 完成回调
```

### 最小复现

```光明
# test_lambda.light
段落 主:
  设 计数 为 0
  设 自增 为 函数():  # ← 匿名函数，捕获外层 计数
    计数 为 计数 + 1
    返回 计数
  打印(自增())  # 期望 1
  打印(自增())  # 期望 2
  打印(计数)     # 期望 2（闭包修改了外层变量）

  # 带参数的匿名函数
  设 加法 为 函数(a, b): a + b
  打印(加法(3, 4))  # 期望 7

  # 匿名函数作为回调参数
  设 列表 为 [1, 2, 3]
  设 翻倍 为 映射(列表, 函数(x): x * 2)
  打印(翻倍)  # 期望 [2, 4, 6]
```

### 期望能力

1. **匿名函数语法**：`函数(参数列表): 表达式` 或 `函数(参数列表): <块>` 两种形式
   - 单行形式：`函数(x): x * 2`（表达式体，自动返回）
   - 块形式：`函数(x):` 换行缩进多语句（需 `返回`）
2. **闭包捕获**：匿名函数可捕获外层作用域的变量
   - 读取外层变量：自动捕获（Python closure 语义）
   - 修改外层标量：自动生成 `nonlocal`（与 L-078 命名嵌套函数一致）
   - 修改外层可变对象（列表/字典）：无需 nonlocal，直接操作
3. **作为一等值**：匿名函数可赋值给变量、作为参数传递、作为返回值
4. **语法别名考虑**：是否同时支持 `λ(参数): 表达式`（更简洁，但需确认词法器能否处理 `λ` 字符）

### 技术要点

- **解析器**：在表达式解析中新增 `函数` 关键字开头的匿名函数表达式（与 `段落` 定义区分：`段落` 是语句级定义，`函数` 是表达式级匿名函数）
- **代码生成**：匿名函数编译为 Python `lambda`（单行表达式体）或嵌套 `def`（块体），闭包变量自动 `nonlocal`（复用 L-078 的 nonlocal 分析逻辑）
- **作用域分析**：匿名函数的参数名不能与外层捕获变量冲突（需 shadowing 规则）
- **递归限制**：匿名函数无法直接递归（无名字），这是预期限制（如需递归用命名 `段落`）

### 验收标准

- [ ] `examples/test_lambda.light`：单行匿名函数 + 带参数 + 闭包读取 + 闭包修改标量(nonlocal) + 作为回调参数 + 作为返回值，全部通过
- [ ] 匿名函数内修改外层标量自动 nonlocal（与 L-078 命名嵌套函数行为一致）
- [ ] 现有命名嵌套段落（L-078）行为不变，全量回归不破坏
- [ ] 匿名函数可在 `注册定时器`、事件回调等位置直接使用（端到端验证：用匿名函数重写 R69 异步子进程的一个回调，验证可用）
- [ ] 性能：匿名函数创建开销与 Python lambda 相当（无额外全局查找）

### 交付物

1. 编译器改动（解析器 + 代码生成 + 作用域分析）
2. `examples/test_lambda.light` 最小复现用例（≥8 组断言）
3. 端到端验证：用匿名闭包重写 `src/异步子进程.light` 的回调派发（验证后可保留为可选优化，或仅作为验证用例）
4. 修复说明：语法设计决策（是否支持 `λ` 别名、块体与表达式体的边界）+ 改动点 + 验证结果
5. 更新 `docs/功能对标/语言缺陷账.md`：新增 L-xxx（匿名函数/闭包深化）条目，状态「已修复」

---

## 任务 B：async/await 协程语法

### 背景与现象

L-归档-02 明确：光明无 async/await 协程语法。R66 事件循环、R69 异步子进程/异步PTY 全部采用**回调式异步**，代码嵌套深、错误处理分散：

```光明
# 当前回调式（R69 异步子进程）：
异步运行(["echo", "hello"], 配置,
  函数(结果):  # ← 完成回调
    打印(结果.输出)
    异步运行(["echo", "world"], 配置,
      函数(结果2):  # ← 回调地狱
        打印(结果2.输出)))
```

期望能写顺序式异步代码：

```光明
# 期望 async/await：
异步 段落 主:
  设 r1 为 等待 异步运行(["echo", "hello"], 配置)
  打印(r1.输出)
  设 r2 为 等待 异步运行(["echo", "world"], 配置)
  打印(r2.输出)
```

### 最小复现

```光明
# test_async_await.light
异步 段落 延迟打印 接收 消息, 毫秒:
  等待 睡眠异步(毫秒)
  返回 消息

异步 段落 主:
  设 a 为 等待 延迟打印("hello", 100)
  设 b 为 等待 延迟打印("world", 100)
  打印(a + " " + b)  # 期望 "hello world"，总耗时 ~200ms（顺序执行）

  # 并发执行
  设 任务1 为 延迟打印("并发1", 100)
  设 任务2 为 延迟打印("并发2", 100)
  设 结果 为 等待 全部([任务1, 任务2])
  打印(结果)  # 期望 ["并发1", "并发2"]，总耗时 ~100ms（并发）
```

### 期望能力

1. **`异步` 修饰符**：`异步 段落 名 接收 参数:` 定义异步函数（编译为 Python `async def`）
   - 注意：`异步` 当前是光明保留修饰符（R69 踩坑：不能用作变量名），需确认其当前语义是否已部分实现
2. **`等待` 表达式**：`等待 异步调用()` 挂起当前协程，等待结果（编译为 Python `await`）
3. **事件循环集成**：
   - 模块级 `异步 段落 主` 自动被事件循环驱动（入口约定）
   - 普通同步代码中调用异步函数返回"任务对象"（类似 Python coroutine），需 `等待` 或提交到事件循环
4. **并发原语**（若实现成本低可一并提供，否则仅核心语法）：
   - `全部(任务列表)`：等待所有任务完成（类似 `asyncio.gather`）
   - `任一(任务列表)`：等待第一个完成
   - `睡眠异步(毫秒)`：异步睡眠（不阻塞事件循环）
5. **错误处理**：`等待` 中的异常正常传播到 `尝试/捕获`（与同步代码一致）

### 技术要点

- **解析器**：`异步` 作为 `段落`/`函数` 的前置修饰符（类似 Python `async def`）；`等待` 作为一元前缀运算符（优先级需明确）
- **代码生成**：
  - `异步 段落` → `async def`
  - `等待 expr` → `await expr`
  - 异步函数内的 `返回` → `return`（async def 中 return 正常）
- **运行时**：
  - 入口检测：模块级 `异步 段落 主` 自动用 `asyncio.run(主())` 或 R66 事件循环驱动
  - 需确认与 R66 `事件循环.light` 的集成方式（R66 事件循环是回调式的，asyncio 是另一个运行时；需决策是基于 asyncio 还是基于 R66 事件循环）
- **向后兼容**：现有回调式异步代码（R66/R69）不受影响，async/await 是新增语法

### 验收标准

- [ ] `examples/test_async_await.light`：异步段落定义 + 等待表达式 + 顺序执行 + 并发执行(全部) + 异常传播 + 异步睡眠，全部通过
- [ ] 模块级 `异步 段落 主` 可直接运行（`python 运行.py test_async_await.light`），事件循环自动启动
- [ ] `等待` 只能在 `异步 段落` 内使用，在同步段落中使用应报明确编译错误（"等待 只能在异步段落中使用"）
- [ ] 现有回调式代码（R66 事件循环测试、R69 异步子进程/PTY 测试）全量回归不破坏
- [ ] 端到端验证：用 async/await 重写 R69 异步子进程的一个测试用例，验证 `等待 异步运行(...)` 可用
- [ ] 性能：async/await 开销与 Python asyncio 相当

### 交付物

1. 编译器改动（解析器 + 代码生成）
2. 运行时入口改动（异步主段落自动驱动事件循环）
3. `examples/test_async_await.light` 最小复现用例（≥8 组断言）
4. 并发原语（若实现）：`全部`/`任一`/`睡眠异步` 的实现 + 测试
5. 端到端验证：async/await 版本的异步子进程调用示例
6. 修复说明：与 R66 事件循环的关系/集成决策 + 语法设计 + 改动点 + 验证结果
7. 更新 `docs/功能对标/语言缺陷账.md`：L-归档-02 状态更新为「已修复」，新增 async/await 详细条目

---

## 任务 C：块级作用域与嵌套段落加固

### 背景与现象

L-078 实现了命名嵌套段落（函数级嵌套），但光明的变量作用域目前是**函数级**（类似 Python），`如果`/`遍历`/`当` 等块内定义的变量会泄漏到块外：

```光明
段落 主:
  如果 真:
    设 块内变量 为 42
  打印(块内变量)  # 当前：42（变量泄漏到块外）
                  # 期望（块级作用域）：名称错误（块内变量不可见）
```

这在复杂代码中可能导致变量名意外覆盖、逻辑错误。同时，嵌套段落的深度和闭包捕获需要加固验证。

### 最小复现

```光明
# test_block_scope.light
段落 主:
  # 1. 块级作用域：if 内变量不泄漏
  如果 真:
    设 x 为 1
  # 打印(x)  # ← 期望：名称错误（x 不可见）

  # 2. for 循环变量不泄漏
  遍历 i 之 [1, 2, 3]:
    设 循环内 为 i * 2
  # 打印(循环内)  # ← 期望：名称错误

  # 3. 块内外同名变量互不干扰
  设 y 为 10
  如果 真:
    设 y 为 20  # ← 块级作用域：这是块内新变量，不影响外层
  打印(y)  # 期望：10（外层 y 不变）

  # 4. 显式修改外层变量（需非局部声明或赋值语法区分）
  设 z 为 10
  如果 真:
    z 为 20  # ← 用 为 赋值（非 设）表示修改外层变量
  打印(z)  # 期望：20
```

### 期望能力

1. **`设` = 块级声明**：`设 X 为 值` 在当前块内声明变量，作用域限于当前块（类似 JavaScript `let`）
2. **`为` = 赋值**：`X 为 值` 赋值给已存在的变量（沿作用域链向上查找），不声明新变量
   - 注意：当前光明 `设 X 为 Y` 和 `X 为 Y` 可能都是赋值，需区分声明与赋值
3. **块级作用域**：`如果`/`遍历`/`当`/`尝试` 等块内 `设` 的变量不泄漏到块外
4. **嵌套段落加固**：验证 L-078 嵌套段落的多层闭包捕获、递归嵌套、闭包变量生命周期
5. **向后兼容模式**（关键决策）：
   - 现有大量代码依赖"变量泄漏"行为（函数级作用域），直接改为块级作用域会破坏全量回归
   - **方案A**：引入 `设` 为块级声明、`为` 为赋值，现有代码中 `设` 在块内的用法需审计迁移（破坏性大）
   - **方案B**：新增 `令` 关键字作为块级声明（`令 X 为 值`），`设` 保持函数级声明（向后兼容）
   - **方案C**：仅做嵌套段落加固（不引入块级作用域），块级作用域留待后续
   - **建议方案B**（新增 `令`，不破坏现有代码），或方案C（仅加固，风险最低）

### 技术要点

- **作用域分析**：代码生成器需跟踪块级作用域栈，`设`/`令` 的变量注册到当前作用域
- **代码生成**：
  - 方案B：`令 X 为 V` → Python 无直接对应（Python 是函数级作用域），需用额外的作用域字典模拟，或编译为不同的变量名（mangling）
  - 或：块级作用域通过 Python 的 `if True:` 块 + 变量名 mangling 实现（块内变量名加后缀，块外不可访问）
- **嵌套段落**：复用 L-078 的 nonlocal 分析，验证多层嵌套的正确性

### 验收标准

- [ ] 明确块级作用域的实现方案（A/B/C），并在文档中说明决策理由
- [ ] 若实施方案B：`examples/test_block_scope.light` 验证 `令` 的块级作用域 + `设` 的函数级兼容 + 嵌套段落多层闭包
- [ ] 若实施方案C：`examples/test_nested_deep.light` 验证 ≥3 层嵌套段落 + 多层闭包捕获 + 闭包变量生命周期 + 递归嵌套
- [ ] 全量回归不破坏（这是硬约束，任何方案都必须通过）
- [ ] 现有 L-078 测试（6 场景）继续全绿

### 交付物

1. 编译器改动（作用域分析 + 代码生成，视方案而定）
2. 测试用例（块级作用域 或 嵌套段落加固）
3. 方案决策文档：为什么选 A/B/C，对现有代码的影响评估
4. 全量回归结果
5. 更新 `docs/功能对标/语言缺陷账.md`

---

## 任务 D：高优先级缺陷批量修复

### 背景

语言缺陷账中仍有若干"已登记，未修"的条目，且 R66~R69 开发中可能有新发现未登记。本任务批量修复高优先级、改动面小的缺陷。

### 待修复条目（候选清单，执行者可根据实际情况增减）

| 编号 | 现象 | 改动层 | 优先级 |
|------|------|--------|--------|
| **L-084** | `为` 字作为赋值关键字是硬关键字，黏在标识符尾部被切碎（`行为`→`行`+`为`），需做上下文感知软关键字 | 词法 | P1 |
| **L-085** | `e` 指数浮点字面量不支持（`1e400` 被解析为标识符 `inf`） | 词法/解析 | P1 |
| **L-086** | 字符串中 `{1,4}` 被当重复语法破坏，正则花括号量词不可用 | 词法/字符串解析 | P1 |
| **L-046** | 单字 `出` 不能作变量名（被当组合关键字前缀） | 词法 | P2 |
| **L-165** | 新建 Mock 服务未声明全局，模块级当前实例恒为空 | 解析/代码生成 | P2 |
| **新发现** | R66~R69 开发中踩坑但未登记的缺陷（执行者需先扫描代码中的绕法注释，提取未登记缺陷） | 各层 | P1 |

### 执行要求

1. **先扫描后修复**：扫描 `lightharness/src/` 和 `lightharness/examples/` 中的绕法注释（`# 绕`、`# 语言缺陷`、`# workaround` 等），提取未登记到缺陷账的条目，先登记再修复
2. **每条独立验证**：每条缺陷修复后有独立的最小复现测试
3. **全量回归**：全部修复合入后跑全量回归，确保零破坏

### 验收标准

- [ ] 每条修复有 `examples/test_Lxxx.light` 最小复现用例
- [ ] 绕法扫描报告：列出扫描到的绕法、对应缺陷编号、是否已登记
- [ ] 新发现缺陷已登记到 `docs/功能对标/语言缺陷账.md`
- [ ] 全量回归零破坏
- [ ] 缺陷账中对应条目状态更新为「已修复」

### 交付物

1. 编译器改动（按条目分散）
2. 每条缺陷的测试用例
3. 绕法扫描报告
4. 缺陷账更新
5. 全量回归结果

---

## 任务 E：全量回归门与文档收口

### 执行时机

任务 A/B/C/D 全部合入后执行。

### 工作内容

1. **全量回归**：运行 lightharness 全量测试套件（`scripts/ci_test.py` 或等效命令），记录结果
2. **交叉验证**：
   - 用匿名闭包（任务A）重写 R69 异步子进程/PTY 中的回调派发绕法，验证代码简化效果
   - 用 async/await（任务B）重写 R69 异步测试，验证协程式异步可用
   - 确认 R66 事件循环、R67 沙箱、R68 FreeBSD 回归、R69 异步模块全部不受影响
3. **文档收口**：
   - 更新 `docs/功能对标/语言缺陷账.md`：所有本轮修复条目状态更新
   - 编写 `docs/光明语言升级_R70批次.md`：本轮升级总览（语法新增、行为变更、迁移指南）
   - 更新 `docs/README.md` 中的语言特性列表
4. **性能基准**：对比升级前后的编译耗时和运行时性能（确保无显著回归）

### 验收标准

- [ ] 全量回归零红（或已知红项有明确说明）
- [ ] 交叉验证用例通过（匿名闭包重写 + async/await 重写）
- [ ] 升级文档完成
- [ ] 性能无显著回归（编译耗时增加 ≤10%，运行时无退化）

### 交付物

1. 全量回归报告
2. 交叉验证用例
3. `docs/光明语言升级_R70批次.md`
4. 性能基准对比报告

---

## 公共约束与注意事项

### 所有任务必须遵守

1. **不许用 `引 Python` 绕过语言缺陷**——这是语言升级轮，必须在编译器层面解决
2. **向后兼容是硬约束**：全量回归必须通过，现有代码行为不得无故改变
3. **每条改动有测试**：最小复现用例 + 反跑验证（把断言改反应立即红）
4. **错误信息友好**：新增语法的误用场景需给出中文报错，定位到行列
5. **只动编译器和测试**：不修改 lightharness 业务模块（除非是端到端验证用例，且验证后恢复或保留为可选优化）

### 编译器代码定位提示

- 词法器：`light-merge/` 根目录或子目录中的 `*lexer*`/`*token*` 文件
- 解析器：`*parser*`/`*stmt*` 文件
- 代码生成：`*codegen*`/`*generator*`/`*compiler*` 文件
- AST 节点：`*ast*` 文件
- 内置原语：`stdlib/builtins.py` 或 `light_compiler/` 目录
- 执行者需先 `Read` 对应文件确认实际路径和结构，禁止凭记忆修改

### 并行协作

- 任务 A/B/C/D 可同时进行，改动面分别在：
  - A：表达式解析（匿名函数表达式）+ 代码生成（lambda/嵌套def）
  - B：语句解析（异步修饰符）+ 代码生成（async/await）+ 运行时入口
  - C：作用域分析 + 代码生成（变量声明/赋值区分）
  - D：分散的词法/解析小改动
- 若 A 和 B 在代码生成器的同一函数中有冲突，优先保证各自测试通过，合并时由任务 E 统一解决
- 任务之间通过 git 分支隔离，合入时逐个跑全量回归

---

## 完成定义（DoD）

- [ ] 任务 A/B/C/D 各自的测试用例全绿
- [ ] 任务 E 全量回归零红
- [ ] 语言缺陷账更新完成
- [ ] 升级文档完成
- [ ] 交叉验证（匿名闭包重写 + async/await 重写）通过
- [ ] 性能无显著回归
