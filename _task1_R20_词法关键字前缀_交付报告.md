# 任务1 交付报告（修订版）：词法分析器关键字前缀标识符回归修复（R20，P0）

- 轮次：第20轮（编译器回归修复轮）
- 任务：清零第19轮遗留的 CI 临时例外①②（`test_宿主上下文.light` / `test_宿主事件.light`，均 SKIP），并确保无新回归
- 修复文件（互斥表内）：`light-merge/src/lexer.py`（**词法器单文件**，未改 parser / codegen / stdlib）
- 新增/更新文件：`lightharness/examples/test_R20_关键字前缀标识符.light`、`lightharness/tests/test_R20_词法边界.py`
- 反跑脚本：`lightharness/_antirun_r20_t1_词法关键字前缀.py`
- 验收：例外①② 从 SKIP 移除后 rc=0；全量 examples 套件**零回归**

> **本文件为修订版（2026-09-14 10:5x）**。首版（08:58）沿用任务书的诊断口径，把例外①② 归因为
> 「`去重占位`/`作用域匹配` 等关键字前缀标识符在表达式上下文被切碎」。经扫描（任务3）与本轮实测证伪，
> §1 已订正为真实根因，§2/§3 按最终落地的代码如实重写。**首版中「去重占位/作用域匹配 整体 IDENT ✅」的表格
> 结论不成立**（那段在孤立片段口径下确实被切碎；真实文件口径下则由预扫描还原，见 §1.2）。

---

## 1. 根因分析（订正版）

### 1.1 例外①② 的真根因：`设 X为<值起始>` 的赋值关键字丢失

`src/宿主上下文.light` 中 `设 合并为 {}`（`合并` 不是关键字）在词法 EMBED 最大匹配段被整体当成标识符 `合并为`，
**赋值关键字 `为` 丢失** → `{}` 处报「期望'为'或'等于'，但得到「{」」。
由于 `宿主上下文.light` 是被导入模块，`format_error` 又把位置错锚到入口文件的其它行
（报成 `test_宿主上下文.light:100`，而第 100 行实为 `断言真(列表长度(名单) == 列表长度(去重占位(名单)), …)`），
形成「写的是 `==` 断言却报 `{}`」的强误导。`test_宿主事件.light` 经导入链连锁失败，同因。

`设 甲为空` 形态同理：`甲为空` 被整体成标识符，`为` 丢失。

### 1.2 被证伪的假设：关键字前缀标识符切碎

- 全项目扫描（`_scan_r20_keyword_prefix.py`，任务3）结论：**真实文件口径下 1708 个关键字前缀标识符全部整体成词，0 个被切碎**。
  原因：`Lexer._scan_user_definitions` 会把模块级 `段落 X` / `设 X` / 导出名预先登记，
  随后 `_tokenize_chinese_sequence` 在 `if full_identifier in user_definitions` 分支整体输出。
  `去重占位` 在 `src/宿主上下文.light` 中既有 `段落 去重占位` 定义又有调用，故**从未被切碎**。
- 「孤立片段口径切碎」是**假象**：`Lexer().tokenize('去重占位(名单)')`（无定义登记）确会切碎，
  但那种场景运行期本就是 `NameError`，不构成真实破坏面。
- 逐条实测（第20轮，`_probe_r20_state.py`，定义+调用同源码）：
  `去重占位 / 作用域匹配 / 排序函数 / 遍历器 / 筛选器 / 求和函数 / 读取器 / 输出流 / 调试器 / 映射表 /
   生成器 / 加载器 / 匹配器 / 反转函数 / 删除属性 / 保护器 / 实现类 / 继承链 / 抽象类 / 构造器 /
   如果条件 / 当循环 / 尝试块 / 捕获块 / 抛出异常 / 新建对象 / 返回值 / 外部函数 / 导入模块 / 导出模块 …
   (≥70 个)` → **全部 整体 IDENTIFIER**。

**结论**：任务书 §例外①② 的根因描述不准确；真缺陷是「赋值尾巴并坏」（`X为`），本报告按实测订正。

### 1.3 附带发现的两个真缺陷（本轮一并修复）

- **R20-A（回归）**：首版修复的「情形 (a)：关键字紧邻值字面量尾巴 → 切出前缀」**未区分调用语境**，
  导致 `断言为真(...)` 被切成 `断言为` + `真` → 「「真」是保留关键字，不能直接作为语句开头」，
  引发 **6 个用例回归**：`test_代理策略 / test_启动环境 / test_大模型回放 / test_文件深 / test_时间上下文 / test_联调CLI`。
- **R20-B（漏网）**：切分逻辑执行在 `if full_identifier in user_definitions` **之前**，
  导致已登记的用户定义名也被切分：`段落 断言为真 接收 参数甲:` → `断言` + `为` + `真` → 函数定义崩。
  （由 `tests/test_R20_词法边界.py::test_call_context_never_split` 捕获。）

---

## 2. 修复方案（最终落地）

### 2.1 修复点 A：运算符动词的函数调用语境（`删除属性`）

运算符动词（`加/减/乘/除…`）嵌于纯 CJK 长串、且**整串后紧随 `(`**（函数调用语境）→ `skip_kw = True`（整串成标识符）；
非调用语境（`甲加乙` 后无 `(`）仍按中缀切分。限制为纯 CJK 串，避免 `增加count(` 这类「复合动词 + ASCII 后缀」被误并。

### 2.2 修复点 B：`为`/`返回`/`尝试` 的精确切分与三重保护

在 `_EMBED_MAX_MATCH_KEYWORDS = frozenset({'为','返回','尝试'})` 分支内：

| 情形 | 触发条件 | 行为 |
|---|---|---|
| (a) 值字尾巴 | 关键字后紧邻 `空`/`真`/`假` 且前缀非空 | 切分：emit `IDENT(前缀)`，关键字交还 KEYWORD |
| (b) 词尾 + 值起始 | 关键字位于词尾、整串后（跳空白）为 `{ [ "` 或数字 | 切分：同上 |
| (c) **调用语境（新增守卫）** | 整串后**紧随** `(`（中间无空格） | **绝不切分**，整串作函数名 |
| (d) **用户定义名（新增守卫）** | `full_identifier in user_definitions` | **绝不切分**，整串作标识符 |
| 其它 | 词中（`返回表`/`尝试记录`/`为字`）、词尾后跟非值起始（`行为`/`末位行为`） | 整串作标识符 |

- (c) 的「紧随」而非「跳空白后」是刻意的：`设 X为 (甲 + 乙)` 这类**括号值表达式**不得被误判为调用。
- (d) 必须**前置**（`user_definitions` 分支在函数后段，原先来不及生效）。

### 2.3 清理：删除重复的死定义 + 订正注释

首版在模块级写了两次 `_EMBED_MAX_MATCH_KEYWORDS`（第二行覆盖第一行，第一行为死代码，且注释声称覆盖
`去重`/`作用域` —— 与 §1.2 事实不符）。本轮删掉死定义，并把模块级与函数内注释订正为与实测一致的表述。

---

## 3. 不破坏合法语法的验证证据

### 3.1 词法 token 级（`tests/test_R20_词法边界.py`，**76 passed**）

- 关键字前缀标识符（定义 + 调用同源码）≥70 名整体 IDENTIFIER；
- 合法语句关键 token 保持：`如果 条件:` / `否则:` / `否则若 条件:` / `遍历 键 之 列表:` / `当 条件:` /
  `尝试:` / `捕获 异常 为 甲:` / `最终:` / `设 甲 为 真|假|空` / `返回 真`；
- 赋值尾巴切分（翻转后的断言）：`设 合并为 {}` → `合并` + `为`；`设 甲为空/甲为真/甲为假` → `甲` + `为`；
- 调用语境守卫：`断言为真 / 判定为空 / 断言为假 / 合并为 / 头版本为 / 内层返回 / 结算被终止尝试` 作段名与调用点均整体成词；
- 中缀不误并：`甲加乙` → `甲` / `加` / `乙`；`删除属性(子, "共享")` → 整体成词。

### 3.2 运行期 rc（实测）

| 用例 | 修复前（首版） | 修复后 |
|---|---|---|
| `test_宿主上下文.light`（例外①） | 0 | 0 |
| `test_宿主事件.light`（例外②） | 0 | 0 |
| `test_代理策略 / 启动环境 / 大模型回放 / 文件深 / 时间上下文 / 联调CLI` | **1（回归）** | **0** |
| `test_会话格式 / 授权链 / test_L119 / test_L120 / test_L086 / test_L096 / test_L097` | 0 | 0 |
| `test_R20_关键字前缀标识符.light` | 0 | 0 |
| `test_R20_词法边界综合.light` | 1 | 0 |
| `test_L095.light` / `_taskM_integration_r17.light` | 0 | 0 |

### 3.3 全量 examples 扫描（`_sweep_r20.py`，296 个 `test_*.light`）

| 版本 | 绿 | 红 | 说明 |
|---|---|---|---|
| 首版（宽泛合并，已放弃） | 187 | 108 | 104 个为真回归 |
| 首版（收敛版，未加 (c) 守卫） | 284 | 11 | 6 个为 `断言为真` 家族回归 |
| **最终版（含 (c)/(d) 守卫）** | **292** | **3** | 3 红 = `test_L101`（合规 EXPECT_RED）+ `test_L143`（合规 EXPECT_RED）+ `test_会话存储`（**既有失败**，HEAD 同样红） |

- `test_会话存储.light` 归因证据：`_sweep_head_r20_final.py` 用 **HEAD 词法器**复跑该文件 2 次均 rc=1 → **PREEXISTING**，非本轮回归。
- **计数口径说明**：上表为 `_sweep_r20.py` 原始输出（295 个执行文件 = 296 − 脚本 `EXPECT_RED` 中陈旧的 `test_L095`）。`test_L095` 本轮已 rc=0，故**全量 296 个 test_*.light 的真实绿数 = 292 + 1 = 293**（293 绿 + 2 合规 EXPECT_RED + 1 既有红 = 296）。

### 3.4 反跑（`_antirun_r20_t1_词法关键字前缀.py`）

```
判据A PASS: 断裂态 test_宿主上下文.light 红 (rc=1)
判据A PASS: 断裂态 test_宿主事件.light 红 (rc=1)
判据B PASS: 修复态 test_宿主上下文.light 绿 (rc=0)
判据B PASS: 修复态 test_宿主事件.light 绿 (rc=0)
判据C PASS: 正向控制 test_R20_关键字前缀标识符.light 绿 (rc=0)
判据C PASS: 词法探针过（甲加乙切分 / 合并为切出为 / 返回真独立）
ALL OK
```

---

## 4. 扫描清单（引自任务3 报告，供对照）

- 扫描文件 641（lightharness `src/examples/tests` + light-merge `examples/stdlib`）
- 关键字前缀标识符（去重）**1708** 个；**定义登记后全部整体成词，0 个切碎**
- 真实受影响的赋值尾巴形态：`设 X为 {}`（`X` 不含关键字）、无空格赋值 `设甲为…`、`返回真/为空值` 尾形态

---

## 5. 已知局限

1. **词尾非值起始的 `甲为乙`** 仍整体成标识符（`为` 后跟标识符而非值起始 → 不切分）。历史口径一致，非本轮引入。
2. **`为` 后带空格再跟值字面量**（`设 甲为 空`）仍按 `甲为`(IDENT) + `空` 处理；现有套件未出现该写法。
3. `_EMBED_MAX_MATCH_KEYWORDS` 仍为**白名单**（`为/返回/尝试`），本质是「打地鼠」式维护；
   泛化到全部非运算符关键字已实测引发 104 个解析回归（`接收参数`/`如果条件` 等合法「关键字+名」被误并）→ **明确不做**。
   根治方向：`复刻harness驱动_light升级计划.md` **P0-A 词法确定性切词重构**。
4. **新增缺陷 L-152（本轮发现，未修）**：嵌套段落（段落体内定义段落）的**形参名不被 `_scan_user_definitions` 覆盖**，
   故 `段落 内层返回 接收 函数值:` 的 `函数值` 仍被切成 `函数` + `值` → `name '值' is not defined`；
   模块级同形态正常。临时处置：`examples/test_R20_词法边界综合.light` 该处形参改用 `算子` 并就地加注。
5. **例外③④ 归他路**：`test_L095`（任务2，本轮已复核 rc=0）、`_taskM_integration_r17`（任务3，评估为必要测试，现 rc=0）。

---

## 6. 交付物索引

| 类型 | 文件 |
|---|---|
| 修复代码 | `light-merge/src/lexer.py`（未提交，归路M） |
| 复现/覆盖用例 | `lightharness/examples/test_R20_关键字前缀标识符.light`、`lightharness/examples/test_R20_词法边界综合.light`（跨任务4，本轮修其语法/限制问题） |
| pytest 层 | `lightharness/tests/test_R20_词法边界.py`（76 passed，含翻转后的赋值尾巴断言与调用语境守卫） |
| 反跑脚本 | `lightharness/_antirun_r20_t1_词法关键字前缀.py`（ALL OK） |
| 归因脚本 | `lightharness/_sweep_r20.py`、`_sweep_head_r20_final.py`、`_probe_r20_state.py` |
| 交付报告 | 本文件 |
