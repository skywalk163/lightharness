# 第17轮任务6 交付报告 —— 宿主配置接线原型

> 对标编号：#115 ｜ 差异编号：R17-D6 ｜ 缺陷预分配：L-150
> 模块：`src/宿主配置.light`（320行 / 50段）｜ 测试：`examples/test_宿主配置.light`（143行）
> 反跑：`_antirun_t6_宿主配置.py` ｜ 日期：2026-09-13

## 一、上游对应与实现映射

自包含配置接线框架，只读复用 `设置.light`（分层合并/脱敏值）+ `凭据.light`（凭证引用）：

| 设计面（任务书） | 光明实现（`宿主配置.light`） | 复用 |
|---|---|---|
| 配置注册表 | `造配置项`/`注册配置项`/`获取配置项`/`列举配置项`（命名空间/分组/可变性过滤）/`校验类型` | — |
| 配置存储与获取 | `造存储`/`获取配置`/`尝试获取配置`/`设置配置`/`重置配置`/`获取配置来源`/`配置快照` | 默认存于注册表项 |
| 配置加载器 | `环境变量到键`/`按优先级升序`/`合并层数据源`/`加载配置层`/`加载全部` | **复用 `分层合并`** 递归合并层数据源 |
| 配置变更事件 | `监听配置变更`/`取消监听`/`变更前被否决`/`派发变更后`/`记录变更`/`获取变更历史` | 变更前可否决 |
| 凭证接线 | `造凭证表`/`注册凭证提供者`/`解析凭证`/`凭证脱敏` | **复用 `凭证引用`**（格式校验，非法抛错）；**复用 `脱敏值`**（secret → `***`） |
| 配置验证器 | `验证配置项`/`验证全部配置` | 类型 + 自定义校验函数 |

## 二、实现要点

1. **默认值来源**：默认存于注册表项 `项["默认值"]`；`获取配置` 优先当前值表，回退默认值，均无则抛错（避免误用独立 `默认值表` 字段）。
2. **可变性检查**：`只读` 且已有当前值时 `设置配置` 抛错（首次写入放行，符合"默认值→当前值"语义）。
3. **类型校验**：`校验类型` 用 `是字符串/是数字(是整数或浮点)/type(值)==bool/是字典/是列表`；布尔用 `type(值)==bool`（规避 L-129 的 `0==假` 陷阱）。
4. **加载优先级**：`按优先级升序` 选择排序（光明无内置 sort），`加载全部` 由低到高依次应用，**高优先级覆盖低优先级**；`合并层数据源` 复用 `分层合并` 验证多层递归合并。
5. **变更事件**：`设置配置`/`重置配置` 触发变更前（可否决，返回 `{允许:假}` 即阻止）/变更后监听；累计 `变更历史`。
6. **凭证接线**：`解析凭证` 复用 `凭证引用` 校验引用格式（非法抛错），按 `env>user>project>default` 优先级从各提供者解决；`凭证脱敏` 复用 `脱敏值` 标 secret → 显示 `***`。
7. **验证器**：`验证全部配置` 遍历注册项做类型+自定义校验，返回 `{通过, 错误列表}`。

## 三、验证

- **测试 `examples/test_宿主配置.light`**：**34 断言**（29 断言相等 + 5 断言真），`test_宿主配置 PASS`（RC=0）
  - 覆盖 §1 注册表 / §2 存储获取（默认/当前/来源/快照/类型/校验）/ §3 加载器多层合并 / §4 变更事件（触发/取消/历史/可否决）/ §5 凭证接线（解析/脱敏/非法引用）/ §6 验证器，正常·边界·异常三态。
- **反跑 `_antirun_t6_宿主配置.py`**：**3/3 ALL OK**（sha256 `b8da246b4691`）
  - A 获取配置跳过当前值 → 2b 红；B 只读可变性检查被删 → 4f 红；C 加载优先级排序翻转 → 3d 红；恢复后回归绿。

## 四、语言缺陷处置（L-150 空缺）

**本轮未遇到真实新语言缺陷，L-150 空缺顺延。**

排查说明（均为笔误/设计失误，非语言局限，不登记）：
- `store` 笔误（应为 `存储`）、`值是数字`→`是数字`、`层File`/`层Env` 标识符不一致、`字符转小写` 不存在（改为去前缀+下划线转点，测试用全小写 env 名）——均属调用侧失误。
- 默认值未写入独立 `默认值表`、变更历史计数预期偏差、`3a` env 映射期望值写错——设计/断言侧失误。
- 已规避保留字（`定义`/`回调`/`等待`/`尝试` 等），本路模块名用 `注册表`/`存储`/`凭证表` 等安全词，无保留字冲突。

## 五、剔除与偏差

- 上游 schemastery 模式解析、profile/manifest/patch 层、launcher flags 等**未逐行复刻**（任务书明确定位为接线框架，仅定义接口 + 内存模拟 + 复用既有纯逻辑）。
- 环境变量解析为简化映射（`DSH_` 前缀剥离 + `_`→`.`），值类型转换仅做字符串/数字/布尔的朴素转换（未做严格 schemastery 类型推断）。
- 命令行参数解析为简化 `--键=值`/`--键 值`（未做全量 argv 分词）。

## 六、移交

- 对标 #115 / 差异 R17-D6，交付物：模块 + 测试（34断言） + 反跑（3/3） + 本报告。
- 缺陷账 L-150 空缺，由路M 统一回填（铁律③ docs 归路M）。
- 全程零越界：仅新增互斥文件表内文件，未修改任何既有 `src/` 模块（含 设置.light/凭据.light，仅只读导入），未 commit。

---
*纪律：中文回复 / 反跑自定位 BASE=__file__ / 测试≥12断言 / 反跑 A/B/C 三判据 / 不碰 docs 与既有模块。*
