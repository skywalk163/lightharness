# 外发任务：光明语言缺陷修复 D 批次（L-068~L-074）

> 本批次 7 条缺陷均在 lightharness Web 大模型配置功能开发（2026-09-09）中实测发现，已登记入 `docs/功能对标/语言缺陷账.md`（L-068~L-074）。
> 按修复领域拆为 3 个可并行任务（D1/D2/D3），分发至光明开发团队（A9 泳道）。

## 缺陷总览

| 编号 | 缺陷 | 修复领域 | 任务 |
|------|------|----------|------|
| L-068 | 类方法定义边界不清晰，方法易写在类外 | 编译器/语法 | D1 |
| L-073 | 运行时 AttributeError/KeyError 未翻译为光明层可读提示 | 运行时/错误提示 | D1 |
| L-069 | `序列化JSON` 不支持光明布尔值 `真`/`假` | 序列化 | D2 |
| L-072 | `转字符串` 对字典输出 Python repr，非 JSON/友好格式 | 序列化/字符串化 | D2 |
| L-070 | 默认参数不支持 `{}`/`[]` 字典列表字面量 | 编译器/语法 | D3 |
| L-071 | 普通字典键访问不安全，无通用 `.get()` 安全访问原语 | 标准库/语言特性 | D3 |
| L-074 | 类型判断函数 `是字典`/`是字符串`/`是列表` 非内置原语 | 标准库/内置 | D3 |

## 共享约束

1. **每条缺陷必须有最小复现用例**（`examples/test_L0XX.light`），修复前立红、修复后变绿。
2. **不破坏现有回归**：修复后 `python -m pytest tests/` 全绿（当前基线 188 passed）。
3. **向后兼容**：现有绕法（数字代替布尔、`空` 代替 `{}`、`取可选` 等）必须继续可用，不得因修复而失效。
4. **交付物**：修复代码 + 最小复现用例 + 修复说明（根因 + 改动点 + 验证结果）。
5. **缺陷账状态更新**：修复后将对应条目的状态从「新发现」改为「已修复（日期；验证绿）」，并补充复现套件路径。

## 任务文件

- `外发任务_光明缺陷_D1_编译器与错误提示.md` — L-068 + L-073
- `外发任务_光明缺陷_D2_序列化与字符串化.md` — L-069 + L-072
- `外发任务_光明缺陷_D3_语言特性补齐.md` — L-070 + L-071 + L-074
