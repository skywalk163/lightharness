# 第41轮任务4交付：多平台矩阵基建

## 结论

任务4完成。`scripts/多平台矩阵.py` 已在 Windows 10 与 0.82 FreeBSD 15.1-STABLE 上实跑；四个核心用例两平台全部 rc=0，退出码矩阵一致。0.82互举反跑也已完成，0新增解析失败。

## 交付物

- `scripts/多平台矩阵.py`
- `docs/多平台差异清单.md`
- `reports/R41_windows_matrix.json`
- `reports/R41_freebsd_matrix.json`
- `reports/R41_windows_compare.json`
- 本报告

## 核心矩阵

| 用例 | Windows | FreeBSD 0.82 | 输出摘要 |
|---|---:|---:|---|
| `test_CLI命令面.light` | 0 | 0 | CLI尾部因密钥环境不同而有预期差异 |
| `test_R39_系统提示.light` | 0 | 0 | 摘要哈希一致 |
| `test_R39_作用域.light` | 0 | 0 | 摘要哈希一致 |
| `test_R40_端到端互举.light` | 0 | 0 | 摘要哈希一致 |

`--compare` 结果：`COMPARE PASS`，两端用例名与退出码字典一致。

## 0.82环境探测

- OS/内核：FreeBSD 15.1-STABLE amd64
- Python：3.11.16（`python3.11`）
- shell：`/bin/sh`
- 路径分隔符：`/`
- 换行：`\n`
- locale / 首选编码：UTF-8
- 文件系统编码：utf-8
- IPv6能力：true
- 环境变量大小写：实测敏感
- 三方库：矩阵与反跑均只依赖标准库；SSH客户端依赖仅存在于验证端，不下沉至远端脚本

## 互举反跑联动

Windows 本机与 0.82 均执行完成：

- 扫描 644 个 `.light`
- 可解析 640
- 基线内语法失败 4
- 新增失败 0
- 信息变化 0
- rc=0

两平台结果与既有基线一致。本机识别编译器修订为 `d58f1478`；远端归档不含 `.git`，故修订显示 `unknown`，但光明版本识别为 7.0.0。这不影响基线失败集合对比。

## 设计说明

- 仅用 Python 标准库。
- 固定收集平台、Python、shell、路径、换行、编码、socket、环境变量大小写等探测项。
- 子进程统一 UTF-8；输出换行标准化后计算摘要。
- 比对以用例集合与退出码一致为硬判据；输出摘要用于定位，不把耗时差异误判成失败。
- 任务书要求的 fork/spawn 差异未在当前核心集实际触发，因此清单明确标为未直接测试，不臆造结果。

## 约束核对

- 仅新增 `scripts/`、`docs/`、`reports/` 与报告文件；未改 `src/` 业务模块。
- 0.82使用 `/tmp` 临时副本只读执行。
- 所有平台结论均来自实测JSON或命令输出。
