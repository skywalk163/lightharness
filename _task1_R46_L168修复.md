# 第46轮 任务1：L-168 修复 —— `test_启动配置.light` 的「假 L-006 夹具」

## 缺陷回顾（第45轮登记，立项未修）

`lightharness/examples/test_启动配置.light` 第 78 行原注释：

```
# 模块级测试夹具（主() 内赋值；L-006 自动 nonlocal 允许跨段读写）
设 测试主目录 为 "/home/u"
设 临时profile目录 为 ""
设 临时缺失目录 为 ""
设 临时根 为 ""
```

**注释声称的「L-006 自动 nonlocal 允许跨段读写」实测不成立**（第45轮 `_r45_L006探针` 已证：
跨段读 = `[]`，生成的 Python 里没有任何 `global`/`nonlocal`）。两个包装段落
`抛数组manifest` / `抛缺失manifest` 正是靠这个错误主张，想通过模块级变量向段落传路径。

## 为什么一直没暴露：消费方是死代码

```
段落 抛数组manifest:
  读取profile清单("t", 临时profile目录)     ← 读模块级变量（永远读到 ""）
段落 抛缺失manifest:
  读取profile清单("t", 临时缺失目录)
```

全仓检索确认：**这两个段落从未被任何地方调用**。它们的语义职责（③ 数组 manifest 抛错 /
③ 缺失 manifest 抛错）早已由 `主()` 内联的 `try/catch` 承担（第 122–142 行，带文案断言）。

所以缺陷的真实状态是「**陷阱**」而非「假绿」：死代码不执行 → 不会红；但任何后来者照
注释去写「模块级变量传参给段落」的新代码，都会立刻踩坑。

## 本轮修法

| # | 改动 | 理由 |
|---|---|---|
| 1 | 删除 `抛数组manifest` / `抛缺失manifest` 两个包装段落 | 死代码，且是错误主张的唯一载体 |
| 2 | 删掉模块级 `临时profile目录` / `临时缺失目录` / `临时根` 声明 | 它们只在 `主()` 内被写，写在模块级纯属误导 |
| 3 | `主()` 内 `设 临时profile目录 为 profile目录`（106 行）删除 | 死代码删掉后的无用赋值 |
| 4 | 保留 `临时根` / `临时缺失目录` 在 `主()` 内作为**局部**变量 | 局部写了局部读，语义正确（132→137 行） |
| 5 | 第 78 行注释整段重写 | 说清「读模块级安全、写回必须 `全局`」+ L-168 由来 |

## 关键判据：断言零减少

- ③ 的两处校验（数组 manifest / 缺失 manifest）**本来就是** `主()` 内联 `try/catch`
  + `断言包含(错2, "must hold a JSON object")` / `断言包含(错3, "failed to read profile manifest")`，
  删掉的是**从未执行的重复路径**，覆盖度不变。
- 其余 12 组断言（①~⑫）一行未动。

## 验证

| 项 | 结果 |
|---|---|
| `python 运行.py examples/test_启动配置.light` | **rc=0，输出 `test_启动配置 PASS`** |
| 连跑 3 次 | 3/3 rc=0（稳定） |
| L-166 全语料重扫该文件 | **0 命中**（原 3 条） |

> 中途出现过 1 次 rc=1：与另一个并行 Bash 同时跑导致的 `.test_启动配置` 临时目录争用
> （Windows 文件锁），单独复跑与连跑 3 次均绿。**非代码缺陷，不修。**

## 交付物

- `examples/test_启动配置.light`（改）
- 缺陷账 L-168 标记为**已修复**
