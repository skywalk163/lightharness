# 第7轮收口说明 —— 宿主逼近（CLI 命令面 + 可落地宿主卡纯逻辑）

- 轮次：第 7 轮（宿主逼近）｜日期：2026-09-11
- 任务书：`宿主逼近_第7轮_任务prompt分发.md`（3 并行 + 路M）
- 合入后 HEAD：`854e9bb` ｜ 全量 CI：**218 passed / 0 failed（保持全绿）**

---

## 1. 本轮目标与结果

把 13 张宿主/OS 层卡中**可落地为光明纯逻辑**的 4 张卡移植完毕，命令面与关键行为进一步逼近 deepseek-harness。

| 项 | 结果 |
|---|---|
| 合入 | 任务1/2 工作区直提（`98dac05`/`ae8824a`）+ 任务3 分支 `--no-ff` 合并（`854e9bb`），零冲突 |
| 改动 src | 4 个（`总入口.light` +144 行命令面 / `远端总线.light` 白名单段 / `抓取策略.light` +11 段 / `webhook会话.light` 5 行内） |
| 新增测试 | 3 个 examples/*.light（test_CLI命令面 / test_远端白名单 / test_IP安全校验） |
| 回归门禁 | **218 passed / 0 failed（保持全绿）**，smoke 5/5 |
| 缺陷账 | **L-081 新登记**（尾随逗号不支持；L-080 维持） |
| 对标清单 | #11/#55/#47/#71 四卡回填「纯逻辑面已覆盖，宿主面维持登记」 |

## 2. 三路摘要

### 任务1｜CLI 命令面（#11）—— 直提 `98dac05`
- `总入口.light` 新增 `HARNESS_CMD` 多命令调度：`run`（缺省，行为逐字节不变）/ `dump-config`（打印生效配置：模型/单价表/压缩阈值 + **每行来源标注** 环境变量/.env/内置默认，对齐 dump-config.ts「注释命名每个来源」，不 boot 不读密钥）/ `version`（0.1.5-lh）/ `help` / 未知命令非零退出。
- 对齐 args.ts「launcher 只解析自己的 flags」哲学；光明无 Cordis profile → profile/patch/plugin 层与 argv 子命令明确「维持登记」。
- run 本体重命名 `执行运行`，正文零改动；测试 5 断言绿 + CLI 冒烟 7 调用全符合预期；反跑 3/3。

### 任务2｜remotes 转发白名单（#55）—— 直提 `ae8824a`
- `远端总线.light` 新增 19 条白名单（`转发白名单`）+ `事件可否转发` / `转发模式`（emit/waterfall）/ `名单转义模式`，逐条对齐 remote-events.ts；旧 11 项清单原样保留（既有测试依赖）。
- 测试 3 组（长度 19 / 19 条全量断言 / 命中未命中）全绿；反跑 4/4。
- **语言缺陷移交（编号修正）**：任务2 报告声称「模块级多行字典解析失败」建议 L-080——路M 探针复核：**模块级多行字典字面量本身可用**，真实缺陷是**尾随逗号不支持**（段落内/模块级均报「意外的标记: ]」且定位误导）；编号 L-080 已被第6轮占用 → 修正登记 **L-081**。

### 任务3｜URL/IP 加固 + webhook（#47/#71）—— merge `854e9bb`
- `抓取策略.light` +11 段：剥IPv6括号 / 是IP字面量（IPv4 精确 + IPv6 字符扫描近似）/ IP是否公网（IPv4 11 个拒绝前缀按段精确 + IPv6 前导近似）/ 主机是否非公网字面量（等价 isNonPublicIpLiteral）。
- `webhook会话.light` `初始模型监听` 改**载荷优先**（对齐 session.ts `{agent}` 解构简化），保留 装配槽 回退与空守卫（保既有直调契约）。
- 测试 3 组判据全绿（含 IPv4 11 前缀全覆盖）；反跑 2/2；分支自验 CI 215/0（任务1/2 测试合入后 + 2 复现用例 = 218/0）。

## 3. 回归与反跑汇总

| 路 | 反跑判据 | 回归 |
|---|---|---|
| 任务1 | 3/3（来源文案/未知命令退出码/版本号） | test_总入口/web路由/入口默认模型/联调CLI/令牌计量 全绿 |
| 任务2 | 4/4（删一条/模式改错双向） | test_远端总线 绿 |
| 任务3 | 2/2（127.0.0.1 放行错/8.8.8.8 拒绝错） | test_抓取策略/webhook会话 绿 |
| 路M | 全量 CI **218 passed / 0 failed**（= 213 基线 + 3 新用例 + _repro_L080/_repro_L081 复现用例 2），smoke 5/5 |

## 4. 缺陷账收口

- **L-081 新登记**（2026-09-11）：字典/列表字面量不支持尾随逗号，报「意外的标记: ]」定位误导；模块级多行字典字面量本身可用（任务2 绕法可简化）。绕法：末尾条目不加逗号。最小复现 `examples/_repro_L081.light`（修复形态 rc=0，进 CI 门禁）。
- **L-080 维持**（第6轮，浮点除法/切片）；L-079 维持（生成器错位 else）。

## 5. 对标清单回填（4 卡）

| 卡 | 回填 |
|---|---|
| #11 CLI 入口 | 命令面（run/dump-config/version/help+来源标注）已覆盖；profile/patch/plugin + argv 宿主面维持登记 |
| #55 api/remotes | 转发白名单 19 条已覆盖；gateway 总线/网络 IO 宿主面维持登记 |
| #47 web-fetch-http | isNonPublicIpLiteral 纯逻辑面已覆盖；DNS 解析/地址固定/NAT64 探测宿主面维持登记 |
| #71 webhook | 初始模型监听载荷优先已对齐；事件总线/scoped Agent 装配宿主胶水维持登记 |

## 6. 遗留事项

1. **宿主面维持登记（未做）**：#12 subprocess linux-execve/scope、#13 sandbox profiles、#20 code-runtime、#21/#57 e2b、#37/#40 acp 协议宿主、#56 hooks 提供方、#62 typert registry、#28 feedback（纯逻辑已确认不变）、#10 hooks 提供方包——OS/协议宿主，Windows 侧无等价可移植面。
2. **光明侧改进建议**（缺陷账登记项）：L-080 切片下标类型报错提示；L-081 尾随逗号支持/报错定位。
3. **上游窗口**：仍无 0.1.5-rc.2 之后官方版本。
4. 工作区仅剩 `_taskM_*` 路M 工具（保留备查）；`task-7-ip-webhook` 分支与 `wt-R7T3` worktree 已清理（收口动作）。

