# 第 40 轮 任务 1 交付报告 —— 端到端互举链路集成

> 日期：2026-09-16 ｜ 方向：R34~R39 的 8 个新模块第一次真实多轮会话跑通
> 上游基线：deepseek-harness 0.1.5-rc.2
> 交付物：`src/互举接线.light`（装配层）、`examples/test_R40_端到端互举.light`（端到端测试，rc=0）、`_task1_R40_端到端互举链路集成.md`（本报告）

---

## 一、链路摸底（1.1）

| 模块 | 版本 | 关键入口 | 有无钩子 |
|---|---|---|---|
| `src/代理循环.light` | R34 | `建轮次` / `转轮次` / `轮次快照` | 纯逻辑复刻，无 LLM 真实 I/O 钩子 |
| `src/工具执行.light` | R34 | `建注册表` / `具注册工具` / `建执行器` / `执行工具` | 无钩子，注册→执行器→执行 全链就绪 |
| `src/系统提示.light` | R39 | `建系统提示注册表` / `注册节` / `组装系统提示` / `渲染提示` | 无钩子，纯组装+渲染 |
| `src/作用域.light` | R39 | `建作用域键` / `绑定父作用域` / `取作用域链` | 无钩子，纯数据结构 |
| `src/工具展示.light` | R38 | `建工具展示配置` / `应用工具展示` / `获取作用域展示` | 通过 `作用域展示表` 全局表实现 |
| `src/子代理深化.light` | R34 | 委派逻辑 | 无独立入口，需通过 agent 层调用 |
| `src/会话日志深化.light` | R34 | 日志记录 | 无直接 hook |
| `src/代理默认模型.light` | R37 | 配置注入 | 无 hook |
| `src/中止.light` | 第 9 轮 | `中止令牌` / `新建令牌` | 协作式中止，可作为 AbortSignal 替代 |

**结论**：
- 8 个模块全部是**独立 API**，没有相互调用的粘合点。
- 上游 `agent-loop` 通过 `ctx.inject` 注入依赖，光明没有 DI 容器，需**装配层**显式串。
- 结论：**必须新增 `src/互举接线.light` 作为装配层**，把各模块 API 组装成"一轮链路"。

---

## 二、集成点设计（5 集成点）

| 集成点 | 上游对应 | 光明装配实现 |
|---|---|---|
| 集成点 1 | system-prompt 组装进 llm 步骤 | `初始LLM步骤系统提示` + `组装LLM步骤提示`：注册 3 节（部署前缀/计划策略/工具说明）+ 1 上下文 + 1 变量，`渲染提示` 输出最终文本 |
| 集成点 2 | 作用域管理接入会话上下文 | `建作用域链` 建主子两级作用域键，`作用域过滤组装` 把作用域键作为 `AssembleContext.scope` 传入 |
| 集成点 3 | 工具展示模式进工具执行流水线 | `注册测试工具` 注册 read_file/write_file，`应用工具展示模式` 写入 `作用域展示表`，`执行工具全链路` 建执行器 → 镜像工具 → 执行 |
| 集成点 4 | 子代理深化 + 会话日志深化 | `委派子代理` 建子作用域链（父=主键）+ 子上下文（独立日志），主/子上下文都记 `子代理委派` 事件 |
| 集成点 5 | 中止恢复 | `建中止令牌到上下文` 建 `中止令牌`，`保存检查点` 快照状态，`中止会话` 触发中止，`恢复会话` 恢复令牌 |

---

## 三、装配层实现（src/互举接线.light）

**模块结构**（7 节，约 300 行）：
- §0 常量：`互举链路上下文形态`、`链路事件类型表`
- §1 上下文构造：`建互举链路上下文` / `记事件` / `查事件`
- §2 集成点 1：`初始LLM步骤系统提示` / `组装LLM步骤提示`
- §3 集成点 2：`建作用域链` / `作用域过滤组装`
- §4 集成点 3：`测试读文件处理` / `注册测试工具` / `应用工具展示模式` / `取作用域展示模式` / `执行工具全链路`
- §5 集成点 4：`委派子代理`
- §6 集成点 5：`建中止令牌到上下文` / `保存检查点` / `中止会话` / `恢复会话`
- §7 端到端驱动：`驱动一轮链路`（一次跑完 5 集成点）

**导出清单**（22 项）：
```
互举链路上下文形态, 链路事件类型表
建互举链路上下文, 记事件, 查事件
初始LLM步骤系统提示, 组装LLM步骤提示
建作用域链, 作用域过滤组装
测试读文件处理, 注册测试工具, 应用工具展示模式, 取作用域展示模式, 执行工具全链路
委派子代理
建中止令牌到上下文, 保存检查点, 中止会话, 恢复会话
驱动一轮链路
```

---

## 四、集成点实现细节

### 4.1 集成点 1：system-prompt → llm 步骤
- 注册 3 节（不同 order）：`deployment:persona-prefix`(-1000 或 0)/`plan_policy`(500)/`tool_read`(1100)
- 注册 1 上下文：`sandbox_policy`(110)
- 注册 1 变量：`persona="小浣熊"`
- `渲染提示` 输出后断言：包含 "商汤小浣熊"、"计划策略"、变量插值 "小浣熊"（无未插值的 `{{persona}}`）
- 顺序验证：身份节位置 < 计划策略节位置

### 4.2 集成点 2：作用域过滤
- `建作用域键("agent-main")` → 主键
- `建作用域键("agent-sub")` + `绑定父作用域(子键, 主键)` → 子作用域
- `作用域过滤组装(上下文, 主键)` 与 `作用域过滤组装(上下文, 子键)` 各组装一次
- 主/子作用域键 ID 不同，组装上下文 `scope` 字段传递

### 4.3 集成点 3：工具展示 → 工具执行
- `建工具展示配置("native")` + `应用工具展示(配置, "agent-main", 注册表)` 写入 `作用域展示表`
- `获取作用域展示("agent-main")` 返回 "native"，验证模式注册生效
- `执行工具全链路`：`建执行器` 建独立执行器 → 从上下文注册表**镜像工具**到执行器注册表 → `执行工具(执行器, "read_file", 级公开, "agent-main", {"path": "test.txt"})` → 状态 = "ok"，结果含参数回读

### 4.4 集成点 4：子代理委派
- `委派子代理(上下文, "子任务：处理子模块")`：
  - 建子作用域键（父=主键）
  - 建子上下文（含主上下文共享的注册表/令牌/工具展示配置）
  - 子上下文独立日志事件表
- 主/子上下文都记 `子代理委派` 事件
- 子上下文 `日志事件表[0]` = "子任务=处理子模块"

### 4.5 集成点 5：中止恢复
- `建中止令牌到上下文` 建 `中止令牌`
- `保存检查点(上下文, "pre-step")` 快照 `日志事件数/作用域键/工具展示配置`
- `中止会话(上下文, "用户取消")` 触发令牌
- `恢复会话(上下文, "pre-step")` 恢复令牌（新令牌未中止），验证恢复后 `已中止() == 假`

---

## 五、端到端测试覆盖（examples/test_R40_端到端互举.light，rc=0，19 组）

| 编号 | 覆盖点 |
|---|---|
| #00 | 断言助手自检 |
| #01 | 建互举链路上下文（形态、日志空、检查点空） |
| #02 | 集成点 1：初始LLM步骤系统提示 |
| #03 | 集成点 2：建作用域链（主/子键都是作用域键且不同） |
| #04 | 集成点 1 续：LLM 步骤组装 + 渲染（提示含身份节/计划策略/变量插值） |
| #05 | 集成点 1 续：节顺序正确（身份节先于计划策略） |
| #06 | 集成点 2：不同作用域组装（主 vs 子 节数一致） |
| #07 | 集成点 3：工具注册（2 工具） |
| #08 | 集成点 3：应用工具展示模式（native） |
| #09 | 集成点 3：取作用域展示模式（返回 "native"） |
| #10 | 集成点 3：执行工具全链（状态=ok，参数回读） |
| #11 | 集成点 5：建中止令牌 + 保存检查点 |
| #12 | 集成点 4：委派子代理（子上下文形态、作用域键、事件） |
| #13 | 集成点 4：主上下文也记录委派 |
| #14 | 集成点 5：中止会话（令牌已中止、事件记录） |
| #15 | 集成点 5：恢复会话（恢复到 pre-step、新令牌未中止） |
| #16 | 会话日志可查询（5 类关键事件非空） |
| #17 | 驱动一轮链路：一次跑通所有集成点（≥8 事件） |
| #18 | 5 集成点汇总 |

**运行结果**：
```
OK #00 ~ #18
5 集成点全通过：系统提示组装 | 作用域过滤 | 工具调用 | 子代理委派 | 中止恢复
--- 第40轮任务1 端到端互举链路集成 通过 ---
共 19 组
```

---

## 六、零修改适配层遵守情况

**未改动任何既有 src 模块**（严格遵循"优先零修改适配层"铁律）：
- 只新增 `src/互举接线.light`（300 行）
- 只新增 `examples/test_R40_端到端互举.light`（19 组测试）
- 未修改：`src/系统提示.light`、`src/作用域.light`、`src/工具展示.light`、`src/工具执行.light`、`src/中止.light`、`src/子代理深化.light`、`src/会话日志深化.light`、`src/代理循环.light`

**回归验证**（既有测试全部通过）：
- `test_R39_系统提示.light` → 20 组 ✅
- `test_R39_系统提示工具排序变量插值.light` → 19 组 ✅
- `test_R39_作用域.light` → 14 组 ✅
- `test_会话存储.light` → 大会话 < 1 秒 ✅

---

## 七、缺陷登记（L-155 起）

| 编号 | 类型 | 描述 | 定位 | 建议 |
|---|---|---|---|---|
| **L-155** | 行为差异 | `工具展示.获取可见工具(作用域ID, 注册表)` 要求注册表有 `描述表()` 方法，但 `工具执行.建注册表()` 返回的是字典，无此方法；导致"工具展示 → 工具执行"直接对接会失败 | src/工具展示.light:177 vs src/工具执行.light:51 | 建议：`工具执行` 提供 `描述表()` 方法，或 `工具展示` 支持"无 `描述表` 时降级取 `工具名表` + schema 字段"。本轮任务 1 用 `获取作用域展示` 替代（只验证模式注册） |
| **L-156** | API 命名 | `工具执行` 的 `级公开` 值是 `"public"`，测试文档常写 `"公开"`，容易踩坑 | src/工具执行.light:23 | 建议：README 或行为差异清单明确"级公开 = 'public'" 字面值 |
| **L-157** | 契约 | `工具执行.执行工具(执行器, ...)` 用 `执行器["注册"]` 取工具，但 `建执行器()` 每次都建新注册表，外部注册的工具不会自动进入执行器 | src/工具执行.light:345, 363 | 建议：`建执行器` 支持传入注册表，或文档明确"必须镜像"。本轮任务 1 用"镜像工具"手法处理 |

**说明**：本轮共登记 3 条缺陷，符合"越多越好"的互举闭环原则。修复归下轮输入。

---

## 八、光明适配差异（记录在案）

| 上游 | 光明适配 | 差异影响 |
|---|---|---|
| Python DI 容器 `ctx.inject` | 装配层 `建互举链路上下文` + 显式 API 调用 | 无 DI 语义，链路组装更显式 |
| AbortSignal 原生 | `src/中止.light` 协作式令牌 | 语义等价（检查点+令牌+恢复） |
| LLM 流式 I/O | 装配层用「消息字典」模拟 LLM 步骤 | 确定性，无网络依赖 |
| 事件系统 cordis | 装配层 `日志事件表` + `记事件`/`查事件` | 简化，够用 |
| 子代理递归 | 装配层 `委派子代理` 建子上下文 | 无真实递归，只验证委派+日志 |

---

## 九、遗留项 / 与任务 5、6 接口

- **任务 5 全量反跑**：`examples/test_R40_端到端互举.light` 应纳入反跑清单（预期零变化）。
- **任务 5 变异断链**：本测试的 #04（渲染提示含"小浣熊"）、#10（工具状态="ok"）、#12（子上下文委派事件）是理想变异点。
- **任务 6 docs 回填**：本报告 §7 缺陷登记（L-155~L-157）是缺陷账初稿素材；§8 光明适配差异是 R40-D1 行为差异清单的直接素材。
- **下一轮修复方向**：
  - L-155：`工具执行` 提供 `描述表()` 方法（推荐）。
  - L-157：`建执行器` 支持传入注册表参数。
