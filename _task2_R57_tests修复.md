# 第57轮 任务2 交付：tests/ 侧修复（flaky + 墙钟性能断言）

> 日期：2026-09-18 ｜ 目标（任务书 §五）：消除 R56 已取证的两类**假红**，让门判据可信，且不失去判据覆盖。
> 结论：**flaky 用例按方案 A 落地（并且定位到「方案 A 单独不够」的真原因，补上缩短心跳窗口这个决定性杠杆），0.82 实测 30% 红 → 0/10 红；墙钟性能断言阈值按并行实测放宽到 10s 并留痕。全部原断言保留，未 deselect、未退化成恒真。**
> ⚠️ 说明：`light-merge` 是独立 git 仓；本文件按任务书命名落在 `lightharness/`，但被改动的是 `light-merge/tests/` 两个文件（`git status` 只显示这 2 个 tests 文件 + 任务1 的 `src/lexer.py`）。

---

## 1. 5.1 flaky 用例：`test_重派与心跳_杀节点后重派且无静默丢条`

### 1.1 修复前复现（0.82 / py3.12，单用例重复跑 10 次）

```
run 1 rc=0  1 passed      run 6  rc=0  1 passed
run 2 rc=0  1 passed      run 7  rc=1  1 failed
run 3 rc=0  1 passed      run 8  rc=1  1 failed
run 4 rc=0  1 passed      run 9  rc=0  1 passed
run 5 rc=1  1 failed      run 10 rc=0  1 passed
BASE_TOTAL pass=7 fail=3        ← 红率 30%
```

失败原文（3 次完全一致）：

```
>       assert len(失联) == 1, ("心跳：失联应恰为被杀节点 1 个，实际 %r" % 失联)
E       AssertionError: 心跳：失联应恰为被杀节点 1 个，实际 []
E       assert 0 == 1
tests/test_distributed_eval_light.py:265: AssertionError
```

「重派」两条断言（1200 条完整 / 无重复计分）每次都过 —— 红的只是**失联标记的时序**，与 R56 判断一致。

### 1.2 根因（**实测测得，不是沿用 R56 的推断**）

R56 方案 A 假定「轮询等一会儿失联就会出现」。本轮先用诊断脚本 `_r57_diag_flaky.py`（复用官方 helper 复刻 `_跑集群(1200, 杀=1)`，在 kill 时刻与报告完成时刻打点）**实测**，发现问题的真正形状：

**修复前（心跳间隔 0.3 / 心跳超时 1.5，即 R56 默认）：**

| iter | kill@ | kill 时 已汇聚 / 在跑 | **kill→报告完成** | 状态失联 |
|---|---|---|---|---|
| 1 | 1.21s | 263 / 1 | 1.52s | ['N0C20EB37'] ✅ |
| 2 | 1.21s | 245 / 2 | 1.52s | ['NA8596144'] ✅ |
| 3 | 1.22s | 185 / 2 | **1.31s** | **[]** ❌ |
| 4 | 1.12s | 293 / 1 | **1.01s** | **[]** ❌ |
| 5 | 1.24s | 245 / 1 | 1.53s | ['NFA2FAC92'] ✅ |
| 6 | 1.31s | 276 / 2 | **1.34s** | **[]** ❌ |

**关键发现**：监控循环的**完成条件**（`结果表==总数 且 队列空 且 在跑==0`）一旦成立就 `跳出`——它写状态文件（含失联）**在**完成判定之前，但**只写「当前已标出的失联」**。于是：

```
kill→报告完成时间  vs  心跳超时窗口(1.5s)
  > 1.5s  → 监控循环先标失联再完成 → 失联非空  ✅ 绿
  < 1.5s  → 监控循环先完成并退出，失联来不及标 → 失联=[]  ❌ 红
```

实测 kill→报告完成 = **1.01 ~ 1.53s**，恰好压在 1.5s 窗口上 → 抖动 ≈30%。

⚠️ **由此推出：单纯「事后轮询状态文件」不足以修复**——监控循环退出后状态文件就冻结了，之后不会再出现失联标记。所以必须**同时缩短心跳窗口**，让标记必然先于完成。

### 1.3 修复（方案 A + 决定性杠杆；全部原断言保留）

**改动文件：`light-merge/tests/test_distributed_eval_light.py`**

1. `_跑集群` 新增参数 `心跳间隔=0.3`（默认不变，不影响其它用例），并透传给 `_工进程`：
   ```diff
   -def _跑集群(任务数, 杀=None, 容量=None, 心跳超时=1.5, 并发度=4, 慢秒=0):
   +def _跑集群(任务数, 杀=None, 容量=None, 心跳超时=1.5, 并发度=4, 慢秒=0, 心跳间隔=0.3):
   ...
   -        workers = [ctx.Process(target=_工进程, args=(端口, i, 并发度, 0.3, 身份目录, 慢秒)) ...]
   +        workers = [ctx.Process(target=_工进程, args=(端口, i, 并发度, 心跳间隔, 身份目录, 慢秒)) ...]
   ```
2. 新增 `_等失联(状态文件, 被杀节点ID, 上限)`：轮询状态文件直到「被杀节点进 `失联节点`」，**超时 = 心跳超时 × 2** 才放弃（即任务书的方案 A 原语）。
3. `_跑集群` 在**已杀**场景下用 `_等失联` 取代单次读状态：
   ```diff
    报告 = _等报告(报告文件, 任务数)
    状态 = {}
   -    if os.path.isfile(状态文件):
   +    if 已杀 and 被杀节点ID:
   +        状态 = _等失联(状态文件, 被杀节点ID, 心跳超时 * 2)
   +    elif os.path.isfile(状态文件):
        状态 = 解析JSON(读文本(状态文件))
   ```
4. **本用例显式指定心跳窗口**（决定性）：
   ```diff
   -    报告, 状态, 已杀, 被杀节点ID = _跑集群(1200, 杀=1)
   +    报告, 状态, 已杀, 被杀节点ID = _跑集群(1200, 杀=1, 心跳间隔=0.05, 心跳超时=0.5)
   ```
   - `心跳超时 0.5s` **<< kill→报告 1.0s+**（余量 ≈2×）→ 标记必然先于完成；
   - `心跳间隔 0.05s` **<< 心跳超时 0.5s**（余量 10×）→ 存活 worker 不会被误标成失联。

> 不采用的手段：**未** deselect、**未**打 flaky 标记、**未**放宽/下界化任何断言（`assert_quality` 门禁会拦下界断言）。心跳窗口是本用例**自己传入**的场景参数，**未改产品默认值**（master 默认仍是 1.5s）。

### 1.4 修复后验证（0.82 / py3.12，重复 10 次）

```
run 1..10  rc=0  1 passed   （逐条）
FIX_TOTAL pass=10 fail=0
```

**30% 红 → 0/10 红。**

另：改前先用候选参数在诊断脚本上做了 **8 轮预验证**（`心跳间隔=0.05 / 心跳超时=0.5`），8/8 都成功标出失联，且 `kill→报告` 落在 1.01~1.42s；同时确认失联数恰为 1（无误标存活 worker）。

---

## 2. 5.2 墙钟性能断言：`test_lexer_performance_10000_lines`

### 2.1 假红事实（R56 + 基线）

- xdist 并行下同一份 10000 行输入实测 **3.6099s > 2.0s** 阈值 → 恒假红；基线 `082_lightmerge基线_2026-09-18-072454.json` 中该用例红（`2.5448 not less than 2.0`）。
- 根因：**xdist 并行时多个 worker 争抢 CPU，墙钟被系统性拉长**；串行本机约 0.6s，并行 0.82 约 2.5~3.6s。

### 2.2 处置（改阈值 + 留痕）

**改动文件：`light-merge/tests/unit/test_lexer_perf.py`**

```diff
-        perf 预算（秒）可通过环境变量 LEXER_PERF_LIMIT 覆盖，默认 2.0：
+        perf 预算（秒）可通过环境变量 LEXER_PERF_LIMIT 覆盖，默认 10.0。
+
+        阈值依据（R57 任务2，墙钟断言在 xdist 并行下假红）：
+          · 同一份 10000 行输入，0.82（FreeBSD 15.1 / python3.12）xdist 并行实测约 3.6s，
+            串行本机约 0.6s …… 旧的 2.0s 预算在此场景恒假红（R56 实测 3.6099s > 2.0s）。
+          · 故默认放宽到 10.0s：相对并行实测 3.6s 留有约 2.8x 余量，消除并行假红；
+            同时仍能拦住数量级回退（如 O(n^2) 把 10000 行推到数十秒）——断言未被削弱成恒真。
+          · 需要更严格/更宽松的预算时用 LEXER_PERF_LIMIT 覆盖。
-        limit = float(os.environ.get("LEXER_PERF_LIMIT", "2.0"))
+        # 默认 10.0s（并行可接受范围，见上方依据）；LEXER_PERF_LIMIT 可覆盖。
+        limit = float(os.environ.get("LEXER_PERF_LIMIT", "10.0"))
```

**口径说明**：这是**固定阈值 + 注明并行实测依据**（任务书建议的两种合法方式之一），未改成「恒真」、未跳过用例。`assertLess(elapsed, limit)` 仍是真断言——10s 能拦住 O(n²) 级回退。

### 2.3 同类「耗时 < N 秒」墙钟断言扫描（`light-merge/tests/`）

| 位置 | 形式 | 处置 |
|---|---|---|
| `tests/unit/test_lexer_perf.py::test_lexer_performance_10000_lines` | `assertLess(elapsed, LEXER_PERF_LIMIT 默认2.0)` | ✅ 本轮放宽至 10.0 |
| `tests/bench.py:100` | `time.perf_counter()` 记耗时，**无断言**（基准打印脚本，不进 pytest 门） | 无需处置（记录留痕） |
| 其余 `tests/` | 未发现第二处「墙钟 < N 秒」形式断言 | —— |

### 2.4 验证（0.82 / py3.12 并行）

```
pytest tests/unit/test_lexer_perf.py -k "performance_10000"  →  1 passed in 3.15s
```

并行下 3.15s < 10.0s **绿**；改前同口径是红。

---

## 3. 交付物

| 文件 | 变更 |
|---|---|
| `light-merge/tests/test_distributed_eval_light.py` | 新增 `_等失联`；`_跑集群` 增 `心跳间隔` 参数 + 已杀场景轮询；本用例显式 `心跳间隔=0.05 / 心跳超时=0.5`（48 行） |
| `light-merge/tests/unit/test_lexer_perf.py` | `LEXER_PERF_LIMIT` 默认 2.0 → 10.0 + 并行依据注释（16 行） |
| `lightharness/_task2_R57_tests修复.md` | 本文件 |
| 探针（待任务5 移档） | `_r57_diag_flaky.py`（flaky 根因打点诊断；支持 `DIAG_HB_INTERVAL/DIAG_HB_TIMEOUT` 环境变量）、`_r57_082.py`（0.82 远端执行助手） |

## 4. 遗留

1. `_等失联` 是「事后轮询」，单独用**不足以**消除本 flaky（监控循环会先退出）；真正起作用的是**缩短心跳窗口**（§1.2/§1.3）。二者一起用：轮询兜底 + 窗口保证，语义更稳。
2. 本例的 `心跳间隔=0.05` 只作用于本用例；`test_心跳独立于执行_长任务期间不被标失联`（`_跑集群(6, 慢秒=3)`）仍走默认 `心跳间隔=0.3 / 心跳超时=1.5`，行为未变。
3. 未在**串行**模式下补测抖动率（R56 遗留），本轮沿用「py3.12 单跑」口径。
