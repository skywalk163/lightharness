# 缺陷：L-009 文本级批量替换会把注释内嵌的「导入 X」字样拦腰劈开成代码行（工程层陷阱，非语言运行时缺陷）
# 当前：直接 python 运行.py 本文件为绿（注释被词法忽略）；但用文档中的 -replace '导入 builtins
从 JSON 导入 序列化JSON' 脚本处理后，
#       注释行内被插入换行，注释尾巴变成代码行 → 伪语法错误
# 修复后：注释与字符串内的目标串不被文本级替换误伤（工具应跳过注释/字符串），源码不受影响
# 复现目标注释（工程层替换脚本的命中目标）：
# 导入 builtins
从 JSON 导入 序列化JSON
段落 主:
  打印 "L-009 注释内嵌导入字样演示"
  打印 "--- 测试L009 结束 ---"

主()
