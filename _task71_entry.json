{
  "编号": 71,
  "功能": "webhook 会话构建纯逻辑（会话构造/消息注入/错误链/会话校验）",
  "原版包": "packages/webhook/webhook/src/{index.ts, session.ts, types.ts, invariant.ts, brand.ts}",
  "光明模块": "src/webhook会话.light",
  "状态": "done",
  "证据": [
    "src/webhook会话.light",
    "examples/test_webhook会话.light"
  ],
  "本轮目标": "done：1:1 复刻 packages/webhook/webhook 五个源文件的可提取纯逻辑，宿主绑定全部投影剥离。\n\n【原版函数 → 光明段落 对照表】\nindex.ts（178 行）\n  snapshotDelivery(delivery)            → 快照投递(投递)（拆成 kind/source/deliveryId/receivedAt 四道闸 + 可无损JSON 闸）\n  WebhookRuntime.register(rule)         → 注册(规则)（id/kind 非空串闸、run 可调用闸、重名闸、关闭闸）\n  WebhookRuntime.dispatch(delivery)     → 派发(投递)（关闭闸 → 快照 → 按注册序遍历，跳过回收中的登记，仅派发同 kind）\n  WebhookRuntime#startInvocation        → 发起调用(登记, 投递)（前置/后置中止检查 → run → 非空则建会话 → catch 走错误链日志）\n  调用诊断串 provider/source/delivery/rule → 造调用标签(投递, 规则号)（JSON.stringify 各字段）\n  WebhookRuntime#disposeRegistration    → 回收(规则号)（隐藏 → 中止信号 → 幂等）\n  constructor ctx.effect 生命周期收尾    → 关闭运行时()（置关闭态并回收全部登记）\n  static inject（agents/…6 项）          → 剥离，投影为「上下文」字典的能力键约定（见下）\nsession.ts（181 行）\n  requiredString(record, field)         → 必填字符串(记录, 字段)\n  resolveRequest(ctx, input)            → 解析请求(上下文, 输入)（非对象闸、绝对路径闸、五字段必填闸、model 对象闸、maxTokens 正安全整数闸；默认模型分支 vs 显式模型分支分别产出 agentOptions / modelSelection）\n  reportRollbackFailure(ctx, subj, err) → 报告回滚失败(上下文, 主题, 错误)\n  installInitialModelSelection 监听器体  → 初始模型监听(载荷, 下一步)（agent 缺失闸 → 已有持久请求头闸 → provider/model 不匹配闸 → 剥离 reasoningEffort 后回填 selection.reasoningEffort）\n  createWebhookSession 的 setup 回调      → 装配置(代理上下文)（挂载代理预设 + 注册 agent/request 监听器）\n  createWebhookSession(…)               → 创建Webhook会话(上下文, 投递, 规则号, 请求, 信号)（resolve → 权限预设解析 → 代理预设解析 → 常驻键 → 中止检查 → 工作区 → 中止检查 → 会话号生成 → agents.create → try{中止检查/挂载/权限设置/改名/注入提示}catch{摘除回滚/销毁回滚/原错重抛}）\n  catch 里两段内层 try/catch             → 回滚摘除(上下文, 工作区, 会话号) / 回滚销毁(上下文, 句柄, 会话号)（拆具名段落，避开嵌套 尝试）\ninvariant.ts（48 行）\n  installWebhookMessages 判定体          → 校验Webhook准入(会话, 事件, 工作区表)（返回 \"\" 通过 / 失败文案；事件类型闸 → webhook 消息过滤 → 缺 cwd 闸 → 归属数≠1 闸 → 路径不符闸）\n  'internal/dispatch' 入口事件名过滤      → 内部派发检查(事件名, 会话, 事件, 工作区表)\nbrand.ts（39 行）\n  WebhookRuleId / WebhookSourceId / WebhookDeliveryId → 规则标识 / 来源标识 / 投递标识（编译期品牌擦除，运行时恒等投射）\ntypes.ts（84 行）\n  VerifiedWebhookDelivery 字段集          → 快照投递 的四道字段校验（直接取自该 interface）\n  WebhookSessionRequest 字段集            → 解析请求 的字段校验与「已解析」输出形状\n  WebhookModelSelection（含 maxTokens?）  → 解析请求 的模型分支与 agentOptions 构造\n  MessageSourceMap['webhook'] 形状        → 造投递来源(投递, 规则号)（kind/provider/source/deliveryId/ruleId/form='notice'/summary）\n被复刻的跨包纯逻辑（原版 import 而来，本仓必须自带）\n  dsh-llm boundContextSummary             → 绑定上下文摘要(摘要)（120 字上限 + '…'）\n  dsh-llm errorChain                      → 错误链(值) + 取错误文本(值)（cause 链 ': ' 连接，逐字重复片段去重）\n  dsh-llm createUserMessage               → 造用户消息(内容表, 来源, 编号)（role='user'）\n  dsh-session snapshotJsonValue           → 可无损JSON(根)（栈式迭代，深度上限 32，环即假）\n  node:path isAbsolute                    → 是绝对路径(路径) + 是盘符(字)（POSIX 根与 Windows 盘符双形态）\n  Number.isSafeInteger                    → 是安全整数(值)（±2^53-1 界）\n  typeof x === 'function'                 → 是可调用(值)（光明无 callable 内建，用「非全部基本型」反推）\n  deepFreeze / Object.isFrozen            → 深复刻(值, 层) / 深冻结(值) / 是冻结(值)\n\n【剥离的宿主绑定 → 投影】\n  Cordis Context/Service/ctx.effect      → 普通字典「上下文」+ 模块级全局 运行时状态 / 装配槽\n  ctx.logger.warn / debug                → 上下文[\"日志警告\"] / 上下文[\"日志调试\"]（入参能力函数）\n  ctx.permissionPresets.resolve/set      → 上下文[\"权限预设解析\"] / 上下文[\"权限预设设置\"]\n  ctx.agentPresets.resolve/standingKeyFor/mount → 上下文[\"代理预设解析\"] /[\"代理预设常驻键\"] /[\"代理预设挂载\"]\n  ctx.workspaceRegistry.create/list      → 上下文[\"工作区创建\"]（返回 {\"path\",\"挂载会话\",\"摘除会话\"}）/ 不变式校验的 工作区表 入参列表\n  ctx.agents.create                      → 上下文[\"会话创建\"](选项)，选项含 sessionId/signal/meta/agentOptions/setup\n  ctx.sessionTitle.rename                → 上下文[\"会话标题改名\"]\n  ctx.agentDefaultModel.currentSelection → 上下文[\"默认模型\"]()\n  agentCtx.on('agent/request', listener) → 上下文[\"监听\"](代理上下文, 事件名, 监听器)\n  SessionId(`webhook-${randomUUID()}`)   → \"webhook-\" + 上下文[\"唯一标识\"]()\n  AbortController / AbortSignal          → 字典 {\"已中止\": 真假, \"原因\": 串} + 检查未中止 / 中止信号 / 存活信号\n  Promise / await / async（含 Promise.allSettled 排空）→ 光明同步模型：await 点折叠为顺序调用，注册回收为同步幂等操作\n  octokit / github webhook handler（webhook-github 包）→ 完全剥离，本模块零 github/octokit 专有逻辑\n\n【光明降级口径（3 处，已写入模块头注释）】\n  1. undefined 与 null 在光明同为 空。原版 model===undefined（走默认）与 model===null（报 model must be an object）\n     改由「键是否存在」（字典包含键）区分：命中键且值为 空 视作 null → 抛 [WEBHOOK_MODEL_NOT_OBJECT]，与 session.spec 一致。\n  2. Object.freeze 无对应物：deepFreeze 投影为「深复刻（断共享引用）+ 根级 \"冻结\" 标记键」，递归冻结降级为深复刻；\n     是冻结() 只认带标记的根字典。runtime.spec 的 isFrozen(seen)/isFrozen(seen.event) 对应为 是冻结(快照) 与「篡改原投递快照不随之变」。\n  3. 错误对象在光明不是字典（实测 是字典(错误对象)==假），故 错误链() 主路径面向「含 message/cause 键的字典形错误」，\n     非字典值回退 转字符串；报告回滚失败 用 转字符串(e) 取原文，行为与原版一致。\n\n【测试覆盖】examples/test_webhook会话.light 共 10 个正例用例 + 14 组反跑用例，\n断言调用点 225 处（199 条 断言相等 + 26 条 检查抛码，其中若干位于遍历体内展开执行），覆盖 runtime.spec.ts（派发/快照/回收/关闭/不去重/错误收敛）、\nsession.spec.ts（调用序列/消息形状/10 类畸形请求/6 处预检失败/3 处回滚/双重回滚失败/2 处中止/模型监听器）\n与 invariant.spec.ts（缺 cwd / 0 归属 / 2 归属 / 路径不符 / 非 webhook 消息 / 非 session-event）全部输入→输出路径。",
  "反跑判据": "test_webhook会话.light：14 组反跑，共 16 次实测突变全部 RED（rc=1），全部已还原并复跑变绿（rc=0）。\n①反跑_投递闸：删 快照投递 的 kind 空串闸 → 「期望抛错但未抛: 反跑_投递_空种类」即红（同组另 4 条闸 source/deliveryId/receivedAt/非无损JSON 同样被断言守住）；\n②反跑_路径闸：删 workspacePath 绝对路径闸 → 「期望抛错但未抛: 反跑_路径_相对」即红（并把 D:/work/space 放行断言写进同组，双向守）；\n③反跑_模型闸：把 model 对象闸取反（是字典(模型)==假 → ==真）→ 合法模型被拒、「反跑_模型闸: 合法模型放行」即红；删闸则「错误码不符[反跑_模型_空]」亦红；\n④反跑_令牌闸：删 maxTokens 的 NOT_POSITIVE / NOT_SAFE 两道闸 → 「期望抛错但未抛: 反跑_令牌_零 / _负 / _非整」即红；\n⑤反跑_必填闸：把 必填字符串 的空串闸改成 返回 值 → 「期望抛错但未抛: 反跑_必填_空标题/空提示/空代理预设/空权限预设」即红（非对象请求与 42 非字符串另有独立断言）；\n⑥反跑_回滚闸：删 回滚摘除(…) 调用 → 「断言失败[反跑_回滚_permission-set: 必摘除会话]：实际=False 期望=True」即红；\n⑦反跑_原错闸：把 catch 里的 `抛出 e` 换成 `抛出 新建 错误(\"rollback swallowed\")` → 「断言失败[反跑_原错闸: 抛的是原始错误]」即红（同组另有 长度(警告表)==2 与两条 rollback failed 文案断言，回滚失败不上报亦红）；\n⑧反跑_中止闸：删 检查未中止 的 抛出 → 「期望抛错但未抛: 反跑_中止_信号」即红（同组另有 中止点=workspace/agent 两处的 未注入提示 + dispose/detach 归属断言）；\n⑨反跑_种类闸：把 `规则[\"kind\"] == 快照[\"kind\"]` 换成 `真 == 真` → 「断言失败[运行时: 仅同种类 2 条被调用]：实际=3 期望=2」即红；\n⑩反跑_快照闸：把 `返回 深冻结(投递)` 换成 `返回 投递` → 「断言失败[反跑_快照闸: 规则看到派发时刻的旧值]：实际=2 期望=1」即红；\n⑪反跑_去重闸：给 派发 增加 deliveryId 去重表（原版 runtime.spec 明确保持不去重）→ 「断言失败[运行时: 两次派发注入 2 次]：实际=1 期望=2」即红；\n⑫反跑_不变式闸：把 缺 cwd 闸的返回值改成 \"\" → 「断言失败[反跑_不变式: 缺 cwd]：实际=False 期望=True」即红（同组另有 0 归属 / 2 归属 / 路径不符 三类文案断言）；\n⑬反跑_注册闸：删 重名闸 → 「期望抛错但未抛: 反跑_注册_重复」即红（同组另有空 id / 空 kind / 缺 run 三道闸与「回收后可重注册」断言）；\n⑭反跑_关闭闸：删 派发 的 `运行时状态[\"关闭中\"]` 抛出 → 「期望抛错但未抛: 关闭后派发」即红（另测把 派发 遍历守卫改成 假==真 → 「仅同种类 2 条被调用：实际=0」亦红）。\n变异纪律：设计「非法输入被放行」的变异时一律实测确认 RED，并对 ③ 这种会被下游兜底挡住的场景改用「闸取反 + 正向放行断言」双向锁定，避免假绿。",
  "语言缺陷": [
    "L-036",
    "L-037",
    "L-041",
    "L-051",
    "L-054",
    "L-055",
    "L-056",
    "L-057",
    "L-063",
    "L-065"
  ]
}
