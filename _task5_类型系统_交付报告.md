# 任务5 交付报告 —— 类型系统域（typert generator + protocol，纯逻辑面）

> 轮次：复刻 第14轮 ｜ 日期：2026-09-13 ｜ 对标卡：**#96（新增，generator + protocol 合并一路）**
> 上游只读：`G:\github\deepseek-harness` @ `a305303422`（0.1.5-rc.2）
> 本路可写文件（4 个，全部新增，零越界）：
> `lightharness/src/类型系统.light`（756 行 / 74 段）
> `lightharness/examples/test_类型系统.light`（302 行 / 167 断言）
> `lightharness/_antirun_typert.py`
> `lightharness/_task5_类型系统_交付报告.md`（本文件）
> 另：最小复现 `lightharness/examples/_repro_L116.light`（铁律 3 要求，缺陷 L-116）

---

## 一、上游对应表（文件 / 行号 / 核心语义）

### A. `packages/typert/protocol/src`

| 上游 | 行 | 核心语义 | 本模块落点 |
|---|---|---|---|
| `index.ts` | 20 `isTypertRemoteSegment` | 段名合法性：非 `.` / `..`，逐字符落在段字母表 | §1 `是合法远程段` + `是段字符` |
| `remote-error.ts` | 1–49 `RemoteError` / `remoteErrorOf` | 结构化远端错误：`isDSHRemoteError === true` 且 `code` 为串（**不靠 instanceof**） | §1 `造远程错误` / `是远程错误` |
| `types.ts` | 57 `RemoteErrorCode` / 63 `RemoteFailure` / 74 `RemoteResult` | 远端错误码表与失败/结果联合 | §1 错误响应形状 |
| `types.ts` | 120 `TypertForwardableEvent` / 212 `TypertSchema` / 222 `TypertCodec` | 可转发事件、schema/codec 形状 | 简化登记（见 §五） |
| `index.ts` | 141 `bindTypertRemote` / 153 `TypertRemoteService` / 174·183 `Remote` / 226 `RemoteScope` / 241 `remoteMethods` | 装饰器绑定与远端方法枚举 | §1 `方法枚举` + `是合法方法`（**简化**：取 `analyze/render/emit/listSymbols`） |

### B. `packages/typert/generator/src`

| 上游 | 行 | 核心语义 | 本模块落点 |
|---|---|---|---|
| `model.ts` | 11 `TypeNodeId` / 17 `KeywordTypeName` / 31 `TypeOperatorName` | 类型节点 id、关键字类型名、类型算子 | §2 节点种类词汇 |
| `model.ts` | 34 `SourceLocation` / 41 `ExportModel` / 49 `JsDocTagModel` / 57 `DocumentationModel` | 源位置、导出模型、JSDoc 文档模型 | §4 `取文档描叙` / §5 `渲染文档` |
| `model.ts` | 204 `TypeParameterModel` / 214 `ParameterModel` / 225 `SignatureModel` | 形参 / 参数 / 签名 | §2 函数节点 / §5 `渲染成员` |
| `model.ts` | 232 `MemberBase` / 251 `PropertyMemberModel` / 257 `MethodMemberModel` | 成员基类 / 属性成员 / 方法成员 | §2 `造属性成员` / `造方法成员` |
| `renderer.ts` | 21 `TypeGraphRenderError` / 26 `TypeGraphRenderer` | IR → 类型串渲染 | §5 `渲染节点` / `渲染成员` / `渲染符号` / `需括号` |
| `emitter.ts` | 27 `TypertEmitError` / 32 `ModelEmitResult` / 42 `RemoteModelEmitResult` / 88 `FaceModelEmitter` | 模型 → 声明文件发射 | §6 `声明文件头` / `排序名` / `只导出公开` / `发射声明文件` |
| `analyzer.ts` | 55 `TypertAnalysisError` / 62 `AnalysisMode` / 287 `WorkspaceAnalyzer` | 工作区分析 | §4 `分析声明`（**简化**：仅声明头提取，不做完整 TS 解析） |

---

## 二、实现要点

### §1 协议（`是合法远程段` / `方法枚举` / `造消息ID` / `造请求` / `造响应` / `造错误响应` / `造通知` / `序列化消息` / `解析消息` / `造远程错误` / `是远程错误`）
- 段语法：非 `"."` / `".."` / 空串，且逐字符落在 ASCII 段字母表 `[A-Za-z0-9_$-.?]` 的可服务子集（`是段字符`）。实测列：`a-b_c$1` 真；`.`、`..`、`""`、`a/b`、中文段、非串 一律假。
- 消息 id：`req_<整数>`（与第11轮 JSONRPC 同范式）。
- 四种消息 kind：`request` / `response` / `error` / `notification`；`request` 与 `notification` 都在构造期校验方法名，未知方法抛 `unknown typert method: <名>`。
- 远端错误：**结构标记**判定（`isDSHRemoteError === 真` 且 `code` 为 `str`），刻意不依赖类身份，跨进程/跨语言边界可判定。

### §2 类型 IR（`造节点` / `造原语` / `造字面量` / `造联合` / `造交叉` / `造数组` / `造元组` / `造属性成员` / `造方法成员` / `造对象` / `造函数` / `造引用` / `造作用域` / `作用域注册` / `作用域查找`）
- 节点统一 `{种类, …}` 字典；`造节点(种类, 字段)` 做通用字段合并，专用构造器在其上定型。
- 作用域为**父链**：`{上作用域, 符号表, 类型表}`，`作用域查找` 逐层上溯，未命中返回 `空`。

### §3 结构相等与可赋值性（`节点相等` / `成员相等` / `节点表相等` / `参数表相等` / `可赋值` / `任一可赋值` / `全部可赋值` / `表逐项可赋值` / `对象可赋值`）
- `节点相等`：按种类分派**结构**比较（primitive 比名称、literal 比文本+原语、union/intersection 比成员序列、array 比元素、tuple 比元素序列、reference 比名称+实参、function 比返回+参数、object 比成员集合），未知种类回退 JSON 形状比较。
- `可赋值`（简化子类型）：`any` / `unknown` / `never` 三顶底；字面量→原语；结构宽→窄；数组协变；引用同实参；联合按「源成员逐一可赋给目标」与「源可赋给目标某成员」两端判定。
  **实现注记**：联合 → 联合的判定必须**先看源是联合**再展开（否则源成员会被误配到目标错误分支）；此前把目标联合判定放前面导致 `(string|number) → (string|number|boolean)` 误判为假，已修正。

### §4 分析器（`分词` / `记号下标` / `取记号` / `声明种类` / `取声明名` / `跳过尖括号` / `取形参表` / `取关键字后名` / `按行拆` / `剥前导星` / `取文档描叙` / `分析声明` / `造符号表` / `注册符号` / `有符号` / `查找符号`）
- **声明级形状**而非完整 TS 解析：从声明头文本提取 `种类/名称/形参/继承/实现/修饰符（导出·抽象·静态·只读）/文档`。
- 支持 `class` / `interface` / `function` / `type` / `enum`；`跳过尖括号` 保证 `class Foo<T extends Bar>` 的形参提取不被 `extends` 约束污染。
- 符号表：`造符号表(包名)` + `注册符号`（**重复符号抛错** `duplicate symbol <名> in package <包>`）+ `有符号` / `查找符号`，并维护注册顺序表。

### §5 渲染器（`连接文本` / `连接节点` / `需括号` / `连接参数` / `渲染成员` / `渲染节点` / `形参文本` / `承继文本` / `渲染文档` / `渲染符号`）
- 节点渲染：`primitive→名称`、`literal→文本`、`union→" | "`、`intersection→" & "`、`array→T[]`（元素为联合/交叉时加括号）、`tuple→[A, B]`、`object→{ m; }`、`function→(a: T) => R`、`reference→Foo<A, B>`。
- 符号渲染：class（`export [abstract] class X<T> extends B implements I {…}`）、interface、alias（`export type X<T> = …;`）、enum；文档注释按 JSDoc 形状渲染。

### §6 发射器（`声明文件头` / `名比较` / `排序名` / `只导出公开` / `发射声明文件`）
- 头注释固定为 `/* Generated by @deepseek-ai/dsh-typert-generator from FaceModel — do not edit. */`；
- 只导出公开符号 → 按名称排序 → 逐条渲染 → 以换行收尾。

---

## 三、测试验证

命令：`cd lightharness && python 运行.py examples/test_类型系统.light`

```
===== 第 14 轮任务 5：类型系统域（纯逻辑面）=====
test_类型系统 PASS
```

`rc=0`，167 条断言全绿，覆盖 §1 协议（段语法/方法枚举/四种消息/序列化往返/远端错误结构标记）、
§2 IR（节点构造/作用域父链）、§3 结构相等与可赋值性（含联合子集→超集、元组→联合数组、结构宽窄、引用实参）、
§4 分析器（分词/记号下标/声明头各字段/JSDoc/符号表/重复符号抛错）、§5 渲染器（各节点种类/括号规则/成员/符号/JSDoc）、
§6 发射器（头/排序/过滤/发射内容与顺序/尾换行）。

**L-089 绕法**：本文件用 `设 花左 为 "{"` / `设 花右 为 "}"` 单花括号字面量拼接构造所有含花括号的
源码文本与期望串（`"{标识符}"` 会触发字符串插值，单 `"{"` / `"}"` 字面量安全，已探针确认）。

---

## 四、反跑结果（3/3 ALL OK）

命令：`python _antirun_typert.py`

```
=== 第 14 轮任务 5 反跑判据（类型系统域）===
✓ A 结构相等退化为仅同一对象 (判红运行 rc=1)
✓ B union 渲染不加 | 分隔 (判红运行 rc=1)
✓ C 分析器不再检测重复符号 (判红运行 rc=1)
✓ 字节级恢复校验 (sha256 dd02853e5cec)
✓ 恢复后回归绿 (rc=0，PASS 已打印)
ALL OK
```

判据细节（变异点均在 `src/类型系统.light`）：
- A：`节点相等` 首行 `如果 甲种 != 乙种: 返回 假` → `如果 真: 返回 假`（节点恒不等）→ 相等断言红；
- B：union 渲染分隔符 `" | "` → `"|"` → 联合渲染断言红；
- C：`注册符号` 的 `抛出 … duplicate symbol …` → `返回 符号` → 重复符号应抛错断言红。
恢复走 `finally`，逐字节 sha256 校验通过。

---

## 五、未移植项（宿主面登记）

| 上游位置 | 内容 | 处置 |
|---|---|---|
| `generator/src/cordis-catalog.ts`（1065 行） | Cordis 插件目录扫描、服务注入、catalog 生成 | 剔除 |
| `generator/src/tsdown-plugin.ts`（162 行） | 构建插件钩子 | 剔除 |
| `generator/src/workspace.ts`（155 行） | 文件系统扫描、tsconfig 解析、包发现 | 剔除 |
| `analyzer.ts`（3235 行）大部 | TS compiler/checker 依赖、完整 AST 遍历、缓存层 `WorkspaceCaches` | 剔除；只留**声明头提取**的纯文本部分 |
| `emitter.ts` 大部 | zod schema 发射、Remote `.d.ts` 生成、多包 workspace 发射 | 剔除；只留单包声明文件发射 |
| `protocol` decorator 体系 | `Remote` / `RemoteScope` 装饰器、`TypertRemoteService` 基类、Service 绑定 | 剔除；只留消息形状 + 段语法 + 结构化远端错误 |
| `protocol/types.ts` 类型级设施 | `TypertLookup` / `TypertContext` / `TypertForwardableEvent` 等纯类型工具 | 剔除（编译期概念，运行期无对应） |

任务书扩展（上游无对应，按 §复刻范围 实现）：
方法枚举 `analyze/render/emit/listSymbols`、四种消息 kind 词汇、`造符号表/注册符号/查找符号` 的运行期符号表、
以及「简化发射流程」（头注释 + 名称排序 + 仅公开）。

---

## 六、语言差异（新缺陷）

### L-116（本路登记）

**现象**：`序列化JSON` **保留字典插入序、不做键排序**，与 `字典 ==`（Python dict 语义，键序无关）语义不一致 ——
同一份内容的两个字典 `甲 == 乙` 为真，而 `序列化JSON(甲) == 序列化JSON(乙)` 为假。

**实测**（探针）：
```
甲 = {"a": 1, "b": 2}  →  序列化JSON(甲) = '{"a": 1, "b": 2}'
乙 = {"b": 2, "a": 1}  →  序列化JSON(乙) = '{"b": 2, "a": 1}'
甲 == 乙                          → 真
序列化JSON(甲) == 序列化JSON(乙)  → 假
```

**触发点**：`src/类型系统.light` 的 `节点相等(甲, 乙)` 在**未知节点种类**分支回退为
`序列化JSON(甲) == 序列化JSON(乙)`，故「同构但键构造顺序不同」的节点会被判不等。
本模块所有节点都经同一条构造路径产生（键序天然一致），故当前可用；跨来源比较则不成立。

**绕法**：结构相等不要裸用 JSON 串比较 —— ① 直接用 `甲 == 乙`（键序无关）；
② 若必须走 JSON，先做**键规范化**（`字典键列表` → `排序` → 按序重建字典 → 再序列化）。

**最小复现**：`examples/_repro_L116.light`（绕法形态，rc=0）。

**建议修复方向**：给 `序列化JSON` 增加「稳定/排序键」选项（或新增 `稳定序列化JSON`），
使「以 JSON 串做结构相等/哈希」成为可依赖的写法。

> 任务书预分配的 **L-117 / L-118 本路未占用**。本路其余摩擦均命中既有登记项
> （L-004 关键字字样切碎、L-084 `为` 字、L-089 花括号插值），无新增。

---

## 七、移交清单（→ 路M）

| # | 事项 | 说明 |
|---|---|---|
| 1 | 对标卡 #96 | typert generator + protocol（合并一路）→ `src/类型系统.light`，建议状态 `done` |
| 2 | 缺陷编号 | **L-116** = `序列化JSON` 键序敏感且与字典相等语义不一致；L-117 / L-118 未占用 |
| 3 | 行为差异 | 见 §一.A 方法枚举简化、§五 任务书扩展、§六 触发点 —— 请汇总为 R14-D5 |
| 4 | 只读引用 | `src/JSONRPC.light`（协议消息同范式参考）、`src/无损JSON.light`、`src/类型注册.light`（对标卡 #62，同域）均**只读未改** |
| 5 | 提交文件 | `src/类型系统.light`、`examples/test_类型系统.light`、`_antirun_typert.py`、`examples/_repro_L116.light`、本报告（5 个新文件） |
| 6 | 注意 | `节点相等` 的 JSON 回退带 L-116 前提（同构造路径）；若路M 后续引入 `稳定序列化JSON`，可一并替换该回退 |
