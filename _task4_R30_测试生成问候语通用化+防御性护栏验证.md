# 第30轮 任务4：测试_生成问候语 通用化 + 防御性护栏移除验证

- **轮次**：R30 任务4（P0）
- **任务书**：`复刻_第30轮_任务prompt分发_CCW保留10条进一步通用化.md` 任务4 区段
- **状态**：✅ 全部完成
- **交付时间**：2026-09-15

---

## 一、执行摘要

1. **方案A 落地**：在汉字+ASCII后缀合并路径（`lexer.py` :1951 前置区段）新增「ASCII下划线+Han 粘连通用规则」——前缀形如 `xxx_` 且**不在 user_definitions 中**时，后随汉字整段并入为 IDENTIFIER。
2. **CCW 清空**：`测试_生成问候语` 已从 `COMMON_COMPOUND_WORDS` 移除，**CCW 终态 = 0 条**（R29 的 10 条 → R30 全轮清零）。
3. **快速验证**：`examples/test_R30_测试生成问候语通用化_快速.light` **rc=0**，覆盖段落声明/实参调用/赋值形态三态自校验。
4. **全语料反跑**：855 文件 / 851 可比文件，**token 零变化（+0）**，基线为 R29 终态（CCW=10 条）——即任务1-4 全链路累计零回归。
5. **防御性护栏 6 条**：全部验证通过，均可直接从 CCW 移除（其中 5 条由任务1-3 通用化覆盖，本任务只验证不修改）。

---

## 二、4.1 测试_生成问候语 通用化（方案A）

### 2.1 问题

`测试_生成问候语` 由「ASCII 下划线 + Han 粘连」构成：`测试_` 先被识别为 IDENTIFIER，其后 `生成问候语` 在 CCW 移除后会被劈开为单独 token（`测试_` + `生成问候语`），导致孤立调用形态 `返回 测试_生成问候语(1)` 语义错误。

### 2.2 方案选择

| 方案 | 内容 | 结论 |
|---|---|---|
| A（选用） | 扩展 ASCII下划线+Han 粘连规则：`_` 后缀前缀 + 后随汉字 → 整段并入 IDENTIFIER | ✅ 通用，覆盖所有同类造名形态 |
| B | 扩展 `_P0A_MERGE_WHOLE` 整词并入白名单 | ❌ 白名单方案，不通用 |

### 2.3 落地判据（关键设计）

在汉字前缀合并循环中，当后继汉字命中关键字（`_match_keyword` 返回非空）时，**新增分支**：

```python
_prefix = tokens[-1].value
if _prefix.endswith('_') and _prefix not in user_definitions:
    # 整段并入：xxx_ + 汉字 → IDENTIFIER(full_combined)
    tokens[-1] = _Token(_TokenType.IDENTIFIER, full_combined, ...)
    consumed += len(han_suffix)
    continue
break  # 是关键字 且 前缀已注册 → 保持劈开
```

**为什么必须带 `user_definitions` 边界**：

- 语言以「汉字+_+汉字」造名（参考 `:2008` 英文分支哲学：`_` 后紧跟汉字必为名字成分，不可能是运算符/语句关键字），正常情况下 `_` 后随汉字必并入；
- 但 R27 反向测试的 `测试_返回真` / `测试_返回语句` / `测试_捕获到` **必须保持劈开**（调用场景前缀 `测试_` 已被预扫描截断注册——`段落 测试_返回真:` 注册的是 `测试_` 而非完整名）；
- 判据 `_prefix not in user_definitions` 精确区分两者：前缀已注册（R27 反向）→ 劈开；前缀未注册（孤立调用 `测试_生成问候语(1)`）→ 整词并入。

### 2.4 真实语料影响面（实证，11 处全形态）

对 855 文件全语料扫描「IDENTIFIER 以 `_` 结尾 且 后继为汉字」：

| 文件 | token 对 | 次数 | 判定 |
|---|---|---|---|
| `light-merge/examples/test_turing.light` | `_`(I) → `为`(KEYWORD) | 5 | 英文分支形态，前缀 `_` 已注册 → **劈开保持**（走 break） |
| `light-merge/stdlib/字符串工具轻量.light` | `__name__`(I) → `加上`(KEYWORD) | 2 | 英文分支，前缀 `__name__` 已注册 → **劈开保持**（走 break） |
| `lightharness/examples/test_R27_词首并入反向.light` | `测试_`(I) → `返回真`/`返回语句`(IDENTIFIER) | 4 | 前缀 `测试_` 已注册 → **劈开保持**（走 break） |

**结论：方案A 新增并入分支在真实语料中零触发**——3 类真实形态全部命中「前缀已注册 → 保持劈开」一侧，token 流零变化；`测试_生成问候语` 孤立调用由「前缀未注册 → 并入」侧救回（边界测试验证）。

---

## 三、4.2 防御性护栏移除验证（6 条）

对真实语料 0 命中的防御性条目做**只验证、不修改**：确认从 CCW 移除后全语料 token 零变化。

| # | 条目 | CCW 移除轮次 | 任务1-3 通用化覆盖 | 全语料零变化 | 结论 |
|---|---|---|---|---|---|
| 1 | 位异或 | 任务1 | ✅ `_P0A_HEAD_MERGE_PREFIX`（位前缀） | ✅ | 可直接移除 |
| 2 | 零除错误 | 任务3 | ✅ `_r30_cn_num_head_merge`（中文数字词首） | ✅ | 可直接移除 |
| 3 | 应当 | 任务2 | ✅ `_P0A_HEAD_MERGE_PREFIX`（应前缀） | ✅ | 可直接移除 |
| 4 | 测试_生成问候语 | **本任务** | ✅ 方案A（ASCII下划线+Han 粘连） | ✅ | 可直接移除 |
| 5 | 幂次 | 任务3 | ✅ 关键字上下文规则 | ✅ | 可直接移除 |
| 6 | 记录类型 | 任务3 | ✅ 复合词头规则 | ✅ | 可直接移除 |

### 验证证据

- **当前 CCW = `[]`（0 条）**，6 条均不在表中；
- 全语料反跑（对比 R29 终态基线 CCW=10）**token 零变化** —— 由于基线含全部 10 条，本次反跑实际覆盖了任务1-4 的全链路零回归，6 条护栏全部包含在内；
- 5 条（除测试_生成问候语外）的通用化落地细节见任务1-3 交付报告：`_task1_R30_位运算符通用化.md`、`_task2_R30_应当除非通用化.md`（任务3 报告由并行通道交付）。

---

## 四、验证证据汇总

### 4.1 快速验证

```
$ python 运行.py examples/test_R30_测试生成问候语通用化_快速.light
R30 测试_生成问候语 通用化快速验证通过（CCW 已清空，方案A 整词并入）
RC=0
```

覆盖三态：
- ① 段落声明形态（预扫描注册完整名，CCW 无关，本就整词）；
- ② 函数调用形态 `设 数值 为 测试_生成问候语()`（表达式起始位置，**方案A 救回**）；
- ③ 实参位置断言 `断言相等(测试_生成问候语(), 1, ...)`（**方案A 救回**）。

若方案A 失效劈成 `测试_`+`生成问候语`，②③ 编译期 NameError → 测试即红，自校验成立。

### 4.2 全语料 token 反跑

- 脚本：`_antirun_r30_t3_全量反跑.py compare lexer _antirun_r30_t3_基线快照.json`
- 基线：R29 终态（CCW=10 条），模块内存态一致
- 结果：

```
语料文件数：855
模块：lexer             CCW 条数：0
可比文件：851   token 总数差：+0
基线缺失 4 文件（任务1-3 新增 .light 测试，不影响可比判定）
✅ 全语料 token 零变化
```

---

## 五、测试遗留说明（非本轮引入，如实记录）

`examples/test_R29_CCW精简边界.light` 运行 rc=1，报错行13「捕获」是保留关键字不能直接作为语句开头。

**结论：R29 终态即存在，非本轮引入。**

- 该文件行13 `捕获 零除错误:` 是**顶层语句**，而光明语言「捕获」只能出现在「尝试」块内（parser 仅对 `尝试`/`试` 分发 try 解析，顶层 `捕获` 关键字无合法路径）；
- 用 `git archive HEAD`（R29 终态整棵源码树）编译同一文件 → **同样的 ParseError（行13）**，token 层与当前完全一致（`KEYWORD 捕获` + `IDENTIFIER 零除错误` + `COLON`）；
- R29 交付报告将该文件标注「rc=0（待路M最终确认）」——实际未被确证，属历史误标；
- 该文件的 token 形态变化已由全语料反跑覆盖（零变化），故不受本轮影响；
- 依据铁律「只改测试_生成问候语相关规则，不碰其他条目」，本轮不修复该遗留，仅记录。

---

## 六、交付物清单

| 交付物 | 路径 | 状态 |
|---|---|---|
| lexer.py 修改（方案A 规则 + CCW 移除） | `light-merge/src/lexer.py` | ✅ |
| 设计文档 + 验证结果（本文档） | `lightharness/_task4_R30_测试生成问候语通用化+防御性护栏验证.md` | ✅ |
| 快速验证测试（rc=0） | `lightharness/examples/test_R30_测试生成问候语通用化_快速.light` | ✅ |
| 全语料 token 零变化证据 | 见本报告 4.2 节（可复跑 `_antirun_r30_t3_全量反跑.py`） | ✅ |
| 防御性护栏移除验证清单 | 见本报告第三节 | ✅ |

---

## 七、铁律符合性确认

| 铁律 | 状态 |
|---|---|
| 只改测试_生成问候语相关规则，不碰其他条目 | ✅ 本轮仅新增 1 个判据分支 + 删除 1 条 CCW |
| 防御性护栏验证只做验证，不重复修改任务1-3 的内容 | ✅ 未触碰任何防护表/前缀规则 |
| 全语料 token 零变化是硬门槛 | ✅ 851 可比文件 +0 |
| 从 CCW 移除后必须再次全量反跑确认零回归 | ✅ 对 R29 终态基线反跑通过 |
| 新增一律 .light，禁 Python 绕语言缺陷 | ✅ 新增测试为 .light 文件，快速验证走 `运行.py` 真编译路径 |