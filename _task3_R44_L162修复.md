# 第44轮 任务3 交付报告 —— L-162 修复（mock 处理器字典契约对齐）+ 顺带修复 L-165

> 日期：2026-09-16 ｜ 优先级 P1 ｜ 修改区域：**仅** `src/mock大模型服务器.light`

## 一、结论

L-162 已修复：`mock处理器` 由返回 `[状态,头,体]` 列表改为返回 `{"状态","头","体"}` 字典，
对齐 `HTTP服务端` 处理器契约。修复过程中**顺带发现并修复 L-165**（`新建Mock服务` 漏 `全局` 声明，
导致模块级 `当前实例` 恒空、`启动Mock服务` 起的真实 HTTP 服务永远 500）。

修复后 `examples/test_R43_真实LLM往返.light` 与 `examples/test_R43_真实agent循环.light`
**已删除 R43 的自写 handler 绕行**，改为直接使用 `启动Mock服务`，均 rc=0。

## 二、L-162 修复

### 2.1 改法

```light
# 新增边界转换（分派 仍是纯逻辑三元组）
段落 造HTTP响应 接收 三元组:
  返回 ["状态": 三元组[0], "头": 三元组[1], "体": 三元组[2]]

异步 段落 mock处理器 接收 请求:
  如果 当前实例 == 空:
    返回 造HTTP响应([500, [...], 错误JSON(...)])
  返回 造HTTP响应(当前实例.分派(请求))
```

### 2.2 为什么 `分派` 保持三元组不变（关键取舍）

`分派` 是**纯逻辑层契约**（请求字典 → `[状态, 头, 体]`），
`examples/test_mock大模型服务器.light` 有 **29 处** `分派(...)` 调用、
以 `r1[0]`（状态）、`r1[2]`（体）等列表下标消费。
若把 `分派` 改成字典，该文件将大面积破功，与「改后既有测试不破」铁律冲突。

⇒ **两契约定界**：`分派` 保持三元组（纯逻辑，可单测），`mock处理器` 做边界转换（HTTP 集成）。
这也是 HTTP 边界该有的分层——形状转换属于传输适配，不属于业务逻辑。

## 三、L-165（新发现，本轮顺带修复）

### 3.1 现象

L-162 修完后，改用 `启动Mock服务` 的两个 R43 用例**仍然失败**：
```
错误: 运行时错误
  HTTP 请求失败 [500]: Internal Server Error
```

### 3.2 根因

`新建Mock服务` 内 `设 当前实例 为 实例` **没有 `全局 当前实例` 声明**。
光明按「函数内赋值 = 局部变量」处理，只写了**局部影子变量**，
**模块级 `当前实例` 始终为 `空`** ⇒ `mock处理器` 每次都走「未初始化」分支返回 `500 MOCK_NOT_READY`。

### 3.3 为何此前从未暴露

所有历史用法都绕开了 `当前实例` 这条路径：
- 只用纯逻辑 `分派`（不绑 socket）→ 用不到 `当前实例`；
- R43 任务1/2 自写 handler **自己持有实例** → 也用不到 `当前实例`。

即：`启动Mock服务`（唯一走 `当前实例` 的入口）**自入库起就是坏的**，只是没人用过。

### 3.4 修法

```light
段落 新建Mock服务 接收 选项:
  全局 当前实例          # ← 补上
  设 实例 为 新建 Mock大模型服务器(选项)
  设 当前实例 为 实例
  返回 实例
```

## 四、装配层去绕行（任务书目标 3）

| 用例 | R43（绕行） | R44（去绕行） |
|---|---|---|
| `test_R43_真实LLM往返.light` | 自写 `我的处理器` + `新建 HTTP服务端(我的处理器, 5.0)` | `启动Mock服务(选项, 5.0)`，`服务端 = 我的实例.HTTP服务` |
| `test_R43_真实agent循环.light` | 同上自写 handler | 同上 `启动Mock服务` |

## 五、验证

| 用例 | 结果 |
|---|---|
| `examples/test_mock大模型服务器.light`（29 处分派，向后兼容） | **rc=0** ✅ |
| `examples/test_R43_真实LLM往返.light`（去绕行后） | **rc=0** ✅ |
| `examples/test_R43_真实agent循环.light`（去绕行后） | **rc=0** ✅ |
| 本机全量 pytest（改 src 后） | 见「路M收口」——基线外零新增红 |

## 六、约束核对

- **只改 `src/mock大模型服务器.light`**（另改了依赖它的两个 examples 测试以去绕行）。
- 改后既有测试不破（`test_mock大模型服务器` rc=0 + 全量 pytest 回归）。
- L-162 / L-165 已在 `docs/功能对标/语言缺陷账.md` 标记 **已修复**。

## 七、建议（未做）

L-165 这类「函数内对模块级名字赋值漏写 `全局`」属**静默影子变量**，极难定位
（表现是远端 500 而非本地报错）。建议光明编译器对「赋值目标与模块级已存在同名变量重合、
且本函数未声明 `全局`」给出编译告警。
