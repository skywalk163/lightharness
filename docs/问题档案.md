# lightharness 问题档案（已知问题 / 待办）

本文件记录 CI 上已知、但**暂不修复**、仅登记为预期红的用例，及其根因与后续处置线索。
目的：避免「已知红」反复刷屏成背景噪音，同时不丢根因（修好时反向校验）。

登记格式：用例 → 现象 → 根因 → 与 P0-2 关系 → 后续处置。

---

## test_终端PTY.light（**已解决**：2026-08-30 硬化测试 + 删 EXPECT_RED 登记）

- **原现象（历史留痕）**：FreeBSD runner 上该用例偶发 rc=1，断言失败
  `输出应含计算结果 3，实际=expr 1 + 2`——
  PTY 会话写入 `expr 1 + 2` 后只回显了命令本身，没读到 shell 的执行结果 `3`。
- **原根因误判（已纠正）**：曾归因为「FreeBSD PTY 实现既有缺陷、平台相关」。
  但实际是**测试侧计时脆弱**——原测试用固定 `time.sleep(0.5)` + 单次 `读取(N)`，
  且只断言最后一段 delta；慢 shell / 高负载 runner 上执行结果晚于读取窗口到达，
  单段断言漏判（回显见、结果漏）。与编译器/PTY 实现无关：两轮 CI 之间
  lightharness 代码零改动，PTY 变绿的唯一变量是编译器版本（而 P0-2 不碰 `终端.light`），
  故这次红→绿属计时脆弱侥幸，非真修复。
- **真实修复（测试侧）**：`examples/test_终端PTY.light` 改用 `读至包含(会话, 标记, 预算)`
  轮询助手——循环 `读取(0.5)` 累积全文、直到命中标记或预算耗尽（读写预算 15s、
  exit 轮询 10s），断言改为对**累积全文**判含。消除固定 sleeps 与单段 delta 断言的脆弱性，
  对慢 shell / 高负载免疫。语义三段（启动/读写/持续）与结尾 `test_终端PTY PASS` 不变。
- **登记动作**：`tests/test_回归.py` 的 `EXPECT_RED` 在非 Windows 下曾包含
  `test_终端PTY.light` 作临时挡板；现**已删除**，门禁全量硬判绿
  （`EXPECT_RED` 归零，符合「EXPECT_RED 应保持为空」验收口径）。
- **与 P0-2 关系**：**无关**（同原判定，保留）。
- **验证**：本地 Windows/ConPTY 跑 `test_终端PTY.light` 稳定绿；
  b93bb23 触发 CI 时该用例已变绿并触发 EXPECT_RED 自清理断言失败 →
  本次硬化 + 删登记后，CI 应全绿。

---

## 历史：PLATFORM_ENV_DEBT 三条（已修，留痕）

以下三条曾按平台登记为预期红（FreeBSD），已于 `671b0f8` 修好并清空 `PLATFORM_ENV_DEBT`：
- `test_子进程码.light`：shell 转义导致 `exit(3)` 被读成 2 → 加引号解决。
- `test_沙箱.light`：Windows 路径当「根外」误判 → 改用 `<根>/..` 解决。
- `test_终端PTY.light`：早期形态「PTY 读到退出而非数据」→ 按平台挑外壳/行尾/算式。
  注意：本次（2026-08-30）登记的 `test_终端PTY` 是**新形态**（回显未执行），
  与早期形态不同，非同一问题复发。

---

## test_终端.light（管道式终端会话，根因已修 · 2026-08-31）

- **现象**：CI(8-31) 该用例红，失败在 `测启动读取` 段：
  `会话.读取(5)` 返回 `原因=退出`（空内容），断言 `应读到数据` 失败。
  本地 Windows 跑恒绿，CI FreeBSD/调度差异下复现。
- **根因（harness 竞态，非测试缺陷）**：`src/终端.light` 的 `终端会话.读取()`
  在「进程已退出（`码 != 空`）+ 缓冲为空」分支**直接返回「退出」**，但此时后台
  读线程可能还没把子进程输出（如 `python -c "print('hi')"` 的 `hi`）刷进缓冲——
  `取退出码()` 的 poll 先于读线程落盘即拿到「已退出」，于是 `读取` 丢数据返回空「退出」。
- **修复（commit 88074b3，已推 gitea `skywalk/lightharness`）**：该分支改为最多约 0.3s
  轮询宽限，等待读线程排空管道剩余输出；期间缓冲一旦出现新数据即按「数据」返回，
  超时仍空才判「退出」。对所有调用 `终端会话.读取()` 的下游（命令输出读取场景）一并生效。
- **验证**：`test_终端.light` 本地循环 **30 次 0 失败**；`test_终端PTY.light` 循环 5 次稳绿；
  完整 pytest 门禁 **106 passed 0 failed**（无回归）。`EXPECT_RED` 保持为空。
- **方法论**：CI 偶发红在 `读取`/`读取输出` 类接口 → 先查「进程退出是否抢在缓冲落盘前」
  这类 harness 竞态，再决定是否改测试；本次是真 bug，修 harness 而非改测试（改测试会掩盖回归）。
