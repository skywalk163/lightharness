# test_R30_CCW通用化边界.light —— CCW 10→0清零后边界测试
# 覆盖：原10条CCW条目通用化后的保护语义 + CCW清零验证

# === 原位运算符4条（通用化后整词成IDENTIFIER）===
设 r1 为 位与(3, 1)
设 r2 为 位异或(3, 1)
设 r3 为 位或(3, 1)
设 r4 为 位非(3)

# === 原应当/除非（通用化后整词成IDENTIFIER）===
如果 应当 那么:
    打印 "应当为真"
设 r5 为 除非(条件)

# === 原零除错误/幂次/记录类型（通用化后整词成IDENTIFIER）===
捕获 零除错误:
    打印 "捕获零除错误"
设 r6 为 幂次(2, 10)
设 r7 为 记录类型(用户)

# === 原测试_生成问候语（ASCII下划线+Han粘连通用化后整词）===
段落 测试_生成问候语:
    返回 "你好"

# === 关键字独立使用不受影响 ===
当 x > 0:
    设 x 为 x - 1
非 真
位 为 1
零 个

打印 "CCW通用化边界测试通过"
