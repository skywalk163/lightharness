# R90-E · `_等端口` 等待窗口放宽（15s → 30s）

> 承接 R89 §6 遗留 3；执行顺序 E（在 B 之后 —— B 的全量已确认这 3 条本轮未复现）

---

## 1. 改动（只有一处）

`light-merge/tests/test_distributed_eval_light.py:105`

```python
- def _等端口(端口文件, 上限=15.0):
+ def _等端口(端口文件, 上限=30.0):
```

加中文注释说明：全量 `-n auto` 负载下主控（master）启动 + 写出端口文件实测可能超过 15s；
**只放宽「等多久」，不碰断言语义**——用例仍要求结果不丢条、心跳不被误标失联。
幂等哨兵：`R90-PORTWINDOW`。

## 2. 取证（一手）

| 取证项 | 结果 |
|---|---|
| `test_分发与结果汇聚` 孤立 ×3 | **3/3 passed** |
| `test_重派与心跳_杀节点后重派且无静默丢条` 孤立 ×3 | **3/3 passed** |
| `test_心跳独立于执行_长任务期间不被标失联` 孤立 ×3 | **3/3 passed** |
| 整个文件串行 ×3 | **3/3：6 passed（40.89s / 41.77s / 39.53s）** |

## 3. 必须说清的一点：本轮**没能证明**这个改动解决了什么

诚实记录，避免下轮误读：

- R89 全量里这 3 条红的共同签名是「主控未在 15.0 秒内写出端口」，据此建议放宽到 30s；
- 但 **R90-B 的全量（8 failed）里这 3 条一条都没红**（同样 `-n auto --dist loadscope`）；
- 所以本轮的取证只能证明「放宽后仍然全绿、没有引入新问题」，
  **不能证明**「放宽让红转绿」——因为对照组（15s 版本）在本轮负载下本来也是绿的。

这个改动是**按 R89 遗留建议执行的预防性加固**。真正的验证要等某轮全量再次复现
「15s 写不出端口」时才能做（届时若 30s 下不红，结论才成立）。

按任务书红线：单次放宽后若仍红就停下取证，**禁止循环加大窗口**——本轮没有出现仍红的情况，
因此**没有继续加窗口**。

## 4. 遗留

1. 台账 W-08~W-10（`tests/test_distributed_eval_light.py` 的 3 条）保留为负载类容忍条目；
   若连续两轮不红，可在 R91 销账。
2. 若下轮再出现「主控未在 30.0 秒内写出端口」，不要再放宽窗口，
   应该去查**主控为什么写不出端口文件**（路径/权限/启动失败），那是真问题。
