diff --git a/src/JSONRPC传输.light b/src/JSONRPC传输.light
index 2cd5b15..eb81213 100644
--- a/src/JSONRPC传输.light
+++ b/src/JSONRPC传输.light
@@ -140,6 +140,33 @@
     设 己.待定 为 ""
     返回 残行
 
+# ---- 取消错误（AbortSignal 触发后的明确结果；继承 错误 → Exception）----
+类 取消错误 继承 错误:
+  属性 消息 等于 ""
+  段落 构造 接收 文本:
+    设 己.消息 为 文本
+
+# ---- 取消令牌（AbortSignal 等价物：取消信号 + 监听回调）----
+类 取消令牌:
+  属性 已取消 等于 假
+  属性 监听表 等于 []
+  段落 构造:
+    设 己.已取消 为 假
+    设 己.监听表 为 []
+  段落 取消:
+    如果 己.已取消 == 真:
+      返回
+    设 己.已取消 为 真
+    遍历 己.监听表 之 回调:
+      回调()
+  段落 当取消 接收 回调:
+    如果 己.已取消 == 真:
+      回调()
+      返回
+    己.监听表.追加(回调)
+  段落 是否已取消:
+    返回 己.已取消
+
 # ---- 内存终端（同步模拟 JsonRpcLineTransport 的服务端分派纯逻辑）----
 # 对齐 transport.ts:226-238 handleIncomingRequest / 221-223 通知分派：
 #   - 安装 onRequest 处理器；入站请求无处理器 → -32601；处理器抛错 → -32603。
@@ -158,6 +185,11 @@
     设 己.通知处理器 为 空
     设 己.待发 为 []
     设 己.收到响应 为 []
+    设 己.pending表 为 {}
+    设 己.应答表 为 {}
+    设 己.错答表 为 {}
+    设 己.已取消表 为 {}
+    设 己.流 为 空
 
   段落 安装请求处理器 接收 处理器:
     设 己.请求处理器 为 处理器
@@ -193,6 +225,7 @@
       己.分派通知(消息)
     否则:
       己.收到响应.追加(消息)
+      己.撮合入站响应(消息)
 
   # 请求分派（对齐 transport.ts:226-238）
   段落 分派请求 接收 消息:
@@ -224,3 +257,110 @@
       设 原始参数 为 消息["params"]
     设 参数 为 归一化参数(原始参数)
     处理器(方法, 参数)
+
+  # ---- pending 请求表（请求发出→按 id 挂号→响应回来按 id 撮合）----
+  # 挂号：把本次请求 id 记入 pending，绑定解决器/拒绝器/取消令牌。
+  段落 挂号请求 接收 标识, 解决器, 拒绝器, 令牌:
+    设 条目 为 ["标识": 标识, "解决器": 解决器, "拒绝器": 拒绝器, "令牌": 令牌, "已决": 假]
+    己.pending表[标识] 为 条目
+
+  # 撮合：响应回来按 id 找到 pending 条目，回填结果并清理。
+  段落 撮合结果 接收 标识, 结果:
+    如果 己.pending表.包含(标识) == 假:
+      返回
+    设 条目 为 己.pending表[标识]
+    条目["已决"] 为 真
+    条目["结果"] 为 结果
+    如果 条目["解决器"] != 空:
+      条目["解决器"](结果)
+    己.应答表[标识] 为 结果
+    己.pending表.删除(标识)
+
+  # 撮合：入站错误响应按 id 找到 pending 条目，回填错误并清理。
+  段落 撮合错误 接收 标识, 错误:
+    如果 己.pending表.包含(标识) == 假:
+      返回
+    设 条目 为 己.pending表[标识]
+    条目["已决"] 为 真
+    条目["错误"] 为 错误
+    如果 条目["拒绝器"] != 空:
+      条目["拒绝器"](错误)
+    己.错答表[标识] 为 错误
+    己.pending表.删除(标识)
+
+  # 入站响应帧（来自 接收行）：保留旧「收到响应」记录，同时按 id 撮合 pending。
+  段落 撮合入站响应 接收 消息:
+    设 标识 为 消息["id"]
+    如果 己.pending表.包含(标识) == 假:
+      返回
+    如果 消息.包含("error"):
+      己.撮合错误(标识, 消息["error"])
+      返回
+    设 结果 为 空
+    如果 消息.包含("result"):
+      结果 为 消息["result"]
+    己.撮合结果(标识, 结果)
+
+  # 取消：清理 pending 条目，通知等待方收到明确取消结果（取消错误）。
+  段落 取消请求 接收 标识:
+    如果 己.pending表.包含(标识) == 假:
+      返回
+    设 条目 为 己.pending表[标识]
+    设 令牌 为 条目["令牌"]
+    条目["已决"] 为 真
+    设 错 为 新建 取消错误("请求已取消: " + 转字符串(标识))
+    如果 条目["拒绝器"] != 空:
+      条目["拒绝器"](错)
+    己.已取消表[标识] 为 错
+    己.pending表.删除(标识)
+    如果 令牌 != 空:
+      令牌.取消()
+
+  # 出站请求：编码帧 + 挂号 pending（帧缓冲进 待发，等 flush / 挂接流写出）。
+  段落 发送请求 接收 标识, 方法, 参数, 令牌 = 空:
+    己.写出(造请求帧(标识, 方法, 参数))
+    己.挂号请求(标识, 空, 空, 令牌)
+
+  # ---- flush：把缓冲帧强制写成字节，推到挂接的字节流（无流则仅清空缓冲）----
+  段落 flush:
+    如果 己.流 == 空:
+      设 己.待发 为 []
+      返回
+    设 全部行 为 ""
+    遍历 己.待发 之 行:
+      设 全部行 为 全部行 + 行
+    设 字节段 为 全部行.编码("utf-8")
+    己.流.推入字节(字节段)
+    设 己.待发 为 []
+
+  # ---- 流挂接：把传输层接到可读写字节流（如 字节缓冲），而非只吃/吐字符串 ----
+  段落 挂接流 接收 流:
+    设 己.流 为 流
+
+  # 泵：从挂接流按字节切完整帧，逐行喂回 接收行（响应回来即撮合 pending）。
+  段落 泵:
+    如果 己.流 == 空:
+      返回
+    设 帧表 为 己.流.按字节切(b'\n')
+    遍历 帧表 之 字节帧:
+      设 行 为 字节帧.解码("utf-8")
+      设 行 为 去除空白(行)
+      如果 行 != "":
+        己.接收行(行)
+
+  # ---- 异步等价实现（R70-B async/await；不再依赖宿主 node:stream + Promise）----
+  段落 是待决 接收 标识:
+    返回 己.pending表.包含(标识)
+
+  异步 段落 等待解决 接收 标识:
+    当 己.是待决(标识):
+      等待 异步睡眠(0.001)
+    如果 己.已取消表.包含(标识):
+      抛出 新建 取消错误("等待方被取消: " + 转字符串(标识))
+    如果 己.错答表.包含(标识):
+      抛出 新建 取消错误("等待方收到错误: " + 转字符串(己.错答表[标识]))
+    返回 己.应答表[标识]
+
+  异步 段落 请求并等待 接收 标识, 方法, 参数, 令牌 = 空:
+    己.发送请求(标识, 方法, 参数, 令牌)
+    返回 等待 己.等待解决(标识)
