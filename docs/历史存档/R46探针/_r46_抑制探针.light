# R46 探针：验证 L-166 影子变量告警的**注释抑制**（# 抑制L166）
# 预期：只有『真缺陷』段落告警 1 条；行级与段落级抑制各消掉 1 条。
设 计数器 为 0。

段落 写回:
  全局 计数器。
  设 计数器 为 计数器 + 1。
  返回。

段落 遮蔽_行级:
  设 计数器 为 99。  # 抑制L166
  返回。

# 抑制L166
段落 遮蔽_段落级:
  设 计数器 为 0。
  返回。

段落 真缺陷:
  设 计数器 为 5。
  返回。

段落 主:
  写回()。
  遮蔽_行级()。
  遮蔽_段落级()。
  真缺陷()。
  打印("模块级 计数器 = " + 转字符串(计数器) + "（写回生效则=1；末段是局部影子，不影响）")。
  返回。

主()。
