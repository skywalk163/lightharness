# R95 收口报告 · xdist worker 被杀根治（Job Object）+ 创建时间闸门 + W-13 去抖动化

> 轮次：R95（2026-09-25，承接 R94 开发冻结轮遗留的根因战场）
> 仓库：`light-merge`（commit `83156fe0`，分支 `release/r95`，标签 `r95-dev`）
> 结论：**KI-R94-01（Windows 高负载 worker 被杀的误杀面）与 KI-R94-02（W-13 条件绿）均收口为 `resolved`**。

---

## 1. 根因 / 定位

R94 把「Windows 本机全量 xdist worker 被杀」定为**已知风险、不根治**，留下两条收口入口：

- **KI-R94-01** 的「PID 复用 + 进程树 PPID 链补杀误伤兄弟 worker」假设——25,285 样本探针从未复现，但机理未定性。
- **KI-R94-02** 的 W-13 时序断言绿是 R92-B **放宽阈值换来的条件绿**（无窗口上限 0.05→0.15、配对下限 0.05→0.08），按 R94 裁定记 `accepted-risk`、不销账。

R95 的任务是把这两处从「规避/条件绿」推进到「结构性关闭」。

---

## 2. 做了什么（按文件面）

### 2.1 stdlib/进程树.light —— [R95-B2] 创建时间闸门（根治误杀面）
- 新增 `段落 _创建时间秒(pid)`：ctypes 直调 `GetProcessTimes` 取进程创建时间（Unix 秒），**不引入 psutil 依赖**；非 win32 / 取不到一律返回 `空`。
- 在 `_R92补杀迟建后代` 的「PPID 链命中已杀集合」判定**之后**新增创建时间闸门：
  - 计算 `下限秒 = 己.开始时刻/1000 - 5.0`（本树起点前 5s，容差覆盖启动时戳采样滞后）；
  - 候选 PID 创建时间 **早于下限**（老进程 PID 被 OS 复用）→ `继续` 跳过，**从根上消除误杀无关进程**；
  - 取不到创建时间（返回 `空`）也跳过：**宁可漏杀一个可疑进程，绝不误伤无关进程**。

> 该兜底路径只在 Windows 生效；FreeBSD 0.82 上 `_创建时间秒` 恒返回 `空`，闸门为 no-op，保持原有行为（那一侧本就无此误杀面）。

### 2.2 tests/test_进程树_创建时间闸门.py（新增，Windows-only）
单测 `_R92补杀迟建后代`：构造根=1000、记录后代=1001，三个候选（PPID 链都回溯到本树）：
- `8888` 创建时间新（本树迟建后代）→ **应被补杀**；
- `9999` 创建时间老（老 PID 复用）→ **绝不误杀**；
- `7777` 创建时间取不到（返回 `空`）→ **不误杀**。
**结果：1 passed。**

### 2.3 tests/test_进程树_jobobject.py（新增，Windows-only）—— [R95-B1] Job Object 验证
- `test_绑定任务对象_产生有效句柄`：启动即断言 `任务句柄 != 0`，证明 Job Object 根治路径**运行时确实生效**（非死代码）。
- `test_杀树_整树被回收且无关进程存活`：构造「根→子→孙」三层进程树，杀树后**整树回收 + 一个与本树无关的存活进程毫发无伤**。
**结果：2 passed。**

### 2.4 tests/test_concurrency_light.py —— [R95-B4] W-13 去抖动化
- `test_超窗口上限的调用会被阻塞`：跑 **5 次取最小耗时**（最接近真实阻塞），下限 `窗口秒*0.9`（≈0.09），**比原 0.08 更紧**且由窗口常量推导，不再是魔数。
- `test_无窗口时行为完全不变`：跑 **5 次取最大耗时**（最坏情况），上限 `0.12s`，**比原 0.15 更紧**，远小于阻塞路径 ~0.1s+ 分界。
**结果：类级 3 passed（含未改动的共享窗口用例）。**

### 2.5 tests/ci_environment_reds.txt —— W-13 从台账销账
W-13 数据行转注释（不再被 judge 容忍），台账条目 21→20；`ci_judge_env_reds.py self-check` 仍 PASS（20 条 + 反向演示 1 条合成红正确识别）。

### 2.6 docs/known-issues/* + docs/运行手册_Windows本机全量.md
- `R94-xdist-worker-kill.md`：状态改 `resolved (R95)`，新增 §4b 收口说明（Job Object 验证 + 创建时间闸门 + 残余=上游 xdist 高负载脆弱性已规避）。
- `R94-W13-accepted-risk.md`：状态改 `resolved (R95)`，新增 §7 去抖动化销账说明。
- `README.md` 看板：两条均标 `resolved (R95)`。
- `运行手册_Windows本机全量.md`：补「门禁必要非充分」说明（R94 `r94r1` 实测峰值 86.7%）。

---

## 3. 验证（本机 Windows，单进程 `-o addopts=`，避开 xdist）

| 项 | 结果 |
|---|---|
| 进程树.light 编译/导入 smoke（含 `_创建时间秒` 生效、创建时间取值正确） | PASS |
| test_进程树_创建时间闸门.py | 1 passed |
| test_进程树_jobobject.py | 2 passed |
| test_concurrency_light.py::Test滑动窗口接入派发 | 3 passed |
| 三文件合并跑 | **6 passed / 3.52s** |
| ci_judge_env_reds.py self-check | PASS（20 条 + 反向演示） |

### 未在本会话执行的验证（环境限制）
- **0.82（FreeBSD）权威判绿**：本沙箱 ping 192.168.0.82 不通，无法直连跑 `082全量回归.py`。0.82 判绿为既定纪律，命令见 §5，请在有 0.82 可达环境执行。
- **Windows 本机 `-n 4` 连续 3 轮全量**：受 HP Z620 休眠/BSOD 风险与单机负载抖动影响，本会话未跑满 3 轮全量；门禁口径与 R94 一致（CPU<80% / `-n 4` / 判绿以 0.82 为准）。

---

## 4. 残余风险（已规避，非 LM 缺陷）

高负载下 **xdist/execnet 在 Windows 上的自身脆弱性**（node down → INTERNALERROR / 挂死）未被证明与 LM 杀树逻辑相关——25,285 样本探针从未复现误杀，且 R95 已结构性关闭误杀面。该残余由 R94 门禁口径继续规避，不再作为 LM 缺陷跟踪；若低负载（<80%）下 `-n 4` 连续 2 轮出现 node down，按 KI-R94-01 §6 回滚条件重开。

---

## 5. 0.82 权威判绿命令（待执行）

```bash
# 在 0.82（FreeBSD 15.1）上，先同步 light-merge 工作树，再跑全量判绿：
cd <0.82 上 light-merge>
python lightharness/scripts/同步0.82.py          # os.walk 同步工作树（含未提交改动）
LIGHT_MERGE=<0.82 上 light-merge> PYTHONPATH=<0.82 上 light-merge/src> \
  python lightharness/scripts/082全量回归.py test --py /usr/local/bin/python3.12 \
  --base reports/082_lightmerge基线_<时间戳>.json
# 判据：两侧「新增红 = 0」才 rc=0；务必带 --base 对拍时间戳基线
```

---

## 6. 版本与推送

- 提交：`83156fe0`（main，9 files / +333 / -36）。
- 分支：`release/r95`；标签：`r95-dev`（annotated）。
- 推送（凭据注入，绕过 GCM；pre-push 地板守卫 ✅ 名单双向咬合）：
  - light-merge → `gitea` / `gitcode` / `github` / `origin`：**main + release/r95 + r95-dev 均 OK**
  - lightharness → `origin` / `myrepo` / `github`：main OK（R95 未改 LH，仅同步）
- KI 看板：`KI-R94-01` / `KI-R94-02` 均 `resolved (R95)`。
