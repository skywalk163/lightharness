# R45 探针：判定「模块级变量在 主() 内赋值、被另一个顶层段落读」时到底读到什么
设 临时profile目录 为 ""
设 计数 为 0

段落 读夹具:
  返回 临时profile目录

段落 写计数:
  设 计数 为 计数 加上 1
  返回 计数

段落 主:
  设 临时profile目录 为 "abc"
  打印 "段内直接读 = [" + 临时profile目录 + "]"
  打印 "跨段读     = [" + 读夹具() + "]"
  打印 "计数器初值 = " + 转字符串(写计数())
  打印 "计数器再读 = " + 转字符串(写计数())
