# L-093 破坏形态复现（调用点未捕获）
从 会话日志增量 导入 接受至

段落 造畸形视图:
  设 畸形 为 []
  列表追加(畸形, {"类型": "session-log-deepseek/delivery-accepted", "序号": 0, "时间": 0, "数据": {"sessionId": "s_1", "sessionFormatVersion": 2, "throughSeq": 0}})
  返回 {"头": {"版本": 2, "标识": "s_1"}, "事件表": 畸形, "记忆表": {}}

段落 主:
  设 占位 为 接受至(造畸形视图())    # ← 真实抛点，未用 尝试 包裹
  打印 "never"

主()
