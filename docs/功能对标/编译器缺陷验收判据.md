# 编译器缺陷验收判据 · L-001~L-017

> **移交方**：lightharness（光明复刻 deepseek harness 项目）
> **接收方**：光明语言团队（light-merge 编译器）
> **日期**：2026-08-27（第 10 轮）
> **第11轮结论（2026-08-28）**：L1/L2/L3 修复已合入 light-merge main，本判据 18 个用例
> 除 L-009（工程层）外全部转绿；lightharness 侧绕法已移除并恢复正式写法。

> **用途**：作为编译器缺陷修复的验收判据——每个缺陷对应一个可运行的复现用例，修复后用例转绿即验收通过。

## 复现命令与判红/判绿规则

```bash
cd G:\dswork\duan-light-merge\lightharness
python 运行.py examples/test_L0XX.light      # 逐一复现
python 运行.py examples/test_L缺陷清单.light  # 汇总打印（可按行核对 CI）
```

- **绿** = 退出码 0 且正常结束（`--- 测试L0XX 结束 ---`）。
- **红** = 退出码非 0，且报错与缺陷账「最小复现」一致。
- 用例结构 = 三段注释头（缺陷/当前/修复后）＋ 复现段 ＋「修复后断言段」（当前注释不调用，**修复后启用**）。
- **反跑判据**：改断言值或绕法即红/绿翻转；已缓解项若未来回归会立红（回归保护）。

## 缺陷矩阵（17 项）

| 编号 | 缺陷摘要 | 复现用例 | 当前 | 修复后验收（断言要点） |
|---|---|---|---|---|
| L-001 | 跨目录点号导入不可用（有 `__init__.py` 也不解析） | `test_L001.light` | 绿（已修复，L3） | `从 _import_test.数据 导入 造值` 后 `造值()==42` |
| L-002 | 多行字面量/续行语法错误「意外的标记: 换行」 | `test_L002.light` | 绿（已修复，L1） | 跨行字典/列表字面量可编译运行 |
| L-003 | 类内成员空列表赋值被误编译（比较/拆分） | `test_L003.light` | 绿（已修复，L2 加固） | `事件表` 为真实列表，追加/数量断言通过 |
| L-004 | 关键字边界拆分中文标识符（家族性，最严重） | `test_L004.light`（载体 `_helper_L004.light`） | 绿（已修复，L1） | 关键字开头成员名 `导出事件表` 跨模块可调用 |
| L-005 | 非 self 成员下标 `对象.表[键]` 被编译成调用 | `test_L005.light` | 绿（已修复，L2） | `总线.监听器表[事件名]` 按下标取值 |
| L-006 | 闭包内不能修改外层局部标量（UnboundLocalError） | `test_L006.light` | 绿（已修复，L2） | 内层对 `设` 于外层的标量 `X=X+1` 可更新 |
| L-007 | 成员名拆分状态性（属性/方法前缀重叠） | `test_L007.light` | 绿（已修复，L1） | 属性 `用量值` + 方法 `用量` 共存不拆分 |
| L-007b | 模块级局部变量名污染跨模块成员访问 | `test_L007b.light`（载体 `_helper_L007b.light`） | 绿（已修复，L1） | 局部 `头/事件/轮次` 不拆 `对象.头信息` |
| L-007c | 类内局部变量名与属性名前缀重叠触发拆分 | `test_L007c.light` | 绿（已修复，L1） | 参数 `轮次/步骤` 不拆 `己.轮次计数` |
| L-008 | `序列化`/`解析JSON` 非模块级自动可用 | `test_L008.light` | 绿（已修复，L2） | `序列化(...)` 直接可用（JSON 内置） |
| L-009 | 注释内嵌 `导入 X` 字样被文本替换误伤（工程层） | `test_L009.light`（`_替换后.light` 变体） | 工具（工程层，未修） | `-replace '导入 builtins'` 不劈开注释 |
| L-010 | 模块名首字符为关键字 → `从 X 导入 Y` 解析失败 | `test_L010.light` | 绿（已修复，L1） | `主程序/主入口/外部命令` 模块名整体成词 |
| L-011 | 关键字根 `等待` 拆分成员访问（`等待进程`） | `test_L011.light` | 绿（已修复，L1，用例入参已修正） | `外部命令.等待进程(进程, 超时)` 正常调用 |
| **L-012** | 模块名与类名共享关键字前缀，`终端.终端会话` 跨模块被拆成 `终端().终(...)` | `test_L012.light` | 绿（已修复，L1） | `新建 终端.终端会话(...)` 构造成功，状态=未启动 |
| **L-013** | 单行 `遍历 X 之 Y: 语句` 曾被解析为不闭合循环、吞后续类 | `test_L013.light` | 绿（已修复，L1） | 单行 for 立即闭合，后续 `返回/类` 不被吞（回归保护） |
| **L-014** | 语句级下标调用 `字典["键"](参数)` → 未知语句类型 FunctionCallExpr | `test_L014.light` | 绿（已修复，L2） | `表["甲"](1)` 作为合法调用语句执行 |
| **L-015** | 模块名含关键字词根（`异步`）→ `从 X 导入 Y` 解析失败 | `test_L015.light`（载体 `异步样例.light`） | 绿（已修复，L1） | `从 异步样例 导入 样例函数` 成功，`样例函数()==7` |
| **L-016** | CancelledError（BaseException）无法被光明 `捕获`（except Exception）接住 | `test_L016.light` | 绿（已修复，L2） | 取消后 `等待` 被取消任务被 `捕获` 接住，接住==真 |
| **L-018** | 顶层模块重名时用户侧（src/）模块不可达（`from 重试 import` 恒命中 stdlib） | `test_L018.light` | **红** | `from 重试 import 造策略` 命中用户侧模块，`造策略(…)` 可调用 |

> 加粗行 = 本轮（第 10 轮查收后）新增/移交。L-017 为 L-010 的工程缓解（stdlib 安全字段 `退出码值`），无独立用例，复用 `test_L010.light`。

## 语言团队修复顺序建议

1. **最高优先**：L-004（标识符拆分家族根因）→ 修好即同时收敛 L-010/L-011/L-012/L-015 及多数字典/属性拆分类问题；`test_L004/L010/L011/L012/L015` 是直接判据。
2. **高优先**：L-014（下标调用语句）、L-002（多行字面量）、L-006（闭包标量）——harness 主链路常见写法。
3. **中优先**：L-001（分层目录导入）、L-008（JSON 内置）、L-016（BaseException 捕获，建议 `捕获 全部` → `except BaseException`）。
4. **回归保护（当前已绿）**：L-003/L-007/L-007b/L-007c/L-009/L-013——编译器改动后必须保持这些用例绿，防止缺陷回潮。

## 移交注意事项

- L-013 在 T10-05（2026-08-27 早）实测为红（单行 for 吞类），本轮移交时 SRC 后端已不再复现（单行 for 生成 `for 循环变量 in 迭代对象:` 立即闭合）——请语言团队确认是已修复还是形态不再触发，并保留 `test_L013.light` 作回归保护。
- L-003/L-007/L-007b/L-007c 已在 T10-07 标注「已缓解转绿」（lexer 预扫描改进），本轮未回退。
- L-015 的复现需要载体文件 `examples/异步样例.light`（模块名以关键字词根开头）；若语言团队按「模块名整体成词」修复，`异步样例` 与 `异步代理`（lightharness 计划换回正式名）应一并可导入。
- L-016 依赖 asyncio 取消语义；验收需在异步上下文运行（用例已用 `异步 段落` + `异步 运行 主()`）。
