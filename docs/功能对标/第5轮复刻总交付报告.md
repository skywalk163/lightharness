# 复刻增量总交付报告 —— 第5轮（0.1.5-rc.2 遗留收尾）

- 轮次：第 5 轮（0.1.5-rc.2 遗留处理）
- 对标窗口：上游 0.1.2-rc.1（`a66e470204`，2026-09-03）→ 0.1.5-rc.2（`a305303422`，2026-09-10）
- lightharness 合入后 HEAD：`3b17933`
- 交付形态：4 条并行任务（任务1 入口默认模型 / 任务2 消息词汇+附件 / 任务3 团队事件+goal守卫 / 任务4 会话查询）+ 元任务（路M）
- 计划与任务书：`0.15遗留_第5轮_任务prompt分发.md`（4 任务 + 路M，已分发）
- 基线：第4轮收口点 `4e06d53`（全量 CI 207 passed / 2 failed 既有）

---

## 1. 本轮目标与结果一览

| 维度 | 结果 |
|---|---|
| 合入 | 四路全部并入 main：任务1/2 工作区直提（`efbcb77`/`1c56e7b`）+ 任务3/4 分支 `--no-ff` 合并（`3b17933`/`4ae4d91`），**零冲突** |
| 新增 src 模块 | `src/会话查询.light`（26 段 / 596 行，session-query extraction/documents/tracing/sources/corpus 纯逻辑） |
| 改动 src 文件 | 10 个 `src/*.light`（完整清单见 §3） |
| 新增测试 | 4 个 `examples/*.light`（test_入口默认模型 / test_附件准入1.5 / test_目标恢复守卫 / test_会话查询1.5） |
| 回归门禁 | 全量 CI：**211 passed / 2 failed**（= 第4轮 207 + 本轮新增 4 用例；2 个既有失败同基线，见 §4） |
| 合入回归 | 一次到位，**无跨路形状修复**（任务1/2 与任务3/4 文件互斥表约束生效） |
| 波及核对 | 73 卡逐卡核对，本轮处置 9 卡 + 宿主层确认 13 卡（见 §6） |
| 缺陷账 | 四路均无新增 L-0xx；L-079 维持登记（见 §7） |
| 遗留闭环 | 第4轮「真缺口 7 卡」全部处置完毕（#4/#48/#65/#66/#36/#39/tool-goal 守卫）；入口默认模型移交项闭环（任务1） |

---

## 2. 四路交付摘要（逐路）

### 任务1｜入口默认模型 + 计量价格 —— 直提 `efbcb77`
- 三处硬编码 `"deepseek-chat"`（`总入口.light:225`、`web服务器.light:65/377`）全部收敛到 `取默认模型()`（第4轮路5 已交付，返回 `"deepseek-flash"`）——模型换代只改 `客户端.light` 目录。
- `总入口.light` 新增注入式 `解析模型(环境取值, 文件甲取值, 文件乙取值)`（4 级 fallback 链抽段，环境变量 `HARNESS_MODEL` → `.env HARNESS_MODEL` → `.env OPENAI_MODEL` → 内置默认，显式配置永远优先）。
- `令牌计量.light` 单价表补 `"deepseek-flash": [0.27, 1.10]`（与 chat 同代同价），未知模型回落缺省价契约不变。
- 模型路由核对：`客户端.light` 无模型 id 分支路由，模型名原样进请求体 → 无需额外映射。
- 新增 `test_入口默认模型` 3 节全绿；反跑 2/2 成立（fallback 改回 deepseek-chat 红、单价表去掉 flash 红）；`test_令牌计量/令牌估算/令牌估算1.5/模型目录/web路由/web服务器E5/总入口` 全绿。

### 任务2｜消息词汇表 + 通用文件附件 —— 直提 `1c56e7b`
- `附件准入.light` 新增 6 段：`判定规范base64按策略`（空串 reject/accept 参数化，对齐 admission.ts `decodeCanonicalBase64` 的 empty 参数）、`解码校验`（Image/File 错误码文案逐字对齐）、`规整文件名`（剥路径留叶名）、`存文件输入`（零字节合法）、`造文件引用`（**纯逻辑哈希近似 sha256**，格式 `file-<hex>`，差异已登记）、`准入编码文件`（端到端）。既有 4 段行为零改动。
- `消息.light` 新增 `造系统消息`（对齐 createSystemMessage：空文本→`content:[]`、source plugin）、`造文件块`（FileBlock）；`取消息文本` 补 file 分支投影 `[文件 <name>: <bytes> 字节]`。
- `代理.light` 收编局部 `造系统消息` 改走 `消息.造系统消息(文本, "system-prompt")`，消息形状逐字段不变。
- 已查证：`会话格式.light` V3 catalog 是事件类型词汇，与 llm 消息内容块层无交叉 → 无需改。
- 新增 `test_附件准入1.5` 全绿；广泛回归 21/21 绿；反跑 4/4 成立。

### 任务3｜团队事件版本2 + goal 恢复守卫 —— merge `3b17933`（分支 `task-5-team-goal`，`d5d6b71`）
- `团队依赖图.light` 4 个事件构造段（构造成员/任务/排队/送达通知）version 1→2 逐行对齐上游 `types.ts:221-228`；tests 4 处断言同步。
- **团队看板复核结论：已核对（无对应移植点）**——上游 `task-board.ts` 的 version bump 经 Cordis journal 发射版本化持久事件；光明看板走本地 `造记录()` + 注入 `追加事件`，不发射上游 team/task 事件（grep 确认无引用）。
- `目标折叠.light` 新增 `判定模型可恢复`：paused + id/revision 命中 → 抛 `the model cannot resume a paused goal; the user must resume it`（GOAL_TOOL_RESUME_PAUSED，语义+文案逐字对齐）；active/blocked/complete/引用不匹配 → 放行。
- 新增 `test_目标恢复守卫` 7 组全绿；8 定向用例全绿；反跑 6/6 成立。
- 未移植项：task-board version bump（无对应点）/ delivery 字段（从未实现）/ tool-goal 工具注册与 authority.ts（宿主层）。

### 任务4｜会话查询新能力 —— merge `4ae4d91`（分支 `task-5-query-4`，`ce3e0a5`）
- 新建 `src/会话查询.light`（26 段 / 596 行）：抽取族（连接/块/内容/结束原因/抽取事件文本，对齐 extraction.ts 89 行）、表面折叠族（是表面合格/表面操作取/索引查找/折叠表面，对齐 surface.ts foldSurface 只做形状判定；字符串 `"replace"` = V3 节点 0 语义、字典 `{op:replace,startSeq,endSeq}` 闭区间）、分析事件日志（current/shadowed/log-only 三分类）、追踪族（追踪事件/追踪会话——后代树用游标 BFS 因光明无 `栈.移除`，循环父抛错/缺父 partial/complete/root）、头族（校验头兼容/头取字段/头是否种子/复制头加标识——光明头信息无 id 键，id 在 `.标识`，需补标识）、语料族（解析逻辑会话/解析会话语料/合并会话记录——同 id 校验头兼容，live/persisted 双标记）。
- 既有 `会话查询过滤.light` 仅加一行接口衔接，段落零改动。
- 新增 `test_会话查询1.5` 5 组判据全绿；涉及模块既有回归全绿；反跑 3/3 成立。
- 光明语言适配实测记录（字典键表→字典键列表、列表无 .移除→游标遍历、下标写须带 `设`、链式写静默无效→整字典替换、字符串工具显式导入）。
- 未移植项：持久化 seam / Cordis / 并发 worker / foldSurface 深层校验 / turn/end reason 强枚举（宿主层或明确保留）。

---

## 3. 本轮改动文件

`git diff --name-only 4e06d53..HEAD -- src/`（10 个）：

```
src/总入口.light  src/web服务器.light  src/令牌计量.light   （任务1）
src/代理.light  src/消息.light  src/附件准入.light          （任务2）
src/团队依赖图.light  src/目标折叠.light                    （任务3）
src/会话查询.light  src/会话查询过滤.light                  （任务4）
```

合并提交序列：`efbcb77`（任务1）→ `1c56e7b`（任务2）→ `4ae4d91`（merge 任务4）→ `3b17933`（merge 任务3）。

---

## 4. 回归门禁（全量 CI）

`python scripts/ci_test.py`（LIGHT_MERGE=light-merge），本轮终值：**211 passed / 2 failed**（耗时 317s）。

- 2 个失败均为**既有失败**（第4轮基线同样失败，非本轮引入）：
  - `test_agentE5钩子.light`
  - `test_压缩E5.light`
- 新增 4 用例全过（test_入口默认模型 / test_附件准入1.5 / test_目标恢复守卫 / test_会话查询1.5），211 = 第4轮 207 + 4。
- 核心模块冒烟 5/5 通过（会话/代理/工具/消息/流）。
- 任务3/4 分支自验（分支上全量）均为 208 passed / 2 failed（各 +1 新用例），与合入后口径一致。

---

## 5. 路M 合入过程

- 任务1/2 工作区直提：文件分组提交（各自 src 改动 + 测试 + 反跑脚本 + 交付报告），commit 成功后工作区仅剩路M 自身工具文件。
- 任务3 工作区残留副本（`团队依赖图.light`/`目标折叠.light`/`test_团队依赖图.light`/`test_目标恢复守卫.light`）与分支 blob **逐字节一致**（`git rev-parse` vs `git hash-object` 全 True）→ 丢弃工作区副本后 `--no-ff` 合并，无冲突。
- 任务4 分支直接 `--no-ff` 合并，无冲突。
- 两分支均基于第4轮收口点 `4e06d53`；任务1/2 提交后 main 前进，三方合并按文件互斥表预期零冲突。
- 合入后全量 CI 一次到位（211/2），**无需跨路修复**——与第4轮（15 failed → 修复形状 2 处）不同，本轮四路文件互斥执行彻底。

---

## 6. 波及核对（73 卡对 0.1.2→0.1.5 增量，第5轮视角）

方法沿用第4轮：预计算上游 `git diff --name-only a66e470204..a305303422 -- packages apps` 全量清单 → 包路径映射 → 概念路径解析候选 → 逐卡统计 src 变更数并用本轮触及 src 文件（第4轮 22 + 本轮 10 = 30 个）判定覆盖。脚本：`_taskM_波及核对.py`（v2 复用，TOUCHED 集合更新至第5轮），输出 `_taskM_波及核对_out_r5.txt`。

**脚本机械分类（73 卡）：** 已覆盖 22 ｜ 仅文档/测试 18 ｜ 无变更 10 ｜ 实质变化未派单 23

**本轮处置（9 卡回填 + 13 卡确认，均已写入 `对标清单.json` 各卡状态）：**

| 卡 | 处置 | 内容 |
|---|---|---|
| #4 消息词汇表 | ✅ 已覆盖（任务2） | 消息.light 造系统消息（createSystemMessage）+ 造文件块 + 代理收编 |
| #11 CLI 入口 | ✅ 已覆盖（任务1） | 总入口 解析模型 末级接线 取默认模型()；其余 CLI 宿主变更维持登记 |
| #36 会话查询 | ✅ 已覆盖（任务4） | 会话查询.light 移植 extraction/documents/tracing/sources/corpus 纯逻辑面 |
| #48 附件准入 | ✅ 已覆盖（任务2） | 通用文件附件 6 段（空串策略/错误码区分/纯逻辑哈希近似 sha256 已登记差异） |
| #65 团队依赖图 | ✅ 已覆盖（任务3） | 4 事件构造段 version 1→2 逐行对齐；delivery 字段从未实现 |
| #17 interaction 命令 | ✅ 已核对（宿主命令层） | 附件广义化属宿主命令注册层，纯逻辑无对应点；附件能力本体已由任务2 完成 |
| #39 plan-mode 附件 | ✅ 已核对（宿主命令层） | attachments 广义化属宿主命令注册层，无纯逻辑对应点 |
| #66 团队看板 | ✅ 已核对（无对应移植点） | 光明看板本地 造记录 形状，不发射上游 team/task 版本化事件 |
| #73 interaction 纯逻辑 | ✅ 已核对（宿主命令层） | 同 #17 |
| #35 tool-goal 守卫 | ✅ 已覆盖（任务3 补充） | 判定模型可恢复 移植进 目标折叠.light；工具注册仍宿主层 |

**宿主/OS/提供方层确认维持登记（13 卡，本轮任务书既定不做）**：#10、#12、#13、#20、#21、#37、#40、#47、#55、#56、#57、#62 hooks/subprocess/sandbox/code-runtime/e2b/acp/web-fetch/api 等，各卡状态已补「0.1.5第5轮确认：维持登记不移植」。

> 第4轮「真缺口 7 卡」（#4/#48/#65/#66/#36/#39/#35 补充）至此**全部处置完毕**，0.1.2→0.1.5 遗留清零。

---

## 7. 缺陷账收口

`docs/功能对标/语言缺陷账.md` 追加「第5轮收口确认」：
- **无新增 L-0xx**：四路交付审读与合入完成，各报告明确声明无新缺陷；新增代码均复用既有绕法，未触发新缺陷面。
- **L-079 维持登记**（待光明侧生成器修复，并入 L-066 括号栈泳道）；绕法已落地（else 分支体必须比 `否则:` 更深）。

---

## 8. 遗留事项（移交清单）

1. **宿主/OS 层 13 卡**（§6）：按既定「宿主不移植」原则维持登记；若后续需要 Windows 侧对应能力再评估。
2. **附件 attachmentId 非 sha256**（任务2 差异登记）：纯逻辑哈希（乘加 FNV 变体）近似，确定性与内容寻址语义一致，非加密强度；若后续宿主层引入真 sha256 可替换。
3. **工作区残留**：`_w3_sess/`（第4轮探针残留，确认无用途）本轮删除；`_taskM_波及核对.py` / `_taskM_*_out*.txt` / `_taskM_update_ledger5.py` / `_taskM_ci_run5.log` 为路M 工具与输出，保留工作区未提交（备查）。
4. **遗留分支**：task-5-team-goal / task-5-query-4（已合入）以及第4轮遗留分支（task-1-session-v3 / task-2-agent-team / task-3-agent-loop-goal / task-6-delivery-15 / task-T6-lh / lh-map / lh-web）未删除，可保留备查。
5. **上游后续窗口**：0.1.5-rc.2 之后如有新版本（如 0.1.6），按既有流程重开对标窗口。

---

## 9. 验证方式与覆盖范围

- 全量 CI `scripts/ci_test.py`：211 passed / 2 failed（2 既有失败同基线）；核心冒烟 5/5。
- 四路反跑判据：任务1 2/2、任务2 4/4、任务3 6/6、任务4 3/3（改反即红，恢复即绿）。
- 合入验证：任务3 工作区与分支逐字节比对（blob hash 全同）后清理；两分支 `--no-ff` 合并零冲突。
- 波及核对：脚本 v2 输出 `_taskM_波及核对_out_r5.txt`（30 个触及 src 文件），9 卡回填 + 13 卡确认写入 `对标清单.json`。
- 覆盖范围：src 全部 10 个改动文件、73 卡逐卡、缺陷账收口；未覆盖：宿主/OS 层（§6 登记项）未移植、FreeBSD 0.88 复核未重跑（`_ssh_upload_88.py` 可用）。
