# 行为差异清单（第8轮行为级对照汇总）

> 生成：2026-09-12（第8轮汇总）｜第9轮回填：2026-09-12（10 项已修复）
> 来源：四份对照交付报告 §5 差异清单（_task1_代理循环对照 / _task2_会话持久化对照 / _task3_LLM对照 / _task4_工具子智能体对照）
> 原则：第8轮只登记；第9轮修复 10 项（见判定列 ✅），剩余 4 项维持。
> 判定列：🔴=建议修复（真实行为 bug）｜🟡=复刻缺口/语义差异（可后置）｜⚪=语言层限制/宿主面（维持登记）

---

## 一、差异总表

| ID | 轮·路 | 上游包·spec | 光明模块 | 差异描述（上游期望 → 光明实际） | 疑似根因 | 判定 |
|---|---|---|---|---|---|---|
| T2-D5 | 8·任务2 | core/session surface.spec | `src/会话格式.light:537` 模块级投影 | 节点0 append 连接分支读 `数据["system"]`（英文键），与 `记录系统提示` 写入的 `["系统"]`（中文键）不一致——**第二条 append 必抛 KeyError**；类方法 `会话.投影节点0`(:89) 用 `["系统"]` 正确 | 键名笔误 | ✅ 第9轮已修复（:543 改 ["系统"]，补 append 连接分支断言） |
| T2-D1 | 8·任务2 | surface.spec | `src/会话.light:175-201` | replace 起点/终点不存在、start>end **抛错** → 静默无操作 | 该段仅按序号过滤区间，无存在性与顺序校验 | ✅ 第9轮已修复（压缩替换三重区间校验，文案对齐上游） |
| T2-D2 | 8·任务2 | surface.spec | `src/会话格式.light:174-177` | sourceEventSeqs 是**快照**（改动不影响已记录）→ 直接引用同一列表，调用方追加透传 | 光明无浅拷贝内置，段落直赋引用 | ✅ 第9轮已修复（事件带来源逐元素拷贝快照，登记 L-082） |
| T2-D3 | 8·任务2 | canonical-envelopes / session | `src/会话格式.light:188-209` | V0 编码→解码**还原版本 3.0** → 编码写中文键 `主/次`、解码读英文键 `major/minor`，往返退化 2.0 | 编解码键名约定不一致（持久化主路径走英文键不受影响） | ✅ 第9轮已修复（V0+V2 头统一英文键 major/minor） |
| T2-D4 | 8·任务2 | sequence-types | `src/会话格式.light:27-32` | 恢复边界**拒绝负零序号** → 光明/Python `-0` 与 `0` 不可区分，仅判 `值<0` 放行 | 数值域差异（语言层） | ⚪ 语言层限制 |
| T2-G1 | 8·任务2 | repair.spec | 光明无对应段落 | 为未闭合 tool/call 合成 tool/result（含 surfaceOp/来源序号） | 该域未翻译 | 🟡 缺口登记，是否补真身待裁决 |
| T3-D1 | 8·任务3 | llm assembler/mapUsage | `src/令牌估算.light` | usage 在 prompt+completion 之外补 `totalTokens` 桶 → 光明仅 输入/输出 | 纯语义未实现（复刻缺口） | ✅ 第9轮已修复（翻译用量补 totalTokens=prompt+completion，对齐 mapUsage） |
| T3-D2 | 8·任务3 | llm message.spec | `src/消息.light` | createXxxMessage 经 deep-freeze 与传入 content **脱钩** → 光明按引用组装，调用方改入参会污染已构造消息 | 无深拷贝/冻结 | ✅ 第9轮已修复（拷贝内容块表逐块拷贝脱钩，登记 L-083） |
| T3-D3 | 8·任务3 | llm-deepseek image-tokens.spec | 光明无对应 | 字节级图片 token 估算 → 未移植 | 无字节级模型 | ⚪ 未移植域（等价能力缺失） |
| T1-S18 | 8·任务1 | agent-loop tool-calls.spec | `src/工具注册表.light`（工具注册表.执行） | concludesTurn 由工具函数返回值传播 → 始终设 `假` | 执行方法不读工具返回值字段 | ✅ 第9轮已修复（造工具结果+执行主路径读 concludesTurn，代理循环集成验证） |
| T4-D1 | 8·任务4 | subagent assistant-output.spec | `src/子智能体.light`（取最后回答） | 返回**内容块表** `[{type:"text",...}]`、无输出→`undefined`、reasoning-only 视为**非空** → 返回**拼接文本字符串**、无输出→`""`、reasoning-only **跳过** | 投影形态+边界语义差异 | ✅ 第9轮已修复（新增取最后回答块表，reasoning 非空，取最后回答复用拼接） |
| T4-D2 | 8·任务4 | tools execution-mode.spec | `src/工具.light`（执行策略） | isConcurrencySafe 未声明 → **exclusive** → 未注册/缺省 → **parallel** | 默认并发语义差异（既有 test_工具 亦断言 parallel） | ✅ 第9轮已修复（默认 exclusive，上游 execution-mode.spec 核实确认） |
| T4-D3 | 8·任务4 | tools tools.spec | `src/工具.light`（校验参数） | 上游英文文案+字段结构 → 光明中文文案+结构化错误表 `{路径,原因,细节}` | 文案语言差异 | ⚪ 语义等价、结构不同，维持 |
| T4-D4 | 8·任务4 | preset display.spec | `src/预设.light` | shipped 预设走字典键解析（t:presetStandardName）→ 无 shipped 字典翻译（user 元数据原样保留 ✓） | 无 i18n 字典键 | ✅ 第9轮已修复（内置预设文案字典+造显示文本，standard/ptc/minimal/cordis） |

## 二、汇总统计（第9轮回填后）

- **✅ 第9轮已修复**：10 项（T2-D5/D1/D2/D3、T3-D1/D2、T1-S18、T4-D1/D2/D4）
- **🟡 维持（后续轮）**：1 项（T2-G1 repair 域缺口——域较大，留后续轮）
- **⚪ 语言层限制/宿主面/未移植**：3 项（T2-D4 负零、T3-D3 图片 token、T4-D3 文案语言）

## 三、第9轮修复结论

1. **10 项已修复**（🔴1 + 🟡9）：会话域 4 项、LLM 域 2 项、工具域 2 项、子智能体+预设域 2 项；全量 CI 保持全绿。
2. **T2-G1 repair 域**维持登记（域较大，涉及未闭合 tool/call 合成，留后续专项轮）。
3. **⚪ 3 项**维持登记（语言层/宿主面，无等价移植面）。
4. 新登记语言缺陷：**L-082**（副本内置假实现，浅拷贝绕法）、**L-083**（无深拷贝内置，逐块拷贝绕法）。
