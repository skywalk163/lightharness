# 行为差异清单（第8轮行为级对照汇总）

> 生成：2026-09-12（第8轮汇总）｜第9轮回填：2026-09-12（10 项已修复）
> 来源：四份对照交付报告 §5 差异清单（_task1_代理循环对照 / _task2_会话持久化对照 / _task3_LLM对照 / _task4_工具子智能体对照）
> 原则：第8轮只登记；第9轮修复 10 项（见判定列 ✅），剩余 4 项维持。
> 判定列：🔴=建议修复（真实行为 bug）｜🟡=复刻缺口/语义差异（可后置）｜⚪=语言层限制/宿主面（维持登记）

---

## 一、差异总表

| ID | 轮·路 | 上游包·spec | 光明模块 | 差异描述（上游期望 → 光明实际） | 疑似根因 | 判定 |
|---|---|---|---|---|---|---|
| T2-D5 | 8·任务2 | core/session surface.spec | `src/会话格式.light:537` 模块级投影 | 节点0 append 连接分支读 `数据["system"]`（英文键），与 `记录系统提示` 写入的 `["系统"]`（中文键）不一致——**第二条 append 必抛 KeyError**；类方法 `会话.投影节点0`(:89) 用 `["系统"]` 正确 | 键名笔误 | ✅ 第9轮已修复（:543 改 ["系统"]，补 append 连接分支断言） |
| T2-D1 | 8·任务2 | surface.spec | `src/会话.light:175-201` | replace 起点/终点不存在、start>end **抛错** → 静默无操作 | 该段仅按序号过滤区间，无存在性与顺序校验 | ✅ 第9轮已修复（压缩替换三重区间校验，文案对齐上游） |
| T2-D2 | 8·任务2 | surface.spec | `src/会话格式.light:174-177` | sourceEventSeqs 是**快照**（改动不影响已记录）→ 直接引用同一列表，调用方追加透传 | 光明无浅拷贝内置，段落直赋引用 | ✅ 第9轮已修复（事件带来源逐元素拷贝快照，登记 L-082） |
| T2-D3 | 8·任务2 | canonical-envelopes / session | `src/会话格式.light:188-209` | V0 编码→解码**还原版本 3.0** → 编码写中文键 `主/次`、解码读英文键 `major/minor`，往返退化 2.0 | 编解码键名约定不一致（持久化主路径走英文键不受影响） | ✅ 第9轮已修复（V0+V2 头统一英文键 major/minor） |
| T2-D4 | 8·任务2 | sequence-types | `src/会话格式.light:31-38` | 恢复边界**拒绝负零序号** → 光明/Python `-0` 与 `0` 不可区分，仅判 `值<0` 放行 | 数值域差异（语言层） | ✅ 第11轮已修复（任务5 T2-D4：提供 是负零 原语，`安全整数` 置于 是整数 之前显式拒绝浮点负零，对齐上游 Object.is(value,-0)） |
| T2-G1 | 8·任务2 | repair.spec | `src/会话格式.light` §13 | 为未闭合 tool/call 合成 tool/result（含 surfaceOp/来源序号） | 该域未翻译 | ✅ 第10轮已修复（中断轮闭合器：未闭合 turn/step/tool-call 合成关闭事件，30断言覆盖repair.spec.ts全10场景；未接入恢复路径，增强留后续） |
| T3-D1 | 8·任务3 | llm assembler/mapUsage | `src/令牌估算.light` | usage 在 prompt+completion 之外补 `totalTokens` 桶 → 光明仅 输入/输出 | 纯语义未实现（复刻缺口） | ✅ 第9轮已修复（翻译用量补 totalTokens=prompt+completion，对齐 mapUsage） |
| T3-D2 | 8·任务3 | llm message.spec | `src/消息.light` | createXxxMessage 经 deep-freeze 与传入 content **脱钩** → 光明按引用组装，调用方改入参会污染已构造消息 | 无深拷贝/冻结 | ✅ 第9轮已修复（拷贝内容块表逐块拷贝脱钩，登记 L-083） |
| T3-D3 | 8·任务3 | llm-deepseek image-tokens.spec | 光明无对应 | 字节级图片 token 估算 → 未移植 | 无字节级模型 | ⚪ 未移植域（等价能力缺失） |
| T1-S18 | 8·任务1 | agent-loop tool-calls.spec | `src/工具注册表.light`（工具注册表.执行） | concludesTurn 由工具函数返回值传播 → 始终设 `假` | 执行方法不读工具返回值字段 | ✅ 第9轮已修复（造工具结果+执行主路径读 concludesTurn，代理循环集成验证） |
| T4-D1 | 8·任务4 | subagent assistant-output.spec | `src/子智能体.light`（取最后回答） | 返回**内容块表** `[{type:"text",...}]`、无输出→`undefined`、reasoning-only 视为**非空** → 返回**拼接文本字符串**、无输出→`""`、reasoning-only **跳过** | 投影形态+边界语义差异 | ✅ 第9轮已修复（新增取最后回答块表，reasoning 非空，取最后回答复用拼接） |
| T4-D2 | 8·任务4 | tools execution-mode.spec | `src/工具.light`（执行策略） | isConcurrencySafe 未声明 → **exclusive** → 未注册/缺省 → **parallel** | 默认并发语义差异（既有 test_工具 亦断言 parallel） | ✅ 第9轮已修复（默认 exclusive，上游 execution-mode.spec 核实确认） |
| T4-D3 | 8·任务4 | tools tools.spec | `src/工具.light`（校验参数） | 上游英文文案+字段结构 → 光明中文文案+结构化错误表 `{路径,原因,细节}` | 文案语言差异 | ⚪ 语义等价、结构不同，维持 |
| T4-D4 | 8·任务4 | preset display.spec | `src/预设.light` | shipped 预设走字典键解析（t:presetStandardName）→ 无 shipped 字典翻译（user 元数据原样保留 ✓） | 无 i18n 字典键 | ✅ 第9轮已修复（内置预设文案字典+造显示文本，standard/ptc/minimal/cordis） |

## 二、汇总统计（第12轮收口后）

- **✅ 已修复**：19 项（第9轮 10 项 + 第10轮 T2-G1 + 第11轮任务5 T2-D4 + 第12轮 R12-D1~D6、D8 共 7 项）
- **🟡 维持（后续轮）**：0 项
- **⚪ 语言层限制/宿主面/未移植/待决策**：10 项（T3-D3 图片 token、T4-D3 文案语言 + 第12轮 R12-D7、D9~D15 共 8 项）

## 三、第10轮修复结论

1. **T2-G1 repair 域**已修复（第10轮）：`src/会话格式.light` §13 新增中断轮闭合器（interruptedTurnClosers），为未闭合 turn/step/tool-call 合成关闭事件（错误 tool/result → step/end → turn/end {interrupted}），30 断言覆盖 repair.spec.ts 全 10 场景，反跑 3/3。未接入持久化恢复路径（增强留后续轮）。
2. **chat 对话框无响应**：任务2 排查后判定为误报（当前无需改动），可能是环境/配置问题，留待后续观察。
3. **⚪ 2 项**维持登记（T3-D3 图片 token、T4-D3 文案语言：语言层/宿主面，无等价移植面）。
4. 本轮无新登记语言缺陷。

## 四、第11轮任务5修复结论（T2-D4 负零）

1. **上游核实**：`packages/session/session-format/src/json.ts` 的 `sessionFormatCount`/`sessionFormatSafeInteger`
   均以 `Object.is(value, -0)` 显式拒绝负零（throw "must be a non-negative safe integer"），
   `filename.spec.ts` 亦把 `-0` 列入非法 generation；**上游真拒绝负零，T2-D4 是真实差异**。
2. **方案A（语言层）落地**：light-merge 新增 是负零 原语（`stdlib/内置核心判型.light` 纯光明实现，
   零 Python 导入：`str.startswith(转字符串(值), "-")`；builtins.py 一行转发；codegen/keywords/
   type_inferencer/统一生成器四路注册 + codegen stdlib 兜底 lambda），差分测试
   `tests/unit/test_地板搬迁_判型_S2.py` 负零矩阵 13 条逐条与 oracle 对跑。
3. **恢复边界接入**：`src/会话格式.light` `安全整数` 在 是整数 之前先查 是负零，
   浮点 -0.0 → "must be non-negative"（与上游对 -0 的文案同语义）；`examples/test_行为对照_会话持久化.light`
   新增 6d 断言（`安全整数(转浮点("-0.0"), "seq")` 拒绝）。
4. **残留宿主差异（已注释登记）**：JSON 文本 `"-0"` 在 Python/光明解析为整数 0，符号位在解析层丢失
   （JS 无 int/float 之分，`JSON.parse("-0")` 保留 -0）——该形态语言层不可区分，属宿主解析差异；
   数值域负零（float -0.0）已显式拒绝，对齐上游 Object.is 语义。


## 五、第11轮路M行为差异登记（任务4/5/6 纯逻辑复刻边界）

> 生成：2026-09-12（第11轮路M收口）｜均为纯逻辑复刻边界，线形状语义对齐上游，宿主/语言层差异维持登记。
> 判定列：⚪=语言层限制/宿主面（维持登记，不阻塞）。

| ID | 轮·路 | 上游包·spec | 光明模块 | 差异描述（上游期望 -> 光明实际） | 疑似根因 | 判定 |
|---|---|---|---|---|---|---|
| R11-D1 | 11·任务4 | sdk/protocol transport.ts | `src/JSONRPC传输.light` | 上游 `Buffer + StringDecoder('utf8')` 字节流按 `\n` 切行 -> 光明按**文本块**推入，多字节 UTF-8 跨块切断由 `stdlib/UTF8增量解码器.light` 承担，本模块按文本行流模拟 | 光明无 byte 流一等原语 | ⚪ 语言层限制（文本行流模拟等价线帧） |
| R11-D2 | 11·任务4 | sdk/protocol transport.ts:121 | `src/JSONRPC传输.light` 造请求ID | 上游 `req_`（UUID v4 去横杠）-> 光明 `req_` + 三段随机整数拼接（1e8~1e9 + 1e4~1e5 + 1e3~1e4，熵约 10^18），前缀同、唯一性测试咬住（100 个全唯一），但非 UUID v4 形状 | 光明无 `node:crypto.randomUUID` | ⚪ 语义等价（req_ 前缀+高熵唯一段），形状不同 |
| R11-D3 | 11·任务5 | boot/app-boot profile.ts L42-45 | `src/启动配置.light` | 上游 patch 文件 `cordis.patch.yml`（YAML）-> 光明 `cordis.patch.json`（JSON 数组，每条 `{id, config, insert}`） | 光明无 YAML 解析器 | ⚪ 格式差异（语义等价：id 覆盖 + insert 追加） |
| R11-D4 | 11·任务5 | boot/app-boot + apps/cli args.ts | `src/启动配置.light` | 上游 profile/patch/plugin 子命令接入 commander 树与 `src/总入口.light` 命令面 -> 光明仅复刻纯解析逻辑，**未接入总入口命令面**（总入口已有"未映射宿主面"明确登记，test_CLI命令面 已锁定命令面行为） | 跨域风险（总入口命令面被既有测试锁定） | ⚪ 宿主面未接入（后续轮如需 HARNESS_PROFILE 环境变量可在总入口 dump-config 分支加只读查询） |
| R11-D5 | 11·任务6 | test-support/llm-mock-server index.ts seededRandom | `src/mock大模型服务器.light` 类 播种随机 | 上游 mulberry32（`>>>`/异或/`Math.imul` 位运算）-> 光明整数 LCG（`状态=(状态*1664525+1013904223)%4294967296`，返回 `状态/4294967296`） | 光明无位运算原语 | ⚪ 契约等价（种子可注入/同种子同序列/异种子异序列/按权重比例），浮点流不复刻 |
| R11-D6 | 11·任务6 | test-support/llm-mock-server chunkDelayMs/cli.ts/bin.ts | `src/mock大模型服务器.light` | 上游 `slow_success`/`chunkDelayMs` 块间延时 + CLI `--listen-delay`/进程包装 -> 光明 `slow_success` 线形状同 success（不模拟延时），cli.ts/bin.ts 未移植 | 计时维度 + CLI 生命周期属宿主面 | ⚪ 未移植（线形状已对齐；真实内核端口 bind 走 启动Mock服务 复用 HTTP服务端） |

### 汇总（第11轮路M）

- 本轮新增 6 项 ⚪ 维持登记（R11-D1~D6），均为纯逻辑复刻边界，不阻塞 CI。
- 语言层新缺陷：L-084（`为` 赋值关键字切碎尾部中文标识符）已登记入 `语言缺陷账.md`，绕法=裸标识符避开「为」字；`examples/_repro_L084.light` 按修复形态（绕法）保留 rc=0。
- 任务2 提及「等待」是 await 语句关键字（`等待 异步睡眠(...)`），不能作局部变量名；经评估属**已知语言行为**（与 L-004/L-021/L-025 同族的关键字词法约束），非本轮新引入缺陷，不入缺陷账，仅在此说明。

## 六、第12轮路M行为差异登记（任务1/3/4/6 对照修复与纯逻辑复刻边界）

| ID | 轮·路 | 上游包·spec | 光明模块 | 差异描述（上游期望 → 光明实际） | 疑似根因 | 判定 |
|---|---|---|---|---|---|---|
| R12-D1 | 12·任务6 | interaction/user-approval index.ts | `src/审批.light` | 裁决结果**四结果闭集**（allowed-once/rejected/cancelled/unavailable）→ 光明原「批准」真/假 二值 + 状态字段 | 复刻缺口（#73 未覆盖子包） | ✅ 已修复（新增四结果常量 + `审批结果词汇` 闭集；批准/状态 字段保留向后兼容） |
| R12-D2 | 12·任务6 | index.ts ApprovalPolicy | `src/审批.light` | ask/never 策略 + 会话级覆盖 + 缺省 ask → 光明无策略概念（权限分类三值） | 复刻缺口 | ✅ 已修复（审批管理器 增 `策略` 字段缺省 ask + `设置审批策略` 词汇校验 TypeError 文案 + `取审批策略`） |
| R12-D3 | 12·任务6 | index.ts decide 分派前 | `src/审批.light` | never 策略分派**前**确定性 rejected（不落应答者）→ 光明无 | 复刻缺口 | ✅ 已修复（`请求审批审计` never 分支直接 rejected，应答者不被调用） |
| R12-D4 | 12·任务6 | index.ts waterfall 兜底 | `src/审批.light` | 无应答者 fail-closed → `unavailable` → 光明 等待结果 超时拒绝（近似） | 宿主异步面 | ✅ 已修复（ask 策略 + 应答者为空 → `unavailable`；超时拒绝路径保留） |
| R12-D5 | 12·任务6 | index.ts rogue normalization | `src/审批.light` | 词汇外结果归一 → `unavailable` → 光明无 | 复刻缺口 | ✅ 已修复（`规范审批结果`） |
| R12-D6 | 12·任务6 | index.ts hasOpenTurn/request 前置 | `src/审批.light` | 开轮前置（非开轮抛错）+ hasOpenTurn 倒序扫描判定 → 光明 请求审批 无轮上下文校验 | 复刻缺口 | ✅ 已修复（`是开轮` 倒序扫描 + `请求审批审计` 开轮外抛错，文案要点对齐） |
| R12-D7 | 12·任务6 | index.ts 审计对（approval/asked + approval/decided） | `src/审批.light` + `src/会话格式.light` | 上游会话日志持久审计事件 id 配对、可选字段条件携带 → 光明事件词汇表（会话格式.light，只读）**无 approval/* 注册**，审计包由调用方落盘 | 事件词汇表只读 + 注册点移交路M | ⚪ 部分（`请求审批审计` 返回 审计包 形状对齐 types.ts；approval/* 词汇注册移交路M 待决策） |
| R12-D8 | 12·任务6 | index.ts 策略文案 | `src/审批.light` | NEVER_SENTENCE / ASK_SENTENCE 逐字 → 光明无 | 复刻缺口 | ✅ 已修复（`从不文案`/`询问文案`/`策略文案` 逐字对齐） |
| R12-D9 | 12·任务6 | interaction/tool-ask-user index.ts | `src/审批.light`（造提问调用/规范提问答案）+ `src/工具.light` | ask_user_question 输入/输出形状（multi_select→multiSelect、selected 复制、custom 条件携带）→ 光明无；且**未注册进 工具.light 注册表** | 注册点移交路M | ⚪ 形状已修复；工具注册表接入点移交路M 待决策（与 R12-D12 同决策） |
| R12-D10 | 12·任务6 | 上游无此面 | `src/权限.light` | 光明 权限4预设（放行/按类别/全拒绝/自定义）→ 上游 policy 只 ask/never | 光明扩展 | ⚪ 维持（never 是问询层语义，权限是工具准入层语义，正交不合并） |
| R12-D11 | 12·任务1 | session-query cold-read.ts | `src/会话冷读.light` | 上游 readColdSessionLog 直读会话日志 → 光明走**持久化只读面**（日志路径 + 文件系统.读取文件），不经过 会话存储.light | 存储访问通道不同 | ⚪ 语义对齐（只读句柄→读全量→中断轮闭合器合成→返回形状一致），通道不同 |
| R12-D12 | 12·任务1 | session-query tool-session-query | `src/查询工具.light` + `src/工具.light` | 查询工具**不注册进 工具.light 注册表**（任务书明确，避免与第11轮在飞任务冲突） | 注册点移交路M | ⚪ 纯逻辑面已复刻（过滤构造/ISO 精确解析/呈现全族）；工具注册表接入点移交路M 待决策（与 R12-D9 同决策） |
| R12-D13 | 12·任务3 | util/http-proxy policy.ts | `src/代理策略.light` | 上游 URL 类解析 → 光明 `HTTP解析URL`（urlparse）宽松解析 + 手写 `拆分网络位置` 还原 hostname/port | 光明无 URL 类一等原语 | ⚪ 语义对齐（空协议→invalid 诊断同上游），解析实现不同 |
| R12-D14 | 12·任务4 | test-support/llm-replay index.ts | `src/大模型回放.light` | 上游 v3 事件数据英文键（stream/message）→ 光明 v3 落盘中文键（流/消息） | 光明 v3 中文键 | ⚪ 双键兼容（`取流` 优先英文回退中文），事件类型名英文原样 |
| R12-D15 | 12·任务4 | llm-replay session-format-catalog 宿主接线 | `src/大模型回放.light` | 上游投影行/打包行/dense seq 语义在 session-format-catalog 宿主接线层 → 光明在 `解析会话日志` 内实现 | 任务书纯逻辑面边界 | ⚪ 边界不同（语义对齐，宿主接线不移植） |

### 汇总（第12轮路M）

- 本轮新增 8 项 ⚪ 维持登记（R12-D7、D9~D15：宿主面边界 / 语言层差异 / 注册点移交待决策）+ 7 项 ✅ 已修复（R12-D1~D6、D8）。
- **移交路M 待决策项**：① `approval/*` 事件词汇注册（D7）；② ask_user_question（D9）与 查询工具（D12）工具注册表接入点——两处均为"纯逻辑面已复刻、注册接入点移交路M 统一决策"；本轮收口**暂不注册**（避免影响第11轮已合入的工具注册表行为），登记为 ⚪ 待决策，后续轮如需接入在 `src/工具.light` 注册表补登记（含 schema 导出）。

## 七、第13轮路M行为差异登记（fs编辑器/shell环境/goal轮驱动/session日志/授权链/webhook 六域）

| ID | 轮·路 | 上游包·spec | 光明模块 | 差异描述（上游期望 → 光明实际） | 疑似根因 | 判定 |
|---|---|---|---|---|---|---|
| R13-D1 | 13·任务2 | shell/shell-env index.ts | `src/外壳环境.light` | 上游 `localeCompare` 排序（ICU 拼音序）→ 光明字符串 `<` 码点序近似；ASCII 键集与 ASCII 贡献者名下两者一致，中文名（甲 U+7532 < 乙 U+4E59 于拼音序相反）与「下划线-字母交叉」键的 locale 权重未逐一对齐 | 光明无 localeCompare 一等原语 | ⚪ 语言层限制（ASCII 域一致，非 ASCII 域差异登记） |
| R13-D2 | 13·任务6 | webhook/webhook-github handler.ts | `src/网络钩子GitHub.light` | 上游 octokit 标准 HMAC-SHA256 验签 → 光明 `哈希.HMAC_SHA256` 实为 **pbkdf2 单轮近似**（密钥未走 ipad/opad），同一输入与 Python 标准库 hmac 输出不一致（实测 supersecret/payload-body：光明 ec8ebaf2… vs 标准 36a6b169…） | stdlib 哈希实现偏差（L-095） | ⚪ 自洽链路内可用；对外对拍（GitHub 真实签名）必须宿主面标准 hmac（宿主入库时对拍） |
| R13-D3 | 13·任务3 | goal/goal-round-driver | `src/目标轮驱动.light` + `src/目标折叠.light` | 上游仅一份 goal-round-driver；光明 `src/目标折叠.light` 更早轮次已内置「轮次驱动决策」区块（驱动决策/预约有效/预步目标决策），本轮新增专用模块与其**并存**（语义一致：深比较/就绪栅栏/轮号=已开+1，本模块补检查点阶段与 queue-failed 阻止块） | 迭代轮次叠加（#35 先于 #88 落地） | ⚪ 并存待归并（路M 决策：后续轮可将 目标折叠.light 驱动区块收敛到 目标轮驱动.light，或标注 #88 为驱动面专用） |
| R13-D4 | 13·任务4 | session/session-log-deepseek invariant.ts:29-33 | `src/会话日志增量.light` | 上游 invariant 允许「继承分叉」事件（parentSession 存在且 !isOwnSeq 时标识不等不判不符）→ 光明按 index.ts 运行时折叠语义实现（标识不符一律跳过），继承豁免未移植（需宿主提供 isOwnSeq） | 宿主面 isOwnSeq 未接线 | ⚪ 边界不同（运行时语义对齐，invariant 豁免未移植） |
| R13-D5 | 13·任务6 | webhook/webhook-github handler.ts | `src/网络钩子GitHub.light` | 上游秘密 resolve 失败/为空 → **503**「GitHub webhook secret is unavailable」→ 光明门禁链 秘密空 → **401**；上游 assertConfig 在插件加载时抛错 → 光明 `校验配置` 在请求链 400（测试形态） | 实现偏差（测试注入面简化） | ⚪ 维持（自洽链路成立；宿主接线时须按上游 503 收敛秘密不可用语义） |
| R13-D6 | 13·任务5 | credentials/authorization AbortSignal | `src/授权链.light` | 上游 AbortSignal 可异步中止（流程运行中可被外部取消）→ 光明以运行上下文 `{中止标志, 中止处理器表}` **同步模拟**（前置检查拦截，流程运行中无法被外部异步中止） | 光明无异步信号原语 | ⚪ 语言层限制（同步近似，测试适配；宿主接线可补异步面） |

### 汇总（第13轮路M）

- 本轮新增 6 项 ⚪ 维持登记（R13-D1~D6）：语言层限制 3（D1 locale 序 / D2 HMAC 近似 / D6 异步中止）、迭代并存 1（D3 归并待决）、宿主面边界 1（D4 继承豁免）、实现偏差 1（D5 秘密 401/503）。
- 语言缺陷账新增 **L-091~L-103**（13 项，详见 语言缺陷账.md §第13轮段）：L-091 匿名段落内联 / L-092 关键字字样切碎（任务2+任务5 D7）/ L-093 跨模块异常位置块错配（任务3/4）/ L-094 入口目录遮蔽静默（无 repro）/ L-095 HMAC pbkdf2 近似（任务6 原撞号重排）/ L-096~L-103 任务5 D1-D9（字典属性/if 内段落提升/闭包 nonlocal/抛出丢属性/空字典/回调保留字/默认参数/可选键）。repro 文件：`_repro_L091/092/093/095.light` 已落地 rc=0；L-094 无 repro（登记原因见账本）。
- **移交路M 待决策延续**：① approval/* 事件词汇注册（R12-D7）；② ask_user_question 与 查询工具 工具注册表接入点（R12-D9/D12）；③ #88 与 #35 目标折叠驱动区块归并（R13-D3）；④ L-095 HMAC 宿主面标准 hmac 对拍入库。

## 第14轮（2026-09-13）

| 编号 | 轮次·任务 | 上游源 | 光明模块 | 差异描述 | 原因 | 状态 |
|---|---|---|---|---|---|---|
| R14-D1 | 14·任务1 | fs/tool-fs error.ts + edit.ts | `src/文件系统工具.light` | 上游 `FsError` 结构化 `code` 字段（错误以 code+message 承载）→ 光明错误以**消息文本**承载（无独立 code 字段）；上游 edit 唯一性判定在属后端契约（`assertUniqueEdit`）→ 光明本层补齐唯一性判定（单命中/未找到/歧义行号）；`read_image` 条件注册（无 attachments 服务时不注册）为注册面，光明以呈现形状入参化 | 光明无结构化错误码一等原语；宿主面入参化 | ⚪ 纯逻辑层可用；结构化错误码待语言层支持 |
| R14-D2 | 14·任务2 | experimental/agent-team types.ts + lifecycle.ts | `src/代理团队.light` | 上游任务状态枚举 `pending/in_progress/completed/deleted`（deleted 为墓碑终态）→ 任务书文案写 `cancelled`，光明以上游为准用 `deleted` 墓碑；上游 roster spawn 的子会话实际拉起与 settle/failed 流程、mailbox `dispatchTails` 串行投递/steer/flush、activity 变更等待器(wait/notify/timeout 10000..3600000)、lifecycle 处置所有权(AbortController/allSettled/超时) → 光明简化（accepted 判定为「目标 active 即达」、活动流纯聚合无等待器、生命周期纯状态机无 AbortController）；任务书补充实现（任务分页/活动流聚合/团队生命周期状态机与终止清理）上游无对应 | 宿主面（子会话拉起/串行投递/等待器/AbortController）入参化剔除；任务书与上游枚举不一致 | ⚪ 纯逻辑面语义一致；宿主异步行为待接线 |
| R14-D3 | 14·任务3 | session/session-persistence-jsonl format.ts + migration-verifier.ts | `src/会话持久化JSONL.light` | 上游 magic header 为独立 magic 字符串 + 注释行概念 → 光明 magic header 为**判别键 `type:"session"`**（无独立 magic 字符串、无注释行概念）；上游格式版本常量 `SESSION_FORMAT_VERSION` 数值在外部 catalog → 光明取 V3=3；上游 migration-verifier 以抛错+message 承载校验结果 → 光明输出**校验报告**（通过/失败+行号+原因）形态；上游无 seq 排序生成/增量生成/行号映射 → 光明任务书补充实现 | 任务书与上游实现差异；光明无独立 magic 原语 | ⚪ 纯逻辑面可用；magic header 判别键与上游物理格式差异登记 |
| R14-D4 | 14·任务4 | web/web-search-* provider.ts + 错误类 | `src/搜索提供商.light` | 上游 `WebError`/`HarnessError` 类体系与堆栈语义 → 光明以 **`{ok, code, message, provider}` 纯字典**承载错误；上游三家 `provider.ts` fetch 段（fetch/undici 网络IO/redirect:'error'/重定向策略）、deepseek `abortable`/`throwIfSearchAborted`/`AbortSignal` 竞速、三家 `resolveApiKey` 异步凭证解析、deepseek `recordRequest` 会话事件落盘 → 光明全部剔除（请求形状构造+响应归一纯逻辑，网络IO/密钥解析/事件落盘宿主面） | 光明无类继承/堆栈一等原语；宿主面入参化剔除 | ⚪ 纯逻辑面可用；错误类体系与异步凭证待宿主接线 |
| R14-D5 | 14·任务5 | typert/protocol types.ts + generator | `src/类型系统.light` | 上游 protocol 方法枚举完整（analyze/render/emit/listSymbols + 内部方法）→ 光明**方法枚举简化**（保留4个公开方法）；上游 generator `cordis-catalog.ts`（Cordis 插件目录扫描）、`tsdown-plugin.ts`（构建插件钩子）、`workspace.ts`（文件系统扫描/tsconfig 解析）→ 光明剔除；上游类型相等未知种类分支无特殊处理 → 光明回退 `序列化JSON` 串比较（触发 L-116 键序敏感问题，本模块同构造路径键序天然一致故可用）；任务书扩展（类型可赋值性/符号表作用域）上游无对应 | 宿主面剔除；L-116 序列化JSON键序敏感 | ⚪ 纯逻辑面可用；跨来源类型比较需键规范化（L-116 绕法） |
| R14-D6 | 14·任务6 | session/session-title normalize.ts + client.ts | `src/会话标题族.light` | **命名冲突**：任务书原名 `src/会话标题.light` 已被 #49（session-title/normalize）占用且已 done（238行）→ 光明本路改用 **`会话标题族.light`** 并**只读导入** #49 的 `规范化会话标题` 复用规范化能力（避免覆盖既有交付物）；上游 `client.ts`（Cordis 客户端/服务总线/设置注册）、session-title-llm/first-prompt/all-prompts 的实际 LLM API 调用（fetch/流式响应）→ 光明剔除（提示构造+请求/响应形状纯逻辑，LLM 调用宿主面）；上游标题更新事件 `session/title` → 光明构造事件形状，与会话格式词汇表衔接（未知可忽略类型） | 任务书命名与既有 #49 冲突；宿主面剔除 | ✅ 命名冲突已裁定（改族名+只读导入#49）；纯逻辑面可用 |

## 第15轮（2026-09-13）

| 编号 | 轮次·任务 | 上游源 | 光明模块 | 差异描述 | 原因 | 状态 |
|---|---|---|---|---|---|---|
| R15-D1 | 15·任务1 | context/session-reference | `src/会话引用深化.light` | 上游 Config 仅 JSDoc 约束无校验函数体 → 光明补充实现校验(maxReferences 1..3/candidateLimit 正整数/maxReferenceBytes ≥65536/referenceContextFraction 0..1)，错误前缀 `SESSION_REFERENCE_INVALID_CONFIG` 文案自拟；上游 `stringifyTagSafeJson` 不可序列化文案(undefined/循环引用)分支 → 光明 `序列化JSON` 无 undefined/循环引用，该分支不可达；命名冲突(既有 `src/会话引用.light` 已覆盖 #33 uri.ts)改深化名，只读导入复用 URI 编解码 | 任务书补充实现；光明无 undefined/循环引用；命名冲突 | ✅ 命名冲突已裁定(改深化名+只读导入#33)；纯逻辑面可用 |
| R15-D2 | 15·任务2 | storage/storage + storage-json | `src/存储核心.light` | 上游 `StorageError` 结构化 code 字段 → 光明以 `StorageError[码] 消息` 文本承载(7码逐字：backend-not-found/form-not-mounted/duplicate-backend/duplicate-mount/version-mismatch/malformed-medium/closed)；上游 KvUnit 有 list/exists 接口 → 光明 loadAll 全量快照(存在性经快照推导)；上游 storage-json atomic.ts 依赖 Node fs 原子写 → 光明剔除(介质=字典<路径,文档文本>模拟，单记录单元零内存态每次 loadAll 从介质折叠) | 光明无结构化错误码一等原语；fs 依赖宿主面 | ⚪ 纯逻辑面可用；结构化错误码待语言层支持 |
| R15-D3 | 15·任务3 | attachment/attachment + feedback | `src/附件反馈.light` | 上游 `requestImageDimensions` 用 JS `Math.round`(half-up) → 光明 `四舍五入` 为银行家舍入(round-half-even)，.5 时取最近偶数，几何收缩尺寸差 1 像素(L-125)；上游 attachment-local 压缩/编码/文件存储/图片处理 → 光明剔除(纯逻辑面只做准入校验/品牌/错误/请求投影/类型) | L-125 四舍五入语义偏差；宿主面剔除 | ⚪ 纯逻辑面可用；取整用 `取整半数上(x)=向下取整(x+0.5)` 绕法(L-125) |
| R15-D4 | 15·任务4 | schedule/schedule + jobs/jobs + guard | `src/调度深化.light` | 上游 `transaction.ts` 用 `WeakMap<Agent, Promise>` 以 Agent 对象身份为键做按归属串行化 → 光明字典键必须可哈希标量，对象/列表身份键不可表达，且失败抛 Python 原生 TypeError 未包装(L-127)，光明以「归属令牌(字符串)」作键绕法；schedule/domain.ts 已覆盖 #29，本任务只做投影/事务/类型深化；jobs-local/tool-jobs 已覆盖 #53，本任务只做 jobs 核心品牌/类型 | L-127 字典对象身份键不可表达；已覆盖域不重复 | ⚪ 纯逻辑面可用；对象身份键待语言层支持(L-127) |
| R15-D5 | 15·任务5 | preset/agent-presets + persona + settings + skill | `src/预设深化.light` | 上游 `composition-inventory.ts` 用 JS `Boolean(value)` 判真值 → 光明空列表/空字典判假(JS 判真)、`0==假`为真(JS `0===false`为假)，两面同根沿用 Python 语义(L-129)，光明显式实现 JS 真值表函数绕法；agent-presets 7 文件已覆盖 #72，本任务只做组合清单/显示/类型深化；settings/index.ts+invariant.ts 宿主面剔除；skill-filesystem/tool-skill 宿主面剔除 | L-129 真值语义与 JS 不一致；已覆盖域不重复；宿主面剔除 | ⚪ 纯逻辑面可用；真值语义待语言层支持(L-129) |
| R15-D6 | 15·任务6 | acp/acp + hooks/hook-protocol | `src/协议深化.light` | 上游 ACP 基于 JSON-RPC stdio，index.ts 依赖 Cordis Context + Node buffer/crypto/fs → 光明剔除宿主面，只做 MCP 消息/模型控制/语义更新的纯逻辑形状(请求/响应/通知/序列化 round-trip)；hook-protocol matcher/codec/merge 已覆盖 #56，本任务只做分离运行/事件/运行器/类型深化；钩子运行器命令执行结果以纯字典形状承载(成功/失败/输出/退出码) | 宿主面剔除；已覆盖域不重复 | ⚪ 纯逻辑面可用；ACP 会话管理/钩子实际执行待宿主接线 |

## 第16轮（2026-09-13）

| 编号 | 轮次·任务 | 上游源 | 光明模块 | 差异描述 | 原因 | 状态 |
|---|---|---|---|---|---|---|
| R16-D1 | 16·任务1 | subagent/subagent catalog/descriptor/depth/error/assistant-output/inbox | `src/子代理核心.light` | 上游子代理深度计算含「可能除零」的数值归一化(如 `score / total`)，JS 除零产生 Infinity/NaN 可被 isFinite 判定兜底 → 光明除法 `x/0` 直接抛「除零错误」，无法用值语义表达无穷/非数(L-131)，调用点尝试/捕获兜底返回默认值；上游 index.ts(660行cordis+child_process装配面)/client.ts(纯类型重导出并入types)/child-agent.ts/continuation*.ts/control*.ts/invariant.ts/lifecycle.ts/list-children.ts/out-of-process.ts/projection*.ts/types.ts → 光明全部剔除(纯逻辑面只做目录/描述符/深度/错误/输出/收件箱) | L-131 除零抛错非Infinity；宿主面剔除 | ⚪ 纯逻辑面可用；除零语义待语言层支持(L-131) |
| R16-D2 | 16·任务2 | subagent/subagent continuation-messages/control-types/internal/run-settlement + tool-subagent model-selection/state | `src/子代理续传.light` | 上游 model-selection.ts 的 modelRouteKey 用 `provider+'\0'+model`(NUL分隔符)作稳定身份键 → 光明缺「码→字符」方向的 chr 内置(仅有字符转ASCII=ord方向)，无法生成 NUL 分隔符(L-133)，改用 `序列化JSON([provider,model])` 作无碰撞唯一键绕法；上游 tool-subagent/index.ts(707行cordis+defineTool)/invariant.ts/list-models.ts/model-selection-settings.ts(cordis宿主面)/tool-subagent-control(全部cordis+defineTool宿主面) → 光明全部剔除(纯逻辑面只做续传消息/控制类型/内部/运行结算/模型选择/选择状态) | L-133 缺chr内置无法生成NUL分隔符；宿主面剔除 | ⚪ 纯逻辑面可用；chr内置待语言层支持(L-133) |
| R16-D3 | 16·任务3 | api/gateway stream-protocol/remote-error-codes + api/remotes remote-events/types | `src/网关协议.light` | 上游网关协议校验器大量依赖 JS `value.type==='x' && exactKeys(...)` 的「undefined 短路」语义(缺失属性返回undefined不抛) → 光明字典缺失键访问 `值["缺失键"]` 直接抛 KeyError(L-135)，所有可选字段访问改用 `字典获取(值,键,空)` 安全取值绕法；上游 gateway/index.ts(1204行cordis+node)/stream-server.ts(node)/types.ts(cordis宿主面)/remotes/index.ts(cordis+node宿主面) → 光明全部剔除(纯逻辑面只做流协议407行/远程错误码/远程事件/类型) | L-135 字典缺失键抛KeyError非undefined短路；宿主面剔除 | ⚪ 纯逻辑面可用；缺失键语义待语言层支持(L-135) |
| R16-D4 | 16·任务4 | api/session-controller types(611行大文件)/assistant-stream/remote-events | `src/会话控制类型.light` | 上游会话控制器类型含「尝试态」「尝试记录」等命名(变量名以关键字「尝试」开头) → 光明词法分析器在标识符与关键字边界处理不严谨，变量名以关键字开头时后续下标赋值/读取在关键字边界被断开(L-137，与L-120/L-101同族)，变量名完全避开「尝试」等关键字字样绕法(改「活动项」「运行项」「待办态」)；上游 session-controller 其余12文件(agent/catalog/commands/control/file-references/history/index/list/media-references/model-selection-projection/skill-catalog，全部cordis/node宿主面)+client/子目录 → 光明全部剔除(纯逻辑面只做types 611行大文件/assistant-stream/remote-events) | L-137 变量名以关键字开头被断开；宿主面剔除 | ⚪ 纯逻辑面可用；词法边界待语言层修复(L-137) |
| R16-D5 | 16·任务5 | plan/plan-mode types+client + todo/tool-todo types+client + terminal/terminal types | `src/小域类型.light` | 上游 plan-mode/client.ts(10行纯类型重导出 `export type * from './types'`)/tool-todo/client.ts(同) → 光明内容并入对应 types，不单独成文件；上游 plan/index.ts(464行cordis+defineTool)/invariant.ts(cordis不变量安装器)/todo/index.ts(223行cordis+defineTool)/invariant.ts(cordis)/terminal/index.ts(476行cordis)/terminal-bash(全部node/cordis宿主面：config/index/sanitize/session) → 光明全部剔除(纯逻辑面只做三域types：计划模式PlanMode枚举/计划状态/配置/步骤类型 + 待办TodoItem形状/状态转换/优先级/列表过滤 + 终端TerminalSession形状/状态/输出格式/尺寸/光标位置/事件)；三域类型同模块命名空间隔离 | 小域纯逻辑文件少(每域1-2个)合并成任务；宿主面剔除 | ✅ 纯逻辑面可用；三域类型合并无冲突 |
| R16-D6 | 16·任务6 | api/workspace-files types + api/settings-controller types + api/workspace-controller types | `src/控制器类型.light` | 上游 workspace-files/types.ts(166行)/settings-controller/types.ts(39行)/workspace-controller/types.ts(128行)均为纯逻辑类型定义 → 光明合并成单模块，三域类型命名空间隔离；上游各控制器 index.ts/commands.ts/feed.ts/changes.ts/credentials.ts/directory-picker.ts(全部cordis/node宿主面)/runtime-diagnostics/invariants/index.ts(cordis不变量安装器宿主面) → 光明全部剔除(纯逻辑面只做三域types：工作区文件FileChange形状/变更类型/文件快照/列表过滤/变更事件 + 设置控制器Settings形状/设置键/值/变更事件/凭证类型 + 工作区控制器Workspace形状/状态/目录选择/事件/Feed类型) | 小域纯逻辑文件少(每域1个types)合并成任务；宿主面剔除 | ✅ 纯逻辑面可用；三域类型合并无冲突 |

## 第17轮（2026-09-13，宿主接线原型方向）

| 编号 | 轮次·任务 | 上游源 | 光明模块 | 差异描述 | 原因 | 状态 |
|---|---|---|---|---|---|---|
| R17-D1 | 17·任务1 | Cordis Context/Service/插件生命周期模式（原型设计，非逐行复刻） | `src/宿主运行时.light` | 上游 Cordis 插件基于 TypeScript 类继承+装饰器+依赖注入容器，光明无类继承/装饰器/DI容器，原型用「字典形状+段落函数」表达插件接口(名称/版本/依赖/提供服务/监听事件/安装启动停止卸载钩子)，插件加载器用拓扑排序段落函数实现，服务注册表用字典<服务名,实例>承载；上游 Cordis 运行时由 Node 进程承载，光明原型为内存模拟 | 光明无类继承/装饰器/DI容器；原型设计非逐行复刻 | ✅ 原型可用；插件生命周期+服务注册表内存模拟 |
| R17-D2 | 17·任务2 | Cordis Context类型增强(declare module interface Context)+上游 scope 包作用域过滤(@dshScopeScan) | `src/宿主上下文.light` | 上游 Cordis Context 用 TypeScript 声明合并(declare module)做类型增强，光明无声明合并，原型用「属性字典+父上下文引用」表达Context继承链(子上下文覆盖父上下文/逐级向上查找)；上游 scope 用 TypeScript 泛型 Scoped<T>+编译期扫描，光明原型用「作用域字典+作用域匹配段落函数」实现运行时作用域过滤(全局匹配所有/同类型同目标匹配/父作用域匹配)；`作用域`是保留关键字(L-143)变量名改域标记绕法 | 光明无声明合并/泛型/编译期扫描；L-143作用域保留关键字 | ✅ 原型可用；Context继承链+作用域过滤运行时实现 |
| R17-D3 | 17·任务3 | Cordis Events声明(declare module interface Events + @mode emit/waterfall)+上游 session/created 等事件scope-filtered dispatch | `src/宿主事件.light` | 上游 Cordis Events 用 TypeScript 声明合并做事件类型声明(参数形状/返回值形状/模式emit-waterfall)，光明无声明合并，原型用「事件类型注册表字典」存储事件类型定义(事件名/模式/参数形状/返回值形状/作用域类型/描述)；上游事件绑定由 Cordis 框架自动扫描插件方法，光明原型用「绑定事件段落函数」显式绑定(插件名+事件声明+监听器)，插件停止时自动解绑；作用域过滤复用宿主上下文的作用域匹配段落函数；错误隔离用try-catch包裹每个监听器(光明尝试/捕获) | 光明无声明合并/框架自动扫描；原型设计非逐行复刻 | ✅ 原型可用；基于已有事件.light的接线层，emit/waterfall双模式 |
| R17-D4 | 17·任务4 | 上游 defineTool 模式(name/description/parameters/execute)+core/tools 工具注册表+agent-loop/tool-calls 工具调用处理 | `src/宿主工具.light` | 上游 defineTool 是 Cordis 装饰器+Context注入，光明无装饰器，原型用「工具声明字典+注册工具段落函数」表达工具定义(工具名/描述/参数模式/执行函数/可见性/分组)；上游工具执行由 Cordis Context 自动注入，光明原型用「造执行上下文段落函数」显式创建执行上下文(运行时引用/上下文/会话ID/作用域/取消令牌/调用者/请求ID)；上游工具权限由 Cordis 中间件链处理，光明原型用「检查调用权限段落函数」实现可见性检查(public/internal/private)+调用审计日志+速率限制；工具名冲突用命名空间前缀(插件名/工具名)；基于已有工具.light只读导入校验参数，不修改 | 光明无装饰器/Context自动注入/中间件链；原型设计非逐行复刻 | ✅ 原型可用；基于已有工具.light的接线层，完整调用协议 |
| R17-D5 | 17·任务5 | Node.js fs/child_process/net/stream API+上游 fs/code-runtime/subagent 包IO抽象 | `src/宿主IO.light` | 上游 IO 基于 Node.js 内置模块(fs/path/child_process/net/stream)，光明无 Node 内置模块，原型用「段落函数接口+内存模拟实现」表达IO抽象层：文件系统提供者接口(读/写/列目录/存在/删除/重命名/创建目录/文件信息)用字典<路径,文件内容>内存模拟；进程提供者接口(启动/等待/终止/列举/stdio/退出码)用字典<进程ID,进程句柄>内存模拟；IO结果统一形状(成功/失败/错误码表)；IO取消令牌(创建/标记取消/监听/校验)；IO超时与重试(重试策略/退避延迟/带重试执行)；路径操作(拼接/规范化/父目录/文件名/扩展名/绝对路径)；初判模块级函数无闭包经探针隔离实为标识符笔误(参数中文数据函数体误写英文data)，修正后append正常生效，非语言缺陷不登记 | 光明无 Node 内置模块；原型设计非逐行复刻 | ✅ 原型可用；IO抽象层接口+内存模拟实现，实际IO待宿主注入 |
| R17-D6 | 17·任务6 | 上游 settings/credentials/boot/settings-controller 包配置/凭证/启动 | `src/宿主配置.light` | 上游设置基于 Cordis 服务+schemastery 模式解析，光明无 Cordis 服务/schemastery，原型用「配置项字典+配置注册表」表达配置定义(键/类型/默认值/校验函数/描述/来源/可变性/命名空间/秘密标记/分组)；上游配置加载由 Cordis 配置服务+环境变量+配置文件多层合并，光明原型用「加载全部段落函数」实现多层合并(命令行>环境变量>配置文件>默认值)，环境变量名到配置键映射+值类型转换，配置文件键值对解析，命令行--键=值解析；上游凭证由 CredentialProvider 抽象+四层存储，光明原型用「凭证提供者接口字典+解析凭证段落函数」实现，凭证脱敏复用设置.light脱敏值；配置变更事件(变更前可否决/变更后/重置/监听器/变更历史)；配置验证器(验证全部/验证单个/启动时验证)；基于已有设置.light/凭据.light只读导入分层合并/脱敏值/凭证引用，不修改 | 光明无 Cordis 服务/schemastery；原型设计非逐行复刻 | ✅ 原型可用；基于已有设置/凭据/启动配置的接线框架 |


---

## R18 第18轮（缺陷修复轮，2026-09-13）

### R18-D1 字典增强工具库与上游差异
- **差异**：上游 TypeScript 字典/对象操作原生支持可选链(`?.`)、空值合并(`??`)、展开运算符(`...`)、`Object.keys/values/entries`、`structuredClone` 等；光明语言通过 `字典增强` 工具库提供等价功能（字典获取/深度设置/键规范化/字典深拷贝等），但调用方式为函数式而非语法级。
- **影响**：代码可读性略低于上游语法糖，但功能等价。
- **绕法**：使用 `字典增强` 工具库函数。

### R18-D2 数值兼容工具库与上游差异
- **差异**：上游 JS/TS 数值语义为 IEEE 754 双精度浮点，`Math.round` 为 half-up，`0/0` 为 NaN、`1/0` 为 Infinity，`Boolean([])` 为 true；光明语言数值语义沿用 Python（银行家舍入、除零抛错、空列表判假），`数值兼容` 工具库提供 JS 语义对齐函数。
- **影响**：需要 JS 语义的场景（如与上游行为对拍）必须使用 `数值兼容` 函数，不能直接用光明内置运算符。
- **绕法**：使用 `取整半数上`/`安全除`/`JS真值`/`JS相等` 等函数。

### R18-D3 错误增强工具库与上游差异
- **差异**：上游 TS 错误处理支持 `throw new Error(message)` / `class CustomError extends Error` / `catch (e) { e.code }` / 错误链 `cause`；光明语言 `抛出` 字典会丢失属性（L-099），`捕获 错误 as e` 不支持（L-088），`错误增强` 工具库提供结构化错误的构造/抛出/解析 round-trip。
- **影响**：错误信息传递必须使用 `错误增强` 工具库的格式化字符串协议，不能直接抛出字典。
- **绕法**：使用 `造结构化错误`/`抛出结构化错误`/`解析错误消息`/`获取最后异常` 等函数。

### R18-D4 字符串增强工具库与上游差异
- **差异**：上游 JS 字符串支持 `String.fromCharCode` / 模板字面量（反引号）/ `RegExp` 构造函数（含花括号量词）/ `padStart/padEnd` / `repeat`；光明语言缺 `chr` 方向（L-133）、字符串插值破坏 JSON 字面量（L-089）、正则花括号量词不可用（L-086），`字符串增强` 工具库提供等价功能。
- **影响**：字符码转换、JSON 构造、正则匹配必须使用 `字符串增强` 函数。
- **绕法**：使用 `码转字符`/`构造JSON字符串`/`转义花括号`/`安全正则匹配` 等函数。

### R18-D5 函数增强工具库与上游差异
- **差异**：上游 JS/TS 支持默认参数值、箭头函数闭包（正确捕获外层变量）、`Function.prototype.bind`、柯里化、函数组合、`_.memoize`；光明语言不支持默认参数（L-102）、闭包修改外层变量不生效（L-098），`函数增强` 工具库提供字典参数协议的默认参数包装和字典状态承载的闭包包装。
- **影响**：`带默认参数` 的目标函数必须接收字典参数而非多位置参数；闭包状态必须用字典承载。
- **绕法**：使用 `带默认参数`/`闭包包装`/`造计数器`/`管道` 等函数。

### R18-D6 词法安全工具库与上游差异
- **差异**：上游 TS/JS 标识符规则为 ASCII 字母/数字/下划线/$，无中文关键字切碎问题；光明语言中文关键字/保留字/L0单字别名（49字）导致大量中文标识符被词法切碎（L-084/L-092/L-119/L-120/L-137/L-143），`词法安全` 工具库提供保留字检查和安全标识符生成。
- **影响**：中文变量名/函数名必须先用 `是保留字`/`安全标识符` 检查，避免运行时词法错误。单字别名49字导致 `是保留字` 对多数中文标识符返回真（保守判定）。
- **绕法**：使用 `保留字表`/`是保留字`/`安全标识符`/`标识符可切分检查` 等函数，或使用英文标识符。

## R19 第19轮（编译器缺陷修复轮，2026-09-14）

### R19-D1 词法分析器最大匹配策略与上游差异
- **差异**：上游 TypeScript/JavaScript 词法分析器基于 ASCII 标识符规则，无中文关键字切碎问题；光明语言中文关键字/保留字/L0单字别名导致标识符含关键字子串时被词法切碎（L-084/L-092/L-137/L-119/L-120）。第19轮修复后 lexer 采用最大匹配+上下文感知策略，标识符含关键字子串不再被切碎。
- **影响**：中文变量名/函数名（如 `为字`/`返回表`/`尝试开头`）可正常使用，不再报解析错误。
- **绕法**：修复后无需绕法；极端边缘 case 仍可用 `词法安全` 工具库的 `安全标识符` 检查。

### R19-D2 保留字评估与上游差异
- **差异**：上游 JS/TS 保留字（`class`/`function`/`return`等）不可作变量名；光明语言 `回调`/`作用域` 评估为合规保留字（分别用于 `外部 回调` FFI语法和 `异步作用域` 语法），parser硬消费，移除会破坏合法语法。误用时报明确的保留字提示（非静默失败）。
- **影响**：`回调`/`作用域` 不可作变量名，但错误提示明确（「回调是保留关键字，不能直接作为语句开头」）。
- **绕法**：使用 `回调处理器`/`作用域表` 等前缀整体标识符（保留字按整token判定，非子串误切）。

### R19-D3 异常位置锚定与上游差异
- **差异**：上游 JS/TS 异常 stack trace 天然包含跨模块调用链和源文件位置；光明语言第19轮修复后，跨模块未捕获异常的位置块指向载体模块源文件:行（而非入口文件），单模块异常仍正确归因于自身。
- **影响**：跨模块异常定位更精准，可直接跳转到载体模块的出错行。
- **绕法**：修复后无需绕法；L-088（捕获错误as变量不支持）仍需用 `捕获 错误:` 不绑定变量。

### R19-D4 字典属性访问与上游差异
- **差异**：上游 JS/TS 对象属性访问 `obj.key` 原生支持（含 getter/setter）；光明语言第19轮修复后，`对象.键` 发射 `_light_attr_get(对象, '键')`，`己.标识 为 值` 发射 `_light_attr_set(self, '标识', 值)`，方法调用保持原生发射。
- **影响**：字典/对象可通过 `.键` 语法访问属性（等价于 `字典["键"]`），类实例属性赋值走 `_light_attr_set`。
- **绕法**：修复后无需绕法；仍可用 `字典["键"]` 方括号访问。

### R19-D5 HMAC标准实现与上游差异
- **差异**：上游 Node.js `crypto.createHmac` 遵循 RFC 2104 标准（双轮哈希：inner/outer pad）；光明语言第19轮修复前 HMAC 为单轮 PBKDF2 近似实现（非标准），修复后改为标准 RFC 2104 双轮哈希实现。
- **影响**：HMAC 输出与上游/标准库一致，可用于跨系统签名验证。
- **绕法**：修复后无需绕法；旧版单轮实现的输出已废弃，需重新生成签名。

### R19-D6 D批次验收与上游差异
- **差异**：D批次（L-068~L-074，2026-09-09外发）7个缺陷在第19轮验收中全部确认已修复或已有内置支持：类方法边界（L-068）、序列化JSON布尔（L-069）、默认参数字面量（L-070）、字典.get原语（L-071）、转字符串字典（L-072）、运行时错误翻译（L-073）、类型判断内置（L-074）。
- **影响**：D批次反馈的7个问题全部闭环，Web服务端开发不再受这些缺陷影响。
- **绕法**：全部已修复，无需绕法；L-073顶层错误提示已翻译为光明层文本（「字典缺少键 '乙'（文件:行）」+建议）。

## R20 第20轮（编译器回归修复轮，2026-09-14）

### R20-D1 词法最大匹配「收敛化」与上游差异
- **差异**：上游TS/JS词法器按ASCII标识符规则切词，不存在中文关键字切词歧义。光明语言因中文无空格，
  必须靠「最大匹配vs关键字边界」启发式；第20轮把该策略**收敛**为
  「`为/返回/尝试`三关键字在赋值/返回语境精确切分 + 函数调用语境（整串后紧随`(`）绝不切分」，
  并明确**拒绝**把全部非运算符关键字泛化合并（实测会引发104个解析回归：`接收参数`/`如果条件`等
  合法「关键字+名」结构被误并）。
- **影响**：`设 合并为 {}` / `设 甲为空` 等无空格赋值尾巴可用；`断言为真(...)`/`判定为空(...)` 等
  含关键字尾的函数名不被切碎；`去重占位`/`作用域匹配` 依赖`_scan_user_definitions`预扫描整体还原
  （**仅在名称有定义登记时成立**）。
- **绕法**：正常写法即可；**嵌套段落形参**等无预扫描覆盖的位置仍须避开关键字前缀（见L-152）。

### R20-D2 HMAC标准实现与上游差异（订正R19-D5）
- **差异**：上游Node.js `crypto.createHmac`遵循RFC 2104标准；光明语言stdlib `哈希.py` 的
  `HMAC_SHA256` 已在第19轮任务4改为Python标准库 `hmac.new(key, msg, hashlib.sha256).hexdigest()`，
  第20轮任务2以7组对拍（标准2+边界5：空密钥/空消息/长密钥>64B/中文）逐字节复核确认与标准一致。
- **影响**：HMAC输出可用于跨系统签名验证，与上游/标准库完全一致。
- **订正**：R19-D5记载的「双轮哈希后对拍仍不一致」系第19轮收口误判——观察值 `5b94b938…` 是
  修复前单轮PBKDF2输出，最终提交（light-merge `49319306`）已是标准实现；收口时未重跑验证即登记
  EXPECT_RED。第20轮已移除该EXPECT_RED登记。
- **绕法**：无需绕法；`.light`语言层仍缺「原始字节哈希」原语，HMAC只能通过stdlib `HMAC_SHA256` 调用。

### R20-D3 嵌套段落形参关键字前缀限制（新增L-152）
- **差异**：上游JS/TS函数参数名无关键字限制（`function f(function) {}` 合法）；光明语言嵌套段落
  （段落体内定义段落）的形参名若以关键字开头（如 `函数值`/`返回值`/`尝试结果`），因预扫描
  `_scan_user_definitions` 只覆盖模块级名称，不覆盖嵌套段落形参，导致形参名被切碎为关键字+标识符，
  运行期 `name '值' is not defined`。
- **影响**：嵌套段落的形参名必须避开关键字前缀（用 `算子`/`回调函数`/`结果值` 等安全名）；
  模块级段落形参无此限制（预扫描覆盖）。
- **绕法**：嵌套段落形参改用非关键字前缀名称（如 `算子` 代替 `函数值`）；或将嵌套段落提升为模块级。
- **状态**：已知未修复（L-152），待后续「词法确定性切词重构」轮次处理。

## R21 第21轮（词法确定性切词重构轮，2026-09-14）

### R21-D1 预扫描基于缩进/嵌套层级与上游差异
- **差异**：上游 deepseek-harness（TS/JS）标识符以 ASCII 构成、无关键字粘连问题，名称收集无需预扫描；光明语言标识符与关键字共用汉字字符集，名称收集依赖 `_scan_user_definitions` 预扫描。第21轮将其从线性文本扫描重构为基于缩进/嵌套层级的结构化收集（段落定义行按缩进级别建层级，段名/形参名/块内设名按层级正确收集）。
- **影响**：嵌套段落形参名、块内设名进入 definitions；预扫描结果为重构前超集（只增不减）。
- **上游对齐**：语义上对齐上游「标识符即名称」的无限制行为；实现路径为光明特有。

### R21-D2 词法上下文敏感切词与上游差异（订正R20-D1）
- **差异**：上游 ASCII 词法以空白/符号为天然边界；光明汉字串与关键字共享字符集，第21轮引入上下文敏感策略：非语句起始位置（表达式上下文）优先最大匹配整体成 IDENTIFIER，仅整串精确等于关键字时作 KEYWORD；语句起始位置切分逻辑不变；运算符关键字（与/或/非/等于/大于/小于等）在表达式上下文仍按关键字切分。
- **影响**：`去重占位(名单)`/`作用域匹配(...)`/`断言为真(...)` 等表达式形态不再依赖预扫描兜底；减少对 COMMON_COMPOUND_WORDS 等保护表的依赖（见 R21-D3）。
- **订正 R20-D1**：第20轮的「收敛化最大匹配」（`_EMBED_MAX_MATCH_KEYWORDS` 三字集合`为`/`返回`/`尝试`）是本策略的过渡形态，第21轮已由「非语句起始位置最大匹配」通用策略替代，该集合的作用范围由新策略覆盖。第19轮T1反跑C判据（变异立红）因新策略覆盖而不再成立，属预期变化。

### R21-D3 保护表精简与上游差异
- **差异**：上游无此类保护表；光明第21轮对 COMMON_COMPOUND_WORDS 保护表完成审计与精简：从216条精简至32条TierB护栏，删除211条（对全语料763个.light文件token序列零影响，逐条隔离中立验证违规0条）。其余保护表（IDENTIFIER_SAFE_KEYWORDS、_COMPOUND_SAFE_SINGLE_KEYWORDS等）完成审计，分类为A类（新策略已覆盖可删）/B类（部分覆盖精简）/C类（必须保留），待后续轮次逐步精简。
- **影响**：词法维护面收敛，减少逐词白名单生长；精简后全量CI无回归，性能+0.1%（无显著下降）。

## R22 第22轮（保护表深度精简轮，2026-09-14）

### R22-D1 保护表深度精简与上游差异（订正R21-D3）
- **差异**：上游 deepseek-harness 无任何汉字保护表；光明第22轮对保护表完成逐条隔离中立验证精简（判据③：撤掉条目后全语料763个.light文件token序列零变化则冗余）：
  - IDENTIFIER_SAFE_KEYWORDS：17→5条（删 包含/匹配/回调/外部/排序/接口/枚举/结构体/联合体/段落/返回/配 共12条）；
  - _TRAILING_ALIAS_MERGE：10→1条（删 遍/捕/抛/终/返/设/承/宏/掷 共9条，仅保留 `己`，依赖 test_L120.light）；
  - COMMON_COMPOUND_WORDS：216→184条（R21保留的32条TierB护栏经重估**全部冗余**，第21轮任务2上下文敏感切词已覆盖其保护场景，含第21轮正向控制 `弹性模量`）；
  - 合计删除53条。IDENTIFIER_SAFE_SUFFIX_ONLY_KEYWORDS 3条全保留（`标准库` 凭单测契约保留：语料中立但 tests/unit/test_lexer.py 断言 `我的标准库` 词中合并）。
- **订正 R21-D3**：R21-D3「从216条精简至32条TierB护栏」表述不准确——216为精简后表内总条数，32条TierB是其中「待重估护栏」标记而非表剩余条数；第22轮重估后表为184条。R21-D3「其余保护表分类C类（必要）」由本轮判据③推翻：IDENTIFIER_SAFE/_TRAILING_ALIAS_MERGE 大部分条目实为冗余。
- **影响**：词法维护面进一步收敛（逐词白名单从531条量级降至约240条）；与上游「无保护表」的差异缩小；精简后全语料token零变化、全量CI无回归。
- **上游对齐**：逐步消除光明词法特有的逐词白名单，向上游「无保护表」形态靠拢；最终路径见 lightharness/_task6_R22_保护表通用化策略建议.md。

### R22-D2 _EMBED_MAX_MATCH_KEYWORDS 保留判定与上游差异（回填任务1结论）
- **差异**：`_EMBED_MAX_MATCH_KEYWORDS`（`为`/`返回`/`尝试`，第19轮窄集精确切分）经任务1等价置空法+全语料770文件判据③验证：**非冗余，保留**（2026-09-14）。`行为`/`返回表`/`尝试记录` 等「纯嵌入」（词首非关键字、整串非关键字）场景有 17 文件依赖，撤掉即被切碎；该场景未被 R21「非语句起始位置最大匹配」块覆盖。
- **订正 R21-D2**：第21轮「该集合的作用范围由新策略覆盖、第19轮T1反跑C判据（变异立红）不再成立属预期变化」系**窄点验证误判**——L-084 单点依赖的是词尾 `为` 的 (b) 值上下文守卫（已被 R21 覆盖故单点变绿），纯嵌入场景并未被覆盖。第19轮T1反跑C判据失效是「窄点验证通过、全量验证失败」的误判实例，非预期变化。
- **状态**：已完成（保留）；证据 lightharness/_task1_R22_嵌入关键字冗余评估_交付报告.md + _antirun_r22_t1_嵌入关键字移除.py。

## R23 第23轮（保护表通用化替代轮，2026-09-14）

### R23-D1 保护表通用化替代与上游差异（阶段A+B+C完成）
- **差异**：上游 deepseek-harness 无任何汉字保护表；光明第23轮完成保护表通用化替代三阶段：
  - **阶段A**：_TRAILING_ALIAS_MERGE 整表删除（1→0），己词尾并入由第21轮上下文敏感切词通用规则兜底（自己/错误己/记己等场景），test_L120.light rc=0；
  - **阶段B**：IDENTIFIER_SAFE_KEYWORDS（5条：函数/打印/标准库/模块/输出）+ IDENTIFIER_SAFE_SUFFIX_ONLY_KEYWORDS（3条：打印/标准库/模块）整表删除（8→0），引入后缀位置上下文规则（scan_pos>0 + 多字 + 非运算符 + 非排除集 → 并入标识符）和排除集 _P0A_SUFFIX_SPLIT_KW（硬语句头如果/那么/否则/段落/类型/捕获/等待/定义在词中/词尾保持切分），全语料765文件token零变化；
  - **阶段C（部分）**：COMMON_COMPOUND_WORDS 184→37条（删147，79.9%），60条有codegen映射者迁移到code_generator内建登记，87条冗余护栏由上下文规则覆盖，剩余37条真护栏（位与/位异或/低级关闭/异步读取文件/并发等待/常量时间比较等含运算符前缀或codegen依赖的复合名），codegen产物sha256两侧完全一致。
- **累计成果**：COMMON_COMPOUND_WORDS 从第20轮427条 → 第21轮216条 → 第22轮184条 → 第23轮37条，三轮累计删390条（91.3%）；逐词白名单从第20轮约530条量级降至约73条（CCW 37 + _COMPOUND_SAFE_SINGLE 30 + _EMBED 3 + OPERATOR_VERBS 19派生，其中_COMPOUND_SAFE_SINGLE为阶段D待处理）。
- **性能**：全语料词法分析中位耗时 11.66s → 9.63s，**提升21%**（保护表变小+上下文规则热路径优化）。
- **订正 R22-D1**：R22-D1「逐词白名单从531条量级降至约240条」已过时——第23轮阶段A/B/C完成后降至约73条（不含_COMPOUND_SAFE_SINGLE阶段D）。
- **上游对齐**：与上游「无保护表」形态的差异大幅缩小；剩余_COMPOUND_SAFE_SINGLE_KEYWORDS（30条单字）为阶段D，须先解决的递归吞分隔符等已知独立缺陷，单独立轮处理。

### R23-D2 后缀位置上下文规则与上游差异（新增规则）
- **差异**：上游 ASCII 词法以空白/符号为天然边界，关键字与标识符无字符集重叠；光明汉字串与关键字共享字符集，第23轮引入后缀位置上下文规则作为IDENTIFIER_SAFE保护表的通用替代：
  - **规则**：多字、非运算符（∉_P0A_OP）、非排除集（∉_P0A_SUFFIX_SPLIT_KW）的关键字位于汉字词词中/词尾（scan_pos>0）→ 并入标识符；
  - **词首语义**：语句关键字优先（scan_pos=0时保持关键字切分），这是SUFFIX_ONLY「词首必须作关键字」的通用化；
  - **排除集**：_P0A_SUFFIX_SPLIT_KW = (_P0A_HARD_STMT - {'函数'}) | {'定义'}（如果/那么/否则/否则如果/段落/类型/捕获/等待/定义）——这些硬语句头在词中/词尾必须保持既有切分，由单测/语料实证（如自定义表的定义需保持切分否则AST变化）。
- **影响**：处理函数/输出格式/我的标准库/学生模块/可打印等后缀位置复合词不再依赖逐词白名单，由通用规则覆盖；词首位置的打印 消息/模块 名/标准库 路径仍正确识别为关键字。
- **上游对齐**：语义上对齐上游「标识符即名称」的无限制行为；实现路径为光明特有（汉字字符集与关键字重叠导致需要上下文判定）。

## R24 第24轮（阶段D单字通用化轮，2026-09-14）

### R24-D1 单字保护表通用化与上游差异（阶段D部分完成，CS保持30条）
- **差异**：上游 deepseek-harness 无任何汉字保护表；光明第24轮完成阶段D单字通用化的部分工作：
  - **之成员访问符被吞缺陷修复**（L-153）：第21轮「非语句起始位置关键字前缀标识符整体成词」规则未排除之成员访问符，自之成绩/自之取平均分被整体吞为IDENTIFIER导致NameError。新增_p0a_contains_sep helper+之守卫修复。
  - **CR1通用规则**（_seg_after_sep游标）：接管13条单字的「成员访问段首」保护场景（甲之<c>顶形态），语料冗余但语义非冗余的单字由通用规则兜底。
  - **死代码清理**：删除_P0A_COMPOUND_SAFE = _COMPOUND_SAFE_SINGLE_KEYWORDS | {'当'}（全库git grep零引用，已被上下文规则取代）。
  - **递归语义类别代数化**：_match_keyword/_skip_compound_safe_and_match的递归语义用类别代数+等价断言落地（防漂移机制），运行路径零改动，行为与R23基线逐字节一致。
- **关键发现**：单字保护表与多字保护表本质不同——多字表大部分是上下文规则缺位的补丁（第22轮TierB 32条全冗余），单字表80%是真护栏（24/30条），其中词尾并入17条（属性名/类名/调用名末字）、词中并入6条（递归副作用承重墙）、词首并入1条（模模拟客户端）。仅6条语料冗余（20%），且其中4条属运算符/值字面量守卫管辖。**结论：CS保持30条，语义层非冗余不可简单删除**，需更深度的递归语义规则化才能替代。
- **175条自由名保护验证**：去除空格/10的幂/索引/种类/阶乘/乘阶乘/加斐波那契等175条自由名在通用化后仍整体成标识符，两侧一致。
- **性能**：全语料词法分析中位耗时6.58s→6.00s，**提升9.8%**（×1.098）。
- **订正 R23-D1**：R23-D1「剩余_COMPOUND_SAFE_SINGLE_KEYWORDS（30条单字）为阶段D，须先解决的递归吞分隔符等已知独立缺陷」需订正——的递归吞分隔符经审计不可复现（deterministic=True路径全部正确），真实缺陷是之成员访问符被吞（L-153，已修复）。单字表保持30条的原因是语义层非冗余，而非已知缺陷阻塞。
- **上游对齐**：与上游「无保护表」形态的差异仍存在（CS 30条+CCW 37条+_EMBED 3条），但单字表的通用化路径已明确（递归语义深度规则化），CR1规则和类别代数化为后续工作奠定基础。

### R24-D2 之成员访问符守卫与上游差异（新增规则）
- **差异**：上游 ASCII 词法以.为成员访问符，无字符集重叠；光明汉字串中之既是成员访问符关键字，又可作标识符组成部分（之前/之后/之所以）。第24轮新增之守卫：非语句起始位置整体成词规则扫描串内是否存在真实_P0A_SEP命中（_p0a_contains_sep helper，用_match_keyword逐位判），串内含之时不整体合并，保持之作为成员访问符切分。
- **影响**：自之成绩/自之取平均分等含之的复合标识符正确切分为自+之+成绩，成员访问语义恢复；之前/之后等之作词首的词不受影响（之在词首时按关键字优先切分）。
- **上游对齐**：语义上对齐上游「成员访问符始终切分」的行为；实现路径为光明特有（汉字字符集与关键字重叠导致需要上下文判定）。

## R25 第25轮（CS词尾并入正面规则化+thinking数据集，2026-09-15）

### R25-D1 CS词尾并入正面规则化与上游差异（CS保持30条，退化为词首并入锚）
- **差异**：上游 deepseek-harness 无汉字保护表；光明第25轮将单字词尾并入从「对CS取补集（21字）」改为「正面类别F（43字，按五类语义排除推导）」，CS表（_COMPOUND_SAFE_SINGLE_KEYWORDS）退化为纯粹的「词首并入锚」。
- **关键发现**：任务2对18字词尾并入候选逐条验证（G1语料+G2词首编译门+G3边界门三重判据），结论**0字可删**——正面规则F只覆盖词尾（scan_pos>0）并入，未覆盖词首（scan_pos=0）并入；18字在词首位置去掉CS后全部浮出为KEYWORD（G2编译门100%失败）。这与第24轮任务3「语料冗余但语义非冗余」结论一致，本轮用编译级证据坐实。
- **新增规则**：_TRAILING_ALIAS_CLASS = F（43字正面类别，按五类语义排除推导）+ _P0A_SUFFIX_SPLIT_SINGLE（单字排除集，五类共21字：运算符/值字面量/控制流/范围/结构助词）。
- **CS表现状**：保持30条，职责从「词首+词尾并入」收窄为「仅词首并入锚」。词尾并入已由正面类别F接管，不再依赖CS补集。
- **验证**：全语料837文件token零变化；2个.light rc=0；pytest 43 passed（18字正向+反向边界+混合场景+保护表形态断言）。
- **订正 R24-D1**：R24-D1「词尾并入17条可由单字后缀规则覆盖」需订正——词尾并入确实可由正面规则F覆盖，但CS表不能因此删除，因为CS还承担词首并入锚的职责（18字词首G2编译门100%失败）。CS表的完整通用化需分别解决词首并入（更难）和词尾并入（已解决）。

### R25-D2 单字后缀位置正面规则与上游差异（新增规则）
- **差异**：上游 ASCII 词法以空白/符号为天然边界；光明汉字串与关键字共享字符集，第25轮新增单字后缀位置正面规则：
  - **规则**：scan_pos>0（词中/词尾）+ 单字关键字 ∈ 正面类别F（43字，非运算符/非值字面量/非控制流/非范围/非结构助词）→ 并入标识符；
  - **排除集**：_P0A_SUFFIX_SPLIT_SINGLE（五类共21字，按语义类别整体排除，不逐字补白名单）；
  - **词首语义**：scan_pos=0时保持关键字优先切分（CS表作为词首并入锚）。
- **影响**：标准输出/有界队列类/成绩的长度等词尾并入场景不再依赖CS补集，由正面类别F直接覆盖；词首位置的出错/列数/规则等仍由CS表保护为整体标识符。
- **上游对齐**：语义上对齐上游「标识符即名称」的无限制行为；实现路径为光明特有（汉字字符集与关键字重叠导致需要上下文判定+正面类别）。

### R25-D3 光明语言Thinking微调数据集（工程产物，非行为差异）
- **产物**：从lightharness项目（stdlib 86文件+src 152文件+examples 352文件）抽取3032段代码，去重过滤后2500段，生成thinking推理链后最终2200条高质量样本。
- **规格**：JSONL格式，8字段（instruction/input/output/thinking/function_name/source_file/source_type/line_count）；平均thinking 401字；7种instruction类型；代码段平均29.7行。
- **用途**：大模型微调（支持thinking模式的模型），训练光明语言代码生成与推理能力。

## R26 第26轮（CS词首并入通用化，2026-09-15）

### R26-D1 CS词首并入正面规则与上游差异（_P0A_HEAD_MERGE_SINGLE，CS表30→16）
- **差异**：上游 deepseek-harness 无汉字保护表；光明第26轮实现词首并入正面规则_P0A_HEAD_MERGE_SINGLE（22字，F−排除集），CS表（_COMPOUND_SAFE_SINGLE_KEYWORDS）从30条缩减为16条。
- **规则**：单字关键字在词首位置（scan_pos=0）+ 后随汉字（_is_han）+ ∈正面类别（非运算符/非值字面量/非范围步长/非await）→ 并入标识符。
- **决定性站点**：第一层skip_verb（lexer.py:2553），而非_match_keyword（:1227）。_match_keyword的CS递归保留为配套保护（"最内层CS单字回报"语义被去除空格等stdlib函数名依赖）。
- **嵌入式扫描三处同步**：B/C/D三处（lexer.py:2745/2829/2982）逐字同判据（scan_pos==0 + 后随汉字 + _P0A_HEAD_MERGE_SINGLE），保证探测循环与输出循环一致性。
- **CS表删除14字**：余/例/出/则/常/引/接/断/末/试/跳/过/长/首（G1∧G2∧G3三重判据全通过）。其中断/过是R26增量可删（R25基线G1打红，正面规则落地后G1归零）。
- **CS表保留16字**：乘/减/列/到/加/对/是/模/段/的/真/空/类/自/配/除。分类：运算符5字（乘/减/加/除/模，∉正面类别，词首后随汉字须并入）、值字面量2字（真/空，∉正面类别）、特殊语义9字（各有独立保留理由）。
- **验证**：全语料843文件token零变化；3个.light rc=0；pytest 167 passed（含保护表形态断言CS=16）。
- **订正 R25-D1**：R25-D1「CS表完整通用化需分两步：词尾并入（已解决）+ 词首并入（更难）」——词首并入已部分解决（正面规则覆盖22字，CS表30→16），但运算符/值字面量的词首并入仍需CS表作为锚（16字保留），CS表完整清零需后续轮次。

### R26-D2 词首并入规则与词尾并入规则的对称结构（实现路径差异）
- **差异**：上游 ASCII 词法以空白/符号为天然边界，无词首/词尾区分；光明汉字串与关键字共享字符集，第23-26轮逐步建立了词尾并入（R23/R25）和词首并入（R26）的对称规则结构。
- **词尾并入规则**（R23任务1+R25任务1）：_TRAILING_ALIAS_CLASS = F（43字正面类别），scan_pos>0 + 单字关键字 ∈ F → 并入标识符。排除集_P0A_SUFFIX_SPLIT_SINGLE（五类21字）。
- **词首并入规则**（R26任务1+2）：_P0A_HEAD_MERGE_SINGLE = F − _P0A_HEAD_SPLIT_SINGLE（22字），scan_pos==0 + 后随汉字 + ∈正面类别 → 并入标识符。排除集_P0A_HEAD_SPLIT_SINGLE = _P0A_SUFFIX_SPLIT_SINGLE（复用R25词尾排除集）。
- **不对称性**：词首并入比词尾并入多一个"后随汉字"条件（_is_han），因为词首位置的关键字可能是真正的语句关键字（如类 名称中类后随空白→关键字，类型中类后随汉字→并入标识符）。词尾位置不存在这个歧义（scan_pos>0已经在标识符内部）。
- **上游对齐**：语义上对齐上游「标识符即名称」的无限制行为；实现路径为光明特有（汉字字符集与关键字重叠导致需要词首/词尾双维度上下文判定）。

### R26-D3 递归与联动保护表评估结论（工程决策，非行为差异）
- **_skip_compound_safe_and_match**：保留现状不简化。递归深度仅1-3层（CS 16字），性能收益~0；"最内层CS单字回报"语义被去除空格等stdlib函数名依赖（递归回报('空',1)→外层判定kw≠'之'→返回None→整体保留为标识符）。
- **compound_safe_single_keywords类属性**：不可删除。嵌入式扫描三处仍以「CS ∪ _P0A_HEAD_MERGE_SINGLE」作为词首并入有效类别，局部缓存（:1286/:2112）是64K+次热路径的性能优化。
- **联动保护表全部保留**：CCW 37条（与词首并入规则互补无重叠）、_EMBED 3条（为/尝试/返回嵌入最大匹配）、OPERATOR_VERBS 19条（运算符语义核心，DUAL 8字以CS保留作词首锚）、_P0A_MERGE_WHOLE 9条（有界精确整串合并）。

## R27 第27轮（CS表16字逐字通用化，2026-09-15）

### R27-D1 CS表16字逐字通用化与上游差异（CS表16→2，剩余列/类）
- **差异**：上游 deepseek-harness 无汉字保护表；光明第27轮将CS表剩余16字逐字通用化，CS表从16条缩减为2条（列/类保留为真护栏）。
- **A类DUAL双位字（8字删除）**：乘/减/到/加/模/真/空/除。新增_P0A_HEAD_MERGE_DUAL规则：词首+后随汉字→并入标识符（乘法/真空/到位），词尾+前导汉字→并入标识符（文件未找到/标准输出失真），后随空白/数字/操作数→切分（甲 加 乙/返回 真/从1到10）。
- **B类6字删除**：对/是/段/的/自/配。由L-119嵌入扫描等价（独立单字+后随[→不标记内嵌，覆盖段[1]/配[0]/对[0]）+ def边界协同（def是否夏令时）+ 词首并入分支（的/自）+ rematch段（自定义表）覆盖。
- **保留2字（真护栏）**：
  - **列**：对于 元素 在 序列:中列在词尾切出KEYWORD（IDENTIFIER(序)+KEYWORD(列)+COLON），与R25词尾F并入方向相反。列∈F不可加入词尾排除集（否则HM词首列数打红），需独立刻画"词尾+前导汉字→切出KEYWORD"的反向规则。
  - **类**：类 独立类:中独立类整词成IDENTIFIER，撤CS后切成独立+类:。基线整词依赖CS与类声明上下文的协同，撤CS后词尾并入被独立+冒号边界打断。
- **列/类行为互为镜像**：列=词尾切出、类=词尾整词，说明"词尾+CS残部字"的token行为由上下文（前导关键字"在"/"类"声明、后随冒号、user_definitions共同决定），单条通用条件无法同时刻画，需后续轮次以_seg_after_sep/user_definitions协同口径逐字定位。
- **验证**：全语料839文件token零变化；3个.light rc=0；pytest 113 passed（含保护表形态断言CS=2）。
- **订正 R26-D1**：R26-D1「CS表完整清零需后续轮次」——第27轮CS表16→2（精简96%），但未完全清零。剩余列/类为真护栏，行为互为镜像无法单条规则刻画，需后续轮次逐字定位。

### R27-D2 A类DUAL双位字词首/词尾双位置并入规则（实现路径差异）
- **差异**：上游 ASCII 词法以空白/符号为天然边界，运算符/值字面量/范围结束符始终切分；光明汉字串与关键字共享字符集，第27轮新增_P0A_HEAD_MERGE_DUAL规则，使运算符/值字面量/范围结束符在词首/词尾后随汉字时并入标识符。
- **规则**：
  - 词首并入：scan_pos==0 + 单字关键字 ∈ DUAL + 后随汉字（_is_han）→ 并入标识符
  - 词尾并入：scan_pos+sub_len==len(full_identifier) + 单字关键字 ∈ DUAL + 前导汉字（_is_han(fi[scan_pos-1])）→ 并入标识符
  - 切分：后随空白/数字/操作数 → 运算符/值字面量/范围结束符切分
- **实现站点**：第一层skip_verb + 嵌入式扫描三处（探测/输出/rematch），与B类HM规则同口径。
- **关键区分**：加法（加后随汉字）→并入；甲 加 乙（加后随空白）→切分。真空（真后随汉字）→并入；返回 真（真后随空白）→切分。到位（到后随汉字）→并入；从1到10（到后随数字）→切分。
- **与_P0A_NEVER_SPLIT的互补**：模/到虽入DUAL类别，但_NEVER_SPLIT语义是词内切分禁止（模型/模块/1至10），DUAL只覆盖词首/词尾并入，两者保护面互补不重叠。

### R27-D3 递归入口扩展与排除集方案A（工程决策，非行为差异）
- **_skip_compound_safe_and_match递归入口扩展**：任务1+2主动将递归入口条件从candidate in CS扩展为candidate in (CS ∪ _P0A_HEAD_MERGE_DUAL ∪ _R27_BCLASS)三集合并集。CS表清零后DUAL/B类仍非空，递归仍被调用，故该函数非死代码（任务书5.3预设"CS清零后递归永不触发→死代码→删除"不成立）。
- **排除集方案A**：新增_P0A_HEAD_MERGE_DUAL类别，_P0A_HEAD_SPLIT_SINGLE保持42字不变。方案A语义更清晰——运算符/值字面量/范围仍需在非词首位置切分，排除集的语义不依赖CS表存在。_P0A_SUFFIX_SPLIT_SINGLE（词尾排除集21字）派生自五类词法依据，与CS无耦合，不需要同步修改。
- **联动保护表五张全部保留**：CCW 37条（与单字CS递归正交）、_EMBED 3条（与单字CS递归无关）、OPERATOR_VERBS 19条（运算符切分职责不变）、_P0A_MERGE_WHOLE 9条（2-4字整词，与单字词首并入不重复）、_P0A_NEVER_SPLIT 4条（词内切分禁止，与DUAL词首并入互补）。
- **自校验断言更新**：①CS⊆F ∧ CS∩DUAL==∅（残部列/类）；③HM−CS==_R26_CS_REMOVED∪_R27_CS_REMOVED。

## R28 第28轮（CS表剩余2字逐字通用化，CS表清零，2026-09-15）

### R28-D1 CS表剩余2字逐字通用化与上游差异（CS表清零，累计52→0）
- **差异**：上游 deepseek-harness 无汉字保护表；光明第28轮将CS表剩余2字（列/类）逐字通用化，CS表从2条清零为0条（_COMPOUND_SAFE_SINGLE_KEYWORDS = frozenset()）。累计从第22轮前52条→第28轮0条，精简100%。
- **列的通用化**：新增_P0A_TAIL_CUT_SINGLE = frozenset({'列'})词尾切出反向规则。列在词尾（scan_pos+sub_len==len(full_identifier)）+ 前导汉字 → 不跳过、落出KEYWORD（与R25词尾并入方向相反）。切出只发生在嵌入输出循环（段内命中真关键字分段后，如甲在序列/甲之序列）；裸序列:/甲序列:/ESC序列:（段内无真关键字）走整词出口不受影响。
- **类的通用化**：扩展user_definitions前缀匹配"余部单字并入"判据从"仅CS锚"扩展为"CS∪HM∪DUAL"。独立∈user_definitions（段落名/设名预扫描）+ 余部类∈可构词单字 → 不拆分整词（覆盖新建 独立类()使用处）。声明处类 独立类:/类 名称:/类 子类 继承 基类:无需额外规则（KEYWORD(类)+IDENTIFIER整词）。
- **打红场景修正**：R27删除清单定位类的打红场景为类 独立类:声明处，R28地面真相探针（_dbg_r28_ground.py）逐token二分定位修正为新建 独立类()使用处。机制结论以本轮为准。
- **验证**：全语料839文件token零变化；3个.light rc=0；pytest 38 passed（R26+R27+R28合跑319 passed）；场景矩阵10/10全过；自校验断言5条全部通过（CS==∅、F==43、HM==22、TAIL_CUT=={'列'}、DUAL==8）。
- **订正 R27-D1**：R27-D1「CS表剩余列/类为真护栏，需后续轮次以_seg_after_sep/user_definitions协同口径逐字定位」——第28轮已完成列/类的逐字通用化，CS表清零。列由词尾切出反向规则覆盖，类由user_definitions前缀余部并入扩展覆盖。

### R28-D2 列的词尾切出反向规则（实现路径差异）
- **差异**：上游 ASCII 词法以空白/符号为天然边界，无词尾切出概念；光明汉字串与关键字共享字符集，第28轮新增_P0A_TAIL_CUT_SINGLE词尾切出反向规则，使列在特定上下文（嵌入输出循环+词尾+前导汉字）中切出为KEYWORD，与R25词尾并入（_TRAILING_ALIAS_CLASS=F）方向相反。
- **规则**：列 ∈ _P0A_TAIL_CUT_SINGLE + 词尾位置（scan_pos+sub_len==len(fi)）+ 前导汉字（_is_han(fi[scan_pos-1])）+ 嵌入输出循环（段内命中真关键字分段后）→ 不跳过、落出KEYWORD。
- **关键机制**：切出只发生在嵌入输出循环（甲在序列/甲之序列分段形态）；裸序列:/甲序列:/ESC序列:（段内无真关键字，embedded_found=False）走整词出口不受影响。这保证了列的词尾切出只在需要的场景触发，不误伤其他场景。
- **与R25词尾并入的关系**：R25词尾规则默认词尾+前导汉字→并入标识符；列的反向规则是例外，优先级高于R25词尾并入。列∈F（正面类别）不可加入词尾排除集（否则HM词首列数打红），故需独立的词尾切出反向规则。

### R28-D3 CS表清零后死代码清理与性能提升（工程决策，非行为差异）
- **12项死代码清理全部执行**：类属性compound_safe_single_keywords删除（恒空）、局部缓存_compound_safe×2删除、_match_keyword/_skip_compound_safe_and_match递归入口删CS项→DUAL∪B类、嵌入扫描三处CS项重写为平衡结构（HM∪DUAL+tail）、前缀拆分检查改写为HM∪DUAL、L-119下标判据删CS项→HM∪DUAL、R26词首并入判据删CS项→(HM and is_han) or (DUAL and is_han)。每处删除前均grep全库复核零引用。
- **_skip_compound_safe_and_match非死代码**：任务书5.1预设CS清零后该函数成为死代码可删除。实测DUAL(8字)∪B类(8字)非空，递归入口仍命中，函数非死代码。与R27报告5.3结论一致，按任务书明示保留。
- **自校验断言更新**：①CS==∅（空frozenset）、②_TRAILING_ALIAS_CLASS==F（43字不变）、③HM−CS==_R26_CS_REMOVED∪_R27_CS_REMOVED∪_R28_CS_REMOVED（=HM 22字）、④_P0A_TAIL_CUT_SINGLE=={'列'}（恒1字防漂移）、⑤_P0A_HEAD_MERGE_DUAL==8字（恒8字防漂移）。import自校验全部通过。
- **联动保护表七张全部保留**：CCW 37条、_EMBED 3条、OPERATOR_VERBS 19条、_P0A_MERGE_WHOLE 9条、_P0A_NEVER_SPLIT 4条、HM 22字、DUAL 8字。CS清零不构成语义冲击。
- **性能提升~32%**：修改前（CS=2）165.36s/×10 → 修改后（CS=∅+清理）112.236s/×10，提升~32%（×1均16.536s→11.224s）。这是多轮保护表精简以来最大的单次性能提升。
- **CS表八轮演化总览**：R21前52字 → R22 30 → R23 30（三表白名单清零）→ R24 30（真护栏确认）→ R25 30（词尾正面类别F=43，CS退化为词首锚）→ R26 16（词首并入正面类别HM=22）→ R27 2（DUAL=8+L-119等价）→ R28 0（列词尾切出反向规则+类前缀余部并入扩展）✅清零。累计精简100%。

## R29 第29轮（CCW 37条分批精简，37→10删27条精简73%，2026-09-15）

### R29-D1 CCW保护表分批精简与上游差异（37→10，累计184→10精简94.6%）
- **差异**：上游 deepseek-harness 无汉字保护表；光明第29轮将CCW（COMMON_COMPOUND_WORDS）从37条分批精简到10条，删除27条（精简73%）。累计从第22轮前184条→第29轮10条，精简94.6%。
- **四批并行精简**：
  - A批（位运算符+异步，9条）：删5条（异步写入文件/异步睡眠/异步读取文件/异步追加文件/并发等待），留4条（位与/位异或/位或/位非——撤后被关键字劈开）
  - B批（错误类型+设置，10条）：删9条（导入错误/类型错误/设指针/设指针值/设系统/设系统错误码/设置/设置数组/低级关闭），留1条（零除错误——撤后被CHINESE_NUM劈开）
  - C批（测试/生成+关键字，10条）：删7条（生成分析/生成问候语/最终答案/当然/当前/终点/开启调试），留3条（测试_生成问候语——ASCII下划线+Han粘连、应当——"当"关键字词首劈开、除非——双侧关键字夹击）
  - D批（其他类型，8条）：删6条（函数对象/常量时间比较/枚举值/正则匹配/环境枚举/结构体值），留2条（幂次——被关键字"幂"劈开、记录类型——语料0命中但边界形态G3失败）
- **删除条件**：每条目必须通过三重判据（G1语料全语料token零变化 ∧ G2编译门可编译 ∧ G3边界门token流不变），三者任一失败即保留。删除27条全部通过，词法通用机制已覆盖。
- **保留10条真护栏**：撤后均会导致token序列变化（G3边界门失败），无通用规则可替代。
- **验证**：全语料850文件token零变化；边界测试rc=0；pytest 35 passed；性能提升~34.4%（11.22s→7.36s）。
- **订正 R22-D1**：R22-D1「CCW 184→37深度精简」——第29轮进一步精简到10条，删除27条。CCW累计从184→10，精简94.6%。

### R29-D2 CCW精简后性能提升（工程优化，非行为差异）
- **性能提升~34.4%**：CCW 37→10精简后，全语料tokenize耗时从11.22s（R28基线）降至7.36s（R29，×5取平均），提升~34.4%。
- **累计性能提升**：从R21前基线（~25s）到R29（7.36s），词法热路径累计性能提升~70%。
- **优化路径**：R21词法确定性切词重构 + R22~R29保护表持续精简（CS 52→0、CCW 184→10、三表白名单清零）+ R28死代码清理（12项）。
- **联动保护表七张全部保留**：_EMBED 3条、OPERATOR_VERBS 19条、_P0A_MERGE_WHOLE 9条、_P0A_NEVER_SPLIT 4字、HM 22字、DUAL 8字、TAIL_CUT 1字。CCW精简不构成语义冲击。

## R30 第30轮（CCW保留10条进一步通用化，CCW清零，累计184→0精简100%，2026-09-15）

### R30-D1 CCW保留10条通用化与上游差异（CCW清零，累计184→0）
- **差异**：上游 deepseek-harness 无汉字保护表；光明第30轮将CCW保留10条全部通用化，CCW表从10条清零为0条（COMMON_COMPOUND_WORDS = frozenset()）。累计从第22轮前184条→第30轮0条，精简100%。
- **四批并行通用化**：
  - 任务1（位运算符4条）：位与/位异或/位或/位非，新增词首前缀类规则，覆盖"位"关键字在词首的情况
  - 任务2（应当+除非）：应当/除非，复用任务1的词首前缀类规则
  - 任务3（零除错误+幂次+记录类型）：扩展数字/关键字识别规则
  - 任务4（测试_生成问候语）：扩展ASCII下划线+Han粘连通用规则（xxx_前缀+后随汉字→整段并入IDENTIFIER）
- **关键发现**：所有10条在"设"后面都整词成IDENTIFIER，在语句起始位置被劈开。CCW的保护作用主要在语句起始位置。除非是唯一在多个真实文件中出现的条目（13次/8文件），其他大部分只在边界测试中出现（防御性护栏）。
- **防御性护栏验证**：6条真实语料0命中的条目（位异或、零除错误、应当、测试_生成问候语、幂次、记录类型）全部验证通过，均可直接移除。
- **验证**：全语料851可比文件token零变化；边界测试rc=0；pytest 19 passed；性能提升~16.5%（7.36s→6.14s）。
- **订正 R29-D1**：R29-D1「CCW 37→10，保留10条真护栏」——第30轮将保留10条全部通用化，CCW表清零。累计从184→0，精简100%。

### R30-D2 CCW清零后通用规则扩展（实现路径差异）
- **词首前缀类规则**：新增通用规则，覆盖"位"/"应"/"除"等关键字在词首且后随汉字时并入标识符。这是对位运算符/应当/除非等条目的通用化替代。
- **ASCII下划线+Han粘连通用规则**：扩展汉字+ASCII后缀合并路径，前缀形如xxx_且不在user_definitions中时，后随汉字整段并入为IDENTIFIER。这是对测试_生成问候语等条目的通用化替代。
- **数字/关键字识别规则扩展**：扩展"零"在词首且后随汉字时不转成CHINESE_NUM，"幂"在词首且后随汉字时并入标识符。这是对零除错误/幂次等条目的通用化替代。
- **关键字独立使用不受影响**：HM规则扩展后，"当"/"非"/"位"/"零"等原有关键字独立使用不受影响。

### R30-D3 CCW清零后性能提升（工程优化，非行为差异）
- **性能提升~16.5%**：CCW 10→0清零+通用规则扩展后，全语料tokenize耗时从7.36s（R29基线）降至6.14s（R30，×5取平均），提升~16.5%。
- **累计性能提升**：从R21前基线（~25s）到R30（6.14s），词法热路径累计性能提升~75%。
- **优化路径**：R21词法确定性切词重构 + R22~R30保护表持续精简（CS 52→0、CCW 184→0、三表白名单清零）+ R28死代码清理（12项）+ R30通用规则扩展。
- **联动保护表全部保留**：_EMBED 3条、OPERATOR_VERBS 19条、_P0A_MERGE_WHOLE 9条、_P0A_NEVER_SPLIT 4字、HM扩展、DUAL 8字、TAIL_CUT 1字、F 43字。CCW清零不构成语义冲击。

## R31 第31轮（_EMBED表3条验证保留+test_审批.light flaky修复，2026-09-15）

### R31-D1 _EMBED表3条逐条验证保留（真护栏确认，与第22轮结论一致）
- **差异**：上游 deepseek-harness 无汉字保护表；光明第31轮对_EMBED_MAX_MATCH_KEYWORDS 3条（为/返回/尝试）逐条三重判据验证，全部确认为真护栏，维持3条不变（lexer.py零改动）。
- **逐条验证结果**：
  - 为：G1 FAIL（23文件token变化，作为f/导入字符串为str_utils/替换为/检查是否为字母等）、G3 FAIL（3形态，断言为真(1)/行为/末位行为撤除后被劈开）→ 保留
  - 返回：G1 FAIL（20文件token变化，返回结果/返回模板/返回文本/返回底数等）、G3 FAIL（2形态，返回表/返回值撤除后被劈开）→ 保留
  - 尝试：G1 PASS（0变化）但G3 FAIL（3形态，尝试记录撤除后被劈开）→ 保留
- **真护栏机制**：_EMBED块是为/返回/尝试触发的嵌入式扫描，含该字的复合名（行为/返回表/尝试记录）被整体成IDENTIFIER；设X为<值>形态精确切分。撤除某字后复合名落回R21上下文敏感块，因行/返/尝不是关键字前缀，被关键字劈开，破坏函数定义名/变量名。
- **与第22轮结论一致**：第22轮验证_EMBED表撤掉后17文件token变化，本轮逐条验证确认3条全部为真护栏，不可精简。
- **订正 R22-Dx**：_EMBED表3条确认为真护栏，非冗余，后续轮次不再尝试精简。

### R31-D2 test_审批.light flaky修复（R20-C，从第20轮持续存在）
- **问题**：examples/test_审批.light:125-127使用time.time()计算耗时（设 耗时 为 time.time() - 开始）。time.time()返回系统墙上时钟（wall clock），两次采样之间若发生系统时间调整（NTP同步、手动改时间等），会导致耗时计算错误，用例12d/12e间歇性失败——典型的非确定性测试（flaky）形态。
- **修复**：将125行和127行的time.time()改为time.monotonic()，共2处。time.monotonic()返回单调递增时钟（秒），不受系统时间调整影响（PEP 418）。
- **验证**：5次运行全部rc=0（13用例全过），flaky已修复。
- **意义**：从第20轮持续存在的flaky问题（R20-C）已解决，全量CI不再有已知flaky。

## R32 第32轮（OPERATOR_VERBS 19条验证保留 + _P0A_MERGE_WHOLE 10→3条精简，2026-09-15）

### R32-D1 OPERATOR_VERBS 19条逐条验证保留（真护栏确认）
- **差异**：上游 deepseek-harness 无运算符动词保护表；光明第32轮对OPERATOR_VERBS 19条（比较运算符9+算术运算符10）逐条三重判据验证，全部确认为真护栏，维持19条不变（lexer.py零改动）。
- **比较运算符9条**：不大于/不小于/不等于/大于/大于等于/小于/小于等于/等于/包含。G1语料判据大部分PASS（大于2文件/大于等于1/小于1/小于等于1/等于2文件变化），G2编译门大部分PASS（包含rc=1），G3边界门全部FAIL（9条全部2形态失败）→ 全部保留。
- **算术运算符10条**：乘/乘以/减/减去/加/加上/除/除以/幂/模。G1语料判据大部分PASS（乘2/减4/加4/除5文件变化），G2编译门全部PASS，G3边界门全部FAIL（10条全部1-2形态失败）→ 全部保留。
- **真护栏机制**：OPERATOR_VERBS是"运算符动词"集合，用于嵌入式扫描（L2555）、词首切分判断（L2665）、关键字内嵌处理（L2734/2785/2934/2963/2992/3096/3103）。移除某运算符动词后，含该字的复合名（如加法/减法/大于号/等于号）可能被关键字劈开，或运算符识别逻辑受影响。
- **与_EMBED表的区别**：_EMBED表（3条）是嵌入式关键字扫描的触发器，OPERATOR_VERBS（19条）是运算符动词集合，两者都是真护栏但作用机制不同。
- **订正 R31-Dx**：OPERATOR_VERBS 19条确认为真护栏，非冗余，后续轮次不再尝试精简。

### R32-D2 _P0A_MERGE_WHOLE 10→3条精简（删除7条非冗余，保留3条真护栏）
- **差异**：上游 deepseek-harness 无精确整串合并保护表；光明第32轮对_P0A_MERGE_WHOLE 10条逐条三重判据验证，删除7条非冗余，保留3条真护栏，_P0A_MERGE_WHOLE从10→3条（精简70%）。
- **已删除7条**（G1∧G2∧G3全通过）：
  - 导出事件表（7处/2文件）
  - 退出码（156处/37文件）
  - 接收参数（6处/3文件）
  - 外部命令（36处/13文件）
  - 排序依据（6处/2文件）
  - 输出块表（5处/1文件）
  - 返回码（55处/16文件）
- **保留3条**（真护栏）：
  - 整理模型消息（G3 F5传参位劈开）
  - 记录类型（G1 2文件变化+G3 F2/F5劈开，类型是硬语句关键字/后缀切分关键字）
  - 非空块（G3 F2/F5劈开）
- **精简机制**：_P0A_MERGE_WHOLE是"精确整串"合并集合，仅当整个汉字段恰好等于集合中的某一整串时才并入标识符。已删除的7条已有其他规则覆盖（成员访问规则/R21上下文敏感块等），_P0A_MERGE_WHOLE中的条目是冗余的。
- **全语料token零变化**：任务3第一批并集删除3条变化文件=0，任务4第二批并集删除4条变化文件=0，合并后全量反跑零回归。
- **保护表精简规律**：多字保护表（CCW/_P0A_MERGE_WHOLE）大部分可通过通用规则替代，单字/运算符保护表（_EMBED/OPERATOR_VERBS）大部分是真护栏。

## R33 第33轮（_P0A_NEVER_SPLIT 4字清零 + 历史遗留清理，2026-09-15）

### R33-D1 _P0A_NEVER_SPLIT 4字清零（模/步/至/到全部可删）
- **差异**：上游 deepseek-harness 无永不切分保护表；光明第33轮对_P0A_NEVER_SPLIT 4字（模/步/至/到）逐条三重判据验证，全部可删，_P0A_NEVER_SPLIT从4→0条清零。
- **模**：G1∧G2∧G3全通过。模在NEVER_SPLIT中是被OPERATOR_VERBS已保护的重复登记——移除它不产生任何派生集变化，G1零变化属"最强理由"而非边界侥幸。模型/模块/模式等复合名由OPERATOR_VERBS/DUAL兜底仍整词。
- **步/至/到**：G1∧G2∧G3全通过。移除后由"后缀排除集"移入F（词尾并入类别，43字）与HM（词首并入），但这3字在语料中唯一形态为数字后独立汉字段（如1到10/1至10/1到10步2），后随数字非汉字：词首并入（需后随汉字）不触发，词尾并入（需前导汉字整词）不触发，故全语料token流零变化。
- **到**：同时位于DUAL（_P0A_HEAD_MERGE_DUAL，8字之一），移除NEVER_SPLIT后DUAL仍生效，词首到位等由DUAL兜底，与NEVER_SPLIT正交。
- **保留空集名**：_P0A_NEVER_SPLIT = frozenset()保留空集名以兼容L3307/L3825引用，与R28清CS同口径。
- **全语料token零变化**：各字单字验证0变化/857，终验0变化/0错误，合并后全量反跑零回归。
- **保护表演化**：CS 0（R28）、CCW 0（R30）、_EMBED 3（R31真护栏）、OPERATOR_VERBS 19（R32真护栏）、_P0A_MERGE_WHOLE 3（R32精简）、_P0A_NEVER_SPLIT 0（R33清零）。

### R33-D2 历史遗留未提交问题清理
- **lightharness已修改未提交（5个）**：
  - _antirun_r26_基线快照.json（6行变化）
  - _task1_R23_TRAILING_ALIAS清表_交付报告.md（2行变化）
  - _task3_R23_CCW内建名迁移_交付报告.md（2行变化）
  - _task5_R23_全量回归扫描报告.md（110行变化，补充交付提交信息和全量套件结果）
  - tests/test_R29_CCW表分批精简_token.py（23行变化，R30时CCW清零导致断言更新）
- **light-merge已修改未提交（1个）**：examples/harness/评测报告.md
- **未跟踪临时脚本**：大量_antirun_r*.py、_dbg_*.py、_probe_*.py等，按惯例不提交（收口工具/临时脚本）
- **清理方式**：路M收口时统一提交已修改文件，确认未跟踪临时脚本无需提交

## R34 第34轮（继续上游复刻——核心模块深化，2026-09-16）

### R34-D1 代理循环事件类型格式与会话日志不兼容 ✅ 已对齐上游
- **差异**：代理循环模块（src/代理循环.light）的事件类型原使用下划线格式（"turn_start"/"turn_end"/"step_start"/"step_end"），而会话日志深化模块（src/会话日志深化.light）的已知类型集使用斜杠格式（"turn/start"/"turn/end"/"step/start"/"step/end"等70+种上游事件类型）。
- **上游实现**：deepseek-harness的agent-loop直接调用`session.append('turn/start', ...)`，使用斜杠格式，没有自己独立的事件类型系统。turn/step状态变化直接写入session日志。
- **修复**：第34轮收口后已将代理循环事件类型从下划线格式改为斜杠格式（turn/start、turn/end、step/start、step/end），与上游和会话日志对齐。checkpoint/abort/resume为代理循环内部内存事件，不持久化到会话日志，保持不变。
- **判定**：✅ 已修复。代理循环派发的turn/step事件现在可直接追加到会话日志。

### R34-D2 工具执行器内部自带注册表
- **差异**：工具执行模块（src/工具执行.light）的`建执行器()`内部自动创建注册表（`执行器["注册"]`），外部创建的注册表（`建注册表()`）与执行器无关联。工具必须注册到执行器内部的注册表才能被`执行工具()`找到。
- **影响**：用户可能先创建外部注册表注册工具，再创建执行器执行工具，导致"permission_denied"（工具不存在被判定为权限拒绝）。
- **判定**：🟡 复刻缺口/语义差异（可后置）。上游设计中执行器与注册表是分离的，光明实现中执行器内部自带注册表简化了使用但可能造成混淆。
- **临时方案**：通过`执行器["注册"]`访问执行器内部注册表进行工具注册，如`具注册工具(执行器["注册"], "echo", 级公开, 处理函数)`。

### R34-D3 stdlib旧版代理循环与src新版并存 ✅ 已解决（第37轮）
- **差异**：stdlib目录下已有旧版`代理循环.light`（单agent单轮工具调用最小闭环，早期复刻），src目录下第34轮新增新版`代理循环.light`（多轮循环核心，turn/step状态机+检查点+中止恢复）。二者同名但功能不同。
- **第37轮差异分析实证**：
  - 实为三处，不是两处：①stdlib旧版（应用层）②src新版（核心层）③src/代理.light内的`类 代理循环`（驱动层，17处`从 代理 导入`引用，不受影响）
  - R34-D3登记前提（stdlib优先于src）**被实证证伪**：`运行.py:_setup_paths`实际顺序为**SRC > ROOT > STDLIB**，`.light`导入钩子亦以SRC优先
  - stdlib旧版是**孤儿**（全语料0个导入方），仅2处测试导入src新版独有符号
- **修复（第37轮任务1）**：选方案B（重命名）——stdlib/代理循环.light → stdlib/代理运行时.light（应用层），src版保持代理循环（核心层）。零回归（5文件token流与HEAD完全一致），更新4个stdlib文件comment引用（事件总线/代理工具集/并发/重试）。
- **判定**：✅ 已解决。同名冲突消除，命名即分层（代理运行时=应用层，代理循环=核心层）。

## R35 第35轮（_P0A_MERGE_WHOLE 剩余3条通用化精简，2026-09-16）

### R35-D1 _P0A_MERGE_WHOLE 3→2：`非空块` 由通用规则接住，`整理模型消息`/`记录类型` 保留为真护栏
- **变更**：新增一元前缀运算符类别 `Lexer._P0A_UNARY_PREFIX_KW = {'非'}`，并在 R21 词首闸门3 把判据从 `_lead_kw not in _OPERATOR_KEYWORDS` 改为 `_lead_kw not in (_OPERATOR_KEYWORDS - _P0A_UNARY_PREFIX_KW)`，另加收窄「余部是**已声明名字**时仍是 `not X` 表达式」。
- **立论语料证据**：无空格 `非X` 在语料中**恒为复合名**（非空 259 / 非法 206 / 非零 164 / 非值 125 / 非负整数 67 处），**零处**是 `not X`；`非` 作取反时**恒带空格**（`非 甲` / `非 是真`）。
- **三重判据**：G1 全语料 867 文件 token 零变化 ∧ G2 边界 `.light` 编译门 rc=0 ∧ G3 五种边界形态（设名/函数名/成员访问/段落名/传参位）token 流一致 ⇒ 移除 `非空块`。
- **保留 2 条的理由（实证反例，非保守）**：
  - `整理模型消息`：`模` 是**真取模运算符**（`test_R24_运算符单字守卫.light` 的 `甲模乙` 专设守卫）。GR-1 打红 6 文件、GR-1c（两侧均 ≥2 字收窄）仍打红 1 文件 ⇒ 任何在表达式位并入 `X模Y` 的规则都会打掉取模。
  - `记录类型`：与 `期望类型`/`参数类型` **结构不可分**（同为「非关键字前缀 + `类型`」）。GR-3 打红 4 个 stdlib；GR-3c（仅调用语境）G1=0 但 **G3-F5 传参位仍 FAIL** ⇒ 只是把矛盾从 G1 推迟到 G3，按铁律保留。
- **判定**：✅ 已完成。`_P0A_MERGE_WHOLE` 由 3 条降为 2 条（`整理模型消息` / `记录类型`），保护表总账再减 1。

### R35-D2 一元前缀字 `非` 的**词首并入 / 词尾切分不对称**（新登记，已知且稳定）
- **差异**：`非` 在**词首**并入复合名，但在**词尾**仍按运算符关键字切出——
  | 形态 | 例 | 结果 |
  |---|---|---|
  | 词首 `非X` | `非空块` | 整词 `IDENTIFIER` |
  | 词中 `X非Y` | `甲非空` | 整词 `甲非空`（`deterministic=True`） |
  | 词尾 `X非` | `甲非` | `IDENTIFIER(甲)` + `KEYWORD(非)` |
- **根因**：R35 只改了 R21 闸门3（词首判据），未改 `_P0A_SUFFIX_SPLIT_SINGLE` 的推导（L3343，词尾切分排除集），故 `非` 在词尾仍是运算符。
- **影响**：当前语料无 `X非` 词尾形态需求；若将来需要对称化，须同时改 L3343，属独立变更。
- **判定**：🟡 已知不对称（可后置）。行为稳定，已被 `tests/test_R35_MERGE_WHOLE通用化_token.py` 钉住三态。

### R35-D3 R26 全语料基线快照过期，致 2 条 `test_fingerprint_unchanged` 打红（非词法回归）
- **现象**：`test_R27` / `test_R28` 的 `TestCorpusZeroTokenChange::test_fingerprint_unchanged` 报「全语料 token 变化 1 文件：`lightharness/examples/test_审批.light`」。
- **根因（已定位）**：对照物 `_antirun_r26_基线快照.json` 生成于 **2026-09-15 13:31**（843 文件），而 `test_审批.light` 于 **2026-09-15 20:02**（提交 `e26ed3d`，第31轮 T4 flaky 修复 `time.time→time.monotonic`）被修改入库，**晚于快照 6.5 小时** ⇒ 指纹差来自**源文件内容变更**，与词法器无关。
- **佐证**：R35 A/B 反跑显示该红在 HEAD（改动前）同样存在；R35 改动后全量红数由 23 降至 2。
- **路M 收口裁定（2026-09-16）**：⚪ 基线过期，非缺陷。**先取证再最小修订**——用**当前（R35 改动后）lexer** 对**快照时点**内容（`e26ed3d^`）重新 tokenize，得到的指纹与快照记录**逐位一致**（`3440261634d1…`），且 token 数 1309→1309 不变 ⇒ **零词法回归**，确证为源文件漂移。遂按 `docs/功能对标/反跑判据.md` §6.3 处置：只修订该条 `per_file[...].sha`，并追加 `_amendments` 审计条目（时间/提交/新旧 token 指纹/新旧源文件内容 sha256/token 数/理由），`corpus_aggregate_fingerprint` **保持原值、不整体重生**，以守住"合并前不刷 baseline"红线（整体重生会把其它文件的真实回归一并洗白）。工具：`_taskM_amend_r26_快照.py`（内置取证，旧内容不符快照会自动 ABORT）。
- **附带发现（同批清理）**：R33 清零 `_P0A_NEVER_SPLIT` 后，R23/R25–R30 共 **16 条**「保护表形态」断言未同步更新（HM 22→25、F 43→46、`_P0A_HEAD_SPLIT_SINGLE` 42→39、`CS∪HM∪DUAL` 30→32、`到` 由 ∉F 变 ∈F、旧属性名 `compound_safe_single_keywords` 已不存在等），本轮已全部修正并记录根因。
### R36-D1 技术债清理：R29/R30死文件移档 + test_lexer 3条基线红重写
- **R29/R30死文件**：examples/test_R29_CCW精简边界.light 和 examples/test_R30_CCW通用化边界.light 自入库起从未rc=0（解析期即失败，整份用例从未真正执行）。根因：CCW清零（R30）后，文件假设的复合名（位与/位非/幂次/除非/应当）已被切分，设计前提被现实推翻；且各含孤立捕获块缺尝试:、调用未定义函数名。处置：从examples/移入docs/历史存档/（git mv保留历史），test_回归.py移除2条EXPECT_RED登记，门禁零噪音。token层钉桩已由pytest接管。
- **test_lexer 3条基线红**：	est_basic_keywords/	est_multiple_keywords/	est_complex_expression基于旧版确定性切词假设（无空格粘连写法期望切出关键字），已过时。根因：为属_EMBED_MAX_MATCH_KEYWORDS（恒带空格），设甲为三无空格游程被整体并成IDENTIFIER。处置：全部重写为规范写法（带空格），保留原断言意图，净增12条断言（含粘连钉现状断言）。10/10通过。
- **判定**：✅ 已完成。技术债清零，门禁从2条EXPECT_RED止血清转为零死文件噪音。

### R36-D2 性能优化：_OPERATOR_KEYWORDS_NO_UNARY_PREFIX预计算常量
- **改动**：将R21词首闸门3每次成词判定时的重复集合差运算_OPERATOR_KEYWORDS - self._P0A_UNARY_PREFIX_KW预计算为模块常量_OPERATOR_KEYWORDS_NO_UNARY_PREFIX（26元），加import自校验兜底（防止未来_P0A_UNARY_PREFIX_KW增删成员时遗漏同步）。
- **性能结果**：ABAB交替+严格配对复测，Δ=+1.15%（σ=8.98%，正2/负7/平1），落在本机测量噪声带内（HP Z620 ±9%）。**未观测到预期的~1.9%回收**——原内联集合差作用于27元frozenset，单次开销<1μs，且R21闸门3仅在小比例路径触发，全语料累计收益远低于噪声。
- **判定**：✅ 已完成。零风险代码整洁度改进（消除重复构造、常量名自文档化、自校验兜底），非性能回收。语义等价已由自校验+pytest双重兜底，全量反跑676文件0差异。

### R36-D3 L-154/R35-D2评估结论：均不立项，维持现状
- **L-154（CCW清零后合成名整词能力丧失）**：深度评估4方案（重引白名单/扩展一元前缀通用规则/改进报错信息/维持现状）。结论：**不立项，维持现状**。全语料零命中，影响面低；修它等于重新引入白名单，违背保护表精简方向；根本矛盾是光明词法缺少"已声明名字/已知复合名"之外的复合名构词依据。可把"报错可诊断性增强"单独登记为低优先待办（不占立项）。
- **R35-D2（词首并入/词尾切分不对称）**：评估对称化成本与收益。结论：**不立项对称化，维持现状**。不对称符合中文构词/语法（非空是复合名，甲非是"甲不是"表达式）；对称化会破坏现有代码（甲非0等词尾切分形态），风险>收益。已由pytest钉住三态（词首/词中/词尾）。
- **判定**：✅ 评估完成，两项均维持现状。为后续轮次提供明确决策依据。

## R37 第37轮（R34-D3代理循环统一 + agent-default-model复刻，2026-09-16）

### R37-D1 R34-D3代理循环统一完成（stdlib旧版重命名为代理运行时）
- **变更**：stdlib/代理循环.light → stdlib/代理运行时.light（git mv保留历史），src/代理循环.light保持不变。
- **关键发现**：①实为三处同名（stdlib应用层/src核心层/src/代理.light内的类），R34-D3只涉及①↔②的模块同名冲突；②登记前提（stdlib优先于src）被实证证伪，实际SRC优先；③stdlib旧版是孤儿（0导入方）。
- **方案选择**：方案B（重命名），零回归（5文件token流与HEAD完全一致），更新4个stdlib文件comment引用。
- **判定**：✅ 已完成。R34-D3消除，命名即分层（代理运行时=应用层，代理循环=核心层）。

### R37-D2 agent-default-model模块复刻（默认模型配置）
- **变更**：新增src/代理默认模型.light（导出11项），复刻上游@deepseek-ai/dsh-agent-default-model。
- **核心功能**：建默认模型配置(提供者,模型,推理力度?)、取当前选择()、更新默认模型()、校验模型配置/校验设置载荷、投影选择()、挂载设置源/卸载设置源/写用户层/保存选择。
- **光明适配**：无cordis/Service→字典+函数；无schemastery→手动谓词（required=键存在且为字符串）；settings集成→内存化投影（挂载/写用户层/卸载可逆）。
- **上游对齐**：selection()缺省推理力度省略键；ReasoningEffortId无验证（未知力度原样保留）；saveSelection未挂载时no-op；settings卸载回退组合入口（可逆）。
- **测试**：examples/test_R37_代理默认模型.light（14组全绿）+ test_R37_集成测试.light §5（6组全绿）。
- **判定**：✅ 已完成。上游核心功能完整复刻，settings集成为内存化投影（磁盘持久化属settings服务本身职责）。

## R38 第38轮（agent-tool-presentation工具展示模式复刻，2026-09-16）

### R38-D1 agent-tool-presentation模块复刻完成
- **变更**：新增src/工具展示.light（导出18项：6常量+8函数+1类+1工厂），复刻上游@deepseek-ai/dsh-agent-tool-presentation。
- **核心功能**：三模式工具展示（native全量/ptc仅run_code+SDK/both全量+run_code+SDK）、作用域隔离、codeRuntime条件等待、卸载恢复（HMR安全）、mode必填校验。
- **光明适配**：配置字典范式（对齐第37轮代理默认模型）、模块级作用域展示表（单进程解释器）、待代码运行时标记+等待函数（对齐上游ctx.inject语义）、零修改适配层（工具展示注册表代理类）。
- **上游对齐**：inject=['tools']（codeRuntime是条件等待不是静态依赖）、mode必填不设默认值、ptc无codeRuntime时pending不静默降级、presentAs返回disposer随row卸载解除。
- **测试**：单元测试14组全绿+集成测试11组全绿（上游8契约全映射）+pytest 13用例全绿。
- **判定**：✅ 已完成。上游核心功能完整复刻，零修改既有src模块，即插即用。

### R38-D2 零修改适配层设计（与上游cordis插件系统的差异）
- **差异**：上游用cordis插件系统（ctx.tools.presentAs()在挂载SCOPE上声明，覆盖每个加入该preset的agent），光明没有插件系统。
- **光明方案**：用「依赖注入+代理注册表适配器」实现零修改适配——工具展示注册表代理类包装真实注册表+作用域ID，覆写描述表()为按作用域过滤后的可见工具，其余10+方法原样委托。代理循环传入此代理即自动获得按作用域的工具展示过滤，代码零改动。
- **与上游语义一致性**：上游presentAs只改「工具展示投影」，不动agent loop调度器；光明代理注册表正是这个投影的具象。旧消费者继续传原注册表→作用域展示表命中不到→返回部署默认native→可见工具全量→行为完全不变。
- **判定**：✅ 合理适配。零侵入设计，与上游语义对齐，即插即用。

## R39 第39轮（scope作用域管理 + system-prompt系统提示复刻，2026-09-16）

### R39-D1 scope作用域管理模块复刻完成
- **变更**：新增src/作用域.light（745行），复刻上游@deepseek-ai/dsh-scope。
- **核心功能**：建作用域键（不透明对象，标识比较）/绑定父作用域（一次性，循环检测）/重绑句柄/作用域链/建作用域目标（路由载体）/载体判型/命名条目表（插入有序，幂等undo）/匿名条目表/作用域层组（全局层+链上遮蔽+附着注销）。
- **上游对齐**：ScopeKey不透明对象/WeakMap父链/一次性绑定+重绑句柄/循环检测（沿父链向上遍历发现自身抛错）/注册视图向下继承（子见父）/事件准入向上扩展（父收子）。
- **并行写入事故**：开工时发现另一路agent已写入同一文件（不可解析，15处语法错误），留档后重写（sha256采样确认停止写入+零引用方确认）。
- **测试**：单元测试14+19组全绿。
- **判定**：✅ 已完成。上游核心功能完整复刻。

### R39-D2 system-prompt系统提示模块复刻完成
- **变更**：新增src/系统提示.light，复刻上游@deepseek-ai/dsh-system-prompt。
- **核心功能**：节注册（重复抛错，幂等undo）/上下文注册/工具提供者注册/变量注册/assemble组装（收集→排序→解析→complete→瀑布流）/complete节处理（0正常/1恢复唯一/>1抛错）/瀑布流监听器/工具排序（orderTools+TOOL_ORDER_REST）/变量插值（{{variable}}，未知保留原样）/SECTION_ORDERS 30+位置常量/变量名校验。
- **上游对齐**：name唯一/order升序同order按name/complete严格/瀑布流允许修改/未知变量保留原样/工具排序未列出在REST位置按字典序插入。
- **测试**：单元测试20+19组全绿+集成测试23组全绿。
- **判定**：✅ 已完成。上游核心功能完整复刻。

### R39-D3 光明适配差异（无cordis/无原生正则/无闭包的适配方案）
- **差异**：上游用cordis插件系统（Context/Service/Fiber/事件系统）、原生Map/WeakMap、闭包undo、原生正则、Array.sort。
- **光明方案**：
  - WeakMap→字典（作用域父表/载体键表用字典，单进程解释器GC可控）
  - 闭包undo→句柄字典+高阶函数引用（撤销节/撤销上下文显式传注册表+名字，活跃标记保证幂等）
  - 原生正则→手动字符集查找（VARIABLE_NAME/GROUP_AT用字符串查找+字符集包含实现）
  - Array.sort→选择排序+比较器自实现（语义等价）
  - AbortSignal→留空占位（光明无中止信号）
  - 瀑布流事件→注册表维护监听器列表，assemble时依次调用（语义等价）
- **判定**：✅ 合理适配。所有适配方案语义等价，不改变上游行为语义。

## R40 第40轮（互举闭环端到端集成轮，2026-09-16）

### R40-D1 端到端互举链路集成（装配层模式，零修改既有模块）
- **变更**：新增 `src/互举接线.light`（装配层，约300行，22导出），把 R34~R39 的8个独立模块第一次串成真实多轮会话链路。
- **差异**：上游 agent-loop 通过 `ctx.inject` 依赖注入自动接线；光明无 DI 容器，8个模块全是独立API无粘合点。
- **光明方案**：装配层 `建互举链路上下文` + 显式API调用，把模块间缺口用适配函数补齐——①系统提示3节+1上下文+1变量组装进llm步骤（节顺序+变量插值{{persona}}）②主/子两级作用域键作为AssembleContext.scope传入 ③工具展示native模式经代理注册表进工具执行全链（注册→展示→镜像→执行，状态=ok）④委派子代理建子作用域链（父=主键）+子上下文独立日志 ⑤中止令牌→检查点快照→恢复。LLM步骤用大模型回放保证确定性（无外部API）。
- **零修改**：未改动 `代理循环/工具执行/子代理深化/会话日志深化/代理默认模型/工具展示/作用域/系统提示` 任一既有模块，严格遵循零修改适配层铁律。
- **测试**：examples/test_R40_端到端互举.light 19组全绿；变异断链5个（变量插值/工具状态/子代理事件数/中止状态/事件总数）全部正确反红。
- **判定**：✅ 已完成。互举闭环第一条真实端到端链路打通。

### R40-D2 宿主底座加固（会话存储竞态 + AbortSignal占位评估）
- **变更**：`src/会话存储.light` 抽出模块级 `保存索引替换`（5次指数退避 0.1/0.2/0.4/0.8/1.6s，最坏~3.1s），原 `os.replace` 调用改走它；新增 test_R40_会话存储.light 11组（含竞态注入、残留不放大、_index.json.tmp清理断言）。
- **差异**：上游跨平台原子写语义；Windows 上 os.replace 遇目标独占瞬时失败。
- **光明方案**：退避重试+上限+失败后清理临时文件并抛明确错误；既有 test_会话存储.light 零改动仍通过（大会话<1s）。
- **AbortSignal占位评估结论**：系统提示组装上下文`信号`留空是**正确策略**——组装是同步毫秒级操作，协作式中止已有 `src/中止.light` 协作式令牌（语义等价）；异步中止走工具执行`取消令牌`+`轮询中止`，与系统提示无耦合。本轮零代码改动，只登记结论。
- **判定**：✅ 已完成。两笔登记债（R37起）清偿。

### R40-D3 语言支撑配套（全角逗号判定 + NameError合并嫌疑提示）
- **变更**：`light-merge/src/enhanced_errors.py`（唯一改动源码文件）NameError报错路径追加「词法整串合并嫌疑」提示；新增 tests/test_R40_语言支撑配套.py 47组。
- **全角逗号判定**：判定为**非缺陷**——`keywords.SYMBOL_MAP` 已把全角标点归一化为半角等价token（全/半角发同型token，逐字一致），规范文档4处明文承诺「，与,等价」；换半角逗号修不好（归一化后报错逐字相同），真因是 R19`为`整串合并/R22`接收`合并（见缺陷账L-155）。未改语言、未改示例逗号（避免伪修复），改为测试钉桩+报错提示。
- **NameError提示**：判据三轮收紧（误报率4.8%→0.25%核心/0.10%全树）——「≥2个不同从句关键字」或「含为且含运算符」才提示；只改报错文本生成路径，不碰切词/解析，对既有测试零影响（结构性证明+143报错相关用例全绿）。
- **_EMBED复验**：`d8cf337d` 只修了`返回`分支，`为`分支残口仍在（basic.light L10/L40仍红），已登记L-155，不等待不修改。
- **判定**：✅ 已完成。配套改进落地，真因入缺陷账。
