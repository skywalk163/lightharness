# 行为差异清单（第8轮行为级对照汇总）

> 生成：2026-09-12 ｜ 轮次：第8轮（行为级对照验证，4 路并行）
> 来源：四份对照交付报告 §5 差异清单（_task1_代理循环对照 / _task2_会话持久化对照 / _task3_LLM对照 / _task4_工具子智能体对照）
> 原则：本清单只登记、不擅改 src；修复与否由路M 与用户按轮决策。
> 判定列：🔴=建议修复（真实行为 bug）｜🟡=复刻缺口/语义差异（可后置）｜⚪=语言层限制/宿主面（维持登记）

---

## 一、差异总表

| ID | 轮·路 | 上游包·spec | 光明模块 | 差异描述（上游期望 → 光明实际） | 疑似根因 | 判定 |
|---|---|---|---|---|---|---|
| T2-D5 | 8·任务2 | core/session surface.spec | `src/会话格式.light:537` 模块级投影 | 节点0 append 连接分支读 `数据["system"]`（英文键），与 `记录系统提示` 写入的 `["系统"]`（中文键）不一致——**第二条 append 必抛 KeyError**；类方法 `会话.投影节点0`(:89) 用 `["系统"]` 正确 | 键名笔误 | 🔴 建议修复（:537 改 `["系统"]`） |
| T2-D1 | 8·任务2 | surface.spec | `src/会话.light:175-201` | replace 起点/终点不存在、start>end **抛错** → 静默无操作 | 该段仅按序号过滤区间，无存在性与顺序校验 | 🟡 补区间校验抛错或登记简化 |
| T2-D2 | 8·任务2 | surface.spec | `src/会话格式.light:174-177` | sourceEventSeqs 是**快照**（改动不影响已记录）→ 直接引用同一列表，调用方追加透传 | 光明无浅拷贝内置，段落直赋引用 | 🟡 引入 列表快照 或逐元素拷贝 |
| T2-D3 | 8·任务2 | canonical-envelopes / session | `src/会话格式.light:188-209` | V0 编码→解码**还原版本 3.0** → 编码写中文键 `主/次`、解码读英文键 `major/minor`，往返退化 2.0 | 编解码键名约定不一致（持久化主路径走英文键不受影响） | 🟡 统一版本键为英文 major/minor |
| T2-D4 | 8·任务2 | sequence-types | `src/会话格式.light:27-32` | 恢复边界**拒绝负零序号** → 光明/Python `-0` 与 `0` 不可区分，仅判 `值<0` 放行 | 数值域差异（语言层） | ⚪ 语言层限制 |
| T2-G1 | 8·任务2 | repair.spec | 光明无对应段落 | 为未闭合 tool/call 合成 tool/result（含 surfaceOp/来源序号） | 该域未翻译 | 🟡 缺口登记，是否补真身待裁决 |
| T3-D1 | 8·任务3 | llm assembler/mapUsage | `src/令牌估算.light` | usage 在 prompt+completion 之外补 `totalTokens` 桶 → 光明仅 输入/输出 | 纯语义未实现（复刻缺口） | 🟡 补 totalTokens 桶 |
| T3-D2 | 8·任务3 | llm message.spec | `src/消息.light` | createXxxMessage 经 deep-freeze 与传入 content **脱钩** → 光明按引用组装，调用方改入参会污染已构造消息 | 无深拷贝/冻结 | 🟡 行为差异（建议后续补回归） |
| T3-D3 | 8·任务3 | llm-deepseek image-tokens.spec | 光明无对应 | 字节级图片 token 估算 → 未移植 | 无字节级模型 | ⚪ 未移植域（等价能力缺失） |
| T1-S18 | 8·任务1 | agent-loop tool-calls.spec | `src/工具注册表.light`（工具注册表.执行） | concludesTurn 由工具函数返回值传播 → 始终设 `假` | 执行方法不读工具返回值字段 | 🟡 补 concludesTurn 传播 |
| T4-D1 | 8·任务4 | subagent assistant-output.spec | `src/子智能体.light`（取最后回答） | 返回**内容块表** `[{type:"text",...}]`、无输出→`undefined`、reasoning-only 视为**非空** → 返回**拼接文本字符串**、无输出→`""`、reasoning-only **跳过** | 投影形态+边界语义差异 | 🟡 对文本消费方等价，对块级/判空方不等价 |
| T4-D2 | 8·任务4 | tools execution-mode.spec | `src/工具.light`（执行策略） | isConcurrencySafe 未声明 → **exclusive** → 未注册/缺省 → **parallel** | 默认并发语义差异（既有 test_工具 亦断言 parallel） | 🟡 确认上游语义后改默认或登记 |
| T4-D3 | 8·任务4 | tools tools.spec | `src/工具.light`（校验参数） | 上游英文文案+字段结构 → 光明中文文案+结构化错误表 `{路径,原因,细节}` | 文案语言差异 | ⚪ 语义等价、结构不同，维持 |
| T4-D4 | 8·任务4 | preset display.spec | `src/预设.light` | shipped 预设走字典键解析（t:presetStandardName）→ 无 shipped 字典翻译（user 元数据原样保留 ✓） | 无 i18n 字典键 | 🟡 功能缺口 |

## 二、汇总统计

- **🔴 建议修复**：1 项（T2-D5 键名笔误——真实 KeyError bug）
- **🟡 复刻缺口/语义差异**：9 项（T2-D1/D2/D3/G1、T3-D1/D2、T1-S18、T4-D1/D2/D4）
- **⚪ 语言层限制/宿主面/未移植**：3 项（T2-D4、T3-D3、T4-D3）

## 三、建议决策

1. **T2-D5 单独开修复轮**（一行级修复，风险极低，修复后补一条断言覆盖 append 连接分支）。
2. 其余 🟡 按优先级排修复轮（T3-D1 totalTokens 桶、T1-S18 concludesTurn 传播、T4-D2 执行策略默认值 为语义缺口，建议优先；T2-D1/D2/D3、T4-D1/D4、T2-G1 为增强/缺口，可后置）。
3. ⚪ 项维持登记（语言层/宿主面），不进入修复轮。
