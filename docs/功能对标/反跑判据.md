# 光明语言缺陷 · 反跑判据（T10-07 缺陷复现套件）

> 配套：`examples/test_L001.light` ~ `test_L011.light`（含 007b/007c/011）+ `docs/功能对标/语言缺陷账.md`
> 统一运行通道：`python 运行.py examples/test_L0XX.light`（默认 SRC 后端）

## 1. 判红 / 判绿总则

- **判红（缺陷未修）**：`python 运行.py examples/test_L0XX.light` 退出码 ≠ 0，且错误信息与文件头「# 当前：」行描述一致。
- **判绿（缺陷已修）**：启用「修复后断言」段（见 §2），退出码 = 0，且 `断言修复后()` 内全部 `断言等于` 通过。
- **反跑即判据**：用例本身同时是复现脚本与验收脚本；未修保持红，修好后启用断言转绿。

## 2. 「修复后断言」段写法与启用方式

每个用例文件统一结构：

```
# 缺陷：L-0XX <一句话现象>
# 当前：<预期红/报错现象>
# 修复后：<应绿的行为断言>
（复现段：触发缺陷的最小源码）

# 修复后断言段——当前刻意不调用（修复后启用）
段落 断言修复后:
  断言等于(<实际>, <期望>, "L-0XX …")

段落 断言等于 接收 实际, 期望, 标签:
  如果 实际 != 期望:
    抛出 "断言失败: " + 标签 + " 实际=" + 转字符串(实际) + " 期望=" + 转字符串(期望)

段落 主:
  <复现段>
  打印 "--- 测试L0XX 结束 ---"
  # 修复后启用：断言修复后()

主()
```

**启用步骤（语言团队修复缺陷后）**：把 `主()` 里 `# 修复后启用：断言修复后()` 的注释去掉，使 `断言修复后()` 被调用；用例即从「红」转为「绿」。

**断言失败语义**：`断言等于` 不匹配即 `抛出`，运行器按运行期异常分类、退出码非 0 → CI 判定为失败（防「假绿」）。

## 3. 当前状态一览（2026-08-28 第11轮松绑后实测）

> 编译器 L1/L2/L3 缺陷修复（2026-08-27 合入 light-merge main）后，本套件 18 个用例
> 除 L-009（工程层）外全部转绿；lightharness 侧绕法已按第11轮松绑移除。

| 编号 | 用例 | 当前 | 说明 |
|---|---|---|---|
| 编号 | 用例 | 当前 | 说明 |
|---|---|---|---|
| L-001 | `test_L001.light` | 绿（已修复 L3） | 点号导入 `_import_test.数据` 可解析 |
| L-002 | `test_L002.light` | 绿（已修复 L1） | 多行字面量可编译运行 |
| L-003 | `test_L003.light` | 绿（已修复 L2 加固） | 成员空列表赋值稳定；回归哨兵 |
| L-004 | `test_L004.light` | 绿（已修复 L1） | 跨模块关键字开头成员名整段成词 |
| L-005 | `test_L005.light` | 绿（已修复 L2） | 非 self 成员下标按下标取值 |
| L-006 | `test_L006.light` | 绿（已修复 L2） | 闭包改外层标量生成 nonlocal |
| L-007 | `test_L007.light` | 绿（已修复 L1） | 成员名拆分状态性未复现；回归哨兵 |
| L-007b | `test_L007b.light` | 绿（已修复 L1） | 跨模块局部变量污染未复现；回归哨兵 |
| L-007c | `test_L007c.light` | 绿（已修复 L1） | 属性名/参数名前缀重叠未复现；回归哨兵 |
| L-008 | `test_L008.light` | 绿（已修复 L2） | `序列化` 全局内置可用 |
| L-009 | `test_L009.light` | 工具（工程层，未修） | 直接运行绿；需替换脚本演示红（§4） |
| L-010 | `test_L010.light` | 绿（已修复 L1） | `返回码` 成员访问整段成词 |
| L-011 | `test_L011.light` | 绿（已修复 L1） | `等待进程` 整段成词；用例入参已修正 |
| L-012 | `test_L012.light` | 绿（已修复 L1） | `终端.终端会话` 构造成功；`会话工厂` 已移除 |
| L-013 | `test_L013.light` | 绿（已修复 L1） | 单行 for 立即闭合；回归哨兵 |
| L-014 | `test_L014.light` | 绿（已修复 L2） | `表["甲"](1)` 合法调用语句 |
| L-015 | `test_L015.light` | 绿（已修复 L1） | `从 异步代理 导入` 可用；模块名已恢复正式名 |
| L-016 | `test_L016.light` | 绿（已修复 L2） | `捕获 全部` → `except BaseException` |
| L-018 | `test_L018.light` | 绿（已修复） | `from 重试 import 退避间隔, 造策略` 命中用户侧模块，断言通过 |
| L-019 | `test_L019.light` | 绿（已修复 L1） | 段落内 `设 X 为 新建 X()` 不再被同名局部变量遮蔽；`新建 变量()` 护栏同绿 |


## 4. L-009（工程层）判据

L-009 是文本级批量替换缺陷，不是语言运行时缺陷，直接运行 `test_L009.light` 恒绿。判据：

```
# 对副本执行（含「导入 builtins」字样的注释行会被劈开）：
$c = Get-Content examples/test_L009.light -Raw -Encoding UTF8
$c -replace '导入 builtins', "导入 builtins`n从 JSON 导入 序列化JSON" > 副本.light
python 运行.py 副本.light   # 应为红（伪词法错误：注释尾巴变成代码行）
```

修复方向：文本级批量替换工具应跳过注释/字符串内的目标串；注释应始终被词法忽略。

## 5. 缓解项（L-003/L-007/L-007b/L-007c）判据

这 4 项在当前 `lexer.py` 预扫描下已转绿（无法复现缺陷账所述错误形态）。用例文件仍保留完整复现段与 `断言修复后()`（当前刻意不调用）：

- 当前运行：绿（输出正确结果）→ 作为回归哨兵。
- 若编译器拆分逻辑退化、缺陷回归：启用 `断言修复后()` 后应立即变红 → 提醒团队该形态重新暴露。
