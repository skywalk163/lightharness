# R71 批次 · 收口总报告（第 1 轮：核心语法补缺）

> 日期：2026-09-19　|　来源：`docs/功能对标/光明语言语法缺陷清零_R71-R73三轮并发开发任务书.md`
> 结论：**R71 五任务（A/B/C/D/E）全部完成，全量回归零红，门禁全绿，R72 可启动。**

## 一、五任务收口总表

| 任务 | 覆盖缺陷 | 交付物 | 状态 |
|------|----------|--------|------|
| A 三元表达式 | G-01（高） | parser_expr.py 三元分支不吞逗化 + parser_stmt.py 返回语句三元；`test_R71_A_三元表达式.light`；修复说明 `R71任务A_三元表达式_修复说明.md` | ✅ |
| B 增量赋值 | G-02（高） | parser_stmt.py 动词+为二元组（加为/减为/乘为/除为/取模为/幂为，双形态切词覆盖+成员/下标增量）；`test_R71_B_增量赋值.light` | ✅ |
| C 数值字面量 | G-03 + L-161 | lexer.py:915 前缀整数分支（0x/0X/0b/0B/0o/0O + 明确无效报错）；`test_R71_C_数值字面量.light`（63 断言）+ 反向哨兵 `test_R71_C_无效十六进制.light` / `test_R71_C_无效八进制.light`（EXPECT_RED 登记） | ✅ |
| D 字符串能力 | G-04 | lexer.py 原始字符串 r''/R'' + 三引号 '''/""" + 插值格式说明符 `{x:.2f}`；`test_R71_D_字符串能力.light`（49 断言） | ✅ |
| E stdlib 原语 | L-160 + L-174 | 纯光明真身 `stdlib/内置核心系统.light` + builtins.py 惰性转发（双副本）+ 双 codegen builtin_map；`test_R71_E_stdlib原语.light`（1000 次零碰撞）；交付说明 `R71任务E_stdlib原语_交付说明.md` | ✅ |

## 二、验收判据（任务书 §6.2/§6.3）

| 判据 | 结果 |
|------|------|
| 全量回归 `python -m pytest tests/test_回归.py -q` | **448 passed，零红**（含 5 个 R71 新用例 + 2 个 R71-C 反向哨兵，自动收集） |
| 反向用例登记 | R71-C 无效十六进制/八进制已入 `tests/test_回归.py` EXPECT_RED |
| 缺陷账更新 | `语言缺陷账.md` R71 批次小节：G-01/G-02/G-03/G-04/L-160/L-161/L-174 全部 ✅ 已修复 |
| 兼容性硬约束（§6.4） | 全部为新增语法/内置；现有 如果语句、普通赋值 `为`、数字/字符串字面量、`{变量}` 插值均不回归（回归零红实证） |
| 语句/表达式冲突要点 | `如果` 三态区分（语句块体冒号 / 表达式 则·那么 / 后置式）探针+用例双覆盖 |

## 三、门禁与度量（收口=合并点，基线已刷新）

| 门禁 | 结果 |
|------|------|
| 地板自举率 `floor_bootstrap.py` | 通过；**69.57% → 70.21%**（66/94），基线已刷新锁定 |
| 原生产品门禁 `native_product.py` | 通过；可编译比例 22.81%（↑），未实测/坏路径未增，基线已刷新 |
| CI 门禁测试（round9 + gates） | 68 passed |
| 存量清账 | 修复 `内置核心列表.light` 11 条清单证据行漂移（修复前地板门禁即红）；新增 `内置核心系统.light` 登记进两账 |

## 四、遗留与 R72 移交

1. **形式2 `?:` 不收录**：`?` 永不支持是既定口径（lexer A2-7，与可空类型抢词法位），
   报错已补三元写法指路——R72/R73 任务书涉及项无需再议。
2. **原生腿缺口**：R71 新内置（主目录/随机UUID）与 `随机字节`/`环境变量` 等真边界
   同口径，仅在 Python 腿；原生腿注册不在 R71 范围。
3. **R72 启动检查**（任务书 §八）：R71 已收口 → 确认 HEAD 全量回归零红 ✓ →
   可启动 R72 五任务（L-172 块级作用域 / L-159+L-175 字节 / G-05 解构 / G-06 推导式 / G-07 星号解包）。
4. B/C/D 三任务的独立修复说明文档未单独成文（实现要点已录缺陷账 R71 小节，
   测试用例自解释）；如需补齐按 A/E 报告模板归档至本目录。
