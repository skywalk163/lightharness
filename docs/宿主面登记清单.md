# 宿主面登记清单

> **任务来源**：R87 轮任务 C（`R87_agent任务分发_prompts.md`）
> **上游源**：内网 fork `192.168.1.5:3000/skywalk/deepseek-harness`，HEAD `877717787c`（已合 dsh-v0.1.7-alpha.1）
> **登记范围**：R85 各路遗留 7 号宿主面（HTTP / files-api / pi-ai 装配 / git 执行 / LibreOffice / Node 安装 / Cordis / FFI 等）+ A 路 §2-B 新登记的 0.1.7 宿主面包
> **文档性质**：登记清单（登记|已坐实|下轮），不改代码、不 commit/push

## 0. 字段说明

| 字段 | 含义 |
| --- | --- |
| 宿主能力 | 该行登记的宿主侧能力点（lightharness 需要的宿主实现面） |
| 上游实现面 | 上游 deepseek-harness 中承载该能力的位置（包/文件/钩子） |
| lightharness 现状 | 已实现 = 存在可运行纯逻辑/宿主模块；部分 = 已有纯逻辑但宿主接线缺；缺 = 无对应模块 |
| 三平台可行性 | Win / Linux / FreeBSD 三平台能否落地的判断（✓ 可行 / △ 受限 / ✗ 不可行） |
| 状态 | 登记 = 本清单新登记待坐实；已坐实 = 上游存在且 lightharness 已有对应模块；下轮 = 需后续轮次实现 |

## 1. 登记总览

| # | 宿主能力 | 状态 | 备注 |
| --- | --- | --- | --- |
| 1 | 真实 HTTP 客户端 / 传输层 | 已坐实 | `真实HTTP客户端.light` |
| 2 | files-api 文件服务接线 | 登记 | 0.1.7 增量主文件服务，未移植 |
| 3 | pi-ai 装配（login/auth/stream/provider discovery） | 登记 | llm 宿主对话未坐实 |
| 4 | git 执行（工作区变更 宿主钩子） | 已坐实 | `工作区变更.light` 宿主钩子["跑命令"] |
| 5 | LibreOffice 转 PDF（宿主钩子） | 已坐实 | `office转PDF.light` 宿主钩子["转换"] |
| 6 | Node 安装（插件管理 宿主钩子） | 已坐实 | `插件管理.light` 宿主钩子["执行安装"] |
| 7 | Cordis owner / catalog 装配 | 部分 | 纯逻辑已复刻，宿主接线缺 |
| 8 | FFI（win32-process / sandbox-windows-acl） | 登记 | R85-B/E 登记，未移植 |
| 9 | FreeBSD jail 沙箱后端 | 已坐实 | `沙箱_freebsd.light` + `freebsd/` 脚本 |
| 10 | node-pty FreeBSD 补丁 + process-inspector | 登记 | FreeBSD ps 语法宿主面 |
| 11 | ripgrep FreeBSD 回退 | 登记 | @vscode/ripgrep 无 FreeBSD 预编译，回退系统 rg |
| 12 | terminal-bash 平台默认 shell | 部分 | `终端.light` 已有，freebsd 分支待核实 |
| 13 | native flock 插件 + FreeBSD 预编译 | 登记 | 无 Node native 层 |
| 14 | web crypto.randomUUID 无头 polyfill | 登记 | webui 无 polyfill |
| 15 | settings-controller 无头开配置 | 部分 | `设置.light` 纯逻辑已复刻 |
| 16 | path-opener 平台命令分发 | 登记 | 无对应模块 |
| 17 | Gitea Actions CI + FreeBSD 构建脚本 | 已坐实 | `.gitea/workflows/ci.yml` + `freebsd/编译安装.sh` |
| 18 | API catalog / provider 注册 | 部分 | tool-cordis 数据注册表未移植 |
| 19 | sharp / attachment 图像原生处理 | 登记 | 原生图像库，无法移植 |
| 20 | sandbox-ssh 远端 SSH | 登记 | R85-E 登记，未移植 |

## 2. R85 遗留 7 号宿主面登记（C 路主责）

### 2.1 HTTP 传输层

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 真实 HTTP/HTTPS 客户端（fetch 传输、流式响应、SSE） | `packages/llm/llm-deepseek/src/transport/`（真实 HTTP 传输，R85-A 核对） | 已实现：`src/真实HTTP客户端.light`（TCP/套接字直连 + 流式读取，R40 已验证） | Win ✓ / Linux ✓ / FreeBSD ✓ | 已坐实 |
| adapter-failure / HTTP 错误传播（超时、限流、5xx 语义） | `packages/llm/` adapter 接线层（R85-A 登记） | 部分：传输层错误已实现，adapter 层语义未完整对齐 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| serialize 注入面（images / access / fileIds 请求字段装配） | `packages/llm/llm-deepseek/src/serialize.ts`（R85-A 登记） | 部分：请求拼装由 llm 对话模块承载，注入面未逐字段对齐 | Win ✓ / Linux ✓ / FreeBSD ✓ | 下轮 |

### 2.2 files-api 文件服务接线

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| files-api 文件服务（file-store 持久化、files-api 路由/控制器） | `packages/llm/llm-deepseek/src/` file-store / files-api（R85-A 登记，0.1.7 增量主文件服务） | 缺：无 files-api 模块；仅有 `真实文件系统提供者.light`（底层 FS 读写） | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| 文件引用解析 / 附件本地化 | `packages/fs/` attachment-local、file-reference-local（R85-E 登记） | 部分：本地路径读取已实现，normalization/sharp 依赖缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 2.3 pi-ai 装配

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| pi-ai provider 登录/鉴权（login/auth 会话） | `packages/llm/llm-pi-ai/src/` login/auth（R85-A 登记，需真实网络） | 缺：无 pi-ai 模块 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| pi-ai 流式对话（stream）与 provider discovery | `packages/llm/llm-pi-ai/src/`（R85-A 登记） | 缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| builtinModels / createProvider 装配（provider 注册表） | `packages/llm/` llm 装配层（R85-A 登记） | 缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 2.4 git 执行

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| git 命令执行（工作区变更 宿主钩子["跑命令"]，真实 git 调用） | `apps/web/src/` 工作区变更宿主钩子（R85-D 登记） | 已实现：`src/工作区变更.light` 宿主钩子["跑命令"]（R85-D 已坐实） | Win ✓ / Linux ✓ / FreeBSD ✓（均需系统 git） | 已坐实 |

### 2.5 LibreOffice 转换

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 文档转 PDF（office转PDF 宿主钩子["转换"]，调用 LibreOffice） | `apps/web/src/` 转换宿主钩子（R85-D 登记） | 已实现：`src/office转PDF.light` 宿主钩子["转换"]（R85-D 已坐实） | Win ✓ / Linux ✓ / FreeBSD △（需 liboffice 二进制） | 已坐实 |

### 2.6 Node 安装（插件管理）

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 插件依赖安装执行（插件管理 宿主钩子["执行安装"]，真实 npm/pnpm） | `apps/web/src/` 插件管理宿主钩子（R85-D 登记） | 已实现：`src/插件管理.light` 宿主钩子["执行安装"]（R85-D 已坐实） | Win ✓ / Linux ✓ / FreeBSD ✓ | 已坐实 |

### 2.7 Cordis 装配

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| Cordis owner 绑定（ReactLoopInbox / rootPools WeakMap） | `packages/agent/` agent-loop inbox、catalog rootPools（R85-C 登记） | 部分：inbox 异步化纯逻辑已复刻，Cordis owner 宿主机制缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| api-catalog 数据注册表（tool-cordis 7300 行数据） | `packages/catalog/` api-catalog（R85-D 登记） | 缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| typert registry / loader / cordis-catalog | `packages/typert/`（R85-D 登记） | 缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 2.8 FFI

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| win32-process ABI / FFI / koffi 原生绑定 | `packages/subprocess/subprocess-win32/` win32-process（R85-B 登记） | 缺：无 koffi/native 绑定（已有 `stdlib/进程树.light` 纯逻辑） | Win ✓ / Linux ✗ / FreeBSD ✗ | 登记 |
| sandbox-windows-acl FFI / ACL（Windows 沙箱 ACL） | `packages/sandbox/sandbox-windows-acl/`（R85-E 登记） | 缺 | Win △ / Linux ✗ / FreeBSD ✗ | 登记 |
| Linux 沙箱后端（bwrap / landlock，0.86 无 sudo 时 fail-closed） | `packages/sandbox/sandbox-local/`（R85-F 核对） | 部分：`沙箱.light` 已实现，Linux 后端可用性受 bwrap/landlock 约束（R85-F：无 sudo 时 fail-closed） | Win ✗ / Linux △（需 bwrap/landlock） / FreeBSD ✓ | 登记 |

## 3. A 路 §2-B 新登记宿主面（0.1.7 增量）

> 上游宿主面包：host / subprocess / terminal / sandbox / fs / native / freebsd / patches / vendor / .gitea / .github / scripts / website

### 3.1 FreeBSD jail 沙箱后端

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| FreeBSD jail 沙箱执行（dsh-jail-run） | `freebsd/dsh-jail-run.c`（cc -O2 -ljail 编译）、`freebsd/dsh_web.rcd`（rc.d 服务脚本）、`packages/sandbox/sandbox-local` | 已实现：`src/沙箱_freebsd.light`（对标 #209 jail confine）+ `freebsd/dsh-jail-run.c`、`freebsd/dsh_web.rcd`、`freebsd/编译安装.sh`、`freebsd/远程回归.sh`、`freebsd/verify-sandbox.mjs`（已坐实） | Win ✗ / Linux ✓（需 jail 类机制，转 sandbox 其它后端）/ FreeBSD ✓ | 已坐实 |
| boxrun 补丁（box-runner 集成） | `patches/boxrun-dsh.patch` | 缺 | Win ✗ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.2 node-pty FreeBSD 补丁 + 进程检视

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| node-pty FreeBSD 编译补丁（PTY fork/exec） | `patches/node-pty@1.2.0-beta.15.patch` | 部分：`stdlib/伪终端.light` / `异步PTY` 已实现纯逻辑，非 node-pty 原生 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| process-inspector FreeBSD ps 语法（ps -axo 行解析） | `packages/subprocess/subprocess-local/src/process-inspector.ts`（FreeBSD ps 行格式） | 部分：`异步子进程.light` 已有进程查检，freebsd ps 语法待对齐 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.3 ripgrep FreeBSD 回退

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 文件内容搜索：@vscode/ripgrep 打包二进制 + 系统 rg 回退（FreeBSD 无预编译则回退系统 rg，raw argv 无 shell 引用） | `packages/fs/tool-fs-search/src/search-core.ts`（PACKAGED ripgrep + 系统回退逻辑） | 部分：`src/工具_搜索文件.light` / `搜索提供商.light` 已实现搜索；FreeBSD 系统 rg 回退分支未对齐 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.4 terminal-bash 平台默认 shell

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 平台感知默认 shell（FreeBSD 分支解析默认 shell /bin/sh，放行非 bash-only flags；shellDialect/shellPath/shellArgs 默认） | `packages/terminal/terminal-bash/src/config.ts`（第 23 行 freebsd 分支） | 部分：`src/终端.light` 已实现终端；平台默认 shell 分支待对齐 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.5 native flock 插件 + FreeBSD 预编译

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| flock 文件锁 native 插件（node-api napi8） | `native/system/`、`native/system/packages/freebsd-x64/`（bin/system.node 预编译，prebuilds.json 声明） | 缺（无 Node native 层；锁语义可走 `stdlib/` 事件循环 + 文件系统实现） | Win △ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.6 web crypto.randomUUID 无头 polyfill

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 无头（非安全上下文 / LAN IP http）crypto.randomUUID polyfill | `apps/web/src/polyfill-crypto.ts`（`__dshCrypto.randomUUID`） | 缺：`webui/app.js` 无 polyfill（webui 尚为静态前端） | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.7 settings-controller 无头开配置

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 无头开文档策略（Config.nativeOpen，openNativePath/openNativeTextFile 分发；无头环境且不可 nativeOpen 时禁用开文档） | `packages/api/settings-controller/src/index.ts`（构造器对齐，R87 分发书载明 fork 已对齐） | 部分：`src/设置.light`（settingsNamespace/deepEqualJson/mergeLayers/redactSecrets 纯逻辑已复刻）；configuration/nativeOpen 宿主开文档接线缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.8 path-opener 平台命令分发

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 平台命令分发打开路径/文本（darwin/linux/wsl 分支、shell-free） | `packages/util/native-command/src/path-opener.ts` | 缺（与 3.7 同属开文档宿主面，可合并坐实） | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.9 Gitea Actions CI + FreeBSD 构建脚本

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| Gitea Actions CI（7 个 yml：ci/docs-pages/e2b-e2e/e2e/expected-filenames/pi-ai-provider-e2e/sandbox） | `.gitea/workflows/`（内网 192.168.1.5:3000 检出 light-merge 编译器，docs/CI.md 载明） | 已实现：`.gitea/workflows/ci.yml` 存在 | Win ✓ / Linux ✓ / FreeBSD ✓ | 已坐实 |
| GitHub Actions CI（21 个：build-exe/node-addon-system/release 等） | `.github/workflows/` | 已实现：`.github/workflows/ci.yml` 存在 | Win ✓ / Linux ✓ / FreeBSD ✓ | 已坐实 |
| FreeBSD 构建脚本（build-new.sh / build-lib.sh / dsh-web-run.sh / dsh-web-restart.sh / probe-binary.sh / push.sh / run-e2e.sh / run-verify.sh / probe freebsd 二进制） | `freebsd/` 9 个脚本 + `scripts/`（dev-web 等） | 已实现：`freebsd/编译安装.sh`、`freebsd/远程回归.sh`（功能对应 build-new/dsh-web-run/run-e2e）；dsh_web.rcd 同名存在 | Win ✗ / Linux ✗ / FreeBSD ✓ | 已坐实 |
| vendor 依赖包（cordis/cosmokit/schemastery/loader/logger-console/hmr/group/timer/include） | `vendor/`（上游内联 vendor） | 登记（lightharness 不引 Node 依赖，纯逻辑已含 schemastery 模式描述） | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

### 3.10 host 包 / website 目录

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| 原生目录选择对话框（win32-dialog bindings/logic/worker，真实系统对话框） | `packages/host/directory-picker/`（含 native win32-dialog-*） | 缺（对话框宿主面未移植） | Win ✓ / Linux △ / FreeBSD ✗ | 登记 |
| frontend-static 静态前端托管 | `packages/host/frontend-static/` | 缺（webui 为本地静态文件，未走前端托管服务） | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| open-in-app 应用内打开 | `packages/host/open-in-app/` | 缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| plugin-inventory 插件盘点 | `packages/host/plugin-inventory/` | 部分（`插件管理.light` 已有，盘点宿主缺） | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| webserver 宿主 HTTP 服务 | `packages/host/webserver/` | 缺（真实HTTP客户端.light 为客户端侧，服务端宿主缺） | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| website 渲染与截图（dev-web / run-web-snapshots） | `website/` + `scripts/dev-web.ts`、`scripts/run-web-snapshots.ts` | 缺 | Win ✓ / Linux ✓ / FreeBSD △ | 登记 |

## 4. 附：R85-E 等报告额外宿主面登记

| 宿主能力 | 上游实现面 | lightharness 现状 | 三平台可行性 | 状态 |
| --- | --- | --- | --- | --- |
| fs lstat/stat + session 事件（交付呈现宿主） | `packages/fs/tool-fs/`（R85-D 登记） | 已实现：`交付呈现.light` | Win ✓ / Linux ✓ / FreeBSD ✓ | 已坐实 |
| fs/tool-fs sandbox 策略联动真实 FS | `packages/fs/tool-fs/` sandbox 联动（R85-E 登记） | 已实现：`沙箱策略.light` + `真实文件系统提供者.light` | Win ✓ / Linux ✓ / FreeBSD ✓ | 已坐实 |
| subprocess index / managed-owner（真实 spawn 内核调用） | `packages/subprocess/subprocess-local/src/index.ts`（R85-B 登记） | 已实现：`异步子进程.light`（R85-B 复刻） | Win ✓ / Linux ✓ / FreeBSD ✓ | 已坐实 |
| 真实 execve / systemd-run / Job API / PTY fd 判定 | `packages/subprocess/` 后端（R85-B 登记） | 部分：PTY 已有（`stdlib/伪终端.light`），execve/systemd-run/Job API 宿主调用缺 | Win △ / Linux ✓ / FreeBSD ✓ | 登记 |
| session-controller 客户端会话管理（队列镜像/投影存储/快照） | `packages/api/session-controller/`（R85-C 登记） | 部分：`webhook会话.light` / 会话上下文已实现，队列镜像/投影存储/快照未对齐 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| session-cwd 宿主（会话工作目录） | `packages/fs/`（R85-E 登记） | 部分：会话上下文已实现，cwd 宿主接线待核对 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| compaction checkpoint / 凭据签名 | `packages/context/` compaction（R85-E 登记） | 部分 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| summarizer recover / LLM 注入 | summarizer（R85-E 登记） | 部分 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| command-compact 装配 | （R85-E 登记） | 部分 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| workflow-ptc 线程编排 / worker 加载 | （R85-E 登记） | 部分 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| attachment sharp 图像处理 | attachment-local（R85-E 登记） | 缺（sharp 原生依赖） | Win ✓ / Linux ✓ / FreeBSD △ | 登记 |
| shell bash/pwsh 真实 shell 执行 | `packages/shell/`（R85-E 登记） | 部分：`终端.light` 已实现，bash/pwsh 真实调用待坐实 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| sandbox-ssh 远端 SSH | `packages/sandbox/sandbox-ssh/`（R85-E 登记） | 缺 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| time-context 时钟 | （R85-E 登记） | 部分 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| agent-presets PackageResolves 注入 | （R85-E 登记） | 部分 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |
| permission-presets Auto hook / projection | （R85-E 登记） | 部分 | Win ✓ / Linux ✓ / FreeBSD ✓ | 登记 |

## 5. 状态汇总与下轮建议

| 状态 | 条数 | 说明 |
| --- | --- | --- |
| 已坐实 | 11 | HTTP 传输 / git 执行 / LibreOffice / Node 安装 / FreeBSD jail / Gitea+GH CI / FreeBSD 构建脚本 / fs lstat、sandbox 联动、subprocess index（R85-D/E/B） |
| 登记 | 41 | 新登记待实现或宿主接线缺（files-api、pi-ai、FFI、Cordis、native flock、crypto polyfill、path-opener、sharp、sandbox-ssh、vendor 等） |
| 下轮 | 1 | serialize 注入面逐字段对齐 |

**下轮优先项**（按依赖与价值排序）：
1. **files-api + pi-ai 装配**（0.1.7 增量主文件服务 + llm 宿主对话，支撑真实对话闭环）
2. **Cordis owner / api-catalog 注册表**（打通 agent 装配与工具目录）
3. **terminal-bash 平台默认 shell + process-inspector freebsd ps 语法**（三平台补位 B 路对接）
4. **settings-controller / path-opener 开文档宿主**（单一坐实项，可合并）
5. **native flock + web crypto polyfill**（轻量，一并对齐 0.1.7 增量）

---

*本清单由 R87-C 产出，覆盖 R85-A/B/C/D/E/M 六份报告 + R87 增量清单 §2-B；红线遵守：仅文档产出、未改代码、未 commit/push。*