# _task3_R32_MERGE_WHOLE第一批精简 —— 前 5 条逐条验证（G1∧G2∧G3）

> R32 任务3（P0）｜ 修改区域：`light-merge/src/lexer.py` `_P0A_MERGE_WHOLE`
> 条目：导出事件表 / 整理模型消息 / 退出码 / 接收参数 / 非空块

## 一、结论

| 条目 | 语料命中 | G1 语料门 | G2 编译门 | G3 边界门 | 判定 |
|---|---|---|---|---|---|
| 导出事件表 | 7 处/2 文件 | ✅ 0 变化 | ✅ rc=0 | ✅ 5 形态一致 | **删除** |
| 整理模型消息 | 0 处 | ✅ 0 变化 | ✅ rc=0 | ❌ F5 传参位劈开 | **保留（真护栏）** |
| 退出码 | 156 处/37 文件 | ✅ 0 变化 | ✅ rc=0 | ✅ 5 形态一致 | **删除** |
| 接收参数 | 6 处/3 文件 | ✅ 0 变化 | ✅ rc=0 | ✅ 5 形态一致 | **删除** |
| 非空块 | 8 处/1 文件 | ✅ 0 变化 | ✅ rc=0 | ❌ F2/F5 劈开 | **保留（真护栏）** |

第一批 5 → 3 删 2 留；两批合计 10 → 3（精简 70%）。

## 二、场景定位（3.1）

语料 856/857 个 `.light` 全文扫描（含注释行）：

- `导出事件表`：`lightharness/examples/_helper_L004.light`(3)、`test_L004.light`(4)
  —— L-004 家族历史雷区用例，全部处于**成员访问/设名位**；
- `整理模型消息`：语料 0 命中（防御性条目，§8.4 点名的 6 个历史雷区完整词形）；
- `退出码`：37 文件 156 处（系统接口/代理工具集/伪终端/主程序 等），全部处于
  成员访问（`结果.退出码`）或设名位；
- `接收参数`：3 文件 6 处（test_L020/test_L022/函数增强），段落名与设名位；
- `非空块`：test_L030 8 处，设名位/成员访问。

## 三、逐条验证（3.2/3.3）

### G1 语料判据（变化文件数=0 通过）

方法：由编辑前快照（`_r32_lexer_head.py` = `git show HEAD:src/lexer.py`）行级手术
生成「仅移除该条」变体模块，对全语料 tokenize，与基线逐文件比对 sha256。
锚点断言保证变体与本任务改动**有且仅有**该条差异。

```
G1 导出事件表  通过  变化文件=0  新增错误=0
G1 整理模型消息 通过  变化文件=0  新增错误=0
G1 退出码      通过  变化文件=0  新增错误=0
G1 接收参数    通过  变化文件=0  新增错误=0
G1 非空块      通过  变化文件=0  新增错误=0
第一批并集删除(3 条)  变化文件=0  ✅ token 零变化
```

### G3 边界门（5 种形态 token 流逐 token 一致）

| 形态 | 导出事件表 | 整理模型消息 | 退出码 | 接收参数 | 非空块 |
|---|---|---|---|---|---|
| F1 设名位 `设 X 为 1` | 一致 | 一致 | 一致 | 一致 | 一致 |
| F2 函数名 `返回 X(1)` | 一致 | 一致 | 一致 | 一致 | **变化** |
| F3 成员访问 `结果.X` | 一致 | 一致 | 一致 | 一致 | 一致 |
| F4 段落名 `段落 X:` | 一致 | 一致 | 一致 | 一致 | 一致 |
| F5 传参 `f(X, 1)` | 一致 | **变化** | 一致 | 一致 | **变化** |

打红明细（撤后整串被关键字劈开 → 真护栏）：

- `整理模型消息` F5：`IDENTIFIER:整理模型消息` → `IDENTIFIER:整理 + KEYWORD:模 +
  IDENTIFIER:型消息`（模 为嵌入扫描关键字）；
- `非空块` F2/F5：`IDENTIFIER:非空块` → `KEYWORD:非 + IDENTIFIER:空块`
  （非 为逻辑运算符）。

### G2 编译门

边界用例 `lightharness/examples/test_R32_MERGE_WHOLE精简边界.light`
（设名位 + 函数名 + 传参调用位，覆盖本批删除/保留条目）：
移除前 rc=0、移除后 rc=0（`python 运行.py` 运行，13 断言全过）。

## 四、执行删除（3.4）

- lexer.py `_P0A_MERGE_WHOLE`：移除 导出事件表/退出码/接收参数（整理模型消息/
  非空块 保留），同步类 docstring、定义处与 :2268 调用点注释；
- 删除后全量反跑：`git show HEAD:src/lexer.py`（pre-R32）↔ 工作区，
  **857 文件 token 序列零变化、token 总数差 +0**（`_antirun_r30_t3_全量反跑.py`
  snapshot/compare 口径，快照 `_r32_head_快照.json`/`_r32_cur_快照.json`）。

## 五、为什么删除后整串仍整词（机制归因）

撤出 MERGE_WHOLE 后这些形态由既有通用规则接住，与 R23/R30 一脉相承：

1. **设名/段落名预扫描**（`_scan_user_definitions`）：`设 导出事件表 为 …`、
   `段落 退出码:` 的名字先行登记，收集层 `full_seq in user_definitions` 整体成词；
2. **函数调用语境整串合并**：整串后紧跟 `(` 时按标识符输出（R20 嵌入扫描口径
   上提，R30 幂次同款）；
3. **成员访问规则**（:1833 一带）：`对象.退出码` 的 `.` 后段不再按关键字边界拆分。

「精确整串匹配」语义决定了删除零外溢：不在集合中的 `接收数`、`导出JSON` 等
仍走原路径。

## 六、交付物

- `light-merge/src/lexer.py`（_P0A_MERGE_WHOLE 10→3，本批贡献 删2）
- `lightharness/_antirun_r32_t3_MERGE_WHOLE第一批.py`（本批逐条反跑，t3 口径）
- `lightharness/_antirun_r32_t34_MERGE_WHOLE逐条验证.py`（共用引擎）
- `lightharness/_r32_t3_g1_结果.json` / `_r32_t3_g1.log`（G1 证据）
- `lightharness/_r32_t34_g3_边界门.py` + 运行输出（G3 证据）
- `lightharness/examples/test_R32_MERGE_WHOLE精简边界.light`（G2 用例，前后 rc=0）
- `lightharness/_r32_head_快照.json` / `_r32_cur_快照.json` + compare 输出（零变化证据）
