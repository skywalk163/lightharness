# R23 任务3 交付物：COMMON_COMPOUND_WORDS 内建名迁移 / 清表清单

> 日期：2026-09-14 ｜ 轮次：第23轮 任务3（阶段C 部分）
> 修改文件：`light-merge/src/lexer.py`（`COMMON_COMPOUND_WORDS` 定义；**未改**任何引用逻辑）
> 语料规模：824 个 `.light` 文件（lightharness/examples+src+tests、light-merge/examples+stdlib+bootstrap+src+tests）
> 结果：**184 → 37 条**（删 147 条 = 79.9%），全语料 token 序列零变化，codegen 产物不变

## 一、判据（双中立，逐条独立验证，不批量删除）

| # | 判据 | 含义 | 工具 |
|---|---|---|---|
| ① | 语料中立 | 撤掉该条 → 全语料 824 文件 token 序列零变化 | `_r23_t3_ccw_sweep.py` → `_r23_t3_ccw_sweep.json` |
| ② | 隔离中立 | 撤掉该条 → **该词自身**单独 tokenize 序列零变化 | `_r23_t3_ccw_isolation.py` → `_r23_t3_ccw_isolation.json` |

①②同时成立 ⇒ 冗余，删除；否则保留并记录。**取最严口径**（仅①成立、②不成立者仍保留）。

## 二、分类矩阵（184 条）

| 分类 | 条数 | codegen 登记归属 | 处置 |
|---|---|---|---|
| ① 内建名 | 45 | `code_generator.builtin_map`（**已登记**） | 全语料/隔离双中立 ⇒ 删除 |
| ② 运算符/异常名 | 15 | `operator_map` / `exception_name_map`（**已登记**） | 双中立 ⇒ 删除 |
| ③ 无 codegen 映射 | 87 | 无（由 STDLIB_VERB_ARITY / 预扫描 / 上下文规则覆盖） | 双中立 ⇒ 删除 |
| ④ 保留·A 组 | 17 | — | **保留**（其中 14 条仅隔离非中立，3 条条件中立） |
| ⑤ 保留·B 组 | 20 | — | **保留**（全部仅隔离非中立） |
| | **184** | | |

> **重要结论（code_generator.py 未修改的原因）**：分类①的 45 条内建名**全部早已**登记在
> `src/code_generator.py` 的 `builtin_map` 中（静态 AST 复核见反跑 [G]），"迁移"即"确认登记已存在、
> 词法层不再白名单"；任务书「如果没有，新增登记」的前提不成立，新增即为重复登记。
> 分类②同理由 `operator_map` / `exception_name_map` 承载。
> 分类①中**不该**新增登记的另两类已显式排除：
> ① `异步读取文件/异步写入文件/异步追加文件`——无任何实现，补 `builtin_map` 指向不存在的目标
>    只是把 NameError 换成 AttributeError，属第二轮总纲 §5 明令禁止的「词法白名单式假实现」；
> ② 有真实现的 `并发等待/异步睡眠/限时/创建任务/首个完成` 等**已在** `builtin_map` 中，无需新增。

## 三、删除明细（147 条）

### 3.1 分类①：builtin_map 内建名（45 条）

- `FFI禁用调试`
- `FFI获取日志`
- `FFI调试`
- `FFI错误`
- `二进制`
- `位域获取`
- `位域设置`
- `低级写`
- `低级打开`
- `低级读`
- `余弦`
- `分配内存`
- `创建任务`
- `创建函数指针`
- `创建回调`
- `创建数组`
- `创建类型别名`
- `十六进制`
- `参数列表`
- `反余弦`
- `反正切`
- `反正切2`
- `反正弦`
- `取地址`
- `向上取整`
- `向下取整`
- `四舍五入`
- `定义宏`
- `平方根`
- `指针偏移`
- `是空`
- `最大公约数`
- `注册回调`
- `注销回调`
- `系统错误码`
- `获取回调`
- `获取宏`
- `解引用`
- `追加`
- `释放内存`
- `限时`
- `随机整数`
- `随机浮点`
- `随机选择`
- `首个完成`

### 3.2 分类②：operator_map / exception_name_map（15 条）

- `值错误`
- `停止迭代`
- `内存错误`
- `右移`
- `属性错误`
- `左移`
- `文件错误`
- `断言错误`
- `溢出错误`
- `系统错误`
- `索引错误`
- `运行时错误`
- `迭代停止`
- `递归错误`
- `键错误`

### 3.3 分类③：无 codegen 映射（87 条）

- `std`
- `一次性`
- `三合一`
- `乘客`
- `乘法`
- `乘积`
- `乘除`
- `二选一`
- `伽马函数`
- `位取反`
- `余切`
- `倍数`
- `值列表`
- `元素列表`
- `八进制`
- `减少`
- `减弱`
- `减速`
- `函数`
- `初值`
- `加入`
- `加减`
- `加强`
- `加快`
- `十进制`
- `千分位`
- `协`
- `协方差`
- `历史记录`
- `压缩模`
- `去除`
- `各加`
- `名称列表`
- `均值`
- `外部错误`
- `常数`
- `年级当量`
- `库路径`
- `循环迭代`
- `总人数`
- `总次数`
- `总资产增长`
- `按值传递`
- `推迟`
- `收缩`
- `教育年龄`
- `数字列表`
- `数据聚合函数`
- `文件列表`
- `文本前缀`
- `方差`
- `方差和`
- `星期一`
- `星期三`
- `星期二`
- `星期五`
- `星期六`
- `星期四`
- `星期日`
- `最小公倍数`
- `最长公共子序列`
- `期值`
- `标准差`
- `模式`
- `清除`
- `状态码错误`
- `环节1`
- `环节2`
- `百分位`
- `百分比格式`
- `目标函数`
- `社会福利`
- `编译正则表达式`
- `行列表`
- `记录列表`
- `记录调用`
- `误差函数`
- `贝塔函数`
- `财务配股价格`
- `跨平台库`
- `输入文本`
- `过滤模`
- `追踪内存`
- `配置文件路径`
- `键列表`
- `除法`
- `项目列表`

## 四、保留明细（37 条）

> **保留是保守选择**：多留一条冗余条目**不是回归**（删除才是风险，已由反跑 [C]/[D] 证明安全）。
> 下表分类由反跑 **`[D2]` 在合并态 lexer（任务1+2+3）下实测**给出。

### 4.1 必要性分类总览

| 分类 | 条数 | 判据 |
|---|---|---|
| 仅隔离非中立 | **34** | 撤条 → 该词**自身**单独 tokenize 变化（隔离护栏） |
| 条件中立 | **3** | 撤条后语料与自身**皆不变** ⇒ 合并态下已冗余，本轮保守保留，**建议后轮追加删除** |

### 4.2 仅隔离非中立（34 条）—— 撤条后切分

| 词 | [D2] | 撤掉该条后的切分 |
|---|---|---|
| `位与` | self | 位 / 与(KW) |
| `位异或` | self | 位异 / 或(KW) |
| `位或` | self | 位 / 或(KW) |
| `位非` | self | 位 / 非(KW) |
| `函数对象` | self | 函数(KW) / 对象 |
| `导入错误` | self | 导入(KW) / 错误 |
| `常量时间比较` | self | 常量(KW) / 时间比较 |
| `幂次` | self | 幂(KW) / 次 |
| `并发等待` | self | 并(KW) / 发 / 等待(KW) |
| `应当` | self | 应 / 当(KW) |
| `开启调试` | self | 开启(KW) / 调试(KW) |
| `异步写入文件` | self | 异步(KW) / 写入文件 |
| `异步睡眠` | self | 异步(KW) / 睡眠 |
| `异步读取文件` | self | 异步(KW) / 读取文件 |
| `异步追加文件` | self | 异步(KW) / 追加文件 |
| `当前` | self | 当(KW) / 前 |
| `当然` | self | 当(KW) / 然 |
| `最终答案` | self | 最终(KW) / 答案 |
| `枚举值` | self | 枚举(KW) / 值 |
| `测试_生成问候语` | self | 测试_ / 生成问候语 |
| `生成分析` | self | 生成(KW) / 分析 |
| `生成问候语` | self | 生成(KW) / 问候语 |
| `类型错误` | self | 类型(KW) / 错误 |
| `终点` | self | 终(KW) / 点 |
| `结构体值` | self | 结构体(KW) / 值 |
| `记录类型` | self | 记录 / 类型(KW) |
| `设指针` | self | 设(KW) / 指针 |
| `设指针值` | self | 设(KW) / 指针值 |
| `设系统` | self | 设(KW) / 系统 |
| `设系统错误码` | self | 设(KW) / 系统错误码 |
| `设置` | self | 设(KW) / 置 |
| `设置数组` | self | 设(KW) / 置数组 |
| `除非` | self | 除 / 非(KW) |
| `零除错误` | self | 0 / 除错误 |

（KW = KEYWORD。这两张表说明：撤条后这些词会被切成「关键字 + 标识符」，
故它们确实承担了保护职责。）

### 4.3 条件中立（3 条）—— 本轮保守保留

| 词 | 含该词的语料文件数 | 语料变化 | 自身变化 | 备注 |
|---|---|---|---|---|
| `低级关闭` | 7 | 0 | 否 | 原防切 `低级` + `关闭`（多字关键字），已被**任务2 后缀位置上下文规则**通用覆盖 |
| `正则匹配` | 21 | 0 | 否 | 原防切 `正则` + `匹配`，同上 |
| `环境枚举` | 6 | 0 | 否 | 原防切 `环境` + `枚举`，同上 |

> **与 20:16 初次扫描的差异**：初次扫描时这 3 条尚非中立（语料变化 3/8/3 文件，
> 见 `_r23_t3_ccw_sweep.json` 的 `guard` 段）；其后 `lexer.py` 被并行改动
> （任务2 落地「后缀位置上下文规则」）⇒ 转为中立。本清单以**合并态实测**为准。
> 建议：任务2 规则稳定后，在阶段D 追加删除，届时 CCW **37 → 34**。

## 五、验证与证据

| 项 | 结论 | 证据文件 |
|---|---|---|
| 全语料 token 零变化（逐条扫描） | 147 条删除条目逐条零变化 | `_r23_t3_ccw_sweep.json` |
| 全语料 token 零变化（批量补回） | 一次性补回 147 条 → 零变化（反跑 [C]） | `_task3_R23_CCW精简_证据.json` |
| **删除条目逐条冗余**（反跑 [D]） | 147/147 逐条撤离零变化 | 同上 |
| 保留条目必要性分类（反跑 [D2]） | 34 仅隔离非中立 ｜ 3 条件中立 | 同上 |
| 反跑 ALL OK | A~G 全 PASS | `_antirun_r23_t3_CCW内建名迁移.py` |
| codegen 输出不变 | 探针编译产物 sha256 两侧一致（`17df02f1…`） | 同上 [F] |
| 迁移登记复核 | 分类① 45 全在 `builtin_map`，分类② 15 在其它映射 | 同上 [G] |
| 交付报告 | — | `_task3_R23_CCW内建名迁移_交付报告.md` |

