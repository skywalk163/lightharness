# 任务5（R27）排除集精简 + 联动评估 + 性能

> 评估基准：`light-merge/src/lexer.py` 工作树状态（2026-09-15 12:34 快照，3822 行）
> —— 任务1（方案A：`_P0A_HEAD_MERGE_DUAL` 8字）与任务2（`_R27_BCLASS` 8字 +
> 递归抑制条件扩展为 `CS ∪ DUAL ∪ B类`）**已由并行路落地**；任务3（CS 表清零）尚未落地
> （`_COMPOUND_SAFE_SINGLE_KEYWORDS` 仍为 16 字），并行路调试中（PID 9068）。

---

## 5.1 排除集精简 —— 结论：方案A落地，42字保持不变 ✅

| 项 | 结论 | 证据 |
|---|---|---|
| `_P0A_HEAD_SPLIT_SINGLE` | **保持 42 字，无需改 34 字**（任务1采用方案A） | `:3770` 定义为 `frozenset(Lexer._P0A_SUFFIX_SPLIT_SINGLE) \| _R26_STMT_HEAD_SINGLE` = 21 ∪ 21 = 42 字；运行时实测 42 字 ✅ |
| 语义清晰确认 | 42 字 = 词尾排除 21 字 + 语句头 21 字，两段职责明确，无 A类 8字（乘减加除模真空到）误入 | SUFFIX 21 字（`与且为之乘于假减到加在幂或模步真空等至除非`）为五类排除；STMT_HEAD 21 字（`设己父从导返若并终现当异宏匹否承抛捕掷跃遍`）为语句头关键字，语义自洽 |
| `_P0A_SUFFIX_SPLIT_SINGLE`（词尾排除集 21 字） | **不需要同步修改** | `:3215` 定义为 `_P0A_OP \| _OPERATOR_KEYWORDS \| _P0A_NEVER_SPLIT \| _AWAIT_KEYWORDS \| _VALUE_LITERAL_KEYWORDS` 的单字子集 —— 纯词法切分固有依据，不依赖 CS 表存在与否 |
| 文末自校验断言 | 待任务3清表后由 `CS==(F∩CS)∪DUAL` 调整为 `CS==∅` | 当前断言区仍为 R26 断言（`:3778-3779` `_P0A_HEAD_MERGE_SINGLE - CS == _R26_CS_REMOVED`）；CS 清零后该断言仍需按任务书更新。**本任务不触碰**（铁律：清表属任务3区域） |

**5.1 小结**：方案A下本任务**无需任何代码修改**；`_P0A_HEAD_SPLIT_SINGLE` 保持 42 字
语义清晰，`_P0A_SUFFIX_SPLIT_SINGLE` 派生自五类词法依据、与 CS 无耦合，联动评估通过。

---

## 5.2 联动保护表评估（只出结论，不执行删除）—— 结论：五张表全部保留 ✅

| 保护表 | 规模 | CS清表后是否仍需要 | 评估结论 |
|---|---|---|---|
| `COMMON_COMPOUND_WORDS`（CCW） | 37 条（17 真护栏 + 20 隔离非中立） | **需要，全部保留** | CCW 为逐词整串白名单（独立于 `_P0A_MERGE_WHOLE` 的 9 条有界精确整串集合，`:2238` 注释明确区分两者），保护场景由「第21轮上下文敏感最大匹配 + R23词尾并入 + 内建登记」覆盖，**与单字 CS 递归完全正交**；R22 已按最严双中立口径逐条验证保留（184→37），无新增清表理由。删除需逐条重新验证（后续轮次） |
| `_EMBED_MAX_MATCH_KEYWORDS`（为/返回/尝试） | 3 条 | **需要，全部保留** | 嵌入扫描最大匹配关键字集合（引用 `:2255/:2273`），与单字 CS 递归无关；CS 清表不改变嵌入扫描语义 |
| `OPERATOR_VERBS` | 19 条 | **需要，全部保留** | 运算符语义核心：`_P0A_OP`（`:3146`）与 `_OPERATOR_KEYWORDS`（`:33`）均由它推导，表达式切分 7 处引用（`:2501/:2709/:2847/:2876/:2905/:3004/:3011`）。CS 清表后 A类 8字词首并入改由 DUAL 规则接管，**运算符切分职责不变** |
| `_P0A_MERGE_WHOLE` | 9 条 | **需要，全部保留** | 有界精确整串合并（导出事件表/整理模型消息/退出码/接收参数/非空块/外部命令/排序依据/输出块表/返回码），与「单字词首并入」正面规则**不重复**：前者是 2-4 字整词、后者只处理单字；`_:2236` 引用 |
| `_P0A_NEVER_SPLIT`（模/步/至/到） | 4 字 | **需要，全部保留** | 「模/到」虽已入 DUAL 类别，但 NEVER_SPLIT 语义是**词内**切分禁止（`模型/模块/模式/模拟` 2900+ 成分、`1至10/1到10步2` 数字范围），DUAL 只覆盖**词首**并入，两者保护面互补不重叠（`:3170` 定义） |

**5.2 小结**：CS 表清空对五类联动保护表的语义均不构成冲击；**本轮不删除任何一条**。
其中 CCW 若考虑后续精简（37→更低），须按 R23 双中立口径**逐条独立验证**（语料中立 +
隔离中立），属后续轮次工作。

---

## 5.3 `_skip_compound_safe_and_match` 死代码评估 —— 结论：**非死代码，保留** ⚠️

**任务书预设**：CS 表清零后 `candidate in _COMPOUND_SAFE_SINGLE_KEYWORDS` 恒为 False，
递归永不触发 → 死代码 → 删除函数 + 相关注释 + `_match_keyword` 递归调用。

**实际代码（任务2落地后）推翻该预设**：

```
# :1244-1246（_match_keyword 内联快速路径）
if (candidate in _COMPOUND_SAFE_SINGLE_KEYWORDS
        or candidate in _P0A_HEAD_MERGE_DUAL      # ← A类 8字，非空！
        or candidate in _R27_BCLASS) and pos + 1 < text_len:   # ← B类 8字，非空！
    kw, l = self._skip_compound_safe_and_match(text, pos + 1, text_len)

# :1327-1330（_skip_compound_safe_and_match 本体递归入口，同样的三集合条件）
```

- 任务2 为保持「B类 8 字在『后随关键字（非汉字）』语境的全位置吸收」（对应/对于/
  对象/类的…），**主动扩展**了递归入口条件为 `CS ∪ DUAL ∪ B类` 三集合并集。
- 因此 **CS 清零后 `candidate in _P0A_HEAD_MERGE_DUAL` 或 `candidate in _R27_BCLASS`
  仍可命中** → `_skip_compound_safe_and_match` 仍被调用、递归深度仍非 0。
- 函数引用面（`:1247/:1330` 两处调用 + `:1256` 定义 + `:1214/:1215` 内联注释）**全部存活**。

**结论**：该函数当前（含 CS 清零后）**均非死代码**，不得按任务书 5.3 原预设删除。
任务书该条预设未考虑任务2 的递归条件扩展（旧假设：递归仅由 CS 触发）。
若未来某轮想删除，必须先把 DUAL/B类 的「全位置吸收」也正面规则化并逐字通过
G1/G2/G3 编译门——属后续轮次大规模重构，本轮明确**保留**。
（附：CS 项在清表后恒 False，属**可精简项**而非函数删除依据；精简建议见 5.4。）

---

## 5.4 `compound_safe_single_keywords` 类属性评估 —— 结论：CS清表后可清理 CS 项，三处 if 保留 ⚠️

| 引用点 | 实际行号（任务2后） | CS清表后的状态 |
|---|---|---|
| 类属性定义 `compound_safe_single_keywords = _COMPOUND_SAFE_SINGLE_KEYWORDS` | `:608` | 清表后为空 frozenset → 类属性本身可删 |
| 嵌入式扫描① `sub_kw in self.compound_safe_single_keywords` | `:2760` | 恒 False，但**同 if 的 SINGLE/DUAL 词首分支仍可能为真** → 仅精简 CS 项，if 保留 |
| 嵌入式扫描② | `:2863` | 同上 |
| 嵌入式扫描③ | `:3019` | 同上（rematch 段） |
| 局部缓存 `_compound_safe = _COMPOUND_SAFE_SINGLE_KEYWORDS` | `:1294` | 恒空 → 缓存与 `candidate in _compound_safe`（`:1327`）项可精简 |
| 局部缓存（`_tokenize_chinese_sequence` 段） | `:2123` | 恒空 → 同理可精简 |

**关键点**：嵌入式扫描三处条件均为三集合 OR（CS 项 / SINGLE 词首+tail / DUAL 词首+tail）。
CS 清表后即使 CS 项恒 False，**SINGLE 与 DUAL 分支仍承担词首并入**（乘法/真空/到位/列数/
段[1]下标等），故：
- ✅ 可删：类属性 `:608`、局部缓存 `:1294/:2123` 中 CS 部分、三处条件中的 CS 项；
- ❌ 不可删：三处 if 语句整体、SINGLE/DUAL 分支。

**执行前提**：任务3 将 CS 清零落地后，删除前再次 `grep compound_safe_single_keywords`
全库零引用确认（当前 CS=16 字，**不可执行删除**）。

---

## 5.5 性能对比 —— 状态：**待并行路静置 + 任务3清表后重测** ⏳

### 已有基线（跨轮次同机同口径）
| 轮次 | CS 规模 | 全语料 tokenize ×10 平均 | 备注 |
|---|---|---|---|
| R25 | 30 字 | 9.63s | 843 文件 |
| R26 | 16 字 | 9.86s（写基线轮）；8.62–12.04s（3轮区间） | 843 文件 |

### 本轮未测得数据的直接原因（非可绕过）
1. **并行路仍在修改 lexer.py**：12:34:42 最后落盘（3822 行），PID 9068 调试运行中；
   任何测量窗口都会被并发修改污染（此前单次全语料 17.8s 即为污染期间数据，弃用）。
2. **任务3 清表未落地**：当前代码是「任务1+2 已入、CS 仍 16 字」的中间态，
   「修改后」状态（CS==∅ + 精简后）尚不存在，无法对比。

### 重测方案（已就绪）
- 脚本：`lightharness/_perf_r27_t5_基线.py`（全语料 843 文件纯 tokenize ×10 取平均 ×3 轮，
  输出 `_perf_r27_t5_基线.json`；与 R26 同构可对齐）；
- 时机：并行路合并静置 + 无 lexer/tokenize 相关进程 + 任务3 清表落地后，
  同机同环境跑「修改后」一轮，与 R25/R26 基线对比；
- 预计耗时：单轮 ≈ 178s（单次 17.8s×10），3 轮 ≈ 534s，须后台跑（`nohup` + 日志）。

### 预期
任务书预期「CS 清零 + 递归删除带来性能提升」——但 5.3 已证明递归不可删（DUAL/B类
仍命中），故提升来源将主要是：① CS 表查找从 16 字集合降到 ∅/DUAL∪B类；② 局部缓存
精简。预计收益小幅（参考 R26：CS 30→16 仅 9.63→9.86s 量级），最终以实测为准。

---

## 交付总结

| 子任务 | 结论 | 动作 |
|---|---|---|
| 5.1 排除集精简 | 方案A：42字保持不变；SUFFIX 21字无需改 | **无代码修改** |
| 5.2 联动保护表 | 五张表全部保留（CCW/EMBED/OP_VERBS/MERGE_WHOLE/NEVER_SPLIT） | 只出结论，不删除 |
| 5.3 递归函数 | **非死代码**（任务2扩展条件为 CS∪DUAL∪B类，CS清空后仍命中） | **保留**，任务书预设被实际代码推翻 |
| 5.4 类属性 | CS清表后：类属性+局部缓存+三处CS项可精简，if 骨架保留 | 待任务3清表落地后执行，删前 grep 复核 |
| 5.5 性能 | 基线：R25 9.63s / R26 9.86s；修改后待测 | 并行路静置+清表后后台重测 |

**铁律遵守**：只评估未删除任何代码；删除动作均标注了前置条件（任务3清表 + grep
零引用复核）；不触碰任务1-4 修改区域。
