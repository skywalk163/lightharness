# 第23轮 任务4 交付报告：通用规则边界测试套件 + 性能验证

> 日期：2026-09-14 ｜ 任务书：复刻_第23轮_任务prompt分发_保护表通用化替代轮.md ｜ 状态：已完成
> 交付物：examples/test_R23_*.light ×4（新增，未改任何既有文件）；
>        tests/test_R23_词法边界_token.py + tests/test_R23_词法边界_反向.py（新增）；
>        _perf_r23_上下文规则.py；未修改 lexer.py 任何逻辑（任务书约束）

---

## 一、测试覆盖矩阵

| 任务书场景大类 | .light 运行期自校验 | pytest token 层 |
|---|---|---|
| ① 保护表替代场景（己词尾/后缀并入/CCW内建名） | test_R23_己词尾并入.light；test_R23_IDENTIFIER_SAFE后缀位置.light；test_R23_CCW内建名.light | test_R23_词法边界_token.py（自己/错误己/处理函数/可打印/我的标准库/学生模块/输出格式/输出结果/输出甲/CCW 6名） |
| ② 反向验证（合法语法不被误并） | test_R23_合法语法不误并.light（如果/否则/那么分支、遍历、尝试捕获、无空格算术、比较/逻辑运算、接收 形参、打印/输出语句） | test_R23_词法边界_反向.py（关键字+名结构、无空格语句头、运算符、_P0A_SUFFIX_SPLIT_KW 排除集契约、控制流语句头、子序列守卫） |
| ③ 边界混合场景 | test_R23_边界混合场景.light（同行嵌套调用、嵌套段落形参、字典键/列表元素、ASCII混合、数字尾标识符、多形态串接） | test_R23_词法边界_token.py::test_ascii_han_mixed |
| ④ 性能验证 | — | _perf_r23_上下文规则.py（全语料计时对比） |

任务书要求 ≥4 个 .light / ≥2 个 pytest：实际 4 + 2，全部通过。

## 二、测试结果

1. **4 个 .light 全部 rc=0**（运行期自校验）：
   - test_R23_IDENTIFIER_SAFE后缀位置.light ✅
   - test_R23_己词尾并入.light ✅
   - test_R23_CCW内建名.light ✅
   - test_R23_合法语法不误并.light ✅
   - test_R23_边界混合场景.light ✅（共5个，超出要求）
   均被 tests/test_回归.py 的 glob 参数化自动纳入门禁。
2. **pytest 70 passed**：token 层（38 用例，含参数化）+ 反向验证（32 用例）。
   关键契约：`打印甲`→打印KW+甲（无空格 print 一等写法）；`学生模块等于甲` 的 等于 存活；
   `等待结果` 整体成词（见 §四 说明）；`等于空那么`/`除类型错误`/`自定义表` 排除集契约。
3. **子序列守卫**：15 个代表性词形的 token 字面必须是源文本子序列（沿用
   TestNoFabricatedCharacters 口径，防"凭空造字"回归）。

## 三、性能验证（_perf_r23_上下文规则.py，输出 _r23_perf_out.txt / _r23_perf_result.json）

| 项 | 基线（HEAD，含白名单） | 现状（清表+上下文规则） |
|---|---|---|
| 全语料 tokenize 总耗时（769 文件 ×3轮取最优） | 9.51s | 9.37s |
| 相对变化 | — | **-1.4%（提升）** |

判据：回退 <10% → **PASS**（实际为小幅提升：白名单删除减少了集合查找，规则新增的
scan_pos 判断在既有热路径上，代价可忽略）。

## 四、测试设计中的行为记录（供路M/后续轮次）

1. **`等待结果` 整体成词**（行为细化，非回归）：`等待` 是 VERB_ARITY 动词（await
   语句用法恒带空格），按词首动词复合名头规则并入；全语料 A/B（任务2 报告 §三）
   实证 765 文件零影响，且第21轮 NAMES 名单早已把 等待结果 列为合法变量名。
   反向测试据此将该形态记为"动词复合名头合并"契约而非切分契约。
2. **`输出甲`/`输出格式`/`输出结果` 词首合并**：输出∉_P0A_STMT_SPLIT_VERBS，
   与第21轮既有行为一致（v7 新单 B 注释），非本表新增。
3. **`空` 保留字不可作运行期比较值**（`等于空那么` 形态只能 token 层验证）——
   运行期验证改用 `如果…那么…` 真实分支，token 层由反向测试覆盖。
4. **元数调用实参收集怪癖（既有，非本轮引入）**：空格分隔的 3 元动词调用中，
   第 2/3 实参为字符串字面量时收集失败（`断三 拼接校验, "wenyan|", "标签"` 只绑定
   实际 1 参）；括号调用形态正常。既有测试均用括号形态故未暴露。建议登记为
   独立词法/解析问题（候选 L-154，归任务6 裁量），本轮测试一律用括号形态规避。

## 五、通用约束遵守情况

- 未修改 lexer.py / code_generator.py（仅新增 lightharness 测试与脚本）；
- 4 大类场景覆盖完整、全部通过；性能回退 <10% 达标；
- 测试不依赖任何任务未收口的中间态：己词尾断言与 _TRAILING_ALIAS_MERGE
  存/删两态兼容（词尾并入行为不变），CCW 内建名断言与任务3迁移兼容（迁移前提
  即 token 零变化）。
