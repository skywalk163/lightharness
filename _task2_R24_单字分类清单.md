# 第24轮 任务2 分类清单：_COMPOUND_SAFE_SINGLE_KEYWORDS 30条单字

> 日期：2026-09-14 ｜ 方法：判据③逐条隔离中立验证（模块属性+类属性双替换，全语料 769 个 .light 文件 token 序列逐文件对比）
> 证据：_task2_R24_单字逐条验证_证据.json（含每条撤掉后的变化文件清单）｜ 脚本：_sweep_r24_t2_compound_safe_single.py

---

## 分类总览

| 分类 | 条数 | 单字 |
|---|---|---|
| ① 冗余可删 | 4 | 是、首、余、减、空、除 中：**是/首**（非守卫管辖）；**余/减/空/除**（属守卫管辖的冗余，见③） |
| ② 真护栏 | 24 | 乘(1)例(3)出(2)列(12)则(2)到(1)加(2)对(7)常(3)引(9)接(3)断(11)末(1)模(22)段(13)的(4)真(1)类(26)自(2)试(1)跳(1)过(1)配(2)长(1) |
| ③ 冗余+运算符/值字面量（守卫管辖） | 4 | 余、减、空、除 |
| ③' 真护栏+运算符/值字面量（守卫管辖） | 3 | 乘、加、真 |

括号内=撤掉后 token 变化的语料文件数。**冗余仅 6 条（20%），24 条为真护栏**——单字表是当前最"实"的一张保护表，与第22轮多字表的 32/32 全冗余形成鲜明对比。

## 分类①：冗余可删（4条：是、首、余、减、空、除 中的非守卫部分）

| 单字 | 验证结果 | 说明 |
|---|---|---|
| 是 | 撤掉后零变化 | `是否`/`但是`/`还是` 等场景由上下文切词与最长匹配覆盖 |
| 首 | 撤掉后零变化 | `首字母`/`月末` 场景由其他机制覆盖（末 仍需保护） |
| 余 | 撤掉后零变化 | ③类：`余额`/`剩余` 已被覆盖 |
| 减 | 撤掉后零变化 | ③类：`减法` 词首由动词复合名头并入；运算符场景由 _P0A_OP 切分 |

## 分类③：运算符/值字面量守卫管辖（验证其守卫现状）

| 单字 | 冗余性 | 守卫现状 |
|---|---|---|
| 余 | 冗余（零变化） | 无需增强 |
| 减 | 冗余（零变化） | 无需增强 |
| 空 | 冗余（零变化） | `空白`/`空值` 由上下文覆盖；值字面量 `为 空` 由最长匹配保证 |
| 除 | 冗余（零变化） | **注意**：`去除空格` 的保护不依赖 `除` 在表中——靠 `空`(在表) 的递归副作用回报（`除空格`→`除` 后递归遇 `空` 是 compound-safe 且无后继真关键字→回报 `空`→`除` 被跳过）。若 `空` 被删则 `除` 需回表。`除法` 由 _P0A_OP 切分 |
| 乘 | 护栏(1文件) | `乘阶乘`（hello.light 调用名）词中并入依赖递归副作用；运算符 `甲乘乙` 由 _P0A_OP 切分。乘 删除会破坏该自由名 |
| 加 | 护栏(2文件) | `加斐波那契` 词中并入依赖递归副作用；`甲加乙` 由 _P0A_OP 切分 |
| 真 | 护栏(1文件) | `标准输出失真` 词尾并入；值字面量 `等于 假/真` 由位置判定 |

## 分类②：真护栏 24 条——保护场景类型

| 场景类型 | 条数 | 单字（典型例证） |
|---|---|---|
| **词尾并入**（属性名/类名/调用名/异常名的末字） | 17 | 出(标准输出)、列(日期列)、则(规则/正则)、到(文件未找到)、常(首异常)、引(最后索引)、接(符号链接)、断(截断)、末(月末)、的(成绩的长度)、真(标准输出失真)、类(有界队列类)、试(测试)、跳(最后心跳)、过(通过)、长(块长)、配/段(配[0]/段[下标] 单字别名下标访问，L-119 家族) |
| **词中并入**（调用名/标识符中部） | 6 | 乘(乘阶乘)、例(用例)、加(加斐波那契)、对(对于 归属)、自(自定义表)、自(自闭合标签集) |
| **词首并入**（类名头） | 1 | 模(模拟客户端：模+拟客户端) |

## 关键发现（供任务3/4设计通用规则）

1. **词尾并入是最大宗场景（17/24）**：全部是「属性名/类名/调用名末字不得浮出为 KEYWORD」。第23轮的**多字**后缀规则不覆盖单字（`sub_len > 1` 门），本轮任务3的单字后缀规则（scan_pos>0 + 单字 + 非运算符 + 非值字面量 + 非排除集 → 并入）理论上可覆盖 17 条中的大部分——但必须保住「词尾仍作 KEYWORD」的既有形态（如 `返回真`、`配[0]`、`遍历 块长 之` 的边界）。
2. **递归副作用语义是词中并入的承重墙**：`乘阶乘`/`加斐波那契` 依赖 `_skip_compound_safe_and_match` 的回报机制（后随 compound-safe 单字+无真关键字 → 回报内层→调用方 skip）。任务4若规则化递归，须以「非语句起始位置整体成词」等价替代。
3. **`的` 的递归吞分隔符已获语料实证**：`成绩的长度`（07_字典.light）在 `的` 移除后切成 `成绩`+`的长度`——即当前 `的` 在表内时该串是整体，移除后暴露分段。任务1 的修复必须同时保证 `成绩的长度` 类形态与 `X 的 Y` 分隔形态（后随空白）不回归。
4. **`模` 是唯一词首并入护栏**（22文件）：`模拟客户端` 类名。`模` 已在 _P0A_NEVER_SPLIT（永不切分），其保护主要由 P0-A 层承担，表内冗余度待任务3验证。
5. **`对` 场景特殊**：`x对于x` 形态中 `对`/`于` 的归属判定（`对于` 是双字关键字，剥离后 `对` 需并入前名）——`对于` 在 `甲对于乙` 的既有切分依赖 `对` 的 compound-safe 递归（`对`后递归见`于`非之→返回None→`对`并入）。

## 逐条验证结果表（30条全景）

| 单字 | 分类 | 变化文件数 | 保护场景 |
|---|---|---|---|
| 乘 | ②'守卫管辖但真护栏 | 1 | 词中并入：乘阶乘 |
| 余 | ③ | 0 | —（冗余） |
| 例 | ② | 3 | 词中并入：用例 |
| 减 | ③ | 0 | —（冗余） |
| 出 | ② | 2 | 词尾并入：标准输出/本轮输出 |
| 列 | ② | 12 | 词尾并入：日期列 等 |
| 则 | ② | 2 | 词尾并入：规则/正则 |
| 到 | ② | 1 | 词尾并入：文件未找到 |
| 加 | ②' | 2 | 词中并入：加斐波那契 |
| 对 | ② | 7 | 词中：对于 归属敏感 |
| 常 | ② | 3 | 词尾并入：首异常 |
| 引 | ② | 9 | 词尾并入：最后索引 |
| 接 | ② | 3 | 词尾并入：符号链接 |
| 断 | ② | 11 | 词尾并入：截断 |
| 是 | ① | 0 | —（冗余） |
| 末 | ② | 1 | 词尾并入：月末 |
| 模 | ② | 22 | 词首并入：模拟客户端 |
| 段 | ② | 13 | 词尾下标：段[ ] |
| 的 | ② | 4 | 词中并入：成绩的长度 |
| 真 | ②' | 1 | 词尾并入：标准输出失真 |
| 空 | ③ | 0 | —（冗余；但其递归副作用承托 `除`） |
| 类 | ② | 26 | 词尾并入：有界队列类 |
| 自 | ② | 2 | 词中并入：自定义表/自闭合标签集 |
| 试 | ② | 1 | 词尾并入：测试 |
| 跳 | ② | 1 | 词尾并入：最后心跳 |
| 过 | ② | 1 | 词尾并入：通过 |
| 配 | ② | 2 | 词尾下标：配[0]（L-119） |
| 长 | ② | 1 | 词尾并入：块长之块长表 |
| 除 | ③ | 0 | —（冗余；依赖 `空` 的副作用） |
| 首 | ① | 0 | —（冗余） |
