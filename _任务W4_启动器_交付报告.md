# 任务 W4 — 网页启动器升级 交付报告

- 关联文档：`外发任务_网页启动_W4_启动器.md`（修订版 160d8d35）
- 交付日期：2026-09-09
- 白名单变更文件：仅 `lightharness\examples\运行Web服务器.light`（最终修订版 `2ae5fd40`）
- 验收结论：**全部通过**

---

## 1. 任务与范围

按任务文档完成"W4 启动器"升级，白名单文件为 `examples\运行Web服务器.light`，其余 source 文件（认证模块、服务端路由）由前置任务交付并保持不动。本任务仅改启动器，以满足网页启动的端到端装配。

## 2. 变更摘要

启动器 `运行Web服务器.light`（修订版 `2ae5fd40`）完成以下装配：

| 契约 | 内容 | 实现 |
|------|------|------|
| 契约2 | 认证接入 | `从 认证 导入 初始化认证, 取令牌`；启动服务前调用 `初始化认证()`，随后 `取令牌()`；令牌为空则输出旧式 URL（无 `?token=`） |
| 契约4 | 装配新签名 | `服务.配置(客户端对象, 注册表, 根, 30.0, 界面目录)` |
| 契约6 | 登录 URL 输出 | 已认证 → `web 服务已启动: http://127.0.0.1:<实际端口>/?token=<令牌>`；保留 `HARNESS_PORT` 广播与 `HARNESS_PORT_FILE` 写入 |
| 契约6 | 自动打开浏览器 | 默认开启；Windows 用 `start "" "url"`，非 Windows 用 `xdg-open`/`open`；`HARNESS_WEB_NO_OPEN=1` 禁用；失败打印 `自动打开浏览器失败，请手动访问 <URL>` 不崩溃；在 URL 打印后、常驻循环前执行 |
| 既有行为 | 保留 | mock/real 双模式（`HARNESS_WEB_MOCK`）、`.env` 密钥解析、`HARNESS_WEB_LIFETIME` 常驻/生命周期逻辑 |

### 界面目录（webui）定位规则（文档行 25–29）

`解析界面目录` 函数按优先级解析：

1. `HARNESS_WEB_UI_DIR` 非空 → **直接使用，不做 `目录存在` 校验**；
2. 否则从 `获取脚本路径()` 推导（同级 `lightharness\webui`，再 `..\webui`，均需存在）；
3. 两者均失败 → 打印警告 `未找到 webui 目录，静态页面不启用`，返回 `""`，API 仍运行。

**不读取 `当前目录()`。**

## 3. 环境变量清单

### 3.1 本任务新增

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `HARNESS_WEB_UI_DIR` | 空 → 自动推导 | 静态界面目录；非空时直接采用（不做存在性校验），为空时按文档规则推导 |
| `HARNESS_WEB_TOKEN` | 空 → 启动时自动生成 64 位十六进制 | 预置访问令牌；由认证模块 `初始化认证()` 读取并生成 |
| `HARNESS_WEB_NO_OPEN` | 0（自动打开） | 置 1 禁用自动打开浏览器 |
| `HARNESS_WEB_CORS` | 预留，默认不启用 | 本任务不处理，仅列入清单 |

### 3.2 既有变量（保留不变）

| 变量 | 说明 |
|------|------|
| `HARNESS_WEB_MOCK` | mock/real 双模式切换 |
| `HARNESS_WEB_LIFETIME` | 秒数；设置后生命周期结束后退出（executor 场景） |
| `HARNESS_PORT` / `HARNESS_PORT_FILE` | 端口广播/落盘 |

## 4. 登录 URL 样例（脱敏）

启动器输出格式（已认证）为：

```
web 服务已启动: http://127.0.0.1:<实际端口>/?token=<64位十六进制令牌>
```

手动验收实测输出（令牌已脱敏，仅保留首尾 4 位）：

```
web 服务已启动: http://127.0.0.1:50729/?token=13d2…c2
```

- 未认证（令牌为空）时输出旧式 URL：`web 服务已启动: http://127.0.0.1:<实际端口>/`（无 `?token=`）。

## 5. LIFETIME rc=0 记录

- 全量回归门禁：`HARNESS_WEB_LIFETIME=3`，**188 passed in 243.05s**，全部用例以正常退出码结束（0 失败 0 报错）。
- 验收脚本中 5 次 LIFETIME=3 验证运行（S1/S2/S3/S4/R1）均以 **rc=0** 退出。

## 6. 浏览器自动打开记录（验收判据③ 手动验收）

- **默认路径实测（2026-09-09）**：以常驻模式启动（未设 `HARNESS_WEB_NO_OPEN`、未设 `HARNESS_WEB_LIFETIME`）：
  - 启动器打印登录 URL：`http://127.0.0.1:50729/?token=13d2…c2`；
  - **未出现** `自动打开浏览器失败` 提示 → 系统接口 `start "" "url"` 执行成功；
  - 浏览器进程 msedge（pid 4828）于打开窗口期内启动（进程 StartTime 13:11:16）；
  - 对登录 URL 发起 HTTP GET → **200 OK**，返回 body 为 lightharness webui 首页 HTML（`<!DOCTYPE html><html lang="zh-CN">…`）；
  - 验收后终止常驻进程（pid 13596），port 50729 已确认无监听（残留 = 0）。
- **反跑（禁用路径）**：验收脚本中 5 个 `noopen_*` 检查在 `HARNESS_WEB_NO_OPEN=1` 下全部 PASS（无打开动作、无失败提示）。

## 7. webui 定位三态 + 空推导分支 测量

验收脚本 `_verify_webui.py`（22 项检查全绿）关键实测：

| 场景 | 环境变量 | webui 目录判定 | 静态页面 | 服务/退出码 | 警告 |
|------|----------|----------------|----------|-------------|------|
| S1 | `HARNESS_WEB_UI_DIR` = 自定义临时目录（含自定义 index.html） | 直接使用（优先级①，不做存在性校验） | 200，返回 `CUSTOM-UI-OK` | 运行/rc=0 | 无 |
| S2 | 未设置（推导：同级 `lightharness\webui` 存在） | 推导成功 → `lightharness\webui` | 200 | 运行/rc=0 | 无 |
| S3 | `HARNESS_WEB_UI_DIR` = 不存在目录（`Z:\__nonexist__\webui`） | 非空 → 直接使用，**不做存在性校验** | 404（目录无文件） | 运行/rc=0 | **无**（按契约优先级①） |
| S4 | 同时使同级 `webui` 不可用（临时目录重命名）→ 推导失败 | 降级推导失败 | 404 | 运行/rc=0 | 打印 `未找到 webui 目录，静态页面不启用` |
| S4restore | 重命名恢复后复核 | 推导恢复（目录已还原） | — | — | 通过 |

- S3 的"无警告"为**契约要求**：优先级①规定非空 `HARNESS_WEB_UI_DIR` 直接使用、不校验存在性；警告仅属于空环境推导失败分支（由 S4 覆盖）。
- 所有场景均验证：静态页面不启用时 API 仍正常运行，退出码保持 0。

## 8. 验收判据对照

| # | 判据 | 结果 |
|---|------|------|
| ① | 门禁兼容性 | ✅ 全量回归 `188 passed in 243.05s`（无 F/E） |
| ② | 输出格式 | ✅ `web 服务已启动: http://127.0.0.1:<端口>/?token=<令牌>`（含令牌） |
| ③ | 手动验收 | ✅ 浏览器自动打开实测成功（见第 6 节） |
| ④ | webui 定位三态 | ✅ S1/S2/S3/S4 全绿（见第 7 节） |
| ⑤ | 反跑 | ✅ R1 显式令牌校验通过；`HARNESS_WEB_NO_OPEN=1` 禁用打开通过；未认证旧式 URL 分支由启动器兜底（实际令牌恒非空） |

## 9. 反跑检查明细

- **R1（显式令牌）**：`HARNESS_WEB_TOKEN=t0k3n-xYz123` → 登录 URL 令牌与预置值一致，`取令牌()` 返回 `t0k3n-xYz123`（含在 URL 中），HTTP 200。
- **禁用自动打开**：`HARNESS_WEB_NO_OPEN=1` → 5 个用例均无浏览器打开动作、无失败提示、服务正常、rc=0。
- **未经认证分支**：启动器对空令牌输出旧式无 `?token=` URL（行 136–137 兜底）。

## 10. 交付物清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `lightharness\examples\运行Web服务器.light` | **白名单变更** | 启动器升级（修订版 `2ae5fd40`） |
| `lightharness\_任务W4_启动器_交付报告.md` | 交付物 | 本报告 |
| `lightharness\_verify_webui.py` | 验证辅助（非白名单） | 可复现验收脚本（22 项检查，ALL PASS），含 5 个场景 + token/noopen 反跑 |

> 说明：验证过程中的瞬时日志（`_manual_run.log`、`_gate_run.log`、`_verify_run.log`）含临时生成的访问令牌，已完成清理；停止运行的常驻进程已全部退出。