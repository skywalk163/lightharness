# 第44轮 任务6 交付报告 —— 质量审查 + docs 回填 + 探针移档

> 日期：2026-09-16 ｜ 优先级 P2 ｜ 评估 + docs 定稿

## 一、质量审查：任务3/4 改 src 是否破既有

### 1.1 全量 pytest 回归（硬门槛）

| 轮次 | 平台 | 结果 |
|---|---|---|
| 基线（改 src **前**） | 本机 Windows | 8 failed / 1231 passed / 3 skipped（845.67s） |
| **改 src 后** | 本机 Windows | **7 failed / 1232 passed / 3 skipped**（733.79s） |
| 全量重同步后 | 0.82 FreeBSD | 7 failed / 1230 passed / 3 skipped / 2 errors（338.03s） |
| **改 src 后（定版）** | 0.82 FreeBSD | **7 failed / 1230 passed / 3 skipped / 2 errors**（314.96s） |

**判定：基线外零新增红 ✅**

- 本机失败 8 → 7：少的那条是 `test_子进程后台.light`（单跑 rc=0 的 **flaky**），非修复引入，也非回归。
- 0.82 改前/改后 **逐项完全一致**（7/1230/3/2）→ 三处 src 修改在 POSIX 下零影响。
- 两平台失败清单（7 条）逐条一致，均为既有词法/样例存量红。

### 1.2 受影响用例专项验证（向后兼容）

| 用例 | 关注点 | 结果 |
|---|---|---|
| `test_mock大模型服务器.light` | `分派` 29 处 `r[0]`/`r[2]` 列表下标（护住纯逻辑契约） | rc=0 ✅ |
| `test_R42_bash跨平台.light` | `bash处理` **单参**调用（护住默认形参） | rc=0 ✅ |
| `test_R42_真实工具链.light` | `bash处理` **单参**调用 | rc=0 ✅ |
| `test_R34_集成测试.light` | 工具执行 + `调正常` 常量 | rc=0 ✅ |
| `test_R40_端到端互举.light` | **硬编码 `"ok"`** 断言（护住 L-164 不改值） | rc=0 ✅ |
| `test_工具系统.light` / `test_agent工具调用.light` | 核心工具注册（另一套 `工具注册表` 抽象） | rc=0 ✅ |
| `test_R43_真实LLM往返.light` | 去绕行后（直接用 `启动Mock服务`） | rc=0 ✅ |
| `test_R43_真实agent循环.light` | 去绕行后（`启动Mock服务` + `注册bash工具`） | rc=0 ✅ |

### 1.3 任务2 真 POSIX 红归因（复核）

1. `test_子进程后台.light`：本机 pytest 偶发红，单跑 rc=0 → **flaky**，非平台差异（已复跑定性）。
2. `test_R21_词法确定性_超集` 2 条 ERROR：`FileNotFoundError: 'git'` → **0.82 未装 git**，环境缺失非代码缺陷，不擅自装包。
3. 其余 7 条两平台同红 → 既有存量。

## 二、docs 回填（三件套）

| 文件 | 回填内容 |
|---|---|
| `docs/功能对标/对标清单.json` | 追加 **#172**（0.82 全量回归）、**#173**（L-162/163/164 + L-165 修复）；**更正 #171** 中被证伪的「副本不全」归因（保留原文 + 追加更正标注）。条目数 171 → **173** |
| `docs/功能对标/行为差异清单.md` | 追加 **R44-D1**（0.82 全量重同步 + 干净回归 + 归因更正）、**R44-D2**（装配契约四处修复）；并在 R43-D3 下追加归因更正注记 |
| `docs/功能对标/语言缺陷账.md` | L-162 / L-163 / L-164 标记 **✅ 已修复**；新增 **L-165**（`新建Mock服务` 漏 `全局` 声明）并标记已修 |
| `docs/多平台差异清单.md` | 追加 **R44 跨平台回归门**章节（12 用例双平台实测表）+ 真·平台差异表 |

> 对标清单插入采用**外科式文本追加**而非 `json.dump` 整体重写：原文件为 CRLF + 非标准缩进，
> 整体重排会产生 **5000+ 行 diff** 淹没真实改动。实际 diff = **19 增 1 改**。

## 三、探针移档

`docs/历史存档/R44探针/`（`_r44_回填对标清单.py`、`_r44_remote_dir.txt`）；工作区 `_r44*` 已清空，
临时 tar 包（97.4MB）上传后即删，无残留。

## 四、验收标准核对

| 项 | 状态 |
|---|---|
| 0.82 全量同步，examples 402 到位 | ✅（402 = 402） |
| 0.82 完整 pytest 干净对比，真 POSIX 红逐条归因 | ✅（7/1230/3/2；3 条差异已归因） |
| L-162 修复，mock 处理器字典契约对齐，既有测试不破 | ✅ |
| L-163/164 修复，bash 装配 + 状态字面值对齐 | ✅ |
| 跨平台 CI 门固化 | ✅（12/12 双平台，门 PASS） |
| 本机全量 pytest 基线外零新增红（改 src 后） | ✅（8→7，少的那条是 flaky） |
| docs 三件套回填 + 探针移档 | ✅ |
| git 提交（不 push） | 见路M |

## 五、遗留与建议（未做）

1. **L-165 类缺陷防复发**：建议光明编译器对「函数内对模块级同名变量赋值、但本函数未声明 `全局`」
   给出编译告警——静默影子变量的表现是远端 500，极难定位。
2. **`test_子进程后台.light` flaky**：建议单独立项加固（超时/等待条件），本轮只定性不修
   （不在本轮授权范围，且不影响平台一致性结论）。
3. **0.82 装 git** 可消掉 2 条 ERROR，但属他人机器环境变更，本轮**不做**（需用户授权）。
