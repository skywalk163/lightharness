# 第27轮 任务3：CS表16字逐条验证删除清单

日期：2026-09-15 ｜ 结论：**移除 14 字（CS 16→2），残部 列/类 为真护栏保留**
验证脚本：`lightharness/_antirun_r27_t3_CS表逐字验证.py`（逐字 monkeypatch 隔离，
G1 全语料 843 文件 token 比对 + G2 词首编译门 + G3 六类边界形态门）
证据文件：`lightharness/_task3_R27_CS表16字逐条验证_证据.json`

---

## 一、可删字（14 字，G1∧G2∧G3 全通过）

### A类 DUAL 8 字（由任务1 `_P0A_HEAD_MERGE_DUAL` 词首/词尾正面规则覆盖）

| 字 | G1 打红场景（撤CS后） | 覆盖规则 |
|---|---|---|
| 乘 | `乘阶乘`→切成 乘+阶乘（hello.light 等） | 词首+后随汉字→并入 |
| 减 | `减天数`→切成 减+天数（bootstrap/日期时间） | 同上 |
| 到 | `文件未找到`→切成 文件未找+到（词尾，password_manager） | **词尾**+前导汉字→并入 |
| 加 | `加载`/`加载库`→切开（bootstrap/FFI 等 8 文件） | 词首+后随汉字→并入 |
| 模 | `模式`/`模板`→切开（bootstrap/JSON 等 29 文件） | 同上 |
| 真 | `标准输出失真`→切成 标准输出失+真（词尾，stdlib/进程树） | **词尾**+前导汉字→并入 |
| 空 | `空白字符`→切开（bootstrap/字符串常量） | 词首+后随汉字→并入 |
| 除 | `除异常`→切开（bootstrap/CSV读写器 等 2 文件） | 同上 |

### B类 6 字（由 HM 正面类别 + 任务2 L-119 嵌入扫描等价覆盖）

| 字 | G1 打红场景（撤CS后） | 覆盖规则 |
|---|---|---|
| 对 | `对[0]`下标→KEYWORD(对)（stdlib/HTTP服务端 等 4 文件） | L-119 嵌入扫描等价 |
| 是 | `def是否夏令时`→def+是否夏令时 分离（2 文件） | 词首并入 + def 边界协同 |
| 段 | `段[x]`下标→KEYWORD(段)（snake 等 9 文件） | L-119 嵌入扫描等价 |
| 的 | `应失败的长度`→应失败+的长度（5 文件） | 词首并入分支（词中由整串路径覆盖） |
| 自 | `自定义表`→切成 自+定义+表（lightharness/src/权限） | 词首并入（rematch 段） |
| 配 | `配[0]`下标（test_L119 等 4 文件） | L-119 嵌入扫描等价 |

**实现落点（lexer.py）**：
1. `_P0A_HEAD_MERGE_DUAL`（8字）+ 第一层 skip_verb 分支 + 嵌入扫描三处
   （探测 :2761 区 / 输出 :2861 区 / rematch :3010 区）；
2. DUAL 词尾并入（探测/输出循环词尾分支，条件：词尾+前导汉字 `_is_han(fi[scan_pos-1])`）；
3. L-119 嵌入扫描等价：独立单字（`sub_len==len(full_identifier)`）+ 后随 `[`
   → 不标记内嵌，与第一层 skip_verb 同口径（段[1]/配[0]/对[0]）。

## 二、保留字（2 字，真护栏——撤掉任一即 G1 打红）

| 字 | 场景 | 基线行为 | 撤CS后行为 | 不可删原因 |
|---|---|---|---|---|
| 列 | `bootstrap/release/stdlib/断言工具.light`<br>`对于 元素 在 序列: ` | `IDENTIFIER(序)+KEYWORD(列)+COLON`<br>（词尾切出 KEYWORD） | 列∈F 词尾并入→`序列` 整词 | 「词尾+前导汉字→**切出** KEYWORD」与 R25 词尾 F 并入方向相反；列∈F 不可加入词尾排除集（否则 HM 词首 `列数` 打红）。需独立刻画，留后续轮次 |
| 类 | `lightharness/examples/test_L013.light`<br>`类 独立类:` | `IDENTIFIER(独立类)` 整词 | 切成 `独立+类:` | 基线整词依赖 CS 与类声明上下文的协同；撤 CS 后词尾并入被 `独立+冒号` 边界打断。同上留后续轮次 |

**方向性说明**：两字的基线行为互为镜像（列=词尾切出、类=词尾整词），说明
「词尾 + CS残部字」的 token 行为由上下文（前导关键字"在"/"类"声明、后随冒号、
user_definitions 共同决定），单条通用条件无法同时刻画，需在后续轮次以
`_seg_after_sep`/user_definitions 协同口径逐字定位（任务书 3.4 出口）。

## 三、自校验断言更新（lexer.py 文末）

| 断言 | 旧口径（R26） | 新口径（R27） |
|---|---|---|
| ① | `CS == (F−TAM) ∪ DUAL`（16字） | `CS ⊆ F` ∧ `CS ∩ DUAL == ∅`（残部 列/类） |
| ② | `_TRAILING_ALIAS_CLASS == F` | 不变（仍 43 字） |
| ③（新） | — | `HM − CS == _R26_CS_REMOVED ∪ _R27_CS_REMOVED` |

## 四、全量反跑确认（删除后）

- 全语料 839 文件可比（既有失败 2 排除）：**token 零变化** ✅
- 3 个 .light 边界测试 rc=0 ✅；pytest 113 passed ✅
- 详见 `_task4_R27_边界测试+反跑_交付报告.md`
