# 任务3（R34）：子代理深化复刻 —— 交付报告

> 日期：2026-09-15 ｜ 轮次：第34轮 ｜ 优先级：P0
> 交付物：`src/子代理深化.light`、`examples/test_子代理深化.light`、本报告
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）`packages/subagent/src/`

---

## 1. 做了什么

新增纯逻辑模块 `子代理深化.light`（367 行 / 36 段落 / 44 导出），复刻四个核心面：

| 上游文件 | 复刻内容 | 本模块章节 |
|---|---|---|
| `directory.ts` | 父属目录：层级、归属、隔离、切换 | §1 |
| `inbox.ts` | Human Inbox：入队/出队/已读/已决/积压判定 | §2 |
| `chunked-list.ts` | ChunkedList：不可变追加分块链表（64/块） | §3 |
| `depth.ts` | DepthLimit：最大深度 3、创建前检查、父代理链 | §4 |

### 3.1 父属目录（ParentDirectory）

- 目录形状 `{名称, 上级, 子目录表, 子代理表}`；`建目录根` 建 `根`。
- 操作：`加子目录`（重名抛 `DUPLICATE_DIR`）/ `删子目录` / `列子目录` / `目录路径`（自根拼接全路径）。
- **归属**：`归属目录(目录, 子代理名)` —— 每个子代理**归属且仅归属一个父目录**，重复归属返回假；
  `查归属` / `列归属` 查询。
- **隔离（铁律）**：`隔离检查(起点, 路径)` —— **路径中任何 `..` 一律拒绝**
  （错误码 `CROSS_DIRECTORY_DENIED`），子代理工作目录即其根，不得逃逸到父/兄弟目录；
  向下路径不存在则返回 `PATH_NOT_FOUND`。`切换目录` 在拒绝时抛错（消息含错误码）。
- 只向下解析：`解析路径` 不处理 `..`；`切段` 去空段（`/甲//丙/` → 2 段）。

### 3.2 Human Inbox

- 收件箱 `{消息表, 序号, 积压阈值}`，默认阈值 4。
- 消息形状：`{序号, sender, content, priority, status, createdAt}`；
  状态 `pending → acknowledged → resolved`；优先级 `low/normal/high/urgent`（权重 0/1/2/3）。
- 操作：`投入消息`（未知优先级抛 `UNKNOWN_PRIORITY`，空发送者抛 `INVALID_SENDER`）、
  `取待办`（出队：**优先级高者先出，同级按序号**；出队即置 `acknowledged`；空队列返回 `空`）、
  `标记已读`（仅 pending 可转，重复/不存在返回假）、`标记已决`、`列举消息`（按状态过滤）、`待办数`。
- **积压判定**：`是否积压` = 待办数 ≥ 阈值。
- 时刻由调用方传入（逻辑时钟），不用墙钟。

### 3.3 ChunkedList

- 块容量 `深块容量 = 64`；节点 `{值表, 前件?}`，空列表 = `空`。
- **不可变**：`深追加(头, 值)` 只复制最新块、旧块共享，返回新节点；**原头不被修改**
  （测试用 `序列化JSON(深展平(头))` 逐版本比对证明）。
- `深展平`（正序，插入序）/ `深逆展平`（最新优先）/ `深分块长` / `深块数` / `深栈`。
- **校验 `深校验`**：每块非空且 ≤ 容量；手工构造的空块/含空块前件返回假。

### 3.4 深度限制（DepthLimit）

- 子代理 `{名称, 深度, 上级, 子代表}`；根深度 1，子代 = 上级深度 + 1。
- **创建前检查（铁律）**：`可否派生(代理, 最大深度)` = `代理["深度"] + 1 <= 最大深度`；
  `派生子代` 超限抛 `SUBAGENT_DEPTH_EXCEEDED`，**且消息含建议**（"建议合并子任务或提升深度上限"），
  超限时不创建任何子代。
- `默认最大深度 = 3`。
- `代理链` 返回自底向上的父代理链名称表；`深度自洽` = 链路长度 == 自身深度（不变量）。

---

## 2. 验证结果

| 项 | 结果 |
|---|---|
| `运行.py examples/test_子代理深化.light` | **rc=0，打印 `test_子代理深化 PASS`** |
| 测试规模 | 211 行 / 87 条断言 / 8 组应抛校验 / 5 个用例段 |
| 反跑（值反 + 抛反） | 2/2 变异体**变红**，还原后**转绿** |
| 命名冲突扫描 | 与既有 `src/*.light` **0 冲突** |

测试用例段覆盖：
1. 目录层级（`根/甲/丙` 路径拼接）、重名/空名抛错、删除
2. 归属（重复归属返回假）、查归属、切段、向下解析、**`..` 逃逸被拒**（含嵌套 `..`）、
   `PATH_NOT_FOUND`、切换目录抛错
3. Inbox：入队/序号递增/未知优先级·空发送者抛错/优先级权重/**urgent 优先出队**/标记已读已决/积压判定
4. ChunkedList：**不可变性**（头甲/头乙/头丙 三版本并存比对）、正序逆序展平、130 项跨 3 块、
   空块校验失败
5. DepthLimit：深度递增、上限 3 处拒绝派生、超限消息含错误码与"建议"、提升上限后可继续、
   父代理链完整、深度自洽

---

## 3. 反跑判据

脚本：`_antirun_r34_t123_反跑.py`（变异文件 `_red_tmp.light`，根目录，跑完即删）。

| 判据 | 变异 | 变异后 rc | 结论 |
|---|---|---|---|
| A 值反 | `目录路径(甲丙)` 期望 `"根/甲/丙"` → `"根/丙"` | 1（红） | OK |
| B 抛反 | `派生子代(三层, "四层", 3)` → 上限改成 `9`（不再超限） | 1（红，报「应抛错未抛」） | OK |

还原后三个用例复跑全绿。反跑与用例执行严格串行。

---

## 4. 与上游的差异（纯逻辑取舍）

| 差异 | 原因 |
|---|---|
| 目录为内存树，非真实文件系统 | 文件系统 I/O 剔除；隔离语义（禁 `..`）保持一致 |
| 出队按「优先级 + 序号」而非纯 FIFO | 上游 inbox 有 priority 字段，优先级出队更贴合字段语义；同级仍按入队序 |
| `createdAt` 由调用方传入 | 避免墙钟，保证可重放 |
| ChunkedList 用字典节点而非类 | 与既有 `src/分块列表.light` 同构；本模块独立实现以守「只创建子代理深化.light」铁律 |
| 无 zod | 用 `深校验` 纯函数等价提供尺寸约束校验 |

---

## 5. 铁律自检

- [x] 只创建 `子代理深化.light`，未修改任何既有模块（含既有 `分块列表.light`，本模块独立实现 `深*` 一族）
- [x] 新增一律 `.light`，未用 Python 绕语言缺陷
- [x] 父属目录隔离：任何 `..` 拒绝（`CROSS_DIRECTORY_DENIED`）
- [x] Human Inbox 持久化：内存队列 + `列举消息/待办数` 可完整导出
- [x] ChunkedList 不可变：追加返回新列表，原头不被修改（三版本 JSON 比对证明）
- [x] 深度在创建子代理**前**检查，超限不创建
- [x] 段落名一律 `深` 前缀，与既有模块 0 冲突

---

## 6. 命名冲突处置

初版 `解析路径` 与 `主目录路径.light`、`智能体指令.light` 撞名 → 改名 `深解析路径`，冲突归零。

## 7. 本轮踩到的坑

1. **字典身份比较不可靠**：目录树无法用 `目标 == 起点` 判包含关系（解释器路径下字典比较语义不稳），
   改为「**禁止 `..`**」的显式隔离策略，语义更清晰也更严格。
2. **逆序展平需双层倒序**：先按块栈正序（新→旧），再块内倒序，两步都不可省。
3. 追加 130 项构造跨块场景时，`当 计 < 130:` + `设 头丁 为 深追加(头丁, 计)` 的**重绑定**写法在段落体内有效；
   但在段落体内改**模块级标量**无效（同任务1 报告的坑 2），状态一律用字典承载。
