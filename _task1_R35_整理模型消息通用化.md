# 任务1（R35）：整理模型消息 通用化替代 —— 结论：不可通用化，保留真护栏

> 日期：2026-09-16 ｜ 范围：light-merge/src/lexer.py + lightharness 验证
> 判据：G1 全语料 token 零变化 ∧ G2 边界 .light rc=0 ∧ G3 五种边界形态 token 一致

---

## 一、结论

| 项 | 结果 |
|---|---|
| 通用化是否成功 | **否** |
| `_P0A_MERGE_WHOLE` 中该条目 | **保留**（3 → 2 后仍含 `整理模型消息`） |
| 撤单条 G1 | **0 变化**（语料 864 可比文件） |
| G3 五种形态 | **F5 传参位 FAIL** |
| 不可通用化的根因 | `模` 是**真取模运算符**，有显式守卫用例 |

---

## 二、根因分析

### 2.1 劈开机制

撤除条目后，`整理模型消息` 在**词中**被 `模` 劈开，产出三段：

```
基线（含条目）: IDENTIFIER·整理模型消息
撤除后        : IDENTIFIER·整理  KEYWORD·模  IDENTIFIER·型消息
```

定位：`_tokenize_chinese_sequence` 的**嵌入扫描探测循环**（lexer.py 原 L2789）：

```python
if sub_len == 1 and sub_kw in OPERATOR_VERBS:   # 模 ∈ OPERATOR_VERBS，scan_pos=2
    ...
    # ① 后随 '('（函数调用语境）→ skip（这就是 F2 能通过的原因）
    if (not skip_kw and scan_pos + sub_len < len(full_identifier)
            and full_identifier.isalpha()):
        if source[pos + len(full_identifier)] == '(': skip_kw = True
    # ② 运算符在词尾 → skip（此处不成立：模 在词中）
    ...
    # 否则按运算符识别 ⇒ embedded_found = True ⇒ 分段输出
```

关键点：`模 ∈ OPERATOR_VERBS`，而 `_P0A_OP` 已把 `模/步/至/到` 剔除；但**嵌入扫描用的是 `OPERATOR_VERBS` 而非 `_P0A_OP`**，两者口径不一致——这是本条能存在的结构性原因。

### 2.2 为什么只有「传参位」等少数形态 FAIL

16 种上下文矩阵实测（撤除态）：

| 形态 | 结果 | 由谁接住 |
|---|---|---|
| 设名位 `设 X 为 1` | OK | `_scan_user_definitions` 预扫描 |
| 段落名 `段落 X:` | OK | 预扫描 |
| 成员访问 `结果.X` | OK | 成员访问规则 |
| 调用语境 `返回 X(1)` / `X(1)` | OK | 嵌入扫描①「整串后紧随 `(`」 |
| **传参位 / 值位 / 列表元素 / 条件位 / 打印位 / 算术位 / 返回位 / 裸串** | **★劈开** | 无规则接住 |

即：**只有「整串后紧随 `(`」这一种语境**能接住；凡表达式/实参位皆劈开。

### 2.3 语料实态

`整理模型消息` 全语料命中 **4 处 / 1 文件**，且全部落在
`examples/test_R32_MERGE_WHOLE精简边界.light`（段落名 + 调用语境）——
**语料中不存在传参位形态**。故撤单条 G1 = 0 变化。

---

## 三、通用规则尝试与实测（两版均失败）

### GR-1：嵌入扫描运算符分支口径统一到 `_P0A_OP`

思路：`OPERATOR_VERBS` 中已被 `_P0A_OP` 剔除的 `模/步/至/到` 已声明「非真运算符」，
嵌入扫描应与 `_P0A_OP` 同口径，让它们落到 P0-A 单字并入判据。
补丁：三处 `sub_len == 1 and sub_kw in OPERATOR_VERBS` 追加 `and sub_kw in self._P0A_OP`。

**G1 结果：打红 6 文件**

| 文件 | 现状 | 变更后 |
|---|---|---|
| bootstrap/…/临时文件.light | 文本**模**式 | 文本模式（并入） |
| bootstrap/…/文件匹配.light | 文件路径**模**式匹配 | 并入 |
| examples/test_R24_运算符单字守卫.light | 甲**模**乙 | **甲模乙（守卫被打掉）** |
| lightharness/src/e2b客户端.light | 本地**模**拟 | 本地模拟 |
| lightharness/src/子智能体.light | 目录**模**式 | 目录模式 |
| lightharness/src/权限.light | 危险**模**式 | 危险模式 |

**决定性反例**：`examples/test_R24_运算符单字守卫.light` L29
`断言相等(甲模乙, 3, "甲模乙 无空格取模")` —— 该文件是 R24 专门立的
**单字运算符守卫**，明确要求 `模` 在无空格形态下仍作取模运算符。
⇒ **`模` 是真运算符，不能从运算符分支剔除。**

### GR-1c：收窄为「运算符字两侧汉字段均 ≥2 字才并入」

思路：运算符表达式形态恒为「单字操作数 + 运算符 + 单字操作数」（`甲加乙`/`甲模乙`），
两侧均为 ≥2 字复合词段时该字是构词成分（`整理`+`模`+`型消息`）。

**G1 结果：仍打红 1 文件**
`bootstrap/release/stdlib/文件匹配.light` —— `提供文件路径模式匹配功能`（模 左侧「文件路径」4 字、右侧「式匹配功能」5 字）。
⇒ 该形态正是「≥2 字」判据要放行的，而它当前必须保持切分。

**结论：任何让 `X模Y` 在表达式位并入的规则，都无法同时保住 `甲模乙` 与
`文件路径模式` 两种真实形态 —— 二者在词法层结构不可分。**

---

## 四、三重判据

| 判据 | 结果 |
|---|---|
| **G1 语料门** | 撤单条 **0 变化 / 0 新错**（864 可比文件） |
| **G2 编译门** | `examples/test_R35_整理模型消息边界.light` **rc=0** |
| **G3 边界门** | **F5 传参位 FAIL**（`整理`+`模`+`型消息`），F1/F2/F3/F4 通过 |

按铁律「三重判据全通过才可移除，任一失败即保留」⇒ **保留**。

---

## 五、交付物

- `light-merge/src/lexer.py` —— 该条目**保留**，注释补充 R35 不可通用化证据
- `lightharness/examples/test_R35_整理模型消息边界.light` —— 边界测试（rc=0）
- 证据脚本：`_r35_probe_根因.py`、`_r35_probe_上下文矩阵.py`、`_r35_g1_engine.py`、`_r35_g3_边界门.py`、`_r35_probe_变体diff.py`
- 变体定义：`_r35_variants.py`（`gr1_only` / `gr1c_only`）

---

## 六、遗留建议（不执行，仅记录）

若未来要清零本条，只有两条路，均需**语义层**而非词法层：

1. **parser 级**：让 parser 在 `X 模 Y` 且两侧均已声明为数值名字时才当取模，
   词法层统一并入标识符 —— 改动面极大，不建议。
2. **语言层**：把取模改用运算符符号（如 `%`）而非汉字 `模`，从根上消除歧义。
