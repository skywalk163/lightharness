# 第44轮 任务4 交付报告 —— L-163 / L-164 修复（bash 装配契约 + 状态字面值）

> 日期：2026-09-16 ｜ 优先级 P1 ｜ 修改区域：**仅** `src/工具_bash.light` + `src/工具执行.light`

## 一、结论

- **L-163 已修复**：`注册bash工具` 改走 `具注册工具`（对齐 `工具执行` 的 dict 注册表）；
  `bash处理` 形参扩为 `(参数, 取消令牌 = 空)`，对齐 `执行工具` 的 `处理(参数, 令牌)` 调用约定。
- **L-164 已修复**：执行状态字面值**保持 `"ok"` 不变**（避免破坏既有硬编码断言），
  改为「常量化 + 显式文档 + 直觉别名」消除 `"ok"` vs `"normal"` 歧义。
- `examples/test_R43_真实agent循环.light` **已删除 R43 的 bash 2 参适配器绕行**，rc=0。

## 二、L-163 修复

### 2.1 症状复盘（R43 登记）

1. `注册bash工具(注册表)` 调 `注册表.注册(...)` →
   `AttributeError: 'dict' object has no attribute '注册'`
   （`工具执行.建注册表()` 返回普通 dict，工具挂在 `注册["工具表"]`）。
2. `执行工具` 以 `处理(参数, 执行器["令牌"])` 调 handler，而 `bash处理` 只声明 1 个形参 →
   `bash处理() takes 1 positional argument but 2 were given`（光明对形参数量**严格**，不忽略多余参）。

### 2.2 改法（`src/工具_bash.light`）

```light
从 工具执行 导入 具注册工具, 级公开

# 形参带默认值：既有单参调用方零影响
段落 bash处理 接收 参数, 取消令牌 = 空:
  ...

# 改走 具注册工具，对齐 dict 注册表
段落 注册bash工具 接收 注册表, 级别 = 级公开:
  设 定义 为 造bash工具定义()
  具注册工具(注册表, 定义["name"], 级别, 定义["execute"], 空)
  返回 真
```

### 2.3 兼容性论证（动手前的静态核查）

| 入口 | 外部调用方 | 处理 |
|---|---|---|
| `bash处理` | `test_R42_bash跨平台.light`（3 处）、`test_R42_真实工具链.light`（1 处），**均为单参调用** | 新增形参**带默认值 `空`** → 零影响 |
| `注册bash工具` | **无任何外部调用方** | 可安全改签名 |
| `造bash工具定义` | `src/核心工具.light` | 未改动 |

另核查：`核心工具.light` 的 `注册表.注册(...)` 用的是**另一套** `工具.工具注册表` 抽象
（确实有 `.注册` 方法），**不是 bug**，不在本次改动范围（也符合「只改两个文件」铁律）。
循环依赖核查：`工具执行.light` **无任何 import**，故 `工具_bash` 反向导入它不会成环。

## 三、L-164 修复（`src/工具执行.light`）

### 3.1 关键取舍：不改字面值

`调正常` 当前值是 `"ok"`。但 `examples/test_R40_端到端互举.light:144` 直接硬编码断言：
```light
断言相等(结果["状态"], "ok", "#10 执行状态=ok")
```
改成直觉的 `"normal"` 会**破坏既有测试**，与「改后既有测试不破」铁律冲突。

⇒ **保持 `"ok"`**（wire 兼容），用「常量化 + 文档 + 别名」消除歧义——这也正是任务书给的
「**或**导出常量 `调正常`，消除 'ok' vs 'normal' 歧义」这一选项。

### 3.2 改法

```light
# L-164：以上执行状态字面值请勿硬编码。
#   成功态字面值是 "ok"，不是直觉的 "normal"——既有 test_R40 直接断言 "ok"，
#   为保持 wire 兼容不改值。调用方统一取 调正常 常量即可消除歧义；
#   另导出直觉别名 调成功（与 调正常 同值）。
设 调正常 为 "ok"
...
设 调成功 为 调正常
设 状态字面值表 为 [调正常, 调拒绝, 调限流, 调取消, 调失败]
```
并新增导出 `调成功`、`状态字面值表`。

## 四、装配层去绕行（任务书目标 3）

`examples/test_R43_真实agent循环.light`：
```
R43：具注册工具(执行注册, "bash", 级公开, 我的bash处理, 空)   # 自写 2 参适配器
R44：注册bash工具(执行器["注册"])                              # 直接调用，无适配器
```

## 五、验证

| 用例 | 结果 |
|---|---|
| `test_R42_bash跨平台.light`（单参 `bash处理`，向后兼容） | **rc=0** ✅ |
| `test_R42_真实工具链.light`（单参 `bash处理`，向后兼容） | **rc=0** ✅ |
| `test_R34_集成测试.light`（工具执行 + `调正常` 常量） | **rc=0** ✅ |
| `test_R40_端到端互举.light`（硬编码 `"ok"` 断言） | **rc=0** ✅ |
| `test_工具系统.light` / `test_agent工具调用.light`（核心工具注册） | **rc=0** ✅ |
| `test_R43_真实agent循环.light`（去绕行后） | **rc=0** ✅ |
| 本机全量 pytest（改 src 后） | 见「路M收口」——基线外零新增红 |

## 六、约束核对

- **只改 `src/工具_bash.light` 与 `src/工具执行.light`**（另改了依赖它们的 1 个 examples 测试以去绕行）。
- 改后既有测试不破（6 项受影响用例 rc=0 + 全量 pytest 回归）。
- L-163 / L-164 已在 `docs/功能对标/语言缺陷账.md` 标记 **已修复**。
