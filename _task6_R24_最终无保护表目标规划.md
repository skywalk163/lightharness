# 第24轮 最终无保护表目标规划

> 日期：2026-09-15 ｜ 供后续轮次参考
> 背景：第24轮完成阶段D单字通用化的部分工作（之被吞缺陷修复+CR1规则+死代码清理+递归语义类别代数化），单字保护表CS保持30条（语义层非冗余）。本文档规划最终消除所有逐词白名单的路径。

---

## 一、当前保护表盘点（第24轮后）

| 表 | 条数 | 性质 | 可通用化程度 |
|---|---|---|---|
| _COMPOUND_SAFE_SINGLE_KEYWORDS | 30 | 递归副作用承重墙（80%真护栏） | 低（需深度递归语义规则化） |
| COMMON_COMPOUND_WORDS | 37 | 真护栏（运算符前缀/异步*/设*/错误类型） | 中（部分可由上下文规则覆盖） |
| _EMBED_MAX_MATCH_KEYWORDS | 3 | 纯嵌入场景依赖（为/返回/尝试） | 中（单字通用化后可能覆盖更多） |
| OPERATOR_VERBS | 19 | 运算符语义核心 | 不动 |
| _OPERATOR_KEYWORDS（派生） | 27 | 运算符关键字 | 不动 |
| _VALUE_LITERAL_KEYWORDS | 3 | 值字面量（假/真/空） | 不动（语义核心） |
| **逐词白名单合计** | **70** | CS 30 + CCW 37 + _EMBED 3 | |

> 注：OPERATOR_VERBS和_VALUE_LITERAL_KEYWORDS是语义核心关键字，不是"保护表"（不是为了弥补词法缺陷而加的白名单），最终目标中保留。

---

## 二、各表演进路径

### 2.1 _COMPOUND_SAFE_SINGLE_KEYWORDS（30条）——最难，需多轮

**第24轮结论**：80%真护栏，语料冗余仅20%，语义层非冗余不可简单删除。

**保护场景分布**：
- 词尾并入：17条（属性名/类名/调用名末字，如标准输出/日期列/规则/文件未找到/首异常/最后索引/符号链接/截断/月末/成绩的长度/标准输出失真/有界队列类/测试/最后心跳/通过/块长/配[0]/段[下标]）
- 词中并入：6条（递归副作用承重墙，如乘阶乘/用例/加斐波那契/对于归属/自定义表/自闭合标签集）
- 词首并入：1条（模：模拟客户端类名，已在_P0A_NEVER_SPLIT）

**通用化路径**：
1. **词尾并入17条**：可由"单字后缀位置规则"覆盖（scan_pos>0 + 单字 + 非运算符 + 非值字面量 + 非排除集 → 并入标识符）。第23轮已实现多字版本，单字版本需扩展。但必须保住"词尾仍作KEYWORD"的既有形态（返回真/配[0]/遍历块长之等边界）。
2. **词中并入6条**：依赖`_skip_compound_safe_and_match`的递归副作用语义。需将递归副作用规则化——"非语句起始位置 + 单字运算符/关键字后跟汉字/字母 + 整串不是合法关键字序列 → 整体成标识符"。第24轮任务4已做类别代数+等价断言（运行路径零改动），下一步需实际替换运行路径。
3. **词首并入1条（模）**：已在_P0A_NEVER_SPLIT，P0-A层承担保护，表内冗余度可进一步验证。

**建议轮次**：第25~26轮，分两步：
- 第25轮：词尾并入17条的单字后缀规则实现+验证
- 第26轮：词中并入6条的递归副作用运行路径替换+验证

### 2.2 COMMON_COMPOUND_WORDS（37条）——中等，可逐批

**剩余37条分类**：
- 运算符前缀：位与/位异或/位或/位非（4条，`位`是单字但不在CS中）
- 异步*系列：异步写入文件/异步睡眠/异步读取文件/异步追加文件（4条，有codegen映射）
- 设*系列：设指针/设指针值/设系统/设系统错误码/设置/设置数组（6条）
- 错误类型：导入错误/类型错误/零除错误（3条）
- 其他真护栏：低级关闭/函数对象/常量时间比较/幂次/并发等待/应当/开启调试/当前/当然/最终答案/枚举值/正则匹配/测试_生成问候语/环境枚举/生成分析/生成问候语/终点/结构体值/记录类型（19条）
- 除非（1条，控制流关键字）

**通用化路径**：
1. 有codegen映射的（异步*/并发等待/常量时间比较等）：验证code_generator中已有登记后从CCW删除
2. 错误类型（导入错误/类型错误/零除错误）：验证stdlib中有对应定义后从CCW删除
3. 运算符前缀（位与/位异或等）：`位`不是关键字，这些词在P0-A确定性切词下应不拆分，可验证后删除
4. 其他真护栏：逐条验证上下文规则覆盖后删除

**建议轮次**：第25轮可与CS词尾并入并行，分批处理。

### 2.3 _EMBED_MAX_MATCH_KEYWORDS（3条）——中等

**当前状态**：第22轮验证不冗余（撤掉后17文件token变化，返回结果/尝试记录等纯嵌入场景）。

**通用化路径**：
- 单字通用化后（CS的递归副作用规则化），`为`/`返回`/`尝试`的纯嵌入场景可能被通用规则覆盖
- 需在CS词中并入规则化后重新评估_EMBED是否冗余
- 如果仍不冗余，分析具体场景并设计针对性规则

**建议轮次**：第26轮（CS词中并入规则化后）重新评估。

---

## 三、最终目标

**词法层结构**：
1. **上下文规则**（非逐词白名单）：
   - 非语句起始位置最大匹配（第21轮）
   - 后缀位置上下文规则（第23轮，多字+单字扩展）
   - CR1成员访问段首规则（第24轮）
   - 递归副作用规则化（待实现，词中并入）
   - `之`成员访问符守卫（第24轮）
2. **内建名登记**（code_generator.py，非词法层白名单）：
   - 有codegen映射的复合名（异步读取文件/并发等待等）
3. **语义核心关键字**（保留，不是保护表）：
   - OPERATOR_VERBS（19条运算符）
   - _VALUE_LITERAL_KEYWORDS（3条值字面量）
   - ALL_KEYWORDS（语句/控制流/定义关键字）

**与上游对齐**：上游deepseek-harness无汉字保护表（ASCII词法以空白/符号为天然边界）。光明最终目标是词法层只保留"上下文规则+内建名登记+语义核心关键字"，消除所有"为弥补词法缺陷而加的逐词白名单"。

---

## 四、风险与缓解

| 风险 | 说明 | 缓解 |
|---|---|---|
| 递归副作用运行路径替换引发回归 | 第24轮验证纯类别推导直接替换会导致26文件漂移（丢失乘/减/加/除/模/真/空/除8个"语义双栖"字的递归资格） | 分步替换，每步全语料token A/B验证；先做词尾并入（风险低），再做词中并入（风险高） |
| 单字后缀规则破坏"词尾仍作KEYWORD"形态 | 返回真/配[0]/遍历块长之等边界 | 排除集设计+专项测试+全语料验证 |
| CCW删除破坏codegen行为 | 有codegen映射的复合名删除后codegen可能找不到名字 | 验证code_generator登记存在性+codegen产物sha256对比 |
| _EMBED重新评估后仍不冗余 | 纯嵌入场景可能无法被通用规则完全覆盖 | 保留_EMBED作为最后兜底，不强制清零 |

---

## 五、建议轮次规划

| 轮次 | 方向 | 预期成果 |
|---|---|---|
| 第25轮 | CS词尾并入17条单字后缀规则 + CCW分批精简 | CS 30→约13条，CCW 37→约20条 |
| 第26轮 | CS词中并入6条递归副作用运行路径替换 + _EMBED重新评估 | CS 13→约7条，_EMBED可能清零 |
| 第27轮 | 剩余真护栏逐条处理 + 最终清理 | 逐词白名单清零或接近清零 |
| 第28轮+ | 回到上游复刻主线 | — |

> 注：以上为乐观估计，实际进度取决于每轮的验证结果和回归情况。单字表是最"实"的保护表，通用化难度远大于多字表。

---

## 六、证据链

- 第22轮：_task3_R22_COMPOUND_SAFE_SINGLE精简_交付报告.md（52→30条，删22条冗余）
- 第23轮：_task6_R23_阶段D规划.md（阶段D初步规划）
- 第24轮：_task2_R24_单字分类清单.md（30条逐条验证+分类）、_task3_R24_P0A复合安全通用化_交付报告.md（CR1规则+CS保持30条决策）、_task4_R24_递归语义规则化_交付报告.md（类别代数+175自由名验证）
