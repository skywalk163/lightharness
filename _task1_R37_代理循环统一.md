# 任务1（R37）交付报告：代理循环统一（R34-D3）

> 目标：消除 `stdlib/代理循环.light`（旧版）与 `src/代理循环.light`（新版）**同名冲突**。
> 采用方案：**方案B（重命名）**——stdlib 版重命名为 `代理运行时`，src 版保持 `代理循环`。

---

## 一、差异分析（先分析后决策）

### 1.1 「代理循环」实为**三处**，不是两处
| # | 位置 | 层次 | 形态 | 导入方 |
|---|---|---|---|---|
| ① | `stdlib/代理循环.light`（旧，41.6KB） | **应用层**（会话+大模型调用+工具执行+线程并行） | 模块：`导出 会话 代理循环` | **0（孤儿）** |
| ② | `src/代理循环.light`（新，R34，18.9KB） | **核心层**（纯状态机：轮次/步骤/检查点/中止/事件/投影） | 模块：导出 60+ 函数 | 2 个 examples 测试 |
| ③ | `src/代理.light` 内的 `类 代理循环` | 驱动层（对齐上游 agent.ts） | **类** | 17 处 `从 代理 导入 代理循环` |

R34-D3 登记的是 ①↔② 的**模块同名冲突**；③ 是同名的**类**（与本议题无关，17 处引用全部走 `从 代理 导入`，不受影响）。

### 1.2 R34-D3 登记前提**实证证伪**
任务书原文：「模块搜索路径 **stdlib 优先于 src**，`从 代理循环 导入` 会导入 stdlib 旧版」。
实测**不成立**：
- `运行.py:_setup_paths` 用 `for p in [STDLIB, ROOT, SRC, ...]: sys.path.insert(0, p)` →
  最终顺序 **`SRC` > `ROOT` > `STDLIB`**（后者被 insert(0) 压到前面）；
- `.light` 导入钩子 `_light_import_hook.install([SRC, STDLIB, ROOT])` 亦以 **SRC 优先**；
- **反证**：`examples/test_代理循环.light` / `test_R34_集成测试.light` 导入的符号（`建循环`/`轮次待启`/`循轮次后继`…）
  **只存在于 src 新版**，两测试 **rc=0** → 解析对象是 src，而非 stdlib。

### 1.3 关键发现：`stdlib/代理循环.light` 是**孤儿**
全语料搜索 `从 代理循环 导入` / `导入 代理循环`：**仅 2 处**（上述两测试），且均需 src 独有符号。
→ **无任何代码导入 stdlib 旧版**（其余 "代理循环.light" 出现全在注释里）。

### 1.4 功能重叠度
两者**几乎零重叠**：旧版是应用层（LLM 调用 + 工具线程并行 + 退避重试 + 会话 token 预算），
新版是核心层（纯状态机，不含任何 LLM 调用）。**不可合并**（层次不同）。

---

## 二、方案对比与选择

| 方案 | 内容 | 评估 |
|---|---|---|
| A 合并 | 并入 src/代理循环.light，删 stdlib 版 | ✗ 应用层与核心层**层次不同不可合并**；且违背「纯状态机」定位 |
| **B 重命名** ✅ | stdlib 版 → `代理运行时`（应用层），src 版保持 `代理循环`（核心层） | ✅ 零回归（旧版是孤儿，无引用需改）；保留旧版历史/参考价值；命名即分层 |
| C 迁移+重命名 | stdlib 版 → `src/代理运行时.light`，删 stdlib 版 | △ 等效，但「消除 stdlib 优先问题」的立论前提已被 §1.2 证伪，且搬动仓库归属无必要 |

**选 B**。理由：① 立论前提（stdlib 优先）不存在，最小改动即达成「消除同名冲突」；
② 旧版是孤儿 → 重命名**零引用更新、零回归**；③ 不删有价值代码（旧版应用层骨架留作参考）。

---

## 三、实现（已落地）

1. `git mv stdlib/代理循环.light stdlib/代理运行时.light`（保留 git 历史）。
2. 更新被重命名文件**头部注释**：写明原名、R34-D3 由来、层次分工（应用层 vs 核心层）、
   以及「零回归」的实证依据（src 优先 + 孤儿）。
3. 更新其余 stdlib 文件中指向旧文件名的 **comment 引用**（`代理循环.light` → `代理运行时.light`）：
   - `stdlib/事件总线.light`（2 处）
   - `stdlib/代理工具集.light`（2 处）
   - `stdlib/并发.light`（1 处）
   - `stdlib/重试.light`（4 处）
   （均为注释路径指针；行号为历史遗留、本轮不动。）

> 兼容旧类名：模块内的 `类 代理循环` 保持不变（模块重命名已消除**模块名**冲突；
> 该类现以 `代理运行时.代理循环` 限定访问，且无人导入）。

> ⚠️ **行尾约定（过程中踩坑并已修正）**：仓库 `core.autocrlf=true`（blob 存 LF，工作树应为 CRLF）。
> 首轮用 Python `open(f,'w',newline='')` 批量改写 comment 引用，导致 4 个 stdlib 文件 **CRLF→LF**，
> 与工作树约定不符。已 `git checkout --` 还原后改用 **Edit 工具**（保留 CRLF）重做替换。
> 终态：`事件总线/代理工具集/并发/重试` 四文件均 **纯 CRLF、LF-only=0**。

---

## 四、引用更新核查（全语料）

| 项 | 结果 |
|---|---|
| 代码引用（`从 代理循环 导入`/`导入 代理循环`） | 2 处，均解析到 **src** 新版，**无需改动** |
| .py / .json 代码引用 | **0**（仅历史探针 `_antirun_*.py` 与快照，非构建产物） |
| comment 路径引用 `代理循环.light` | 已全部更新为 `代理运行时.light`（4 文件 9 处） |
| 残留合法引用 | `src/代理循环.light`（自身）、`examples/test_代理循环.light`（测试文件名）、docs（归路M） |

---

## 五、验证

- **token 零变化**：改动 5 文件（重命名 1 + comment 4）**逐文件 token 流与 HEAD 完全一致**
  （`_r37_task5_perf.py`，过滤 EOF/NEWLINE 口径）。
- **两导入测试**：`test_代理循环.light` rc=0、`test_R34_集成测试.light` rc=0（复测确认）。
- **集成测试**：`examples/test_R37_集成测试.light` rc=0（两模块共存导入无冲突）。
- **全量回归**：`pytest tests/test_回归.py -q` → **382 passed / 0 failed**（20m45s，Windows 串行）。
  → **零回归**（R36 时该套件 381 passed，本轮新增 R37 集成测试 +1）。

---

## 六、遗留发现（登记，非本轮范围）

1. **另有两处同名分层**：`加密.light`、`重试.light` 同时存在于 `src/` 与 `stdlib/`。
   二者是 R37 范围外的既有分层（src 优先，未报冲突），已登记，建议后续轮次按同法评估是否统一。
2. **docs 陈旧引用（归路M 回填）**：`docs/功能对标/行为差异清单.md:566` 仍描述
   「stdlib 目录下已有旧版 `代理循环.light`」——本轮重命名后该表述已过期，须在 docs 回填（#158）时更正。
   另 `对标清单.json:2357` 的 `lightharness/src/代理循环.light` 指向 src，**仍正确、无需改**。
3. 全语料扫描：`代理循环.light` 其余文本出现均为 **自身/测试文件名/R37 断言/重命名文件头部注释**，
   无遗漏的陈旧指针。

---

## 七、交付物
- `lightharness/stdlib/代理运行时.light`（重命名自 `代理循环.light`，头部注释更新）
- `lightharness/stdlib/{事件总线,代理工具集,并发,重试}.light`（comment 引用更新）
- 本报告 `_task1_R37_代理循环统一.md`
