# 外发任务 W2：lightharness 登录认证模块（纯新增）

> 定位：本任务是"lightharness 网页启动"四路并行中的 **W2 认证模块**。产出独立的令牌
> 生成/校验模块，供 W3（服务端路由）与 W4（启动器）按 `总览.md` 契约 2/3 调用。
> 纯新增文件，不碰任何现有文件。

## 白名单文件（只新建这两个）

- 新建 `src/认证.light`
- 新建 `examples/test_认证.light`

## 功能要求（按总览契约 2，签名一字不差）

`src/认证.light` 导出三个段落：

```
从 认证 导入 初始化认证, 取令牌, 校验令牌

初始化认证()          # 幂等：首次生成并保存 32 位十六进制令牌；再次调用返回同一令牌，不覆盖
取令牌()              # 返回当前令牌字符串；未初始化 → ""
校验令牌(令牌)        # 空 → 假；等于当前令牌 → 真；否则假
```

实现要点：

1. **令牌生成**：`从 随机 导入 安全随机令牌`（`stdlib/随机.py` 已有，`安全随机令牌()` 返回
   hex 字符串，直接用，**不要自造随机**）。若想控制长度，可在其输出上做校验/截取，
   但默认直接采用其返回值即可；须保证 32 位十六进制或更长。
2. **固定令牌**：`初始化认证()` 先读环境变量 `HARNESS_WEB_TOKEN`（`环境变量("HARNESS_WEB_TOKEN", "")`）；
   非空 → 直接用该值当令牌；为空 → 用 `安全随机令牌()` 生成。**幂等**：模块级单例保存，
   第二次调用 `初始化认证()` 时若已有令牌，直接返回，不重新生成、不覆盖固定令牌。
3. **模块级单例**：光明类体限制与跨作用域约定——用模块级 `设 当前令牌 为 ""` + 段落读写
   （参考 `src/web服务器.light` 的 `全局服务` 单例写法：模块级 `设` + 段落里 `设 当前令牌 为 …`）。
4. **校验**：`校验令牌(令牌)`：`令牌 == 空 或 令牌 == ""` → 假；`令牌 == 当前令牌` → 真；否则假。
   返回布尔，**用 `返回 真/返回 假`，不要返回字符串**。

## 语言坑提醒（历批踩过，直接规避）

- 断言一律用 `断言相等`（L-037 曾是 no-op）；字符串比较直接用 `==`。
- 环境变量读取：`环境变量(名, 缺省)`（已用于 `examples/运行Web服务器.light`）。
- 成员赋值 `设 己.X 为 …`；模块级变量在段落里直接 `设 变量 为 …` 即可。
- 发现"写不出/写错/绕不过"→ 登记 `docs/功能对标/语言缺陷账.md`，别顺手改编译器。

## 验收判据（每条立得住反跑）

1. `python 运行.py examples/test_认证.light` 退出码 0，断言全过。
2. 测试用例覆盖：
   - 初始化后 `取令牌()` 非空且长度 ≥ 32 的十六进制；
   - 两次 `初始化认证()` → `取令牌()` 相同（幂等）；
   - `校验令牌(取令牌())` → 真；`校验令牌("错误令牌")` → 假；`校验令牌("")` → 假；
     `校验令牌(空)` → 假；
   - 未初始化时 `取令牌()` → `""`、`校验令牌(任意)` → 假（用独立进程/独立测试段验证，避免污染单例）；
   - `HARNESS_WEB_TOKEN=abc123` 时初始化 → `取令牌()` == "abc123"（固定令牌分支）。
3. **反跑**：把 `校验令牌` 的相等判断改成不等（或把返回布尔取反），测试必须立红。

## 交付物

- `src/认证.light` + `examples/test_认证.light`。
- 交付报告：导出签名确认（贴最终 3 个签名）+ 测试运行输出 + 反跑验证记录。
- 说明：若 `安全随机令牌` 的实际返回格式与契约有出入（如不足 32 位、非 hex），在报告中如实记录
  并给出适配写法，**不要擅自改 `stdlib/随机.py`**。

## 运行通道

```powershell
cd G:\dswork\duan-light-merge\lightharness
python 运行.py examples/test_认证.light
```
