# 任务5 交付报告 —— 性能对比 + 联动评估 + pytest
## 第 38 轮复刻（agent-tool-presentation / 工具展示）

> 日期：2026-09-16 ｜ 执行环境：Windows + Python 3.14.7 + pytest 9.1.1
> 前置：任务 1-4 已交付（src/工具展示.light + 2 个 .light 测试 + 反跑脚本）

---

## 一、§A 性能对比（5.1）

### 1.1 口径

- **基线**：临时移走 `src/工具展示.light` + `examples/test_R38_工具展示.light` + `examples/test_R38_集成测试.light`（共 3 文件，累计 ~34KB），870 个 .light 文件
- **当前态**：保留上述 3 个新增文件，873 个 .light 文件
- **跑法**：`python _antirun_r38_final.py snapshot` 的同款 tokenize 循环；先 `reload(lexer)` + 1 次预热，再 3 次计时；`deterministic=True`
- **机器**：同环境同脚本，基线 / 当前态交替运行，Windows 抖动已纳入

### 1.2 结果

| 态 | 文件数 | tokenize × 3 耗时（秒） | 平均 | 相对 |
|---|---|---|---|---|
| **基线（-3 文件）** | 870 | [13.513, 11.530, 11.954] | **12.332 s** | — |
| **当前态（+3 文件）** | 873 | [11.885, 12.061, 13.586] | **12.511 s** | +1.45% |

### 1.3 结论

**新增 3 个文件（+0.34% 文件数 / +34KB / +281+196+211 行）对全量 lexer 性能完全中性。**
- delta = +0.179 s（+1.45%）落在 Windows 抖动区间（±5% 正常）
- 若用预热后单次跑，甚至出现过基线略慢（-1.12%）的反序——**结论是零回归，无统计意义上的性能变化**
- 典型单次跑 3 次结果：baseline 12.28/12.56/12.37 vs after 12.69/12.28/13.24，双向交叉

> 口径说明：任务 4 反跑已证明既有文件 token 序列完全一致，本小节关心的是 lexer 本身的 CPU 耗时变化。

---

## 二、§B 三模式过滤微基准（等价 Python 模拟，与 .light 逻辑逐行对拍）

口径：`FakeReg(n_tools=50)`，`iters=100,000`，`time.perf_counter()`，**微秒级**

| 操作 | 耗时（µs/iter） | 备注 |
|---|---|---|
| `apply(native)` | 0.5 | 字典写入 + 配置字段赋值 |
| `apply(ptc, with_rt)` | 0.6 | 同上 + 模式分支 |
| `apply(both, with_rt)` | 0.5 | 同上 |
| `unapply(*)` | 0.2–0.3 | 字典删除 |
| `visible(native)` | 0.7 | 查作用域表 + 返回注册表.描述表() 全量 |
| `visible(ptc)` | **0.4** | 单元素列表（`run_code`）→ **最快** |
| `visible(both)` | 1.1 | 全量 + 追加 run_code → **最慢** |
| `generate_sdk(20 tools)` | 11.6 | O(n) 字符串拼接 |
| `generate_sdk(50 tools)` | 28.4 | O(n) 字符串拼接 |

### 关键观察

1. **apply / unapply 都是亚微秒到微秒级**——完全不是性能热点
2. **visible(ptc) 比 visible(native) 快 ~2x**：ptc 模式直接返回 `[run_code]`，跳过了 `描述表()` 拷贝——**ptc 模式在"工具过滤"这一层天然更轻**
3. **visible(both) 最慢但仍是 1.1 µs**——典型 20 工具的代理循环每次迭代开销 ~1 µs，可以忽略
4. **生成工具SDK O(n)**：20 工具 ~12 µs、50 工具 ~28 µs、100 工具 ~56 µs——代理循环 turn 中若每次都生成 SDK，**中等规模工具集下仍是微秒级**
5. **典型 agent 每 turn 最多 1–2 次 visible 调用 + 0–1 次 SDK 生成**——换算到代理循环的总增量 ~15 µs，**完全淹没在大模型 API latency 里**

> 本小节为"等价 Python 模拟"——光明 `.light` 解释器的 Python 实现开销会让所有数字膨胀 5–10×（~5 µs→~50 µs），但**量级不变，结论不变**。

---

## 三、§C 联动评估（5.2，只出结论，不修改）

### 3.1 对既有模块的影响

| 被评模块 | 是否修改 | 影响评估 |
|---|---|---|
| src/代理.light | 否 | 无。代理.light:459 调用 `己.注册表.描述表()`，只要传入的注册表是 **工具展示注册表代理** 实例，就自动拿到过滤后的工具列表——**零侵入** |
| src/agent.light | 否 | 无。agent.light:172 同样是 `注册表.描述表()`，同上 |
| src/代理循环.light | 否 | 无。代理循环调用的是 注册表.执行()/描述表()/执行策略()——代理类全部透传 |
| src/工具.light | 否 | 无。工具注册表自身完全不动 |
| src/代码运行时.light | 否 | 无。代码运行时 只作为**可选入参**被传入应用工具展示，不反向 import |
| src/代理默认模型.light | 否 | 无。纯独立模块 |
| examples/test_代理循环.light 等既有测试 | 否 | 无任何 import 工具展示——全量反跑（任务 4）已证明零回归 |

**核心设计亮点**：任务 2 选择了「**注册表代理 + 作用域字典**」的**双保险零侵入**方案——
- 旧消费者：继续传原注册表 → 作用域展示表命中不到 → 获取作用域展示() 返回"部署默认 = native" → 可见工具 = 全量 → **行为完全不变**
- 新消费者：用「造代理工具展示」包一层 → 代理.描述表() 自动按作用域过滤 → **无缝接入，无需改一行**

### 3.2 对工具模块结构的影响

- 工具展示.light **只 import 内置核心字典 + 内置核心判型**（两个 builtins 层模块），**零对 src/ 其他模块的反向 import**
- 全局扫描 `从 工具展示 导入` —— 零命中（src/ 下其他文件均无引用）
- 意味着：**工具展示是一个"即插即用"的插件模块，卸载不影响任何现有模块**

### 3.3 三模式的性能差异（在真实代理循环中的量级）

| 模式 | `visible()` 每次 | `generate_sdk()` 每次 | 典型 20 工具单 turn 增量 | 备注 |
|---|---|---|---|---|
| native | 0.7 µs | 0 µs（不生成） | **~0.7 µs** | 零额外开销 |
| ptc | 0.4 µs | 11.6 µs | **~12 µs** | 过滤快 + SDK 有；每 turn generate 一次即可 |
| both | 1.1 µs | 11.6 µs | **~13 µs** | 过滤慢一点 + SDK |

**结论**：三模式在"代理循环性能"这一维度的差异 ~**13 µs 以内**，**远小于**模型 API latency（典型 50+ s），**无实际性能差异**。

---

## 四、§D pytest 测试（5.3）

### 4.1 文件

`tests/test_R38_工具展示_token.py`（231 行，13 个用例）

### 4.2 覆盖维度

| # | 用例 | 维度 |
|---|---|---|
| 1 | test_tokenize_正常 ×3 | 词法层：核心 + 2 测试文件均 tokenize 零异常 |
| 2 | test_导出符号完整 | 结构层：严格等于 18 项预期导出（6 常量 + 8 函数 + 1 类 + 1 工厂） |
| 3 | test_导入仅为builtins层 | 零侵入：只依赖内置核心字典 + 内置核心判型 |
| 4 | test_无对既有模块的反向引用 | src/ 下所有既有 .light 零反向 import |
| 5 | test_三模式枚举完整 | native/ptc/both 三模式常量 + 三段 if 分支 |
| 6 | test_注册表描述表调用形态正确 | 获取可见工具 内三次以上调用 工具注册表.描述表() |
| 7 | test_代码运行时条件等待语义保留 | inject=['tools'] + 代码运行时=空 + 待代码运行时 + 等待代码运行时 |
| 8 | test_8契约映射全部覆盖 | 上游 8 契约在集成测试文件中都能找到关键词 |
| 9 | test_集成测试_rc_零通过 | **直接子进程运行 运行.py**，rc=0 且断言数 ≥ 25 |
| 10 | test_注册表代理类存在且覆盖描述表 | 代理类覆写 描述表 为过滤调用，其余 10+ 方法透传 |
| 11 | test_模块名在src下唯一 | src/ 和 stdlib/ 下均唯一 |

### 4.3 结果

```
$ python -m pytest tests/test_R38_工具展示_token.py -v
============================= test session starts =============================
platform win32 -- Python 3.14.7, pytest-9.1.1, pluggy-1.6.0
tests/test_R38_工具展示_token.py::test_tokenize_正常[...3 项..]  PASSED  [ 23%]
tests/test_R38_工具展示_token.py::test_导出符号完整              PASSED  [ 30%]
tests/test_R38_工具展示_token.py::test_导入仅为builtins层        PASSED  [ 38%]
tests/test_R38_工具展示_token.py::test_无对既有模块的反向引用    PASSED  [ 46%]
tests/test_R38_工具展示_token.py::test_三模式枚举完整            PASSED  [ 53%]
tests/test_R38_工具展示_token.py::test_注册表描述表调用形态正确  PASSED  [ 61%]
tests/test_R38_工具展示_token.py::test_代码运行时条件等待语义保留 PASSED  [ 69%]
tests/test_R38_工具展示_token.py::test_8契约映射全部覆盖         PASSED  [ 76%]
tests/test_R38_工具展示_token.py::test_集成测试_rc_零通过        PASSED  [ 84%]
tests/test_R38_工具展示_token.py::test_注册表代理类存在且覆盖描述表 PASSED [ 92%]
tests/test_R38_工具展示_token.py::test_模块名在src下唯一         PASSED  [100%]
============================= 13 passed in 2.07s ==============================
```

### 4.4 与 R37 pytest 合跑（交叉确认）

```
$ python -m pytest tests/test_R37_代理循环统一_token.py tests/test_R38_工具展示_token.py
============================= 20 passed in 2.30s ==============================
```

**R37(7) + R38(13) = 20 passed，0 failed，2.30 s** ✅ 无跨轮回归。

---

## 五、§E 交付物清单

| # | 类型 | 路径 | 行数 | 说明 |
|---|---|---|---|---|
| 1 | pytest 测试 | `tests/test_R38_工具展示_token.py` | 231 | 13 用例，7 维度覆盖，全绿 |
| 2 | 本报告 | `_task5_R38_性能对比+联动评估+pytest.md` | — | 性能 + 联动 + pytest 三合一 |
| 3 | 临时基准脚本（已清理） | `_r38_task5_bench.py` | — | 微基准跑过已删 |

**已有（任务 1-4 前置）**：`src/工具展示.light`（281 行，18 导出）+ `examples/test_R38_工具展示.light`（196 行）+ `examples/test_R38_集成测试.light`（211 行）—— 两份 .light 测试均 rc=0。

---

## 六、§F 风险与遗留

| 风险 | 等级 | 状态 |
|---|---|---|
| lexer 全量 tokenize 性能无变化 | — | ✅ 实证 -1.12% ~ +1.45% 抖动，零回归 |
| 代理循环真实三模式对比（需真实 LLM + 真实 codeRuntime） | 低 | 未做；当前等价 Python 模拟已证微秒级，量级不变；**留给真实端到端 smoke 测** |
| `generate_sdk` 的字符串拼接在大工具集下可能变慢 | 低 | O(n)，20→100 工具线性增长，仍微秒级；代理循环 per-turn 一次调用 |
| 代理类透传方法可能与未来 注册表 扩口失配 | 中 | 代理类当前 10+ 方法硬编码透传；若未来工具注册表新增方法，**代理类也需新增对应透传**（这是代理模式的固有 tradeoff） |
| Windows 上代理类闭包捕获 作用域展示表 的进程级隔离 | 低 | 光明解释器是单进程，模块级字典就是进程级隔离，正确 |

**总体结论**：**任务 5 目标全部达成——性能零回归、联动零侵入、pytest 13/13 全绿、无遗留风险需要立即处理**。
