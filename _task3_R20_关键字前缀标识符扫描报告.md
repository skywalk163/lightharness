# 任务3 交付报告：_taskM_integration_r17 评估 + 关键字前缀标识符全项目扫描（第20轮）

> 任务来源：`复刻_第20轮_任务prompt分发_编译器回归修复轮.md` 任务3
> 日期：2026-09-13 ｜ 本路未修改 light-merge 编译器源码 ✓

---

## 一、_taskM_integration_r17.light 评估结论：**必要测试，保留修复（依赖任务1），不删除**

1. **内容**：第17轮路M整合验证——宿主运行时/宿主上下文/宿主事件/宿主工具/宿主IO/宿主配置 6 个模块的共存与接口兼容验证（22 断言），非临时探针，是有跨模块回归价值的集成测试。
2. **失败根因（实测定位）**：文件本身 71 行、写法规范（全部带空格 `为`）。失败发生在**被导入的 `src/宿主上下文.light:100`**：`设 合并为 {}` 中 `合并为` 被词法 EMBED 为-合并整体切成 IDENTIFIER(`合并为`)，解析报「期望'为'或'等于'，但得到「{」」；宿主事件.light 与本文件均因导入链在编译期连锁失败。
3. **佐证**：对 `src/宿主上下文.light` 全文 tokenize 词法本身不报错（并坏产物是合法 token 流），错误在解析层；`设 合并 为 {}`（带空格）token 形态正确（ID+KW+{）。
4. **处置**：保留。任务1（词法 `X为` 赋值尾巴收窄）落地后本文件应自然 rc=0，届时从 SKIP 移除。本路不改 src/宿主上下文.light（文件互斥）与 test_回归.py（任务6 范围）。

## 二、全项目扫描结论（`_scan_r20_keyword_prefix.py`，可复现）

```
扫描文件数: 641（lightharness src/examples/tests + light-merge examples/stdlib）
关键字前缀标识符总数（去重）: 1708
定义登记后仍切碎: 0 个；定义登记后正常: 1708 个
`设 X为 值` 赋值尾巴并坏（X为 整体成 ID）: 5 个（探针构造场景）
```

### 关键发现一：任务书例外1/2 的根因诊断为**误诊**

- 任务书判断「`去重占位`/`作用域匹配` 等以语句关键字开头的标识符在表达式上下文中被切碎」。
- 实测：在**真实文件口径**（`_scan_user_definitions` 预扫描把 `段落 X`/`设 X`/导出名登记进 user_definitions 后）,1708 个关键字前缀标识符**全部整体成词，0 个切碎**——包括 `去重占位` 与 `作用域匹配`（`src/宿主上下文.light` 全文 tokenize 通过即为证）。
- 孤立上下文（无定义登记，如 `打印 去重占位()`）确实切碎，但那种场景运行期本就是 NameError，不属于真实破坏面。
- **真正的例外1/2 根因是 `设 合并为 {}` 的 `X为` 赋值尾巴并坏**（与"关键字前缀"无关——`合并` 不是关键字），属于第19轮任务1 EMBED 为-合并的收窄缺口。

### 关键发现二：真实受影响的赋值尾巴形态（任务1 收窄范围）

| 形态 | 例 | 状态 |
|---|---|---|
| `设 X为 {}`（X 不含关键字） | `设 合并为 {}`（宿主上下文.light:100，例外1/2/r17 真因） | 并坏：ID(`合并为`) |
| 无空格赋值 `设甲为…/设和为…` | light-merge examples/basic.light:4、hello.light:16 | 并坏（light-merge 自有示例，不在 lightharness CI 内） |
| `返回真/为空值` 值字尾巴 | `返回真` | 部分并坏（见发现三①） |

### 关键发现三：第20轮任务1 在途实现（lexer.py 未提交改动）的两处收窄缺口

当前工作区 lexer.py 已含任务1 的 `_EMBED_NON_OP_KEYWORDS` 通用最大匹配（约90个非运算符关键字），实测两处缺口：

1. **单字别名绕过值字收窄**：`返回真` → ID(`返回真`)。原因：`返`（L0 单字别名）也是非运算符关键字，`返回真` 以 `返` 命中时 after=`回`（非值字）→ 合并。值字收窄只在按"最长关键字 `返回`"命中时才生效。
2. **`X为` 词尾无收窄**：`设 合并为 {}` → ID(`合并为`)。原因：`为` 命中处于词尾、after 为空串，`if _after and _after in 值字` 不成立 → 合并。任务书例外1 现象（含 r17/宿主事件 连锁失败）皆源于此。

以上两处复现一行可验：`python -c "import sys; sys.path.insert(0,'light-merge/src'); from lexer import Lexer; print(Lexer().tokenize('设 合并为 {}'))"`。已在 `tests/test_R20_词法边界.py` 以 skip 测试登记（`test_assign_tail_known_regression`），任务1 修正后翻转断言即可验收。

## 三、交付物

- 扫描脚本：`_scan_r20_keyword_prefix.py`（支持 `--deep` 扩展 light-merge 全仓；输出标识符/前缀/上下文/切碎状态/文件行号样例）
- 扫描原始输出样例见本报告第二节；完整清单可一键复现。
- 本报告（含 r17 评估结论）。

## 四、移交

- 移交任务1：第二节发现二/三的收窄清单与复现；`tests/test_R20_词法边界.py` 的 skip 测试即翻转点。
- 移交任务6/路M：r17 保留结论 → SKIP 清单中 `_taskM_integration_r17.light` 待任务1 修复后移除；test_回归.py 注释中「宿主事件:100 设 监听域 为 字典获取…」的描述与实测不符（实测错误为 `合并为` 行），建议修正注释。
