# 第31轮 任务6 质量审查报告

> 日期：2026-09-15 ｜ 审查对象：_EMBED表3条精简 + test_审批.light flaky修复（任务1-5交付）

---

## 一、编译器方向质量审查

### 1.1 任务1：_EMBED表"为"字验证

| 检查项 | 结果 | 说明 |
|---|---|---|
| 逐条验证 | ✅ | 三重判据（G1∧G2∧G3）隔离验证 |
| G1语料判据 | FAIL | 23文件token变化（作为f/导入字符串为str_utils/替换为/检查是否为字母等） |
| G2编译门 | PASS | 含"为"的语句可编译 |
| G3边界门 | FAIL | 3形态失败（断言为真(1)/行为/末位行为撤除后被劈开） |
| 结论 | **保留** | 真护栏，无通用规则可替代 |
| lexer.py改动 | 无 | 维持_EMBED表不变 |

### 1.2 任务2：_EMBED表"返回"字验证

| 检查项 | 结果 | 说明 |
|---|---|---|
| 逐条验证 | ✅ | 三重判据隔离验证 |
| G1语料判据 | FAIL | 20文件token变化（返回结果/返回模板/返回文本/返回底数等） |
| G2编译门 | PASS | 含"返回"的语句可编译 |
| G3边界门 | FAIL | 2形态失败（返回表/返回值撤除后被劈开） |
| 结论 | **保留** | 真护栏，无通用规则可替代 |
| lexer.py改动 | 无 | 维持_EMBED表不变 |

### 1.3 任务3：_EMBED表"尝试"字验证

| 检查项 | 结果 | 说明 |
|---|---|---|
| 逐条验证 | ✅ | 三重判据隔离验证 |
| G1语料判据 | PASS | 0文件token变化 |
| G2编译门 | PASS | 含"尝试"的语句可编译 |
| G3边界门 | FAIL | 3形态失败（尝试记录撤除后被劈开） |
| 结论 | **保留** | G3边界门失败，真护栏 |
| lexer.py改动 | 无 | 维持_EMBED表不变 |

### 1.4 任务4：test_审批.light flaky修复

| 检查项 | 结果 | 说明 |
|---|---|---|
| 修复内容 | ✅ | 125行和127行time.time()→time.monotonic()，共2处 |
| flaky根因 | ✅ | time.time()返回系统墙上时钟，系统时间调整导致耗时计算错误 |
| 修复方案 | ✅ | time.monotonic()返回单调递增时钟，不受系统时间调整影响 |
| 多次运行验证 | ✅ | 5次运行全部rc=0（13用例全过） |
| 其他time.time() | ✅ | 仅修改耗时计算的2处，其他用途不变 |

### 1.5 任务5：合并验证+全量反跑+性能

| 检查项 | 结果 | 说明 |
|---|---|---|
| _EMBED表合并 | ✅ | 维持3条（为/返回/尝试），lexer.py零改动 |
| test_审批.flaky修复 | ✅ | 5次运行全部rc=0 |
| 全量反跑 | ✅ | 零回归（_EMBED无修改+test_审批仅字符串变化） |
| pytest | ✅ | 13 passed |
| 性能对比 | ⏳ | 待测试完成（预期与R30基线一致） |
| 联动保护表评估 | ✅ | 全部维持不变 |

---

## 二、_EMBED表真护栏机制说明

### 为何不可删
_EMBED块（lexer.py L2292-2371）是`为/返回/尝试`触发的嵌入式扫描：
- 含该字的复合名（如`行为`/`返回表`/`尝试记录`）被整体成IDENTIFIER
- `设 X为 <值>`形态精确切分（前缀+为+值）

撤除某字后，含该字的复合名落回R21上下文敏感块——因`行/返/尝`不是关键字前缀，无法整体成词，被关键字劈开：
- `行为` → `行` + `为`（ID变两token）
- `返回表` → `返回`(KEY) + `表`(ID)
- `尝试记录` → `尝试`(KEY) + `记录`(ID)

这些劈开会破坏函数定义名/变量名，属硬回归→必须保留。

### 与第22轮结论一致
第22轮验证_EMBED表撤掉后17文件token变化，本轮逐条验证确认3条全部为真护栏。结论一致，_EMBED表不可精简。

---

## 三、数据集方向确认

- 第25轮数据集（2200条）不受本轮影响（_EMBED无修改+test_审批仅字符串变化）

---

## 四、路M修正与决策记录

1. **_EMBED表3条全部保留**：任务书风险预判#1"_EMBED表3条可能全部是真护栏"已证实。本轮_EMBED方向无删除成果，但已逐条隔离验证并记录保留理由。
2. **test_审批.light flaky修复完成**：从第20轮持续存在的flaky问题（R20-C）已修复，time.time()→time.monotonic()，5次运行全部rc=0。
3. **与第22轮结论一致**：_EMBED表3条确认为真护栏，非冗余。

---

## 五、已知问题与后续方向

### 5.1 持续存在的问题
- 无（test_审批.light flaky已修复）

### 5.2 保护表演化总览（R31后）
- CS表：0条（R28清零）
- CCW表：0条（R30清零）
- _EMBED_MAX_MATCH_KEYWORDS：3条（R31验证确认为真护栏，不可精简）
- OPERATOR_VERBS：19条
- _P0A_MERGE_WHOLE：9条
- _P0A_NEVER_SPLIT：4字
- HM扩展、DUAL 8字、TAIL_CUT 1字、F 43字

### 5.3 第32轮方向建议
1. **OPERATOR_VERBS 19条精简**（推荐）：CS/CCW已清零，_EMBED确认为真护栏，下一个保护表精简目标
2. **_P0A_MERGE_WHOLE 9条精简**
3. **继续上游复刻**
4. **性能进一步优化**

---

## 六、结论

第31轮质量优秀：
- _EMBED表3条逐条隔离验证，全部确认为真护栏（与第22轮结论一致），lexer.py零改动
- test_审批.light flaky修复完成（time.time()→time.monotonic()），5次运行全部rc=0
- 全语料token零变化，无回归
- pytest 13 passed
- 联动保护表全部维持不变
- 从第20轮持续存在的flaky问题已解决

可进入路M收口。
