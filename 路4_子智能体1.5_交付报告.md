# 路 4（B）交付报告 —— subagent 1.5：父属目录 + human inbox + continuation 对齐

> 轮次：0.1.5 复刻增量 · 第 4 轮 ｜ 路：4（B）｜ 日期：2026-09-11
> 原版只读参考：`G:\github\deepseek-harness`（HEAD `9d9035b7c1`，0.1.5-rc.2 `a305303422` 的子代）
> 复刻仓库：`G:\dswork\duan-light-merge\lightharness`

## 1. 改动清单（只动本路文件）

| 文件 | 性质 | 内容 |
|----|----|----|
| `src/子智能体.light` | 改动 | 新增 0.1.5 增量：分块列表（chunked-list）、父属目录（subagent/catalog 事件 + 投影 + 校验）、目录视图（catalogView）、子智能体收件箱（SubagentInbox）；`子智能体` 类新增父会话登记面与目录失败路径 |
| `examples/test_子智能体1.5.light` | 新增 | 8 节反跑判据测试，已自动纳入 `tests/test_回归.py` 全量门禁 |

未触碰任何其它文件（工作树中 `客户端.light`/`流.light`/`目标折叠.light` 的改动属并行路 3/5，与本路无关）。

## 2. 新模块与上游文件逐行对应表

| 上游（0.1.5） | 提交 | lightharness 落点 | 对齐方式 |
|----|----|----|----|
| `packages/util/chunked-list/src/index.ts` | `9f69a7fb16`（新包） | `追加分块` / `分块展平` / `校验分块` / `分块容量` | 1:1：64 容量块栈、追加至多复制一个头块、展平按插入序、检查点严格校验（值表 min1/max64、仅 `值表/前块` 键、递归前块） |
| `packages/subagent/subagent/src/catalog.ts`（establishCatalogChild） | `2db4bdd31d` | `建立目录子项` | 1:1：`subagent/catalog` 事实 `{版本:0, 子标识, 子创建时间, 模式, 标签?}`；one-shot 标签可选、continuable 必带；只发布成功事实 |
| `catalog.ts`（subagentCatalogProjectionDefinition） | `2db4bdd31d`/`661135fe29` | 类 `目录投影`（应用事件/条目/校验状态）+ `重建父属目录` | 1:1：折叠 `subagent/catalog`、继承序号以下忽略（fork 隔离）、非法自有事实抛错拒绝恢复 |
| `catalog.ts`（subagentCatalogEntries，事件序） | `80e34ce709` | `目录投影.条目` | 1:1：经分块列表老→新展平，条目顺序 = 父目录事件顺序，与子创建时间无关 |
| `catalog.ts`（stateSchema/viewSchema 严格面） | `661135fe29` | `校验目录数据` | 1:1：未知字段/缺字段/不支持的版本/未知模式/缺标签一律抛错 |
| `packages/subagent/subagent/src/index.ts`（start 中目录失败路径） | `ba731d1c9e` | `子智能体.执行` 成功分支 | 1:1：目录追加失败 → 运行被处置（失败计数、状态失败、不登记），调用方收到目录错误；无补偿事件/回滚 |
| `packages/subagent/subagent/src/control.ts`（catalogView）+ `532e712c7b` | `532e712c7b` | `目录视图` | 纯函数复刻：行活动由运行中 Agent 状态覆盖（running/inactive）、父可用提示；运行表/父可用由调用方注入（宿主编排面不移植） |
| `packages/subagent/subagent/src/inbox.ts`（SubagentInbox） | `96ead6091d`（#3223） | 类 `子智能体收件箱` | 语义对齐：queue/steer 两目的地、`是否待处理`（hasPending）、关闭截止（ACTIVATION_CLOSING，消息不被接受）、关闭幂等（释放仅启动一次并记忆结果）、关闭不丢弃已接受待处理 |
| `control-types.ts`/`control.ts`（`delivery: 'queue'\|'steer'`） | `96ead6091d` | `子智能体收件箱.校验提示请求` | 1:1：mode 必须 `continuable`（one-shot 只读）、投递方式仅 queue/steer、缺会话标识拒绝 |
| `packages/subagent/subagent/src/projection-types.ts`（SubagentCatalogEntry） | `2db4bdd31d` | `目录投影.条目` 行结构 | 1:1：`{标识, 创建时间, 模式, 标签?}`（one-shot 无标签时不带标签字段） |

## 3. 任务细分逐条结论

1. **父属目录**：已复刻（记录/观察/事件序列出/经投影视图暴露/非法目录拒绝/目录失败后被拒运行）。fork 隔离用 `继承序号`（对应上游 `inheritedEventCount`）实现，测试验证切点前事实被忽略。
2. **human inbox controls**：已对齐（queue/steer 分路、待处理保留、关闭截止、关闭幂等、one-shot 只读、提示请求校验）。委托目标差异见 §5 移交清单。
3. **continuation 语义**：核对结论——**重构无行为变化，仅核对**。上游 `96ead6091d` 将 `continuation.ts`（1426 行）拆为 `continuation-activation.ts`（854 行）/`continuation-messages.ts`（154 行）/`inbox.ts`（70 行），`SubagentInbox` 持有的是原内联的 Activation closing promise，管理器不保留第二份消息预约状态；净行为变化仅 `SubagentPromptRequest.delivery` 字段（本路已复刻）。lightharness 现有 `子智能体` 为一次性执行模型，无 continuable 接续状态机，故无可对齐的接续语义面。
4. **chunked-list**：已并入（lightharness 无等价现有实现，按 1:1 新增纯逻辑；容量常量 64 由本工具自有，测试经 `分块容量()` 断言）。
5. **反跑判据**：见 §4，逐条「改反即红」实测成立。

## 4. 验证结果与反跑判据

定向验证（本机，`python 运行.py`）：

| 用例 | rc | 结果 |
|----|----|----|
| `examples/test_子智能体1.5.light`（新增） | 0 | 8 节全过 |
| `examples/test_子智能体.light`（既有） | 0 | 不回退 |
| `examples/test_子智能体深化.light`（既有） | 0 | 不回退 |
| `pytest tests/ -k "1.5"`（回归门禁定向） | 0 | 1 passed, 198 deselected |

反跑判据「改反即红」实测（对测试副本翻转断言，全部转红）：

| 判据 | 翻转方式 | 结果 |
|----|----|----|
| 目录事件顺序（80e34ce709） | 期望 `行表[1]=sub_丙`（顺序倒置） | 红：`实际=sub_乙 期望=sub_丙` |
| 缺父属记录即红（2db4bdd31d） | 期望成功执行后新增两条事实 | 红：`实际=4 期望=5` |
| fork 隔离改反即红 | 继承序号改 0（关闭隔离） | 红：`实际=3 期望=2` |
| 关闭幂等改反即红（#3223） | 期望释放执行两次 | 红：`实际=1 期望=2` |

测试内建「应抛未抛即红」面：非法目录（版本/缺标签/未知字段/缺标识/未知模式）、空值表块、非法投递方式、关闭后投递、one-shot 提示请求、非法请求投递方式，共 10 处。

## 5. 未移植项与移交清单

**未移植（宿主/编排面，与 §0 不做项一致）**：
- `tool-subagent` 的 HTTP Remote、Agent registry、projection-cache、session-query corpus 枚举：宿主编排，非纯逻辑。`目录视图` 以纯函数复刻，运行中集合与父可用由调用方注入。
- `types.ts` 的 `ContinuableStartSpec`/`ContinuableStart`（依赖宿主 provision 面）：登记不移植；其纯数据面（continuable 模式 + 必带标签）已在目录事实中体现。
- 持久化侧无需改动：lightharness `记录事件` 为通用「类型+字典」通道，`subagent/catalog` 天然容纳；投影缓存/水位线缓存属上游投影框架，lightharness 以即时折叠等价。

**移交清单（跨路）**：
1. **→ 路 3（代理.light）**：上游 `SubagentInbox.deliver` 把 queue/steer 委派给 Agent 收件箱（`agent.inbox.nextTurn/nextStep`、`Agent.followup/Agent.steer`）。lightharness 代理循环目前无收件箱原语，本路收件箱先自持 `下一轮表/下一步表` 两个目的地（FIFO 认领已实现）。路 3 落地代理收件箱后，可将两表替换为委托调用，准入/截止语义不变。
2. **→ 路 1（会话.light）**：无阻塞。`subagent/catalog` 依赖的仅是通用事件通道与事件序号，现有导出已足够；若路 1 引入 V3 事件词汇白名单，需把 `subagent/catalog` 纳入（登记备查）。
3. **→ 路 M（对标清单）**：`对标对象` 中 subagent 相关卡片建议加注「0.1.5：父属目录 + human inbox 已复刻（路 4）」。

## 6. 语言缺陷账

本轮未发现新的语言缺陷（未新增 L-079 条目）。沿用既有绕法：释放计数用类实例 + bound method（绕 L-078 无闭包）；零参方法调用显式带 `()`。
