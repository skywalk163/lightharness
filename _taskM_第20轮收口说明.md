# 第20轮路M收口说明 —— 编译器回归修复轮

> 日期：2026-09-14 ｜ 方向：编译器回归修复轮（清零第19轮4个CI临时例外）
> 跨项目操作：light-merge（编译器修复）+ lightharness（验证与复现）
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）

---

## 一、本轮概述

第19轮（编译器缺陷修复轮）修复了15个C级编译器缺陷，全量CI 329 passed + smoke 5/5，但引入了**4个CI临时例外**（SKIP/EXPECT_RED）。本轮目标是**清零所有临时例外，确保全量CI无例外全绿**。

路M前置探查已确认4个例外的根因：
1. **test_宿主上下文.light**（SKIP）：`去重占位(名单)` 中`去重`是关键字，表达式上下文中函数名被切碎。
2. **test_宿主事件.light**（SKIP）：`作用域匹配(...)` 中`作用域`是保留字（L-143），同样被切碎。
3. **test_L095.light**（EXPECT_RED）：HMAC对拍不匹配——后经任务2复核，**实现本身已正确**（Python标准库hmac.new），第19轮收口登记的EXPECT_RED系**状态滞后误判**（观察值5b94b938…是修复前单轮PBKDF2输出，最终提交已是标准实现）。
4. **_taskM_integration_r17.light**（SKIP）：第17轮临时集成测试，任务3修复后rc=0（22断言ALL PASS）。

---

## 二、6路交付摘要

### 任务1：词法关键字前缀标识符回归修复（核心，P0）
- **修改文件**：light-merge/src/lexer.py（91行插入/12行删除）
- **修复策略**（收敛化，非泛化）：
  - **函数调用语境守卫**：整串标识符后紧随 `(` 时绝不切分——修复 `断言为真(...)`/`判定为空(...)`/`去重占位(...)`/`作用域匹配(...)` 等含关键字的函数名被切碎问题。
  - **赋值/返回语境精确切分**：`为`/`返回`/`尝试` 三关键字在「词尾+值起始（`{ [ "` 或数字）」或「紧邻值字面量 真/假/空」时切回KEYWORD——修复 `设 合并为 {}`/`设 甲为空` 等无空格赋值尾巴的 `为` 丢失问题。
  - **明确拒绝泛化合并**：不把全部约90个非运算符关键字泛化合并（实测会引发104个解析回归：`接收参数`/`如果条件` 等合法「关键字+名」结构被误并）。
  - `去重占位`/`作用域匹配` 等用户定义名依赖 `_scan_user_definitions` 预扫描整体还原（仅在名称有定义登记时成立）。
- **复现用例**：examples/test_R20_关键字前缀标识符.light（rc=0）
- **反跑**：_antirun_r20_t1_词法关键字前缀.py → ALL OK（A判据：断裂态两用例红；B判据：修复态两用例绿；C判据：正向控制test_R20绿+词法探针过）
- **交付报告**：_task1_R20_词法关键字前缀_交付报告.md
- **附带发现**：L-152（嵌套段落形参名不被预扫描覆盖），未修，登记待后续轮次。

### 任务2：HMAC RFC 2104标准实现确认（P0）
- **修改文件**：无编译器源码修改（实现本身已正确）；更新 examples/test_L095.light（7组对拍用例）
- **核心结论**：第19轮任务4最终提交（light-merge `49319306`）的HMAC实现已是Python标准库 `hmac.new(key, msg, hashlib.sha256).hexdigest()`，标准RFC 2104语义。第19轮收口登记的EXPECT_RED「部分修复」系**状态滞后误判**——观察值 `5b94b938…` 是修复前单轮PBKDF2输出，收口时未重跑验证即登记。
- **7组对拍**（全部与Python hmac逐字节一致）：
  - 标准用例2：HMAC("密钥","消息")=51785c60…、HMAC("key","The quick brown fox…")=f7bc83f4…（RFC 4231经典）
  - 边界用例5：空密钥空消息、空密钥、空消息、长密钥（100字节>64字节块长，密钥先SHA256路径）、中文密钥/消息（UTF-8多字节）
- **复现用例**：examples/test_L095.light（更新，rc=0，从EXPECT_RED移除）
- **反跑**：_antirun_r20_t2_HMAC标准实现.py → ALL OK（A1：test_L095 7组对拍rc=0；A2：两份哈希.py直接import对拍7组全过；B：其他哈希函数MD5/SHA1/SHA256/SHA512/Base64不受影响；C：变异立红+恢复）
- **交付报告**：_task2_R20_HMAC标准实现_交付报告.md
- **补全第19轮缺失报告**：_task2_R20补全_第19轮词法扩展、_task4_R20补全_第19轮字符串哈希、_task6_R20补全_第19轮字典函数_D批次

### 任务3：关键字前缀标识符全扫描 + _taskM_integration_r17修复（P1）
- **修改文件**：无编译器源码修改；新增 examples/_taskM_integration_r17.light（修复后提交）
- **全项目扫描**：扫描lightharness/下所有.light文件，找出所有以关键字开头的标识符，建立完整清单。区分语句开头位置（已正确处理）vs 表达式上下文（会被切碎）。扫描报告含完整清单、统计、被切碎的标识符列表。
- **_taskM_integration_r17评估**：读取该文件（第17轮路M临时集成测试，3851字节），评估为有价值的集成测试（覆盖多个模块协同行为），修复后rc=0（"第17轮整合验证 ALL PASS (22断言)"），从SKIP移除并提交。
- **交付物**：_task3_R20_关键字前缀标识符扫描报告.md + _scan_r20_keyword_prefix.py（可复现扫描脚本）
- **交付报告**：_task3_R20_关键字前缀标识符扫描报告.md

### 任务4：词法边界测试套件建设（P1）
- **修改文件**：无编译器源码修改；新增测试用例和pytest测试
- **测试覆盖**（100+场景）：
  - 50个关键字前缀标识符场景（去重占位/作用域匹配/排序函数/遍历器/筛选器/求和函数/读取器/输出流/调试器/跳过标记/跳出循环/继续标记/等待器/推迟执行/映射表/生成器/加载器/关闭标记/开启标记/保护器/使用者/定义表/实现类/继承链/抽象类/最终值/构造器/枚举值/类型表/模块名/函数表/段落名/情况表/那么分支/否则分支/如果条件/当循环/尝试块/捕获块/抛出异常/打印函数/新建对象/返回值/设值器/等于判断等）
  - 30个合法关键字语句场景（如果/否则/否则若/遍历/当/尝试/捕获/抛出/打印/新建/返回/设...为.../去重列表/作用域:/异步作用域:/外部回调...等，确保修复不破坏合法语法）
  - 20个边界混合场景（关键字前缀标识符作为函数参数/返回值/字典键/列表元素/嵌套调用/与合法关键字语句相邻等）
- **三层覆盖**：运行期自校验.light（test_R20_词法边界综合.light）+ pytest token层验证（tests/test_R20_词法边界.py）+ 全项目扫描脚本
- **复现用例**：examples/test_R20_词法边界综合.light（rc=0）
- **pytest测试**：tests/test_R20_词法边界.py（token切分验证）
- **交付报告**：_task4_R20_词法边界测试套件_交付报告.md
- **注意**：L-152（嵌套段落形参关键字前缀）未修，测试用例中该处形参改用 `算子` 并就地加注。

### 任务5：第19轮反跑脚本审查 + 交付报告规范总结（P2）
- **修改文件**：无
- **审查结论**：
  - 识别出第19轮test_L095反跑验证不充分的根因：反跑T4只验证「变异立红+恢复」，未验证修复后HMAC输出与标准对拍正确——这是部分修复未被发现、收口误判EXPECT_RED的直接原因。
  - 其他5路反跑验证基本充分。
  - 建议：后续轮次反跑脚本必须包含「修复后输出正确性验证」（不只是rc=0），尤其是对拍类/数值类/输出类修复。
- **交付报告规范总结**：基于第19轮3份缺失报告的教训，总结交付报告的必要要素（修复文件清单/修复策略/复现用例/反跑判据/不破坏回归验证/已知局限/与上游差异）。
- **交付物**：_task5_R20_反跑脚本审查报告.md

### 任务6：docs更新草案 + 临时例外清理验证 + 收口框架（P2，路M前置）
- **修改文件**：tests/test_回归.py（临时例外清理）
- **临时例外清理**：从test_回归.py移除4个第19轮临时例外——
  - SKIP移除：test_宿主上下文.light、test_宿主事件.light、_taskM_integration_r17.light
  - EXPECT_RED移除：test_L095.light
  - 清理后：SKIP仅2个helper文件（_helper_L004/_helper_L007b），EXPECT_RED仅L-101/L-143（合规保留字，预期行为，非临时例外）
- **docs更新草案**：对标清单#128~#130、缺陷账L-095订正+L-151/L-152新增、行为差异R20-D1~D3的具体修改内容（归路M统一写入）
- **例外清理验证脚本**：_verify_r20_exception_cleanup.py（逐一验证4个例外rc=0）
- **收口说明框架**：_task6_R20_收口说明框架.md
- **交付物**：_task6_R20_docs更新草案.md + _task6_R20_临时例外清理验证报告.md + _task6_R20_收口说明框架.md + _verify_r20_exception_cleanup.py

---

## 三、验证结果

### 3.1 4个临时例外逐一验证
| 例外 | 修复前 | 修复后 | 修复方式 |
|---|---|---|---|
| test_宿主上下文.light | SKIP（rc=1） | **rc=0** | 任务1词法函数调用语境守卫 |
| test_宿主事件.light | SKIP（rc=1） | **rc=0** | 任务1词法函数调用语境守卫 |
| test_L095.light | EXPECT_RED（rc=1） | **rc=0** | 任务2确认实现已正确，更新7组对拍用例，移除误判登记 |
| _taskM_integration_r17.light | SKIP（rc=1） | **rc=0** | 任务3修复（22断言ALL PASS） |

### 3.2 第20轮新增反跑（2路）
- T1 词法关键字前缀：ALL OK
- T2 HMAC标准实现：ALL OK

### 3.3 第19轮6反跑回归验证（任务1修改lexer.py后无回归）
- T1 词法核心：rc=0
- T2 词法扩展：rc=0
- T3 保留字内置映射：rc=0
- T4 字符串哈希：rc=0
- T5 异常模块：rc=0
- T6 字典函数_D批次：rc=0

### 3.4 第20轮新增测试用例
- test_R20_关键字前缀标识符.light：rc=0
- test_R20_词法边界综合.light：rc=0

### 3.5 全量CI
- **pytest**：待CI结果（目标：无例外全绿，330+ passed）
- **smoke**：待CI结果（目标：5/5）
- **test_回归.py状态**：SKIP仅2个helper，EXPECT_RED仅L-101/L-143（合规保留字），4个临时例外全部清零

---

## 四、已知问题与后续方向

### 4.1 L-152：嵌套段落形参关键字前缀（未修，P0待后续）
- **现象**：嵌套段落（段落体内定义段落）的形参名若以关键字开头（如 `函数值`/`返回值`），因预扫描 `_scan_user_definitions` 只覆盖模块级名称，不覆盖嵌套段落形参，导致形参名被切碎，运行期 `name '值' is not defined`。
- **绕法**：嵌套段落形参改用非关键字前缀名称（如 `算子` 代替 `函数值`）；或将嵌套段落提升为模块级。
- **未修原因**：属「词法确定性切词重构」范畴；泛化合并关键字已实测引发104个解析回归，需要系统性重构而非局部修补。
- **建议**：后续专门轮次处理「词法确定性切词重构」，覆盖嵌套段落形参/块内设名/动态定义名等所有预扫描盲区。

### 4.2 R20-C：test_审批.light wall-clock计时断言抖动（非编译器缺陷，仅登记）
- **现象**：全量CI（pytest -n 8）偶发 `test_审批.light` rc=1，用例12d `断言(耗时 >= 0.2)` 实际0.196s（低于下界0.004s）。
- **归因**：非本轮编译器改动（test_审批.light由第19轮E4提交，本轮未改动）；单独运行13次全绿；`等待结果` 用 `time.time()` 而非 `time.monotonic()` 计耗时，CI期间墙钟被校时步进导致测量偏小。
- **建议**：`等待结果` 类耗时测量改用 `time.monotonic()`；或测试下界放宽（0.2→0.1）。本轮未改（超出任务1文件互斥范围）。
- **结论**：非回归、非编译器缺陷。

### 4.3 后续方向建议
第21轮可选择以下方向之一：
1. **词法确定性切词重构轮**：系统性解决L-152及预扫描盲区（嵌套段落形参/块内设名/动态定义名），实现词法切词确定性。
2. **继续上游复刻**：回到deepseek-harness上游复刻主线，继续对标未完成的功能模块。
3. **L-101/L-143上下文敏感保留字**：实现`回调`/`作用域`的上下文敏感保留字（仅在`外部回调`/`异步作用域`语法位置消费，其余位置允许作标识符）。

建议优先选择方向1（词法确定性切词重构），从根本上解决关键字前缀标识符家族问题。

---

## 五、docs三件回填

1. **对标清单**（docs/功能对标/对标清单.json）：追加#128~#130（词法回归修复/HMAC标准实现/词法边界测试套件），总数130条；版本字段更新为「覆盖记录至第20轮」。
2. **缺陷账**（docs/功能对标/语言缺陷账.md）：
   - L-095状态订正：从「部分修复」订正为「已修复」（第19轮实现已正确，第20轮7组对拍复核确认；第19轮EXPECT_RED系收口误判）。
   - L-097状态订正：从「已修复（第19轮新修）」订正为「已修复（既有实现已覆盖，第19轮为验收确认）」。
   - L-095详述段追加第20轮订正说明。
   - 新增L-151（赋值尾巴X为<值>的为被并入标识符，已修复）。
   - 新增L-152（嵌套段落形参名不被预扫描覆盖，未修，登记待后续）。
3. **行为差异清单**（docs/功能对标/行为差异清单.md）：追加R20-D1~D3（词法最大匹配收敛化/HMAC标准实现订正R19-D5/嵌套段落形参限制L-152）。

---

## 六、git提交

### light-merge项目（1个提交）
- `d2857dd5` 第20轮编译器回归修复：词法关键字前缀标识符最大匹配通用化（函数调用语境守卫+赋值尾巴精确切分），test_宿主上下文/事件从SKIP移除转绿，第19轮6反跑无回归（1文件，91行插入/12行删除）

### lightharness项目（6路分路提交 + 收口提交）
- `3efbdf8` 任务1：词法关键字前缀标识符最大匹配通用化（test_R20_关键字前缀标识符+反跑+报告）
- `0f850b4` 任务2：HMAC RFC2104标准实现确认（test_L095更新7组对拍+反跑+报告+补全第19轮3份缺失报告）
- `fa7fa9c` 任务3：全项目关键字前缀标识符扫描报告 + _taskM_integration_r17修复提交
- `5ffa883` 任务4：词法边界测试套件（test_R20_词法边界综合+pytest+报告）
- `d54c173` 任务5：第19轮反跑脚本审查报告
- `52e0636` 任务6：test_回归.py临时例外清理 + docs更新草案+例外清理验证+收口框架
- 收口提交：docs三件回填 + 收口说明

---

## 七、路M修正记录

1. **HMAC例外根因订正**：路M前置探查时认为HMAC"双轮哈希后与标准RFC 2104对拍仍不一致，部分修复"。任务2复核后确认：实现本身已正确（Python标准库hmac.new），第19轮收口登记的EXPECT_RED系状态滞后误判（观察值5b94b938…是修复前单轮PBKDF2输出，最终提交已是标准实现）。路M认可任务2的订正结论。
2. **任务1修复方案比预期更收敛**：路M任务书建议"建立关键字前缀标识符最大匹配通用机制，覆盖所有关键字前缀标识符"。任务1实际采用收敛化策略（函数调用语境守卫+赋值尾巴精确切分+拒绝泛化合并），因为泛化合并会引发104个解析回归。路M认可该收敛方案——它修复了本轮4个例外中的2个（test_宿主上下文/事件），同时不破坏合法语法。
3. **L-152新发现未修**：任务1附带发现L-152（嵌套段落形参关键字前缀），因属词法确定性切词重构范畴，本轮未修，登记待后续轮次。路M认可该处置。
