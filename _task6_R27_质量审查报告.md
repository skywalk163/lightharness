# 第27轮 任务6 质量审查报告

> 日期：2026-09-15 ｜ 审查对象：CS表剩余16字逐字通用化（任务1-5交付）

---

## 一、编译器方向质量审查

### 1.1 任务1：A类DUAL双位字词首并入通用规则

| 检查项 | 结果 | 说明 |
|---|---|---|
| 规则定义 | ✅ | `_P0A_HEAD_MERGE_DUAL`（8字：乘/减/到/加/模/真/空/除） |
| 实现方案 | ✅ | 方案A：新增DUAL类别，排除集保持42字不变（语义清晰） |
| 第一层skip_verb | ✅ | 增加DUAL类别判定（词首+后随汉字→并入） |
| 嵌入式扫描三处 | ✅ | 探测/输出/rematch三处同步（B/C/D同口径） |
| DUAL词尾并入 | ✅ | 新增词尾分支（词尾+前导汉字→并入），覆盖到/真的词尾场景 |
| 运算符切分 | ✅ | `甲 加 乙`后随空白→切分；`加法`后随汉字→并入 |
| 值字面量切分 | ✅ | `返回 真`后随空白→切分；`真空`后随汉字→并入 |
| 范围切分 | ✅ | `从1到10`后随数字→切分；`到位`后随汉字→并入 |
| 快速验证 | ✅ | `test_R27_任务1_2_词首并入.light` rc=0 |

### 1.2 任务2：B类F∩CS八字G1打红场景定位与修复

| 检查项 | 结果 | 说明 |
|---|---|---|
| G1打红定位 | ✅ | 8字逐条定位变化文件和场景 |
| L-119下标边界 | ✅ | 独立单字+后随`[`→不标记内嵌（段[1]/配[0]/对[0]） |
| def边界协同 | ✅ | `def是否夏令时`→def+是否夏令时分离 |
| 词首并入分支 | ✅ | 的/自等字的词首并入场景覆盖 |
| rematch段 | ✅ | `自定义表`→词首并入（rematch段） |
| 全语料token零变化 | ✅ | 修复后B类6字可删 |

### 1.3 任务3：CS表16字逐条验证删除

| 检查项 | 结果 | 说明 |
|---|---|---|
| CS表变化 | ✅ | 16→2（净删14字） |
| 删除14字 | ✅ | A类8（乘/减/到/加/模/真/空/除）+ B类6（对/是/段/的/自/配） |
| 保留2字 | ✅ | 列/类（真护栏，行为互为镜像） |
| 三重判据 | ✅ | G1语料+G2词首编译门+G3边界门，逐条验证 |
| 列保留理由 | ✅ | `对于 元素 在 序列:`中列在词尾切出KEYWORD，与R25词尾F并入方向相反 |
| 类保留理由 | ✅ | `类 独立类:`中独立类整词依赖CS与类声明上下文协同 |
| 自校验断言更新 | ✅ | ①CS⊆F ∧ CS∩DUAL==∅；③HM−CS==_R26_CS_REMOVED∪_R27_CS_REMOVED |
| 全量反跑 | ✅ | 839文件token零变化 |

**列/类保留的方向性说明**：两字的基线行为互为镜像（列=词尾切出、类=词尾整词），说明"词尾+CS残部字"的token行为由上下文（前导关键字"在"/"类"声明、后随冒号、user_definitions共同决定），单条通用条件无法同时刻画，需后续轮次以`_seg_after_sep`/user_definitions协同口径逐字定位。

### 1.4 任务4：边界测试+全量反跑

| 检查项 | 结果 | 说明 |
|---|---|---|
| 正向.light | ✅ | `test_R27_词首并入正向.light` rc=0（16字×2名=32标识符） |
| 反向.light | ✅ | `test_R27_词首并入反向.light` rc=0（运算符5/值字面量3/范围/硬语句/下标） |
| 混合.light | ✅ | `test_R27_词首并入混合.light` rc=0（9组） |
| pytest | ✅ | 113 passed（含保护表形态断言CS=2） |
| 全量反跑 | ✅ | ALL OK，839文件token零变化 |

### 1.5 任务5：排除集精简+联动评估+性能

| 检查项 | 结果 | 说明 |
|---|---|---|
| 排除集 | ✅ | 方案A落地，`_P0A_HEAD_SPLIT_SINGLE`保持42字不变 |
| 词尾排除集 | ✅ | `_P0A_SUFFIX_SPLIT_SINGLE`（21字）不需要同步修改（派生自五类词法依据） |
| CCW 37条 | ✅ 保留 | 与单字CS递归正交，R22已逐条验证保留 |
| _EMBED 3条 | ✅ 保留 | 与单字CS递归无关 |
| OPERATOR_VERBS 19条 | ✅ 保留 | 运算符切分职责不变 |
| _P0A_MERGE_WHOLE 9条 | ✅ 保留 | 2-4字整词，与单字词首并入不重复 |
| _P0A_NEVER_SPLIT 4条 | ✅ 保留 | 词内切分禁止，与DUAL词首并入互补 |
| _skip_compound_safe_and_match | ✅ 保留 | **非死代码**：递归入口扩展为CS∪DUAL∪B类，CS清零后DUAL/B类仍可命中 |

**任务书预设修正**：任务书5.3预设CS清零后`_skip_compound_safe_and_match`成为死代码可删除。实测任务1+2主动扩展了递归入口条件为`CS ∪ DUAL ∪ B类`三集合并集，CS清零后DUAL/B类仍非空，递归仍被调用。路M认可这一修正——不得按任务书原预设删除。

---

## 二、数据集方向确认

- 第25轮数据集（2200条）不受本轮编译器修改影响（编译器修改为词法层规则扩展，全语料token零变化）
- 抽样验证数据集中的代码段仍可编译

---

## 三、路M修正与决策记录

1. **CS表未完全清零**：任务书目标"CS表清零"，实际16→2（列/类保留）。路M认可——列/类的基线行为互为镜像（列=词尾切出、类=词尾整词），单条通用条件无法同时刻画，需后续轮次以`_seg_after_sep`/user_definitions协同口径逐字定位。CS表从第22轮52条→第27轮2条，已精简96%。

2. **_skip_compound_safe_and_match非死代码**：任务书5.3预设CS清零后该函数成为死代码可删除。实测任务1+2扩展递归入口为CS∪DUAL∪B类，CS清零后DUAL/B类仍非空。路M认可保留。

3. **排除集保持42字**：任务书预期排除集精简，实际采用方案A（新增DUAL类别，排除集不变）。路M认可——方案A语义更清晰（运算符/值字面量/范围仍需在非词首位置切分），排除集的语义不依赖CS表存在。

4. **联动保护表五张全部保留**：CCW/_EMBED/OPERATOR_VERBS/_P0A_MERGE_WHOLE/_P0A_NEVER_SPLIT评估后全部保留。路M认可——这些表与单字CS递归正交或互补，CS清零不影响其语义。

---

## 四、已知问题与后续方向

### 4.1 CS表剩余2字（列/类）的进一步通用化
- 列：`对于 元素 在 序列:`中列在词尾切出KEYWORD，与R25词尾F并入方向相反。需独立刻画"词尾+前导汉字→切出KEYWORD"的反向规则。
- 类：`类 独立类:`中独立类整词依赖CS与类声明上下文协同。需`_seg_after_sep`/user_definitions协同口径。
- 两字行为互为镜像，单条通用条件无法同时刻画，需后续轮次逐字定位。

### 4.2 持续存在的问题
- test_审批.light flaky（R20-C）：从第20轮持续存在，`等待结果`用`time.time()`而非`time.monotonic()`，非编译器缺陷。

### 4.3 第28轮方向建议
1. **CS表剩余2字（列/类）逐字通用化**（推荐）：CS表完整清零的最后一步
2. **CCW 37条分批精简**
3. **test_审批.light flaky修复**
4. **继续上游复刻**

---

## 五、结论

第27轮CS表16字逐字通用化质量良好：
- A类DUAL双位字（8字）规则实现完整（词首+词尾双位置并入，运算符/值字面量/范围切分正确）
- B类6字G1打红场景全部定位并修复（L-119下标边界+def边界协同+词首并入+rematch段）
- CS表16→2（删14字，保留列/类为真护栏），精简96%
- 全语料839文件token零变化，无回归
- 边界测试覆盖全面（3个.light + pytest 113 passed）
- 排除集/联动表/递归函数评估严谨，全部保留有依据
- 任务书预设修正2处（CS未清零、递归非死代码），均有充分理由

可进入路M收口。
