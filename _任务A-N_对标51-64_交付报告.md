# 任务 A–N 批次（对标 #51–#64）复刻交付报告

> 仓库：`g:\dswork\duan-light-merge\lightharness`
> 对标对象：`g:\github\deepseek-harness`（TypeScript monorepo，pure 逻辑核心）
> 覆盖：外发任务卡 A–N ↔ 功能对标 #51–#64 共 14 条，全部 `done`
> 日期：2026-09-02

---

## 1. 本批成果总览

分 4 个合并批次把 14 个纯逻辑模块落地到 `src/`，每模块配一个测试用例 `examples/test_*.light`，并同步维护对标文档。最终 pytest 全量门禁 **171 passed 全绿**。

| 批次 commit | 对标编号 | 模块（src/） |
|---|---|---|
| `3fc8dd7` 对标51-54 | #51–#54 | 网页搜索 / 超时策略 / 任务系统 / 压缩配套 |
| `1619af2` 对标55-57 | #55–#57 | 远端总线 / 钩子协议 / e2b配套 |
| `6e0e1a0` 对标58-62 | #58–#62 | 工作区 / 存储域 / 路径规则 / 安全策略 / 类型注册 |
| `369fd98` 对标63-64 | #63–#64 | 语言服务器 / 推理工具 |

**复刻口径**：全部为**纯逻辑核心**——把原版 TS 模块中可脱离宿主的判定/变换/状态机逻辑用光明 1:1 复刻，真实 IO（网络 / 子进程 / 文件系统 / LLM 调用）一律以「入参能力函数 / 字典投影」抽象，不落地任何真实设施。

---

## 2. 对标卡清单（#51–#64）

| # | 功能 | 光明模块 | 测试 | 引用的语言缺陷 |
|---|---|---|---|---|
| 51 | web 搜索与抓取工具纯逻辑 | `src/网页搜索.light` | `test_网页搜索.light` | L-036/037 |
| 52 | 超时策略与超时工具纯逻辑 | `src/超时策略.light` | `test_超时策略.light` | L-036/040 |
| 53 | 后台任务 jobs 子系统纯逻辑 | `src/任务系统.light` | `test_任务系统.light` | L-032/041/043/046 |
| 54 | compaction 压缩配套纯逻辑 | `src/压缩配套.light` | `test_压缩配套.light` | L-036/037/040/041 |
| 55 | 远端 API 总线纯逻辑 | `src/远端总线.light` | `test_远端总线.light` | L-036/037/040/041/050 |
| 56 | hooks 钩子协议纯逻辑 | `src/钩子协议.light` | `test_钩子协议.light` | L-036/037/040/041/043/049/051/052 |
| 57 | e2b 沙箱配套纯逻辑 | `src/e2b配套.light` | `test_e2b配套.light` | L-036~L-041 等 |
| 58 | 工作区管理纯逻辑 | `src/工作区.light` | `test_工作区.light` | L-036/038/039/041/049/050/051 |
| 59 | 存储域模型纯逻辑 | `src/存储域.light` | `test_存储域.light` | L-036/040/041/049/050 |
| 60 | 文件系统路径规则纯逻辑 | `src/路径规则.light` | `test_路径规则.light` | L-036~L-040 |
| 61 | 安全策略纯逻辑 | `src/安全策略.light` | `test_安全策略.light` | L-036/038/039/049/050/051/052 |
| 62 | typert 类型注册纯逻辑 | `src/类型注册.light` | `test_类型注册.light` | L-036/037/040/041 |
| 63 | lsp 语言服务器纯逻辑 | `src/语言服务器.light` | `test_语言服务器.light` | L-036/037/040/041/050/053/054 |
| 64 | 推理与工具纯逻辑 | `src/推理工具.light` | `test_推理工具.light` | L-036~L-041 |

> 任务卡 H–N ↔ #58–#64（H=工作区 … N=推理工具）为历次合并逐卡确认；任务卡 A–G 对应 #51–#57，按连续编号对应，具体模块即上表前 7 行。

---

## 3. 本批新增/触发的语言缺陷

本批通过实测触发并登记到 `docs/功能对标/语言缺陷账.md`：

- **L-049~L-052**（#55/#56/#58 等批次）：远端总线 / 钩子协议类缺陷，`1619af2` 合并时登记 4 条。
- **L-053**（#62 类型注册测试踩中）：`己`（天干名）是光明 self 保留名，类方法外作普通变量名报 `name '己' is not defined`；绕法改用 `辛`。
- **L-054**（#63 语言服务器测试踩中）：函数调用实参的**嵌套字典/列表字面量**内字符串含 `\n` 转义会破坏后续语句缩进（降级为模块级代码）；单层字符串的 `\n` 不受影响；绕法用 `chr(10)` / 预置变量拼接。

已被复用（非本批新增，见缺陷账历史）的既有缺陷：L-036（缺键用 `取可选`）、L-037（`断言` no-op）、L-038（`截取到末尾` 前缀拆名）、L-039（break 用 `跳出`）、L-040（参数用 `接收`）、L-041（`字典/映射` 不作标识符）等。

---

## 4. 文档交付与一致性核查

本次「整理文档」依下述链路交叉核查，结论**内部一致，无待补齐**：

1. **对标清单**：64 条编号 1–64 连续、无缺失、无重复；必备字段（编号/功能/原版包/光明模块/状态/证据/本轮目标/反跑判据/语言缺陷）齐全。
2. **证据存在性**：#51–#64 的 `光明模块` 与 `证据`（源码 + 测试）文件实际全部存在于仓库。
3. **缺陷账回链**：所有对标卡「语言缺陷」字段引用的 `L-0XX` 均已在 `语言缺陷账.md` 有定义行，无「引用但未登记」；反之，仅历史编译缺陷（L-001~L-021、L-047）成孤儿——它们由「反跑判据.md 复现套件」承载，不属模块引用，为正常设计。
4. **判据边界补全**：在 `反跑判据.md` 新增「§3.x 判据体系边界」，明确 L-022 起缺陷走「缺陷账 + 对标卡反跑判据字段 + 模块测试」链路，不再进编译复现套件，消除「找不到 = 遗漏」的误读。
5. **README 对齐**：在 README「对齐进度」补充本批 #51–#64 完成信息（见 §6）。

---

## 5. 回归门禁

```powershell
cd G:\dswork\duan-light-merge\lightharness
python -m pytest tests/ -q        # 全量 examples 用例，171 passed 全绿
```

`tests/test_回归.py` 自动发现 `examples/*.light`，本批新增 `test_语言服务器.light`、`test_推理工具.light` 已纳入并保持 `rc=0`。

---

## 6. README 对齐（本次已同步）

`README.md`「对齐进度」段更新为：现有功能对标 **#1–#64 共 64 张卡**，其中本批外发任务 A–N（#51–#64）14 个纯逻辑模块已全部 `done` 合并并推送；详细状态以 `docs/功能对标/对标清单.json` 为准。

---

## 7. 复跑方式

```powershell
# 单模块，如语言服务器 / 推理工具
python 运行.py examples/test_语言服务器.light
python 运行.py examples/test_推理工具.light
# 全量回归
python -m pytest tests/ -q
# 文档体系自检（本次核查使用的一键脚本，已随本报告说明清理）
```