# 第 38 轮 任务 2 交付报告 —— 与现有工具注册/代码运行时/代理循环联动适配

> 日期：2026-09-16 ｜ 方向：agent-tool-presentation 与 lightharness 现有模块联动
> 交付物：`src/工具展示.light`（含零修改适配层）、`examples/test_R38_工具展示.light`（联动验证，rc=0）
> 结论：**现有 src 模块零修改**；联动通过「依赖注入 + 代理注册表适配器」完成。

---

## 一、现有模块分析

### 1.1 `src/工具.light`（工具注册表）
- 数据结构：`ToolSchema {name, description, parameters}`；`工具注册表` 类提供 `注册/注销/有无/查找/名单/描述表/执行/执行策略/统计/汇总统计/批量执行`。
- 关键方法 `描述表()`：按登记顺序返回模型可见 schema 列表 `{name, description, parameters}` —— **正是 tool-presentation 需要过滤的源数据**。
- 结论：工具展示模块**只读** `描述表()` 即可获得工具列表，无需改动 `工具.light`。

### 1.2 `src/代码运行时.light`（代码运行时）
- 类 `代码运行时`：`构造(超时秒, 选项=空)`、`执行(代码)`、`状态()` 等；PTC 模式依赖其存在性。
- 结论：上游语义是「codeRuntime 是条件等待的服务」，光明侧把「运行时实例/空」作为 `应用工具展示` 的入参判定 present/absent。**只读传入的实例，不改动 `代码运行时.light`**。

### 1.3 `src/代理循环.light`（代理循环，第 34 轮）
- 关键行（约 L459）：`设 工具模式表 为 己.注册表.描述表()` —— 模型请求前从注册表取工具 schema 列表。
- 结论：代理循环**只通过 `注册表.描述表()` 取工具列表**，并调用 `注册表.执行()/执行策略()`。只要传入的「注册表」对象具备同名方法，即可在不改代理循环代码的前提下获得按作用域过滤的工具列表。

### 1.4 `src/代理.light`（代理核心）
- 构造时接收 `注册表` 并透传给 `代理循环`。结论是「代理携带工具展示配置」= 构造时传入一个已包好作用域的代理注册表。

### 1.5 `src/代理默认模型.light`（第 37 轮）
- 提供「配置字典 + apply/挂载/卸载」的成熟范式（config 字典承载、调用方薄包装）。工具展示沿用同样的「配置字典 + apply」范式，风格一致。

---

## 二、联动设计决策

**核心决策：零修改适配（依赖注入 + 代理注册表适配器）**。

依据任务书铁律「只修改必要的联动点，不重构现有模块」「如果现有模块不需要修改，明确说明并只做适配层」：
- 工具注册表 / 代码运行时 / 代理循环 / 代理 的接口**天然与 tool-presentation 兼容**——它们都通过「注册表对象的方法」交互，而 tool-presentation 只改变「模型可见的工具列表」这一投影。
- 因此**不修改任何既有 src 模块**，只在 `工具展示.light` 内提供适配层：一个**代理注册表**类，包装真实注册表 + 作用域 ID，覆盖 `描述表()` 为按作用域过滤后的可见工具，其余方法原样委托。

**为何比「改代理循环」更优**：
1. 不改 `代理循环.light`（核心、已重测试，回归风险高）。
2. 适配层可独立单测（本任务 §13/§12 已验）。
3. 与上游语义一致——上游 presentAs 只改「工具展示投影」，不动 agent loop 调度器；代理注册表正是这个投影的具象。

---

## 三、实现细节（适配层，均在 `src/工具展示.light`）

### 3.1 `工具展示注册表代理`（类）
包装 `(作用域ID, 注册表)`，提供与 `工具注册表` 同名的接口：
- `描述表()` → `获取可见工具(作用域ID, 包装)`（按作用域过滤）
- `执行(名字, 原始参数)` → `包装.执行(...)`（委托）
- `执行策略(名字)` → `包装.执行策略(...)`（委托）
- `名单/有无/查找/注册/注销/统计/汇总统计/批量执行` → 全部委托

代理循环传入此代理即自动获得按作用域的工具展示过滤，**代码零改动**。

### 3.2 `造代理工具展示(作用域ID, 注册表, 模式, 代码运行时=空)`
「代理携带工具展示配置」的薄包装（对齐 `代理默认模型` 配置范式）：
```
设 配置 = 建工具展示配置(模式)
设 注销器 = 应用工具展示(配置, 作用域ID, 注册表, 代码运行时)
设 代理 = 新建 工具展示注册表代理(作用域ID, 注册表)
返回 {配置, 代理注册表, 注销器}
```
调用方把 `代理注册表` 传给 `代理循环` 即完成端到端联动；`注销器` 用于卸载恢复。

### 3.3 作用域 ID 传递
上游「每 composition 一行、覆盖该 preset 下所有 agent」→ 光明侧每个 agent 用唯一 `作用域ID`（可用 agent ID）。代理注册表在构造时固化 `作用域ID`，代理循环无需感知展示逻辑。

---

## 四、联动验证（`examples/test_R38_工具展示.light`，rc=0）

| 组 | 验证点 | 结果 |
|---|---|---|
| §12 | 代理 `描述表()` 返回按作用域过滤（`["run_code"]`） | ✅ |
| §12b | 代理 `执行()` 委托真实注册表（`isError=假`） | ✅ |
| §12c | 代理 `执行策略()` 委托（返回 `"exclusive"`） | ✅ |
| §12d | 代理 `有无()` 委托（`search` 存在） | ✅ |
| §13 | 代理 pending → `等待代码运行时` 后自动切换为 `["run_code"]` | ✅ |
| §13c | 代理 `注销工具展示` 后恢复全量可见 | ✅ |

全部 14 组通过（含 §1–§11 核心 + §12–§13 联动），rc=0。

---

## 五、改动文件清单（铁律：只改本路互斥表内文件）

| 文件 | 操作 | 说明 |
|---|---|---|
| `src/工具展示.light` | **新增** | 核心模块 + 零修改适配层（代理注册表 + 造代理工具展示） |
| `examples/test_R38_工具展示.light` | **新增** | 单元测试 + 联动验证 |
| `src/工具.light` | 未修改 | 接口兼容，只读 `描述表()` |
| `src/代码运行时.light` | 未修改 | 仅作入参判定存在性 |
| `src/代理循环.light` | 未修改 | 经代理注册表获得过滤，代码零改动 |
| `src/代理.light` | 未修改 | 传入代理注册表即携带展示配置 |

---

## 六、铁律遵守

- ✅ 只修改必要的联动点（实际为零修改，仅新增适配层）。
- ✅ 不破坏现有模块的功能和测试（既有 `test_R37_代理默认模型.light` 仍 rc=0）。
- ✅ 联动修改有充分理由（依赖注入 + 适配器，对齐上游 host-plane 不变语义）。
- ✅ 现有模块不需要修改的部分已明确说明，仅做适配层。
- ✅ 新增一律 .light。
- ✅ 不依赖真实大模型 API（用 mock 工具 + 代码运行时实例）。
