# 任务1（R42·P0）交付报告 —— 真实web-fetch提供方

> 日期：2026-09-16 ｜ 负责人：WorkBuddy ｜ 状态：**完成（本机 Windows rc=0）**
> 目标：把 `抓取策略.light`（纯逻辑：URL校验/MIME分类/非公网IP判定）与 R41 `真实HTTP客户端` 拼成**真实 fetch 提供方**：URL校验通过 → 真发 HTTP GET → Content-Type 分类 → 返回体。**零修改**纯逻辑层。

---

## 一、根因 / 本轮回溯（最重要的修正点）

### 1.1 死端口一度"静默返回 0 状态"而非报错（隐蔽缺陷，已在提供层堵住）
初版验收脚本对「死端口」用例断言「必抛错」。实测**未抛**：`真实抓取("http://127.0.0.1:<空闲端口>/x", 0.4)` 返回 `[0, '', '']`。
探针（`_probe_r42_fetch.light`）实测确认：对端拒绝连接时，R41 `真实HTTP客户端.真实请求` 的 `recv` 拿到 `b""` → 视作「对端正常关闭」→ 跳出 → 空缓存 → 解析分支 `分隔 < 0` → 返回 `["状态":0, "头":"", "体":""]`。

> **问题定性**：连接被拒/空响应被**静默映射成状态码 0 的"正常返回"**，调用方（web-fetch）无法区分"抓到了个空响应"和"根本没连上"。这是**提供方契约层的洞**——R41 客户端本身不分类这种退化态是可接受的（它只负责收发），但 fetch 提供方必须把它翻译成明确失败。
> **修正**：`真实抓取` 增加状态闸——`响应["状态"] == 0` → `抛出 新建 错误("抓取失败：无有效 HTTP 响应（对端未返回可解析报文）")`。真实 HTTP 响应状态码恒 ≥ 100，故该闸不会误伤正常抓取。

### 1.2 URL 路径需自行切分（`校验抓取地址` 不回路径）
`抓取策略.校验抓取地址` 只回 `{protocol, hostname, port}`（它对齐原版 `validateFetchUrl`，只做卫生校验），**不含路径**。故提供方自行 `取URL路径(网址)`：按 `://` 后首个 `/` 切分，无则回 `/`。

### 1.3 沙箱强制"默认抓本机、公网仅登记"
`抓取策略` 的 `主机是否非公网字面量` 只判 IP 字面量（域名一律返回"公网"）。提供方据此实现 `允许抓取(主机名)`：`localhost`/`127.0.0.1`/`::1` 或**非公网 IP 字面量**放行；公网 IP 或域名 → `抛出 新建 错误("公网抓取未启用（仅登记，不主动外呼）")`。**默认不主动外呼公网**由本层强制，不依赖调用方自律。

---

## 二、做了什么（交付物 + 设计）

### 2.1 交付物（三项，全部到位）
| 文件 | 类型 | 说明 |
|---|---|---|
| `lightharness/src/真实抓取提供.light` | 新增 | 真实 fetch 提供方（纯逻辑 + 真实 GET 拼装） |
| `lightharness/examples/test_R42_真实抓取.light` | 新增 | 全链验收，rc=0 |
| `lightharness/_task1_R42_真实抓取.md` | 新增 | 本报告 |

**铁律遵守**：零修改 `抓取策略.light`（只调其纯逻辑函数）；默认抓本机、公网仅登记；超时兜底（复用 R41 monotonic 截止）。

### 2.2 提供方接口
- `真实抓取(网址, 超时值=5.0)` → `[状态, 内容类型, 体]`
  1) `校验抓取地址(网址, 8192)`（长度上限 / 协议白名单 / authority 非空 / 无 `@` 凭据）；
  2) `允许抓取(主机名)` 沙箱闸（公网 → 拒）；
  3) `取端口整数(协议, 端口串)`（默认 http→80 / https→443）；
  4) `等待 真实GET(主机名, 端口, 路径, "", 超时值)`；
  5) 状态闸（0 → 抛错）；
  6) `取响应头(头文本, "content-type")`，返回 `[状态, 内容类型, 体]`。
- `真实抓取分类(网址, 超时值=5.0)` → `[状态, 内容类型, 分类, 体]`；分类由 `抓取策略.分类内容类型` 给出（html/text/空）。
- 辅助段落：`取URL路径` / `取响应头`（大小写不敏感）/ `允许抓取` / `取端口整数`。

### 2.3 设计对齐
- **超时**：复用 R41 `真实HTTP客户端` 的 per-request monotonic 截止（connect/send/recv 各有上界），禁止无限挂起。
- **非阻塞**：真实 GET 走 R41 客户端的非阻塞 + 探测就绪 + 让步轮询；提供方 `真实抓取` 是 `异步 段落`，主协程里必须 `等待`（否则冻住服务端 `处理循环` 任务 → 死锁）。
- **零外呼**：公网目标在提供层直接拒，不发任何 socket。

### 2.4 light 内建/纯逻辑探查（缺则登记）
本轮**未暴露新的 light 内建缺失**：`抓取策略` 全部纯逻辑函数可直调；socket/字符串方法（`.lower()`/`.去除空白()`/`.分割()`/`.查找()`/切片）均可用。
**唯一可回流的是「提供方契约注意点」**：R41 客户端把"连接被拒"退化为状态 0（见 1.1）——属 R41 客户端的行为边界，已在本提供层堵口，暂不列为语言缺陷；若下轮 R41 客户端需修，可复议。

---

## 三、验证结果（本机 Windows）

运行：`cd lightharness && python 运行.py examples/test_R42_真实抓取.light` → **EXIT_CODE=0**
（输出：`任务1 全部断言通过（真实web-fetch：真GET 200 + Content-Type/分类；非法URL拒；公网沙箱拒；超时兜底 rc=0）`）

| 用例 | 覆盖 | 断言 | 结果 |
|---|---|---|---|
| 起服务 | 内核分配端口 | `端口 > 0` | ✅ |
| 真 GET /health | 校验通过真发 GET | `状态 == 200` 且 Content-Type 含 `application/json` 且体含 `ok` | ✅ |
| 抓取分类 | `分类内容类型` | 分类 `== "text"` | ✅ |
| ftp: 协议 | 协议白名单 | 抛错 | ✅ |
| 含 `@` 凭据 | 凭据拒绝 | 抛错 | ✅ |
| 无 authority | `http:///noauthority` | 抛错 | ✅ |
| 公网 IP | 沙箱（仅登记） | `93.184.216.34` 抛错 | ✅ |
| 公网域名 | 沙箱（仅登记） | `example.com` 抛错 | ✅ |
| 死端口 | 超时/失败兜底 | 抛错且耗时 `< 3.0s` | ✅ |

---

## 四、现在 / 待办（任务2/5 衔接）

1. **任务2**：真实 webhook 端点 —— `web服务器.light` 挂 `/webhook/github` 路由，真实 POST → `网络钩子GitHub` 十一段门禁 + HMAC 验签 → 派发。
2. **任务5**：把本条真实 fetch 串进端到端真实工具链。
3. **0.82 复测**：本机绿；0.82（POSIX）复测归任务4/5，同跑本文件比对 rc=0。
4. **缺陷账**：本轮任务1 未暴露新语言缺陷（见 2.4）。

---

## 五、运行方式（copy-paste）
```bash
cd G:/dswork/duan-light-merge/lightharness
G:/dswork/duan-light-merge/light-merge/.venv/Scripts/python.exe 运行.py examples/test_R42_真实抓取.light
# 期望：任务1 全部断言通过（...）  退出码 0
```
