# 任务4 交付报告：词法分析器边界测试套件（第20轮）

> 任务来源：`复刻_第20轮_任务prompt分发_编译器回归修复轮.md` 任务4
> 日期：2026-09-13 ｜ 本路未修改 light-merge 编译器源码 ✓
> 交付物：`examples/test_R20_词法边界综合.light`（rc=0）+ `tests/test_R20_词法边界.py`（pytest）+ 本报告

---

## 一、套件构成与验证结果

### 1. 运行期自校验套件 `examples/test_R20_词法边界综合.light`

```
rc=0，输出 test_R20_词法边界综合 PASS
断言总数 107
```

| 分区 | 场景数 | 内容 |
|---|---|---|
| 一、关键字前缀标识符 | 57 | 任务书清单选取 52 个双字 + 5 个三字（去重占位/作用域匹配/排序函数/…/类型别名表/标准库函数表），每个：`段落 X 接收 输入: 返回 输入 + 1` 定义 + 表达式调用断言（**定义登记后路径**，即真实代码的切词保护口径——第20轮任务3 扫描结论） |
| 二、合法关键字语句 | 34 | 如果/否则/否则若/遍历之/当/尝试/捕获/最终/设…为 真·假·空/返回 真·假·空/去重/排序/求和/求最大/匹配(经 正则表达式.查找，规避 L-157)/管道（含块体嵌套断言与循环退出计数验证） |
| 三、边界混合 | 22 | 关键字前缀名作比较实参/字典值取函数再调用/列表元素存函数/嵌套调用/与如果语句相邻/管道串联/叠加调用/字典多键混用/遍历中调用/尝试体内调用/高阶实参/函数值作参数/求最大·构造·抽象·静态方法·退出循环·否则若·标准库·类型别名·外部·等待 等前缀 |

覆盖口径：三区合计 **113 个命名场景、107 条断言**，满足任务书「至少 100 个不同场景」。

### 2. 编译期 token 验证 `tests/test_R20_词法边界.py`（pytest）

```
72 passed, 1 skipped (0.43s)
```

- `test_keyword_prefix_identifier_keeps_whole`（57 参数化）：定义登记后整名保持 IDENTIFIER（token 级断言，防词法回潮）。
- `test_legal_keyword_statements`（14 参数化）：语句头关键字保持 KEYWORD token。
- `test_han_identifier_no_embed_split`：第19轮修复回归保护（末位行为/错误己/配[0]）。
- `test_assign_tail_known_regression`（**skip 登记**）：`返回真`/`设 合并为 {}`/`设 监听域为 {}` 当前被并坏的已知回归（见第三节），任务1 修正后翻转断言。

## 二、与任务1 的协同口径

1. 任务3 扫描已证明：真实代码的关键字前缀标识符由 `_scan_user_definitions` 定义登记保护（1708/1708 正常）；本套件一区即按该真实口径构建——若未来词法改动破坏定义登记路径（如第19轮 SKIP 回归），本套件立红。
2. 任务1 在途实现存在两处收窄缺口（单字别名绕过值字收窄致 `返回真` 并坏；`X为` 词尾不收窄致 `合并为` 并坏，详见 `_task3_R20_关键字前缀标识符扫描报告.md` 发现三）。本套件以 skip 测试固化翻转点，任务1 修正后取消 skip 即同时完成验收。
3. 套件不依赖任务1 未落地的"孤立表达式上下文"行为——孤立 `名(` 切碎在真实代码中不构成可执行语义（无定义即 NameError），不纳入 rc=0 口径，避免套件长期红。

## 三、编写过程中发现并规避的坑（供后续轮次参考）

- `反转` 返回 Python reversed 迭代器（不可下标/无 len/转字符串落 repr）——断言改用 `求最大`；此为语言行为差异候选（建议登记）。
- `匹配` 直接调用撞 Python match-case（L-157 既有登记），正则场景须 `从 正则表达式 导入 查找`。
- `遍历 X 对 列表:` 语法不成立（合法连接词为 之/在/于/中的/为/之为）。
- `异步作用域 块:` / `等待 1` / `推迟:` 语法形态未获支持（写法存疑，未纳入合法语句区，避免误登记）。

## 四、验证命令

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"
python 运行.py examples/test_R20_词法边界综合.light     # rc=0
python -m pytest tests/test_R20_词法边界.py -q          # 72 passed, 1 skipped
```

## 五、移交

- 移交路M：本套件并入全量 CI 后，目标基线更新为 pytest 311+57(综合用例按1个用例计)+72 ≈ **330+ passed**（与任务书预期吻合）；skip 项待任务1 修复翻转后归零。
- 移交任务1：第三节与 `_task3` 报告发现三的收窄缺口清单。
