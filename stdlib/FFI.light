# 光明 C FFI 模块 - 外部函数接口
# 提供动态库加载、类型映射、函数调用等功能
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
#     。
#
# 用法：
#   从《FFI》导入《FFILibrary》。

导出 FFILibrary FFIManager 加载库 获取库 注册类型 获取类型。
导出 获取类型或空 声明函数 调用函数 编码文本 解码文本 内存分配。
导出 内存释放 取地址 创建结构体 获取字段 设置字段 创建回调。
导出 取地址 解引用 指针偏移 创建数组 设置数组 分配内存。
导出 释放内存 设指针值 获取FFI错误 获取系统错误码 设系统错误码 关闭所有库。
导出 创建回调函数 创建结构体值 创建枚举 创建联合体 解析库路径 变长参数调用。
导出 获取平台 查找库 取地址增强 结构体大小 字段偏移 结构体转字节。
导出 字节转结构体 注册回调 注销回调 获取回调 列出回调 清理回调。
导出 设置调试 启用调试 禁用调试 获取日志 清空日志 FFI调试。
导出 FFI禁用调试 FFI获取日志 位域设置 位域获取 创建函数指针 创建类型别名。
导出 定义宏 获取宏 列出宏 清理宏。
