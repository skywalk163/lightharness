# classAES:
#     def__init__(self,密钥=无,模式="CBC"):
#
#     def设置密钥(self,密钥):
#             抛出错误("密钥长度必须为16/24/32字节")
#
#     def加密(self,明文,IV=无):
#             抛出错误("请先设置密钥")
#             IV=_生成随机字节(16)
#
#     def解密(self,密文,IV):
#             抛出错误("请先设置密钥")
#
# classRSA:
#     def__init__(self,公钥=无,私钥=无):
#
#     def生成密钥对(self,密钥长度=2048):
#         私钥PEM,公钥PEM=_生成RSA密钥对(密钥长度)
#         返回列表(私钥PEM,公钥PEM)
#
#     def导入公钥(self,公钥PEM):
#
#     def导入私钥(self,私钥PEM):
#
#     def加密(self,明文):
#             抛出错误("请先导入公钥")
#
#     def解密(self,密文):
#             抛出错误("请先导入私钥")
#
#     def签名(self,数据):
#             抛出错误("请先导入私钥")
#
#     def验证签名(self,数据,签名):
#             抛出错误("请先导入公钥")

导出 MD5 MD5二进制 SHA1 SHA1二进制 SHA224 SHA224二进制。
导出 SHA256 SHA256二进制 SHA384 SHA384二进制 SHA512 SHA512二进制。
导出 计算文件哈希 HMAC_MD5 HMAC_SHA1 HMAC_SHA256 HMAC_SHA512 HMAC。
导出 AES RSA 生成随机密钥 生成随机IV 密码派生密钥 对称加密。
导出 哈希工具 密钥对 非对称加密 数字签名 TLS简化工具 SSH密钥工具。
导出 加密 解密 哈希。
