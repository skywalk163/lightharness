#     if可迭代对象is None:
#
#     return列表(集合)
#
#     return元组(集合)
#
#     return分隔符.join(str(item)for item in集合)
#
#     return集合1|集合2
#
#     return集合1&集合2
#
#     return集合1-集合2
#
#     return集合1^集合2
#
#     if not集合列表:
#     结果=set(集合列表[0])
#     for s in集合列表[1:]:
#         结果|=s
#     return结果
#
#     if not集合列表:
#     结果=set(集合列表[0])
#     for s in集合列表[1:]:
#         结果&=s
#     return结果
#
#     return集合1<=集合2
#
#     return集合1<集合2
#
#     return集合1>=集合2
#
#     return集合1>集合2
#
#     return集合1.isdisjoint(集合2)
#
#     集合.add(元素)
#     return集合
#
#     集合.remove(元素)
#     return集合
#
#     集合.discard(元素)
#     return集合
#
#     return集合.pop()
#
#     集合.clear()
#     return集合
#
#     return长(集合)
#
#     return元素in集合
#
#     return集合1==集合2
#
#     return集合1!=集合2
#
#     return集合.copy()
#
#     元素列表=列表(集合)
#     幂集=[set()]
#     for元素in元素列表:
#         幂集+=[子集|{元素}for子集in幂集]
#     return幂集
#
#     return全集-集合
#
#     return长(集合1-集合2)
#
#     return长(集合1&集合2)
#
#     return长(集合1|集合2)
#
#     交集大小=长(集合1&集合2)
#     并集大小=长(集合1|集合2)
#     if并集大小==0:
#     return交集大小/并集大小
#
#     出现次数={}
#     for元素in列表:
#         出现次数[元素]=出现次数.get(元素,0)+1
#
# 用法：
#   从《集合》导入《创建集合》。

导出 创建集合 集合转列表 列表转集合 集合转元组 集合转字符串 并集。
导出 交集 差集 对称差 多个并集 多个交集 是否子集。
导出 是否真子集 是否超集 是否真超集 是否不相交 添加元素 移除元素。
导出 丢弃元素 弹出元素 清空集合 集合长度 集合包含 集合相等。
导出 集合不等 复制集合 冻结集合 集合的幂集 集合的笛卡尔积 集合的补集。
导出 集合差集的大小 集合交集的大小 集合并集的大小 杰卡德相似度 集合的唯一元素 集合的重复元素。
