# 光明标准库 - 字符串常量模块
#
# 提供常用字符串常量和字符分类功能。
#
# 导出常量：
#   小写字母 - 所有小写字母
#   大写字母 - 所有大写字母
#   字母 - 所有字母
#   数字 - 所有数字
#   十六进制数字 - 十六进制数字
#   八进制数字 - 八进制数字
#   标点符号 - 标点符号
#   可打印字符 - 可打印字符
#   空白字符 - 空白字符
#   换行符 - 换行符
#   制表符 - 制表符
#   回车符 - 回车符
#   空字符串 - 空字符串
#
# 导出函数：
#   是字母(字符) - 检查是否为字母
#   是数字(字符) - 检查是否为数字
#   是字母数字(字符) - 检查是否为字母数字
#   是小写(字符) - 检查是否为小写
#   是大写(字符) - 检查是否为大写
#   是空白(字符) - 检查是否为空白
#   是可打印(字符) - 检查是否可打印
#   是标点(字符) - 检查是否为标点
#   是十六进制(字符) - 检查是否为十六进制数字
#   是八进制(字符) - 检查是否为八进制数字
#   字符列表全部是(文本, 检测函数) - 检查所有字符是否满足条件
#   字符列表有一个是(文本, 检测函数) - 检查是否有字符满足条件
#   首字母大写(文本) - 首字母大写
#   标题大小写(文本) - 标题大小写
#   全大写(文本) - 转换为全大写
#   全小写(文本) - 转换为全小写
#   交换大小写(文本) - 交换大小写
#   去除两端空白(文本, 字符) - 去除两端空白
#   去除左端空白(文本, 字符) - 去除左端空白
#   去除右端空白(文本, 字符) - 去除右端空白
#   居中填充(文本, 宽度, 填充字符) - 居中填充
#   左对齐(文本, 宽度, 填充字符) - 左对齐
#   右对齐(文本, 宽度, 填充字符) - 右对齐
#   补零(文本, 宽度) - 左侧补零
#   连接(分隔符, 字符串列表) - 用分隔符连接
#   分割(文本, 分隔符, 最大分割次数) - 分割字符串
#   右分割(文本, 分隔符, 最大分割次数) - 从右侧分割
#   按行分割(文本, 保留换行符) - 按行分割
#   替换(文本, 旧子串, 新子串, 替换次数) - 替换子串
#   包含(文本, 子串) - 检查是否包含子串
#   以开头(文本, 前缀) - 检查是否以前缀开头
#   以结尾(文本, 后缀) - 检查是否以后缀结尾
#   查找(文本, 子串, 起始, 结束) - 查找子串位置
#   反向查找(文本, 子串, 起始, 结束) - 反向查找
#   计数(文本, 子串, 起始, 结束) - 统计出现次数
#
# 用法：
#   从《字符串常量》导入《小写字母》。

导出 小写字母 大写字母 字母 数字 十六进制数字 八进制数字。
导出 标点符号 可打印字符 空白字符 换行符 制表符 回车符。
导出 空字符串 是字母 是数字 是字母数字 是小写 是大写。
导出 是空白 是可打印 是标点 是十六进制 是八进制 字符列表全部是。
导出 字符列表有一个是 首字母大写 标题大小写 全大写 全小写 交换大小写。
导出 去除两端空白 去除左端空白 去除右端空白 居中填充 左对齐 右对齐。
导出 补零 连接 分割 右分割 按行分割 替换。
导出 包含 以开头 以结尾 查找 反向查找 计数。
