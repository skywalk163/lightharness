# 纯光明实现 —— stdlib/文件系统.py 的光明门面：真段落实现核心文件原语，取代同名 .py。
# 门面承诺：examples/tests/积木库 实际使用的导出名一个不少（写入文件/读取文件/追加文件/文件存在/
# 删除文件/获取文件名/文件行列表/文件大小/文件列表/创建目录/目录存在/删除目录/遍历目录/路径连接/
# 绝对路径/规范化路径/创建临时文件/删除临时文件）；
# 【第九轮 S1 合并点订正】原承诺只数了 examples/tests，漏了 积木库/工具/列目录文件.light 要的
# 文件列表 —— CI 的积木库闸门当场红（可运行率 0.9945 < 1.0）。门面的覆盖面必须按**全部消费方**数。
# 文件读写走 打开文件 内置（语言 open 映射，避开同名内置「写入文件」的段落遮蔽）；
# 系统调用边界函数（删除/大小/目录/绝对路径等，光明无对应原语，地板清单标 native_required）
# 转发 _light_builtin（builtins.py 注入的 Python os 层）。

导出 写入文件 读取文件 追加文件 文件存在 删除文件 获取文件名 文件行列表。
导出 文件大小 文件列表 创建目录 目录存在 删除目录 遍历目录 路径连接 绝对路径 规范化路径。
导出 创建临时文件 删除临时文件。

从 文件流 导入 行列表

# 写入文件（等价 文件系统.py:20 写入文件(path, content, encoding='utf-8')；自动建父目录）
段落 写入文件 接收 路径, 内容:
  设 目录 为 目录名(路径)
  如果 目录 != "":
    创建目录(目录)
  设 f 为 打开文件(路径, "w", encoding="utf-8")
  f.write(内容)
  f.close()

# 读取文件（等价 文件系统.py:14 读取文件(path, encoding='utf-8')；文件不存在时抛异常）
段落 读取文件 接收 路径:
  设 f 为 打开文件(路径, "r", encoding="utf-8")
  设 内容 为 f.read()
  f.close()
  返回 内容

# 追加文件（等价 文件系统.py:26 追加文件(path, content, encoding='utf-8')）
段落 追加文件 接收 路径, 内容:
  设 f 为 打开文件(路径, "a", encoding="utf-8")
  f.write(内容)
  f.close()

# 文件存在（等价 文件系统.py:32 文件存在 = os.path.isfile；对目录同样返回 假）
段落 文件存在 接收 路径:
  尝试:
    设 f 为 打开文件(路径, "r", encoding="utf-8")
    f.close()
    返回 真
  捕获 异常 e:
    返回 假

# 删除文件（等价 文件系统.py:37 删除文件 = os.remove；系统调用边界，无光明原语，转发内置）
段落 删除文件 接收 路径:
  _light_builtin.删除文件(路径)

# 获取文件名（等价 文件系统.py:67 获取文件名 = os.path.basename；含扩展名）
段落 获取文件名 接收 路径:
  返回 文件名(路径)

# 文件行列表（等价 文件系统.py:325 文件行列表 = f.readlines；行尾换行保留）——委托 文件流.light
段落 文件行列表 接收 路径:
  返回 行列表(路径)

# ---- 以下为 test_stdlib_complete.py（任务书 §6 点名）真实使用的 文件系统.py 其余能力 ----

# 文件大小（等价 文件系统.py:83 文件大小 = os.path.getsize；系统调用边界，转发内置）
段落 文件大小 接收 路径:
  返回 _light_builtin.文件大小(路径)

# 创建目录（等价 builtins.创建目录 = os.makedirs(..., exist_ok=True)；边界，转发内置）
段落 创建目录 接收 路径:
  _light_builtin.创建目录(路径)

# 目录存在（等价 builtins.目录存在 = os.path.isdir；边界，转发内置）
段落 目录存在 接收 路径:
  返回 _light_builtin.目录存在(路径)

# 删除目录（等价 builtins.删除目录 = os.rmdir；边界，转发内置）
段落 删除目录 接收 路径:
  _light_builtin.删除目录(路径)

# 遍历目录（等价 文件系统.py:200 遍历目录(非递归) = os.listdir 文件名列表；边界，转发内置）
段落 遍历目录 接收 路径:
  返回 _light_builtin.列出目录(路径)

# 文件列表（等价 文件系统.py:88 文件列表 = os.listdir，与 遍历目录 同语义，别名保留给 积木库）
段落 文件列表 接收 路径:
  返回 _light_builtin.列出目录(路径)

# 路径连接（等价 文件系统.py:93 路径连接 = os.path.join；支持 2-3 段，内置 连接路径 未遮蔽可直接调用）
段落 路径连接 接收 甲, 乙, 丙 = "":
  如果 丙 == "":
    返回 连接路径(甲, 乙)
  返回 连接路径(连接路径(甲, 乙), 丙)

# 绝对路径（等价 文件系统.py:98 绝对路径 = os.path.abspath；边界，转发内置）
段落 绝对路径 接收 路径:
  返回 _light_builtin.绝对路径(路径)

# 规范化路径（近似 文件系统.py:103 规范化路径 = os.path.normpath；经 目录名+文件名+连接 去冗余分隔符）
段落 规范化路径 接收 路径:
  设 目录 为 目录名(路径)
  设 尾名 为 文件名(路径)
  返回 连接路径(目录, 尾名)

# 创建临时文件（近似 文件系统.py:285 创建临时文件：唯一名 + 空文件落盘）
段落 创建临时文件 接收 前缀 = "tmp", 后缀 = "":
  设 名 为 前缀 加上 转字符串(_light_builtin.时间戳()) 加上 后缀
  设 f 为 打开文件(名, "w", encoding="utf-8")
  f.close()
  返回 名

# 删除临时文件（等价 文件系统.py:297 删除临时文件 = os.remove；边界，转发内置）
段落 删除临时文件 接收 路径:
  _light_builtin.删除文件(路径)
