# 光明标准库 - 临时文件模块
#
# 提供临时文件和临时目录的创建与管理功能。
#
# 导出：
#   创建临时文件(后缀, 前缀, 目录, 文本模式) - 创建临时文件返回(fd,路径)
#   创建临时文件路径(后缀, 前缀, 目录) - 创建临时文件返回路径
#   创建临时目录(后缀, 前缀, 目录) - 创建临时目录
#   获取临时目录() - 获取系统临时目录
#   获取临时目录前缀() - 获取临时文件名前缀
#   安全文件名(名称) - 获取安全的文件名
#
# 用法：
#   从《临时文件》导入《创建临时文件》。

导出 创建临时文件 创建临时文件路径 创建临时目录 获取临时目录 获取临时目录前缀 命名临时文件。
导出 临时文件上下文 临时目录上下文 临时文件 临时目录 安全文件名 Spooled临时文件。
