# 光明标准库 - 外部命令模块
#
# 封装 subprocess 模块，提供执行外部命令的便捷接口。
#
# 导出：
#   命令结果类 - 命令执行结果对象
#   执行命令(命令, 捕获输出, 工作目录, 环境变量, 超时, 编码) - 执行外部命令
#   执行命令并获取输出(命令, 工作目录, 环境变量, 超时, 编码) - 执行命令并返回标准输出
#   命令是否成功(命令, 工作目录, 超时) - 检查命令是否执行成功
#   管道执行(命令列表, 捕获输出, 工作目录, 编码) - 管道执行多个命令
#   后台执行(命令, 工作目录, 环境变量) - 后台执行命令
#   等待进程(进程, 超时) - 等待后台进程完成
#   命令存在(命令名) - 检查命令是否存在
#
# 用法：
#   从《外部命令》导入《命令结果》。

导出 命令结果 执行命令 执行命令并获取输出 命令是否成功 管道执行 后台执行。
导出 等待进程 命令存在。
