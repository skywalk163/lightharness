# 光明标准库 - 集合工具模块
#
# 封装 collections 模块，提供高级集合数据结构。
#
# 导出类：
#   默认字典 - 访问不存在的键时返回默认值
#   有序字典 - 保持插入顺序的字典
#   计数器 - 统计元素出现次数
#   双端队列 - 双端队列
#
# 导出函数：
#   创建默认字典(默认工厂, **kwargs) - 创建默认字典
#   创建有序字典(数据) - 创建有序字典
#   创建计数器(可迭代对象) - 创建计数器
#   创建双端队列(可迭代对象, 最大长度) - 创建双端队列
#   命名元组(类型名, 字段名列表) - 创建命名元组类型
#   链字典(*映射列表) - 链接多个字典
#   用户字典() - 创建UserDict
#   用户列表() - 创建UserList
#   用户字符串() - 创建UserString
#   统计频率(可迭代对象) - 统计频率返回普通字典
#
# 用法：
#   从《集合工具》导入《默认字典》。

导出 默认字典 有序字典 计数器 双端队列 创建默认字典 创建有序字典。
导出 创建计数器 创建双端队列 命名元组 链字典 用户字典 用户列表。
导出 用户字符串 统计频率 可迭代对象。
