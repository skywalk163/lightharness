# 光明标准库 - 系统接口模块
#
# 封装 sys 和 os 模块的常用功能，提供统一的系统接口。
# 包括：环境变量、路径操作、进程信息、平台信息等。
#
# 导出：
#   获取环境变量(名称, 默认值) - 获取环境变量
#   设置环境变量(名称, 值) - 设置环境变量
#   删除环境变量(名称) - 删除环境变量
#   环境变量存在(名称) - 检查环境变量是否存在
#   获取所有环境变量() - 获取所有环境变量字典
#   当前工作目录() - 获取当前工作目录
#   切换工作目录(路径) - 切换工作目录
#   路径存在(路径) - 检查路径是否存在
#   是文件(路径) - 检查是否为文件
#   是目录(路径) - 检查是否为目录
#   是绝对路径(路径) - 检查是否为绝对路径
#   绝对路径(路径) - 获取绝对路径
#   规范化路径(路径) - 规范化路径
#   连接路径(*路径片段) - 连接多个路径片段
#   取目录名(路径) - 获取目录部分
#   取文件名(路径) - 获取文件名部分
#   取文件扩展名(路径) - 获取文件扩展名
#   取文件名无扩展(路径) - 获取不含扩展名的文件名
#   分割路径(路径) - 分割路径为目录和文件名
#   分割扩展名(路径) - 分割扩展名
#   文件大小(路径) - 获取文件大小
#   文件修改时间(路径) - 获取文件修改时间戳
#   文件创建时间(路径) - 获取文件创建时间戳
#   文件访问时间(路径) - 获取文件访问时间戳
#   创建目录(路径, 递归) - 创建目录
#   删除目录(路径, 递归) - 删除目录
#   列出目录(路径) - 列出目录内容
#   遍历目录(路径) - 递归遍历目录
#   重命名(旧路径, 新路径) - 重命名文件或目录
#   删除文件(路径) - 删除文件
#   复制文件(源路径, 目标路径) - 复制文件
#   进程ID() - 获取当前进程ID
#   父进程ID() - 获取父进程ID
#   退出(退出码) - 退出程序
#   获取命令行参数() - 获取命令行参数列表
#   获取脚本路径() - 获取当前脚本路径
#   标准输入() - 获取标准输入
#   标准输出() - 获取标准输出
#   标准错误() - 获取标准错误
#   操作系统() - 获取操作系统名称
#   操作系统版本() - 获取操作系统版本
#   操作系统详细信息() - 获取操作系统详细信息
#   计算机名() - 获取计算机名
#   处理器架构() - 获取处理器架构
#   处理器型号() - 获取处理器型号
#   Python版本() - 获取Python版本
#   Python实现() - 获取Python实现
#   Python编译器() - 获取Python编译器
#   是否Windows() - 是否为Windows系统
#   是否Linux() - 是否为Linux系统
#   是否Mac() - 是否为macOS系统
#   用户名() - 获取当前用户名
#   用户主目录() - 获取用户主目录
#   临时目录() - 获取系统临时目录
#   执行系统命令(命令) - 执行系统命令
#   路径分隔符() - 获取路径分隔符
#   环境变量分隔符() - 获取环境变量分隔符
#   行分隔符() - 获取行分隔符
#   CPU核心数() - 获取CPU核心数
#   内存使用情况() - 获取内存使用情况
#
# 用法：
#   从《系统接口》导入《获取环境变量》。

导出 获取环境变量 设置环境变量 删除环境变量 环境变量存在 获取所有环境变量 当前工作目录。
导出 切换工作目录 路径存在 是文件 是目录 是绝对路径 绝对路径。
导出 规范化路径 连接路径 取目录名 取文件名 取文件扩展名 取文件名无扩展。
导出 分割路径 分割扩展名 文件大小 文件修改时间 文件创建时间 文件访问时间。
导出 创建目录 删除目录 列出目录 遍历目录 重命名 删除文件。
导出 复制文件 进程ID 父进程ID 退出 获取命令行参数 获取脚本路径。
导出 标准输入 标准输出 标准错误 操作系统 操作系统版本 操作系统详细信息。
导出 计算机名 处理器架构 处理器型号 Python版本 Python实现 Python编译器。
导出 是否Windows 是否Linux 是否Mac 用户名 用户主目录 临时目录。
导出 执行系统命令 路径分隔符 环境变量分隔符 行分隔符 CPU核心数 内存使用情况。
导出 内存总量 内存可用 线程数。
