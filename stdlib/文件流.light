# 纯光明实现 —— 本模块由光明段言自写，纯光明文件流读写，不依赖 文件系统.py
# 提供：行列表(路径) / 逐行(路径) 惰性生成器 / 写文本(路径, 文本) / 追加文本(路径, 文本)
# 等价 文件系统.py:325 文件行列表（readlines，保留行尾换行）/ :20 写入文件 / :26 追加文件。
# 全部只用光明内置文件原语（读取文件/写入文件/追加文件），零 导入、零 引 Python。

导出 行列表 逐行 写文本 追加文本。

# 按行读，等价于 文件系统.py:325 文件行列表（f.readlines，行尾换行保留）
段落 行列表 接收 路径:
  设 内容 为 读取文件(路径)
  返回 拆成行(内容)

# 把整段文本切成 readlines 风格的列表（行尾换行保留，末行无换行则不补）
段落 拆成行 接收 内容:
  设 行组 为 内容.split("\n")
  设 结果 为 []
  设 n 为 长(行组)
  设 i 为 0
  当 i < n:
    如果 i < n - 1:
      结果.追加(行组[i] 加上 "\n")
    否则:
      如果 行组[i] != "":
        结果.追加(行组[i])
    设 i 为 i + 1
  返回 结果

# 惰性逐行生成器：大评测集不该整份进内存
段落 逐行 接收 路径:
  设 内容 为 读取文件(路径)
  设 行组 为 内容.split("\n")
  设 n 为 长(行组)
  设 i 为 0
  当 i < n:
    如果 i < n - 1:
      生成 行组[i] 加上 "\n"
    否则:
      如果 行组[i] != "":
        生成 行组[i]
    设 i 为 i + 1

# 整文件写文本（覆盖）
段落 写文本 接收 路径, 文本:
  写入文件(路径, 文本)

# 整文件追加文本
段落 追加文本 接收 路径, 文本:
  追加文件(路径, 文本)
