# 纯光明实现 —— 内置核心字典：stdlib/builtins.py 字典组地板的真身
# 模块名独立（无同名 .py），零 Python 模块导入。
#
# 形参名用「映射」而不用「字典」：后者同时是内置名 字典()，会与内置映射打架。
# 每个段落上方一行注释标明对齐的 stdlib/builtins.py 行号。

# 八条一律写在同一行 导出：多行 导出 会各自生成一句 `__all__ = [...]`，
# 后一句直接覆盖前一句（实测 __all__ 只剩最后一行那几个名字，`import *` 会漏），
# 这是编译器的既有缺陷，本文件绕开它。
导出 字典创建 字典设置 字典删除 字典键列表 字典值列表 字典项列表 字典包含键 字典获取 取可选。

# 对齐 stdlib/builtins.py:701
段落 字典创建:
  设 映射 为 {}
  返回 映射

# 对齐 stdlib/builtins.py:706 —— 原地修改入参，返回 None
# 下标赋值必须写「设 … 为 …」：裸 `映射[键] = 值` 解析报错（意外的标记「=」）。
段落 字典设置 接收 映射, 键, 值:
  设 映射[键] 为 值

# 对齐 stdlib/builtins.py:711 —— 键不存在时静默，返回 None
段落 字典删除 接收 映射, 键:
  如果 映射.包含(键):
    映射.pop(键)

# 对齐 stdlib/builtins.py:717
段落 字典键列表 接收 映射:
  返回 list(映射.keys())

# 对齐 stdlib/builtins.py:722
段落 字典值列表 接收 映射:
  返回 list(映射.values())

# 对齐 stdlib/builtins.py:727
段落 字典项列表 接收 映射:
  返回 list(映射.items())

# 对齐 stdlib/builtins.py:732
段落 字典包含键 接收 映射, 键:
  返回 映射.包含(键)

# 对齐 stdlib/builtins.py:737 —— 默认值不在光明侧写默认参数，收满三个参
段落 字典获取 接收 映射, 键, 默认值:
  返回 映射.获取(键, 默认值)

# 对齐 stdlib/builtins.py:893 —— 取可选（L-036）：缺省默认 空；安全访问可选字段的官方入口。
# 不改 `映射[缺失键]` 抛 键错误 的语义（语言层决策，超出本包）。默认值不在光明侧写，收满三个参。
段落 取可选 接收 映射, 键, 缺省:
  返回 映射.获取(键, 缺省)
