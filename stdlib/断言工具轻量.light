# 纯光明实现 —— 本模块由光明写成，模块名用《断言工具轻量》避免遮蔽 stdlib/断言工具.py
#
# 为什么换名（主线修复，勿改回）：
#   本文件原名 stdlib/断言工具.light 并带「纯光明实现」魔数，于是导入钩子
#   （stdlib/_light_import_hook.py:155）让它**完全遮蔽** stdlib/断言工具.py——
#   注意不是「.py 作兜底」，是彻底取代。而 断言工具.py 有 77 个导出，本文件只有 9 个，
#   且命名不兼容，遮蔽等于能力倒退。纯光明实现独立成名，不与能力更全的 .py 抢模块名。
#   （原第一行自称「无 .py 兜底」是错的，断言工具.py 一直都在。）
# 断言工具轻量.light —— 测试/校验辅助（纯光明实现）

#
# 全部由光明段言写成，是自举写库的示例。
# 提供：断言等于 / 断言真 / 断言假 / 断言接近 / 相等 / 包含。
导出 断言等于 断言真 断言假 断言接近 断言不为空。
导出 相等 包含 以开头 以结尾。

# 断言两值相等；不等则抛异常（消息可省略）
段落 断言等于 接收 实际, 期望, 消息 = "":
  如果 实际 != 期望:
    设 提示 为 消息
    如果 提示 == "":
      设 提示 为 "断言等于失败: " 加上 转字符串(实际) 加上 " != " 加上 转字符串(期望)
    抛出 新建 异常(提示)

# 断言为真
段落 断言真 接收 条件, 消息 = "":
  如果 非 条件:
    设 提示 为 消息
    如果 提示 == "":
      设 提示 为 "断言真失败（期望为真，实际为假）"
    抛出 新建 异常(提示)

# 断言为假
段落 断言假 接收 条件, 消息 = "":
  如果 条件:
    设 提示 为 消息
    如果 提示 == "":
      设 提示 为 "断言假失败（期望为假，实际为真）"
    抛出 新建 异常(提示)

# 断言两浮点接近（默认接受 0.001 绝对误差）
段落 断言接近 接收 实际, 期望, 容差 = 0.001, 消息 = "":
  设 差值 为 实际 减去 期望
  如果 差值 < 0:
    设 差值 为 0 减去 差值
  如果 差值 > 容差:
    设 提示 为 消息
    如果 提示 == "":
      设 提示 为 "断言接近失败: " 加上 转字符串(实际) 加上 " 与 " 加上 转字符串(期望)
    抛出 新建 异常(提示)

# 断言不为空（长度不为 0 或非 空）
段落 断言不为空 接收 值, 消息 = "":
  如果 值 == 空:
    抛出 新建 异常("断言不为空失败：值为空")
  如果 长(值) == 0:
    设 提示 为 消息
    如果 提示 == "":
      设 提示 为 "断言不为空失败：值为空序列"
    抛出 新建 异常(提示)

# ---- 纯函数（返回布尔，不抛异常）----
段落 相等 接收 甲, 乙:
  返回 甲 == 乙

段落 包含 接收 容器, 元素:
  返回 列表包含(容器, 元素)

段落 以开头 接收 文本, 前缀:
  返回 文本.开头(前缀)

段落 以结尾 接收 文本, 后缀:
  返回 文本.结尾(后缀)