# 光明标准库 - 文件匹配模块
#
# 封装 glob 和 fnmatch 模块，提供文件路径模式匹配功能。
#
# 导出：
#   匹配文件(模式, 递归, 目录) - 按模式匹配文件
#   迭代匹配(模式, 递归, 目录) - 迭代器方式匹配文件
#   名称匹配(文件名, 模式) - 检查文件名是否匹配模式
#   名称匹配忽略大小写(文件名, 模式) - 忽略大小写匹配
#   过滤列表(文件列表, 模式) - 从列表中过滤匹配的文件
#   转义元字符(字符串) - 转义特殊字符
#   查找所有Python文件(目录, 递归) - 查找所有Python文件
#   查找所有文本文件(目录, 递归) - 查找所有文本文件
#   查找所有JSON文件(目录, 递归) - 查找所有JSON文件
#
# 用法：
#   从《文件匹配》导入《匹配文件》。

导出 匹配文件 迭代匹配 名称匹配 名称匹配忽略大小写 过滤列表 转义元字符。
导出 查找所有Python文件 查找所有文本文件 查找所有JSON文件。
