# 纯光明实现 —— 内置核心系统：builtins.py 系统类地板的真身（R71 任务E）
# 模块名独立（无同名 .py），零 Python 模块导入；
# 真系统边界留在地板内置：环境变量（读进程环境）、随机字节（os.urandom 熵源）。
# 交付：L-160 主目录() / L-174 随机UUID() 零导入内置（R71-E）。

导出 主目录 随机UUID。

# 对齐 L-160 —— os.path.expanduser("~") 语义：
# Windows 取 USERPROFILE，其余平台取 HOME（与 ntpath/posixpath expanduser 主路径一致）。
段落 主目录:
  设 甲 为 环境变量("USERPROFILE")
  如果 甲 == 空:
    返回 环境变量("HOME")
  如果 甲 == "":
    返回 环境变量("HOME")
  返回 甲

# 对齐 L-174 —— uuid.uuid4() 语义：
# 16 字节熵（随机字节 = os.urandom），第 7 字节高 4 位置 0100（版本 4），
# 第 9 字节高 2 位置 10（RFC 4122 变体），32 个十六进制位按 8-4-4-4-12 连字。
段落 随机UUID:
  设 十六表 为 "0123456789abcdef"
  设 字节组 为 随机字节(16)
  设 结果 为 ""
  设 i 为 0
  当 i < 16:
    设 b 为 字节组[i]
    如果 i == 6:
      设 b 为 (b 位与 15) 位或 64
    如果 i == 8:
      设 b 为 (b 位与 63) 位或 128
    设 结果 为 结果 加上 十六表[(b 整除 16)] 加上 十六表[(b 模 16)]
    设 i 为 i 加上 1
  设 输出 为 结果[0:8] 加上 "-" 加上 结果[8:12] 加上 "-" 加上 结果[12:16] 加上 "-" 加上 结果[16:20] 加上 "-" 加上 结果[20:32]
  返回 输出
