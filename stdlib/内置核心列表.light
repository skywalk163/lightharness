# 纯光明实现 —— 内置核心列表：stdlib/builtins.py 列表类地板的真身
# 模块名独立（无同名 .py），零 Python 模块导入。
#
# 口径约定（勿改）：
#   1. 形参不叫 `列表` —— 它同时是内置名（`列表()`→`list()`），会与内置映射打架；
#      统一用 `表` / `元素` / `索引` / `反向`。
#   2. **默认值不在光明侧写**：`接收 x, y=-1:` 解析失败（期望冒号得到减号），
#      所以 `列表弹出`/`列表排序` 在这里收满参，默认值留在 builtins.py 的 Python 签名里。
#   3. 四条原地段落（追加/插入/排序/反转）不写 `返回`，与 Python 版一样返回 None，
#      副作用直接落在调用方传进来的那个列表对象上。

# 只用一行 导出：实测多行 导出 会生成多条 `__all__ = [...]` 赋值，后一条覆盖前一条，
# 于是 `from 内置核心列表 import *` 只拿到最后一行那几个名字（见交付报告口径差 D1）。
导出 列 列表创建 列表长度 列表获取 列表追加 列表弹出 列表插入 列表排序 列表反转 列表包含 排序列表。

# 对齐 stdlib/builtins.py:647 —— 列(*args) 返回 list(args)
段落 列 接收 *参数们:
  返回 list(参数们)

# 对齐 stdlib/builtins.py:652 —— 列表创建() 返回 []
段落 列表创建:
  设 表 为 []
  返回 表

# 对齐 stdlib/builtins.py:657 —— 列表长度(列表) 返回 len(列表)
段落 列表长度 接收 表:
  返回 长(表)

# 对齐 stdlib/builtins.py:662 —— 列表获取(列表, 索引) 返回 列表[索引]
段落 列表获取 接收 表, 索引:
  返回 表[索引]

# 对齐 stdlib/builtins.py:667 —— 列表追加(列表, 元素) 原地追加，返回 None
段落 列表追加 接收 表, 元素:
  表.追加(元素)

# 对齐 stdlib/builtins.py:672 —— 列表弹出(列表, 索引) 返回 列表.pop(索引)
段落 列表弹出 接收 表, 索引:
  返回 表.弹出(索引)

# 对齐 stdlib/builtins.py:677 —— 列表插入(列表, 索引, 元素) 原地插入，返回 None
段落 列表插入 接收 表, 索引, 元素:
  表.插入(索引, 元素)

# 对齐 stdlib/builtins.py:682 —— 列表排序(列表, 反向) 原地排序，返回 None
段落 列表排序 接收 表, 反向:
  表.sort(reverse=反向)

# 对齐 stdlib/builtins.py:687 —— 列表反转(列表) 原地反转，返回 None
段落 列表反转 接收 表:
  表.反转()

# L-026 返回新列表的排序 —— 不修输入，返回升序（或按 反向）的新列表。
段落 排序列表 接收 表, 反向:
  返回 sorted(表, reverse=反向)

# 对齐 stdlib/builtins.py:692 —— 列表包含(列表, 元素) 返回 元素 in 列表
段落 列表包含 接收 表, 元素:
  返回 表.包含(元素)
