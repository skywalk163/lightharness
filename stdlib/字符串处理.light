# 段言标准库 - 字符串处理 模块
#
# 提供字符串编码/解码、正则、大小写、填充、格式化等处理工具。
# 本模块实现见同名 .py 文件，本 .duan 仅作导出清单。
#
# 用法：
#   从《字符串处理》导入《...》。

导出 ASCII转字符 Base32编码 Base32解码 Base64编码 Base64解码 HTML编码 HTML解码 URL编码 URL解码 交换前缀后缀 以开头 以结尾。
导出 分割 分割成行 分割正则 列表到字符串 删除 判断开头 判断结尾 包含 包含忽略大小写 匹配正则 十六进制编码 十六进制解码。
导出 去空格 去除右侧指定字符 去除右侧空格 去除左侧指定字符 去除左侧空格 去除指定字符 去除换行 去除空格 去除重复空格 反转 右对齐 合并成行。
导出 填充前导 大写 大小写互换 子串位置 子串次数 字符串不等 字符串切片 字符串到列表 字符串哈希 字符串大于 字符串大于等于 字符串小于。
导出 字符串小于等于 字符串截取 字符串相等 字符串转数字 字符转ASCII 小写 居中 左对齐 截取 拼接 插入 搜索正则。
导出 数字转字符串 是否全大写 是否全小写 是否十进制 是否可打印 是否字母 是否字母数字 是否数字 是否数字字符 是否标题格式 是否空白 是数字符。
导出 替换 替换多次 替换正则 查找 查找从右 查找所有 查找所有匹配 标准化空格 标题 格式化 格式化f字符串 格式化为整数。
导出 格式化为浮点数 格式化为百分比 格式化为科学计数法 格式化字典 格式化数字 格式化整数 比较 比较忽略大小写 移除前缀 移除后缀 编码 获取字符。
导出 解码 计数 设置字符 转义 转义正则 重复 长度 首字母大写。
