# 光明标准库 - 参数解析模块
#
# 封装 argparse 模块，提供命令行参数解析功能。
#
# 导出：
#   参数解析器类 - 命令行参数解析器
#     添加参数(名称, 短名称, 类型, 默认值, 描述, 必需, 取值列表, 多值, 标志, 计数)
#     添加位置参数(名称, 类型, 默认值, 描述, 可选, 多值)
#     添加子命令(名称, 描述) - 返回子命令解析器
#     解析(参数列表) - 解析命令行参数返回字典
#     获取(参数名, 默认值) - 获取解析后的参数值
#     打印帮助() - 打印帮助信息
#     帮助文本() - 获取帮助文本
#     用法文本() - 获取用法文本
#   简单解析(参数定义, 参数列表) - 简单参数解析便捷函数
#   快速获取(参数名, 默认值, 参数列表) - 快速获取单个参数值
#
# 用法：
#   从《参数解析》导入《参数解析器》。

导出 参数解析器 简单解析 快速获取。
