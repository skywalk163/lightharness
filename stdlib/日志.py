"""
日志模块 - 提供分级日志、滚动、上下文日志等功能

从 日志系统增强 模块导出便捷函数，保持与 日志系统增强 的功能一致。
同时提供光明侧（.light）导出的中文别名，确保接口统一。
"""
from 日志系统增强 import (
    调试, 信息, 警告, 错误, 致命,
    设置级别, 获取级别, 启用控制台输出,
    设置日志轮转, 日志轮转,
    信息上下文, 调试上下文, 警告上下文, 错误上下文, 致命上下文,
    带上下文日志, 日志异常,
    设置格式, 设置输出文件, 启用文件输出, 禁用文件输出,
    禁用控制台输出, 输出到标准错误, 输出到标准输出,
    添加格式化器, 使用格式化器, 创建简单格式化器, 创建带颜色格式化器,
    创建JSON格式化器, 重置格式化器,
    获取所有级别, 获取格式化器列表, 打印日志配置,
    级别数值, 级别名称, 日志函数,
    日志记录器, 日志管理器, 日志处理器, 日志格式化器, 日志过滤器,
    日志上下文管理器, 日志上下文,
    获取日志记录器, 创建日志记录器, 快速配置,
    获取全局日志管理器, 设置全局日志级别, 配置全局日志,
    结构化日志,
    日志级别,
)

# ============================================================
# 光明侧（.light）导出别名
# 确保日志.light 中声明的导出在 Python 侧可找到
# ============================================================


def 日志记录(消息: str, *参数, **关键字参数):
    """记录日志（等同于 日志记录器 的便捷调用）"""
    return 日志记录器(消息, *参数, **关键字参数)


def 调试日志(消息: str, *参数, **关键字参数):
    """记录调试日志"""
    return 调试(消息, *参数, **关键字参数)


def 信息日志(消息: str, *参数, **关键字参数):
    """记录信息日志"""
    return 信息(消息, *参数, **关键字参数)


def 警告日志(消息: str, *参数, **关键字参数):
    """记录警告日志"""
    return 警告(消息, *参数, **关键字参数)


def 错误日志(消息: str, *参数, **关键字参数):
    """记录错误日志"""
    return 错误(消息, *参数, **关键字参数)


def 关键日志(消息: str, *参数, **关键字参数):
    """记录关键日志"""
    return 致命(消息, *参数, **关键字参数)


def 设置日志级别(级别):
    """设置日志级别"""
    return 设置级别(级别)


def 日志文件配置(文件路径: str, 模式: str = 'a', 编码: str = 'utf-8'):
    """配置日志输出到文件"""
    return 设置输出文件(文件路径, 模式, 编码)


__all__ = [
    '日志记录', '日志级别',
    '调试日志', '信息日志', '警告日志', '错误日志', '关键日志',
    '设置日志级别', '日志文件配置',
    # 从 日志系统增强 导出的所有名称
    '调试', '信息', '警告', '错误', '致命',
    '设置级别', '获取级别', '启用控制台输出',
    '设置日志轮转', '日志轮转',
    '信息上下文', '调试上下文', '警告上下文', '错误上下文', '致命上下文',
    '带上下文日志', '日志异常',
    '设置格式', '设置输出文件', '启用文件输出', '禁用文件输出',
    '禁用控制台输出', '输出到标准错误', '输出到标准输出',
    '添加格式化器', '使用格式化器', '创建简单格式化器', '创建带颜色格式化器',
    '创建JSON格式化器', '重置格式化器',
    '获取所有级别', '获取格式化器列表', '打印日志配置',
    '级别数值', '级别名称', '日志函数',
    '日志记录器', '日志管理器', '日志处理器', '日志格式化器', '日志过滤器',
    '日志上下文管理器', '日志上下文',
    '获取日志记录器', '创建日志记录器', '快速配置',
    '获取全局日志管理器', '设置全局日志级别', '配置全局日志',
    '结构化日志',
]