# 光明标准库 - 环境变量和系统信息模块
#
# 用法：
#   从《环境》导入《获取环境变量》《系统信息》。

导出 获取环境变量。
导出 设置环境变量。
导出 删除环境变量。
导出 环境变量存在。
导出 获取所有环境变量。
导出 系统名称。
导出 系统版本。
导出 系统架构。
导出 系统信息。
导出 Python版本。
导出 Python版本号。
导出 主机名。
导出 CPU核心数。
导出 内存信息。
导出 磁盘信息。
导出 用户名。
导出 用户目录。
导出 临时目录。
导出 PATH列表。
导出 操作系统类型。
导出 是Windows。
导出 是Linux。
导出 是macOS。