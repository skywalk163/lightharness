#     def初始化(创建函数,最大大小=10,初始化数量=0,销毁函数=无):
#
#     def获取():
#         否则:
#
#         return对象
#
#     def释放(对象):
#
#         否则如果self._销毁函数:
#
#     def清理():
#
#     def可用数量():
#
#     def已用数量():
#
#     def总数量():
#
#     def__进入__():
#
#     def__退出__(exc_type,exc_val,exc_tb):
#
#     def初始化(池):
#
#     def__进入__():
#
#     def__退出__(exc_type,exc_val,exc_tb):
#
# def对象上下文管理器(池):
#     return对象上下文(池)
#
#     def初始化(最大容量=128):
#
#     def获取(键,默认值=无):
#             return值
#         return默认值
#
#     def设置(键,值):
#
#     def删除(键):
#             删除self._缓存[键]
#             return真
#         return假
#
#     def包含(键):
#         return键在self._缓存
#
#     def清空():
#
#     def大小():
#
#     def命中率():
#             return0.0
#
#     def统计信息():
#         }
#
#     def键列表():
#
#     def值列表():
#
#     def项列表():
#
#     def初始化(过期时间=无):
#
#     def获取(键,默认值=无):
#                 return值
#             否则:
#                 删除self._缓存[键]
#         return默认值
#
#     def设置(键,值):
#
#     def删除(键):
#             删除self._缓存[键]
#             return真
#         return假
#
#     def包含(键):
#                 return真
#             否则:
#                 删除self._缓存[键]
#         return假
#
#     def清空():
#
#     def清理过期():
#         当前时间=_当前时间()
#         过期键=[]
#                 过期键.追加(键)
#             删除self._缓存[键]
#
#     def大小():
#
#     def初始化(刷新函数,刷新间隔=60):
#
#     def获取():
#         当前时间=_当前时间()
#
#     def刷新():
#
#     def清除():
#
# def缓存装饰器(最大容量=128):
#     def装饰器(函数):
#         缓存=LRU缓存(最大容量)
#
#         def包装(*参数):
#             键=参数
#             结果=缓存.获取(键)
#                 return结果
#             结果=函数(*参数)
#             缓存.设置(键,结果)
#             return结果
#
#         包装.缓存=缓存
#         return包装
#     return装饰器
#
# defLRU缓存装饰器(最大容量=128):
#     return缓存装饰器(最大容量)
#
# def记忆化(函数):
#     缓存={}
#
#     def包装(*参数):
#             缓存[参数]=函数(*参数)
#         return缓存[参数]
#
#     包装.缓存=缓存
#     return包装
#
# def创建对象池(创建函数,最大大小=10,初始化数量=0):
#     return对象池(创建函数,最大大小,初始化数量)
#
# def创建LRU缓存(最大容量=128):
#
# def创建简单缓存(过期时间=无):
#     return简单缓存(过期时间)
#
# def创建定时缓存(刷新函数,刷新间隔=60):
#     return定时缓存(刷新函数,刷新间隔)
#
# 用法：
#   从《对象池缓存》导入《对象池》。

导出 对象池 对象上下文 对象上下文管理器 LRU缓存 简单缓存 内存缓存。
导出 缓存装饰器 LRU缓存装饰器 记忆化 定时缓存 二级缓存 创建对象池。
导出 创建LRU缓存 创建简单缓存 创建定时缓存 创建缓存 设置缓存 获取缓存。
导出 清除缓存。
