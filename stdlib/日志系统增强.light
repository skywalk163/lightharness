#     DEBUG=10
#     INFO=20
#     WARNING=30
#     ERROR=40
#     CRITICAL=50
#
#     级别名称={
#         DEBUG:'DEBUG',
#         INFO:'INFO',
#         WARNING:'WARNING',
#         ERROR:'ERROR',
#         CRITICAL:'CRITICAL'
#     }
#
#     @静态方法
#     def创建标准格式(包含时间=真,包含级别=真,包含模块=真,包含线程=假):
#         格式部分=[]
#
#             格式部分.追加('%(asctime)s')
#             格式部分.追加('%(levelname)s')
#             格式部分.追加('%(name)s')
#             格式部分.追加('%(threadName)s')
#
#         格式部分.追加('%(message)s')
#
#         return_创建格式化器(' | '.连接(格式部分))
#
#     @静态方法
#     def创建JSON格式():
#         return_创建格式化器(
#             '{"时间":"%(asctime)s","级别":"%(levelname)s","模块":"%(name)s","消息":"%(message)s"}'
#         )
#
#     @静态方法
#     def创建简洁格式():
#         return_创建格式化器('%(levelname)s: %(message)s')
#
#     @静态方法
#     def创建详细格式():
#         return_创建格式化器(
#             '%(asctime)s | %(levelname)-8s | %(name)s | %(filename)s:%(lineno)d | %(funcName)s | %(message)s'
#         )
#
#     @静态方法
#     def创建控制台处理器(级别=日志级别.DEBUG,格式化器=无):
#         处理器=_创建控制台处理器(级别)
#
#             处理器.设置格式化器(格式化器)
#
#         return处理器
#
#     @静态方法
#     def创建文件处理器(文件名,级别=日志级别.DEBUG,格式化器=无,编码='utf-8'):
#         处理器=_创建文件处理器(文件名,编码)
#         处理器.设置级别(级别)
#
#             处理器.设置格式化器(格式化器)
#
#         return处理器
#
#     @静态方法
#     def创建滚动文件处理器(文件名,级别=日志级别.DEBUG,格式化器=无,最大大小=1024*1024*10,备份数量=5,编码='utf-8'):
#         处理器=_创建滚动文件处理器(文件名,最大大小,备份数量,编码)
#         处理器.设置级别(级别)
#
#             处理器.设置格式化器(格式化器)
#
#         return处理器
#
#     @静态方法
#     def创建时间滚动处理器(文件名,级别=日志级别.DEBUG,格式化器=无,间隔='D',备份数量=7,编码='utf-8'):
#         处理器=_创建时间滚动处理器(文件名,间隔,备份数量,编码)
#         处理器.设置级别(级别)
#
#             处理器.设置格式化器(格式化器)
#
#         return处理器
#
#     def初始化(允许级别列表=无,拒绝级别列表=无,包含关键词=无,排除关键词=无):
#
#     def过滤(记录):
#             return假
#
#             return假
#
#             包含=任何(关键词在记录.获取消息()对于关键词在self._包含关键词)
#                 return假
#
#                     return假
#
#         return真
#
#     def初始化(名称=__名称__,级别=日志级别.DEBUG):
#
#     def添加处理器(处理器):
#
#     def添加过滤器(过滤器):
#
#     def设置级别(级别):
#
#     def调试(消息,**额外信息):
#
#     def信息(消息,**额外信息):
#
#     def警告(消息,**额外信息):
#
#     def错误(消息,**额外信息):
#
#     def严重(消息,**额外信息):
#
#     def异常(消息,**额外信息):
#
#     def记录(级别,消息,**额外信息):
#
#     def初始化():
#
#     def设置全局级别(级别):
#
#     def获取记录器(名称):
#
#     def配置控制台日志(级别=日志级别.INFO,格式化器=无):
#         格式化器=格式化器或日志格式化器.创建标准格式()
#         处理器=日志处理器.创建控制台处理器(级别,格式化器)
#
#             记录器.添加处理器(处理器)
#
#     def配置文件日志(文件名,级别=日志级别.DEBUG,格式化器=无,滚动=真,最大大小=1024*1024*10,备份数量=5):
#         格式化器=格式化器或日志格式化器.创建详细格式()
#
#             处理器=日志处理器.创建滚动文件处理器(文件名,级别,格式化器,最大大小,备份数量)
#         否则:
#             处理器=日志处理器.创建文件处理器(文件名,级别,格式化器)
#
#             记录器.添加处理器(处理器)
#
#     def配置时间滚动日志(文件名,级别=日志级别.DEBUG,格式化器=无,间隔='D',备份数量=7):
#         格式化器=格式化器或日志格式化器.创建详细格式()
#         处理器=日志处理器.创建时间滚动处理器(文件名,级别,格式化器,间隔,备份数量)
#
#             记录器.添加处理器(处理器)
#
# def获取日志记录器(名称=__名称__):
#     return日志记录器(名称)
#
# def创建日志记录器(名称=__名称__,级别=日志级别.DEBUG,控制台输出=真,文件输出=假,文件名='app.log'):
#     记录器=日志记录器(名称,级别)
#
#         格式化器=日志格式化器.创建标准格式()
#         处理器=日志处理器.创建控制台处理器(级别,格式化器)
#         记录器.添加处理器(处理器)
#
#         格式化器=日志格式化器.创建详细格式()
#         处理器=日志处理器.创建滚动文件处理器(文件名,级别,格式化器)
#         记录器.添加处理器(处理器)
#
#     return记录器
#
#     def初始化(记录器,消息,级别=日志级别.INFO):
#
#     def__进入__():
#
#     def__退出__(exc_type,exc_val,exc_tb):
#         否则:
#
# def日志上下文(记录器,消息,级别=日志级别.INFO):
#     return日志上下文管理器(记录器,消息,级别)
#
#     @静态方法
#     def创建结构化消息(事件,**数据):
#         消息部分=['事件='+事件]
#             消息部分.追加(键+'='+字符串(值))
#
#     @静态方法
#     def创建API日志(方法,路径,状态码,耗时):
#
#     @静态方法
#     def创建数据库日志(操作,表名,影响行数,耗时):
#
#     @静态方法
#     def创建性能日志(操作,耗时,内存=无):
#         消息='性能 '+操作+' 耗时='+字符串(耗时)+'秒'
#             消息+=' 内存='+字符串(内存)+'MB'
#         return消息
#
# 用法：
#   从《日志系统增强》导入《日志级别》。

导出 日志级别 日志格式化器 日志处理器 日志过滤器 日志记录器 日志管理器。
导出 获取日志记录器 创建日志记录器 快速配置 日志上下文管理器 日志上下文 结构化日志。
导出 获取全局日志管理器 设置全局日志级别 配置全局日志 调试 信息 警告。
导出 错误 致命 设置级别 获取级别 设置格式 设置输出文件。
导出 启用控制台输出 禁用控制台输出 启用文件输出 禁用文件输出 输出到标准错误 输出到标准输出。
导出 带上下文日志 调试上下文 信息上下文 警告上下文 错误上下文 致命上下文。
导出 日志异常 调试异常 信息异常 警告异常 错误异常 致命异常。
导出 日志函数 级别数值 级别名称 日志轮转 设置日志轮转 添加格式化器。
导出 使用格式化器 创建简单格式化器 创建带颜色格式化器 创建JSON格式化器 重置格式化器 获取所有级别。
导出 获取格式化器列表 打印日志配置。
