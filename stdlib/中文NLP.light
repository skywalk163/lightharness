# 段言标准库 - 中文 NLP 模块
#
# 提供综合的中文自然语言处理功能：
# - 分词（jieba 精确/全/搜索引擎模式，词性标注，关键词提取）
# - 拼音转换（pypinyin 无音调/带声调/声调数字/首字母/姓氏）
# - 简繁转换（简体繁体互转，台湾/香港变体）
# - 文本统计（字符统计、词频、句子/段落、可读性、语言检测）
# - 数字与金额转换（中文数字、大写金额、百分比）
# - 文本处理工具（提取、相似度、敏感词过滤）
#
# 依赖（可选）:
#   pip install jieba
#   pip install pypinyin
#   pip install opencc-python-reimplemented
#
# 用法：
#   从 中文NLP 导入 分词精确模式, 转拼音, 金额转大写, 敏感词过滤器。
#
# =============================================================================
# 1. 分词 (Tokenization)
# =============================================================================
#
# 精确模式分词
#
# 全模式分词
#
# 搜索引擎模式分词
#
# 添加自定义词典
#
# 添加自定义词语
#
# 删除自定义词语
#
# 词性标注
#
# 提取关键词 (TF-IDF)
#
# 提取关键词 (TextRank)
#
# 分词带位置
#
# =============================================================================
# 2. 拼音转换 (Pinyin)
# =============================================================================
#
# 转拼音（无音调）
#
# 转拼音（带声调）
#
# 转拼音（声调数字）
#
# 转拼音首字母
#
# 转拼音列表
#
# 转拼音（姓氏优先）
#
# =============================================================================
# 3. 简繁转换 (Simplified/Traditional)
# =============================================================================
#
# 简体转繁体
#
# 繁体转简体
#
# 简体转台湾繁体
#
# 简体转香港繁体
#
# =============================================================================
# 4. 文本统计 (Text Statistics)
# =============================================================================
#
# 统计字符
#
# 统计词频
#
# 统计句子数
#
# 统计段落数
#
# 可读性评分
#
# 语言检测
#
# =============================================================================
# 5. 数字与金额转换 (Number/Currency)
# =============================================================================
#
# 数字转中文
#
# 中文转数字
#
# 金额转大写
#
# 百分比转中文
#
# 中文转百分比
#
# =============================================================================
# 6. 文本处理工具 (Text Utilities)
# =============================================================================
#
# 去除空白
#
# 去除标点
#
# 提取汉字
#
# 提取英文
#
# 提取数字
#
# 判断中英混合
#
# 文本相似度 (Jaccard)
#
# 文本相似度 (余弦)
#
# 敏感词过滤器（类）
#
# =============================================================================
# 导出
# =============================================================================

导出 分词精确模式 分词全模式 分词搜索引擎模式 添加自定义词典 添加自定义词语 删除自定义词语。
导出 词性标注 提取关键词TFIDF 提取关键词TextRank 分词带位置 转拼音 转拼音带声调。
导出 转拼音声调数字 转拼音首字母 转拼音列表 转拼音姓氏 简转繁 繁转简。
导出 简转繁台湾 简转繁香港 统计字符 统计词频 统计句子数 统计段落数。
导出 可读性评分 语言检测 数字转中文 中文转数字 金额转大写 百分比转中文。
导出 中文转百分比 去除空白 去除标点 提取汉字 提取英文 提取数字。
导出 判断中英混合 文本相似度Jaccard 文本相似度余弦 敏感词过滤器。
