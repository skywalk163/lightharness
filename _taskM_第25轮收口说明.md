# 第25轮路M收口说明 —— CS词尾并入正面规则化 + Thinking微调数据集

> 日期：2026-09-15 ｜ 双方向：①编译器CS词尾并入正面规则化 ②lightharness代码段thinking微调数据集制作
> 跨项目操作：light-merge（编译器单字后缀规则）+ lightharness（验证+数据集）
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）

---

## 一、本轮概述

第24轮阶段D验证结论：CS表30条中24条真护栏，其中词尾并入17条是最大宗场景。本轮双方向：①将单字词尾并入从"CS补集"改为"正面类别F"，CS退化为"词首并入锚"；②从lightharness项目抽取代码段制作thinking微调数据集。

---

## 方向一：CS词尾并入正面规则化（任务1-3）

### 核心成果

| 指标 | 结果 |
|---|---|
| 单字词尾并入规则 | 从"CS补集（21字）"改为"正面类别F（43字）" |
| 新增排除集 | `_P0A_SUFFIX_SPLIT_SINGLE`（五类共21字：运算符/值字面量/控制流/范围/结构助词） |
| CS表变化 | **30→30不变**（退化为"词首并入锚"） |
| 18字词尾并入验证 | **0字可删**（G2词首编译门100%失败） |
| 全语料token变化 | **0/837文件** |
| pytest | 43 passed（18字正向+反向边界+混合+保护表形态断言） |

### 关键发现：CS表不能删的真正原因

任务2对18字词尾并入候选执行三重判据（G1语料+G2词首编译门+G3边界门），结论**0字可删**：
- 任务1的正面规则F只覆盖**词尾**（scan_pos>0）并入
- 未覆盖**词首**（scan_pos=0）并入
- CS表现退化为纯粹的"词首并入锚"——18字在词首位置去掉CS后全部浮出为KEYWORD（G2编译门100%失败）
- 这与第24轮"语料冗余但语义非冗余"结论一致，本轮用编译级证据坐实

**CS表完整通用化需分两步**：词尾并入（已解决，正面类别F）+ 词首并入（更难，需后续轮次）。

### 三路交付摘要

- **任务1**：实现正面类别F（`_TRAILING_ALIAS_CLASS = F`，43字）+ 单字排除集`_P0A_SUFFIX_SPLIT_SINGLE`（五类21字），全语料834文件token零变化
- **任务2**：18字逐条验证（三重判据），0字可删，CS表保持30条，反跑ALL OK
- **任务3**：边界测试套件（2个.light rc=0 + pytest 43 passed）+ 全量反跑（837文件token零变化）

---

## 方向二：Thinking微调数据集（任务4-6）

### 核心成果

| 指标 | 结果 |
|---|---|
| 原始抽取 | 3032段（stdlib 86文件+src 152文件+examples 352文件） |
| 去重后 | 2963段（移除69个重复） |
| 过滤后 | 2500段（函数1733/方法420/模块314/异步33） |
| 最终样本 | **2200条**（thinking生成+质量过滤） |
| 平均thinking | 401字（范围367-600字） |
| 平均代码段 | 29.7行 |
| Instruction类型 | 7种（debug/optimize/review/improve/explanation/other/implementation） |
| JSONL格式 | 2200/2200全合法，8字段 |

### 来源分布
- src/：1672条（76%）
- stdlib/：336条（15%）
- examples/：192条（9%）

### 数据集规格
- 文件：`lightharness/data/finetune/light_code_thinking_final.jsonl`（5.7MB）
- 字段：instruction/input/output/thinking/function_name/source_file/source_type/line_count
- 用途：大模型微调（支持thinking模式的模型）
- 配套：README.md（使用方法+训练建议+质量说明）

### 质量审查（任务6）
- JSONL全量解析：2200/2200合法
- 抽样50条人工审查：thinking与代码一致性96%，逻辑自洽98%，无幻觉98%
- 已知改进点：src占比偏高（76%）、代码段偏短（平均29.7行）、thinking长度集中在300-400字

---

## 三、验证结果

### 3.1 编译器方向反跑
- T2 词尾并入删除：ALL OK（0字可删）
- T3 全量反跑：ALL OK（837文件token零变化）

### 3.2 数据集方向验证
- JSONL全量解析：2200/2200合法
- 抽样质量审查：通过率94-100%

### 3.3 全量CI
- **pytest**：686 passed + 1 failed（test_审批.light，已知flaky R20-C wall-clock抖动，单独复跑rc=0）
- **smoke**：5/5
- **耗时**：8分12秒

> 注：test_审批.light的flaky从第20轮持续存在（`等待结果`用`time.time()`而非`time.monotonic()`），非本轮引入，非编译器缺陷。

---

## 四、路M修正与决策记录

1. **CS表保持30条，未删除**：任务书预期"CS 30→约13条"，实际验证0字可删。路M认可——正面规则F只覆盖词尾并入，CS还承担词首并入锚职责，G2词首编译门100%失败是硬证据。CS表完整通用化需分别解决词首并入（更难）。

2. **正面类别F替代CS补集**：任务1把词尾并入从"对CS取补集（21字）"改为"正面类别F（43字）"，这是架构改进——词尾并入不再依赖CS表的存在，CS表职责收窄为"词首并入锚"。路M认可这一架构改进。

3. **数据集src占比偏高**：src 76%/stdlib 15%/examples 9%，src占比偏高因src目录文件数多（152个）函数级抽取量大。路M记录为改进点，下一轮扩充时可增加stdlib模块级抽取权重。

4. **test_审批.light flaky持续存在**：从第20轮到第25轮持续存在，路M建议后续轮次专门修复（`等待结果`改用`time.monotonic()`）。

---

## 五、已知问题与后续方向

### 5.1 编译器方向
- CS表30条仍需完整通用化：词尾并入已解决（正面类别F），词首并入待解决（更难）
- CCW 37条仍需分批精简
- _EMBED_MAX_MATCH_KEYWORDS 3条仍需重新评估
- test_审批.light flaky待修复（time.monotonic）

### 5.2 数据集方向
- 可扩充：增加stdlib模块级抽取、增加长代码段（100+行）、增加多轮对话格式
- 可优化：thinking长度多样化、src/stdlib/examples比例均衡化

### 5.3 第26轮方向建议
1. **CS词首并入通用化**（推荐）：解决CS表剩余的词首并入锚职责，这是CS表完整通用化的最后一步
2. **CCW剩余37条分批精简**
3. **数据集扩充与优化**
4. **继续上游复刻**

---

## 六、docs三件回填

1. **对标清单**（docs/功能对标/对标清单.json）：追加#142（CS词尾并入正面规则化，CS退化为词首并入锚）、#143（Thinking微调数据集2200条），总数143条。
2. **缺陷账**（docs/功能对标/语言缺陷账.md）：本轮无新发现缺陷，未更新。
3. **行为差异清单**（docs/功能对标/行为差异清单.md）：追加R25-D1（CS词尾并入正面规则化，CS保持30条退化为词首并入锚，订正R24-D1）、R25-D2（单字后缀位置正面规则新增）、R25-D3（Thinking微调数据集工程产物）。

---

## 七、git提交

### light-merge项目（1个提交）
- 待提交：src/lexer.py（58+/20-，正面类别F+单字排除集）

### lightharness项目（6路分路提交 + 收口提交）
- 任务1：单字后缀规则设计+快速验证
- 任务2：18字逐条验证+删除清单（0字可删）
- 任务3：边界测试+全量反跑
- 任务4：代码段抽取（3032→2500段）
- 任务5：thinking生成（2200条最终样本）
- 任务6：质量审查+数据集README+docs回填
- 收口提交：docs三件 + 质量审查报告 + 收口说明
