# 外发任务：lightharness 第二轮开发 E 批次——复刻 deepseek-harness 核心能力

> 目标：让 lightharness 从"只能聊天的 Web 界面"升级为"能调用工具做事的 agent harness"，复刻 deepseek-harness 的核心设计。
> 参考原型：`G:\github\deepseek-harness`（全插件化 Cordis 架构，60+ packages）
> 当前基线：lightharness 已有 agent 循环（OpenAI 兼容 API + SSE）、Web UI（聊天+配置+登录）、大模型配置热更新、188 回归全绿。

## 差距分析

| 能力 | deepseek-harness | lightharness 当前 | 优先级 |
|------|-----------------|-------------------|--------|
| 工具系统 | 60+ 工具（bash/fs/web/lsp/mcp/terminal/...），插件化注册 | 无（纯对话） | P0 |
| 会话持久化 | JSONL/SQLite，会话列表/标题/查询/检查点 | 内存中，刷新即丢 | P0 |
| 权限与审批 | 工具调用审批、权限预设、沙箱策略 | 无 | P1 |
| 上下文压缩 | 摘要压缩、工具结果修剪、token 计量 | 无（长对话爆上下文） | P1 |
| Web UI 增强 | 会话侧边栏、工具调用卡片、审批界面、轨迹展示 | 基础聊天界面 | P1 |
| 子代理 | fork/spawn 子代理、深度限制、报告 | 无 | P2 |
| 目标/计划 | goal 驱动、plan 模式 | 无 | P2 |
| MCP/LSP | 外部工具服务器、代码智能 | 无 | P2 |

## E 批次任务拆分（5 个可并行任务）

| 任务 | 内容 | 依赖 |
|------|------|------|
| **E1** | 工具系统框架 + 核心工具（bash/fs/read/write/search） | 无（基础） |
| **E2** | 会话持久化（JSONL + 会话管理 API + 标题） | 无 |
| **E3** | Web UI 增强（会话侧边栏 + 工具调用卡片 + 历史渲染） | E1(工具事件格式) + E2(会话API) |
| **E4** | 权限与审批框架（审批队列 + 权限预设 + Web 审批 UI） | E1(工具框架) |
| **E5** | 压缩 + 令牌计量（上下文摘要 + 工具结果修剪 + token 统计） | 无（agent 循环钩子） |

## 共享契约（5 个任务必须遵守）

### 契约 1：工具调用事件格式（E1 定义，E3/E4 消费）

agent 循环中，工具调用和结果以标准事件格式在 SSE 流中推送：

```
工具调用请求：data: {"种类":"工具调用","工具名":"bash","参数":{"命令":"ls"},"轮次":3}
工具调用结果：data: {"种类":"工具结果","工具名":"bash","结果":"file1\nfile2","错误":null,"耗时":0.12}
工具调用错误：data: {"种类":"工具结果","工具名":"bash","结果":null,"错误":"命令执行失败: ...","耗时":0.05}
```

### 契约 2：会话管理 API（E2 定义，E3 消费）

```
GET    /api/sessions       → [{"id","标题","创建时间","最后更新","消息数"}]
GET    /api/sessions/:id   → {"id","标题","消息":[...完整历史...]}
POST   /api/sessions       → {"id","标题"}（新建空会话）
DELETE /api/sessions/:id   → {"ok":true}
```

当前会话通过 `GET /api/sessions/current` 获取（兼容现有无会话模式）。

### 契约 3：审批事件格式（E4 定义，E1/E3 消费）

```
审批请求：data: {"种类":"审批请求","工具名":"bash","参数":{"命令":"rm -rf /"},"会话id":"...","轮次":3}
审批结果（前端 POST /api/approvals/:id）：{"批准":true,"理由":""}
```

### 契约 4：Agent 循环中间件钩子（E1/E4/E5 共用）

agent 循环支持三个钩子点，各任务注册自己的钩子：
- `pre_tool(工具名, 参数) → 批准/拒绝/需审批`（E4 注册）
- `post_tool(工具名, 结果) → 修剪后结果`（E5 注册工具结果修剪）
- `post_turn(消息列表) → 压缩后消息列表`（E5 注册上下文压缩）

E1 实现钩子注册机制和工具调度主循环，E4/E5 只注册钩子不修改主循环。

### 契约 5：静态目录与路由

- 所有 API 挂在 `/api/` 下，静态文件仍由 `webui/` 目录服务
- GET 请求全放行（含 `/api/sessions` 读取），POST/DELETE 受 token 保护（沿用现有认证）
- SSE 流路径不变（`/v1/chat/completions`），工具事件在同一流中推送

## 任务文件

- `外发任务_lightharness_E1_工具系统与核心工具.md`
- `外发任务_lightharness_E2_会话持久化.md`
- `外发任务_lightharness_E3_WebUI增强.md`
- `外发任务_lightharness_E4_权限与审批.md`
- `外发任务_lightharness_E5_压缩与令牌计量.md`

## 集成顺序

1. E1 + E2 + E5 可完全并行（无相互依赖）
2. E3 依赖 E1 的工具事件格式和 E2 的会话 API（可先做 UI 框架，接口定后对接）
3. E4 依赖 E1 的工具框架（可先做审批 UI，E1 定后对接）
4. 集成验收：5 个任务交付后，按 E1→E2→E5→E4→E3 顺序合入，跑全量回归 + 端到端实测
