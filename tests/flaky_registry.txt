# =============================================================================
# lightharness 门禁 flaky 台账（R87-E 建，2026-09-23）
# 收录口径：「隔离复跑恒绿 + 全量偶发红」的环境 flaky 用例（任务书 E 路）。
#   已根治项不入本台账（根治后失败即真回归，必须判红，不得豁免）。
# 用法：门禁判据（tests/test_回归.py）按（文件名, 平台）加载本台账，
#   命中条目在该平台偶发失败时不计入「新增红」，豁免有据可查（见取证记录）。
#   未登记文件/平台的失败仍硬判回归红，判据强度不降。
#   ⚠️ 登记仅豁免「同类偶发抖动」；若条目转为稳定复现，必须人工归因（疑似真退化）。
#   历史「0.82 全量偶发（终端PTY/R21超集）」台账注释见 tests/ci_environment_reds.txt
#   （其已反向指向本台账，R87-F 环境固化路完成互引）。
#
# 字段（制表符分隔，机器可解析；# 开头为注释）：
#   F编号 \t 文件名 \t 平台 \t 现象/取证记录 \t 处置
# 平台取值与 sys.platform 对齐：windows / freebsd / linux
# =============================================================================
#
# ── R87-E 首批登记（2026-09-23，逐条一手取证）────────────────────────────
# 取证口径：对应平台连续全量 2 轮（各自独立复跑，非同进程重放）。
# 0.82 口径：/tmp/r44-20260923-142451 副本，python3.12 串行（-o addopts=，远端缺
#   xdist/timeout 插件，与 R86 收口串行口径一致），git HEAD ac4be9d。
# 0.86 口径：/tmp/r85-20260923-142453 副本，/tmp/r85-venv python + xdist -n auto
#   （同 0.86 CI 口径），git HEAD ac4be9d。
# Windows 口径：本机 light-merge/.venv python，xdist -n auto + timeout 60（pytest.ini）。
F-01	test_终端PTY.light	freebsd	0.82 全量偶发红、单跑恒绿（R81 起观察；PTY 轮询计时类）。R68 已落「轮询+累积读取（15s 预算）」加固。R87-E 取证：0.82 串行全量 2 轮均无该条失败（run1 577.41s / run2 471.45s，1393 passed，仅 E 范围外 3 条红）。	维持 R68 判据不改码（避免为偶发现象扩改动面）；登记豁免同类偶发抖动；稳定复现须人工归因。
F-02	test_async_await.light	linux	0.86 xdist 高负载计时抖动（R85 全量 conc 0.26s>0.18s 偶发）。R86-C 已落「阈值 0.18→0.35 + 顺序-并发≥0.05s 相对收益兜底」。R87-E 取证：0.86 xdist 全量 2 轮均无该条失败（run1 335.03s / run2 451.75s，均 1371 passed 0 failed）。	维持 R86-C 判据不改码；登记豁免负载抖动；稳定复现须人工归因。
#
# ── 已根治不入账（留档备查）──────────────────────────────────────────────
# test_R21_词法确定性_超集.py（freebsd）：全量偶发 ERROR(FileNotFoundError)，
#   git archive 子进程高负载瞬时 spawn 失败（单跑恒绿、复跑全绿）。
#   R87-E 根治：_run_with_retry 3 次线性退避重试 + 连续失败降级 skip
#   （spawn 失败不再产生红/ERROR，故无需台账豁免；真回归=断言失败仍硬判红）。
#   取证：0.82 串行全量 2 轮无失败；本机 Windows 单跑 2 passed。
# test_子进程后台.light（windows）：R44 时代全量偶发红（固定 5s 等待撞高负载）。
#   R45 已根治（HARNESS_PY 绝对路径 + 等待 20s + 失败归因信息）。
#   R87-E 复核：Windows 全量 2 轮均绿（1396 passed，173.59s / 247.65s）无复发。
