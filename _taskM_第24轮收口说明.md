# 第24轮路M收口说明 —— 阶段D：_COMPOUND_SAFE_SINGLE_KEYWORDS 单字通用化

> 日期：2026-09-15 ｜ 方向：阶段D单字通用化（保护表通用化替代最终阶段）
> 跨项目操作：light-merge（编译器单字保护表通用化）+ lightharness（验证与复现）
> 上游基线：deepseek-harness 0.1.5-rc.2（a305303422）

---

## 一、本轮概述

第23轮保护表通用化替代轮完成了阶段A+B+C（3表清零，CCW 184→37，逐词白名单226→73）。本轮目标是阶段D：修复已知独立缺陷，逐条验证30条单字保护表的必要性，用通用规则替代逐词白名单。

### 核心发现：单字表是当前最"实"的保护表

任务2逐条隔离中立验证结果：30条中仅6条语料冗余（20%），24条为真护栏（80%）——与多字表形成鲜明对比（第22轮TierB 32条全冗余）。

**保护场景分布**：
- 词尾并入：17条（属性名/类名/调用名末字，如标准输出/日期列/规则/成绩的长度/有界队列类等）
- 词中并入：6条（递归副作用承重墙，如乘阶乘/用例/加斐波那契/对于归属/自定义表等）
- 词首并入：1条（模：模拟客户端类名，已在_P0A_NEVER_SPLIT）

**决策：CS保持30条**——语料冗余但语义非冗余，撤掉会破坏成员访问段首保护。

### 本轮成果总览

| 工作项 | 成果 |
|---|---|
| `之`成员访问符被吞缺陷修复（L-153） | 新增`_p0a_contains_sep` helper+`之`守卫，`自之成绩`/`自之取平均分`编译恢复 |
| `的`递归吞分隔符审计 | 不可复现（deterministic=True路径全部正确） |
| 运算符/值字面量守卫验证 | 已覆盖，无需增强 |
| 30条单字逐条验证 | 6条冗余(20%)，24条真护栏(80%) |
| CR1通用规则（`_seg_after_sep`游标） | 接管13条的成员访问段首保护场景 |
| 死代码清理 | 删除`_P0A_COMPOUND_SAFE`（全库零引用） |
| 递归语义类别代数化 | 类别代数+等价断言落地（防漂移），运行路径零改动 |
| 175条自由名保护验证 | 去除空格/10的幂/索引/种类/阶乘等全PASS |
| CS表 | **保持30条**（语义层非冗余不可删） |

---

## 二、6路交付摘要

### 任务1（P0）：已知缺陷修复
- **`的`递归吞分隔符**：不可复现（deterministic=True路径全部正确，两侧零变化）
- **真实P0缺陷（任务书未点名）**：`之`成员访问符被第21轮「非语句起始位置关键字前缀标识符整体成词」规则吞掉，`自之成绩`/`自之取平均分`在学生模块.light编译期NameError→已修复
- **修复方案**：新增`_p0a_contains_sep(source, start, length)` helper（扫描串内真实_P0A_SEP命中，用_match_keyword逐位判），非语句起始位置整体成词规则增加`之`守卫
- **运算符单字守卫**（乘加减除余）：已覆盖，无需增强
- **值字面量守卫**（真空）：已覆盖，无需增强
- **验证**：4个.light rc=0，pytest 84 passed，真实源token变化1文件（学生模块.light修正）

### 任务2（P0）：30条单字逐条验证+分类
- **逐条验证**：判据③全语料769文件token序列对比
- **分类**：①冗余可删4条（是/首+余/减/空/除的非守卫部分）②真护栏24条 ③运算符/值字面量守卫管辖7条
- **关键发现**：词尾并入是最大宗场景（17/24），递归副作用是词中并入承重墙
- **交付**：分类清单.md + 证据.json（含每条变化文件清单）

### 任务3（P1）：P0-A复合安全集合通用化
- **决策：CS保持30条**——任务2判为冗余的13条语料层零变化，但语义层非冗余（撤掉破坏成员访问段首保护）
- **CR1通用规则**：新增`_seg_after_sep`游标，接管13条的「成员访问段首」保护场景（`甲之<c>顶`形态）
- **死代码清理**：删除`_P0A_COMPOUND_SAFE = _COMPOUND_SAFE_SINGLE_KEYWORDS | {'当'}`（全库git grep零引用）
- **验证**：反跑ALL OK，真实源token变化1文件（任务1的自之X修正），性能×1.066~1.115（变快）

### 任务4（P1）：递归语义规则化
- **递归语义分析**：穷举`_match_keyword`/`_skip_compound_safe_and_match`的递归返回路径
- **类别代数化**：纯类别推导（全集−运算符−值字面量等=43字）直接替换会导致26文件漂移（丢失乘/减/加/除/模/真/空/除8个"语义双栖"字的递归资格）
- **决策**：类别代数+等价断言落地（防漂移机制），**运行路径零改动**，行为与R23基线逐字节一致
- **175条自由名保护验证**：去除空格/10的幂/索引/种类/阶乘/乘阶乘/加斐波那契/标准输出失真等全PASS
- **验证**：全语料772文件token零差异，全量CI无新增回归

### 任务5（P1）：全量回归扫描
- **ALL OK**：新增红用例0，预期变化1（学生模块.light的自之X修正）
- **175条自由名专项**：全部整体成词且两侧一致
- **性能**：基准中位6.58s→修复态中位6.00s，×1.098（提升9.8%）
- **真实变化文件编译A/B**：base_rc=0→edit_rc=0（不劣于基准）

### 任务6（P2）：docs更新+最终目标规划
- **对标清单**：追加#139（之被吞缺陷修复）、#140（CR1规则+死代码清理+递归语义类别代数化）、#141（单字表精简评估，CS保持30条），总数141条
- **缺陷账**：新增L-153（之成员访问符被吞缺陷，已修复）
- **行为差异清单**：追加R24-D1（单字保护表通用化，CS保持30条，订正R23-D1）、R24-D2（之成员访问符守卫新增规则）
- **最终无保护表目标规划**：_task6_R24_最终无保护表目标规划.md（剩余70条逐词白名单的演进路径+建议轮次规划）

---

## 三、验证结果

### 3.1 第24轮反跑（3路）
- T3 P0-A复合安全通用化：ALL OK
- T4 递归语义规则化：ALL OK
- T5 全量回归扫描：ALL OK

### 3.2 第24轮新增测试用例
- 5个.light运行期自校验：全部rc=0
- pytest：任务1新增84 passed，任务3/4新增测试

### 3.3 全量CI
- **pytest**：641 passed（比第23轮552多89个）
- **smoke**：5/5
- **耗时**：8分05秒

### 3.4 性能
- 全语料词法分析中位耗时：6.58s→6.00s，**提升9.8%**

---

## 四、路M修正与决策记录

1. **任务书设想的"删表"没有发生**：任务2判为冗余可删的13条，任务3进一步验证语义层非冗余（撤掉破坏成员访问段首保护）。路M认可CS保持30条的决策——单字表与多字表本质不同，80%是真护栏。

2. **`的`递归吞分隔符不可复现**：任务书点名的已知缺陷，经审计在deterministic=True路径全部正确。路M记录为"不可复现"，不视为未完成。

3. **发现任务书未点名的真实P0缺陷（L-153）**：`之`成员访问符被吞，导致学生模块.light编译期NameError。任务1主动发现并修复，路M认可并新增L-153到缺陷账。

4. **递归语义运行路径零改动**：任务4验证纯类别推导直接替换会导致26文件漂移，决策采用"类别代数+等价断言落地，运行路径零改动"的保守方案。路M认可——递归副作用是词中并入的承重墙，运行路径替换风险大，留给后续轮次分步处理。

5. **_P0A_COMPOUND_SAFE死代码删除**：全库git grep零引用，任务3安全删除。路M验证删除后CI全绿。

---

## 五、已知问题与后续方向

### 5.1 仍存在的逐词白名单（70条）
- _COMPOUND_SAFE_SINGLE_KEYWORDS：30条（语义层非冗余，需深度递归语义规则化）
- COMMON_COMPOUND_WORDS：37条（真护栏，可分批精简）
- _EMBED_MAX_MATCH_KEYWORDS：3条（纯嵌入场景，CS词中并入规则化后重新评估）

### 5.2 第25轮方向建议
1. **CS词尾并入17条单字后缀规则**（推荐）：实现单字版本的后缀位置上下文规则（scan_pos>0 + 单字 + 非运算符 + 非值字面量 + 非排除集 → 并入标识符），覆盖17条词尾并入场景，CS 30→约13条。可与CCW分批精简并行。
2. **CCW剩余37条分批精简**：有codegen映射的验证登记后删除，错误类型验证stdlib定义后删除，运算符前缀验证P0-A覆盖后删除。
3. **CS词中并入6条递归副作用运行路径替换**：风险较高，建议在词尾并入完成后单独轮次处理。
4. **继续上游复刻**：回到deepseek-harness上游复刻主线。

详细规划见`_task6_R24_最终无保护表目标规划.md`。

---

## 六、docs三件回填

1. **对标清单**（docs/功能对标/对标清单.json）：追加#139（之被吞缺陷修复/L-153）、#140（CR1规则+死代码清理+递归语义类别代数化）、#141（单字表精简评估，CS保持30条语义层非冗余），总数141条；版本字段更新为「覆盖记录至第24轮」。
2. **缺陷账**（docs/功能对标/语言缺陷账.md）：新增L-153（之成员访问符被「非语句起始位置整体成词」规则吞掉，已修复）。
3. **行为差异清单**（docs/功能对标/行为差异清单.md）：追加R24-D1（单字保护表通用化，CS保持30条，80%真护栏，订正R23-D1的`的`递归缺陷描述）、R24-D2（之成员访问符守卫新增规则）。

---

## 七、git提交

### light-merge项目（1个提交）
- 待提交：src/lexer.py（121+/6-，之被吞修复+CR1规则+死代码清理+递归语义类别代数化）

### lightharness项目（6路分路提交 + 收口提交）
- 任务1：已知缺陷修复（之被吞+运算符/值字面量守卫验证）
- 任务2：30条单字逐条验证+分类
- 任务3：P0-A复合安全通用化（CR1规则+死代码清理）
- 任务4：递归语义规则化（类别代数+175自由名验证）
- 任务5：全量回归扫描
- 任务6：docs更新+最终目标规划
- 收口提交：docs三件 + 最终目标规划 + 收口说明
