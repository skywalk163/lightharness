# 任务5 交付报告：函数与闭包增强库（src/函数增强.light）

> 任务来源：`G:\dswork\duan-light-merge\复刻_第18轮_任务prompt分发_缺陷修复轮 (1).md` 任务5
> 日期：2026-09-13 ｜ 对标 #120（光明语言工具库·缺陷修复轮）
> 命名预检查：src/ 下无 函数增强.light 同名模块 ✓

---

## 一、函数清单与修复层级（含设计决策）

| 函数 | 修复对象 | 层级 | 说明 |
|---|---|---|---|
| 带默认参数(函数, 默认参数字典) | L-102 | **A级** | 包装「字典参数函数」，缺键填默认值后调用。设计决策：光明参数个数固定无法变长展开，默认参数统一采用「字典参数函数」约定（R18-D5） |
| 柯里化(函数, 参数个数) | 配套 | B级 | 列表参数函数→单参函数链（嵌套段落返回，利用 L-078 闭包能力） |
| 闭包包装(函数, 状态字典) | L-098 | **A级** | 约定原函数接收 (参数, 状态字典)，状态字典引用跨调用共享——绕开 L-098 闭包外层变量修改不生效；返回 {调用, 状态} |
| 造计数器(初始值) | 配套 | B级 | 闭包包装应用范例：{递增, 获取值, 重置, 状态}，状态持久专条验证 |
| 管道(值, 函数列表) | 通用 | B级 | 值依次通过函数列表 |
| 组合函数(甲, 乙) | 通用 | B级 | 甲(乙(x))，顺序敏感专条 |
| 记忆化(函数) | 通用 | B级 | 单参数字典缓存（键=转字符串(参数)），执行计数专条验证只算一次 |
| 应用参数(函数, 参数列表) | 通用 | B级 | 列表参数函数直通（同上约定） |
| 偏应用(函数, 固定参数列表) | 配套 | B级 | 固定前 N 参，返回接收剩余列表的函数 |
| 断言包装(函数, 前置, 后置) | 通用 | B级 | 前置(参数)/后置(结果) 失败抛 `断言包装：前置/后置条件失败` |

## 二、测试验证

```
cd G:\dswork\duan-light-merge\lightharness
$env:LIGHT_MERGE="G:\dswork\duan-light-merge\light-merge"; python 运行.py examples/test_函数增强.light
test_函数增强 PASS   （rc=0）
```

9 组 ≥35 断言：默认参数（全默认/覆盖甲/覆盖乙/全显式）、柯里化（三元链/二元）、闭包包装（状态跨调用持久/外部读取）、计数器（初始/递增/重置/状态可读）、管道（空/单/多/传递）、组合（双向顺序）、记忆化（首算/缓存/执行计数/不同参数）、应用与偏应用（空参/固定首参/固定多参）、断言包装（通过/前置失败）。

## 三、反跑结果（3/3 ALL OK）

`lightharness/_antirun_t5_函数增强.py`（BASE 自定位；try/finally 恢复）：

```
PASS C=变异 闭包状态递增改错（不持久） -> 红 (变异后运行 rc=1（期望非 0，真红）)
PASS 字节级恢复校验 (sha256 d79f13b347ac)
PASS A/B 判据（默认参数填充 + 计数器持久）回归绿 (rc=0)
ALL OK
```

C 变异：造计数器 递增分支的 `字典获取(状态, "值", 初始值) + 1` 改为 `初始值`——计数不持久递增，「两次递增」断言立红，恢复后绿。

## 四、语言差异（本轮新缺陷登记）

- 无新缺陷（L-159 编号空缺，路M 顺延）。
- 设计决策（R18-D5）：字典参数函数/列表参数函数约定（光明无可变参数与参数展开）；测试自身踩 L-091（匿名段落内联）与 L-119 同族（`参数为取` 撞 `为` 字）均按既有绕法处理。

## 五、移交清单

- `lightharness/src/函数增强.light`、`examples/test_函数增强.light`（PASS rc=0）、`lightharness/_antirun_t5_函数增强.py`（3/3 ALL OK）、本报告。
- 移交路M：对标清单新增 **#120**；行为差异 **R18-D5**；CI 增 1 test + 1 反跑。
