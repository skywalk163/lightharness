# 第23轮 任务3 交付报告 —— 阶段C（部分）：CCW 内建名迁移 / 清表

> 日期：2026-09-14 ｜ 轮次：第23轮 任务3（P1） ｜ 方向：保护表通用化替代
> 修改文件：`light-merge/src/lexer.py`（仅 `COMMON_COMPOUND_WORDS` 定义块）
> **`light-merge/src/code_generator.py` 未修改**（论证见 §四）
> 反跑脚本：`lightharness/_antirun_r23_t3_CCW内建名迁移.py` → **ALL OK**
> 迁移清单：`lightharness/_task3_R23_CCW内建名迁移清单.md`

---

## 零、结论速览

| 项 | 结果 |
|---|---|
| `COMMON_COMPOUND_WORDS` | **184 → 37 条**（删 147，79.9%） |
| 删除判据 | **双中立**（语料中立 ∧ 隔离中立），逐条独立验证 |
| 删除条目逐条冗余验证 | **147/147 全中立**（反跑 [D]） |
| 真实源 token 变化 | **0 / 776 文件** |
| 生成产物树漂移 | 6 文件（均 `bootstrap/release/stdlib/`，不在测试断言路径） |
| codegen 产物 | 两侧 sha256 **完全一致**（`17df02f1…`） |
| `code_generator.py` | **未修改** |
| 反跑判定 | **ALL OK** |

---

## 一、验收标准对照（任务书 §任务3「验证标准」）

| # | 验收标准 | 实测 | 证据 |
|---|---|---|---|
| 1 | codegen 映射复合名识别完成（列出全部） | ✅ 分类①45 / 分类②15 / 分类③87 / 护栏37 | 清单 §二/§三 |
| 2 | 内建名迁移完成（codegen 有登记、CCW 已删） | ✅ 60 条有映射者全部从 CCW 删除并在 codegen 有登记 | 清单 §三.1/§三.2，反跑 [G] |
| 3 | codegen 输出不变 | ✅ 探针编译产物 sha256 两侧一致 | 反跑 [F] |
| 4 | 数学函数名 / 错误类型名处理（有 stdlib 定义者从 CCW 删除，token 零变化） | ✅ 全部删除且逐条零变化 | 反跑 [C]/[D] |
| 5 | 真护栏保留 + 记录保护场景 | ✅ 37 条保留，逐条分类记录（34 仅隔离非中立 + 3 条件中立） | 反跑 [D2]，清单 §四 |
| 6 | 全量 CI 无回归 | ✅ lightharness 552 passed；词法专项单测基线 A/B 零新增失败 | `_task5_R23_全量回归扫描报告.md` §五 |
| 7 | 反跑 ALL OK | ✅ A~G 全 PASS | 反跑输出 |

---

## 二、分类矩阵（184 条 → 37 条）

| 分类 | 条数 | codegen 登记归属 | 处置 |
|---|---|---|---|
| ① 内建名 | 45 | `code_generator.builtin_map`（**已登记**） | 双中立 ⇒ **删除** |
| ② 运算符 / 异常名 | 15 | `operator_map` / `exception_name_map`（**已登记**） | 双中立 ⇒ **删除** |
| ③ 无 codegen 映射 | 87 | 无（由 `STDLIB_VERB_ARITY` / 预扫描 / 上下文规则覆盖） | 双中立 ⇒ **删除** |
| ④ 保留·真护栏（A 组） | 17 | — | **保留** |
| ⑤ 保留·隔离护栏（B 组） | 20 | — | **保留** |
| | **184** | | |

明细见 `_task3_R23_CCW内建名迁移清单.md` §三（147 条逐条）与 §四（37 条逐条）。

### 判据：双中立（取最严口径）

| 判据 | 含义 | 工具 |
|---|---|---|
| ① 语料中立 | 撤掉该条 → 全语料 token 序列零变化 | `_r23_t3_ccw_sweep.py` |
| ② 隔离中立 | 撤掉该条 → **该词自身**单独 tokenize 序列零变化 | `_r23_t3_ccw_isolation.py` |

**①②同时成立才删**（仅①成立者保留）——147 条全部满足，17 条仅不满足②，20 条仅不满足①。

---

## 三、任务书「内建名迁移」的执行结论

任务书设想：把 CCW 中有 codegen 映射的内建名「迁移到内建登记表（若没有则新增）」。

**实际结论：迁移 = 「确认登记已存在 + 词法层不再白名单」，无需新增任何登记。**

- 分类① 45 条内建名**全部早已**登记在 `src/code_generator.py::builtin_map`（该表 275 条）；
- 分类② 15 条早已登记在 `operator_map` / `exception_name_map`；
- 反跑 [G] 用 **AST 静态抽取** `code_generator.py` 的映射键集合逐条复核，45/45 + 15/15 命中。

**因此 `code_generator.py` 未做任何修改**——任务书「如果没有，新增登记」的前提不成立，
新增即为**重复登记**：

1. **不该新增的第一类**：`异步读取文件` / `异步写入文件` / `异步追加文件`——全仓无任何实现，
   补 `builtin_map` 指向不存在的目标只是把 `NameError` 换成 `AttributeError`，
   属第二轮总纲 §5 明令禁止的「**词法白名单式假实现**」。它们保留在 CCW（本轮不动）。
2. **不该新增的第二类**：`并发等待` / `异步睡眠` / `限时` / `创建任务` / `首个完成`——**已在**
   `builtin_map` 中（真映射到 `asyncio.*`），无需新增。
3. **分类③ 87 条无映射者**：其保护场景由 `STDLIB_VERB_ARITY` / `_scan_user_definitions`
   预扫描 / 第21轮上下文敏感切词覆盖，属「词法层不再需要知道这些名字」的正解，
   不应为了「迁移」而给它们造 codegen 登记。

---

## 四、反跑结果（`_antirun_r23_t3_CCW内建名迁移.py`）

| 判据 | 内容 | 结果 |
|---|---|---|
| [A] | 基线自检：两侧各两次 dump 一致（语料一次性快照，免受并行写入干扰） | **PASS** |
| [B] | 真实源 token 变化 0 文件（生成树漂移 6） | **PASS（零回归）** |
| [C] | 批量补回 147 条已删条目 → 语料 token 变化 0 文件 | **PASS** |
| [D] | **删除条目逐条**冗余验证 147 条（逐条撤离 → 全语料 token 零变化） | **PASS** |
| [D2] | 保留条目必要性分类：语料非中立 0 ｜仅隔离非中立 34 ｜条件中立 3 | 信息项 |
| [E] | B 组（20 条）隔离非中立复核 | **PASS** |
| [F] | codegen 产物 sha256：断裂态 `17df02f1…` / 修复态 `17df02f1…` | **PASS（一致）** |
| [G] | 迁移登记复核：分类① 45 在 `builtin_map`，分类② 15 在其它映射，分类③ 87 无映射 | **PASS** |
| 还原校验 | `src/lexer.py` sha256 前后一致 | **OK（`342bc6c4…`）** |

**判定：ALL OK**

### 4.1 [D] 为什么检验「删除集」而不是「保留集」

本轮风险在**删除**（删错会把合法标识符切碎），保留 37 条是**保守选择**——多留一条冗余
条目不是回归。故门控判据为：
- [C] 批量补回 → 零变化（可加性）；
- [D] **逐条**撤离（在 184 条原状下）→ 零变化（任务书「不得批量删除」的逐条要求）。

两者同时成立 ⇒ 147 条删除**逐条安全**。

### 4.2 [D2] 发现：3 条「条件中立」条目

在当前**三方合并态**（任务1 通用类别 + 任务2 后缀位置上下文规则 + 任务3 CCW 清表）的
lexer 下，以下 3 条**两项皆中立**（撤掉后既不影响语料、也不影响自身切分）：

| 词 | 含该词的语料文件数 | 语料变化 | 自身变化 | 说明 |
|---|---|---|---|---|
| `低级关闭` | 7 | 0 | 否 | 含多字关键字 `关闭` |
| `正则匹配` | 21 | 0 | 否 | 含多字关键字 `匹配` |
| `环境枚举` | 6 | 0 | 否 | 含多字关键字 `枚举` |

**根因**：这 3 条原本靠 CCW 防止被切成 `低级+关闭` / `正则+匹配` / `环境+枚举`；
**任务2 的「后缀位置上下文规则」（多字非运算符关键字位于汉字词词中/词尾即并入标识符）
已通用覆盖该场景**，故它们的必要性在合并态下消失。

**本轮处置：保守保留**（不改 37 条）。理由：
1. 该中立性**依赖任务2 的规则**，而任务的边界「不越界」要求任务3 不把 CCW 的规模
   建立在他人规则之上；
2. 保留冗余条目**不构成回归**（[B] 已证真实源零变化）；
3. 已在此**明确记录**，**建议后轮（阶段D）在任务2 规则稳定后追加删除**（届时 37 → 34）。

> 注：这与 20:16 的初次扫描结论不同——初次扫描时 3 条尚非中立（`files=3/8/3`），
> 其后 `lexer.py` 被并行改动（任务2 落地后缀位置规则）因而转为中立。本报告以**合并态实测**为准。

---

## 五、验证标准自查

| 项 | 状态 |
|---|---|
| 反跑 ALL OK | ✅ |
| 逐条验证（147 条逐条零变化，非批量） | ✅ [D] |
| 真实源 token 零变化 | ✅ 0/776 |
| codegen 输出不变 | ✅ [F] |
| `code_generator.py` 未改（避免重复登记/假实现） | ✅ |
| 未改 `_at_statement_start` / 上下文主分支 / `_EMBED_MAX_MATCH_KEYWORDS` / `_COMPOUND_SAFE_SINGLE_KEYWORDS` | ✅ |
| 反跑 finally 恢复 + sha256 校验 + 外部写入检测 | ✅ |

---

## 六、遗留与后续

1. **建议后轮追加删除 3 条条件中立条目**（`低级关闭` / `正则匹配` / `环境枚举`），
   前提是任务2 的后缀位置上下文规则不再变动。
2. `bootstrap/release/stdlib/*.light` 生成树漂移同任务1 报告 §五：产物树不含测试断言，
   建议后续统一重生成。
3. 分类③ 中被判「无 codegen 映射」的 87 条（如 `函数` / `模式` / `除法` / `加入` 等）
   已确认词法层不再需要；若后续引入新的 STDLIB 原语，应优先走「预扫描 + codegen 登记」
   而非回填 CCW。
