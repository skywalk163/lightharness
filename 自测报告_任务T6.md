# 自测报告_任务T6 — 光明编译器 L-054：嵌套字面量实参内 `\n` 转义缩进状态错乱

- 任务代号：T6
- 完成日期：2026-09-04
- 双仓库基线：light-merge main `eb7735af`；lightharness main `d2b8248`
- 分支：light-merge `task-T6`（worktree `wt-T6`，提交 `78a3e5d3`）；lightharness `task-T6-lh`（worktree `lh-T6`，提交 `5d00889`）
- 文件隔离遵守：仅动 lightharness 侧 `examples/test_语言服务器.light`、`docs/功能对标/语言缺陷账.md`；light-merge 侧仅新增测试与示例，**零源码改动**

---

## 一、任务要求

修复光明编译器 L-054 缺陷：函数调用实参的**嵌套字典/列表字面量**内字符串含 `\n` 转义时，缩进状态错乱，导致该行之后所有同级语句被误判为模块级（`def 主` 提前结束 → 函数内变量未定义报 `名称错误`）。要求：①定位根因（参考 L-051 `bracket_stack` 同族逻辑）；②新建/启用最小复现（`test_语言服务器.light` 的 `chr(10)` 绕行改回直接 `"\n"` 后通过）；③回写语言缺陷账 L-054 状态。

## 二、根因探查结论（已完成，定论）

**L-054 与 L-051 是同一族问题（字面量内换行/缩进处理），当前 main 已由 L-051 的机制顺带覆盖，lexer/parser 无需任何源码改动。**

关键机制（`light-merge/src/lexer.py`）：

1. **`_tokenize_string`（1360-1459 行）整体消费 `\n` 转义**：主循环字符串处理（745-751 行）先于换行处理（754 行）。`_tokenize_string` 把 `\n` 转义翻译成**真实换行字符 `'\n'`** 存入 STRING 值（1415-1416 行），consumed 按源码字符数计算，字符串分支整体消费、**不进入换行/缩进逻辑**——即嵌套字面量内字符串的 `\n` 转义**不参与缩进计算、不重置块状态**。
2. **L-051 `bracket_stack`（2026-09-03 commit 74df4169 引入）抑制字面量括号内真实换行**：当 `bracket_stack` 非空且栈顶为 False（处于字面量括号内）时，换行（755-762 行）只推进 line/col/i 后 continue，**不发射 NEWLINE/INDENT/DEDENT**——多行字面量的真实换行不会重置块状态。

`src/parser_core.py`、`code_generator.py` 均无按字符串值换行计数的逻辑（已 grep 确认）。

> **历史反证（验证「修复前是否复现」）**：L-054 登记于 09-02（lightharness 369fd98），`bracket_stack` 于 09-03 引入。用 09-02 当日完整编译器（临时 worktree `_taskT6_hist0902`，detached 3bfac888）跑单行/多行/语句级多行三种复现形态**全部通过**；父提交 lexer 与 09-02 lexer 影子加载同样不复现——L-054 原始缺陷现场在当前可取得的任何历史版本上都不可复原（可能当时另有解析器行为差异）。但缺陷的**修复保障**（`_tokenize_string` 整体消费 + `bracket_stack` 抑制）在当前 main 明确存在，且可被「改坏缩进追踪」探测验证。

## 三、反跑判据验证（两条均满足）

1. **绕行换回直接 `\n` 后测试通过**：`lh-T6/examples/test_语言服务器.light` 已彻底解除 `chr(10)` 绕行（删除 `段落 新行` 辅助、`设 新行1`，265/266 行直接写嵌套字典实参内 `"\n"`），`新行` 残留 0 处。定向运行：
   ```
   LIGHT_MERGE=G:\dswork\duan-light-merge\wt-T6 python 运行.py examples/test_语言服务器.light
   → --- 语言服务器（模块63）测试通过 ---
   ```
2. **改坏缩进追踪一行立即立红**：破坏版 lexer `_taskT6_badindent`（去掉 INDENT 发射一行）下同一文件**立即立红**——「缩进不正确：这一行的缩进层级没有对应的语句块」，指向 `如果 抛了 == 假` 附近。另验证 L-051 测试 `examples/test_L051.light` 在禁用 bracket 换行抑制的破坏版 lexer 下立即立红（「期望 右方括号「]」，但得到 换行」），证明 `bracket_stack` 抑制是同一族关键机制。

## 四、新增复现/回归用例

**light-merge 侧（wt-T6，提交 `78a3e5d3`）**：

- `examples/test_L054.light`（新增，本地复现示例；`test_*.light` 属仓库 gitignore 惯例，与既有 `test_L051.light` 一致不入库）：单行嵌套字典实参+`\n`（L-054 原文形态）+ 后续同级语句 + 多行嵌套字典实参+`\n`（L-051 同族），直接运行输出正确。
- `tests/unit/test_L054_nested_literal_newline.py`（新增，入库回归哨兵，4 条）：

  | 用例 | 断言要点 |
  |------|----------|
  | `test_call_arg_nested_dict_newline_value_preserved` | 嵌套字典实参内 `\n` 值保留（`out == 'a\nb'`），后续同级语句编译进函数段内 |
  | `test_newline_translated_not_literal_backslash` | `\n` 翻译为真实换行非字面反斜杠 |
  | `test_multi_line_nested_dict_newline_blocks_not_broken` | 多行嵌套字典+`\n` 后续块不破坏 |
  | `test_generated_code_no_module_level_demotion` | 生成代码结构无模块级降级 |

  定向 pytest：**4 passed**。

**lightharness 侧（lh-T6，提交 `5d00889`）**：

- `examples/test_语言服务器.light`：`chr(10)` 绕行彻底解除（见第三节），模块 #63 定向运行通过。
- `docs/功能对标/语言缺陷账.md`：L-054 状态列回写 **「已修复（2026-09-04）」**（沿用 L-051 的 8 列样式），注明覆盖机制、实证证据、绕法已解除。

## 五、验证清单

| 项 | 命令/方式 | 结果 |
|----|-----------|------|
| light-merge 定向 pytest | `python -m pytest tests/unit/test_L054_nested_literal_newline.py -q`（wt-T6） | 4 passed |
| lightharness 定向运行 | `python 运行.py examples/test_语言服务器.light`（lh-T6，LIGHT_MERGE=wt-T6） | 模块63 通过 |
| 反跑判据① | 绕行换回直接 `\n` | 通过 |
| 反跑判据② | 破坏版 lexer（删 INDENT 发射） | 立即立红 |
| 同族机制 | L-051 在禁用 bracket 抑制下 | 立即立红 |
| 行尾/编码 | 新文件 CRLF 无 BOM（与仓库一致） | 通过 |
| 临时文件 | `_taskT6_*` 全部清理，`_taskT6_hist0902` worktree 已移除 | 通过 |

**验证范围说明**：遵守协作红线，仅跑定向测试（未跑全量 pytest）。覆盖范围为 L-054 及其同族 L-051 场景；未覆盖其它编译路径。

## 六、交付

- light-merge 分支 `task-T6`：提交 `78a3e5d3`（新增回归哨兵 `tests/unit/test_L054_nested_literal_newline.py`）。
- lightharness 分支 `task-T6-lh`：提交 `5d00889`（缺陷账回写 + `test_语言服务器.light` 绕行解除）。
- 本报告提交于 lightharness 分支 `task-T6-lh`（worktree `lh-T6`）。
